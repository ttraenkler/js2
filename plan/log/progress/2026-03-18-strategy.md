# Project Strategy — 2026-03-18

> **Superseded (2026-05-31).** The commercial-revenue framing below (commercial
> licenses, enterprise tiers, hosted compile API) reflects early exploratory
> thinking and no longer represents the project's direction. js2wasm is a
> fully-open technical foundation for the next-generation internet, deliberately
> not run as a commercial product; all source is released under Apache 2.0 with
> LLVM Exceptions. Retained as historical record only.

## Current state

- **490 issues completed** in 8 days (613 commits)
- **5,751 / 22,865 test262 tests pass** (25.1%)
- 5 issues remaining in ready, 1 blocked
- Compiler handles: classes, closures, generics, async/await, destructuring, Map/Set, Promises, RegExp, BigInt, template literals, optional chaining, spread/rest, try/catch
- React scheduler compiles to Wasm (15/15 tests)
- Playground with Monaco editor, treemap visualizer, test262 browser

## Unique value proposition

**ts2wasm is the only compiler that turns TypeScript directly into tiny, sandboxed WebAssembly modules with no runtime, no GC, and no JS engine dependency.**

| | ts2wasm | AssemblyScript | Cheerp | Emscripten |
|---|---|---|---|---|
| Input language | TypeScript | AssemblyScript (TS-like) | C/C++ | C/C++ |
| Output size | 100B – 10KB | 10KB – 100KB | 100KB+ | 100KB+ |
| Runtime bundled | No (WasmGC) | Yes (linear memory GC) | Yes | Yes |
| JS engine needed | No | No | Yes (for JS interop) | Yes |
| Sandbox isolation | Full Wasm sandbox | Full Wasm sandbox | Partial | Partial |
| Existing TS code | Compatible subset | Requires rewrite | N/A | N/A |

### Three differentiators

1. **Security**: Run untrusted TypeScript in-process without V8 isolates. Each module is sandboxed — no access to filesystem, network, or globals unless explicitly imported.

2. **Size**: No runtime, no allocator, no stdlib. A fib function is ~200 bytes. A DOM app is ~1KB. This makes ESM-style micro-modules practical in Wasm.

3. **Portability**: Run TypeScript in any Wasm host — wasmtime, wasmer, wazero, browser, Cloudflare Workers — without a JS engine.

## Target markets

### 1. Edge computing platforms (Cloudflare, Deno, Vercel)
**Problem**: Running user TypeScript at the edge requires V8 isolates (heavy, slow cold start).
**ts2wasm solves**: Compile to tiny Wasm modules, instant cold start, full sandbox.
**Revenue**: Commercial license for embedding ts2wasm in edge runtimes.

### 2. Plugin/extension systems (SaaS, no-code, game modding)
**Problem**: Letting users write custom logic safely. Current options: sandboxed JS (QuickJS), Lua, or WASI.
**ts2wasm solves**: Users write TypeScript (familiar), it compiles to sandboxed Wasm (safe). No new language to learn.
**Revenue**: Commercial license, or hosted compile API.

### 3. Non-JS backends (Go, Rust, C++ services)
**Problem**: Want to embed TypeScript business logic without bundling a JS engine.
**ts2wasm solves**: Compile TS to Wasm, load in wasmtime/wasmer. No V8 dependency.
**Revenue**: Open source adoption → consulting/support contracts.

### 4. IoT / embedded
**Problem**: Run logic on devices with <1MB RAM, no JS engine.
**ts2wasm solves**: Tiny Wasm modules run on Wasm3, WAMR, or similar lightweight runtimes.
**Revenue**: Long-tail, mostly open source adoption.

## Roadmap to first release

### Phase 0 — Showcase: ts2wasm-jwt (PRIORITY)
- #508: Pure Wasm JWT decode + HS256 verify
- Zero host dependencies, runs on Fastly/wasmtime/wasmer
- All compiler patterns verified — no blockers
- Demonstrates the value proposition: 5KB Wasm vs 300KB JS
- **Target: working JWT library compiled to Wasm, benchmarked vs jsonwebtoken**

### Phase 1 — Conformance & stability (→ 40% pass rate)
- Complete #501 (baseline run)
- #502 skip filter cleanup (+1,160 tests)
- #481 Symbol.iterator (+1,327 tests)
- #488 hasOwnProperty (+1,617 tests)
- #489 .call/.apply (+822 tests)
- Fix top CE patterns (#409, #411, #444)
- Fix top fail patterns (#417 wrong return values)
- **Target: 40% pass rate, ~9,000 tests passing**

### Phase 2 — Real-world validation
- Compile 5 target programs:
  1. React scheduler (already done ✓)
  2. A data transform pipeline (lodash-style)
  3. A simple HTTP request handler
  4. A game loop with math
  5. A JSON schema validator
- Fix any compilation failures found
- **Target: 5/5 programs compile and produce correct output**

### Phase 3 — Distribution & presentation
- #504 Auto-generated README tables
- #505 Playground test262 integration
- npm publish (`ts2wasm` package)
- Deploy playground to public URL
- Blog post / landing page
- **Target: usable by external developers**

### Phase 4 — Advanced features
- #481-#487 Full Symbol support
- #496 eval via host compilation
- #498 Proxy with trap inlining
- #490 Function .name
- Performance optimization (compile speed, output size)
- **Target: 50-60% pass rate, covers most real TypeScript patterns**

## Monetization strategy

### Open core model
- **Open source**: compiler, CLI, playground — Apache 2.0 with LLVM Exceptions
- **Commercial**: embedding license for companies integrating ts2wasm into their products (edge runtimes, SaaS platforms, game engines)
- **Hosted service**: compile API (POST TypeScript, GET Wasm) — usage-based pricing

### Pricing (hypothetical)
- **Free**: open source, self-hosted
- **Indie**: $29/mo — hosted compile API, 10k compilations/mo
- **Team**: $199/mo — priority support, custom features
- **Enterprise**: custom — embedding license, SLA, dedicated support

## What's missing from planning

1. **Milestone dates** — no "40% by when?" target
2. **Real-world test programs** — only React scheduler so far
3. **User documentation** — no getting started guide, no feature reference
4. **Distribution pipeline** — no npm publish, no CI/CD, no release process
5. **Community** — no Discord/forum, no contributor guide, no issue templates for external contributors

## Risks

1. **Diminishing returns** — each additional pass requires more work. The curve is flattening.
2. **WasmGC adoption** — still relatively new. Not all hosts support it yet.
3. **TypeScript evolution** — new TS features (decorators, import attributes) need ongoing support.
4. **Single developer** — bus factor of 1 (+ AI agents). Need to build community.
5. **Competitive moat** — if a well-funded team decides to build a similar compiler, they could catch up quickly. The moat is execution speed and early mover advantage.
