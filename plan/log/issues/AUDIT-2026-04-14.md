# Historical Issue Audit — 2026-04-14

## Scope

This audit pass reviewed issue metadata consistency across:

- issue markdown files under `plan/issues/`
- sprint markdown under `plan/sprints/`
- git history for issue file creation / movement
- targeted codebase and commit verification for ambiguous recent issues

The current schema target is documented in `plan/issues/SCHEMA.md`.

## Safe corrections already applied in this pass

- Normalized stale status aliases:
  - `plan/issues/backlog/1007.md` → `status: in-progress`
  - `plan/issues/backlog/1116.md` → `status: ready`
- Corrected stale done/open records with strong commit + code evidence:
  - `plan/issues/sprints/42/1036.md`
  - `plan/issues/sprints/42/1038.md`
  - `plan/issues/sprints/42/1047.md`
- Added missing frontmatter to early historical wont-fix issues:
  - `plan/issues/sprints/1/123.md`
  - `plan/issues/sprints/1/124.md`
  - `plan/issues/sprints/1/125.md`
  - `plan/issues/sprints/1/129.md`

## Audit tool state

`scripts/normalize-issue-frontmatter.mjs` is now conservative:

- excludes known non-issue artifacts under `plan/issues/`
- preserves historical sprint labels
- derives `created` / `updated` / `completed` from git history
- normalizes only defendable aliases (`bug` → `bugfix`, `enhancement` → `feature`, etc.)
- reports missing fields and duplicate ids without guessing subsystem/feature tags

It is not ready for corpus-wide `--write` because classification coverage still
needs manual review.

## Current audit summary

From `node scripts/normalize-issue-frontmatter.mjs` on 2026-04-14:

- audited issue files: `1100`
- missing sprint: `0`
- missing created: `0`
- missing completed for done/wont-fix: `0`
- missing task_type: `1022`
- missing area: `1100`
- missing language_feature: `1039`
- non-canonical status: `0`
- folder/status mismatch: `0`
- missing git history: `0`

## Resolved duplicate issue numbers

The duplicate-number clusters identified in this audit were renumbered so each
issue record now has a unique issue id.

Renumbered duplicate clusters:

- `#1110` / `#1111` split from later reintroductions of `#123`
- `#1112` split from the later reintroduction of `#124`
- `#1113` split from the later reintroduction of `#125`
- `#1114` split from the later reintroduction of `#129`
- `#1115` split from the closure-specific `#512` follow-up
- `#1116`, `#1117`, `#1118`, `#1119` split from later reopened planning duplicates of `#855`, `#856`, `#858`, and `#973`

Notes:

- Some duplicates are true reopenings / rescopes (`#855`, `#973`).
- Some are historical phase splits or superseded variants (`#512`).
- Some early issues were reintroduced after an earlier wont-fix decision (`#123`,
  `#124`, `#125`, `#129`).
- Suffixed ids like `797a` are treated as distinct records and are no longer
  collapsed into `797`.

## Next steps

1. Fill in `task_type`, `area`, and `language_feature` conservatively in
   verified batches.
2. Audit historical references to the renumbered ids and only update active
   planning links where the newer split record is the intended target.
3. After the metadata correction phase is complete, regenerate sprint issue
   tables from corrected issue frontmatter.
