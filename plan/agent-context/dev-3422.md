# Context summary — dev-3422 session (2026-07-20)

Senior-dev session. Landed #3422 (313 host flips) + two well-scoped hand-off
issues, and verify-first-debunked four mis-framed triage candidates. This file
exists mainly to STOP the next dev re-attempting the four mirages off
dev-3420's `host-fail-triage-2026-07-20.md`.

## 1. The four debunked mirages — DO NOT re-attempt as framed (each ≈0 real flips)

All four were framed as "same bare-string→`buildThrowJsErrorInstrs` fix as
#3422." They are NOT. Measured, not inferred.

- **#14 bigint/symbol coercion (triage #14, ~164)** — NOT a throw-class bug. The
  arithmetic `+`/coercion operators ALREADY throw real `instanceof TypeError`
  (probed `Symbol()+1`, `1n+1` → real TE). The real root is value-rep/ToPrimitive
  fidelity (see #3481). Throw-class fix flips nothing.

- **#11 early-errors (triage #11, "~150")** — the triage's own note is right:
  ~118 of ~150 are `eval-code` (documented SKIP feature). Real addressable ≈32,
  scattered across statements/expressions/module parser early-error detection.
  Not a concentrated win.

- **Runner bundle #13/#16/#17 (triage, "~416 fail→PASS at 0 risk")** — this is
  the biggest trap. It is fail→**SKIP**, %-neutral (like Temporal #1), NOT
  fail→PASS. Verified: #13 (183 "assert is not defined") is `eval('assert…')` =
  eval-SCOPE, not harness-ordering; #16 (122 "Signature declarations…TS-only") is
  all Atomics/SharedArrayBuffer (fixing the parse-reject gives CE→fail, needs
  skip-features); #17 is only 47 exact, all dynamic-import (skip). These are
  skip-feature tests the oracle stopped skipping → the correct lever is restoring
  skip filters (bundle with Temporal #1), which is Lane-A/skip-policy denominator
  work, not a codegen fail→PASS win.

- **#728 null-receiver TypeError (property-access.ts:1015)** — the bare-string
  bug IS real (`null.foo` → caught-but-not-`instanceof TypeError`), BUT it flips
  ≈0. MEASURED: applied the `buildThrowJsErrorInstrs` fix, rebuilt, ran 40
  genuine-subset files (excluded resizable-buffer/Temporal/Proxy/realm) →
  **0/40 flipped** (0 CE). The 486 baseline fails carrying "Cannot access
  property on null or undefined" are overwhelmingly UNEXPECTED-null codegen bugs
  (destructuring/arguments/super-spread with `at LINE:COL`, + resizable-buffer
  #1350). Fixing the error CLASS doesn't help — the null shouldn't be there. If
  anyone still wants the property-access.ts:1015 fix for correctness, note it has
  16 call sites (not 4 like #3422) and needs fctx threaded for the flush.

**General lesson**: for "throw-class" triage rows, split escaped-raw (unexpected)
vs assert.throws-mismatch (expected-throw) BEFORE committing. Only the latter
flips with a real-instance throw. Grep the baseline jsonl; if the signature is
the raw runtime message with `at LINE:COL`, it's an unexpected null/coercion.

## 2. #3481 — bigint/symbol coercion value-rep epic (hand-off, PR #3436)

`plan/issues/3481-bigint-symbol-coercion-value-rep.md`. NOT throw-class. Two sub-families rooted in value
substrate ToPrimitive/ToNumeric:
- A. wrong-coercion (~106): we throw a CORRECT TypeError but shouldn't throw —
  e.g. `Object(2n)*2n` doesn't ToPrimitive-unwrap the BigInt wrapper (want `4n`);
  Symbol-keyed access coerced to number.
- B. missing-throw (~26): `@@toPrimitive` returning Symbol/BigInt not re-validated
  on ToString/ToInteger/ToIndex (~16 tangled with A); ~10 isolated (`1n>>>1n`,
  `toFixed` coercion-order RangeError-before-TypeError, `sort` comparefn, species,
  `String.fromCharCode(1n)`).
Overlaps toPrimitive-nominal-struct epic → needs architect spec. Runtime
`__extern_to_string_default` already re-checks Symbol (runtime.ts ~8722-8736);
the inlined codegen coercions (string-ops.ts `$__any_to_string`) don't.

## 3. #3484 — Iterator Helpers host lane (feature, PR #3437, XL)

`plan/issues/3484-iterator-helpers-host-lane.md`. Native helpers EXIST but standalone-only
(`closed-method-dispatch.ts:243/253` `ctx.standalone` gate); `Iterator` global
undefined (`identifiers.ts:1221` null-fallback). Host-reflection shortcut RULED
OUT (WasmGC/$Object iterators can't be driven by Node's native
`Iterator.prototype.take` via `.call`). 3 slices: (1) un-gate native helpers to
host — reuse `ensureNativeIterHof`/`ensureNativeLazyIter`, flips method-call-form
majority; (2) `Iterator` global + prototype as first-class fn values; (3) generic
`.call`/GetIteratorDirect + spec edges (RangeError, IteratorClose). ~84 host
fails. XL — start early in a budget window.

## 4. Session deliverables + PR states
- **#3422 MERGED** (00:48Z): `typeof-delete.ts` delete-refusal throws real
  TypeError instance. 313 flips, zero regressions. `tests/issue-3422.test.ts`.
- **#3436** (#3481 value-rep hand-off): plan-only, CLEAN, will auto-enqueue.
- **#3437** (#3484 Iterator feature): plan-only, filed.
- Pattern reused from #3471 (read-only-assign sibling, already merged) + #3470
  (name/length realm-restore).
