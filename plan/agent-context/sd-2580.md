# sd-2580 — context note (2026-06-25)

Senior-dev on the #2580 value-rep spine (s66). This note lets a future session
resume without re-deriving the analysis.

## Landed this session
- **#2580 M3 B-protoextend — MERGED (PR #2049, +2/0-reg).** Inherited
  `Object.prototype[idx]` index read in the generic array-method loop.
  `_protoIndexHas`/`_protoIndexGet` in `src/runtime.ts` consult
  `%Object.prototype%[idx]` as the FINAL fallback in `__extern_get_idx` /
  `__extern_has_idx` (after own fields + sidecar + own accessors miss). Routes
  inherited reads through the ONE shared Object.prototype walk (architect ii-a),
  no per-receiver `$proto` field, hot path byte-identical. Test:
  `tests/issue-2580-m3-protoextend.test.ts`.
- **#2580 B-fnctor — HELD (docs PR #2051).** Verify-first re-ground re-confirmed
  (3rd independent session) that the fnctor `new F()` lap has no safe one-pass
  slice. The contained host-mode slice (wire index helpers to `_fnctorProtoLookup`)
  banks 0 rows — the `new Con()` instance never reaches `__extern_get_idx`
  (`some.call(child)` doesn't treat the bespoke `$__fnctor_Con` struct as
  array-like). The real fix is escape-analysis-gated instance reconstruction = the
  #1888-floor eject risk. STOPPED per guardrails, reverted, escalated.
- **#2660 — FILED (architect spec).** Whole-program escape/dynamic-use gate for
  reconstructing `new F()` instances as `$Object`. General value-rep infra (not
  just B-fnctor). Full `## Implementation Plan` in
  `plan/issues/2660-fnctor-instance-dynamic-use-escape-gate.md`: gate predicate
  (A∧B), pass placement vs the IR `analyzeEscape`, `$Object` reconstruction +
  `_fnctorProtoLookup`/#1712 interaction, the #1888-safety argument, S1–S4 slices,
  broader applicability.

## State of #2580
- `status: in-progress`. Laps done: M0, M1 (host arm), M2/M2.2b, B-pre (no-op +
  `__is_truthy` desync fix), B-acc (+35), B-protoextend (+2). Remaining: **B-fnctor
  (LAST lap), BLOCKED on #2660 infra (S1–S3) landing.**

## Hard-won lessons (do not relearn)
- **Runner trap is REAL and bites hard.** Measure test262 deltas ONLY
  single-process / one-fresh-process-per-file. In-process multi-file runs AND
  `-P N` parallel `tsx` scans leak host `Object.prototype[i]=v` mutations + shared
  compile state between files → false `compile_error`s (the "declaredType on
  boolean" / "mapperCache.get" phantoms — both are pollution artifacts, NOT real
  bugs; banked as team memory `reference_test262_inprocess_objproto_pollution`).
  Driver pattern that works: `npx tsx .tmp/run-one.mts <file>` (one file, exits).
- **My harness isolation worktree** is `/workspace/.claude/worktrees/agent-<id>/`
  (NOT a manually-created `issue-*` worktree — that gets a write-block). Rename the
  branch in-place to the issue branch; do edits here.
- B-fnctor blocker mechanism (re-verified): `Con.prototype = {…}` write lands on
  the `$6` closure trampoline struct (`ref.test $Object` misses); `new Con()` is a
  bespoke `$__fnctor_Con` struct, not an `$Object`. Both need the #2660 gate.

## Resume pointer
Next high-value step on this lane = **build #2660 S1 (the inert analysis)**, then
S2/S3, then #2580 S4 (B-fnctor lands). All require an architect review of the
#2660 spec first. Validate everything through the merge_group standalone floor
(#2097), never a scoped sweep; stop-the-line on any #1888 eject.
