# dev-1222 — Sprint 47 Handoff

**Last session**: 2026-05-02 (sprint 47 close).
**Final status**: All assigned tasks completed. Shutdown approved by team-lead.

## Sprint 47 work completed

| Task | Issue | PR | Outcome |
|------|-------|-----|---------|
| #20 | #1229 — eval/RegExp LRU cache + peephole | merged earlier | ✅ |
| #25 | #1237 — playground horizontal swipe | PR#152 | ✅ |
| #26 | #1236 — premature i32 specialization | PR#151 | ✅ admin-merge (drift) |
| #27 | #1244 — Hono Tier 1 stress test | PR#153 | ✅ admin-merge (drift) |
| #31 | #1248 — substring(start) typeof guard | merged in #1244 PR | ✅ |
| #38 | #1258 — for-of dstr boxedCaptures routing | PR#165 | ✅ admin-merge (+33 wasm-real) |
| #39 | #1260 — destructuring null/undefined TypeError | PR#167 | ✅ admin-merge (drift, all 32 wasm-identical) |
| #51 | #1278 — lodash-tier1 stress test refresh | PR#169 | ✅ basic CI green, merged 16:02Z |

## Working state at shutdown
- Worktree `/workspace/.claude/worktrees/issue-1278-lodash-tier1-update` — PR merged, safe to clean.
- Worktree `/tmp/wt-167-base` — temp base for PR#167 hash-compare; safe to remove.
- All branches (`issue-1258-...`, `issue-1260-...`, `issue-1278-...`) merged into main.

## Patterns to remember next session

1. **Drift detection (CI noise filter)**: For PRs flagged with `regressions_wasm_change > 0`, run sha256 hash-compare on PR worktree vs origin/main worktree before assuming real regression. Procedure validated 3× this sprint:
   - Create base worktree at `/tmp/wt-{n}-base` on origin/main.
   - Symlink `node_modules` from /workspace.
   - Run identical `parseMeta → wrapTest → compile (skipSemanticDiagnostics)` pipeline on both.
   - Compare `sha256[0..12]`. SAME = drift / CI noise; DIFF = real regression.
   - PR#167: 32 reported regressions, 32 wasm-identical (100% drift).

2. **dev-self-merge ESCALATE pattern**: `net_per_test < 0` with high `compile_timeouts` (`regressions == regressions_wasm_change + compile_timeouts`) is the classic timing-noise signal. Compute filtered net = `improvements - regressions_wasm_change` and report. Tech-lead has authorized admin-merge on this pattern multiple times (#1234, #1236, #1244, #1258, #1260).

3. **CI-wait protocol** (per developer.md updated mid-sprint): use a single `Bash run_in_background` until-loop on `.claude/ci-status/pr-N.json` matching the full HEAD SHA. Do **not** call Monitor afterward. Do **not** send idle messages while waiting. System fires one notification on loop exit.

4. **Test-only PRs** (zero `src/**` files): test262 not required; dev-self-merge fast-path checks basic CI via `gh pr view`. PR#169 used this path successfully.

5. **Issue file conflicts on merge**: planning artifacts in `plan/`, `dashboard/`, `public/` — resolve with `git checkout --theirs <file>`. Compiler source conflicts in `src/**/*.ts` — escalate as `[CONFLICT]` task to senior-developer; do not resolve inline.

## Deferred follow-ups identified

From #1258 (for-of dstr): destructure-defaults on property targets (`[x.y = 10]`) — silently drops default+value.

From #1260 (dstr null/undefined):
- `emitNestedBindingDefault` ref/ref_null path treats `ref.is_null` as both null AND undefined trigger; spec says default fires only on undefined. Affects ~5 gen-meth test262 cases (`expressions/class/dstr/gen-meth-obj-ptrn-prop-obj-value-null.js` etc). Requires runtime null vs undefined sentinel distinction.
- For-of typed-struct path (`loops.ts:1181-1230`) doesn't dispatch nested array/object property targets in `for ({x: [x]} of [...])`. Affects ~2 for-of test262 cases.

From #1278: `lodash-es/clamp.js` and `add.js` validate as Wasm but fail at instantiation with "undefined" error (likely missing import). HOF closure pattern in `add.js` tracked as #1276.
