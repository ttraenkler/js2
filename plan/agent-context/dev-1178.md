# dev-1178 context summary

**Agent**: developer
**Sprint**: 45
**Last active**: 2026-04-27 (08:15 CEST)
**Status**: terminated by team-lead after PR #64 merged

## Issue assigned
#1178 — `string-hash` benchmark hits `wasm trap: call stack exhausted` at runtime after the #1175 fix.

## What I did

Resumed work in an existing worktree at `/workspace/.claude/worktrees/issues-1178-1179` on branch
`issue-1178-fix-string-concat-stack-overflow`. The iterative rewrite of `__str_copy_tree` was already
in place from a prior session (~191 insertions in `src/codegen/native-strings.ts`).

1. Verified `tsc --noEmit` clean.
2. Wrote `tests/issue-1178.test.ts` (3 cases) covering:
   - 50K iterations of `text += "x"` followed by `text.charAt(0)` (forces flatten)
   - 50K iterations of `text += alphabet.charAt(...)` mixed with `;`
   - 60K iterations of `text += "a"` followed by `charCodeAt(0)`
3. Manual repro on the labs `string-hash.js` benchmark: `run(20000)` no longer traps;
   the concat phase finishes in ~120ms.
4. Documented `## Implementation` and `## Test Results` in `plan/issues/sprints/45/1178.md`.
5. Committed `11fe73979` (fix + test + issue file), merged `origin/main`, pushed branch,
   opened PR #64.

## CI feedback and investigation

- CI reported `net_per_test: +3` (149 improvements vs 146 regressions).
- Tech lead initially flagged the regressions as real and asked me to investigate the iterative
  flatten for correctness bugs.
- I wrote 4 char-by-char correctness probes (`.tmp/correctness-1178.mts`): all matched expected
  values exactly, confirming the iterative descent is functionally equivalent to the recursive
  version.
- I sampled 6 of the supposedly-regressed tests (destructuring + Promise patterns) on
  `origin/main` with the recursive helper — every one reproduced the same failure on both
  branches. The "regressions" were not caused by my fix.
- I traced the `[object WebAssembly.Exception]` cascade to a pre-existing test
  (`test/language/module-code/export-expname-binding-index.js`) that throws a wasm exception
  during runtime, contaminating the test262 worker's incremental compiler state for subsequent
  tests. The same cascade is visible in the baseline JSONL.
- Tech lead later confirmed: native-strings fix is gated on `ctx.nativeStrings`, which test262
  doesn't enable, so it cannot affect those tests. The CI noise was drift.

## Outcome

- PR #64 merged by the project lead at commit `b7de382aa` (2026-04-27 08:10 CEST).
- Fix is on `main`. Issue file moved to `done/` per merge.
- All issue-specific tests (`issue-1178.test.ts`, `issue-1175.test.ts`) pass; `tsc --noEmit`
  clean; equivalence string-method failures match `origin/main` baseline (pre-existing).

## Possible follow-up (not in scope for #1178)

The new worklist is sized by `node.len` per flatten call. For very long ConsString ropes, the
allocation is generous (depth ≤ leaves ≤ chars). A future perf optimization could grow the
worklist dynamically, use a small bounded stack with depth fallback, or share a per-context
worklist buffer. Orthogonal to the stack-overflow fix.

## Worktree state at shutdown

- Path: `/workspace/.claude/worktrees/issues-1178-1179`
- Branch: `issue-1178-fix-string-concat-stack-overflow`
- HEAD: `85acab42a` (merged into main as `b7de382aa`)
- Working tree clean except for upstream-introduced untracked files in `.claude/ci-status/`
  (`pr-65.json`, `pr-66.json`, `pr-67.json`) — those are not mine, left as-is.
- `.tmp/` contains the diagnostic scripts (`correctness-1178.mts`, `repro2-1178.mts`,
  `incr-repro.mts`, `find-real-regressions.mts`) — gitignored, useful as reference if the
  perf follow-up is picked up.
