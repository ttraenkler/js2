# dev-1035 — Session Context Summary

**Session span**: 2026-04-26, single session (~1 hour)
**Branch worked**: `issue-1035-wasi-fs-writefilesync`
**Worktree**: `/workspace/.claude/worktrees/issue-1035`
**PR**: [#42](https://github.com/loopdive/js2/pull/42) — **MERGED** (merge commit `bec52def2`)
**Shutdown trigger**: tech lead — task queue empty after merge

## Work delivered

### #1035 — WASI hello-world (console.log + node:fs writeFileSync) → MERGED (PR #42)

**Status when I picked it up**: Compiler implementation already on main from commit `50a8caba3` (`feat(wasi): map node:fs writeFileSync to WASI path_open + fd_write + fd_close`). Issue file still in `plan/issues/sprints/45/`, README missing from `examples/wasi/`.

**Smoke-test result (before doing any work)**:

- `tests/issue-1035.test.ts`: 5/5 pass on the existing implementation
- Manual end-to-end: `npx js2wasm examples/wasi/hello-fs.ts --target wasi -o /tmp/wasi-out` → run via Node `node:wasi` → `hello world` on stdout + `/tmp/wasi-test-1035/hello.txt` contains `hello world\n` ✓
- Stretch: same source compiles in JS-host mode (without `--target wasi`) ✓

**What I added** (commit `acee9642e`, the only commit on the branch):

1. **`examples/wasi/README.md`** (new, 226 lines) — explains the three native-executable wrapping approaches required by the acceptance criteria:
   - `wasmer create-exe` (recommended — single self-contained binary, cross-platform, no runtime install)
   - `wasmtime compile` (precompiled `.cwasm`, fast startup, requires wasmtime installed)
   - wazero embedded in Go (pure Go, statically linked, slowest of the three)
   Plus the dual-target story (same source compiles in WASI and JS-host modes).
2. **`plan/issues/done/1035.md`** — moved from `plan/issues/sprints/45/`, status `ready` → `done`, appended:
   - `## Implementation summary` — describes both the compiler-core commit (50a8caba3) and the docs commit
   - `## Test Results` — 5/5 issue tests pass + manual end-to-end pass
   - `## Acceptance criteria` — all 7 items checked off
3. **`plan/issues/backlog/backlog.md`** — issue row updated `Ready` → `Done`, link points to `done/1035.md`
4. **`plan/log/dependency-graph.md`** — graph annotation `independent but related` → `DONE (sprint 45)`

## Key findings / discoveries

1. **The CLI's `-o` flag is an output directory, not a filename.** The issue spec showed `-o hello-fs.wasm` but that breaks (treats it as a directory and writes inside). README documents the correct `-o <dir>` form.

2. **The compiler's WASI scaffolding for `writeFileSync` is solid and complete:**
   - `src/compiler.ts:detectNodeFsImports` scans the original source for named `node:fs`/`fs` imports BEFORE `preprocessImports` strips them, so the codegen knows what to wire up.
   - `src/codegen/index.ts:registerWasiImports` only adds `path_open`/`fd_close` imports when `writeFileSync` is in `wasiNodeFsFuncs` — `console.log`-only programs stay minimal.
   - `src/codegen/index.ts:emitWasiWriteFileSyncHelper` generates `__wasi_write_file_sync(pathPtr, pathLen, dataPtr, dataLen)` directly in Wasm: hardcoded `dirfd=3` (first preopen), `oflags=O_CREAT|O_TRUNC=9`, `rights_base=FD_WRITE=64`. iovec at `memory[0..7]`, nwritten at `memory[8]`, opened-fd output at `memory[12]`.
   - `src/codegen/expressions/calls.ts` rewrites `writeFileSync(path, data)` calls in WASI mode: string-literal args go through `wasiAllocStringData` (compile-time data segment, deduped by offset), `const x = '...'` references via `compileWasiStringArgToLinearMemory`. Fully dynamic strings still emit `unreachable` (TODO: runtime GC-string → linear-memory encoder).

3. **Local `wasmtime`/`wasmer` binaries are not installed in the dev container.** All end-to-end verification was done via Node's built-in `node:wasi`. The test in `tests/issue-1035.test.ts` uses `node:wasi` with a `/tmp/wasi-test-1035` preopen.

## Sprint 45 delta contributed

- PR #42 (#1035): docs + issue closure (no compiler changes in this PR — compiler change was 50a8caba3, already on main)

## Handoff notes / known deferred items

These are in `examples/wasi/README.md` and `plan/issues/done/1035.md`'s acceptance section — listing here for the next dev:

1. **Hardcoded `dirfd = 3`** matches `wasmtime --dir=.` convention (first preopen). A proper preopen-table walk via `fd_prestat_get` / `fd_prestat_dir_name` is tracked as **#1041**.

2. **Only `writeFileSync` is wired** for `node:fs`. Follow-ups already in the backlog/blocked queue:
   - **#1036** — `readFileSync` → `path_open` + `fd_read` + `fd_close`
   - **#1037** — `existsSync`/`statSync` → `path_filestat_get`
   - **#1038** — `mkdirSync` → `path_create_directory`
   - **#1039** — `unlinkSync` → `path_unlink_file`
   - **#1040** — `readdirSync` → `fd_readdir`
   - **#1042** — `node:fs/promises` async wrappers
   The same `wasiNodeFsFuncs` detection path + `emitWasi*Helper` pattern works for all of these — just register the corresponding WASI import and emit the helper in `registerWasiImports`, then add a call-site rewrite in `src/codegen/expressions/calls.ts`. No new infrastructure needed.

3. **Dynamic strings in `writeFileSync`** still trap (`unreachable`). For the next iteration: implement a runtime GC-string → linear-memory copy helper (encode UTF-8, bump-allocate, return ptr+len). Track in a follow-up if any user hits it.

4. **Native-executable wrapping is documented but not demonstrated end-to-end.** None of `wasmer`, `wasmtime`, or Go is installed in the dev container, so the README's wrapping commands are unverified locally. They're standard documented invocations, but a follow-up could add a CI matrix that builds the .wasm and feeds it through one of these toolchains.

## State at shutdown

- Branch `issue-1035-wasi-fs-writefilesync` exists on origin, merged via PR #42.
- Worktree `/workspace/.claude/worktrees/issue-1035` still on disk (clean working tree, no uncommitted changes). Tech lead can prune or reuse.
- Issue file at `plan/issues/done/1035.md` with `status: done`, full implementation summary, test results, and all acceptance boxes checked.
