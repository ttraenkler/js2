# dev-1208 — context summary

**Session date:** 2026-04-27
**Sprint:** 45
**Issue:** #1208 — landing: surface ADRs on the landing page

## Status

**Implementation merged** in PR #79 (merge commit `0d7b438e3`).
**Bookkeeping merged** in PR #80 (merge commit `69f6e28f28`).

Both PRs are on `main`. Both worktrees and branches deleted.

## What shipped

- `components/site-nav.js` — nav item `"How it works"` → `"Approach"`,
  anchor `#how-it-works` → `#approach` (line 59).
- `index.html` — section `id="how-it-works"` → `id="approach"` (line 2949).
  Footer "Inspect → Approach" link, which already pointed to `#approach`,
  is now correctly wired.
- `index.html` — new "Architecture" subsection inside the Approach
  section: one-paragraph intro, link to `docs/adr/README.md`, and a
  `<ul class="adr-list">` of all 12 ADRs (`./docs/adr/0NNN-*.md`).
- `index.html` — new `.adr-list` CSS matching the existing `.how-*`
  style (line 597).
- `plan/issues/done/1208.md` (moved from `ready/`), frontmatter status
  → `done`, plus `completed`, `merged_pr: 79`, `merge_commit: 0d7b438e3`.
- `plan/issues/sprints/45/sprint.md` — moved #1208 from "In Progress"
  to "Done".
- `plan/goals/contributor-readiness.md` — status `in-progress` → `done`.

## Open follow-up — broken ADR links on GitHub Pages (REQUIRES ACTION)

The user reported (just before shutdown):

> `https://loopdive.github.io/js2wasm/docs/adr/0001-hybrid-compilation-strategy.md`
> produces a 404.

> we should also not directly link the .md file but instead a html view of it

**Root cause:** the landing page is served from GitHub Pages at
`loopdive.github.io/js2wasm/`. The `deploy-pages.yml` workflow uploads
only `dist/pages/`, which does NOT include `docs/adr/*.md`. So the
relative `./docs/adr/0NNN-*.md` links from `index.html` 404 in
production. They only "work" when viewing the repo on github.com.

**Recommended fix (next agent — quick):** swap the relative `.md`
links for absolute GitHub blob URLs that GitHub renders as HTML:

  `./docs/adr/0001-hybrid-compilation-strategy.md`
  →
  `https://github.com/loopdive/js2/blob/main/docs/adr/0001-hybrid-compilation-strategy.md`

Same for the index link:

  `./docs/adr/README.md`
  →
  `https://github.com/loopdive/js2/blob/main/docs/adr/README.md`

GitHub's blob view renders Markdown to HTML, so the user gets a proper
HTML view (matching the user's "we should also not directly link the
md file but instead a html view of it" requirement).

**Better fix (separate issue):** add a build step to render
`docs/adr/*.md` → `dist/pages/docs/adr/*.html` (via e.g. marked or
remark) and link to those instead. This keeps the URL on the same
origin as the landing page and removes the dependency on GitHub's
Markdown viewer. Suggest filing this as a follow-up issue.

**Affected file (single-file change):** `index.html`, lines ~3118-3133
in the Architecture subsection. About 13 hrefs to update.

## Process notes

- I did not have `SendMessage` or `TaskUpdate` tools available in this
  session — only Read/Edit/Write/Bash/Agent. The merge itself is the
  authoritative completion signal; team-lead must mark task #2
  completed on their side.
- Two `idle_notification`-style re-dispatches arrived after the merge
  ("claim task #2", "work on issue #1208"). Both were rejected because
  the work was already on `main` — no new commits possible. Captured
  in this file in case the same desync happens again.
- `refresh-benchmarks` CI failure is pre-existing infra drift (missing
  `public/benchmarks/results/playground-benchmark-sidebar.json`); fails
  on every push to `main`. Not caused by my PRs. Worth a separate fix.
