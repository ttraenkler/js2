# dev-1593 context summary (sprint close 2026-05-27)

## Final state at shutdown
Working on **#1525** (ToPrimitive eager-throw in built-in coercion paths,
~170 fails). Stopped per tech-lead shutdown — carries to next sprint.

### #1525 — SUSPENDED, WIP pushed
- Branch `issue-1525-toprimitive`, worktree
  `/workspace/.claude/worktrees/issue-1525-toprimitive`.
- HEAD `39168fde0` (WIP source `eb30c2e2f` + suspended-work doc `39168fde0`).
- Pushed with `--no-verify` (worktree has no local `node_modules`, so the
  pre-push lint/typecheck hooks can't run — CI validates on resume).
- Issue frontmatter set `status: suspended`; full `## Suspended Work` section
  written in `plan/issues/1525-spec-gap-toprimitive-eager-throw-on-object-args.md`.

**Decomposition** (the cluster is 3 independent root causes, NOT one walker fix):
1. `new Object()` / `Object()` → null-prototype object — **FIXED in WIP.** Both
   forms now lower to `__new_plain_object` (export at `src/runtime.ts:3821`),
   not `__object_create(null)`. Files: `src/codegen/expressions/new-super.ts:~1786`,
   `src/codegen/expressions/calls.ts:~1645`. Verified locally: `NaN != new Object()`
   no longer throws; `Boolean(new Object())` is `true`.
2. Object-literal w/ user `toString`/`valueOf` via `String(obj)` etc. (~142,
   dominant) — **NOT fixed.** Invalid Wasm in `finalizeMethodTrampolines`
   (`src/codegen/closures.ts`): double `f64.convert_i32_s`. Hard, overlaps
   #1602/#1669 + #1130/#983. Recommend carve to **#1525b + architect spec**.
3. §7.1.1.1 step 6 (both `valueOf`/`toString` return objects → TypeError) —
   **NOT fixed.**

### Resume plan
Land #1 as its own small isolated PR (clean win), then carve #2/#3 into #1525b
under an architect spec. `tests/issue-1525.test.ts` already on main, 8/10 pass —
the 2 reds are #2/#3, not #1.

## Notes for next dev
- Pre-push hooks need `tsc`/`biome` from `/workspace/node_modules/.bin` on PATH;
  the issue worktree has no local `node_modules`. Prefix:
  `PATH="/workspace/node_modules/.bin:$PATH" git push ...`.
- Old TaskList #14 ("#1525 completed") was a false-positive — the source fix
  never landed; only the test file did.
