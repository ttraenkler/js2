---
sprint: 63
status: done
---

# Sprint 63 — routine wave: point fixes, mechanisms, mop-up (no Fable dependencies)

> Pre-staged 2026-06-12 during sprint-62 planning. Sprint 62 is the last
> Fable sprint; 63 is deliberately composed of work default-model devs
> handle well. Issues below already carry `sprint: 63` frontmatter.
> Re-validate at sprint start (some may be fixed by 62's architecture work
> — especially the coercion/value-rep symptom adjacents).

## ⭐ TOP PRIORITY EPIC (added 2026-06-15) — Proxy / Promise / async-await → 100%

> Stakeholder directive 2026-06-15: drive **Proxy, Promise, and async/await
> to 100% conformance in both JS-host and standalone modes** — top priority
> for sprint 63. These were previously parked in Backlog / earlier sprints;
> pulled forward here.

**Reality check (read before dispatching):** every issue below is
`feasibility: hard, reasoning_effort: max/high`, and the standalone async
issues form a **strict dependency chain** — they cannot all *complete* in one
sprint by parallel default-model devs. This contradicts 63's original "routine
wave, no Fable deps" framing. Treat this epic as **senior-dev + architect-led**
work running alongside the routine queue, not as point fixes. **All hard
issues need an architect `## Implementation Plan` before dev dispatch**
(workflow rule). Recommended: spec the chain heads first (#1936, #1100, #2180).

### Promise + async/await

**Host mode (the CPS-flip linchpin — sequential):**
- **#1936** (TOP) — Async contract migration: teach call sites to drive
  Promises. Census/spec started in s62 → impl in 63. **Chain head.**
- **#1796** (TOP) — Flip `ASYNC_CPS_ENABLED`; migrate sync-async contract to
  CPS Promise model. **Follows #1936.**
- **#2028** (already queued, high) — `new Promise(executor)` resolve/reject
  null-deref trap; fixes the executor pattern in host mode.

**Standalone mode (microtask + CPS — sequential chain, partly in-flight):**
- **#1326** (in-review, s58) — microtask queue + CPS scheduler, Phase 1B. *prereq*
- **#1326c** (in-progress, s52) — microtask queue + `Promise.then`
  chained-resolution, Phase 1C. *prereq, active now*
- **#1042** (in-progress, s58, high) — async/await state-machine lowering
  (CPS engine landed inert). *prereq, active now*
- **#1373b** (TOP, pulled from s52) — IR async Phase C: CPS lowering for
  `await` / async-return / async-throw. **Gated on #1326c.**

> Chain: `#1326 → #1326c → #1373b`, with `#1042` providing the state machine
> and `#1936 → #1796` flipping the host contract on top. Dispatch in order;
> do not parallelize the chain.

### Proxy

- **#2180** (TOP, **new** 2026-06-15) — Host-mode Proxy completion: close the
  remaining ~77% of `built-ins/Proxy` failures (invariants, Wasm-typed
  targets, revocation). The only host-mode Proxy tracker (#1466 done).
- **#1100** (TOP, pulled from Backlog) — Wasm-native Proxy meta-object
  protocol, standalone Phase 1 (get/set/has/apply + revocable). **Chain head
  for standalone Proxy.**
- **#1355** (TOP, pulled from Backlog) — Pure-Wasm Proxy: remaining 10 traps +
  invariant checks toward 100% (standalone). **Follows #1100.**

> Note: #1100 and #1355 acceptance criteria as written target ≥50% / ≥75% of
> traps respectively — re-spec to a 100% bar per the stakeholder directive, or
> file Phase 2 follow-ups for the residual.

## Queue (by family)

- **Point fixes moved from 61** (verify each still repros — 62's engine/
  value-rep work may have fixed several): #1994 #2001 #2007 #2008 #2011
  #2012 #2013 #2017 #2021 #2023 #2024 #2025 #2026 #2028 #2033 #2035 #2076
  #2077 #2083 #2118 #2119
- **Structural consolidations** (senior-grade; were 62-stretch): #2086
  (single implicit-derived-ctor synthesis — after #1965/#2101), #2087
  (capture-machinery unification), #2088 (per-builtin representation
  scaffold)
- **Mechanisms / CI**: #2093 (issue→probe coverage rule), #2094 (standalone
  import-leak budget), #2095 (baseline-validator standalone+fail rows),
  #2096 (oracle version stamp), #2097 (standalone pass high-water floor),
  #2098 (flake classification), #2099 (promote-baseline poison re-run),
  #2108 (coercion drift gate — after 62 seals the engine vocabulary),
  #2147 (reconciler flags ready-issues-with-merged-PRs)
- **Shared lowerings**: #2102 (throwJsError), #2103 (binding-info analysis)
- **Quality-review wave residuals** (added 2026-06-12 review): #1920
  (unify instruction walkers — codegen-side sibling of #1922), #1928
  (source-position remapping through pre-parse rewrites), #1929
  (CompileError flatten chains), #1933 (multi-instance isolation leak),
  #1935 (retire undefined sentinel protocol — coordinate with
  #2106/#2142's ownership map), #1943 (ratio/bucket thresholds in CI)
- **Unblocked by the 2026-06-12 review**: #742 (re-scoped: dispatch-table
  registry + #2088 scaffold migration; blocker #688 was long done), #1344
  (blocker #1665 done; re-validate repro first)
- **Backend symmetry / hygiene** (routine, sequenced): #2144 (linear fmod
  parity), #1848 (dead-code sweep + `codegen-linear/simd.ts`
  wire-or-delete), #1860 (backend naming → `src/codegen/{gc,linear}/`,
  AFTER in-flight linear PRs land), #1859 (per-subdir READMEs, after
  #1860), #2145 (generated ir-adoption table), #2146 (shared.ts
  registration indirection — after #1916 A2)
- **Inherited from 62 specs** (impl work, Fable specs already written):
  tag-5 ABI untangle impl (#2141), #1855 fuzzer impl, #2135 capability
  predicate staged impl, #1916 phase 1+ (if not landed as 62 stretch),
  #2107 (value-rep P4, if 62-stretch missed), #1925/#1926 (if 62-stretch
  missed)
- **Oracle**: #1945 step 2 + default flip (needs the 62 soak), with
  re-baseline + comms per the 07-proposal protocol. **The resulting
  publicly visible pass-rate drop was pre-approved by the stakeholder on
  2026-06-12** — proceed without re-asking; comms task still required.

## Notes

- #2109 (BigInt loose-eq) stays Backlog — gated on the i64-bigint-brand
  ValType decision (see `project_bigint_i64_brand_gate`).
- The 07-proposal's sprint-64 sketch (standalone conformance, identity
  registries, host-boundary marshaling wave) is unchanged and follows 63.
## Sprint Result (closed 2026-06-18)

**Sprint 63 closed: 74 issues done, 0 not-done; 34 carried to s64** (honest
done-or-carried close, s62 precedent).

Delivered the routine point-fix / mechanism / mop-up wave: standalone
conformance residuals across String/Number/Symbol/RegExp/Map-Set/TypedArray/
DataView/Error/Date/destructuring, the Ryū float port (#1537),
detectEarlyErrors decomposition (#1931), accessor-literal globals (#2011),
linear-backend soundness fixes, and many optional-chaining / value-rep slices.

**Carried to s64** — the irreducible tail that could not complete by parallel
default-model devs in one sprint:
- **Architecture specs** (need an `## Implementation Plan` first): #1899,
  #1919, #1926, #1927, #2001, #2087, #2146, #2181, #1344, #1373b, #1917,
  promise-async-capability-residual.
- **Epics** (multi-sprint): #1042 (async state machine), #1355 (Proxy),
  #1712 (acorn differential).
- **Max-effort cores**: #2009, #2106, #2151, #2159.
- **Umbrella remainders** (tractable slices landed; residual is architecture/
  max-effort): #2045, #2083, #2158, #2160, #2162, #2167, #2173, #1538, #1938,
  #742, #2026, #2029, #2051, #2161.
- **Blocked**: #2013.

Several carried umbrellas had in-flight slice PRs at close — some landed
(#1702/#1699/#1695); others (#1708/#1709/#1711/#1704) bounced to conflict/
regression and continue under s64. The umbrellas carry regardless.

<!-- GENERATED_ISSUE_TABLES_START -->
## Issue Tables

_Generated from issue files. Update issue `status`, then rerun `node scripts/sync-sprint-issue-tables.mjs`._

### In Progress

| Issue | Title | Priority | Status |
|---|---|---|---|
| #2026 | classes are not first-class values: new K() on a parameter throws 'No dependency provided for extern class', .constructor identity broken | medium | in-progress |

### Done

| Issue | Title | Priority | Status |
|---|---|---|---|
| #1095 | Eliminate `as unknown as Instr` casts — extend Instr union to cover all emitted opcodes | medium | done |
| #1100 | Wasm-native Proxy: meta-object protocol without JS host | top | done |
| #1536c | standalone-native user Error subclass: instance creation via __new_<Parent> + native instanceof tag chain (no host imports) | high | done |
| #1537 | Wasm-native number formatting (Ryū port): toString/toFixed/toPrecision/toExponential | high | done |
| #1694 | Promise.any/all/allSettled/race: non-Promise capability `this` + extends-Promise codegen (~50 fails) | medium | done |
| #1796 | Migrate synchronous-async contract to CPS Promise model (flip ASYNC_CPS_ENABLED) | top | done |
| #1804 | feat(IR): vec.new_fixed — lower fixed-length array literals through the IR path | medium | done |
| #1853 | Track a separate hard-error (compiler-crash / malformed-Wasm) stability bucket on the conformance dashboard, distinct from unsupported-feature | high | done |
| #1854 | Cross-backend differential testing harness — same TS to WasmGC / linear / bytecode-VM must produce identical observable output | high | done |
| #1855 | UB-free TypeScript program generator + automated validity-preserving test-case minimization for equivalence failures | medium | done |
| #1910a | standalone boxed primitive-wrapper ToPrimitive (new Number/String/Boolean) | critical | done |
| #1918 | Stack-balance strict mode + fixup ratchet — stop silently patching emitter bugs into wrong runtime values | high | done |
| #1920 | One instruction walker — peephole misses catchAll bodies; ≥4 divergent recursive walkers | medium | done |
| #1921 | Replace the 'Codegen error:' string-prefix compile-failure gate with structured severity | high | done |
| #1922 | Shared IR traversal/use-collection module — fixes live defect: ordinary while loops demote off the IR path | high | done |
| #1923 | Meter IR post-claim demotions in the fallback ratchet — build/verify/lower failures are invisible to CI | high | done |
| #1924 | Instruction-level type rules in the IR verifier — operands, branch-arg types, and resultType validation | high | done |
| #1925 | Run IR hygiene passes inside nested buffers — or commit to one control-flow representation | medium | done |
| #1928 | Source-position remapping for pre-parse rewrites — diagnostics report wrong line numbers | high | done |
| #1929 | CompileError: add file attribution and stop truncating TS diagnostic message chains | medium | done |
| #1931 | Decompose detectEarlyErrors (3,350-line function) and run it on every path; wire or delete the dead treeshake option | medium | done |
| #1933 | Runtime multi-instance isolation — module-level mutable state bleeds across instances and retains them forever | high | done |
| #1935 | Retire the undefined-as-sentinel protocol in runtime.ts — getters returning undefined are misread as absent | medium | done |
| #1943 | Enforce the documented regression thresholds (10% ratio, 50-per-bucket) in CI — today the hard gate is only net ≥ 0 | high | done |
| #1950 | Default-on optimization — default builds ship unoptimized; add -O default where Binaryen is present plus tiny always-on cleanups | medium | done |
| #1975 | linear backend: NaN and \"\" are truthy (f64.ne 0 / raw i32 pointer truthiness); &&/\|\| return 0/1 instead of operand values | high | done |
| #1976 | linear backend: string relationals compare memory addresses; .length returns UTF-8 byte count; string concat in compound-assign emits invalid module | high | done |
| #1984 | freeze-point discipline: indexSpaceFrozen flag — late addImport/ensureLateImport after final flush throws at the producer (#2043 Option 3) | medium | done |
| #1994 | reduce/reduceRight on string[] trap 'illegal cast' — accumulator local hard-coded to numeric kind | high | done |
| #2007 | array operand in string concatenation traps 'illegal cast' — '+' never routes vecs through ToPrimitive/join | medium | done |
| #2008 | tagged templates broken: cooked elements read as undefined, .raw access traps, String.raw throws (template object unusable) | high | done |
| #2011 | object-literal getter/setter closures capture copies — writes through accessors never reach the outer scope, getter pairs don't share state | high | done |
| #2012 | Object.freeze: no strict-mode TypeError on write, isFrozen false — tracking only fires for identifier args, struct receivers get no integrity bit | medium | done |
| #2017 | assignment to a getter-only object-literal property traps 'illegal cast' instead of throwing strict-mode TypeError | low | done |
| #2021 | array literal [new Subclass(), new Base()] traps 'dereferencing a null pointer' — element type taken from first element, contextual annotation ignored | high | done |
| #2023 | new.target compiles to constant i32 1 — identity comparisons (new.target === A) always wrong | medium | done |
| #2024 | class accessor override with partial pair: get-only override silently drops writes (should TypeError); set-only override reads NaN (should undefined) | medium | done |
| #2025 | calling an extracted method (const f = a.m; f()) traps uncatchably instead of throwing catchable TypeError | low | done |
| #2028 | new Promise(executor): invoking the host-provided resolve/reject from wasm traps null deref — executor pattern fully broken in JS-host mode | high | done |
| #2033 | custom iterables ([Symbol.iterator]): spread emits invalid wasm (CE), destructuring reads NaN — only for-of consults the protocol | high | done |
| #2035 | generator return value leaks into iteration: spread/for-of/Array.from/yield* include it; final {value, done:true} never materializes | high | done |
| #2051 | short-circuited ?. produces the type's default value (0 / \"null\") instead of undefined | high | done |
| #2077 | standalone: caught Error's .message traps null deref; .name returns '[object Object]' (catch-bound value isn't the $Error struct) | high | done |
| #2088 | per-builtin representation scaffold (element accessor + coercion), starting with fromCharCode + join | high | done |
| #2090 | stack-balance self-repair must not invent values — null patch becomes a hard compile error | high | done |
| #2091 | REGEX_STEP_CAP overflow silently reports no-match — must throw RangeError | medium | done |
| #2093 | issue→probe coverage CI rule: bugfix issues cannot flip to done without a permanent probe/test reference | high | done |
| #2096 | oracle_version stamping + cross-version diff guard (prerequisite for the #1945 oracle flip) | high | done |
| #2098 | encode flake-classification rules in diff-test262: ct_flake/ct_suspect split + bucket signature hash | low | done |
| #2099 | promote-baseline must re-run (not carry forward) poison-classified rows | medium | done |
| #2102 | shared throwJsError(kind, msg) lowering + trap-site audit — runtime checks must throw catchable JS errors, not Wasm traps | high | done |
| #2103 | shared binding-info analysis — one mutation/capture/declaration-order oracle for all lowerings | medium | done |
| #2108 | coercion drift gate: scripts/check-coercion-sites.mjs — no 9th hand-rolled ToString | high | done |
| #2118 | self/mutually-recursive const-arrow closures emit invalid Wasm (struct.get type mismatch) or runtime ref.cast traps | high | done |
| #2119 | module code (always strict) gets a mapped arguments object: parameter writes leak into arguments[i] | medium | done |
| #2136 | IR loop conditions: lower non-i32 conds through ToBoolean instead of bailing to legacy | medium | done |
| #2137 | IrPathReport channel: stop laundering IR fallbacks through ctx.errors warnings | medium | done |
| #2139 | CI: linear-backend tests (22 files) are not executed by any CI job | critical | done |
| #2143 | WebAssembly.validate lane for unoptimized pipeline output (split of #1858-C5) | medium | done |
| #2144 | linear %: replace naive trunc-formula with the #2056 fmod helper (cross-backend parity) | medium | done |
| #2145 | Generate plan/log/ir-adoption.md adoption table + bucket counts from source | low | done |
| #2147 | reconcile-tasklist.mjs: flag ready issues whose number appears in a merged PR title | medium | done |
| #2163 | Standalone Symbol conformance residual (~240 tests) | medium | done |
| #2164 | Standalone Date conformance residual (~234 tests) | medium | done |
| #2166 | Standalone JSON conformance residual (~76 tests) | low | done |
| #2169 | standalone: spread / Array.from / array-destructure don't drive a native generator (treat the state struct as a __vec) | high | done |
| #2171 | standalone: native generator only supports numeric yields — string/boolean/object yields bail (#680) | medium | done |
| #2178 | standalone baseline floor goes stale because push:main promote-baseline is cancelled by later main pushes (thrash) → standalone-guard blocks every PR on current main | high | done |
| #2179 | post-delete struct READ returns stale value — inline struct.get fast-path bypasses the runtime tombstone | medium | done |
| #2182 | collectInstrs detached-array audit + liveBodies-empty assertion (funcIdx-shift hazard completeness) | low | done |
| #2184 | linear backend: &&/\|\| yield 0/1 constants instead of operand values (needs result-type unification) | medium | done |
| #2189 | standalone: array .length reads 0 through the externref boundary (latent $Array introspection gap) | high | done |
| #2190 | standalone: array element indexing (arr as any)[i] returns null/0 through the externref boundary | high | done |
| #2190a | standalone: homogeneous string sub-array of any[] traps on e[0][0] ($AnyString[]-in-any[] read-back layer) | medium | done |
| #2190c | standalone: heterogeneous inner tuple of any[] drops off-kind element ([[\"a\",7]] / [[7,\"ab\"]] traps on read-back) | medium | done |
| #2192 | standalone: caught Error .message/.name read inline (=== literal, .length) returns null/empty; works only via a typed local | high | done |

<!-- GENERATED_ISSUE_TABLES_END -->
