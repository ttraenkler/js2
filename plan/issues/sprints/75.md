# Sprint 75 — early hygiene bank (2026-07-23, lead-directed queue purge)

This is NOT a normal budget-rollover freeze record. It banks the `sprint:
current` queue's finished-but-still-tagged entries out of the live window as
part of the 2026-07-23 queue-integrity purge (lead-directed; see the
`docs/ci-truth-and-queue-housekeeping` PR). The queue had drifted to 217
`current`-tagged issues of which only ~79 were genuinely claimable; finished
items were masquerading as available work.

The next regular `freeze-sprint.mjs` run will compute N=76 (this file + the
`sprint: 75` tags below take 75) and freeze the remainder of the window
normally.

## Banked: already `done`, still tagged `current` (13)

#1907, #2717, #3109, #3409, #3533, #3534, #3535, #3536, #3537, #3538, #3541,
#3542, #3547

## Banked: `wont-fix`, still tagged `current` (4)

#2768, #3089, #3218, #3283

## Banked: verified merged-but-never-flipped (done-flip applied in the same PR)

Fixes verified against `git log origin/main` subjects (not just the
reconciler's title match — see the false-positive caution in
`scripts/reconcile-tasklist.mjs` output):

- #3359 — `fix(#3359): thread thisArg into array-method callbacks`
- #3396 — `fix(#3396): don't re-type a boxed-capture ref-cell slot at let/const/var declaration`
- #3407 — `fix(#3407): guard fixture runner against duplicate/contradictory verdict rows`
- #3408 — `fix(#3408): retire non-atomic issue-ID entrypoints and stale collision paths`
- #3410 — `fix(#3410): close legacy-origin bypass in the private-labs pre-push guard`
- #3467 — `fix(ci): compare test262 gate against real merge-base per-SHA cache (#3467)` + follow-ups
- #3476 — `fix(ci): promote landing-page baseline on queue merges` (commit
  mislabeled `(#3468)`, branch `issue-3468-landing-baseline` — subject matches
  #3476's title exactly; #3468 is an unrelated codegen issue)
- #3497 — `fix(ir): resolve JavaScript JSDoc signatures (#3497)` + `use public JSDoc type APIs`
- #3499 — `fix(porffor): lower typed JS bitwise composites (#3499)`
- #3501 — `fix(ir): infer typed vectors for empty arrays (#3501)`
- #3502 — `feat(ir): lower shared ASCII string operations for #3502` + string-contract family

(#3461, #3462, #2787 were also done-flipped in the purge but were not
`sprint: current`, so they are not banked here.)

## NOT banked (deliberately)

- #3381/#3382 — fixes merged, but a NEWER commit deliberately keeps them
  in-progress: `docs(#3381,#3382): keep issues in-progress (#2093 probe gate
  blocks infra done-flip)`. Left untouched.
- Slice-progress epics whose reconciler citation was partial work (#745, #802,
  #1930, #2963, #3032, #2872, #2875, #3389, #3481, #3488, #3498, #2917, #3390,
  #3394, #3395): NOT done — returned to `ready` with dead claims released
  instead.
- Reconciler false positives (arch/spec/filing/reopen/mention commits): #680,
  #739, #743, #773, #904, #1046, #1472, #1888, #1907(bank above), #2026,
  #2039, #2620, #2860, #3024, #3108, #3251, #3337, #3417, #3442, #3474, #3530,
  #3543, #3546, #3548 — left open.

<!-- GENERATED_ISSUE_TABLES_START -->
## Issue Tables

_Generated from issue files. Update issue `status`, then rerun `node scripts/sync-sprint-issue-tables.mjs`._

### Done

| Issue | Title | Priority | Status |
|---|---|---|---|
| #1907 | standalone: built-in static method value reads without __get_builtin (#1888 S6-b) | critical | done |
| #2717 | Array flat/flatMap are host-import-only — no standalone native arm, no ctx.standalone guard | high | done |
| #3109 | Test-helper consolidation: 132 test files re-declare compileAndRun (10+ signature variants) across 292k test LOC | high | done |
| #3359 | standalone: Array.prototype.filter (and callback methods) ignore the thisArg argument — closure runs with wrong `this` | low | done |
| #3396 | standalone: closure-env / promise-reaction / for-loop struct type A used where type B expected — struct.set/get/call-param invalid Wasm (~70 tests) | high | done |
| #3407 | test262 fixture runner emits duplicate and contradictory verdict rows; enforce one canonical result per file | high | done |
| #3408 | Retire non-atomic issue-ID entrypoints and stale collision-remediation guidance | high | done |
| #3409 | Pre-push format gate hard-depends on GNU timeout and falsely blocks macOS pushes | high | done |
| #3410 | Private labs pre-push guard misses the legacy public js2wasm origin URL | critical | done |
| #3467 | test262 regression gate: compare each PR against its REAL merge-base commit (per-SHA cache), not a drifting promoted snapshot | high | done |
| #3476 | Landing-page baseline frozen — promote-baseline env-gate skips on queue merges | high | done |
| #3497 | Resolve exact-source JSDoc signatures for the linear IR landing benchmarks | high | done |
| #3499 | Lower typed JavaScript bitwise composites through the Porffor backend | high | done |
| #3501 | Infer typed linear vectors from empty-array read/write evidence | high | done |
| #3502 | Lower landing string construction and char methods through shared IR | high | done |
| #3533 | codegen: class field initialized to a function value (`c = fn`) emits invalid Wasm — closure-value read reports externref but stores a raw ref | high | done |
| #3534 | codegen: mutually-recursive const-closure funcref-cell RTT desync — matcher-invoking Function.prototype.toString files trap (illegal cast) at construct site | medium | done |
| #3535 | Standalone (start) top-level throws unrenderable (instance===null) — 8,610 baseline rows masked as 'wasm exception during module init' | high | done |
| #3536 | Standalone: object-literal argument to a declared function reads null (struct/externref call-boundary mismatch) — RegExp property-escapes 311-row cluster | high | done |
| #3537 | standalone: expando own-properties on array ($Vec) receivers are silently dropped — writes no-op, reads answer undefined | high | done |
| #3538 | Async-gen abrupt completion + IteratorResult dynamic reads — the 280-test yield*-error cohort (F2 harvest, #3417/#3178) | high | done |
| #3541 | Standalone: String.fromCodePoint.apply(null, vec) returns null (__str_concat null-deref) — the then-current gate on the RegExp property-escapes 311-row family | high | done |
| #3542 | Standalone async-fn rejections lose the thrown value — reason is always NULL (unfinished #1326 Phase-1C payload wiring) | high | done |
| #3547 | codegen: remove the #3024 funcref-cell struct.new stopgap in compileClosureCall (dead after #3534 — producer eliminated + zero-producer probe) | medium | done |

### Won't Fix

| Issue | Title | Priority | Status |
|---|---|---|---|
| #2768 | bare-var receiver recovery: per-type externref→ref recovery hardening + safelist expansion (follow-on of #2767's Date-only gate) | medium | wont-fix |
| #3089 | codegen: BigInt TypedArray tests fail to compile — 'Binary emit error: RangeError: offset is out of bounds' (i64 codegen, ~22/30 sampled, pre-existing) | medium | wont-fix |
| #3218 | standalone: ValidateTypedArray for native __ta_dyn_{fill,copyWithin,reverse} — WONT-FIX (verify-first: target tests already pass; residual is elsewhere) | high | wont-fix |
| #3283 | standalone dstr runtime-semantics — Opus-now unblocked slices (lazy-defaults, obj-rest ToPrimitive, abrupt-step errors, gen brand-check) | high | wont-fix |

<!-- GENERATED_ISSUE_TABLES_END -->
