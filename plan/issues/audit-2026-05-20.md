---
type: audit
created: 2026-05-20
author: product-owner
sprint_context: post-sprint-52
---

# Issue Audit — 2026-05-20

Scope: `plan/issues/` after the close of sprint 52 (v0.52.0, 28,171/43,160
test262 passes). Focus on (1) duplicate IDs, (2) `in-review` → `done`
reconciliation, (3) sprint 52 status sanity, (4) sprint 53 candidate slate.

## 1. Duplicate IDs

### 1a. False positives (acceptable — do not change)

The naive `grep ^id:` scan reports the following matches, all of which are
inline YAML examples inside markdown documentation, not real duplicate issue
files:

- `id: 1006` — appears in `SCHEMA.md` (sample frontmatter) and the real
  `sprints/42/1006-support-eval-via-js-host.md`. ✅ Keep both.
- `id: 1133` (twice in `backlog/1133-fibonacci-story-points-velocity-tracking.md`)
  — the second hit is a YAML code block in the issue body. ✅ Keep file.
- `id: 672` (twice in `sprints/15/672-weakmap-weakset-weakref-support.md`)
  — same pattern; second hit is a fenced example. ✅ Keep file.
- `id: 43`-`52` reported as duplicates because each `sprints/N/sprint.md` uses
  the sprint number as its `id:` field, which collides with the matching old
  issue (`sprints/0/43-void-expression.md` etc.). This is a documented schema
  convention — `sprint.md` files are not issues. ✅ No change.

### 1b. Real duplicates (need follow-up)

The audit found multiple actual file-level collisions. **None were resolved by
this pass** — they all involve sprint history that the PO does not want to
mutate without tech-lead/architect concurrence. They are listed here so the
next sprint-grooming session can decide on canonical names.

| ID | Files | Notes / proposed action |
|----|-------|------------------------|
| 1154 | `backlog/1154-test262-worker-array-prototype-poisoning.md` <br> `sprints/50/1154-test262-worker-array-prototype-poisoning.md` | Same title in two places. Likely the backlog file is stale (sprint 50 already absorbed it). **Propose**: delete the `backlog/` copy. |
| 1278 | `sprints/47/1278.md` <br> `sprints/47/1278-update-stale-lodash-tier1-stress.md` | The bare `1278.md` is a stub. **Propose**: confirm and delete it. |
| 1292 | `sprints/48/1292-lodash-tier2-stress-test.md` <br> `sprints/50/1292-lodash-tier2-stress-test.md` | Same issue exists in two sprint dirs. **Propose**: pick whichever has the final status and delete the other. |
| 1295 | `sprints/48/1295-compiler-rethrow-wasmexception.md` <br> `sprints/48/1295-lodash-init-start-function-throw.md` | Two completely different issues share id 1295. **Propose**: re-number one (next free id, currently >1529). |
| 1307 | `backlog/1307-ci-test262-global-concurrency.md` <br> `sprints/50/1307-ci-test262-global-concurrency.md` | Same as #1154 — stale backlog stub. **Propose**: delete backlog copy. |
| 1310 | `sprints/50/1310-vm-sandbox-isolation-test262.md` <br> `sprints/50/1310-vm-sandbox-test262-isolation.md` | Same id, near-identical titles. **Propose**: delete the older/empty one. |
| 1334–1353 | Each id appears twice in `sprints/50/` (e.g. `1334-ecmascript-spec-compliance-audit.md` + `1334-spec-gap-object-defineproperty-descriptor-attributes.md`, and so on through `1353`). | This is an off-by-one shift in a bulk-created batch of spec-gap issues. About **20 IDs are affected**. **Propose**: re-number the entire `spec-gap-*` series starting from the next free id (≥1530) to make the namespace contiguous; defer to architect + tech lead because cross-references in dependency-graph.md and several `parent:` fields point at these IDs. |
| 1352 | `backlog/1352-regexp-exec-result-wasmgc-string-externref-equality.md` <br> `sprints/50/1352-spec-gap-set-methods-set-like-arg.md` | Truly different issues colliding. **Propose**: re-number the `backlog/` file (cheaper — the sprint-50 one is already referenced in sprint history). |
| 1353 | `backlog/1353-spec-backlog-memory-model.md` <br> `sprints/50/1353-json-stringify-parse-shape-walking-spec.md` | Same as 1352 pattern. **Propose**: re-number backlog. |
| 1392 | `sprints/51/1392-ir-null-safe-access-primitives.md` <br> `sprints/52/1392-refresh-benchmarks-browser-runtime-hang.md` | Two different real issues. The sprint-52 issue is `done` (merged PR #401). **Propose**: re-number the sprint-52 one or rename to use the next id. |
| 1396 | `sprints/51/1396-for-of-dstr-oob-undefined-sentinel.md` <br> `sprints/52/1396-forof-dstr-externref-array-default.md` | Same as 1392 pattern. **Propose**: re-number the sprint-52 copy. |
| 1521 | `sprints/52/1521-wasi-native-messaging-host-example.md` <br> `sprints/52/1521-test262-ci-speedup-cross-pr-cache-and-scope-filter.md` | Two different issues both claim id 1521 inside the same sprint. **Propose**: re-number the WASI Native Messaging one to ≥1530 — the test262-CI-speedup one is already merged (PR #411). |

Total real duplicates that need ID surgery: **~30 issue files** across
sprints 47, 48, 50, 51, 52. Recommend booking one grooming slot in sprint 53
(or a dedicated `audit-cleanup` task) to renumber these — most are deep in
sprint history and a flat rename is safe.

## 2. `in-review` → `done` reconciliation

51 sprint-52 issues carry `status: in-review`. Each was cross-referenced
against the list of merged PR branches (`gh pr list --state merged --limit
500`). Of those:

| ID | Action | Merged via |
|----|--------|------------|
| 1382 | ✅ flipped to `status: done`, set `completed: 2026-05-20` | branches `issue-1382-phase2-hofs` and `issue-1382-wasm-closure-bridge` |
| 1364 | ✅ flipped to `status: done`, set `completed: 2026-05-20` | branch `issue-1364-class-element-descriptors` |

**No other sprint-52 `in-review` issue has a matching merged branch.** The
remaining 47 entries are either (a) genuinely still under review without a
merged PR yet, or (b) work that was merged under a non-standard branch name
the regex couldn't catch.

Recommend the tech lead spot-check the following high-traffic ones manually
before sprint 53 planning (they had related activity in main's commit log):

- `1431-spec-gap-assignment-operators-destructuring-completion.md` — PR #353
  merged with `[skip ci]`; status worth confirming.
- `1397-static-dispatch-method-reassignment.md` — branch
  `issue-1397-static-method-dispatch` is in the merged list; was probably
  closed in sprint 51 but file still says `in-review`.
- `1398-report-edition-filter-category-table.md` — similar; branch
  `issue-1398-report-edition-filter` is merged.

I did not flip these to `done` because the heuristic match isn't strong
enough and the PO playbook says "be conservative". Flag for tech lead.

## 3. Sprint 52 status sanity

Distribution after this audit:

| Status | Count |
|--------|-------|
| done | 17 (was 15 before audit; +1382, +1364) |
| in-review | 47 (was 49) |
| in-progress | 7 |
| ready | 1 (`#1373`) |
| blocked | 2 (`#1373b`, `#1521-wasi-native-messaging`) |
| moved-to-s52 | 1 (`#1387` — non-standard status, see below) |
| active | 1 (`sprint.md`) |

Observations:
- **No `status: backlog` issues remain in sprint 52** — the directory is
  hygienically scoped. ✅
- `#1387` uses the non-canonical status `moved-to-s52`. Per the schema this
  should be one of `backlog / ready / in-progress / in-review / done /
  blocked / suspended / wont-fix`. The history suggests this issue was moved
  *out* of sprint 51 and into 52. **Propose**: change to `status: ready` and
  rely on the `sprint: 52` field for routing. (Did not change in this pass —
  conservative.)
- `#1373` is `ready` inside sprint 52 even though the sprint is closed. It
  should either move to sprint 53 (where #1373b is also waiting) or flip to
  `done` if the IR async work shipped under one of the IR-phase merges.

## 4. Sprint 53 candidates

Wrote `plan/issues/sprints/53/sprint-candidates.md` with:

- **10 primary candidates** (mix of carry-over + high-impact backlog).
- **5 stretch candidates** (smaller spec gaps).
- **Carry-over from sprint 52** (host-independence track #1471–#1474,
  microtask #1326c, audit #1505, IR async #1373).

See that file for the full rationale per pick. Highlights:

- #820 (nullish/TypeError umbrella, 6,993 FAIL) is the single biggest target.
- #779 (assert-failure umbrella, 8,674 tests) — likely needs splitting but
  even partial fixes dwarf any other backlog item.
- Host-independence work (1471–1474) plus #1326c finishes the standalone
  story before we start advertising it externally.

## 5. Stale backlog entries

The task brief referenced `plan/issues/backlog/backlog.md` as the master
list. **No such file exists** in this checkout — the `backlog/` directory is
a flat list of issue files, one per issue, with no index. There is therefore
nothing to prune.

If a `backlog.md` index file is desired, recommend generating it from the
frontmatter of `backlog/*.md` via a one-off script — that index can then be
rebuilt cheaply and never goes stale.

(Some sprint-0/early sprint markdown files include `## Backlog` sections,
but those are historical sprint planning artefacts, not the canonical
backlog.)

## Summary of changes made by this audit

| Change | Files |
|--------|-------|
| `status: in-review` → `status: done` (+ `completed` date) | `sprints/52/1382-wasm-closure-not-js-callable-bridge.md`, `sprints/52/1364-spec-gap-class-elements-method-descriptor-fidelity.md` |
| New planning doc | `sprints/53/sprint-candidates.md` |
| New audit report | `issues/audit-2026-05-20.md` (this file) |

## Open follow-ups (for tech lead + PO at sprint 53 planning)

1. Re-number the ~30 real-duplicate issue IDs (section 1b).
2. Confirm/flip the ~47 `in-review` issues that have no matching merged
   branch.
3. Normalise `#1387`'s `moved-to-s52` status.
4. Decide whether `#1373` rolls forward to sprint 53 or has already shipped.
5. Decide whether to generate a `backlog/backlog.md` index for easier
   navigation.
