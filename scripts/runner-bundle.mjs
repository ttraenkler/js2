var __require = /* @__PURE__ */ ((x) =>
  typeof require !== "undefined"
    ? require
    : typeof Proxy !== "undefined"
      ? new Proxy(x, {
          get: (a, b) => (typeof require !== "undefined" ? require : a)[b],
        })
      : x)(function (x) {
  if (typeof require !== "undefined") return require.apply(this, arguments);
  throw Error('Dynamic require of "' + x + '" is not supported');
});

// tests/test262-runner.ts
import { existsSync, readdirSync, readFileSync } from "fs";
import { join as join2, relative } from "path";

// src/compiler.ts
import ts19 from "typescript";

// src/checker/index.ts
import ts2 from "typescript";

// src/checker/language-service.ts
import ts from "typescript";

// src/checker/index.ts
function getBundledLibFiles() {
  const files = globalThis.__js2wasmTsLibFiles ?? globalThis.__ts2wasmTsLibFiles;
  return files && typeof files === "object" ? files : void 0;
}
async function safeImport(id) {
  try {
    return await import(
      /* @vite-ignore */
      id
    );
  } catch {
    return null;
  }
}
var _nodePathMod = await safeImport("node:path");
function getPath() {
  return _nodePathMod;
}
function dirname(p) {
  return getPath()?.dirname(p) ?? "";
}
function join(...args) {
  return getPath()?.join(...args) ?? args.join("/");
}
var _nodeFsMod = await safeImport("node:fs");
var _nodeModuleMod = await safeImport("node:module");
var _nodeUrlMod = await safeImport("node:url");
function getReadFileSync() {
  return _nodeFsMod?.readFileSync ?? null;
}
function getCreateRequire() {
  return _nodeModuleMod?.createRequire ?? null;
}
function getFileURLToPath() {
  return _nodeUrlMod?.fileURLToPath ?? null;
}
var _tsLibDir;
function getTsLibDir() {
  if (_tsLibDir === void 0) {
    try {
      const cr = getCreateRequire();
      const fup = getFileURLToPath();
      if (!cr || !fup) throw new Error("Node.js modules not available");
      const esmRequire = cr(typeof __filename !== "undefined" ? __filename : fup(import.meta.url));
      _tsLibDir = dirname(esmRequire.resolve("typescript/lib/lib.d.ts"));
    } catch {
      try {
        _tsLibDir = dirname(__require.resolve("typescript/lib/lib.d.ts"));
      } catch {
        _tsLibDir = "";
      }
    }
  }
  return _tsLibDir;
}
function readLibFile(name) {
  const bundled = getBundledLibFiles()?.[name];
  if (typeof bundled === "string" && bundled.length > 0) {
    return bundled;
  }
  try {
    const rfs = getReadFileSync();
    if (!rfs) return "";
    return rfs(join(getTsLibDir(), name), "utf-8");
  } catch {
    return "";
  }
}
var LIB_FILES = {};
var TS_LIB_NAMES = /* @__PURE__ */ new Set([
  "lib.es5.d.ts",
  "lib.dom.d.ts",
  "lib.decorators.d.ts",
  "lib.decorators.legacy.d.ts",
  "lib.es2015.d.ts",
  "lib.es2015.core.d.ts",
  "lib.es2015.collection.d.ts",
  "lib.es2015.generator.d.ts",
  "lib.es2015.iterable.d.ts",
  "lib.es2015.promise.d.ts",
  "lib.es2015.proxy.d.ts",
  "lib.es2015.reflect.d.ts",
  "lib.es2015.symbol.d.ts",
  "lib.es2015.symbol.wellknown.d.ts",
  "lib.es2021.d.ts",
  "lib.es2021.promise.d.ts",
  "lib.es2021.string.d.ts",
  "lib.es2021.weakref.d.ts",
]);
function getLibSource(name) {
  if (name in LIB_FILES) return LIB_FILES[name];
  if (name === "lib.d.ts") {
    const libNames = [
      // ES5 base
      "lib.es5.d.ts",
      // ES2015 sub-libs (from lib.es2015.d.ts references)
      "lib.es2015.core.d.ts",
      "lib.es2015.collection.d.ts",
      "lib.es2015.generator.d.ts",
      "lib.es2015.iterable.d.ts",
      "lib.es2015.promise.d.ts",
      "lib.es2015.proxy.d.ts",
      "lib.es2015.reflect.d.ts",
      "lib.es2015.symbol.d.ts",
      "lib.es2015.symbol.wellknown.d.ts",
      // ES2016
      "lib.es2016.array.include.d.ts",
      "lib.es2016.intl.d.ts",
      // ES2017
      "lib.es2017.object.d.ts",
      "lib.es2017.string.d.ts",
      "lib.es2017.intl.d.ts",
      "lib.es2017.typedarrays.d.ts",
      "lib.es2017.date.d.ts",
      "lib.es2017.sharedmemory.d.ts",
      // ES2018
      "lib.es2018.asyncgenerator.d.ts",
      "lib.es2018.asynciterable.d.ts",
      "lib.es2018.intl.d.ts",
      "lib.es2018.promise.d.ts",
      "lib.es2018.regexp.d.ts",
      // ES2019
      "lib.es2019.array.d.ts",
      "lib.es2019.intl.d.ts",
      "lib.es2019.object.d.ts",
      "lib.es2019.string.d.ts",
      "lib.es2019.symbol.d.ts",
      // ES2020
      "lib.es2020.bigint.d.ts",
      "lib.es2020.date.d.ts",
      "lib.es2020.intl.d.ts",
      "lib.es2020.number.d.ts",
      "lib.es2020.promise.d.ts",
      "lib.es2020.sharedmemory.d.ts",
      "lib.es2020.string.d.ts",
      "lib.es2020.symbol.wellknown.d.ts",
      // ES2021
      "lib.es2021.intl.d.ts",
      "lib.es2021.promise.d.ts",
      "lib.es2021.string.d.ts",
      "lib.es2021.weakref.d.ts",
      // ES2024
      "lib.es2024.collection.d.ts",
      // ESNext — Set methods (union, intersection, difference, etc.)
      "lib.esnext.collection.d.ts",
      // DOM (decorators loaded via /// <reference> in lib.es5.d.ts)
      "lib.dom.d.ts",
    ];
    const parts = libNames.map((n) => getLibSource(n) ?? "");
    const content = parts.join("\n");
    LIB_FILES[name] = content;
    return content;
  }
  if (name.startsWith("lib.") && name.endsWith(".d.ts")) {
    const content = readLibFile(name);
    if (content) {
      LIB_FILES[name] = content;
      return content;
    }
    return void 0;
  }
  return void 0;
}
function isKnownLibName(name) {
  return name === "lib.d.ts" || TS_LIB_NAMES.has(name) || (name.startsWith("lib.") && name.endsWith(".d.ts"));
}
var LIB_SOURCE_FILES = /* @__PURE__ */ new Map();
function getLibSourceFile(name, languageVersion) {
  const content = getLibSource(name);
  if (content === void 0) return void 0;
  const key = `${name}:${JSON.stringify(languageVersion)}`;
  let sf = LIB_SOURCE_FILES.get(key);
  if (!sf) {
    sf = ts2.createSourceFile(name, content, languageVersion);
    LIB_SOURCE_FILES.set(key, sf);
  }
  return sf;
}
function analyzeSource(source, fileName = "input.ts", analyzeOptions) {
  const isJs = fileName.endsWith(".js") || fileName.endsWith(".jsx");
  const scriptKind = isJs ? ts2.ScriptKind.JS : ts2.ScriptKind.TS;
  const useAllowJs = isJs || analyzeOptions?.allowJs === true;
  const compilerOptions = {
    target: ts2.ScriptTarget.ES2022,
    module: ts2.ModuleKind.ESNext,
    strict: !isJs,
    noImplicitAny: false,
    noEmit: true,
  };
  const compilerHost = {
    getSourceFile(name, languageVersion) {
      if (name === fileName) {
        return ts2.createSourceFile(name, source, languageVersion, true, scriptKind);
      }
      const libSf = getLibSourceFile(name, languageVersion);
      if (libSf) return libSf;
      return void 0;
    },
    getDefaultLibFileName: () => "lib.d.ts",
    writeFile: () => {},
    getCurrentDirectory: () => "/",
    getCanonicalFileName: (f) => f,
    useCaseSensitiveFileNames: () => true,
    getNewLine: () => "\n",
    fileExists: (name) => name === fileName || isKnownLibName(name),
    readFile: () => void 0,
    getDirectories: () => [],
    directoryExists: () => true,
  };
  if (useAllowJs) {
    compilerOptions.allowJs = true;
    compilerOptions.checkJs = true;
  }
  const program = ts2.createProgram([fileName], compilerOptions, compilerHost);
  const syntacticDiagnostics = program.getSyntacticDiagnostics();
  const semanticDiagnostics = analyzeOptions?.skipSemanticDiagnostics ? [] : program.getSemanticDiagnostics();
  const diagnostics = [...syntacticDiagnostics, ...semanticDiagnostics];
  return {
    sourceFile: program.getSourceFile(fileName),
    checker: program.getTypeChecker(),
    program,
    diagnostics,
    syntacticDiagnostics,
  };
}

// src/codegen/index.ts
import ts15 from "typescript";

// src/codegen/walk-instructions.ts
function walkInstructions(instrs, visitor) {
  for (const instr of instrs) {
    visitor(instr);
    walkChildren(instr, (child) => walkInstructions(child, visitor));
  }
}
function walkChildren(instr, fn) {
  const a = instr;
  if (a.body && Array.isArray(a.body)) fn(a.body);
  if (a.then && Array.isArray(a.then)) fn(a.then);
  if (a.else && Array.isArray(a.else)) fn(a.else);
  if (a.catches && Array.isArray(a.catches)) {
    for (const c of a.catches) {
      if (Array.isArray(c.body)) fn(c.body);
    }
  }
  if (a.catchAll && Array.isArray(a.catchAll)) fn(a.catchAll);
}

// src/codegen/dead-elimination.ts
function collectRefsFromBody(body, usedFuncs, usedTypes) {
  for (const instr of body) {
    switch (instr.op) {
      case "call":
        usedFuncs.add(instr.funcIdx);
        break;
      case "ref.func":
        usedFuncs.add(instr.funcIdx);
        break;
      case "call_indirect":
        usedTypes.add(instr.typeIdx);
        break;
      case "call_ref":
        usedTypes.add(instr.typeIdx);
        break;
      case "struct.new":
      case "struct.get":
      case "struct.set":
        usedTypes.add(instr.typeIdx);
        break;
      case "array.new":
      case "array.new_fixed":
      case "array.new_default":
      case "array.get":
      case "array.get_s":
      case "array.get_u":
      case "array.set":
      case "array.fill":
        usedTypes.add(instr.typeIdx);
        break;
      case "array.copy":
        usedTypes.add(instr.dstTypeIdx);
        usedTypes.add(instr.srcTypeIdx);
        break;
      case "ref.null":
        if (typeof instr.typeIdx === "number") {
          usedTypes.add(instr.typeIdx);
        }
        break;
      case "ref.cast":
      case "ref.cast_null":
      case "ref.test":
        usedTypes.add(instr.typeIdx);
        break;
      case "block":
      case "loop":
        collectBlockTypeRefs(instr.blockType, usedTypes);
        collectRefsFromBody(instr.body, usedFuncs, usedTypes);
        break;
      case "if":
        collectBlockTypeRefs(instr.blockType, usedTypes);
        collectRefsFromBody(instr.then, usedFuncs, usedTypes);
        if (instr.else) collectRefsFromBody(instr.else, usedFuncs, usedTypes);
        break;
      case "try":
        collectBlockTypeRefs(instr.blockType, usedTypes);
        collectRefsFromBody(instr.body, usedFuncs, usedTypes);
        for (const c of instr.catches) collectRefsFromBody(c.body, usedFuncs, usedTypes);
        if (instr.catchAll) collectRefsFromBody(instr.catchAll, usedFuncs, usedTypes);
        break;
      default: {
        const a = instr;
        if (typeof a.typeIdx === "number") usedTypes.add(a.typeIdx);
        if (typeof a.funcIdx === "number") usedFuncs.add(a.funcIdx);
        if (typeof a.dstTypeIdx === "number") usedTypes.add(a.dstTypeIdx);
        if (typeof a.srcTypeIdx === "number") usedTypes.add(a.srcTypeIdx);
        if (a.blockType) collectBlockTypeRefs(a.blockType, usedTypes);
        break;
      }
    }
  }
}
function collectBlockTypeRefs(bt, usedTypes) {
  if (bt.kind === "type" && typeof bt.typeIdx === "number") {
    usedTypes.add(bt.typeIdx);
  }
  if (bt.kind === "val" && bt.type) {
    collectRefsFromValType(bt.type, usedTypes);
  }
}
function collectRefsFromValType(vt, used) {
  if ((vt.kind === "ref" || vt.kind === "ref_null") && typeof vt.typeIdx === "number") {
    used.add(vt.typeIdx);
  }
}
function collectRefsFromTypeDef(td, used) {
  switch (td.kind) {
    case "func":
      for (const p of td.params) collectRefsFromValType(p, used);
      for (const r of td.results) collectRefsFromValType(r, used);
      break;
    case "struct":
      if (td.superTypeIdx !== void 0) used.add(td.superTypeIdx);
      for (const f of td.fields) collectRefsFromValType(f.type, used);
      break;
    case "array":
      collectRefsFromValType(td.element, used);
      break;
    case "rec":
      for (const inner of td.types) collectRefsFromTypeDef(inner, used);
      break;
    case "sub":
      if (td.superType !== null) used.add(td.superType);
      collectRefsFromTypeDef(td.type, used);
      break;
  }
}
function remapFuncIdxInBody(body, remap) {
  walkInstructions(body, (instr) => {
    const a = instr;
    if (typeof a.funcIdx === "number" && remap.has(a.funcIdx)) {
      a.funcIdx = remap.get(a.funcIdx);
    }
  });
}
function remapTypeIdxInBody(body, remap) {
  walkInstructions(body, (instr) => {
    const a = instr;
    if (typeof a.typeIdx === "number" && remap.has(a.typeIdx)) {
      a.typeIdx = remap.get(a.typeIdx);
    }
    if (typeof a.dstTypeIdx === "number" && remap.has(a.dstTypeIdx)) {
      a.dstTypeIdx = remap.get(a.dstTypeIdx);
    }
    if (typeof a.srcTypeIdx === "number" && remap.has(a.srcTypeIdx)) {
      a.srcTypeIdx = remap.get(a.srcTypeIdx);
    }
    if (a.blockType) {
      if (a.blockType.kind === "type" && remap.has(a.blockType.typeIdx)) {
        a.blockType.typeIdx = remap.get(a.blockType.typeIdx);
      }
      if (a.blockType.kind === "val" && a.blockType.type) {
        a.blockType.type = remapVT(a.blockType.type, remap);
      }
    }
  });
}
function remapVT(vt, remap) {
  if ((vt.kind === "ref" || vt.kind === "ref_null") && typeof vt.typeIdx === "number") {
    const old = vt.typeIdx;
    if (remap.has(old)) {
      return { ...vt, typeIdx: remap.get(old) };
    }
  }
  return vt;
}
function remapTD(td, remap) {
  switch (td.kind) {
    case "func":
      return {
        ...td,
        params: td.params.map((p) => remapVT(p, remap)),
        results: td.results.map((r) => remapVT(r, remap)),
      };
    case "struct": {
      const r = {
        ...td,
        fields: td.fields.map((f) => ({ ...f, type: remapVT(f.type, remap) })),
      };
      if (td.superTypeIdx !== void 0 && remap.has(td.superTypeIdx)) {
        r.superTypeIdx = remap.get(td.superTypeIdx);
      }
      return r;
    }
    case "array":
      return { ...td, element: remapVT(td.element, remap) };
    case "rec":
      return {
        ...td,
        types: td.types.map((t) => remapTD(t, remap)),
      };
    case "sub": {
      const r = {
        ...td,
        type: remapTD(td.type, remap),
      };
      if (td.superType !== null && remap.has(td.superType)) {
        r.superType = remap.get(td.superType);
      }
      return r;
    }
  }
}
function eliminateDeadImports(mod) {
  const numImpF = mod.imports.filter((i) => i.desc.kind === "func").length;
  const usedF = /* @__PURE__ */ new Set();
  const usedT = /* @__PURE__ */ new Set();
  for (let i = 0; i < mod.functions.length; i++) {
    usedF.add(numImpF + i);
  }
  for (const func of mod.functions) {
    collectRefsFromBody(func.body, usedF, usedT);
    usedT.add(func.typeIdx);
    for (const l of func.locals) collectRefsFromValType(l.type, usedT);
  }
  for (const g of mod.globals) {
    collectRefsFromBody(g.init, usedF, usedT);
    collectRefsFromValType(g.type, usedT);
  }
  for (const el of mod.elements) {
    for (const fi of el.funcIndices) usedF.add(fi);
    collectRefsFromBody(el.offset, usedF, usedT);
  }
  for (const ex of mod.exports) {
    if (ex.desc.kind === "func") usedF.add(ex.desc.index);
  }
  for (const fi of mod.declaredFuncRefs) usedF.add(fi);
  for (const tag of mod.tags) usedT.add(tag.typeIdx);
  for (const imp of mod.imports) {
    if (imp.desc.kind === "tag") usedT.add(imp.desc.typeIdx);
    if (imp.desc.kind === "global") collectRefsFromValType(imp.desc.type, usedT);
  }
  let fi2 = 0;
  const impFI = [];
  const deadF = /* @__PURE__ */ new Set();
  for (let i = 0; i < mod.imports.length; i++) {
    if (mod.imports[i].desc.kind === "func") {
      impFI.push(fi2);
      if (!usedF.has(fi2)) deadF.add(fi2);
      fi2++;
    } else {
      impFI.push(-1);
    }
  }
  for (let i = 0; i < mod.imports.length; i++) {
    const imp = mod.imports[i];
    if (imp.desc.kind === "func" && !deadF.has(impFI[i])) {
      usedT.add(imp.desc.typeIdx);
    }
  }
  let chg = true;
  while (chg) {
    chg = false;
    for (const ti of [...usedT]) {
      const td = mod.types[ti];
      if (!td) continue;
      const b = usedT.size;
      collectRefsFromTypeDef(td, usedT);
      if (usedT.size > b) chg = true;
    }
  }
  const fR = /* @__PURE__ */ new Map();
  if (deadF.size > 0) {
    let n = 0;
    for (let o = 0; o < numImpF + mod.functions.length; o++) {
      if (deadF.has(o)) continue;
      if (o !== n) fR.set(o, n);
      n++;
    }
  }
  const tR = /* @__PURE__ */ new Map();
  const surv = [];
  let rem = 0;
  {
    let n = 0;
    for (let o = 0; o < mod.types.length; o++) {
      if (!usedT.has(o)) {
        rem++;
        continue;
      }
      if (o !== n) tR.set(o, n);
      surv.push(mod.types[o]);
      n++;
    }
  }
  if (fR.size === 0 && tR.size === 0 && deadF.size === 0 && rem === 0) {
    return;
  }
  if (deadF.size > 0) {
    let idx = 0;
    mod.imports = mod.imports.filter((imp) => {
      if (imp.desc.kind === "func") {
        const dead = deadF.has(idx);
        idx++;
        return !dead;
      }
      return true;
    });
  }
  if (rem > 0) {
    mod.types = surv.map((td) => (tR.size > 0 ? remapTD(td, tR) : td));
  }
  for (const func of mod.functions) {
    if (fR.size > 0) remapFuncIdxInBody(func.body, fR);
    if (tR.size > 0) remapTypeIdxInBody(func.body, tR);
    if (tR.has(func.typeIdx)) func.typeIdx = tR.get(func.typeIdx);
    if (tR.size > 0) {
      for (let i = 0; i < func.locals.length; i++) {
        func.locals[i] = {
          ...func.locals[i],
          type: remapVT(func.locals[i].type, tR),
        };
      }
    }
  }
  for (const imp of mod.imports) {
    if (imp.desc.kind === "func" && tR.has(imp.desc.typeIdx)) {
      imp.desc = {
        ...imp.desc,
        typeIdx: tR.get(imp.desc.typeIdx),
      };
    }
    if (imp.desc.kind === "tag" && tR.has(imp.desc.typeIdx)) {
      imp.desc = {
        ...imp.desc,
        typeIdx: tR.get(imp.desc.typeIdx),
      };
    }
    if (imp.desc.kind === "global" && tR.size > 0) {
      imp.desc = {
        ...imp.desc,
        type: remapVT(imp.desc.type, tR),
      };
    }
  }
  for (const ex of mod.exports) {
    if (ex.desc.kind === "func" && fR.has(ex.desc.index)) {
      ex.desc = {
        ...ex.desc,
        index: fR.get(ex.desc.index),
      };
    }
  }
  for (const el of mod.elements) {
    el.funcIndices = el.funcIndices.map((f) => fR.get(f) ?? f);
    if (fR.size > 0) remapFuncIdxInBody(el.offset, fR);
    if (tR.size > 0) remapTypeIdxInBody(el.offset, tR);
  }
  mod.declaredFuncRefs = mod.declaredFuncRefs.map((f) => fR.get(f) ?? f);
  for (const g of mod.globals) {
    if (tR.size > 0) g.type = remapVT(g.type, tR);
    if (fR.size > 0) remapFuncIdxInBody(g.init, fR);
    if (tR.size > 0) remapTypeIdxInBody(g.init, tR);
  }
  for (const tag of mod.tags) {
    if (tR.has(tag.typeIdx)) tag.typeIdx = tR.get(tag.typeIdx);
  }
}

// src/codegen/peephole.ts
function optimizeBody(body) {
  let removed = 0;
  for (const instr of body) {
    switch (instr.op) {
      case "block":
      case "loop":
        if (instr.body) removed += optimizeBody(instr.body);
        break;
      case "if":
        if (instr.then) removed += optimizeBody(instr.then);
        if (instr.else) removed += optimizeBody(instr.else);
        break;
      case "try":
        if (instr.body) removed += optimizeBody(instr.body);
        if (instr.catches) {
          for (const c of instr.catches) {
            if (c.body) removed += optimizeBody(c.body);
          }
        }
        break;
    }
  }
  let i = 0;
  while (i < body.length - 1) {
    if (body[i].op === "ref.cast" && body[i + 1].op === "ref.as_non_null") {
      body.splice(i + 1, 1);
      removed++;
    } else {
      i++;
    }
  }
  return removed;
}
function peepholeOptimize(mod) {
  let totalRemoved = 0;
  for (const func of mod.functions) {
    totalRemoved += optimizeBody(func.body);
  }
  return totalRemoved;
}

// src/codegen/stack-balance.ts
var UNREACHABLE = -999;
function isTerminator(op) {
  return (
    op === "return" ||
    op === "return_call" ||
    op === "return_call_ref" ||
    op === "br" ||
    op === "throw" ||
    op === "rethrow" ||
    op === "unreachable"
  );
}
function eliminateDeadCode(body) {
  let removed = 0;
  for (const instr of body) {
    if (instr.op === "if") {
      const ifInstr = instr;
      removed += eliminateDeadCode(ifInstr.then);
      if (ifInstr.else) removed += eliminateDeadCode(ifInstr.else);
    } else if (instr.op === "block" || instr.op === "loop") {
      const blockInstr = instr;
      removed += eliminateDeadCode(blockInstr.body);
    } else if (instr.op === "try") {
      const tryInstr = instr;
      removed += eliminateDeadCode(tryInstr.body);
      for (const c of tryInstr.catches || []) {
        removed += eliminateDeadCode(c.body);
      }
      if (tryInstr.catchAll) removed += eliminateDeadCode(tryInstr.catchAll);
    }
  }
  for (let i = 0; i < body.length; i++) {
    if (isTerminator(body[i].op)) {
      const deadCount = body.length - (i + 1);
      if (deadCount > 0) {
        body.splice(i + 1, deadCount);
        removed += deadCount;
      }
      break;
    }
  }
  return removed;
}
function resolveFuncType(types, typeIdx) {
  const t = types[typeIdx];
  if (!t) return null;
  if (t.kind === "func") return t;
  if (t.kind === "rec") {
    for (const sub2 of t.types) {
      if (sub2.kind === "sub" && sub2.type.kind === "func") return sub2.type;
      if (sub2.kind === "func") return sub2;
    }
  }
  if (t.kind === "sub" && t.type?.kind === "func") {
    return t.type;
  }
  return null;
}
function instrDelta(instr, types, funcSigs) {
  const op = instr.op;
  if (
    op === "return" ||
    op === "return_call" ||
    op === "return_call_ref" ||
    op === "br" ||
    op === "throw" ||
    op === "rethrow" ||
    op === "unreachable"
  ) {
    return UNREACHABLE;
  }
  if (
    op === "i32.const" ||
    op === "i64.const" ||
    op === "f64.const" ||
    op === "f32.const" ||
    op === "v128.const" ||
    op === "local.get" ||
    op === "global.get" ||
    op === "ref.null" ||
    op === "ref.null.extern" ||
    op === "ref.null.eq" ||
    op === "ref.null.func" ||
    op === "ref.func" ||
    op === "memory.size"
  ) {
    return 1;
  }
  if (
    op === "local.tee" ||
    op === "ref.as_non_null" ||
    op === "ref.cast" ||
    op === "ref.cast_null" ||
    op === "ref.test" ||
    op === "ref.is_null" ||
    op === "i32.eqz" ||
    op === "i32.clz" ||
    op === "f64.neg" ||
    op === "f64.abs" ||
    op === "f64.floor" ||
    op === "f64.ceil" ||
    op === "f64.trunc" ||
    op === "f64.nearest" ||
    op === "f64.sqrt" ||
    op === "f64.convert_i32_s" ||
    op === "f64.convert_i32_u" ||
    op === "f64.convert_i64_s" ||
    op === "f64.promote_f32" ||
    op === "f32.demote_f64" ||
    op === "f64.reinterpret_i64" ||
    op === "i64.reinterpret_f64" ||
    op === "i32.trunc_sat_f64_s" ||
    op === "i32.trunc_sat_f64_u" ||
    op === "i32.trunc_f64_s" ||
    op === "i64.trunc_sat_f64_s" ||
    op === "i64.trunc_f64_s" ||
    op === "i64.extend_i32_s" ||
    op === "i64.extend_i32_u" ||
    op === "i32.wrap_i64" ||
    op === "extern.convert_any" ||
    op === "array.len" ||
    op === "memory.grow" ||
    op === "v128.not" ||
    op === "v128.any_true" ||
    op === "i8x16.splat" ||
    op === "i16x8.splat" ||
    op === "i32x4.splat" ||
    op === "i64x2.splat" ||
    op === "f32x4.splat" ||
    op === "f64x2.splat" ||
    op === "i8x16.all_true" ||
    op === "i8x16.bitmask" ||
    op === "i16x8.all_true" ||
    op === "i16x8.bitmask" ||
    op === "i32x4.all_true" ||
    op === "i32x4.bitmask" ||
    op === "nop"
  ) {
    return 0;
  }
  if (op === "drop" || op === "local.set" || op === "global.set") {
    return -1;
  }
  if (
    op === "i32.add" ||
    op === "i32.sub" ||
    op === "i32.mul" ||
    op === "i32.div_s" ||
    op === "i32.div_u" ||
    op === "i32.rem_s" ||
    op === "i32.rem_u" ||
    op === "i32.and" ||
    op === "i32.or" ||
    op === "i32.xor" ||
    op === "i32.shl" ||
    op === "i32.shr_s" ||
    op === "i32.shr_u" ||
    op === "i32.eq" ||
    op === "i32.ne" ||
    op === "i32.lt_s" ||
    op === "i32.le_s" ||
    op === "i32.gt_s" ||
    op === "i32.ge_s" ||
    op === "i32.lt_u" ||
    op === "i32.le_u" ||
    op === "i32.gt_u" ||
    op === "i32.ge_u" ||
    op === "i64.add" ||
    op === "i64.sub" ||
    op === "i64.mul" ||
    op === "i64.div_s" ||
    op === "i64.rem_s" ||
    op === "i64.and" ||
    op === "i64.or" ||
    op === "i64.xor" ||
    op === "i64.shl" ||
    op === "i64.shr_s" ||
    op === "i64.shr_u" ||
    op === "i64.eq" ||
    op === "i64.ne" ||
    op === "i64.lt_s" ||
    op === "i64.le_s" ||
    op === "i64.gt_s" ||
    op === "i64.ge_s" ||
    op === "f64.add" ||
    op === "f64.sub" ||
    op === "f64.mul" ||
    op === "f64.div" ||
    op === "f64.eq" ||
    op === "f64.ne" ||
    op === "f64.lt" ||
    op === "f64.le" ||
    op === "f64.gt" ||
    op === "f64.ge" ||
    op === "f64.copysign" ||
    op === "f64.min" ||
    op === "f64.max" ||
    op === "ref.eq"
  ) {
    return -1;
  }
  if (op === "br_if") return -1;
  if (op === "select") return -2;
  if (op === "struct.new") {
    const typeIdx = instr.typeIdx;
    const t = types[typeIdx];
    if (t && t.kind === "struct") {
      return -t.fields.length + 1;
    }
    return 0;
  }
  if (op === "struct.get") return 0;
  if (op === "struct.set") return -2;
  if (op === "array.new") return -1;
  if (op === "array.new_default") return 0;
  if (op === "array.new_fixed") {
    return -(instr.length || 0) + 1;
  }
  if (op === "array.get" || op === "array.get_s" || op === "array.get_u") return -1;
  if (op === "array.set") return -3;
  if (op === "array.copy") return -5;
  if (op === "array.fill") return -4;
  if (op === "call") {
    const funcIdx = instr.funcIdx;
    const sig = funcSigs.get(funcIdx);
    if (sig) {
      return -sig.params + sig.results;
    }
    return 0;
  }
  if (op === "call_ref") {
    const typeIdx = instr.typeIdx;
    const ft = resolveFuncType(types, typeIdx);
    if (ft) {
      return -(ft.params.length + 1) + ft.results.length;
    }
    return 0;
  }
  if (op === "call_indirect") {
    const typeIdx = instr.typeIdx;
    const ft = resolveFuncType(types, typeIdx);
    if (ft) {
      return -(ft.params.length + 1) + ft.results.length;
    }
    return 0;
  }
  if (op === "if" || op === "block" || op === "loop" || op === "try") {
    const bt = instr.blockType;
    if (!bt || bt.kind === "empty") {
      return op === "if" ? -1 : 0;
    }
    if (bt.kind === "val") {
      return op === "if" ? 0 : 1;
    }
    if (bt.kind === "type") {
      const ft = resolveFuncType(types, bt.typeIdx);
      if (ft) {
        const netBlock = -ft.params.length + ft.results.length;
        return op === "if" ? netBlock - 1 : netBlock;
      }
    }
    return op === "if" ? -1 : 0;
  }
  if (
    op.endsWith(".load") ||
    op.includes(".load8") ||
    op.includes(".load16") ||
    op.includes(".load32_zero") ||
    op.includes(".load64_zero") ||
    op.includes("_splat")
  ) {
    return 0;
  }
  if (op.endsWith(".store") || op.includes(".store8") || op.includes(".store16")) {
    return -2;
  }
  if (
    (op.startsWith("i8x16.") ||
      op.startsWith("i16x8.") ||
      op.startsWith("i32x4.") ||
      op.startsWith("i64x2.") ||
      op.startsWith("f32x4.") ||
      op.startsWith("f64x2.")) &&
    (op.includes(".add") ||
      op.includes(".sub") ||
      op.includes(".mul") ||
      op.includes(".div") ||
      op.includes(".eq") ||
      op.includes(".ne") ||
      op.includes(".lt") ||
      op.includes(".gt") ||
      op.includes(".min") ||
      op.includes(".max") ||
      op.includes(".shl") ||
      op.includes(".shr"))
  ) {
    return -1;
  }
  if (op.includes("extract_lane")) return 0;
  if (op.includes("replace_lane")) return -1;
  if (op === "i8x16.shuffle" || op === "i8x16.swizzle") return -1;
  if (op === "v128.bitselect") return -2;
  if (op === "v128.and" || op === "v128.andnot" || op === "v128.or" || op === "v128.xor") {
    return -1;
  }
  return 0;
}
function sequenceDelta(body, types, sigs) {
  let delta = 0;
  for (const instr of body) {
    const d = instrDelta(instr, types, sigs);
    if (d === UNREACHABLE) return UNREACHABLE;
    delta += d;
  }
  return delta;
}
function blockTypeExpected(bt, types) {
  if (bt.kind === "empty") return 0;
  if (bt.kind === "val") return 1;
  if (bt.kind === "type") {
    const ft = resolveFuncType(types, bt.typeIdx);
    if (ft) return -ft.params.length + ft.results.length;
  }
  return 0;
}
function inferLastType(body, types, sigs) {
  for (let i = body.length - 1; i >= 0; i--) {
    const instr = body[i];
    const op = instr.op;
    if (op === "drop" || op === "local.set" || op === "global.set") continue;
    if (
      op === "f64.const" ||
      op === "f64.add" ||
      op === "f64.sub" ||
      op === "f64.mul" ||
      op === "f64.div" ||
      op === "f64.neg" ||
      op === "f64.abs" ||
      op === "f64.floor" ||
      op === "f64.ceil" ||
      op === "f64.trunc" ||
      op === "f64.nearest" ||
      op === "f64.sqrt" ||
      op === "f64.copysign" ||
      op === "f64.min" ||
      op === "f64.max" ||
      op === "f64.convert_i32_s" ||
      op === "f64.convert_i32_u" ||
      op === "f64.convert_i64_s" ||
      op === "f64.promote_f32" ||
      op === "f64.reinterpret_i64"
    ) {
      return "f64";
    }
    if (
      op === "i32.const" ||
      op === "i32.add" ||
      op === "i32.sub" ||
      op === "i32.mul" ||
      op === "i32.and" ||
      op === "i32.or" ||
      op === "i32.xor" ||
      op === "i32.eqz" ||
      op === "i32.eq" ||
      op === "i32.ne" ||
      op === "i32.lt_s" ||
      op === "i32.le_s" ||
      op === "i32.gt_s" ||
      op === "i32.ge_s" ||
      op === "i32.shl" ||
      op === "i32.shr_s" ||
      op === "i32.shr_u" ||
      op === "i32.trunc_sat_f64_s" ||
      op === "i32.trunc_f64_s" ||
      op === "ref.is_null" ||
      op === "ref.test" ||
      op === "ref.eq" ||
      op === "f64.eq" ||
      op === "f64.ne" ||
      op === "f64.lt" ||
      op === "f64.le" ||
      op === "f64.gt" ||
      op === "f64.ge" ||
      op === "i64.eq" ||
      op === "i64.ne"
    ) {
      return "i32";
    }
    if (
      op === "i64.const" ||
      op === "i64.add" ||
      op === "i64.sub" ||
      op === "i64.mul" ||
      op === "i64.and" ||
      op === "i64.or" ||
      op === "i64.xor" ||
      op === "i64.extend_i32_s" ||
      op === "i64.extend_i32_u" ||
      op === "i64.trunc_sat_f64_s" ||
      op === "i64.trunc_f64_s" ||
      op === "i64.shl" ||
      op === "i64.shr_s" ||
      op === "i64.shr_u" ||
      op === "i64.reinterpret_f64"
    ) {
      return "i64";
    }
    if (op === "ref.null.extern" || op === "extern.convert_any") {
      return "externref";
    }
    if (
      op === "struct.new" ||
      op === "array.new" ||
      op === "array.new_default" ||
      op === "array.new_fixed" ||
      op === "ref.cast" ||
      op === "ref.cast_null" ||
      op === "ref.as_non_null" ||
      op === "any.convert_extern"
    ) {
      return "ref";
    }
    if (op === "ref.null") return "ref";
    if (op === "ref.null.eq") return "eqref";
    if (op === "ref.null.func" || op === "ref.func") return "funcref";
    if (op === "local.tee" || op === "local.get" || op === "global.get") return null;
    if (op === "call_ref") {
      const typeIdx = instr.typeIdx;
      if (typeIdx !== void 0) {
        const ft = resolveFuncType(types, typeIdx);
        if (ft && ft.results.length === 1) {
          const rk = ft.results[0].kind;
          if (rk === "f64") return "f64";
          if (rk === "i32") return "i32";
          if (rk === "i64") return "i64";
          if (rk === "externref" || rk === "ref_extern") return "externref";
          if (rk === "ref" || rk === "ref_null") return "ref";
        }
      }
      return null;
    }
    if (op === "f32.const") return "f32";
    if (op === "select") return null;
    if (op === "call") {
      const funcIdx = instr.funcIdx;
      const sig = sigs.get(funcIdx);
      if (
        sig &&
        sig.resultType &&
        (sig.resultType === "f64" ||
          sig.resultType === "i32" ||
          sig.resultType === "i64" ||
          sig.resultType === "externref")
      ) {
        return sig.resultType;
      }
      return null;
    }
    if (op === "if" || op === "block" || op === "loop" || op === "try") {
      const bt = instr.blockType;
      if (bt?.kind === "val") {
        const t = bt.type;
        if (t.kind === "f64") return "f64";
        if (t.kind === "i32") return "i32";
        if (t.kind === "i64") return "i64";
        if (t.kind === "externref" || t.kind === "ref_extern") return "externref";
        if (t.kind === "ref" || t.kind === "ref_null") return "ref";
      }
      if (bt?.kind === "empty") continue;
      return null;
    }
    return null;
  }
  return null;
}
function typesCompatible(produced, expected) {
  if (expected.kind === "externref" || expected.kind === "ref_extern") {
    return produced === "externref";
  }
  if (expected.kind === "f64") return produced === "f64";
  if (expected.kind === "i32") return produced === "i32";
  if (expected.kind === "i64") return produced === "i64";
  if (expected.kind === "f32") return produced === "f32";
  if (expected.kind === "ref" || expected.kind === "ref_null") {
    return produced === "ref" || produced === "eqref";
  }
  if (expected.kind === "anyref") {
    return produced === "ref" || produced === "externref" || produced === "anyref";
  }
  return true;
}
function fixBranchType(body, blockType, types, sigs) {
  if (blockType.kind !== "val") return 0;
  const expectedType = blockType.type;
  const produced = inferLastType(body, types, sigs);
  if (!produced) return 0;
  if (typesCompatible(produced, expectedType)) return 0;
  const fixups = 0;
  if (
    (expectedType.kind === "externref" || expectedType.kind === "ref_extern") &&
    produced !== "externref" &&
    (produced === "ref" || produced === "eqref" || produced === "funcref" || produced === "anyref")
  ) {
    body.push({ op: "extern.convert_any" });
    return 1;
  }
  if ((expectedType.kind === "ref" || expectedType.kind === "ref_null") && produced === "externref") {
    body.push({ op: "any.convert_extern" });
    body.push({ op: "ref.cast_null", typeIdx: expectedType.typeIdx });
    return 1;
  }
  if ((expectedType.kind === "externref" || expectedType.kind === "ref_extern") && produced === "f64") {
    body.push({ op: "drop" });
    body.push({ op: "ref.null.extern" });
    return 1;
  }
  if ((expectedType.kind === "externref" || expectedType.kind === "ref_extern") && produced === "i32") {
    body.push({ op: "drop" });
    body.push({ op: "ref.null.extern" });
    return 1;
  }
  if (expectedType.kind === "f64" && produced === "externref") {
    body.push({ op: "drop" });
    body.push({ op: "f64.const", value: 0 });
    return 1;
  }
  if (expectedType.kind === "f64" && produced === "i64") {
    body.push({ op: "f64.convert_i64_s" });
    return 1;
  }
  if (expectedType.kind === "f64" && produced === "ref") {
    body.push({ op: "drop" });
    body.push({ op: "f64.const", value: 0 });
    return 1;
  }
  if (expectedType.kind === "f64" && produced === "i32") {
    body.push({ op: "f64.convert_i32_s" });
    return 1;
  }
  if (expectedType.kind === "i32" && (produced === "ref" || produced === "externref")) {
    body.push({ op: "drop" });
    body.push({ op: "i32.const", value: 0 });
    return 1;
  }
  if (expectedType.kind === "anyref" && produced === "externref") {
    body.push({ op: "any.convert_extern" });
    return 1;
  }
  return fixups;
}
function fixBranch(body, expected, types, sigs, blockType) {
  const actual = sequenceDelta(body, types, sigs);
  if (actual === UNREACHABLE) return 0;
  let fixups = 0;
  if (actual > expected) {
    for (let i = 0; i < actual - expected; i++) {
      body.push({ op: "drop" });
      fixups++;
    }
  } else if (actual < expected) {
    for (let i = 0; i < expected - actual; i++) {
      if (blockType.kind === "val") {
        const t = blockType.type;
        switch (t.kind) {
          case "i32":
            body.push({ op: "i32.const", value: 0 });
            break;
          case "i64":
            body.push({ op: "i64.const", value: 0n });
            break;
          case "f64":
            body.push({ op: "f64.const", value: 0 });
            break;
          case "f32":
            body.push({ op: "f32.const", value: 0 });
            break;
          case "externref":
            body.push({ op: "ref.null.extern" });
            break;
          case "ref":
          case "ref_null":
            body.push({ op: "ref.null", typeIdx: t.typeIdx });
            break;
          default:
            body.push({ op: "ref.null.extern" });
            break;
        }
      } else {
        body.push({ op: "ref.null.extern" });
      }
      fixups++;
    }
  }
  if (actual === expected || fixups > 0) {
    const newDelta = sequenceDelta(body, types, sigs);
    if (newDelta === expected && newDelta > 0) {
      fixups += fixBranchType(body, blockType, types, sigs);
    }
  }
  return fixups;
}
function getTagArity(tagIdx, tags, types) {
  const tag = tags[tagIdx];
  if (!tag) return 1;
  const ft = resolveFuncType(types, tag.typeIdx);
  if (ft) return ft.params.length;
  return 1;
}
function fixBody(body, types, sigs, tags) {
  let fixups = 0;
  for (const instr of body) {
    if (instr.op === "if") {
      const ifInstr = instr;
      const expected = blockTypeExpected(ifInstr.blockType, types);
      fixups += fixBody(ifInstr.then, types, sigs, tags);
      if (ifInstr.else) {
        fixups += fixBody(ifInstr.else, types, sigs, tags);
      }
      fixups += fixBranch(ifInstr.then, expected, types, sigs, ifInstr.blockType);
      if (ifInstr.else) {
        fixups += fixBranch(ifInstr.else, expected, types, sigs, ifInstr.blockType);
      } else if (expected > 0) {
        ifInstr.else = [];
        fixups += fixBranch(ifInstr.else, expected, types, sigs, ifInstr.blockType);
      }
    } else if (instr.op === "block" || instr.op === "loop") {
      const blockInstr = instr;
      fixups += fixBody(blockInstr.body, types, sigs, tags);
      const expected = blockTypeExpected(blockInstr.blockType, types);
      fixups += fixBranch(blockInstr.body, expected, types, sigs, blockInstr.blockType);
    } else if (instr.op === "try") {
      const tryInstr = instr;
      const expected = blockTypeExpected(tryInstr.blockType, types);
      fixups += fixBody(tryInstr.body, types, sigs, tags);
      for (const c of tryInstr.catches || []) {
        fixups += fixBody(c.body, types, sigs, tags);
      }
      if (tryInstr.catchAll) {
        fixups += fixBody(tryInstr.catchAll, types, sigs, tags);
      }
      fixups += fixBranch(tryInstr.body, expected, types, sigs, tryInstr.blockType);
      for (const c of tryInstr.catches || []) {
        const tagArity = getTagArity(c.tagIdx, tags, types);
        fixups += fixBranch(c.body, expected - tagArity, types, sigs, tryInstr.blockType);
      }
      if (tryInstr.catchAll) {
        fixups += fixBranch(tryInstr.catchAll, expected, types, sigs, tryInstr.blockType);
      }
    }
  }
  return fixups;
}
function valTypeCategory(vt) {
  switch (vt.kind) {
    case "f64":
      return "f64";
    case "i32":
      return "i32";
    case "i64":
      return "i64";
    case "f32":
      return "f32";
    case "externref":
    case "ref_extern":
      return "externref";
    case "ref":
    case "ref_null":
      return "ref";
    case "funcref":
      return "funcref";
    case "eqref":
      return "eqref";
    case "anyref":
      return "anyref";
    default:
      return void 0;
  }
}
function buildFuncSigs(mod) {
  const map = /* @__PURE__ */ new Map();
  let idx = 0;
  for (const imp of mod.imports) {
    if (imp.desc.kind === "func") {
      const ft = resolveFuncType(mod.types, imp.desc.typeIdx);
      if (ft) {
        const resultType = ft.results.length === 1 ? valTypeCategory(ft.results[0]) : void 0;
        map.set(idx, { params: ft.params.length, results: ft.results.length, resultType });
      }
      idx++;
    }
  }
  for (const func of mod.functions) {
    const ft = resolveFuncType(mod.types, func.typeIdx);
    if (ft) {
      const resultType = ft.results.length === 1 ? valTypeCategory(ft.results[0]) : void 0;
      map.set(idx, { params: ft.params.length, results: ft.results.length, resultType });
    }
    idx++;
  }
  return map;
}
function getFullParamTypes(mod, funcIdx, numImports) {
  if (funcIdx < numImports) {
    let importFuncCount = 0;
    for (const imp of mod.imports) {
      if (imp.desc.kind === "func") {
        if (importFuncCount === funcIdx) {
          const ft2 = resolveFuncType(mod.types, imp.desc.typeIdx);
          return ft2 ? ft2.params : null;
        }
        importFuncCount++;
      }
    }
    return null;
  }
  const localIdx = funcIdx - numImports;
  const func = mod.functions[localIdx];
  if (!func) return null;
  const ft = resolveFuncType(mod.types, func.typeIdx);
  return ft ? ft.params : null;
}
function inferInstrType(instr, localTypes, globalTypes, types, mod, numImports) {
  const op = instr.op;
  if (op === "local.get" || op === "local.tee") {
    const idx = instr.index;
    return localTypes[idx] ?? null;
  }
  if (op === "global.get") {
    const idx = instr.index;
    return globalTypes[idx] ?? null;
  }
  if (
    op === "f64.const" ||
    op === "f64.add" ||
    op === "f64.sub" ||
    op === "f64.mul" ||
    op === "f64.div" ||
    op === "f64.neg" ||
    op === "f64.abs" ||
    op === "f64.floor" ||
    op === "f64.ceil" ||
    op === "f64.trunc" ||
    op === "f64.nearest" ||
    op === "f64.sqrt" ||
    op === "f64.copysign" ||
    op === "f64.min" ||
    op === "f64.max" ||
    op === "f64.convert_i32_s" ||
    op === "f64.convert_i32_u" ||
    op === "f64.convert_i64_s" ||
    op === "f64.promote_f32" ||
    op === "f64.reinterpret_i64"
  ) {
    return { kind: "f64" };
  }
  if (
    op === "i32.const" ||
    op === "i32.add" ||
    op === "i32.sub" ||
    op === "i32.mul" ||
    op === "i32.and" ||
    op === "i32.or" ||
    op === "i32.xor" ||
    op === "i32.eqz" ||
    op === "i32.eq" ||
    op === "i32.ne" ||
    op === "i32.lt_s" ||
    op === "i32.le_s" ||
    op === "i32.gt_s" ||
    op === "i32.ge_s" ||
    op === "i32.shl" ||
    op === "i32.shr_s" ||
    op === "i32.shr_u" ||
    op === "i32.trunc_sat_f64_s" ||
    op === "i32.trunc_f64_s" ||
    op === "i32.wrap_i64" ||
    op === "ref.is_null" ||
    op === "ref.test" ||
    op === "ref.eq" ||
    op === "f64.eq" ||
    op === "f64.ne" ||
    op === "f64.lt" ||
    op === "f64.le" ||
    op === "f64.gt" ||
    op === "f64.ge" ||
    op === "i64.eq" ||
    op === "i64.ne" ||
    op === "array.len"
  ) {
    return { kind: "i32" };
  }
  if (op === "i64.const" || op === "i64.extend_i32_s" || op === "i64.trunc_sat_f64_s" || op === "i64.reinterpret_f64") {
    return { kind: "i64" };
  }
  if (op === "ref.null.extern" || op === "extern.convert_any") {
    return { kind: "externref" };
  }
  if (op === "struct.new") {
    const typeIdx = instr.typeIdx;
    return { kind: "ref", typeIdx };
  }
  if (op === "struct.get") {
    const typeIdx = instr.typeIdx;
    const fieldIdx = instr.fieldIdx;
    const td = types[typeIdx];
    if (td?.kind === "struct" && td.fields[fieldIdx]) {
      return td.fields[fieldIdx].type;
    }
    return null;
  }
  if (op === "array.get" || op === "array.get_s" || op === "array.get_u") {
    const typeIdx = instr.typeIdx;
    const td = types[typeIdx];
    if (td?.kind === "array") return td.element;
    return null;
  }
  if (op === "ref.null") {
    const typeIdx = instr.typeIdx;
    return { kind: "ref_null", typeIdx };
  }
  if (op === "ref.cast" || op === "ref.as_non_null") {
    const typeIdx = instr.typeIdx;
    if (typeIdx !== void 0) return { kind: "ref", typeIdx };
    return null;
  }
  if (op === "ref.cast_null") {
    const typeIdx = instr.typeIdx;
    return { kind: "ref_null", typeIdx };
  }
  if (op === "any.convert_extern") {
    return { kind: "anyref" };
  }
  if (op === "if" || op === "block" || op === "loop" || op === "try") {
    const bt = instr.blockType;
    if (bt && bt.kind === "val") return bt.type;
    if (bt && bt.kind === "type") {
      const ft = resolveFuncType(types, bt.typeIdx);
      if (ft && ft.results.length === 1) return ft.results[0];
    }
    return null;
  }
  if (op === "call") {
    const funcIdx = instr.funcIdx;
    const pt = getFullParamTypes(mod, funcIdx, numImports);
    if (funcIdx < numImports) {
      let importFuncCount = 0;
      for (const imp of mod.imports) {
        if (imp.desc.kind === "func") {
          if (importFuncCount === funcIdx) {
            const ft = resolveFuncType(types, imp.desc.typeIdx);
            return ft && ft.results.length === 1 ? ft.results[0] : null;
          }
          importFuncCount++;
        }
      }
    } else {
      const localFuncIdx = funcIdx - numImports;
      const func = mod.functions[localFuncIdx];
      if (func) {
        const ft = resolveFuncType(types, func.typeIdx);
        return ft && ft.results.length === 1 ? ft.results[0] : null;
      }
    }
    return null;
  }
  return null;
}
function callArgCoercionInstrs(actual, expected, boxNumberIdx, unboxNumberIdx) {
  if (actual.kind === expected.kind) {
    if (
      (actual.kind === "ref" || actual.kind === "ref_null") &&
      (expected.kind === "ref" || expected.kind === "ref_null")
    ) {
      const actualIdx = actual.typeIdx;
      const expectedIdx = expected.typeIdx;
      if (actualIdx === expectedIdx) return [];
      if (expectedIdx !== void 0) {
        return [{ op: "ref.cast_null", typeIdx: expectedIdx }];
      }
    } else {
      return [];
    }
  }
  const actualIsExternref = actual.kind === "externref" || actual.kind === "ref_extern";
  const expectedIsExternref = expected.kind === "externref" || expected.kind === "ref_extern";
  if (actualIsExternref && expectedIsExternref) return [];
  if (
    (actual.kind === "ref" || actual.kind === "ref_null" || actual.kind === "anyref" || actual.kind === "eqref") &&
    (expected.kind === "externref" || expected.kind === "ref_extern")
  ) {
    return [{ op: "extern.convert_any" }];
  }
  if (actual.kind === "f64" && expected.kind === "externref" && boxNumberIdx !== null) {
    return [{ op: "call", funcIdx: boxNumberIdx }];
  }
  if (actual.kind === "i32" && expected.kind === "externref" && boxNumberIdx !== null) {
    return [{ op: "f64.convert_i32_s" }, { op: "call", funcIdx: boxNumberIdx }];
  }
  if (actual.kind === "i64" && expected.kind === "externref" && boxNumberIdx !== null) {
    return [{ op: "f64.convert_i64_s" }, { op: "call", funcIdx: boxNumberIdx }];
  }
  if (actual.kind === "externref" && expected.kind === "f64" && unboxNumberIdx !== null) {
    return [{ op: "call", funcIdx: unboxNumberIdx }];
  }
  if ((actual.kind === "ref" || actual.kind === "ref_null") && expected.kind === "f64" && unboxNumberIdx !== null) {
    return [{ op: "extern.convert_any" }, { op: "call", funcIdx: unboxNumberIdx }];
  }
  if (actual.kind === "i64" && expected.kind === "i32") {
    return [{ op: "i32.wrap_i64" }];
  }
  if (actual.kind === "i32" && expected.kind === "f64") {
    return [{ op: "f64.convert_i32_s" }];
  }
  if (actual.kind === "i64" && expected.kind === "f64") {
    return [{ op: "f64.convert_i64_s" }];
  }
  if (actual.kind === "i32" && expected.kind === "i64") {
    return [{ op: "i64.extend_i32_s" }];
  }
  if (actualIsExternref && (expected.kind === "ref" || expected.kind === "ref_null")) {
    const typeIdx = expected.typeIdx;
    if (typeIdx !== void 0) {
      return [{ op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx }];
    }
  }
  if ((actual.kind === "ref" || actual.kind === "ref_null") && expected.kind === "i32" && unboxNumberIdx !== null) {
    return [{ op: "extern.convert_any" }, { op: "call", funcIdx: unboxNumberIdx }, { op: "i32.trunc_sat_f64_s" }];
  }
  if (actualIsExternref && expected.kind === "i32" && unboxNumberIdx !== null) {
    return [{ op: "call", funcIdx: unboxNumberIdx }, { op: "i32.trunc_sat_f64_s" }];
  }
  return [];
}
function fixCallArgTypesInBody(
  body,
  localTypes,
  globalTypes,
  types,
  mod,
  numImports,
  sigs,
  boxNumberIdx,
  unboxNumberIdx,
) {
  let fixups = 0;
  for (const instr of body) {
    if (instr.op === "if") {
      const ifInstr = instr;
      if (ifInstr.then)
        fixups += fixCallArgTypesInBody(
          ifInstr.then,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
      if (ifInstr.else)
        fixups += fixCallArgTypesInBody(
          ifInstr.else,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
    } else if (instr.op === "block" || instr.op === "loop") {
      const blockInstr = instr;
      if (blockInstr.body)
        fixups += fixCallArgTypesInBody(
          blockInstr.body,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
    } else if (instr.op === "try") {
      const tryInstr = instr;
      if (tryInstr.body)
        fixups += fixCallArgTypesInBody(
          tryInstr.body,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
      if (tryInstr.catches) {
        for (const c of tryInstr.catches) {
          if (c.body)
            fixups += fixCallArgTypesInBody(
              c.body,
              localTypes,
              globalTypes,
              types,
              mod,
              numImports,
              sigs,
              boxNumberIdx,
              unboxNumberIdx,
            );
        }
      }
      if (tryInstr.catchAll)
        fixups += fixCallArgTypesInBody(
          tryInstr.catchAll,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
    }
  }
  const SIMPLE_PRODUCERS = /* @__PURE__ */ new Set([
    "local.get",
    "global.get",
    "local.tee",
    "struct.new",
    "ref.null",
    "struct.get",
    "array.get",
    "array.get_s",
    "array.get_u",
    "call",
    "ref.cast",
    "ref.cast_null",
    "ref.as_non_null",
    "array.new_default",
    "array.new",
    "array.new_fixed",
    // f64/f32 constants — safe, never used as array/struct indices
    "f64.const",
    "f32.const",
    // Ref producers — safe, rarely used as sub-expression inputs
    "ref.null.extern",
    "ref.null.eq",
    "ref.null.func",
    "ref.func",
    // i32/i64 constants — safe when directly before call
    "i32.const",
    "i64.const",
    // i32 arithmetic/comparison — these produce i32 results
    "i32.add",
    "i32.sub",
    "i32.mul",
    "i32.div_s",
    "i32.div_u",
    "i32.rem_s",
    "i32.rem_u",
    "i32.and",
    "i32.or",
    "i32.xor",
    "i32.shl",
    "i32.shr_s",
    "i32.shr_u",
    "i32.eq",
    "i32.ne",
    "i32.lt_s",
    "i32.le_s",
    "i32.gt_s",
    "i32.ge_s",
    "i32.lt_u",
    "i32.le_u",
    "i32.gt_u",
    "i32.ge_u",
    "i32.eqz",
    "i32.clz",
    "i32.wrap_i64",
    "i32.trunc_sat_f64_s",
    "i32.trunc_sat_f64_u",
    "i32.trunc_f64_s",
    // i64 arithmetic — these produce i64 results
    "i64.add",
    "i64.sub",
    "i64.mul",
    "i64.div_s",
    "i64.rem_s",
    "i64.and",
    "i64.or",
    "i64.xor",
    "i64.shl",
    "i64.shr_s",
    "i64.shr_u",
    "i64.eq",
    "i64.ne",
    "i64.lt_s",
    "i64.le_s",
    "i64.gt_s",
    "i64.ge_s",
    "i64.eqz",
    "i64.extend_i32_s",
    "i64.extend_i32_u",
    "i64.trunc_sat_f64_s",
    "i64.trunc_f64_s",
    // f64 arithmetic — these produce f64 results
    "f64.add",
    "f64.sub",
    "f64.mul",
    "f64.div",
    "f64.neg",
    "f64.abs",
    "f64.floor",
    "f64.ceil",
    "f64.trunc",
    "f64.nearest",
    "f64.sqrt",
    "f64.copysign",
    "f64.min",
    "f64.max",
    "f64.convert_i32_s",
    "f64.convert_i32_u",
    "f64.convert_i64_s",
    "f64.promote_f32",
    "f64.reinterpret_i64",
    "i64.reinterpret_f64",
    "f64.eq",
    "f64.ne",
    "f64.lt",
    "f64.le",
    "f64.gt",
    "f64.ge",
    // Other type-producing ops
    "ref.is_null",
    "ref.test",
    "ref.eq",
    "array.len",
    "any.convert_extern",
    "extern.convert_any",
  ]);
  for (let ci = 0; ci < body.length; ci++) {
    const callInstr = body[ci];
    const isCall = callInstr.op === "call" || callInstr.op === "return_call";
    const isCallRef = callInstr.op === "call_ref";
    if (!isCall && !isCallRef) continue;
    let expectedParams;
    if (isCallRef) {
      const typeIdx = callInstr.typeIdx;
      const ft = resolveFuncType(types, typeIdx);
      expectedParams = ft ? ft.params : null;
    } else {
      const funcIdx = callInstr.funcIdx;
      expectedParams = getFullParamTypes(mod, funcIdx, numImports);
    }
    if (!expectedParams || expectedParams.length === 0) continue;
    const paramCount = expectedParams.length;
    let argOffset = isCallRef ? -1 : 0;
    let pos = ci - 1;
    const insertions = [];
    let inSubExpr = false;
    while (pos >= 0 && argOffset < paramCount) {
      const instr = body[pos];
      const op = instr.op;
      if (
        op === "if" ||
        op === "block" ||
        op === "loop" ||
        op === "try" ||
        op === "end" ||
        op === "br" ||
        op === "br_if" ||
        op === "br_table" ||
        op === "return" ||
        op === "throw" ||
        op === "unreachable" ||
        op === "return_call" ||
        op === "return_call_ref"
      ) {
        break;
      }
      const delta = instrDelta(instr, types, sigs);
      if (delta === UNREACHABLE) break;
      const producesValue =
        SIMPLE_PRODUCERS.has(op) && inferInstrType(instr, localTypes, globalTypes, types, mod, numImports) !== null;
      if (producesValue && argOffset >= 0) {
        let effectiveType = inferInstrType(instr, localTypes, globalTypes, types, mod, numImports);
        let insertPos = pos;
        for (let t = pos + 1; t < ci; t++) {
          const tInstr = body[t];
          const tDelta = instrDelta(tInstr, types, sigs);
          if (tDelta !== 0) break;
          const tType = inferInstrType(tInstr, localTypes, globalTypes, types, mod, numImports);
          if (tType) effectiveType = tType;
          insertPos = t;
        }
        const paramIdx = paramCount - 1 - argOffset;
        const expectedType = expectedParams[paramIdx];
        if (effectiveType && expectedType) {
          const coercion = callArgCoercionInstrs(effectiveType, expectedType, boxNumberIdx, unboxNumberIdx);
          if (coercion.length > 0) {
            const isSafeRefToExtern = coercion.length === 1 && coercion[0].op === "extern.convert_any";
            if (!inSubExpr || isSafeRefToExtern) {
              insertions.push({ afterPos: insertPos, instrs: coercion });
            }
          }
        }
      }
      if (delta >= 1) {
        argOffset += delta;
      } else if (delta < 0) {
        if (producesValue) {
          argOffset += 1;
          if (delta < -1) {
          }
          argOffset += delta - 1;
        } else {
          argOffset += delta;
        }
        inSubExpr = true;
        if (argOffset < (isCallRef ? -1 : 0)) break;
      }
      pos--;
    }
    if (insertions.length > 0) {
      for (let k = insertions.length - 1; k >= 0; k--) {
        const { afterPos, instrs } = insertions[k];
        body.splice(afterPos + 1, 0, ...instrs);
        ci += instrs.length;
        fixups += instrs.length;
      }
    }
  }
  return fixups;
}
function fixStructNewFieldCoercion(
  func,
  types,
  mod,
  numImports,
  sigs,
  localTypes,
  globalTypes,
  boxNumberIdx,
  unboxNumberIdx,
) {
  let fixups = 0;
  function processBody(body) {
    for (const instr of body) {
      if (instr.op === "if") {
        const ifInstr = instr;
        if (ifInstr.then) processBody(ifInstr.then);
        if (ifInstr.else) processBody(ifInstr.else);
      } else if (instr.op === "block" || instr.op === "loop") {
        const blockInstr = instr;
        if (blockInstr.body) processBody(blockInstr.body);
      } else if (instr.op === "try") {
        const tryInstr = instr;
        if (tryInstr.body) processBody(tryInstr.body);
        if (tryInstr.catches) {
          for (const c of tryInstr.catches) {
            if (c.body) processBody(c.body);
          }
        }
        if (tryInstr.catchAll) processBody(tryInstr.catchAll);
      }
    }
    const typeStack = [];
    for (let ci = 0; ci < body.length; ci++) {
      const instr = body[ci];
      const op = instr.op;
      if (op === "struct.new") {
        const typeIdx = instr.typeIdx;
        const typeDef = types[typeIdx];
        if (typeDef?.kind === "struct") {
          const fields = typeDef.fields;
          const numFields = fields.length;
          if (numFields > 0 && typeStack.length >= numFields) {
            const fieldTypes = [];
            for (let fi = 0; fi < numFields; fi++) {
              fieldTypes.push(typeStack[typeStack.length - numFields + fi] ?? null);
            }
            let needsCoercion = false;
            const coercions = [];
            for (let fi = 0; fi < numFields; fi++) {
              const actual = fieldTypes[fi];
              const expected = fields[fi].type;
              if (actual) {
                const c = callArgCoercionInstrs(actual, expected, boxNumberIdx, unboxNumberIdx);
                coercions.push(c);
                if (c.length > 0) needsCoercion = true;
              } else {
                coercions.push([]);
              }
            }
            if (needsCoercion) {
              const tempLocals = [];
              const paramCount = resolveFuncType(types, func.typeIdx)?.params.length ?? 0;
              for (let fi = 0; fi < numFields; fi++) {
                const actualType = fieldTypes[fi] ?? fields[fi].type;
                const localIdx = paramCount + func.locals.length;
                func.locals.push({ name: `$sn_tmp_${localIdx}`, type: actualType });
                localTypes.push(actualType);
                tempLocals.push(localIdx);
              }
              const saveInstrs = [];
              for (let fi = numFields - 1; fi >= 0; fi--) {
                saveInstrs.push({ op: "local.set", index: tempLocals[fi] });
              }
              const restoreInstrs = [];
              for (let fi = 0; fi < numFields; fi++) {
                restoreInstrs.push({ op: "local.get", index: tempLocals[fi] });
                for (const c of coercions[fi]) {
                  restoreInstrs.push(c);
                }
              }
              const insertedInstrs = [...saveInstrs, ...restoreInstrs];
              body.splice(ci, 0, ...insertedInstrs);
              ci += insertedInstrs.length;
              fixups += insertedInstrs.length;
            }
          }
          for (let i = 0; i < typeDef.fields.length; i++) typeStack.pop();
          typeStack.push({ kind: "ref", typeIdx });
        }
        continue;
      }
      updateTypeStack(typeStack, instr, types, sigs, localTypes, globalTypes, mod, numImports);
    }
  }
  processBody(func.body);
  return fixups;
}
function updateTypeStack(stack, instr, types, sigs, localTypes, globalTypes, mod, numImports) {
  const op = instr.op;
  if (
    op === "return" ||
    op === "return_call" ||
    op === "return_call_ref" ||
    op === "br" ||
    op === "throw" ||
    op === "rethrow" ||
    op === "unreachable"
  ) {
    stack.length = 0;
    return;
  }
  if (op === "local.get") {
    const idx = instr.index;
    stack.push(localTypes[idx] ?? null);
    return;
  }
  if (op === "global.get") {
    const idx = instr.index;
    stack.push(globalTypes[idx] ?? null);
    return;
  }
  if (op === "f64.const") {
    stack.push({ kind: "f64" });
    return;
  }
  if (op === "f32.const") {
    stack.push({ kind: "f32" });
    return;
  }
  if (op === "i32.const") {
    stack.push({ kind: "i32" });
    return;
  }
  if (op === "i64.const") {
    stack.push({ kind: "i64" });
    return;
  }
  if (op === "ref.null") {
    const typeIdx = instr.typeIdx;
    stack.push({ kind: "ref_null", typeIdx });
    return;
  }
  if (op === "ref.null.extern") {
    stack.push({ kind: "externref" });
    return;
  }
  if (op === "ref.null.eq") {
    stack.push({ kind: "eqref" });
    return;
  }
  if (op === "ref.null.func") {
    stack.push({ kind: "funcref" });
    return;
  }
  if (op === "ref.func") {
    stack.push({ kind: "funcref" });
    return;
  }
  if (op === "v128.const") {
    stack.push(null);
    return;
  }
  if (op === "memory.size") {
    stack.push({ kind: "i32" });
    return;
  }
  if (op === "drop" || op === "local.set" || op === "global.set") {
    stack.pop();
    return;
  }
  if (op === "local.tee") {
    return;
  }
  if (op === "extern.convert_any") {
    stack.pop();
    stack.push({ kind: "externref" });
    return;
  }
  if (op === "any.convert_extern") {
    stack.pop();
    stack.push({ kind: "anyref" });
    return;
  }
  if (op === "ref.cast" || op === "ref.as_non_null") {
    stack.pop();
    const typeIdx = instr.typeIdx;
    if (typeIdx !== void 0) {
      stack.push({ kind: "ref", typeIdx });
    } else {
      stack.push(null);
    }
    return;
  }
  if (op === "ref.cast_null") {
    stack.pop();
    const typeIdx = instr.typeIdx;
    stack.push({ kind: "ref_null", typeIdx });
    return;
  }
  if (
    op === "ref.is_null" ||
    op === "ref.test" ||
    op === "i32.eqz" ||
    op === "i32.clz" ||
    op === "i32.wrap_i64" ||
    op === "i32.trunc_sat_f64_s" ||
    op === "i32.trunc_sat_f64_u" ||
    op === "i32.trunc_f64_s" ||
    op === "array.len"
  ) {
    stack.pop();
    stack.push({ kind: "i32" });
    return;
  }
  if (
    op === "f64.neg" ||
    op === "f64.abs" ||
    op === "f64.floor" ||
    op === "f64.ceil" ||
    op === "f64.trunc" ||
    op === "f64.nearest" ||
    op === "f64.sqrt" ||
    op === "f64.convert_i32_s" ||
    op === "f64.convert_i32_u" ||
    op === "f64.convert_i64_s" ||
    op === "f64.promote_f32" ||
    op === "f64.reinterpret_i64"
  ) {
    stack.pop();
    stack.push({ kind: "f64" });
    return;
  }
  if (op === "f32.demote_f64") {
    stack.pop();
    stack.push({ kind: "f32" });
    return;
  }
  if (
    op === "i64.extend_i32_s" ||
    op === "i64.extend_i32_u" ||
    op === "i64.trunc_sat_f64_s" ||
    op === "i64.trunc_f64_s" ||
    op === "i64.reinterpret_f64"
  ) {
    stack.pop();
    stack.push({ kind: "i64" });
    return;
  }
  if (
    op === "i32.add" ||
    op === "i32.sub" ||
    op === "i32.mul" ||
    op === "i32.div_s" ||
    op === "i32.div_u" ||
    op === "i32.rem_s" ||
    op === "i32.rem_u" ||
    op === "i32.and" ||
    op === "i32.or" ||
    op === "i32.xor" ||
    op === "i32.shl" ||
    op === "i32.shr_s" ||
    op === "i32.shr_u" ||
    op === "i32.eq" ||
    op === "i32.ne" ||
    op === "i32.lt_s" ||
    op === "i32.le_s" ||
    op === "i32.gt_s" ||
    op === "i32.ge_s" ||
    op === "i32.lt_u" ||
    op === "i32.le_u" ||
    op === "i32.gt_u" ||
    op === "i32.ge_u" ||
    op === "ref.eq" ||
    op === "f64.eq" ||
    op === "f64.ne" ||
    op === "f64.lt" ||
    op === "f64.le" ||
    op === "f64.gt" ||
    op === "f64.ge" ||
    op === "i64.eq" ||
    op === "i64.ne"
  ) {
    stack.pop();
    stack.pop();
    stack.push({ kind: "i32" });
    return;
  }
  if (
    op === "i64.add" ||
    op === "i64.sub" ||
    op === "i64.mul" ||
    op === "i64.div_s" ||
    op === "i64.rem_s" ||
    op === "i64.and" ||
    op === "i64.or" ||
    op === "i64.xor" ||
    op === "i64.shl" ||
    op === "i64.shr_s" ||
    op === "i64.shr_u" ||
    op === "i64.lt_s" ||
    op === "i64.le_s" ||
    op === "i64.gt_s" ||
    op === "i64.ge_s"
  ) {
    stack.pop();
    stack.pop();
    stack.push({ kind: "i64" });
    return;
  }
  if (
    op === "f64.add" ||
    op === "f64.sub" ||
    op === "f64.mul" ||
    op === "f64.div" ||
    op === "f64.copysign" ||
    op === "f64.min" ||
    op === "f64.max"
  ) {
    stack.pop();
    stack.pop();
    stack.push({ kind: "f64" });
    return;
  }
  if (op === "select") {
    stack.pop();
    stack.pop();
    const val1 = stack.pop() ?? null;
    stack.push(val1);
    return;
  }
  if (op === "struct.get") {
    stack.pop();
    const typeIdx = instr.typeIdx;
    const fieldIdx = instr.fieldIdx;
    const td = types[typeIdx];
    if (td?.kind === "struct" && td.fields[fieldIdx]) {
      stack.push(td.fields[fieldIdx].type);
    } else {
      stack.push(null);
    }
    return;
  }
  if (op === "struct.set") {
    stack.pop();
    stack.pop();
    return;
  }
  if (op === "array.get" || op === "array.get_s" || op === "array.get_u") {
    stack.pop();
    stack.pop();
    const typeIdx = instr.typeIdx;
    const td = types[typeIdx];
    if (td?.kind === "array") {
      stack.push(td.element);
    } else {
      stack.push(null);
    }
    return;
  }
  if (op === "array.set") {
    stack.pop();
    stack.pop();
    stack.pop();
    return;
  }
  if (op === "array.new") {
    stack.pop();
    stack.pop();
    const typeIdx = instr.typeIdx;
    stack.push({ kind: "ref", typeIdx });
    return;
  }
  if (op === "array.new_default") {
    stack.pop();
    const typeIdx = instr.typeIdx;
    stack.push({ kind: "ref", typeIdx });
    return;
  }
  if (op === "array.new_fixed") {
    const len = instr.length || 0;
    for (let i = 0; i < len; i++) stack.pop();
    const typeIdx = instr.typeIdx;
    stack.push({ kind: "ref", typeIdx });
    return;
  }
  if (op === "array.copy") {
    for (let i = 0; i < 5; i++) stack.pop();
    return;
  }
  if (op === "array.fill") {
    for (let i = 0; i < 4; i++) stack.pop();
    return;
  }
  if (op === "call") {
    const funcIdx = instr.funcIdx;
    const sig = sigs.get(funcIdx);
    if (sig) {
      for (let i = 0; i < sig.params; i++) stack.pop();
      if (sig.results > 0) {
        const fIdx = funcIdx - numImports;
        const fn = fIdx >= 0 ? mod.functions[fIdx] : void 0;
        const ft = fn ? resolveFuncType(types, fn.typeIdx) : null;
        if (ft && ft.results.length > 0) {
          for (const r of ft.results) stack.push(r);
        } else {
          let importFuncIdx = 0;
          let foundImport = false;
          for (const imp of mod.imports) {
            if (imp.desc.kind === "func") {
              if (importFuncIdx === funcIdx) {
                const impFt = resolveFuncType(types, imp.desc.typeIdx);
                if (impFt && impFt.results.length > 0) {
                  for (const r of impFt.results) stack.push(r);
                  foundImport = true;
                }
                break;
              }
              importFuncIdx++;
            }
          }
          if (!foundImport) {
            for (let i = 0; i < sig.results; i++) stack.push(null);
          }
        }
      }
    } else {
      stack.push(null);
    }
    return;
  }
  if (op === "call_ref") {
    const typeIdx = instr.typeIdx;
    const ft = resolveFuncType(types, typeIdx);
    if (ft) {
      stack.pop();
      for (let i = 0; i < ft.params.length; i++) stack.pop();
      for (const r of ft.results) stack.push(r);
    } else {
      stack.push(null);
    }
    return;
  }
  if (op === "br_if") {
    stack.pop();
    return;
  }
  if (op === "if" || op === "block" || op === "loop" || op === "try") {
    const bt = instr.blockType;
    if (op === "if") stack.pop();
    if (!bt || bt.kind === "empty") {
    } else if (bt.kind === "val") {
      stack.push(bt.type);
    } else if (bt.kind === "type") {
      const ft = resolveFuncType(types, bt.typeIdx);
      if (ft) {
        for (let i = 0; i < ft.params.length; i++) stack.pop();
        for (const r of ft.results) stack.push(r);
      } else {
        stack.push(null);
      }
    }
    return;
  }
  const delta = instrDelta(instr, types, sigs);
  if (delta === UNREACHABLE) {
    stack.length = 0;
    return;
  }
  if (delta < 0) {
    for (let i = 0; i < -delta; i++) stack.pop();
  } else if (delta > 0) {
    for (let i = 0; i < delta; i++) stack.push(null);
  }
}
function stackBalance(mod) {
  const sigs = buildFuncSigs(mod);
  const tags = mod.tags || [];
  let totalFixups = 0;
  let numImports = 0;
  let boxNumberIdx = null;
  let unboxNumberIdx = null;
  for (const imp of mod.imports) {
    if (imp.desc.kind === "func") {
      if (imp.name === "__box_number") boxNumberIdx = numImports;
      if (imp.name === "__unbox_number") unboxNumberIdx = numImports;
      numImports++;
    }
  }
  const globalTypes = [];
  for (const imp of mod.imports) {
    if (imp.desc.kind === "global") {
      globalTypes.push(imp.desc.type);
    }
  }
  for (const g of mod.globals) {
    globalTypes.push(g.type);
  }
  for (let fi = 0; fi < mod.functions.length; fi++) {
    const func = mod.functions[fi];
    const ft = resolveFuncType(mod.types, func.typeIdx);
    const localTypes = [];
    if (ft) {
      for (const p of ft.params) localTypes.push(p);
    }
    for (const l of func.locals) localTypes.push(l.type);
    eliminateDeadCode(func.body);
    totalFixups += fixLocalSetCoercion(
      func.body,
      localTypes,
      globalTypes,
      mod.types,
      mod,
      numImports,
      sigs,
      boxNumberIdx,
      unboxNumberIdx,
    );
    totalFixups += fixCallArgTypesInBody(
      func.body,
      localTypes,
      globalTypes,
      mod.types,
      mod,
      numImports,
      sigs,
      boxNumberIdx,
      unboxNumberIdx,
    );
    totalFixups += fixStructNewFieldCoercion(
      func,
      mod.types,
      mod,
      numImports,
      sigs,
      localTypes,
      globalTypes,
      boxNumberIdx,
      unboxNumberIdx,
    );
    totalFixups += fixBody(func.body, mod.types, sigs, tags);
    if (ft) {
      const expectedResults = ft.results.length;
      const funcBlockType =
        expectedResults === 0
          ? { kind: "empty" }
          : expectedResults === 1
            ? { kind: "val", type: ft.results[0] }
            : { kind: "type", typeIdx: func.typeIdx };
      totalFixups += fixBranch(func.body, expectedResults, mod.types, sigs, funcBlockType);
    }
  }
  return totalFixups;
}
function fixLocalSetCoercion(
  body,
  localTypes,
  globalTypes,
  types,
  mod,
  numImports,
  sigs,
  boxNumberIdx,
  unboxNumberIdx,
) {
  let fixups = 0;
  for (const instr of body) {
    if (instr.op === "if") {
      const ifInstr = instr;
      if (ifInstr.then)
        fixups += fixLocalSetCoercion(
          ifInstr.then,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
      if (ifInstr.else)
        fixups += fixLocalSetCoercion(
          ifInstr.else,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
    } else if (instr.op === "block" || instr.op === "loop") {
      const blockInstr = instr;
      if (blockInstr.body)
        fixups += fixLocalSetCoercion(
          blockInstr.body,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
    } else if (instr.op === "try") {
      const tryInstr = instr;
      if (tryInstr.body)
        fixups += fixLocalSetCoercion(
          tryInstr.body,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
      if (tryInstr.catches) {
        for (const c of tryInstr.catches) {
          if (c.body)
            fixups += fixLocalSetCoercion(
              c.body,
              localTypes,
              globalTypes,
              types,
              mod,
              numImports,
              sigs,
              boxNumberIdx,
              unboxNumberIdx,
            );
        }
      }
      if (tryInstr.catchAll)
        fixups += fixLocalSetCoercion(
          tryInstr.catchAll,
          localTypes,
          globalTypes,
          types,
          mod,
          numImports,
          sigs,
          boxNumberIdx,
          unboxNumberIdx,
        );
    }
  }
  for (let i = 0; i < body.length; i++) {
    const instr = body[i];
    if (instr.op !== "local.set" && instr.op !== "local.tee") continue;
    const localIdx = instr.index;
    const localType = localTypes[localIdx];
    if (!localType) continue;
    if (i === 0) continue;
    const prev = body[i - 1];
    const stackType = inferInstrType(prev, localTypes, globalTypes, types, mod, numImports);
    if (!stackType) continue;
    const coercion = callArgCoercionInstrs(stackType, localType, boxNumberIdx, unboxNumberIdx);
    if (coercion.length > 0) {
      body.splice(i, 0, ...coercion);
      i += coercion.length;
      fixups += coercion.length;
    }
  }
  return fixups;
}

// src/checker/type-mapper.ts
import ts3 from "typescript";
var BUILTIN_TYPES = /* @__PURE__ */ new Set([
  "Array",
  "Number",
  "Boolean",
  "String",
  "Object",
  "Function",
  "Symbol",
  "BigInt",
  "Int8Array",
  "Uint8Array",
  "Int16Array",
  "Uint16Array",
  "Int32Array",
  "Uint32Array",
  "Float32Array",
  "Float64Array",
  "ArrayBuffer",
  "DataView",
  "Date",
  "JSON",
  "Math",
  "Promise",
  "Generator",
  "Iterator",
  "IterableIterator",
  "Iterable",
  "IteratorResult",
  "IteratorYieldResult",
  "IteratorReturnResult",
]);
function mapTsTypeToWasm(type, checker, fast) {
  if (type.flags & ts3.TypeFlags.BigInt || type.flags & ts3.TypeFlags.BigIntLiteral) {
    return { kind: "i64" };
  }
  if (type.flags & ts3.TypeFlags.Number || type.flags & ts3.TypeFlags.NumberLiteral) {
    return { kind: fast ? "i32" : "f64" };
  }
  if (type.flags & ts3.TypeFlags.Boolean || type.flags & ts3.TypeFlags.BooleanLiteral) {
    return { kind: "i32" };
  }
  if (type.flags & ts3.TypeFlags.String || type.flags & ts3.TypeFlags.StringLiteral) {
    return { kind: "externref" };
  }
  if (type.flags & ts3.TypeFlags.Void || type.flags & ts3.TypeFlags.Undefined) {
    return { kind: "i32" };
  }
  if (type.flags & ts3.TypeFlags.Null) {
    return { kind: "externref" };
  }
  if ((type.flags & ts3.TypeFlags.ESSymbol) !== 0 || (type.flags & ts3.TypeFlags.UniqueESSymbol) !== 0) {
    return { kind: "i32" };
  }
  if (type.isUnion()) {
    const nonNullish = type.types.filter(
      (t) => !(t.flags & ts3.TypeFlags.Null) && !(t.flags & ts3.TypeFlags.Undefined),
    );
    if (nonNullish.length === 1) {
      const inner = mapTsTypeToWasm(nonNullish[0], checker, fast);
      if (inner.kind === "ref") return { kind: "ref_null", typeIdx: inner.typeIdx };
      return inner;
    }
    if (nonNullish.length > 1) {
      const mapped = nonNullish.map((t) => mapTsTypeToWasm(t, checker, fast));
      if (mapped.every((m) => m.kind === mapped[0].kind)) {
        return mapped[0];
      }
    }
    return { kind: "externref" };
  }
  if (type.flags & ts3.TypeFlags.Object) {
    if (isExternalDeclaredClass(type, checker)) return { kind: "externref" };
    return { kind: "externref" };
  }
  if (type.flags & ts3.TypeFlags.TypeParameter) {
    const constraint = checker.getBaseConstraintOfType(type);
    if (constraint) {
      return mapTsTypeToWasm(constraint, checker, fast);
    }
    return { kind: "externref" };
  }
  if (type.flags & (ts3.TypeFlags.Any | ts3.TypeFlags.Unknown)) {
    return { kind: "externref" };
  }
  return { kind: "externref" };
}
function isExternalDeclaredClass(type, checker) {
  const symbol = type.getSymbol();
  if (!symbol) return false;
  const decls = symbol.getDeclarations();
  if (!decls || decls.length === 0) return false;
  const symName = symbol.getName();
  if (
    decls.some(
      (d) =>
        // declare class Foo { ... }
        (ts3.isClassDeclaration(d) && isDeclareContext(d)) || // declare var Foo: { prototype: Foo; new(): Foo }  (lib.dom.d.ts pattern)
        (ts3.isVariableDeclaration(d) && isDeclareVarWithConstructor(d)) || // declare var Date: DateConstructor  (TypeReferenceNode pattern, skip builtins)
        (ts3.isVariableDeclaration(d) &&
          checker &&
          !BUILTIN_TYPES.has(symName) &&
          isDeclareVarWithTypeRefConstructor(d, checker)),
    )
  )
    return true;
  return false;
}
function isDeclareVarWithConstructor(d) {
  const stmt = d.parent?.parent;
  if (!stmt || !ts3.isVariableStatement(stmt)) return false;
  if (!isDeclareContext(stmt)) return false;
  if (!d.type || !ts3.isTypeLiteralNode(d.type)) return false;
  return d.type.members.some((m) => ts3.isConstructSignatureDeclaration(m));
}
function isDeclareVarWithTypeRefConstructor(d, checker) {
  const stmt = d.parent?.parent;
  if (!stmt || !ts3.isVariableStatement(stmt)) return false;
  if (!isDeclareContext(stmt)) return false;
  if (!d.type || !ts3.isTypeReferenceNode(d.type)) return false;
  const refType = checker.getTypeAtLocation(d.type);
  return refType.getConstructSignatures().length > 0;
}
function isDeclareContext(node) {
  if (ts3.canHaveModifiers(node)) {
    const mods = ts3.getModifiers(node);
    if (mods?.some((m) => m.kind === ts3.SyntaxKind.DeclareKeyword)) return true;
  }
  if (node.parent) {
    if (ts3.isModuleDeclaration(node.parent)) {
      return isDeclareContext(node.parent);
    }
    if (ts3.isModuleBlock(node.parent)) {
      return isDeclareContext(node.parent.parent);
    }
  }
  return false;
}
function isVoidType(type) {
  return (type.flags & ts3.TypeFlags.Void) !== 0 || (type.flags & ts3.TypeFlags.Undefined) !== 0;
}
function isBigIntType(type) {
  return (type.flags & ts3.TypeFlags.BigInt) !== 0 || (type.flags & ts3.TypeFlags.BigIntLiteral) !== 0;
}
function isNumberType(type) {
  return (type.flags & ts3.TypeFlags.Number) !== 0 || (type.flags & ts3.TypeFlags.NumberLiteral) !== 0;
}
function isBooleanType(type) {
  return (type.flags & ts3.TypeFlags.Boolean) !== 0 || (type.flags & ts3.TypeFlags.BooleanLiteral) !== 0;
}
function isStringType(type) {
  if ((type.flags & ts3.TypeFlags.String) !== 0 || (type.flags & ts3.TypeFlags.StringLiteral) !== 0) {
    return true;
  }
  if ((type.flags & ts3.TypeFlags.Object) !== 0) {
    const sym = type.getSymbol();
    if (sym && sym.name === "String") return true;
  }
  return false;
}
function isSymbolType(type) {
  return (type.flags & ts3.TypeFlags.ESSymbol) !== 0 || (type.flags & ts3.TypeFlags.UniqueESSymbol) !== 0;
}
function isPromiseType(type) {
  const symbol = type.getSymbol();
  if (!symbol) return false;
  return symbol.name === "Promise" && !!(type.flags & ts3.TypeFlags.Object);
}
function isGeneratorType(type) {
  const symbol = type.getSymbol();
  if (!symbol) return false;
  return (
    (symbol.name === "Generator" || symbol.name === "Iterator" || symbol.name === "IterableIterator") &&
    !!(type.flags & ts3.TypeFlags.Object)
  );
}
function isIteratorResultType(type) {
  const symbol = type.getSymbol();
  if (!symbol) return false;
  return (
    (symbol.name === "IteratorYieldResult" ||
      symbol.name === "IteratorReturnResult" ||
      symbol.name === "IteratorResult") &&
    !!(type.flags & ts3.TypeFlags.Object)
  );
}
function unwrapPromiseType(type, checker) {
  if (!isPromiseType(type)) return type;
  const typeRef = type;
  const typeArgs = checker.getTypeArguments(typeRef);
  if (typeArgs.length > 0) {
    return typeArgs[0];
  }
  return type;
}
function isHeterogeneousUnion(type, checker, fast) {
  if (!type.isUnion()) return false;
  const nonNullish = type.types.filter((t) => !(t.flags & ts3.TypeFlags.Null) && !(t.flags & ts3.TypeFlags.Undefined));
  if (nonNullish.length <= 1) return false;
  const mapped = nonNullish.map((t) => mapTsTypeToWasm(t, checker, fast));
  return !mapped.every((m) => m.kind === mapped[0].kind);
}

// src/ir/types.ts
function createEmptyModule() {
  return {
    types: [],
    imports: [],
    functions: [],
    exports: [],
    tables: [],
    elements: [],
    globals: [],
    tags: [],
    stringPool: [],
    externClasses: [],
    stringLiteralValues: /* @__PURE__ */ new Map(),
    asyncFunctions: /* @__PURE__ */ new Set(),
    declaredFuncRefs: [],
    memories: [],
    dataSegments: [],
  };
}

// src/codegen/registry/types.ts
function funcTypeKey(params, results) {
  let key = "";
  for (let i = 0; i < params.length; i++) {
    const p = params[i];
    if (i > 0) key += ",";
    key += p.kind;
    if (p.kind === "ref" || p.kind === "ref_null") key += ":" + p.typeIdx;
  }
  key += "|";
  for (let i = 0; i < results.length; i++) {
    const r = results[i];
    if (i > 0) key += ",";
    key += r.kind;
    if (r.kind === "ref" || r.kind === "ref_null") key += ":" + r.typeIdx;
  }
  return key;
}
function addFuncType(ctx, params, results, name) {
  const key = funcTypeKey(params, results);
  const cached = ctx.funcTypeCache.get(key);
  if (cached !== void 0) return cached;
  const idx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "func",
    name: name ?? `type${idx}`,
    params,
    results,
  });
  ctx.funcTypeCache.set(key, idx);
  return idx;
}
function getOrRegisterArrayType(ctx, elemKind, elemTypeOverride) {
  if (ctx.arrayTypeMap.has(elemKind)) return ctx.arrayTypeMap.get(elemKind);
  let elemType =
    elemTypeOverride ??
    (elemKind === "f64" ? { kind: "f64" } : elemKind === "i32" ? { kind: "i32" } : { kind: "externref" });
  if (elemType.kind === "ref") {
    elemType = { kind: "ref_null", typeIdx: elemType.typeIdx };
  }
  const idx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "array",
    name: `__arr_${elemKind}`,
    element: elemType,
    mutable: true,
  });
  ctx.arrayTypeMap.set(elemKind, idx);
  return idx;
}
function getOrRegisterVecType(ctx, elemKind, elemTypeOverride) {
  const existing = ctx.vecTypeMap.get(elemKind);
  if (existing !== void 0) return existing;
  const arrTypeIdx = getOrRegisterArrayType(ctx, elemKind, elemTypeOverride);
  const vecIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: `__vec_${elemKind}`,
    fields: [
      { name: "length", type: { kind: "i32" }, mutable: true },
      {
        name: "data",
        type: { kind: "ref", typeIdx: arrTypeIdx },
        mutable: true,
      },
    ],
  });
  ctx.vecTypeMap.set(elemKind, vecIdx);
  const vecStructName = `__vec_${elemKind}`;
  ctx.structMap.set(vecStructName, vecIdx);
  ctx.typeIdxToStructName.set(vecIdx, vecStructName);
  ctx.structFields.set(vecStructName, [
    { name: "length", type: { kind: "i32" }, mutable: true },
    { name: "data", type: { kind: "ref", typeIdx: arrTypeIdx }, mutable: true },
  ]);
  return vecIdx;
}
function getOrRegisterTemplateVecType(ctx) {
  if (ctx.templateVecTypeIdx >= 0) return ctx.templateVecTypeIdx;
  const baseVecTypeIdx = getOrRegisterVecType(ctx, "externref", { kind: "externref" });
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, baseVecTypeIdx);
  const baseVecDef = ctx.mod.types[baseVecTypeIdx];
  if (baseVecDef && baseVecDef.kind === "struct" && baseVecDef.superTypeIdx === void 0) {
    baseVecDef.superTypeIdx = -1;
  }
  const templateVecIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "__template_vec_externref",
    superTypeIdx: baseVecTypeIdx,
    fields: [
      { name: "length", type: { kind: "i32" }, mutable: true },
      {
        name: "data",
        type: { kind: "ref", typeIdx: arrTypeIdx },
        mutable: true,
      },
      {
        name: "raw",
        type: { kind: "ref_null", typeIdx: baseVecTypeIdx },
        mutable: false,
      },
    ],
  });
  ctx.templateVecTypeIdx = templateVecIdx;
  return templateVecIdx;
}
function getOrRegisterRefCellType(ctx, valType) {
  const key =
    valType.kind === "ref" || valType.kind === "ref_null" ? `${valType.kind}_${valType.typeIdx}` : valType.kind;
  const existing = ctx.refCellTypeMap.get(key);
  if (existing !== void 0) return existing;
  const typeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: `__ref_cell_${key}`,
    fields: [{ name: "value", type: valType, mutable: true }],
  });
  ctx.refCellTypeMap.set(key, typeIdx);
  return typeIdx;
}
function getArrTypeIdxFromVec(ctx, vecTypeIdx) {
  const vecDef = ctx.mod.types[vecTypeIdx];
  if (!vecDef || vecDef.kind !== "struct") return -1;
  const dataField = vecDef.fields[1];
  if (!dataField) return -1;
  if (dataField.type.kind !== "ref" && dataField.type.kind !== "ref_null") {
    return -1;
  }
  return dataField.type.typeIdx;
}
function registerNativeStringTypes(ctx) {
  ctx.nativeStrDataTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "array",
    name: "__str_data",
    element: { kind: "i16" },
    mutable: true,
  });
  ctx.anyStrTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "AnyString",
    fields: [{ name: "len", type: { kind: "i32" }, mutable: false }],
    superTypeIdx: -1,
  });
  ctx.nativeStrTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "NativeString",
    fields: [
      { name: "len", type: { kind: "i32" }, mutable: false },
      { name: "off", type: { kind: "i32" }, mutable: false },
      { name: "data", type: { kind: "ref", typeIdx: ctx.nativeStrDataTypeIdx }, mutable: false },
    ],
    superTypeIdx: ctx.anyStrTypeIdx,
  });
  ctx.consStrTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "ConsString",
    fields: [
      { name: "len", type: { kind: "i32" }, mutable: false },
      { name: "left", type: { kind: "ref", typeIdx: ctx.anyStrTypeIdx }, mutable: false },
      { name: "right", type: { kind: "ref", typeIdx: ctx.anyStrTypeIdx }, mutable: false },
    ],
    superTypeIdx: ctx.anyStrTypeIdx,
  });
}

// src/codegen/context/create-context.ts
function createCodegenContext(mod, checker, options) {
  const ctx = {
    mod,
    checker,
    funcMap: /* @__PURE__ */ new Map(),
    structMap: /* @__PURE__ */ new Map(),
    typeIdxToStructName: /* @__PURE__ */ new Map(),
    structFields: /* @__PURE__ */ new Map(),
    numImportFuncs: 0,
    currentFunc: null,
    funcStack: [],
    errors: [],
    externClasses: /* @__PURE__ */ new Map(),
    funcOptionalParams: /* @__PURE__ */ new Map(),
    anonTypeMap: /* @__PURE__ */ new Map(),
    anonTypeCounter: 0,
    stringLiteralMap: /* @__PURE__ */ new Map(),
    stringLiteralValues: /* @__PURE__ */ new Map(),
    stringLiteralCounter: 0,
    stringGlobalMap: /* @__PURE__ */ new Map(),
    numImportGlobals: 0,
    hasStringImports: false,
    enumValues: /* @__PURE__ */ new Map(),
    enumStringValues: /* @__PURE__ */ new Map(),
    arrayTypeMap: /* @__PURE__ */ new Map(),
    vecTypeMap: /* @__PURE__ */ new Map(),
    externClassParent: /* @__PURE__ */ new Map(),
    declaredGlobals: /* @__PURE__ */ new Map(),
    callbackCounter: 0,
    capturedGlobals: /* @__PURE__ */ new Map(),
    capturedGlobalsWidened: /* @__PURE__ */ new Set(),
    classSet: /* @__PURE__ */ new Set(),
    classThrowsOnEval: /* @__PURE__ */ new Set(),
    classMethodSet: /* @__PURE__ */ new Set(),
    deferredClassBodies: /* @__PURE__ */ new Set(),
    classAccessorSet: /* @__PURE__ */ new Set(),
    staticAccessorSet: /* @__PURE__ */ new Set(),
    staticMethodSet: /* @__PURE__ */ new Set(),
    staticProps: /* @__PURE__ */ new Map(),
    staticInitExprs: [],
    closureCounter: 0,
    closureMap: /* @__PURE__ */ new Map(),
    closureInfoByTypeIdx: /* @__PURE__ */ new Map(),
    genericResolved: /* @__PURE__ */ new Map(),
    funcRestParams: /* @__PURE__ */ new Map(),
    valueOfClosureTypes: /* @__PURE__ */ new Map(),
    exnTagIdx: -1,
    hasUnionImports: false,
    asyncFunctions: /* @__PURE__ */ new Set(),
    generatorFunctions: /* @__PURE__ */ new Set(),
    generatorYieldType: /* @__PURE__ */ new Map(),
    moduleGlobals: /* @__PURE__ */ new Map(),
    moduleInitStatements: [],
    nestedFuncCaptures: /* @__PURE__ */ new Map(),
    classParentMap: /* @__PURE__ */ new Map(),
    classTagCounter: 0,
    classTagMap: /* @__PURE__ */ new Map(),
    classExprNameMap: /* @__PURE__ */ new Map(),
    anonClassExprNames: /* @__PURE__ */ new Map(),
    functionNameMap: /* @__PURE__ */ new Map(),
    sourceMap: options?.sourceMap ?? false,
    tupleTypeMap: /* @__PURE__ */ new Map(),
    fast: options?.fast ?? false,
    nativeStrings: options?.nativeStrings ?? options?.fast ?? options?.wasi ?? false,
    nativeStrDataTypeIdx: -1,
    anyStrTypeIdx: -1,
    nativeStrTypeIdx: -1,
    consStrTypeIdx: -1,
    nativeStrHelpersEmitted: false,
    nativeStrHelpers: /* @__PURE__ */ new Map(),
    refCellTypeMap: /* @__PURE__ */ new Map(),
    anyValueTypeIdx: -1,
    anyHelpers: /* @__PURE__ */ new Map(),
    anyHelpersEmitted: false,
    shapeMap: /* @__PURE__ */ new Map(),
    templateCacheCounter: 0,
    templateVecTypeIdx: -1,
    widenedTypeProperties: /* @__PURE__ */ new Map(),
    widenedVarStructMap: /* @__PURE__ */ new Map(),
    pendingMathMethods: /* @__PURE__ */ new Set(),
    classDeclarationMap: /* @__PURE__ */ new Map(),
    wrapperNumberTypeIdx: -1,
    wrapperStringTypeIdx: -1,
    wrapperBooleanTypeIdx: -1,
    funcRefWrapperCache: /* @__PURE__ */ new Map(),
    pendingInitBody: null,
    inlinableFunctions: /* @__PURE__ */ new Map(),
    symbolCounterGlobalIdx: -1,
    parentBodiesStack: [],
    anonStructHash: /* @__PURE__ */ new Map(),
    funcTypeCache: /* @__PURE__ */ new Map(),
    pendingLateImportShift: null,
    protoGlobals: /* @__PURE__ */ new Map(),
    wasi: options?.wasi ?? false,
    wasiFdWriteIdx: -1,
    wasiProcExitIdx: -1,
    wasiBumpPtrGlobalIdx: -1,
    tdzGlobals: /* @__PURE__ */ new Map(),
    tdzLetConstNames: /* @__PURE__ */ new Set(),
    definedPropertyFlags: /* @__PURE__ */ new Map(),
    nonExtensibleVars: /* @__PURE__ */ new Set(),
    frozenVars: /* @__PURE__ */ new Set(),
    sealedVars: /* @__PURE__ */ new Set(),
    shapePropFlags: /* @__PURE__ */ new Map(),
    funcConstructorMap: /* @__PURE__ */ new Map(),
  };
  getOrRegisterVecType(ctx, "externref", { kind: "externref" });
  getOrRegisterVecType(ctx, "f64", { kind: "f64" });
  if (ctx.nativeStrings) {
    registerNativeStringTypes(ctx);
  }
  return ctx;
}

// src/codegen/context/bodies.ts
function pushBody(fctx) {
  const saved = fctx.body;
  fctx.savedBodies.push(saved);
  fctx.body = [];
  return saved;
}
function popBody(fctx, saved) {
  fctx.savedBodies.pop();
  fctx.body = saved;
}

// src/codegen/context/errors.ts
function reportError(ctx, node, message) {
  try {
    const sf = node.getSourceFile();
    if (sf) {
      const { line, character } = sf.getLineAndCharacterOfPosition(node.getStart());
      ctx.errors.push({ message, line: line + 1, column: character + 1 });
    } else {
      ctx.errors.push({ message, line: 0, column: 0 });
    }
  } catch {
    ctx.errors.push({ message, line: 0, column: 0 });
  }
}

// src/codegen/context/locals.ts
function allocLocal(fctx, name, type) {
  const index = fctx.params.length + fctx.locals.length;
  fctx.locals.push({ name, type });
  fctx.localMap.set(name, index);
  return index;
}
function valTypeKey(type) {
  switch (type.kind) {
    case "ref":
      return `ref:${type.typeIdx}`;
    case "ref_null":
      return `ref_null:${type.typeIdx}`;
    default:
      return type.kind;
  }
}
function allocTempLocal(fctx, type) {
  if (!fctx.tempFreeList) fctx.tempFreeList = /* @__PURE__ */ new Map();
  const key = valTypeKey(type);
  const bucket = fctx.tempFreeList.get(key);
  if (bucket && bucket.length > 0) {
    return bucket.pop();
  }
  return allocLocal(fctx, `__tmp_${fctx.locals.length}`, type);
}
function releaseTempLocal(fctx, index) {
  const type = getLocalType(fctx, index);
  if (!type) return;
  if (!fctx.tempFreeList) fctx.tempFreeList = /* @__PURE__ */ new Map();
  const key = valTypeKey(type);
  let bucket = fctx.tempFreeList.get(key);
  if (!bucket) {
    bucket = [];
    fctx.tempFreeList.set(key, bucket);
  }
  bucket.push(index);
}
function getLocalType(fctx, index) {
  if (index < fctx.params.length) return fctx.params[index].type;
  const localIdx = index - fctx.params.length;
  return fctx.locals[localIdx]?.type;
}

// src/codegen/context/source-pos.ts
function getSourcePos(ctx, node) {
  if (!ctx.sourceMap) return void 0;
  try {
    const sf = node.getSourceFile();
    if (!sf) return void 0;
    const pos = sf.getLineAndCharacterOfPosition(node.getStart());
    return { file: sf.fileName, line: pos.line, column: pos.character };
  } catch {
    return void 0;
  }
}
function attachSourcePos(instr, sourcePos) {
  if (sourcePos) {
    instr.sourcePos = sourcePos;
  }
  return instr;
}

// src/codegen/registry/imports.ts
function addImport(ctx, module, name, desc) {
  ctx.mod.imports.push({ module, name, desc });
  if (desc.kind === "func") {
    ctx.funcMap.set(name, ctx.numImportFuncs);
    ctx.numImportFuncs++;
  }
  if (desc.kind === "global") {
    ctx.numImportGlobals++;
  }
}
function addStringConstantGlobal(ctx, value) {
  if (ctx.stringGlobalMap.has(value)) return;
  const hasModuleGlobals = ctx.mod.globals.length > 0 || ctx.mod.functions.length > 0;
  const oldNumImportGlobals = ctx.numImportGlobals;
  const globalIdx = ctx.numImportGlobals;
  addImport(ctx, "string_constants", value, {
    kind: "global",
    type: { kind: "externref" },
    mutable: false,
  });
  ctx.stringGlobalMap.set(value, globalIdx);
  ctx.stringLiteralMap.set(value, `__str_${ctx.stringLiteralCounter}`);
  ctx.stringLiteralValues.set(`__str_${ctx.stringLiteralCounter}`, value);
  ctx.stringLiteralCounter++;
  ctx.mod.stringPool.push(value);
  if (hasModuleGlobals) {
    fixupModuleGlobalIndices(ctx, oldNumImportGlobals, 1);
  }
}
function nextModuleGlobalIdx(ctx) {
  return ctx.numImportGlobals + ctx.mod.globals.length;
}
function localGlobalIdx(ctx, absIdx) {
  return absIdx - ctx.numImportGlobals;
}
function ensureExnTag(ctx) {
  if (ctx.exnTagIdx >= 0) return ctx.exnTagIdx;
  const typeIdx = addFuncType(ctx, [{ kind: "externref" }], []);
  const tagDef = { name: "__exn", typeIdx };
  ctx.exnTagIdx = ctx.mod.tags.length;
  ctx.mod.tags.push(tagDef);
  return ctx.exnTagIdx;
}
function fixupModuleGlobalIndices(ctx, threshold, delta) {
  function shiftGlobalIndices(instrs) {
    for (const instr of instrs) {
      if ((instr.op === "global.get" || instr.op === "global.set") && instr.index >= threshold) {
        instr.index += delta;
      }
      if ("body" in instr && Array.isArray(instr.body)) {
        shiftGlobalIndices(instr.body);
      }
      if ("then" in instr && Array.isArray(instr.then)) {
        shiftGlobalIndices(instr.then);
      }
      if ("else" in instr && Array.isArray(instr.else)) {
        shiftGlobalIndices(instr.else);
      }
      if ("catches" in instr && Array.isArray(instr.catches)) {
        for (const c of instr.catches) {
          if (Array.isArray(c.body)) shiftGlobalIndices(c.body);
        }
      }
      if ("catchAll" in instr && Array.isArray(instr.catchAll)) {
        shiftGlobalIndices(instr.catchAll);
      }
    }
  }
  const shifted = /* @__PURE__ */ new Set();
  for (const func of ctx.mod.functions) {
    if (!shifted.has(func.body)) {
      shiftGlobalIndices(func.body);
      shifted.add(func.body);
    }
  }
  if (ctx.currentFunc) {
    if (!shifted.has(ctx.currentFunc.body)) {
      shiftGlobalIndices(ctx.currentFunc.body);
      shifted.add(ctx.currentFunc.body);
    }
    for (const sb of ctx.currentFunc.savedBodies) {
      if (shifted.has(sb)) continue;
      shiftGlobalIndices(sb);
      shifted.add(sb);
    }
  }
  for (const parentFctx of ctx.funcStack) {
    if (!shifted.has(parentFctx.body)) {
      shiftGlobalIndices(parentFctx.body);
      shifted.add(parentFctx.body);
    }
    for (const sb of parentFctx.savedBodies) {
      if (!shifted.has(sb)) {
        shiftGlobalIndices(sb);
        shifted.add(sb);
      }
    }
  }
  for (const pb of ctx.parentBodiesStack) {
    if (!shifted.has(pb)) {
      shiftGlobalIndices(pb);
      shifted.add(pb);
    }
  }
  if (ctx.pendingInitBody && !shifted.has(ctx.pendingInitBody)) {
    shiftGlobalIndices(ctx.pendingInitBody);
    shifted.add(ctx.pendingInitBody);
  }
  for (const g of ctx.mod.globals) {
    if (g.init) shiftGlobalIndices(g.init);
  }
  function shiftMap(map) {
    for (const [key, idx] of map) {
      if (idx >= threshold) {
        map.set(key, idx + delta);
      }
    }
  }
  shiftMap(ctx.moduleGlobals);
  shiftMap(ctx.capturedGlobals);
  shiftMap(ctx.staticProps);
  shiftMap(ctx.protoGlobals);
  shiftMap(ctx.tdzGlobals);
  for (const entry of ctx.staticInitExprs) {
    if (entry.globalIdx >= threshold) {
      entry.globalIdx += delta;
    }
  }
  if (ctx.symbolCounterGlobalIdx >= threshold) {
    ctx.symbolCounterGlobalIdx += delta;
  }
  if (ctx.wasiBumpPtrGlobalIdx >= threshold) {
    ctx.wasiBumpPtrGlobalIdx += delta;
  }
}

// src/codegen/expressions.ts
import ts13 from "typescript";

// src/codegen/array-methods.ts
import ts4 from "typescript";

// src/codegen/shared.ts
var VOID_RESULT = Symbol("void");
function resolveThisStructName(ctx, fctx) {
  const selfIdx = fctx.localMap.get("this");
  if (selfIdx === void 0) return void 0;
  const selfType =
    selfIdx < fctx.params.length ? fctx.params[selfIdx].type : fctx.locals[selfIdx - fctx.params.length]?.type;
  if (!selfType || (selfType.kind !== "ref" && selfType.kind !== "ref_null")) return void 0;
  const typeIdx = selfType.typeIdx;
  return ctx.typeIdxToStructName.get(typeIdx);
}
function valTypesMatch(a, b) {
  if (a.kind !== b.kind) return false;
  if ((a.kind === "ref" || a.kind === "ref_null") && (b.kind === "ref" || b.kind === "ref_null")) {
    return a.typeIdx === b.typeIdx;
  }
  return true;
}
function getLine(node) {
  try {
    const sf = node.getSourceFile();
    if (!sf) return 0;
    const { line } = sf.getLineAndCharacterOfPosition(node.getStart());
    return line + 1;
  } catch {
    return 0;
  }
}
function getCol(node) {
  try {
    const sf = node.getSourceFile();
    if (!sf) return 0;
    const { character } = sf.getLineAndCharacterOfPosition(node.getStart());
    return character + 1;
  } catch {
    return 0;
  }
}
var _compileExpression = () => {
  throw new Error("compileExpression not yet registered");
};
function registerCompileExpression(fn) {
  _compileExpression = fn;
}
function compileExpression(ctx, fctx, expr, expectedType) {
  return _compileExpression(ctx, fctx, expr, expectedType);
}
var _compileArrowAsClosure = () => {
  throw new Error("compileArrowAsClosure not yet registered");
};
function registerCompileArrowAsClosure(fn) {
  _compileArrowAsClosure = fn;
}
function compileArrowAsClosure(ctx, fctx, arrow) {
  return _compileArrowAsClosure(ctx, fctx, arrow);
}
var _emitBoundsCheckedArrayGet = () => {
  throw new Error("emitBoundsCheckedArrayGet not yet registered");
};
function emitBoundsCheckedArrayGet(fctx, arrTypeIdx, elementType) {
  _emitBoundsCheckedArrayGet(fctx, arrTypeIdx, elementType);
}
var _resolveEnclosingClassName = () => void 0;
function resolveEnclosingClassName(fctx) {
  return _resolveEnclosingClassName(fctx);
}
var _coerceType = () => {
  throw new Error("coerceType not yet registered");
};
function registerCoerceType(fn) {
  _coerceType = fn;
}
function coerceType(ctx, fctx, from, to, toPrimitiveHint) {
  _coerceType(ctx, fctx, from, to, toPrimitiveHint);
}
var _ensureLateImport = () => {
  throw new Error("ensureLateImport not yet registered");
};
var _flushLateImportShifts = () => {
  throw new Error("flushLateImportShifts not yet registered");
};
function registerEnsureLateImport(fn) {
  _ensureLateImport = fn;
}
function registerFlushLateImportShifts(fn) {
  _flushLateImportShifts = fn;
}
function ensureLateImport(ctx, name, paramTypes, resultTypes) {
  return _ensureLateImport(ctx, name, paramTypes, resultTypes);
}
function flushLateImportShifts(ctx, fctx) {
  _flushLateImportShifts(ctx, fctx);
}

// src/codegen/type-coercion.ts
function emitGuardedRefCast(fctx, typeIdx) {
  const tmpLocal = allocTempLocal(fctx, { kind: "anyref" });
  fctx.body.push({ op: "local.tee", index: tmpLocal });
  fctx.body.push({ op: "ref.test", typeIdx });
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: { kind: "ref_null", typeIdx } },
    then: [
      { op: "local.get", index: tmpLocal },
      { op: "ref.cast_null", typeIdx },
    ],
    else: [{ op: "ref.null", typeIdx }],
  });
  fctx.__lastGuardedCastBackup = tmpLocal;
}
function emitGuardedFuncRefCast(fctx, funcTypeIdx) {
  const tmpFunc = allocLocal(fctx, `__gfc_${fctx.locals.length}`, { kind: "funcref" });
  fctx.body.push({ op: "local.tee", index: tmpFunc });
  fctx.body.push({ op: "ref.test", typeIdx: funcTypeIdx });
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: { kind: "ref_null", typeIdx: funcTypeIdx } },
    then: [
      { op: "local.get", index: tmpFunc },
      { op: "ref.cast_null", typeIdx: funcTypeIdx },
    ],
    else: [{ op: "ref.null", typeIdx: funcTypeIdx }],
  });
}
function pushStringHint(ctx, fctx, hint) {
  addStringConstantGlobal(ctx, hint);
  const globalIdx = ctx.stringGlobalMap.get(hint);
  if (globalIdx !== void 0) {
    fctx.body.push({ op: "global.get", index: globalIdx });
  }
}
function getVecInfo(ctx, typeIdx) {
  const typeDef = ctx.mod.types[typeIdx];
  if (!typeDef || typeDef.kind !== "struct") return null;
  const sd = typeDef;
  if (!sd.name?.startsWith("__vec_")) return null;
  if (sd.fields.length < 2) return null;
  const dataField = sd.fields[1];
  if (dataField.type.kind !== "ref" && dataField.type.kind !== "ref_null") return null;
  const arrTypeIdx = dataField.type.typeIdx;
  const arrDef = ctx.mod.types[arrTypeIdx];
  if (!arrDef || arrDef.kind !== "array") return null;
  return { arrTypeIdx, elemType: arrDef.element };
}
function buildVecFromExternref(ctx, fctx, externLocal, vecTypeIdx, vecInfo) {
  const lenIdx = ensureLateImport(ctx, "__extern_length", [{ kind: "externref" }], [{ kind: "f64" }]);
  flushLateImportShifts(ctx, fctx);
  const getIdx = ensureLateImport(
    ctx,
    "__extern_get",
    [{ kind: "externref" }, { kind: "externref" }],
    [{ kind: "externref" }],
  );
  flushLateImportShifts(ctx, fctx);
  const unboxIdx = ensureLateImport(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
  flushLateImportShifts(ctx, fctx);
  const boxIdx = ensureLateImport(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
  flushLateImportShifts(ctx, fctx);
  if (lenIdx === void 0 || getIdx === void 0) {
    return [{ op: "ref.null", typeIdx: vecTypeIdx }];
  }
  const lenLocal = allocLocal(fctx, `__vec_len_${fctx.locals.length}`, { kind: "i32" });
  const arrLocal = allocLocal(fctx, `__vec_arr_${fctx.locals.length}`, {
    kind: "ref_null",
    typeIdx: vecInfo.arrTypeIdx,
  });
  const idxLocal = allocLocal(fctx, `__vec_idx_${fctx.locals.length}`, { kind: "i32" });
  const buildElemCoerce = () => {
    const et = vecInfo.elemType;
    if (et.kind === "f64" && unboxIdx !== void 0) {
      return [{ op: "call", funcIdx: unboxIdx }];
    }
    if (et.kind === "i32" && unboxIdx !== void 0) {
      return [{ op: "call", funcIdx: unboxIdx }, { op: "i32.trunc_sat_f64_s" }];
    }
    if (et.kind === "externref") return [];
    if (et.kind === "ref" || et.kind === "ref_null") {
      const elemTypeIdx = et.typeIdx;
      const tupleFields = getTupleFields(ctx, elemTypeIdx);
      if (tupleFields && getIdx !== void 0) {
        const tmpElem = allocLocal(fctx, `__tuple_src_${fctx.locals.length}`, { kind: "externref" });
        const instrs = [{ op: "local.set", index: tmpElem }];
        for (let fi = 0; fi < tupleFields.length; fi++) {
          const fieldType = tupleFields[fi];
          instrs.push({ op: "local.get", index: tmpElem });
          if (boxIdx !== void 0) {
            instrs.push({ op: "f64.const", value: fi });
            instrs.push({ op: "call", funcIdx: boxIdx });
          } else {
            instrs.push({ op: "ref.null.extern" });
          }
          instrs.push({ op: "call", funcIdx: getIdx });
          if (fieldType.kind === "f64" && unboxIdx !== void 0) {
            instrs.push({ op: "call", funcIdx: unboxIdx });
          } else if (fieldType.kind === "i32" && unboxIdx !== void 0) {
            instrs.push({ op: "call", funcIdx: unboxIdx });
            instrs.push({ op: "i32.trunc_sat_f64_s" });
          }
        }
        instrs.push({ op: "struct.new", typeIdx: elemTypeIdx });
        return instrs;
      }
      return [{ op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx: elemTypeIdx }];
    }
    return [];
  };
  return [
    { op: "local.get", index: externLocal },
    { op: "call", funcIdx: lenIdx },
    { op: "i32.trunc_sat_f64_s" },
    { op: "local.set", index: lenLocal },
    { op: "local.get", index: lenLocal },
    { op: "array.new_default", typeIdx: vecInfo.arrTypeIdx },
    { op: "local.set", index: arrLocal },
    { op: "i32.const", value: 0 },
    { op: "local.set", index: idxLocal },
    {
      op: "block",
      blockType: { kind: "empty" },
      body: [
        {
          op: "loop",
          blockType: { kind: "empty" },
          body: [
            { op: "local.get", index: idxLocal },
            { op: "local.get", index: lenLocal },
            { op: "i32.ge_u" },
            { op: "br_if", depth: 1 },
            { op: "local.get", index: arrLocal },
            { op: "local.get", index: idxLocal },
            { op: "local.get", index: externLocal },
            ...(boxIdx !== void 0
              ? [{ op: "local.get", index: idxLocal }, { op: "f64.convert_i32_s" }, { op: "call", funcIdx: boxIdx }]
              : [{ op: "ref.null.extern" }]),
            { op: "call", funcIdx: getIdx },
            ...buildElemCoerce(),
            { op: "array.set", typeIdx: vecInfo.arrTypeIdx },
            { op: "local.get", index: idxLocal },
            { op: "i32.const", value: 1 },
            { op: "i32.add" },
            { op: "local.set", index: idxLocal },
            { op: "br", depth: 0 },
          ],
        },
      ],
    },
    { op: "local.get", index: lenLocal },
    { op: "local.get", index: arrLocal },
    { op: "struct.new", typeIdx: vecTypeIdx },
  ];
}
function buildTupleFromExternref(ctx, fctx, anyLocal, tupleTypeIdx, tupleFields) {
  const resultType = { kind: "ref_null", typeIdx: tupleTypeIdx };
  let instrs = [{ op: "ref.null", typeIdx: tupleTypeIdx }];
  for (const [_key, vecIdx] of ctx.vecTypeMap) {
    const vecInfo = getVecInfo(ctx, vecIdx);
    if (!vecInfo) continue;
    const { arrTypeIdx, elemType } = vecInfo;
    const vecLocal = allocLocal(fctx, `__tup_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecIdx });
    const dataLocal = allocLocal(fctx, `__tup_data_${fctx.locals.length}`, {
      kind: "ref_null",
      typeIdx: arrTypeIdx,
    });
    const lenLocal = allocLocal(fctx, `__tup_len_${fctx.locals.length}`, { kind: "i32" });
    const thenInstrs = [
      { op: "local.get", index: anyLocal },
      { op: "ref.cast", typeIdx: vecIdx },
      { op: "local.set", index: vecLocal },
      // Get data array and length
      { op: "local.get", index: vecLocal },
      { op: "struct.get", typeIdx: vecIdx, fieldIdx: 1 },
      { op: "local.set", index: dataLocal },
      { op: "local.get", index: vecLocal },
      { op: "struct.get", typeIdx: vecIdx, fieldIdx: 0 },
      { op: "local.set", index: lenLocal },
    ];
    for (let i = 0; i < tupleFields.length; i++) {
      const fieldType = tupleFields[i];
      const readInstrs = [
        { op: "local.get", index: dataLocal },
        { op: "i32.const", value: i },
        { op: "array.get", typeIdx: arrTypeIdx },
      ];
      const defaultInstrs = defaultValueInstrs(elemType);
      thenInstrs.push(
        { op: "i32.const", value: i },
        { op: "local.get", index: lenLocal },
        { op: "i32.lt_u" },
        {
          op: "if",
          blockType: { kind: "val", type: elemType },
          then: readInstrs,
          else: defaultInstrs,
        },
      );
      if (
        elemType.kind !== fieldType.kind ||
        ((elemType.kind === "ref" || elemType.kind === "ref_null") &&
          (fieldType.kind === "ref" || fieldType.kind === "ref_null") &&
          elemType.typeIdx !== fieldType.typeIdx)
      ) {
        if (elemType.kind === "f64" && fieldType.kind === "externref") {
          const boxIdx = ctx.funcMap.get("__box_number");
          if (boxIdx !== void 0) {
            thenInstrs.push({ op: "call", funcIdx: boxIdx });
          }
        } else if (elemType.kind === "externref" && fieldType.kind === "f64") {
          const unboxIdx = ctx.funcMap.get("__unbox_number");
          if (unboxIdx !== void 0) {
            thenInstrs.push({ op: "call", funcIdx: unboxIdx });
          }
        } else if (elemType.kind === "f64" && fieldType.kind === "f64") {
        } else if (elemType.kind === "externref" && fieldType.kind === "externref") {
        } else if ((elemType.kind === "ref" || elemType.kind === "ref_null") && fieldType.kind === "externref") {
          thenInstrs.push({ op: "extern.convert_any" });
        } else if (elemType.kind === "externref" && (fieldType.kind === "ref" || fieldType.kind === "ref_null")) {
          const toRefIdx = fieldType.typeIdx;
          thenInstrs.push({ op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx: toRefIdx });
        }
      }
    }
    thenInstrs.push({ op: "struct.new", typeIdx: tupleTypeIdx });
    instrs = [
      { op: "local.get", index: anyLocal },
      { op: "ref.test", typeIdx: vecIdx },
      {
        op: "if",
        blockType: { kind: "val", type: resultType },
        then: thenInstrs,
        else: instrs,
      },
    ];
  }
  return instrs;
}
function getTupleFields(ctx, typeIdx) {
  const typeDef = ctx.mod.types[typeIdx];
  if (!typeDef || typeDef.kind !== "struct") return null;
  const sd = typeDef;
  if (!sd.name?.startsWith("__tuple_")) return null;
  return sd.fields.map((f) => f.type);
}
function getStructNarrowInfo(ctx, fromTypeIdx, toTypeIdx) {
  const fromDef = ctx.mod.types[fromTypeIdx];
  const toDef = ctx.mod.types[toTypeIdx];
  if (!fromDef || fromDef.kind !== "struct") return null;
  if (!toDef || toDef.kind !== "struct") return null;
  const srcStruct = fromDef;
  const dstStruct = toDef;
  const srcFieldMap = /* @__PURE__ */ new Map();
  for (let i = 0; i < srcStruct.fields.length; i++) {
    srcFieldMap.set(srcStruct.fields[i].name, { type: srcStruct.fields[i].type, fieldIdx: i });
  }
  const srcFields = [];
  for (const field of dstStruct.fields) {
    const srcField = srcFieldMap.get(field.name);
    if (!srcField) return null;
    srcFields.push({ name: field.name, type: srcField.type, fieldIdx: srcField.fieldIdx });
  }
  return {
    srcFields,
    dstFields: dstStruct.fields.map((f) => ({ name: f.name, type: f.type })),
  };
}
function emitSafeStructConversion(ctx, fctx, fromTypeIdx, toTypeIdx) {
  const srcVec = getVecInfo(ctx, fromTypeIdx);
  if (srcVec) {
    const tupleFields = getTupleFields(ctx, toTypeIdx);
    if (tupleFields) {
      return emitVecToTupleBody(ctx, fctx, fromTypeIdx, toTypeIdx, srcVec, tupleFields);
    }
    const dstVec = getVecInfo(ctx, toTypeIdx);
    if (dstVec && srcVec.elemType.kind !== dstVec.elemType.kind) {
      return emitVecToVecBody(ctx, fctx, fromTypeIdx, toTypeIdx, srcVec, dstVec);
    }
    if (
      dstVec &&
      (srcVec.elemType.kind === "ref" || srcVec.elemType.kind === "ref_null") &&
      (dstVec.elemType.kind === "ref" || dstVec.elemType.kind === "ref_null")
    ) {
      const srcRefIdx = srcVec.elemType.typeIdx;
      const dstRefIdx = dstVec.elemType.typeIdx;
      if (srcRefIdx !== dstRefIdx) {
        return emitVecToVecBody(ctx, fctx, fromTypeIdx, toTypeIdx, srcVec, dstVec);
      }
    }
  }
  const narrowInfo = getStructNarrowInfo(ctx, fromTypeIdx, toTypeIdx);
  if (narrowInfo) {
    return emitStructNarrowBody(ctx, fctx, fromTypeIdx, toTypeIdx, narrowInfo);
  }
  return false;
}
function emitVecToTupleBody(ctx, fctx, fromTypeIdx, toTypeIdx, srcVec, tupleFields) {
  const { arrTypeIdx, elemType } = srcVec;
  const vecRefType = { kind: "ref_null", typeIdx: fromTypeIdx };
  const tmpLocal = allocTempLocal(fctx, vecRefType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  const dataLocal = allocTempLocal(fctx, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenLocal = allocTempLocal(fctx, { kind: "i32" });
  fctx.body.push({ op: "local.get", index: tmpLocal });
  fctx.body.push({ op: "struct.get", typeIdx: fromTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataLocal });
  fctx.body.push({ op: "local.get", index: tmpLocal });
  fctx.body.push({ op: "struct.get", typeIdx: fromTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenLocal });
  for (let i = 0; i < tupleFields.length; i++) {
    const fieldType = tupleFields[i];
    fctx.body.push({ op: "i32.const", value: i });
    fctx.body.push({ op: "local.get", index: lenLocal });
    fctx.body.push({ op: "i32.lt_u" });
    const thenInstrs = [
      { op: "local.get", index: dataLocal },
      { op: "i32.const", value: i },
      { op: "array.get", typeIdx: arrTypeIdx },
    ];
    const elseInstrs = defaultValueInstrs(elemType);
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: elemType },
      then: thenInstrs,
      else: elseInstrs,
    });
    if (elemType.kind !== fieldType.kind) {
      coerceType2(ctx, fctx, elemType, fieldType);
    } else if (
      (elemType.kind === "ref" || elemType.kind === "ref_null") &&
      (fieldType.kind === "ref" || fieldType.kind === "ref_null")
    ) {
      const fromRefIdx = elemType.typeIdx;
      const toRefIdx = fieldType.typeIdx;
      if (fromRefIdx !== toRefIdx) {
        coerceType2(ctx, fctx, elemType, fieldType);
      }
    }
  }
  releaseTempLocal(fctx, lenLocal);
  releaseTempLocal(fctx, dataLocal);
  fctx.body.push({ op: "struct.new", typeIdx: toTypeIdx });
  releaseTempLocal(fctx, tmpLocal);
  return true;
}
function emitVecToVecBody(ctx, fctx, fromTypeIdx, toTypeIdx, srcVec, dstVec) {
  const srcRefType = { kind: "ref_null", typeIdx: fromTypeIdx };
  const srcLocal = allocTempLocal(fctx, srcRefType);
  fctx.body.push({ op: "local.set", index: srcLocal });
  fctx.body.push({ op: "local.get", index: srcLocal });
  fctx.body.push({ op: "struct.get", typeIdx: fromTypeIdx, fieldIdx: 0 });
  const lenLocal = allocTempLocal(fctx, { kind: "i32" });
  fctx.body.push({ op: "local.tee", index: lenLocal });
  fctx.body.push({ op: "array.new_default", typeIdx: dstVec.arrTypeIdx });
  const dstArrRefType = { kind: "ref_null", typeIdx: dstVec.arrTypeIdx };
  const dstArrLocal = allocTempLocal(fctx, dstArrRefType);
  fctx.body.push({ op: "local.set", index: dstArrLocal });
  const iLocal = allocTempLocal(fctx, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iLocal });
  const loopBodyStart = fctx.body.length;
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "local.get", index: lenLocal });
  fctx.body.push({ op: "i32.ge_u" });
  fctx.body.push({ op: "br_if", depth: 1 });
  fctx.body.push({ op: "local.get", index: dstArrLocal });
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "local.get", index: srcLocal });
  fctx.body.push({ op: "struct.get", typeIdx: fromTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "array.get", typeIdx: srcVec.arrTypeIdx });
  if (srcVec.elemType.kind !== dstVec.elemType.kind) {
    coerceType2(ctx, fctx, srcVec.elemType, dstVec.elemType);
  }
  fctx.body.push({ op: "array.set", typeIdx: dstVec.arrTypeIdx });
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: iLocal });
  fctx.body.push({ op: "br", depth: 0 });
  const loopBody = fctx.body.splice(loopBodyStart);
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
  fctx.body.push({ op: "local.get", index: lenLocal });
  fctx.body.push({ op: "local.get", index: dstArrLocal });
  fctx.body.push({ op: "struct.new", typeIdx: toTypeIdx });
  releaseTempLocal(fctx, iLocal);
  releaseTempLocal(fctx, dstArrLocal);
  releaseTempLocal(fctx, lenLocal);
  releaseTempLocal(fctx, srcLocal);
  return true;
}
function emitStructNarrowBody(ctx, fctx, fromTypeIdx, toTypeIdx, info) {
  const srcRefType = { kind: "ref_null", typeIdx: fromTypeIdx };
  const tmpLocal = allocTempLocal(fctx, srcRefType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  for (let i = 0; i < info.dstFields.length; i++) {
    const srcField = info.srcFields[i];
    const dstField = info.dstFields[i];
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "struct.get", typeIdx: fromTypeIdx, fieldIdx: srcField.fieldIdx });
    if (srcField.type.kind !== dstField.type.kind) {
      coerceType2(ctx, fctx, srcField.type, dstField.type);
    } else if (
      (srcField.type.kind === "ref" || srcField.type.kind === "ref_null") &&
      (dstField.type.kind === "ref" || dstField.type.kind === "ref_null")
    ) {
      const fromRefIdx = srcField.type.typeIdx;
      const toRefIdx = dstField.type.typeIdx;
      if (fromRefIdx !== toRefIdx) {
        coerceType2(ctx, fctx, srcField.type, dstField.type);
      }
    }
  }
  fctx.body.push({ op: "struct.new", typeIdx: toTypeIdx });
  releaseTempLocal(fctx, tmpLocal);
  return true;
}
function coerceType2(ctx, fctx, from, to, toPrimitiveHint, compileStringLiteralFn) {
  if (from.kind === to.kind) {
    if ((from.kind === "ref" || from.kind === "ref_null") && (to.kind === "ref" || to.kind === "ref_null")) {
      const fromIdx = from.typeIdx;
      const toIdx = to.typeIdx;
      if (fromIdx === toIdx) return;
      if (isAnyValue(to, ctx) && !isAnyValue(from, ctx)) {
        ensureAnyHelpers(ctx);
        const funcIdx = ctx.funcMap.get("__any_box_ref");
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          return;
        }
      }
      if (isAnyValue(from, ctx) && !isAnyValue(to, ctx)) {
        ensureAnyHelpers(ctx);
        fctx.body.push({ op: "struct.get", typeIdx: ctx.anyValueTypeIdx, fieldIdx: 3 });
        const tmpEq = allocTempLocal(fctx, { kind: "eqref" });
        fctx.body.push({ op: "local.tee", index: tmpEq });
        fctx.body.push({ op: "ref.test", typeIdx: toIdx });
        if (to.kind === "ref_null") {
          fctx.body.push({
            op: "if",
            blockType: { kind: "val", type: to },
            then: [
              { op: "local.get", index: tmpEq },
              { op: "ref.cast_null", typeIdx: toIdx },
            ],
            else: [{ op: "ref.null", typeIdx: toIdx }],
          });
        } else {
          fctx.body.push({
            op: "if",
            blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
            then: [
              { op: "local.get", index: tmpEq },
              { op: "ref.cast_null", typeIdx: toIdx },
            ],
            else: [{ op: "ref.null", typeIdx: toIdx }],
          });
          fctx.body.push({ op: "ref.as_non_null" });
        }
        releaseTempLocal(fctx, tmpEq);
        return;
      }
      if (emitSafeStructConversion(ctx, fctx, fromIdx, toIdx)) {
        return;
      }
      {
        const guardFrom = from.kind === "ref_null" ? { kind: "anyref" } : { kind: "anyref" };
        const tmpGuard = allocTempLocal(fctx, guardFrom);
        fctx.body.push({ op: "local.tee", index: tmpGuard });
        fctx.body.push({ op: "ref.test", typeIdx: toIdx });
        if (to.kind === "ref_null") {
          fctx.body.push({
            op: "if",
            blockType: { kind: "val", type: to },
            then: [
              { op: "local.get", index: tmpGuard },
              { op: "ref.cast_null", typeIdx: toIdx },
            ],
            else: [{ op: "ref.null", typeIdx: toIdx }],
          });
        } else {
          fctx.body.push({
            op: "if",
            blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
            then: [
              { op: "local.get", index: tmpGuard },
              { op: "ref.cast_null", typeIdx: toIdx },
            ],
            else: [{ op: "ref.null", typeIdx: toIdx }],
          });
          fctx.body.push({ op: "ref.as_non_null" });
        }
        releaseTempLocal(fctx, tmpGuard);
      }
      return;
    }
    return;
  }
  if (from.kind === "ref" && to.kind === "ref_null") {
    if (isAnyValue(to, ctx) && !isAnyValue(from, ctx)) {
      ensureAnyHelpers(ctx);
      const funcIdx = ctx.funcMap.get("__any_box_ref");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    const fromRefIdx = from.typeIdx;
    const toRefNullIdx = to.typeIdx;
    if (fromRefIdx !== toRefNullIdx) {
      if (!emitSafeStructConversion(ctx, fctx, fromRefIdx, toRefNullIdx)) {
        const tmpRefNull = allocTempLocal(fctx, { kind: "anyref" });
        fctx.body.push({ op: "local.tee", index: tmpRefNull });
        fctx.body.push({ op: "ref.test", typeIdx: toRefNullIdx });
        fctx.body.push({
          op: "if",
          blockType: { kind: "val", type: to },
          then: [
            { op: "local.get", index: tmpRefNull },
            { op: "ref.cast_null", typeIdx: toRefNullIdx },
          ],
          else: [{ op: "ref.null", typeIdx: toRefNullIdx }],
        });
        releaseTempLocal(fctx, tmpRefNull);
      }
    }
    return;
  }
  if (from.kind === "ref_null" && to.kind === "ref") {
    if (isAnyValue(from, ctx) && !isAnyValue(to, ctx)) {
      ensureAnyHelpers(ctx);
      const toIdx = to.typeIdx;
      fctx.body.push({ op: "struct.get", typeIdx: ctx.anyValueTypeIdx, fieldIdx: 3 });
      const tmpUnbox = allocTempLocal(fctx, { kind: "eqref" });
      fctx.body.push({ op: "local.tee", index: tmpUnbox });
      fctx.body.push({ op: "ref.test", typeIdx: toIdx });
      fctx.body.push({
        op: "if",
        blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
        then: [
          { op: "local.get", index: tmpUnbox },
          { op: "ref.cast_null", typeIdx: toIdx },
        ],
        else: [{ op: "ref.null", typeIdx: toIdx }],
      });
      fctx.body.push({ op: "ref.as_non_null" });
      releaseTempLocal(fctx, tmpUnbox);
      return;
    }
    const fromNullIdx = from.typeIdx;
    const toNonNullIdx = to.typeIdx;
    if (fromNullIdx !== toNonNullIdx) {
      if (!emitSafeStructConversion(ctx, fctx, fromNullIdx, toNonNullIdx)) {
        const tmpCast = allocTempLocal(fctx, { kind: "anyref" });
        fctx.body.push({ op: "local.tee", index: tmpCast });
        fctx.body.push({ op: "ref.test", typeIdx: toNonNullIdx });
        fctx.body.push({
          op: "if",
          blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toNonNullIdx } },
          then: [
            { op: "local.get", index: tmpCast },
            { op: "ref.cast_null", typeIdx: toNonNullIdx },
          ],
          else: [{ op: "ref.null", typeIdx: toNonNullIdx }],
        });
        releaseTempLocal(fctx, tmpCast);
      }
    }
    fctx.body.push({ op: "ref.as_non_null" });
    return;
  }
  if (isAnyValue(to, ctx)) {
    ensureAnyHelpers(ctx);
    if (from.kind === "i32") {
      const funcIdx = ctx.funcMap.get("__any_box_i32");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    if (from.kind === "f64") {
      const funcIdx = ctx.funcMap.get("__any_box_f64");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    if (from.kind === "i64") {
      const funcIdx = ctx.funcMap.get("__any_box_f64");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "f64.convert_i64_s" });
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    if (from.kind === "externref") {
      const funcIdx = ctx.funcMap.get("__any_box_string");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    if (from.kind === "ref" || from.kind === "ref_null") {
      const funcIdx = ctx.funcMap.get("__any_box_ref");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
  }
  if (isAnyValue(from, ctx)) {
    ensureAnyHelpers(ctx);
    if (to.kind === "i32") {
      const funcIdx = ctx.funcMap.get("__any_unbox_i32");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    if (to.kind === "f64") {
      const anyTypeIdx = ctx.anyValueTypeIdx;
      if (anyTypeIdx >= 0) {
        const tmpAny = allocTempLocal(fctx, from);
        const tmpTag = allocTempLocal(fctx, { kind: "i32" });
        fctx.body.push({ op: "local.tee", index: tmpAny });
        fctx.body.push({ op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 });
        fctx.body.push({ op: "local.set", index: tmpTag });
        fctx.body.push({ op: "local.get", index: tmpTag });
        fctx.body.push({ op: "i32.const", value: 2 });
        fctx.body.push({ op: "i32.eq" });
        fctx.body.push({ op: "local.get", index: tmpTag });
        fctx.body.push({ op: "i32.const", value: 4 });
        fctx.body.push({ op: "i32.eq" });
        fctx.body.push({ op: "i32.or" });
        fctx.body.push({
          op: "if",
          blockType: { kind: "val", type: { kind: "f64" } },
          then: [
            { op: "local.get", index: tmpAny },
            { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
            // i32val
            { op: "f64.convert_i32_s" },
          ],
          else: [
            // tag == 1 (undefined) → NaN
            { op: "local.get", index: tmpTag },
            { op: "i32.const", value: 1 },
            { op: "i32.eq" },
            {
              op: "if",
              blockType: { kind: "val", type: { kind: "f64" } },
              then: [{ op: "f64.const", value: NaN }],
              else: [
                // default: f64val (covers tag 0/null=0, tag 3/f64, tag 5/string, tag 6/object)
                { op: "local.get", index: tmpAny },
                { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
                // f64val
              ],
            },
          ],
        });
        releaseTempLocal(fctx, tmpTag);
        releaseTempLocal(fctx, tmpAny);
        return;
      }
      const funcIdx = ctx.funcMap.get("__any_unbox_f64");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    if (to.kind === "i64") {
      const funcIdx = ctx.funcMap.get("__any_unbox_f64");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
        return;
      }
    }
    if (to.kind === "externref") {
      fctx.body.push({ op: "extern.convert_any" });
      return;
    }
  }
  if (from.kind === "i64" && to.kind === "f64") {
    fctx.body.push({ op: "f64.convert_i64_s" });
    return;
  }
  if (from.kind === "f64" && to.kind === "i64") {
    fctx.body.push({ op: "i64.trunc_sat_f64_s" });
    return;
  }
  if (from.kind === "i32" && to.kind === "i64") {
    fctx.body.push({ op: "i64.extend_i32_s" });
    return;
  }
  if (from.kind === "i64" && to.kind === "i32") {
    fctx.body.push({ op: "i64.const", value: 0n });
    fctx.body.push({ op: "i64.ne" });
    return;
  }
  if (from.kind === "i32" && to.kind === "f64") {
    fctx.body.push({ op: "f64.convert_i32_s" });
    return;
  }
  if (from.kind === "f64" && to.kind === "i32") {
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    return;
  }
  if (from.kind === "externref" && to.kind === "i32") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__unbox_number");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      return;
    }
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({ op: "i32.eqz" });
    return;
  }
  if (from.kind === "externref" && to.kind === "f64") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__unbox_number");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return;
    }
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "f64.const", value: 0 });
    return;
  }
  if (from.kind === "externref" && to.kind === "i64") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__unbox_number");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      return;
    }
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "i64.const", value: 0n });
    return;
  }
  if (from.kind === "externref" && (to.kind === "ref" || to.kind === "ref_null")) {
    const toIdx = to.typeIdx;
    const vecInfo = getVecInfo(ctx, toIdx);
    const tmpExternLocal = allocTempLocal(fctx, { kind: "externref" });
    fctx.body.push({ op: "local.tee", index: tmpExternLocal });
    fctx.body.push({ op: "any.convert_extern" });
    const tmpAnyLocal = allocTempLocal(fctx, { kind: "anyref" });
    fctx.body.push({ op: "local.tee", index: tmpAnyLocal });
    fctx.body.push({ op: "ref.test", typeIdx: toIdx });
    let elseBranch;
    if (vecInfo) {
      elseBranch = buildVecFromExternref(ctx, fctx, tmpExternLocal, toIdx, vecInfo);
    } else {
      const tupleFields = getTupleFields(ctx, toIdx);
      if (tupleFields) {
        elseBranch = buildTupleFromExternref(ctx, fctx, tmpAnyLocal, toIdx, tupleFields);
      } else {
        elseBranch = [{ op: "ref.null", typeIdx: toIdx }];
      }
    }
    const resultType = { kind: "ref_null", typeIdx: toIdx };
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: resultType },
      then: [
        { op: "local.get", index: tmpAnyLocal },
        { op: "ref.cast_null", typeIdx: toIdx },
      ],
      else: elseBranch,
    });
    fctx.__lastGuardedCastBackup = tmpAnyLocal;
    releaseTempLocal(fctx, tmpExternLocal);
    return;
  }
  if (from.kind === "f64" && to.kind === "externref") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__box_number");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return;
    }
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "ref.null.extern" });
    return;
  }
  if (from.kind === "i32" && to.kind === "externref") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__box_number");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "f64.convert_i32_s" });
      fctx.body.push({ op: "call", funcIdx });
      return;
    }
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "ref.null.extern" });
    return;
  }
  if (from.kind === "i64" && to.kind === "externref") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__box_number");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "f64.convert_i64_s" });
      fctx.body.push({ op: "call", funcIdx });
      return;
    }
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "ref.null.extern" });
    return;
  }
  if ((from.kind === "ref" || from.kind === "ref_null") && to.kind === "externref") {
    const typeIdx = from.typeIdx;
    const name = ctx.typeIdxToStructName.get(typeIdx);
    if (name !== void 0) {
      const toPrimFuncIdx = ctx.funcMap.get(`${name}_@@toPrimitive`);
      if (toPrimFuncIdx !== void 0) {
        const hint = toPrimitiveHint ?? "string";
        pushStringHint(ctx, fctx, hint);
        fctx.body.push({ op: "call", funcIdx: toPrimFuncIdx });
        const funcDefIdx = toPrimFuncIdx - ctx.numImportFuncs;
        const funcDef = funcDefIdx >= 0 ? ctx.mod.functions[funcDefIdx] : void 0;
        const funcType = funcDef ? ctx.mod.types[funcDef.typeIdx] : void 0;
        const retKind = (funcType?.kind === "func" && funcType.results?.[0]?.kind) || "externref";
        if (retKind === "f64") {
          addUnionImports(ctx);
          const boxIdx = ctx.funcMap.get("__box_number");
          fctx.body.push({ op: "call", funcIdx: boxIdx });
        } else if (retKind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
          addUnionImports(ctx);
          const boxIdx = ctx.funcMap.get("__box_number");
          fctx.body.push({ op: "call", funcIdx: boxIdx });
        }
        if (retKind === "ref" || retKind === "ref_null") {
          fctx.body.push({ op: "extern.convert_any" });
        }
        return;
      }
      const toStringFuncIdx = ctx.funcMap.get(`${name}_toString`);
      if (toStringFuncIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: toStringFuncIdx });
        return;
      }
    }
    fctx.body.push({ op: "extern.convert_any" });
    if (getArrTypeIdxFromVec(ctx, typeIdx) >= 0) {
      const makeIterIdx = ensureLateImport(ctx, "__make_iterable", [{ kind: "externref" }], [{ kind: "externref" }]);
      if (makeIterIdx !== void 0) {
        flushLateImportShifts(ctx, fctx);
        fctx.body.push({ op: "call", funcIdx: makeIterIdx });
      }
    }
    return;
  }
  if ((from.kind === "ref" || from.kind === "ref_null") && to.kind === "eqref") {
    return;
  }
  if ((from.kind === "ref" || from.kind === "ref_null") && to.kind === "anyref") {
    return;
  }
  if (from.kind === "externref" && to.kind === "ref") {
    const toIdx = to.typeIdx;
    fctx.body.push({ op: "any.convert_extern" });
    const tmpExtRef = allocTempLocal(fctx, { kind: "anyref" });
    fctx.body.push({ op: "local.tee", index: tmpExtRef });
    fctx.body.push({ op: "ref.test", typeIdx: toIdx });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
      then: [
        { op: "local.get", index: tmpExtRef },
        { op: "ref.cast_null", typeIdx: toIdx },
      ],
      else: [{ op: "ref.null", typeIdx: toIdx }],
    });
    fctx.body.push({ op: "ref.as_non_null" });
    releaseTempLocal(fctx, tmpExtRef);
    return;
  }
  if (from.kind === "externref" && to.kind === "ref_null") {
    const toIdx = to.typeIdx;
    fctx.body.push({ op: "any.convert_extern" });
    const tmpLocal = allocTempLocal(fctx, { kind: "anyref" });
    fctx.body.push({ op: "local.tee", index: tmpLocal });
    fctx.body.push({ op: "ref.test", typeIdx: toIdx });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: to },
      then: [
        { op: "local.get", index: tmpLocal },
        { op: "ref.cast", typeIdx: toIdx },
      ],
      else: [{ op: "ref.null", typeIdx: toIdx }],
    });
    releaseTempLocal(fctx, tmpLocal);
    return;
  }
  if ((from.kind === "eqref" || from.kind === "anyref") && to.kind === "ref") {
    const toIdx = to.typeIdx;
    const tmpEqAny = allocTempLocal(fctx, from);
    fctx.body.push({ op: "local.tee", index: tmpEqAny });
    fctx.body.push({ op: "ref.test", typeIdx: toIdx });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
      then: [
        { op: "local.get", index: tmpEqAny },
        { op: "ref.cast_null", typeIdx: toIdx },
      ],
      else: [{ op: "ref.null", typeIdx: toIdx }],
    });
    fctx.body.push({ op: "ref.as_non_null" });
    releaseTempLocal(fctx, tmpEqAny);
    return;
  }
  if ((from.kind === "eqref" || from.kind === "anyref") && to.kind === "ref_null") {
    const toIdx = to.typeIdx;
    const tmpLocal = allocTempLocal(fctx, from);
    fctx.body.push({ op: "local.tee", index: tmpLocal });
    fctx.body.push({ op: "ref.test", typeIdx: toIdx });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: to },
      then: [
        { op: "local.get", index: tmpLocal },
        { op: "ref.cast", typeIdx: toIdx },
      ],
      else: [{ op: "ref.null", typeIdx: toIdx }],
    });
    releaseTempLocal(fctx, tmpLocal);
    return;
  }
  if ((from.kind === "anyref" || from.kind === "eqref") && to.kind === "externref") {
    fctx.body.push({ op: "extern.convert_any" });
    return;
  }
  if (from.kind === "externref" && to.kind === "anyref") {
    fctx.body.push({ op: "any.convert_extern" });
    return;
  }
  if (from.kind === "externref" && to.kind === "eqref") {
    fctx.body.push({ op: "any.convert_extern" });
    return;
  }
  if (to.kind === "externref") {
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "ref.null.extern" });
    return;
  }
  if ((from.kind === "ref" || from.kind === "ref_null") && to.kind === "f64") {
    const wasInsideValueOf = ctx.__insideValueOfCoercion ?? false;
    if (wasInsideValueOf) {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "f64.const", value: NaN });
      return;
    }
    ctx.__insideValueOfCoercion = true;
    const cleanup = () => {
      ctx.__insideValueOfCoercion = wasInsideValueOf;
    };
    const typeIdx = from.typeIdx;
    const name = ctx.typeIdxToStructName.get(typeIdx);
    if (name !== void 0) {
      const toPrimFuncIdx = ctx.funcMap.get(`${name}_@@toPrimitive`);
      if (toPrimFuncIdx !== void 0) {
        const hint = toPrimitiveHint ?? "number";
        pushStringHint(ctx, fctx, hint);
        fctx.body.push({ op: "call", funcIdx: toPrimFuncIdx });
        const funcDef = ctx.mod.functions[toPrimFuncIdx - ctx.numImportFuncs];
        const funcType = funcDef ? ctx.mod.types[funcDef.typeIdx] : void 0;
        const retKind = (funcType?.kind === "func" && funcType.results?.[0]?.kind) || "f64";
        if (retKind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
        } else if (retKind === "externref") {
          addUnionImports(ctx);
          const unboxIdx = ctx.funcMap.get("__unbox_number");
          if (unboxIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: unboxIdx });
          } else {
            fctx.body.push({ op: "drop" });
            fctx.body.push({ op: "f64.const", value: NaN });
          }
        }
        cleanup();
        return;
      }
      const fields = ctx.structFields.get(name);
      if (fields) {
        const fieldIdx = fields.findIndex((f) => f.name === "valueOf");
        if (fieldIdx < 0) {
          const valueOfFuncIdx = ctx.funcMap.get(`${name}_valueOf`);
          if (valueOfFuncIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: valueOfFuncIdx });
            const voFuncDefIdx = valueOfFuncIdx - ctx.numImportFuncs;
            const voFuncDef = voFuncDefIdx >= 0 ? ctx.mod.functions[voFuncDefIdx] : void 0;
            const funcType = voFuncDef ? ctx.mod.types[voFuncDef.typeIdx] : void 0;
            if (funcType?.kind === "func" && funcType.results?.[0]?.kind === "i32") {
              fctx.body.push({ op: "f64.convert_i32_s" });
            } else if (funcType?.kind === "func" && funcType.results?.[0]?.kind === "externref") {
              addUnionImports(ctx);
              const unboxIdx = ctx.funcMap.get("__unbox_number");
              if (unboxIdx !== void 0) {
                fctx.body.push({ op: "call", funcIdx: unboxIdx });
              } else {
                const pfIdx = ctx.funcMap.get("parseFloat");
                if (pfIdx !== void 0) {
                  fctx.body.push({ op: "call", funcIdx: pfIdx });
                } else {
                  fctx.body.push({ op: "drop" });
                  fctx.body.push({ op: "f64.const", value: NaN });
                }
              }
            } else if (
              funcType?.kind === "func" &&
              (funcType.results?.[0]?.kind === "ref" || funcType.results?.[0]?.kind === "ref_null")
            ) {
              fctx.body.push({ op: "drop" });
              fctx.body.push({ op: "f64.const", value: NaN });
            }
            cleanup();
            return;
          }
          if (tryToStringFallback(ctx, fctx, from, typeIdx, name, fields)) {
            cleanup();
            return;
          }
          fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "f64.const", value: NaN });
          cleanup();
          return;
        }
        const valueOfField = fields[fieldIdx];
        if (!valueOfField) {
          fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "f64.const", value: NaN });
          cleanup();
          return;
        }
        if (valueOfField.type.kind === "ref" || valueOfField.type.kind === "ref_null") {
          const closureTypeIdx = valueOfField.type.typeIdx;
          const closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
          if (closureInfo) {
            const structLocal = allocLocal(fctx, `__coerce_struct_${fctx.locals.length}`, from);
            fctx.body.push({ op: "local.set", index: structLocal });
            fctx.body.push({ op: "local.get", index: structLocal });
            fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
            const closureLocal = allocLocal(fctx, `__coerce_closure_${fctx.locals.length}`, valueOfField.type);
            fctx.body.push({ op: "local.tee", index: closureLocal });
            fctx.body.push({ op: "local.get", index: closureLocal });
            fctx.body.push({ op: "struct.get", typeIdx: closureTypeIdx, fieldIdx: 0 });
            {
              const tmpFunc = allocTempLocal(fctx, { kind: "funcref" });
              fctx.body.push({ op: "local.tee", index: tmpFunc });
              fctx.body.push({ op: "ref.test", typeIdx: closureInfo.funcTypeIdx });
              fctx.body.push({
                op: "if",
                blockType: { kind: "val", type: { kind: "ref_null", typeIdx: closureInfo.funcTypeIdx } },
                then: [
                  { op: "local.get", index: tmpFunc },
                  { op: "ref.cast_null", typeIdx: closureInfo.funcTypeIdx },
                ],
                else: [{ op: "ref.null", typeIdx: closureInfo.funcTypeIdx }],
              });
              releaseTempLocal(fctx, tmpFunc);
            }
            fctx.body.push({ op: "ref.as_non_null" });
            fctx.body.push({ op: "call_ref", typeIdx: closureInfo.funcTypeIdx });
            if (!closureInfo.returnType) {
              fctx.body.push({ op: "f64.const", value: NaN });
            } else if (closureInfo.returnType.kind === "i32") {
              fctx.body.push({ op: "f64.convert_i32_s" });
            } else if (closureInfo.returnType.kind === "externref" || closureInfo.returnType.kind === "ref_extern") {
              addUnionImports(ctx);
              const unboxIdx = ctx.funcMap.get("__unbox_number");
              if (unboxIdx !== void 0) {
                fctx.body.push({ op: "call", funcIdx: unboxIdx });
              } else {
                fctx.body.push({ op: "drop" });
                fctx.body.push({ op: "f64.const", value: NaN });
              }
            } else if (closureInfo.returnType.kind === "ref" || closureInfo.returnType.kind === "ref_null") {
              fctx.body.push({ op: "drop" });
              fctx.body.push({ op: "f64.const", value: NaN });
            }
            cleanup();
            return;
          }
        }
        if (valueOfField.type.kind === "externref") {
          fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "f64.const", value: NaN });
          cleanup();
          return;
        }
        if (valueOfField.type.kind === "eqref") {
          const trackedTypes = ctx.valueOfClosureTypes.get(name) ?? [];
          const callableClosureTypes = [];
          for (const closureTypeIdx of trackedTypes) {
            const info = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
            if (info && info.paramTypes.length === 0) {
              callableClosureTypes.push({ closureTypeIdx, info });
            }
          }
          if (callableClosureTypes.length > 0) {
            const structLocal = allocLocal(fctx, `__vo_struct_${fctx.locals.length}`, from);
            fctx.body.push({ op: "local.set", index: structLocal });
            fctx.body.push({ op: "local.get", index: structLocal });
            fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
            const eqLocal = allocLocal(fctx, `__vo_eq_${fctx.locals.length}`, { kind: "eqref" });
            fctx.body.push({ op: "local.set", index: eqLocal });
            const buildDispatch = (idx) => {
              if (idx >= callableClosureTypes.length) {
                return [{ op: "f64.const", value: NaN }];
              }
              const { closureTypeIdx, info } = callableClosureTypes[idx];
              const closureLocal = allocLocal(fctx, `__vo_cl_${fctx.locals.length}`, {
                kind: "ref",
                typeIdx: closureTypeIdx,
              });
              const funcTmp = allocLocal(fctx, `__vo_fn_${fctx.locals.length}`, { kind: "funcref" });
              const thenInstrs = [
                { op: "local.get", index: eqLocal },
                { op: "ref.cast", typeIdx: closureTypeIdx },
                { op: "local.tee", index: closureLocal },
                { op: "local.get", index: closureLocal },
                { op: "struct.get", typeIdx: closureTypeIdx, fieldIdx: 0 },
                // Guarded funcref cast to avoid illegal cast traps
                { op: "local.tee", index: funcTmp },
                { op: "ref.test", typeIdx: info.funcTypeIdx },
                {
                  op: "if",
                  blockType: { kind: "val", type: { kind: "ref_null", typeIdx: info.funcTypeIdx } },
                  then: [
                    { op: "local.get", index: funcTmp },
                    { op: "ref.cast_null", typeIdx: info.funcTypeIdx },
                  ],
                  else: [{ op: "ref.null", typeIdx: info.funcTypeIdx }],
                },
                { op: "ref.as_non_null" },
                { op: "call_ref", typeIdx: info.funcTypeIdx },
              ];
              if (info.returnType?.kind === "i32") {
                thenInstrs.push({ op: "f64.convert_i32_s" });
              } else if (info.returnType?.kind === "externref" || info.returnType?.kind === "ref_extern") {
                addUnionImports(ctx);
                const unboxIdx = ctx.funcMap.get("__unbox_number");
                if (unboxIdx !== void 0) {
                  thenInstrs.push({ op: "call", funcIdx: unboxIdx });
                } else {
                  thenInstrs.push({ op: "drop" });
                  thenInstrs.push({ op: "f64.const", value: NaN });
                }
              } else if (!info.returnType) {
                thenInstrs.push({ op: "f64.const", value: NaN });
              } else if (info.returnType.kind !== "f64") {
                thenInstrs.push({ op: "drop" });
                thenInstrs.push({ op: "f64.const", value: NaN });
              }
              return [
                { op: "local.get", index: eqLocal },
                { op: "ref.test", typeIdx: closureTypeIdx },
                {
                  op: "if",
                  blockType: { kind: "val", type: { kind: "f64" } },
                  then: thenInstrs,
                  else: buildDispatch(idx + 1),
                },
              ];
            };
            for (const instr of buildDispatch(0)) {
              fctx.body.push(instr);
            }
            return;
          }
          const standaloneValueOf = ctx.funcMap.get(`${name}_valueOf`);
          if (standaloneValueOf !== void 0) {
            fctx.body.push({ op: "call", funcIdx: standaloneValueOf });
            const funcType = ctx.mod.types[ctx.mod.functions[standaloneValueOf - ctx.numImportFuncs]?.typeIdx ?? -1];
            const retKind = funcType?.kind === "func" ? funcType.results?.[0]?.kind : void 0;
            if (retKind === "i32") {
              fctx.body.push({ op: "f64.convert_i32_s" });
            } else if (retKind === "externref" || retKind === "ref_extern") {
              addUnionImports(ctx);
              const unboxIdx = ctx.funcMap.get("__unbox_number");
              if (unboxIdx !== void 0) {
                fctx.body.push({ op: "call", funcIdx: unboxIdx });
              } else {
                fctx.body.push({ op: "drop" });
                fctx.body.push({ op: "f64.const", value: NaN });
              }
            } else if (retKind === "ref" || retKind === "ref_null") {
              fctx.body.push({ op: "drop" });
              fctx.body.push({ op: "f64.const", value: NaN });
            }
            return;
          }
          if (tryToStringFallback(ctx, fctx, from, typeIdx, name, fields)) {
            cleanup();
            return;
          }
          fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "f64.const", value: NaN });
          cleanup();
          return;
        }
      }
    }
  }
  fctx.body.push({ op: "drop" });
  pushDefaultValue(fctx, to, ctx);
}
function tryToStringFallback(ctx, fctx, from, typeIdx, structName, fields) {
  const toStrFieldIdx = fields.findIndex((f) => f.name === "toString");
  if (toStrFieldIdx >= 0) {
    const toStrField = fields[toStrFieldIdx];
    if (toStrField.type.kind === "ref" || toStrField.type.kind === "ref_null") {
      const closureTypeIdx = toStrField.type.typeIdx;
      const closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
      if (closureInfo) {
        const structLocal = allocLocal(fctx, `__ts_struct_${fctx.locals.length}`, from);
        fctx.body.push({ op: "local.set", index: structLocal });
        fctx.body.push({ op: "local.get", index: structLocal });
        fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: toStrFieldIdx });
        const closureLocal = allocLocal(fctx, `__ts_closure_${fctx.locals.length}`, toStrField.type);
        fctx.body.push({ op: "local.tee", index: closureLocal });
        fctx.body.push({ op: "local.get", index: closureLocal });
        fctx.body.push({ op: "struct.get", typeIdx: closureTypeIdx, fieldIdx: 0 });
        {
          const tmpFunc = allocTempLocal(fctx, { kind: "funcref" });
          fctx.body.push({ op: "local.tee", index: tmpFunc });
          fctx.body.push({ op: "ref.test", typeIdx: closureInfo.funcTypeIdx });
          fctx.body.push({
            op: "if",
            blockType: { kind: "val", type: { kind: "ref_null", typeIdx: closureInfo.funcTypeIdx } },
            then: [
              { op: "local.get", index: tmpFunc },
              { op: "ref.cast_null", typeIdx: closureInfo.funcTypeIdx },
            ],
            else: [{ op: "ref.null", typeIdx: closureInfo.funcTypeIdx }],
          });
          releaseTempLocal(fctx, tmpFunc);
        }
        fctx.body.push({ op: "ref.as_non_null" });
        fctx.body.push({ op: "call_ref", typeIdx: closureInfo.funcTypeIdx });
        emitToStringResultToF64(ctx, fctx, closureInfo.returnType);
        return true;
      }
    }
    if (toStrField.type.kind === "eqref") {
      const trackedTypes = ctx.valueOfClosureTypes.get(structName) ?? [];
      for (const closureTypeIdx of trackedTypes) {
        const info = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
        if (info && info.paramTypes.length === 0) {
          const structLocal = allocLocal(fctx, `__ts_struct_${fctx.locals.length}`, from);
          fctx.body.push({ op: "local.set", index: structLocal });
          fctx.body.push({ op: "local.get", index: structLocal });
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: toStrFieldIdx });
          const eqLocal = allocLocal(fctx, `__ts_eq_${fctx.locals.length}`, { kind: "eqref" });
          fctx.body.push({ op: "local.set", index: eqLocal });
          fctx.body.push({ op: "local.get", index: eqLocal });
          fctx.body.push({ op: "ref.test", typeIdx: closureTypeIdx });
          fctx.body.push({
            op: "if",
            blockType: { kind: "val", type: { kind: "f64" } },
            then: [
              { op: "local.get", index: eqLocal },
              { op: "ref.cast", typeIdx: closureTypeIdx },
              (() => {
                const closureLocal2 = allocLocal(fctx, `__ts_cl2_${fctx.locals.length}`, {
                  kind: "ref",
                  typeIdx: closureTypeIdx,
                });
                return { op: "local.tee", index: closureLocal2 };
              })(),
              (() => {
                const closureLocal2 = fctx.locals.length - 1 + fctx.params.length;
                return { op: "local.get", index: closureLocal2 };
              })(),
              { op: "struct.get", typeIdx: closureTypeIdx, fieldIdx: 0 },
              (() => {
                const funcTmp = allocTempLocal(fctx, { kind: "funcref" });
                const instrs = [
                  { op: "local.tee", index: funcTmp },
                  { op: "ref.test", typeIdx: info.funcTypeIdx },
                  {
                    op: "if",
                    blockType: { kind: "val", type: { kind: "ref_null", typeIdx: info.funcTypeIdx } },
                    then: [
                      { op: "local.get", index: funcTmp },
                      { op: "ref.cast_null", typeIdx: info.funcTypeIdx },
                    ],
                    else: [{ op: "ref.null", typeIdx: info.funcTypeIdx }],
                  },
                  { op: "ref.as_non_null" },
                  { op: "call_ref", typeIdx: info.funcTypeIdx },
                ];
                releaseTempLocal(fctx, funcTmp);
                if (info.returnType?.kind === "i32") {
                  instrs.push({ op: "f64.convert_i32_s" });
                } else if (info.returnType?.kind === "externref" || info.returnType?.kind === "ref_extern") {
                  addUnionImports(ctx);
                  const unboxIdx = ctx.funcMap.get("__unbox_number");
                  if (unboxIdx !== void 0) {
                    instrs.push({ op: "call", funcIdx: unboxIdx });
                  } else {
                    instrs.push({ op: "drop" });
                    instrs.push({ op: "f64.const", value: NaN });
                  }
                } else if (!info.returnType || info.returnType.kind !== "f64") {
                  if (info.returnType) instrs.push({ op: "drop" });
                  instrs.push({ op: "f64.const", value: NaN });
                }
                return instrs;
              })(),
            ].flat(),
            else: [{ op: "f64.const", value: NaN }],
          });
          return true;
        }
      }
    }
  }
  const toStrFuncIdx = ctx.funcMap.get(`${structName}_toString`);
  if (toStrFuncIdx !== void 0) {
    fctx.body.push({ op: "call", funcIdx: toStrFuncIdx });
    const funcType = ctx.mod.types[ctx.mod.functions[toStrFuncIdx - ctx.numImportFuncs]?.typeIdx ?? -1];
    const retKind = funcType?.kind === "func" ? funcType.results?.[0]?.kind : void 0;
    emitToStringResultToF64ByKind(ctx, fctx, retKind);
    return true;
  }
  return false;
}
function emitToStringResultToF64(ctx, fctx, returnType) {
  if (!returnType) {
    fctx.body.push({ op: "f64.const", value: NaN });
  } else if (returnType.kind === "f64") {
  } else if (returnType.kind === "i32") {
    fctx.body.push({ op: "f64.convert_i32_s" });
  } else if (returnType.kind === "externref" || returnType.kind === "ref_extern") {
    addUnionImports(ctx);
    const unboxIdx = ctx.funcMap.get("__unbox_number");
    if (unboxIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: unboxIdx });
    } else {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "f64.const", value: NaN });
    }
  } else {
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "f64.const", value: NaN });
  }
}
function emitToStringResultToF64ByKind(ctx, fctx, retKind) {
  if (retKind === "f64") {
  } else if (retKind === "i32") {
    fctx.body.push({ op: "f64.convert_i32_s" });
  } else if (retKind === "externref" || retKind === "ref_extern") {
    addUnionImports(ctx);
    const unboxIdx = ctx.funcMap.get("__unbox_number");
    if (unboxIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: unboxIdx });
    } else {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "f64.const", value: NaN });
    }
  } else {
    if (retKind && retKind !== "void") fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "f64.const", value: NaN });
  }
}
function emitSafeExternrefToF64(ctx, fctx) {
  addUnionImports(ctx);
  const unboxIdx = ctx.funcMap.get("__unbox_number");
  const typeofNumIdx = ctx.funcMap.get("__typeof_number");
  const tmpLocal = allocTempLocal(fctx, { kind: "externref" });
  fctx.body.push({ op: "local.tee", index: tmpLocal });
  fctx.body.push({ op: "call", funcIdx: typeofNumIdx });
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: { kind: "f64" } },
    then: [
      // JS number: safe to unbox
      { op: "local.get", index: tmpLocal },
      { op: "call", funcIdx: unboxIdx },
    ],
    else: [
      // Not a number (GC struct, string, null, etc.): return NaN
      { op: "f64.const", value: NaN },
    ],
  });
  releaseTempLocal(fctx, tmpLocal);
}
function emitUndefinedValue(ctx, fctx) {
  const funcIdx = ensureLateImport(ctx, "__get_undefined", [], [{ kind: "externref" }]);
  if (funcIdx !== void 0) {
    flushLateImportShifts(ctx, fctx);
    fctx.body.push({ op: "call", funcIdx });
  } else {
    fctx.body.push({ op: "ref.null.extern" });
  }
}
function pushDefaultValue(fctx, type, ctx) {
  switch (type.kind) {
    case "f64":
      fctx.body.push({ op: "f64.const", value: 0 });
      break;
    case "i32":
      fctx.body.push({ op: "i32.const", value: 0 });
      break;
    case "i64":
      fctx.body.push({ op: "i64.const", value: 0n });
      break;
    case "externref":
      if (ctx) {
        emitUndefinedValue(ctx, fctx);
      } else {
        fctx.body.push({ op: "ref.null.extern" });
      }
      break;
    case "eqref":
      fctx.body.push({ op: "ref.null.eq" });
      break;
    case "anyref":
      fctx.body.push({ op: "ref.null.eq" });
      break;
    case "ref_null":
      fctx.body.push({ op: "ref.null", typeIdx: type.typeIdx });
      break;
    case "ref":
      fctx.body.push({ op: "ref.null", typeIdx: type.typeIdx });
      fctx.body.push({ op: "ref.as_non_null" });
      break;
    default:
      fctx.body.push({ op: "i32.const", value: 0 });
      break;
  }
}
function pushParamSentinel(fctx, type, ctx, optInfo) {
  if (optInfo?.constantDefault) {
    const cd = optInfo.constantDefault;
    if (cd.kind === "f64") {
      fctx.body.push({ op: "f64.const", value: cd.value });
    } else {
      fctx.body.push({ op: "i32.const", value: cd.value });
    }
    return;
  }
  if (type.kind === "f64" && (optInfo?.hasExpressionDefault ?? true)) {
    fctx.body.push({ op: "i64.const", value: 0x7ff00000deadc0den });
    fctx.body.push({ op: "f64.reinterpret_i64" });
  } else {
    pushDefaultValue(fctx, type, ctx);
  }
}
function defaultValueInstrs(vt) {
  switch (vt.kind) {
    case "f64":
      return [{ op: "i64.const", value: 0x7ff00000deadc0den }, { op: "f64.reinterpret_i64" }];
    case "f32":
      return [{ op: "f32.const", value: 0 }];
    case "i32":
      return [{ op: "i32.const", value: 0 }];
    case "i64":
      return [{ op: "i64.const", value: 0n }];
    case "externref":
    case "ref_extern":
      return [{ op: "ref.null.extern" }];
    case "ref":
      return [{ op: "ref.null", typeIdx: vt.typeIdx }];
    case "ref_null":
      return [{ op: "ref.null", typeIdx: vt.typeIdx }];
    case "eqref":
      return [{ op: "ref.null.eq" }];
    case "anyref":
      return [{ op: "ref.null.eq" }];
    case "funcref":
      return [{ op: "ref.null.func" }];
    default:
      return [{ op: "i64.const", value: 0x7ff00000deadc0den }, { op: "f64.reinterpret_i64" }];
  }
}
function coercionInstrs(ctx, from, to, fctx) {
  if (from.kind === to.kind) return [];
  if (from.kind === "f64" && to.kind === "externref") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__box_number");
    if (funcIdx !== void 0) {
      return [{ op: "call", funcIdx }];
    }
  }
  if (from.kind === "i32" && to.kind === "externref") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__box_number");
    if (funcIdx !== void 0) {
      return [{ op: "f64.convert_i32_s" }, { op: "call", funcIdx }];
    }
  }
  if (from.kind === "i32" && to.kind === "f64") {
    return [{ op: "f64.convert_i32_s" }];
  }
  if (from.kind === "f64" && to.kind === "i32") {
    return [{ op: "i32.trunc_sat_f64_s" }];
  }
  if (from.kind === "externref" && to.kind === "f64") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__unbox_number");
    if (funcIdx !== void 0) {
      return [{ op: "call", funcIdx }];
    }
  }
  if (from.kind === "ref_null" && to.kind === "ref") {
    return [{ op: "ref.as_non_null" }];
  }
  if ((from.kind === "ref" || from.kind === "ref_null") && to.kind === "externref") {
    return [{ op: "extern.convert_any" }];
  }
  if (from.kind === "externref" && to.kind === "i32") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__unbox_number");
    if (funcIdx !== void 0) {
      return [{ op: "call", funcIdx }, { op: "i32.trunc_sat_f64_s" }];
    }
  }
  if (from.kind === "i64" && to.kind === "f64") {
    return [{ op: "f64.convert_i64_s" }];
  }
  if (from.kind === "f64" && to.kind === "i64") {
    return [{ op: "i64.trunc_sat_f64_s" }];
  }
  if (from.kind === "i32" && to.kind === "i64") {
    return [{ op: "i64.extend_i32_s" }];
  }
  if (from.kind === "i64" && to.kind === "i32") {
    return [{ op: "i32.wrap_i64" }];
  }
  if (from.kind === "i64" && to.kind === "externref") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__box_number");
    if (funcIdx !== void 0) {
      return [{ op: "f64.convert_i64_s" }, { op: "call", funcIdx }];
    }
  }
  if (from.kind === "externref" && to.kind === "i64") {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__unbox_number");
    if (funcIdx !== void 0) {
      return [{ op: "call", funcIdx }, { op: "i64.trunc_sat_f64_s" }];
    }
  }
  if ((from.kind === "ref" || from.kind === "ref_null") && to.kind === "f64") {
    return [{ op: "drop" }, { op: "f64.const", value: NaN }];
  }
  if ((from.kind === "ref" || from.kind === "ref_null") && to.kind === "i32") {
    return [{ op: "drop" }, { op: "i32.const", value: 0 }];
  }
  if (from.kind === "funcref" && to.kind === "externref") {
    return [{ op: "drop" }, { op: "ref.null.extern" }];
  }
  if (from.kind === "funcref" && to.kind === "anyref") {
    return [];
  }
  if (from.kind === "eqref" && to.kind === "externref") {
    return [{ op: "extern.convert_any" }];
  }
  if (from.kind === "anyref" && to.kind === "externref") {
    return [{ op: "extern.convert_any" }];
  }
  if (from.kind === "externref" && to.kind === "anyref") {
    return [{ op: "any.convert_extern" }];
  }
  if (from.kind === "externref" && to.kind === "eqref") {
    return [{ op: "any.convert_extern" }];
  }
  if (from.kind === "externref" && to.kind === "ref_null") {
    const toIdx = to.typeIdx;
    if (fctx) {
      const tmp = allocTempLocal(fctx, { kind: "anyref" });
      const result = [
        { op: "any.convert_extern" },
        { op: "local.tee", index: tmp },
        { op: "ref.test", typeIdx: toIdx },
        {
          op: "if",
          blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
          then: [
            { op: "local.get", index: tmp },
            { op: "ref.cast_null", typeIdx: toIdx },
          ],
          else: [{ op: "ref.null", typeIdx: toIdx }],
        },
      ];
      releaseTempLocal(fctx, tmp);
      return result;
    }
    return [{ op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx: toIdx }];
  }
  if (from.kind === "externref" && to.kind === "ref") {
    const toIdx = to.typeIdx;
    if (fctx) {
      const tmp = allocTempLocal(fctx, { kind: "anyref" });
      const result = [
        { op: "any.convert_extern" },
        { op: "local.tee", index: tmp },
        { op: "ref.test", typeIdx: toIdx },
        {
          op: "if",
          blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
          then: [
            { op: "local.get", index: tmp },
            { op: "ref.cast_null", typeIdx: toIdx },
          ],
          else: [{ op: "ref.null", typeIdx: toIdx }],
        },
      ];
      releaseTempLocal(fctx, tmp);
      return result;
    }
    return [{ op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx: toIdx }];
  }
  if ((from.kind === "eqref" || from.kind === "anyref") && to.kind === "ref_null") {
    const toIdx = to.typeIdx;
    if (fctx) {
      const tmp = allocTempLocal(fctx, from);
      const result = [
        { op: "local.tee", index: tmp },
        { op: "ref.test", typeIdx: toIdx },
        {
          op: "if",
          blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
          then: [
            { op: "local.get", index: tmp },
            { op: "ref.cast_null", typeIdx: toIdx },
          ],
          else: [{ op: "ref.null", typeIdx: toIdx }],
        },
      ];
      releaseTempLocal(fctx, tmp);
      return result;
    }
    return [{ op: "ref.cast_null", typeIdx: toIdx }];
  }
  if ((from.kind === "eqref" || from.kind === "anyref") && to.kind === "ref") {
    const toIdx = to.typeIdx;
    if (fctx) {
      const tmp = allocTempLocal(fctx, from);
      const result = [
        { op: "local.tee", index: tmp },
        { op: "ref.test", typeIdx: toIdx },
        {
          op: "if",
          blockType: { kind: "val", type: { kind: "ref_null", typeIdx: toIdx } },
          then: [
            { op: "local.get", index: tmp },
            { op: "ref.cast_null", typeIdx: toIdx },
          ],
          else: [{ op: "ref.null", typeIdx: toIdx }],
        },
      ];
      releaseTempLocal(fctx, tmp);
      return result;
    }
    return [{ op: "ref.cast", typeIdx: toIdx }];
  }
  return [];
}
registerCoerceType(coerceType2);

// src/codegen/timsort.ts
var L = (i) => ({ op: "local.get", index: i });
var LS = (i) => ({ op: "local.set", index: i });
var I = (v) => ({ op: "i32.const", value: v });
var ADD = { op: "i32.add" };
var SUB = { op: "i32.sub" };
var LT_S = { op: "i32.lt_s" };
var LE_S = { op: "i32.le_s" };
var GT_S = { op: "i32.gt_s" };
var GE_S = { op: "i32.ge_s" };
var EQZ = { op: "i32.eqz" };
var I_AND = { op: "i32.and" };
var I_OR = { op: "i32.or" };
var SHR_U = { op: "i32.shr_u" };
var RET = { op: "return" };
var AG = (t) => ({ op: "array.get", typeIdx: t });
var AS = (t) => ({ op: "array.set", typeIdx: t });
var AC = (d, s) => ({ op: "array.copy", dstTypeIdx: d, srcTypeIdx: s });
var AND = (t) => ({ op: "array.new_default", typeIdx: t });
function BLOCK(body) {
  return { op: "block", blockType: { kind: "empty" }, body };
}
function LOOP(body) {
  return { op: "loop", blockType: { kind: "empty" }, body };
}
function BR(d) {
  return { op: "br", depth: d };
}
function BR_IF(d) {
  return { op: "br_if", depth: d };
}
function IF(then, els) {
  const r = { op: "if", blockType: { kind: "empty" }, then };
  if (els) r.else = els;
  return r;
}
function CALL(idx) {
  return { op: "call", funcIdx: idx };
}
function emitFunc(ctx, name, params, results, locals, body) {
  const typeIdx = addFuncType(ctx, params, results, `${name}_type`);
  const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set(name, funcIdx);
  ctx.mod.functions.push({ name, typeIdx, locals, body, exported: false });
  return funcIdx;
}
function emitInsertionSort(ctx, arrTypeIdx, elemKind) {
  const name = `__isort_${elemKind}`;
  const existing = ctx.funcMap.get(name);
  if (existing !== void 0) return existing;
  const elemType = { kind: elemKind };
  const arrRef = { kind: "ref_null", typeIdx: arrTypeIdx };
  const gtOp = elemKind === "i32" ? { op: "i32.gt_s" } : { op: "f64.gt" };
  const DATA = 0,
    LO = 1,
    HI = 2,
    II = 3,
    JJ = 4,
    KEY = 5;
  const body = [
    // i = lo + 1
    L(LO),
    I(1),
    ADD,
    LS(II),
    BLOCK([
      LOOP([
        // if i >= hi: break
        L(II),
        L(HI),
        GE_S,
        BR_IF(1),
        // key = data[i]
        L(DATA),
        L(II),
        AG(arrTypeIdx),
        LS(KEY),
        // j = i - 1
        L(II),
        I(1),
        SUB,
        LS(JJ),
        // Inner: while j >= lo && data[j] > key
        BLOCK([
          LOOP([
            L(JJ),
            L(LO),
            LT_S,
            BR_IF(1),
            L(DATA),
            L(JJ),
            AG(arrTypeIdx),
            L(KEY),
            gtOp,
            EQZ,
            BR_IF(1),
            // data[j+1] = data[j]
            L(DATA),
            L(JJ),
            I(1),
            ADD,
            L(DATA),
            L(JJ),
            AG(arrTypeIdx),
            AS(arrTypeIdx),
            // j--
            L(JJ),
            I(1),
            SUB,
            LS(JJ),
            BR(0),
          ]),
        ]),
        // data[j+1] = key
        L(DATA),
        L(JJ),
        I(1),
        ADD,
        L(KEY),
        AS(arrTypeIdx),
        // i++
        L(II),
        I(1),
        ADD,
        LS(II),
        BR(0),
      ]),
    ]),
  ];
  return emitFunc(
    ctx,
    name,
    [arrRef, { kind: "i32" }, { kind: "i32" }],
    [],
    [
      { name: "i", type: { kind: "i32" } },
      { name: "j", type: { kind: "i32" } },
      { name: "key", type: elemType },
    ],
    body,
  );
}
function emitMerge(ctx, arrTypeIdx, elemKind) {
  const name = `__merge_${elemKind}`;
  const existing = ctx.funcMap.get(name);
  if (existing !== void 0) return existing;
  const arrRef = { kind: "ref_null", typeIdx: arrTypeIdx };
  const leOp = elemKind === "i32" ? { op: "i32.le_s" } : { op: "f64.le" };
  const DATA = 0,
    TMP = 1,
    LO = 2,
    MID = 3,
    HI = 4;
  const LEFT = 5,
    II = 6,
    JJ = 7,
    KK = 8;
  const body = [
    // leftLen = mid - lo
    L(MID),
    L(LO),
    SUB,
    LS(LEFT),
    // if leftLen <= 0: return
    L(LEFT),
    I(0),
    LE_S,
    IF([RET]),
    // Copy left half to tmp[0..leftLen)
    L(TMP),
    I(0),
    L(DATA),
    L(LO),
    L(LEFT),
    AC(arrTypeIdx, arrTypeIdx),
    // i=0, j=mid, k=lo
    I(0),
    LS(II),
    L(MID),
    LS(JJ),
    L(LO),
    LS(KK),
    // Merge loop
    BLOCK([
      LOOP([
        L(II),
        L(LEFT),
        GE_S,
        BR_IF(1),
        // left exhausted
        L(JJ),
        L(HI),
        GE_S,
        BR_IF(1),
        // right exhausted
        // if tmp[i] <= data[j]: take from left, else from right
        L(TMP),
        L(II),
        AG(arrTypeIdx),
        L(DATA),
        L(JJ),
        AG(arrTypeIdx),
        leOp,
        IF(
          [L(DATA), L(KK), L(TMP), L(II), AG(arrTypeIdx), AS(arrTypeIdx), L(II), I(1), ADD, LS(II)],
          [L(DATA), L(KK), L(DATA), L(JJ), AG(arrTypeIdx), AS(arrTypeIdx), L(JJ), I(1), ADD, LS(JJ)],
        ),
        L(KK),
        I(1),
        ADD,
        LS(KK),
        BR(0),
      ]),
    ]),
    // Copy remaining left elements (right are already in place)
    L(II),
    L(LEFT),
    LT_S,
    IF([L(DATA), L(KK), L(TMP), L(II), L(LEFT), L(II), SUB, AC(arrTypeIdx, arrTypeIdx)]),
  ];
  return emitFunc(
    ctx,
    name,
    [arrRef, arrRef, { kind: "i32" }, { kind: "i32" }, { kind: "i32" }],
    [],
    [
      { name: "leftLen", type: { kind: "i32" } },
      { name: "i", type: { kind: "i32" } },
      { name: "j", type: { kind: "i32" } },
      { name: "k", type: { kind: "i32" } },
    ],
    body,
  );
}
function emitMergeRun(ctx, arrTypeIdx, i32ArrTypeIdx, elemKind, mergeFuncIdx) {
  const name = `__merge_run_${elemKind}`;
  const existing = ctx.funcMap.get(name);
  if (existing !== void 0) return existing;
  const arrRef = { kind: "ref_null", typeIdx: arrTypeIdx };
  const i32ArrRef = { kind: "ref_null", typeIdx: i32ArrTypeIdx };
  const DATA = 0,
    TMP = 1,
    SBASE = 2,
    SLEN = 3,
    SSIZE = 4,
    IDX = 5;
  const BASE1 = 6,
    LEN1 = 7,
    LEN2 = 8;
  const body = [
    // base1 = sBase[idx]
    L(SBASE),
    L(IDX),
    AG(i32ArrTypeIdx),
    LS(BASE1),
    // len1 = sLen[idx]
    L(SLEN),
    L(IDX),
    AG(i32ArrTypeIdx),
    LS(LEN1),
    // len2 = sLen[idx + 1]
    L(SLEN),
    L(IDX),
    I(1),
    ADD,
    AG(i32ArrTypeIdx),
    LS(LEN2),
    // call __merge(data, tmp, base1, base1+len1, base1+len1+len2)
    L(DATA),
    L(TMP),
    L(BASE1),
    L(BASE1),
    L(LEN1),
    ADD,
    L(BASE1),
    L(LEN1),
    ADD,
    L(LEN2),
    ADD,
    CALL(mergeFuncIdx),
    // sLen[idx] = len1 + len2
    L(SLEN),
    L(IDX),
    L(LEN1),
    L(LEN2),
    ADD,
    AS(i32ArrTypeIdx),
    // Shift stack entries: copy from idx+2.. to idx+1..
    // length = stackSize - idx - 2 (always >= 0)
    L(SBASE),
    L(IDX),
    I(1),
    ADD,
    L(SBASE),
    L(IDX),
    I(2),
    ADD,
    L(SSIZE),
    L(IDX),
    SUB,
    I(2),
    SUB,
    AC(i32ArrTypeIdx, i32ArrTypeIdx),
    L(SLEN),
    L(IDX),
    I(1),
    ADD,
    L(SLEN),
    L(IDX),
    I(2),
    ADD,
    L(SSIZE),
    L(IDX),
    SUB,
    I(2),
    SUB,
    AC(i32ArrTypeIdx, i32ArrTypeIdx),
    // return stackSize - 1
    L(SSIZE),
    I(1),
    SUB,
  ];
  return emitFunc(
    ctx,
    name,
    [arrRef, arrRef, i32ArrRef, i32ArrRef, { kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [
      { name: "base1", type: { kind: "i32" } },
      { name: "len1", type: { kind: "i32" } },
      { name: "len2", type: { kind: "i32" } },
    ],
    body,
  );
}
function emitTimsortMain(ctx, vecTypeIdx, arrTypeIdx, i32ArrTypeIdx, elemKind, isortIdx, mergeRunIdx) {
  const name = `__timsort_${elemKind}`;
  const elemType = { kind: elemKind };
  const vecRef = { kind: "ref_null", typeIdx: vecTypeIdx };
  const ltOp = elemKind === "i32" ? { op: "i32.lt_s" } : { op: "f64.lt" };
  const VEC = 0;
  const DATA = 1,
    LEN = 2,
    TMP = 3,
    MIN_RUN = 4;
  const NMR = 5,
    RMR = 6;
  const LO = 7,
    RUN_END = 8,
    RUN_LEN = 9,
    FORCE = 10;
  const SB = 11,
    SL = 12,
    SS = 13;
  const SN = 14,
    MIDX = 15;
  const I_REV = 16,
    J_REV = 17,
    T_SWAP = 18,
    SHOULD = 19;
  const locals = [
    { name: "data", type: { kind: "ref_null", typeIdx: arrTypeIdx } },
    { name: "len", type: { kind: "i32" } },
    { name: "tmp", type: { kind: "ref_null", typeIdx: arrTypeIdx } },
    { name: "minRun", type: { kind: "i32" } },
    { name: "nmr", type: { kind: "i32" } },
    { name: "rmr", type: { kind: "i32" } },
    { name: "lo", type: { kind: "i32" } },
    { name: "runEnd", type: { kind: "i32" } },
    { name: "runLen", type: { kind: "i32" } },
    { name: "force", type: { kind: "i32" } },
    { name: "sBase", type: { kind: "ref_null", typeIdx: i32ArrTypeIdx } },
    { name: "sLen", type: { kind: "ref_null", typeIdx: i32ArrTypeIdx } },
    { name: "stackSize", type: { kind: "i32" } },
    { name: "sn", type: { kind: "i32" } },
    { name: "mergeIdx", type: { kind: "i32" } },
    { name: "iRev", type: { kind: "i32" } },
    { name: "jRev", type: { kind: "i32" } },
    { name: "tSwap", type: elemType },
    { name: "should", type: { kind: "i32" } },
  ];
  const sec1 = [
    L(VEC),
    { op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 },
    LS(DATA),
    L(VEC),
    { op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 },
    LS(LEN),
    // if len < 2: return
    L(LEN),
    I(2),
    LT_S,
    IF([RET]),
  ];
  const sec2 = [L(LEN), I(64), LT_S, IF([L(DATA), I(0), L(LEN), CALL(isortIdx), RET])];
  const sec3 = [
    L(LEN),
    LS(NMR),
    I(0),
    LS(RMR),
    BLOCK([
      LOOP([
        L(NMR),
        I(64),
        LT_S,
        BR_IF(1),
        // rmr |= (nmr & 1)
        L(RMR),
        L(NMR),
        I(1),
        I_AND,
        I_OR,
        LS(RMR),
        // nmr >>= 1
        L(NMR),
        I(1),
        SHR_U,
        LS(NMR),
        BR(0),
      ]),
    ]),
    L(NMR),
    L(RMR),
    ADD,
    LS(MIN_RUN),
  ];
  const sec4 = [
    L(LEN),
    AND(arrTypeIdx),
    LS(TMP),
    I(85),
    AND(i32ArrTypeIdx),
    LS(SB),
    I(85),
    AND(i32ArrTypeIdx),
    LS(SL),
    I(0),
    LS(SS),
    I(0),
    LS(LO),
  ];
  const detectRun = [
    // runEnd = lo + 1
    L(LO),
    I(1),
    ADD,
    LS(RUN_END),
    L(RUN_END),
    L(LEN),
    GE_S,
    IF(
      // Single element remaining
      [I(1), LS(RUN_LEN)],
      [
        // Check if descending: data[lo+1] < data[lo]
        L(DATA),
        L(RUN_END),
        AG(arrTypeIdx),
        L(DATA),
        L(LO),
        AG(arrTypeIdx),
        ltOp,
        IF(
          // Descending run
          [
            L(RUN_END),
            I(1),
            ADD,
            LS(RUN_END),
            BLOCK([
              LOOP([
                L(RUN_END),
                L(LEN),
                GE_S,
                BR_IF(1),
                // if data[runEnd] >= data[runEnd-1]: stop
                L(DATA),
                L(RUN_END),
                AG(arrTypeIdx),
                L(DATA),
                L(RUN_END),
                I(1),
                SUB,
                AG(arrTypeIdx),
                ltOp,
                EQZ,
                BR_IF(1),
                L(RUN_END),
                I(1),
                ADD,
                LS(RUN_END),
                BR(0),
              ]),
            ]),
            // Reverse [lo, runEnd)
            L(LO),
            LS(I_REV),
            L(RUN_END),
            I(1),
            SUB,
            LS(J_REV),
            BLOCK([
              LOOP([
                L(I_REV),
                L(J_REV),
                GE_S,
                BR_IF(1),
                // swap data[iRev] <-> data[jRev]
                L(DATA),
                L(I_REV),
                AG(arrTypeIdx),
                LS(T_SWAP),
                L(DATA),
                L(I_REV),
                L(DATA),
                L(J_REV),
                AG(arrTypeIdx),
                AS(arrTypeIdx),
                L(DATA),
                L(J_REV),
                L(T_SWAP),
                AS(arrTypeIdx),
                L(I_REV),
                I(1),
                ADD,
                LS(I_REV),
                L(J_REV),
                I(1),
                SUB,
                LS(J_REV),
                BR(0),
              ]),
            ]),
          ],
          // Ascending run
          [
            L(RUN_END),
            I(1),
            ADD,
            LS(RUN_END),
            BLOCK([
              LOOP([
                L(RUN_END),
                L(LEN),
                GE_S,
                BR_IF(1),
                // if data[runEnd] < data[runEnd-1]: stop
                L(DATA),
                L(RUN_END),
                AG(arrTypeIdx),
                L(DATA),
                L(RUN_END),
                I(1),
                SUB,
                AG(arrTypeIdx),
                ltOp,
                BR_IF(1),
                L(RUN_END),
                I(1),
                ADD,
                LS(RUN_END),
                BR(0),
              ]),
            ]),
          ],
        ),
        // runLen = runEnd - lo
        L(RUN_END),
        L(LO),
        SUB,
        LS(RUN_LEN),
      ],
    ),
  ];
  const extendRun = [
    // force = min(minRun, len - lo)
    L(MIN_RUN),
    L(LEN),
    L(LO),
    SUB,
    // select: if minRun <= len-lo then minRun else len-lo
    L(MIN_RUN),
    L(LEN),
    L(LO),
    SUB,
    LE_S,
    { op: "select" },
    LS(FORCE),
    L(RUN_LEN),
    L(FORCE),
    LT_S,
    IF([L(DATA), L(LO), L(LO), L(FORCE), ADD, CALL(isortIdx), L(FORCE), LS(RUN_LEN)]),
  ];
  const pushRun = [
    L(SB),
    L(SS),
    L(LO),
    AS(i32ArrTypeIdx),
    L(SL),
    L(SS),
    L(RUN_LEN),
    AS(i32ArrTypeIdx),
    L(SS),
    I(1),
    ADD,
    LS(SS),
  ];
  const mergeCollapse = [
    BLOCK([
      LOOP([
        L(SS),
        I(2),
        LT_S,
        BR_IF(1),
        // stackSize < 2: done
        L(SS),
        I(2),
        SUB,
        LS(SN),
        // sn = stackSize - 2
        I(0),
        LS(SHOULD),
        L(SN),
        LS(MIDX),
        // default mergeIdx = sn
        L(SN),
        I(0),
        GT_S,
        IF(
          // 3+ entries: check invariant 1
          [
            // runLen[sn-1] <= runLen[sn] + runLen[sn+1]?
            L(SL),
            L(SN),
            I(1),
            SUB,
            AG(i32ArrTypeIdx),
            // runLen[sn-1]
            L(SL),
            L(SN),
            AG(i32ArrTypeIdx),
            // runLen[sn]
            L(SL),
            L(SN),
            I(1),
            ADD,
            AG(i32ArrTypeIdx),
            // runLen[sn+1]
            ADD,
            // runLen[sn] + runLen[sn+1]
            LE_S,
            IF(
              [
                I(1),
                LS(SHOULD),
                // if runLen[sn-1] < runLen[sn+1]: mergeIdx = sn-1
                L(SL),
                L(SN),
                I(1),
                SUB,
                AG(i32ArrTypeIdx),
                L(SL),
                L(SN),
                I(1),
                ADD,
                AG(i32ArrTypeIdx),
                LT_S,
                IF([L(SN), I(1), SUB, LS(MIDX)]),
              ],
              // Invariant 1 OK, check invariant 2: runLen[sn] <= runLen[sn+1]
              [
                L(SL),
                L(SN),
                AG(i32ArrTypeIdx),
                L(SL),
                L(SN),
                I(1),
                ADD,
                AG(i32ArrTypeIdx),
                LE_S,
                IF([I(1), LS(SHOULD)]),
              ],
            ),
          ],
          // 2 entries: only check invariant 2
          [L(SL), L(SN), AG(i32ArrTypeIdx), L(SL), L(SN), I(1), ADD, AG(i32ArrTypeIdx), LE_S, IF([I(1), LS(SHOULD)])],
        ),
        L(SHOULD),
        EQZ,
        BR_IF(1),
        // no merge needed: done
        // call __merge_run(data, tmp, sBase, sLen, stackSize, mergeIdx)
        L(DATA),
        L(TMP),
        L(SB),
        L(SL),
        L(SS),
        L(MIDX),
        CALL(mergeRunIdx),
        LS(SS),
        BR(0),
      ]),
    ]),
  ];
  const advanceLo = [L(LO), L(RUN_LEN), ADD, LS(LO)];
  const mainLoop = [
    BLOCK([
      LOOP([
        L(LO),
        L(LEN),
        GE_S,
        BR_IF(1),
        ...detectRun,
        ...extendRun,
        ...pushRun,
        ...mergeCollapse,
        ...advanceLo,
        BR(0),
      ]),
    ]),
  ];
  const forceCollapse = [
    BLOCK([
      LOOP([
        L(SS),
        I(2),
        LT_S,
        BR_IF(1),
        L(SS),
        I(2),
        SUB,
        LS(SN),
        L(SN),
        LS(MIDX),
        // if sn > 0 and sLen[sn-1] < sLen[sn+1]: mergeIdx = sn-1
        L(SN),
        I(0),
        GT_S,
        IF([
          L(SL),
          L(SN),
          I(1),
          SUB,
          AG(i32ArrTypeIdx),
          L(SL),
          L(SN),
          I(1),
          ADD,
          AG(i32ArrTypeIdx),
          LT_S,
          IF([L(SN), I(1), SUB, LS(MIDX)]),
        ]),
        L(DATA),
        L(TMP),
        L(SB),
        L(SL),
        L(SS),
        L(MIDX),
        CALL(mergeRunIdx),
        LS(SS),
        BR(0),
      ]),
    ]),
  ];
  const body = [...sec1, ...sec2, ...sec3, ...sec4, ...mainLoop, ...forceCollapse];
  return emitFunc(ctx, name, [vecRef], [], locals, body);
}
function ensureTimsortHelper(ctx, vecTypeIdx, arrTypeIdx, elemKind) {
  const name = `__timsort_${elemKind}`;
  const existing = ctx.funcMap.get(name);
  if (existing !== void 0) return existing;
  const i32ArrTypeIdx = getOrRegisterArrayType(ctx, "i32");
  const isortIdx = emitInsertionSort(ctx, arrTypeIdx, elemKind);
  const mergeIdx = emitMerge(ctx, arrTypeIdx, elemKind);
  const mergeRunIdx = emitMergeRun(ctx, arrTypeIdx, i32ArrTypeIdx, elemKind, mergeIdx);
  return emitTimsortMain(ctx, vecTypeIdx, arrTypeIdx, i32ArrTypeIdx, elemKind, isortIdx, mergeRunIdx);
}

// src/codegen/array-methods.ts
function emitThrowString(ctx, fctx, message) {
  addStringConstantGlobal(ctx, message);
  const strIdx = ctx.stringGlobalMap.get(message);
  fctx.body.push({ op: "global.get", index: strIdx });
  const tagIdx = ensureExnTag(ctx);
  fctx.body.push({ op: "throw", tagIdx });
}
function throwStringInstrs(ctx, message) {
  addStringConstantGlobal(ctx, message);
  const strIdx = ctx.stringGlobalMap.get(message);
  const tagIdx = ensureExnTag(ctx);
  return [
    { op: "global.get", index: strIdx },
    { op: "throw", tagIdx },
  ];
}
function isKnownNonCallable(ctx, arg) {
  if (arg.kind === ts4.SyntaxKind.NullKeyword) return true;
  if (arg.kind === ts4.SyntaxKind.UndefinedKeyword) return true;
  if (arg.kind === ts4.SyntaxKind.TrueKeyword || arg.kind === ts4.SyntaxKind.FalseKeyword) return true;
  if (ts4.isNumericLiteral(arg)) return true;
  if (ts4.isStringLiteral(arg)) return true;
  if (ts4.isIdentifier(arg) && arg.text === "undefined") return true;
  const tsType = ctx.checker.getTypeAtLocation(arg);
  const NON_CALLABLE_FLAGS =
    ts4.TypeFlags.Undefined |
    ts4.TypeFlags.Void |
    ts4.TypeFlags.Null |
    ts4.TypeFlags.BooleanLike |
    ts4.TypeFlags.NumberLike |
    ts4.TypeFlags.StringLike |
    ts4.TypeFlags.BigIntLike;
  if (tsType.flags & NON_CALLABLE_FLAGS) return true;
  return false;
}
function emitCallbackTypeCheck(ctx, fctx, callExpr, methodName) {
  if (callExpr.arguments.length < 1) {
    emitThrowString(ctx, fctx, `TypeError: ${methodName} callback is not a function`);
    return true;
  }
  const cbArg = callExpr.arguments[0];
  if (isKnownNonCallable(ctx, cbArg)) {
    const cbType = compileExpression(ctx, fctx, cbArg);
    if (cbType) fctx.body.push({ op: "drop" });
    emitThrowString(ctx, fctx, `TypeError: ${methodName} callback is not a function`);
    return true;
  }
  return false;
}
function guardedFuncRefCastInstrs(fctx, funcTypeIdx) {
  const tmpFunc = allocLocal(fctx, `__gfc_${fctx.locals.length}`, { kind: "funcref" });
  return [
    { op: "local.tee", index: tmpFunc },
    { op: "ref.test", typeIdx: funcTypeIdx },
    {
      op: "if",
      blockType: { kind: "val", type: { kind: "ref_null", typeIdx: funcTypeIdx } },
      then: [
        { op: "local.get", index: tmpFunc },
        { op: "ref.cast_null", typeIdx: funcTypeIdx },
      ],
      else: [{ op: "ref.null", typeIdx: funcTypeIdx }],
    },
  ];
}
function emitReceiverNullGuard(ctx, fctx, localIdx, receiverExpr) {
  if (receiverExpr && isReceiverNonNull(receiverExpr, ctx.checker)) return;
  fctx.body.push({ op: "local.get", index: localIdx });
  fctx.body.push({ op: "ref.is_null" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: throwStringInstrs(ctx, "TypeError: Array method called on null or undefined"),
    else: [],
  });
}
function isReceiverNonNull(expr, checker) {
  let inner = expr;
  while (ts4.isParenthesizedExpression(inner)) inner = inner.expression;
  switch (inner.kind) {
    case ts4.SyntaxKind.NewExpression:
    case ts4.SyntaxKind.ObjectLiteralExpression:
    case ts4.SyntaxKind.ArrayLiteralExpression:
      return true;
    default:
      break;
  }
  if (ts4.isIdentifier(inner)) {
    const sym = checker.getSymbolAtLocation(inner);
    if (sym) {
      const decl = sym.valueDeclaration;
      if (decl && ts4.isVariableDeclaration(decl) && decl.initializer) {
        const declList = decl.parent;
        if (ts4.isVariableDeclarationList(declList) && (declList.flags & ts4.NodeFlags.Const) !== 0) {
          return isReceiverNonNull(decl.initializer, checker);
        }
      }
    }
  }
  return false;
}
function emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elementType) {
  const idxLocal = allocLocal(fctx, `__bounds_idx_${fctx.locals.length}`, { kind: "i32" });
  const arrLocal = allocLocal(fctx, `__bounds_arr_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: idxLocal });
  fctx.body.push({ op: "local.set", index: arrLocal });
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "local.get", index: arrLocal });
  fctx.body.push({ op: "array.len" });
  fctx.body.push({ op: "i32.lt_u" });
  const thenInstrs = [
    { op: "local.get", index: arrLocal },
    { op: "local.get", index: idxLocal },
    { op: "array.get", typeIdx: arrTypeIdx },
  ];
  const elseInstrs = defaultValueInstrs(elementType);
  const needsNullableBlock = elementType.kind === "ref";
  const blockType = needsNullableBlock ? { kind: "ref_null", typeIdx: elementType.typeIdx } : elementType;
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: blockType },
    then: thenInstrs,
    else: elseInstrs,
  });
  if (needsNullableBlock) {
    fctx.body.push({ op: "ref.as_non_null" });
  }
}
function emitClampIndex(fctx, idxLocal, lenLocal) {
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "i32.lt_s" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "local.get", index: lenLocal },
      { op: "local.get", index: idxLocal },
      { op: "i32.add" },
      { op: "local.set", index: idxLocal },
      // if still < 0, clamp to 0
      { op: "local.get", index: idxLocal },
      { op: "i32.const", value: 0 },
      { op: "i32.lt_s" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "i32.const", value: 0 },
          { op: "local.set", index: idxLocal },
        ],
      },
    ],
  });
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "local.get", index: lenLocal });
  fctx.body.push({ op: "i32.gt_s" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "local.get", index: lenLocal },
      { op: "local.set", index: idxLocal },
    ],
  });
}
function emitClampNonNeg(fctx, local) {
  fctx.body.push({ op: "local.get", index: local });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "i32.lt_s" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "i32.const", value: 0 },
      { op: "local.set", index: local },
    ],
  });
}
function resolveArrayInfo(ctx, tsType) {
  if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0 && isStringType(tsType)) return null;
  const wasmType = resolveWasmType(ctx, tsType);
  if (wasmType.kind !== "ref" && wasmType.kind !== "ref_null") return null;
  const vecTypeIdx = wasmType.typeIdx;
  const vecDef = ctx.mod.types[vecTypeIdx];
  if (!vecDef || vecDef.kind !== "struct") return null;
  if (vecDef.fields.length < 2) return null;
  const dataField = vecDef.fields[1];
  if (dataField.type.kind !== "ref") return null;
  const arrTypeIdx = dataField.type.typeIdx;
  const arrDef = ctx.mod.types[arrTypeIdx];
  if (!arrDef || arrDef.kind !== "array") return null;
  return { vecTypeIdx, arrTypeIdx, elemType: arrDef.element };
}
function compileArrayPrototypeCall(ctx, fctx, callExpr, propAccess) {
  if (propAccess.name.text !== "call") return void 0;
  if (!ts4.isPropertyAccessExpression(propAccess.expression)) return void 0;
  const methodAccess = propAccess.expression;
  const methodName = methodAccess.name.text;
  if (!ts4.isPropertyAccessExpression(methodAccess.expression)) return void 0;
  const protoAccess = methodAccess.expression;
  if (protoAccess.name.text !== "prototype") return void 0;
  if (!ts4.isIdentifier(protoAccess.expression)) return void 0;
  if (protoAccess.expression.text !== "Array") return void 0;
  if (callExpr.arguments.length < 1) return void 0;
  const receiverArg = callExpr.arguments[0];
  if (!ARRAY_METHODS.has(methodName)) return void 0;
  let receiverTsType;
  if (ts4.isIdentifier(receiverArg)) {
    const shapeInfo = ctx.shapeMap.get(receiverArg.text);
    if (shapeInfo) {
      const { vecTypeIdx, arrTypeIdx, elemType } = shapeInfo;
      switch (methodName) {
        case "indexOf":
          return compileArrayPrototypeIndexOf(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType);
        case "includes":
          return compileArrayPrototypeIncludes(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType);
        case "every":
          return compileArrayPrototypeEvery(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType);
        case "some":
          return compileArrayPrototypeSome(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType);
        case "forEach":
          return compileArrayPrototypeForEach(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType);
        default:
          return void 0;
      }
    }
    receiverTsType = ctx.checker.getTypeAtLocation(receiverArg);
  } else {
    receiverTsType = ctx.checker.getTypeAtLocation(receiverArg);
  }
  if (!receiverTsType) return void 0;
  const arrInfo = resolveArrayInfo(ctx, receiverTsType);
  if (!arrInfo) return void 0;
  const syntheticPropAccess = ts4.factory.createPropertyAccessExpression(receiverArg, methodName);
  syntheticPropAccess.parent = callExpr.parent;
  const remainingArgs = callExpr.arguments.slice(1);
  const syntheticCall = ts4.factory.createCallExpression(syntheticPropAccess, void 0, remainingArgs);
  syntheticCall.parent = callExpr.parent;
  return compileArrayMethodCall(ctx, fctx, syntheticPropAccess, syntheticCall, receiverTsType);
}
function compileArrayPrototypeIndexOf(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 2) {
    ctx.errors.push({
      message: "Array.prototype.indexOf.call requires at least 2 arguments",
      line: getLine(callExpr),
      column: getCol(callExpr),
    });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__apc_iof_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__apc_iof_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__apc_iof_i_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__apc_iof_len_${fctx.locals.length}`, { kind: "i32" });
  const valTmp = allocLocal(fctx, `__apc_iof_val_${fctx.locals.length}`, elemType);
  compileExpression(ctx, fctx, receiverArg);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  compileExpression(ctx, fctx, callExpr.arguments[1], elemType);
  fctx.body.push({ op: "local.set", index: valTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  let apcEqInstrs;
  if (elemType.kind === "externref") {
    addStringImports(ctx);
    const equalsIdx = ctx.funcMap.get("equals");
    apcEqInstrs = [{ op: "call", funcIdx: equalsIdx }];
  } else if (elemType.kind === "ref" || elemType.kind === "ref_null") {
    apcEqInstrs = [{ op: "ref.eq" }];
  } else {
    const eqOp = elemType.kind === "f64" ? "f64.eq" : "i32.eq";
    apcEqInstrs = [{ op: eqOp }];
  }
  const resType = ctx.fast ? { kind: "i32" } : { kind: "f64" };
  const resTmp = allocLocal(fctx, `__apc_iof_res_${fctx.locals.length}`, resType);
  if (ctx.fast) {
    fctx.body.push({ op: "i32.const", value: -1 });
  } else {
    fctx.body.push({ op: "f64.const", value: -1 });
  }
  fctx.body.push({ op: "local.set", index: resTmp });
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    { op: "local.get", index: valTmp },
    ...apcEqInstrs,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: ctx.fast
        ? [
            { op: "local.get", index: iTmp },
            { op: "local.set", index: resTmp },
            { op: "br", depth: 2 },
          ]
        : [
            { op: "local.get", index: iTmp },
            { op: "f64.convert_i32_s" },
            { op: "local.set", index: resTmp },
            { op: "br", depth: 2 },
          ],
    },
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: resTmp });
  if (ctx.fast) {
    return { kind: "i32" };
  }
  return { kind: "f64" };
}
function compileArrayPrototypeIncludes(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 2) {
    ctx.errors.push({
      message: "Array.prototype.includes.call requires at least 2 arguments",
      line: getLine(callExpr),
      column: getCol(callExpr),
    });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__apc_inc_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__apc_inc_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__apc_inc_i_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__apc_inc_len_${fctx.locals.length}`, { kind: "i32" });
  const valTmp = allocLocal(fctx, `__apc_inc_val_${fctx.locals.length}`, elemType);
  compileExpression(ctx, fctx, receiverArg);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  compileExpression(ctx, fctx, callExpr.arguments[1], elemType);
  fctx.body.push({ op: "local.set", index: valTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iTmp });
  const eqOp = elemType.kind === "f64" ? "f64.eq" : "i32.eq";
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  const resTmp = allocLocal(fctx, `__apc_inc_res_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: resTmp });
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    { op: "local.get", index: valTmp },
    { op: eqOp },
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: 1 },
        { op: "local.set", index: resTmp },
        { op: "br", depth: 2 },
        // break out of block
      ],
    },
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: resTmp });
  return { kind: "i32" };
}
function compileArrayPrototypeEvery(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 2) {
    ctx.errors.push({
      message: "Array.prototype.every.call requires at least 2 arguments",
      line: getLine(callExpr),
      column: getCol(callExpr),
    });
    return null;
  }
  const cbArg = callExpr.arguments[1];
  if (!ts4.isArrowFunction(cbArg) && !ts4.isFunctionExpression(cbArg)) {
    return void 0;
  }
  const cbResult =
    ts4.isArrowFunction(cbArg) || ts4.isFunctionExpression(cbArg)
      ? compileArrowAsClosure(ctx, fctx, cbArg)
      : compileExpression(ctx, fctx, cbArg);
  if (!cbResult || (cbResult.kind !== "ref" && cbResult.kind !== "ref_null")) return null;
  const closureTypeIdx = cbResult.typeIdx;
  const closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
  if (!closureInfo) return null;
  const closureTmp = allocLocal(fctx, `__apc_ev_cb_${fctx.locals.length}`, cbResult);
  fctx.body.push({ op: "local.set", index: closureTmp });
  const vecTmp = allocLocal(fctx, `__apc_ev_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__apc_ev_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__apc_ev_i_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__apc_ev_len_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, receiverArg);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  const numParams = closureInfo.paramTypes.length;
  const resTmp = allocLocal(fctx, `__apc_ev_res_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "local.set", index: resTmp });
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    // break out of block
    // Call closure(element, index, array): push closure ref, then args
    { op: "local.get", index: closureTmp },
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    ...coercionInstrs(ctx, elemType, closureInfo.paramTypes[0] ?? elemType, fctx),
    // Push index (2nd user param) if callback expects it
    ...(numParams >= 2
      ? [
          { op: "local.get", index: iTmp },
          ...coercionInstrs(ctx, { kind: "i32" }, closureInfo.paramTypes[1] ?? { kind: "i32" }, fctx),
        ]
      : []),
    // Push array (3rd user param) if callback expects it
    ...(numParams >= 3
      ? [
          { op: "local.get", index: vecTmp },
          ...coercionInstrs(
            ctx,
            { kind: "ref_null", typeIdx: vecTypeIdx },
            closureInfo.paramTypes[2] ?? { kind: "ref_null", typeIdx: vecTypeIdx },
            fctx,
          ),
        ]
      : []),
    // Get function ref from closure struct field 0 and call_ref
    { op: "local.get", index: closureTmp },
    { op: "struct.get", typeIdx: closureTypeIdx, fieldIdx: 0 },
    ...guardedFuncRefCastInstrs(fctx, closureInfo.funcTypeIdx),
    { op: "ref.as_non_null" },
    { op: "call_ref", typeIdx: closureInfo.funcTypeIdx },
    // Check if result is falsy (0 for i32, 0.0 for f64)
    ...(closureInfo.returnType?.kind === "f64"
      ? [{ op: "f64.const", value: 0 }, { op: "f64.eq" }]
      : closureInfo.returnType?.kind === "i32"
        ? [{ op: "i32.eqz" }]
        : [{ op: "i32.eqz" }]),
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: 0 },
        { op: "local.set", index: resTmp },
        { op: "br", depth: 2 },
        // break out of block
      ],
    },
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: resTmp });
  return { kind: "i32" };
}
function compileArrayPrototypeSome(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 2) return null;
  const cbArg = callExpr.arguments[1];
  if (!ts4.isArrowFunction(cbArg) && !ts4.isFunctionExpression(cbArg)) return void 0;
  const cbResult =
    ts4.isArrowFunction(cbArg) || ts4.isFunctionExpression(cbArg)
      ? compileArrowAsClosure(ctx, fctx, cbArg)
      : compileExpression(ctx, fctx, cbArg);
  if (!cbResult || (cbResult.kind !== "ref" && cbResult.kind !== "ref_null")) return null;
  const closureTypeIdx = cbResult.typeIdx;
  const closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
  if (!closureInfo) return null;
  const closureTmp = allocLocal(fctx, `__apc_some_cb_${fctx.locals.length}`, cbResult);
  fctx.body.push({ op: "local.set", index: closureTmp });
  const vecTmp = allocLocal(fctx, `__apc_some_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__apc_some_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__apc_some_i_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__apc_some_len_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, receiverArg);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  const numParams = closureInfo.paramTypes.length;
  const resTmp = allocLocal(fctx, `__apc_some_res_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: resTmp });
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    { op: "local.get", index: closureTmp },
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    ...coercionInstrs(ctx, elemType, closureInfo.paramTypes[0] ?? elemType, fctx),
    // Push index (2nd user param) if callback expects it
    ...(numParams >= 2
      ? [
          { op: "local.get", index: iTmp },
          ...coercionInstrs(ctx, { kind: "i32" }, closureInfo.paramTypes[1] ?? { kind: "i32" }, fctx),
        ]
      : []),
    // Push array (3rd user param) if callback expects it
    ...(numParams >= 3
      ? [
          { op: "local.get", index: vecTmp },
          ...coercionInstrs(
            ctx,
            { kind: "ref_null", typeIdx: vecTypeIdx },
            closureInfo.paramTypes[2] ?? { kind: "ref_null", typeIdx: vecTypeIdx },
            fctx,
          ),
        ]
      : []),
    { op: "local.get", index: closureTmp },
    { op: "struct.get", typeIdx: closureTypeIdx, fieldIdx: 0 },
    ...guardedFuncRefCastInstrs(fctx, closureInfo.funcTypeIdx),
    { op: "ref.as_non_null" },
    { op: "call_ref", typeIdx: closureInfo.funcTypeIdx },
    ...(closureInfo.returnType?.kind === "f64" ? [{ op: "f64.const", value: 0 }, { op: "f64.ne" }] : []),
    ...(closureInfo.returnType?.kind === "i32" ? [] : [{ op: "i32.eqz" }, { op: "i32.eqz" }]),
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: 1 },
        { op: "local.set", index: resTmp },
        { op: "br", depth: 2 },
        // break out of block
      ],
    },
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: resTmp });
  return { kind: "i32" };
}
function compileArrayPrototypeForEach(ctx, fctx, callExpr, receiverArg, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 2) return null;
  const cbArg = callExpr.arguments[1];
  if (!ts4.isArrowFunction(cbArg) && !ts4.isFunctionExpression(cbArg)) return void 0;
  const cbResult =
    ts4.isArrowFunction(cbArg) || ts4.isFunctionExpression(cbArg)
      ? compileArrowAsClosure(ctx, fctx, cbArg)
      : compileExpression(ctx, fctx, cbArg);
  if (!cbResult || (cbResult.kind !== "ref" && cbResult.kind !== "ref_null")) return null;
  const closureTypeIdx = cbResult.typeIdx;
  const closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
  if (!closureInfo) return null;
  const closureTmp = allocLocal(fctx, `__apc_fe_cb_${fctx.locals.length}`, cbResult);
  fctx.body.push({ op: "local.set", index: closureTmp });
  const vecTmp = allocLocal(fctx, `__apc_fe_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__apc_fe_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__apc_fe_i_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__apc_fe_len_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, receiverArg);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  const numParams = closureInfo.paramTypes.length;
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    { op: "local.get", index: closureTmp },
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    ...coercionInstrs(ctx, elemType, closureInfo.paramTypes[0] ?? elemType, fctx),
    // Push index (2nd user param) if callback expects it
    ...(numParams >= 2
      ? [
          { op: "local.get", index: iTmp },
          ...coercionInstrs(ctx, { kind: "i32" }, closureInfo.paramTypes[1] ?? { kind: "i32" }, fctx),
        ]
      : []),
    // Push array (3rd user param) if callback expects it
    ...(numParams >= 3
      ? [
          { op: "local.get", index: vecTmp },
          ...coercionInstrs(
            ctx,
            { kind: "ref_null", typeIdx: vecTypeIdx },
            closureInfo.paramTypes[2] ?? { kind: "ref_null", typeIdx: vecTypeIdx },
            fctx,
          ),
        ]
      : []),
    { op: "local.get", index: closureTmp },
    { op: "struct.get", typeIdx: closureTypeIdx, fieldIdx: 0 },
    ...guardedFuncRefCastInstrs(fctx, closureInfo.funcTypeIdx),
    { op: "ref.as_non_null" },
    { op: "call_ref", typeIdx: closureInfo.funcTypeIdx },
    // Drop the result if there is one
    ...(closureInfo.returnType ? [{ op: "drop" }] : []),
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  return VOID_RESULT;
}
var ARRAY_METHODS = /* @__PURE__ */ new Set([
  "push",
  "pop",
  "shift",
  "indexOf",
  "includes",
  "slice",
  "concat",
  "join",
  "reverse",
  "splice",
  "at",
  "fill",
  "copyWithin",
  "lastIndexOf",
  "sort",
  "filter",
  "map",
  "reduce",
  "forEach",
  "find",
  "findIndex",
  "some",
  "every",
  "entries",
  "keys",
  "values",
]);
function compileArrayMethodCall(ctx, fctx, propAccess, callExpr, receiverType, overrideMethodName) {
  const methodName = overrideMethodName ?? (ts4.isPropertyAccessExpression(propAccess) ? propAccess.name.text : void 0);
  if (!methodName || !ARRAY_METHODS.has(methodName)) return void 0;
  const arrInfo = resolveArrayInfo(ctx, receiverType);
  if (!arrInfo) return void 0;
  const { vecTypeIdx, arrTypeIdx, elemType } = arrInfo;
  const methodAccess = propAccess;
  let moduleGlobalIdx;
  let savedLocal;
  const MUTATING = /* @__PURE__ */ new Set(["push", "pop", "shift", "reverse", "splice", "fill", "copyWithin", "sort"]);
  if (ts4.isIdentifier(propAccess.expression)) {
    const name = propAccess.expression.text;
    const gIdx = ctx.moduleGlobals.get(name);
    if (gIdx !== void 0 && !fctx.localMap.has(name)) {
      moduleGlobalIdx = gIdx;
      const globalDef = ctx.mod.globals[localGlobalIdx(ctx, gIdx)];
      if (!globalDef) return null;
      const tempLocal = allocLocal(fctx, `__mod_proxy_${name}`, globalDef.type);
      fctx.body.push({ op: "global.get", index: gIdx });
      fctx.body.push({ op: "local.set", index: tempLocal });
      fctx.localMap.set(name, tempLocal);
      savedLocal = tempLocal;
    }
  }
  let result;
  switch (methodName) {
    case "indexOf":
      result = compileArrayIndexOf(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "includes":
      result = compileArrayIncludes(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "reverse":
      result = compileArrayReverse(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "push":
      result = compileArrayPush(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "pop":
      result = compileArrayPop(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "shift":
      result = compileArrayShift(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "slice":
      result = compileArraySlice(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "concat":
      result = compileArrayConcat(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "join":
      result = compileArrayJoin(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "splice":
      result = compileArraySplice(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "at":
      result = compileArrayAt(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "fill":
      result = compileArrayFill(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "copyWithin":
      result = compileArrayCopyWithin(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "lastIndexOf":
      result = compileArrayLastIndexOf(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType);
      break;
    case "sort":
      result =
        elemType.kind === "f64" || elemType.kind === "i32"
          ? compileArraySort(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    // Functional array methods -- supported for numeric (f64, i32) and externref element types
    case "filter":
      result =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArrayFilter(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    case "map":
      result =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArrayMap(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    case "reduce":
      result =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArrayReduce(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    case "reduceRight":
      result =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArrayReduceRight(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    case "forEach": {
      const feResult =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArrayForEach(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      result = feResult === null ? VOID_RESULT : feResult;
      break;
    }
    case "find":
      result =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArrayFind(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    case "findIndex":
      result =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArrayFindIndex(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    case "some":
      result =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArraySome(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    case "every":
      result =
        elemType.kind === "f64" || elemType.kind === "i32" || elemType.kind === "externref"
          ? compileArrayEvery(ctx, fctx, methodAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType)
          : void 0;
      break;
    case "entries":
    case "keys":
    case "values":
      result = compileArrayIteratorMethod(ctx, fctx, methodAccess, methodName);
      break;
    default:
      result = void 0;
  }
  if (moduleGlobalIdx !== void 0 && savedLocal !== void 0) {
    if (MUTATING.has(methodName) && result !== null && result !== void 0) {
      fctx.body.push({ op: "local.get", index: savedLocal });
      fctx.body.push({ op: "global.set", index: moduleGlobalIdx });
    }
    if (ts4.isIdentifier(propAccess.expression)) {
      fctx.localMap.delete(propAccess.expression.text);
    }
  }
  return result;
}
function compileArrayIteratorMethod(ctx, fctx, propAccess, methodName) {
  addArrayIteratorImports(ctx);
  const importName = `__array_${methodName}`;
  const funcIdx = ctx.funcMap.get(importName);
  if (funcIdx === void 0) return null;
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "extern.convert_any" });
  fctx.body.push({ op: "call", funcIdx });
  return { kind: "externref" };
}
function emitArrayCopy(fctx, arrTypeIdx, dstArr, dstOffset, srcArr, srcOffset, count) {
  fctx.body.push({ op: "local.get", index: dstArr });
  if (dstOffset !== null) {
    fctx.body.push({ op: "local.get", index: dstOffset });
  } else {
    fctx.body.push({ op: "i32.const", value: 0 });
  }
  fctx.body.push({ op: "local.get", index: srcArr });
  if (srcOffset !== null) {
    fctx.body.push({ op: "local.get", index: srcOffset });
  } else {
    fctx.body.push({ op: "i32.const", value: 0 });
  }
  fctx.body.push({ op: "local.get", index: count });
  fctx.body.push({ op: "array.copy", dstTypeIdx: arrTypeIdx, srcTypeIdx: arrTypeIdx });
}
function compileArrayAt(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 1) {
    ctx.errors.push({ message: "at() requires 1 argument", line: getLine(callExpr), column: getCol(callExpr) });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__arr_at_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const idxTmp = allocLocal(fctx, `__arr_at_idx_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__arr_at_len_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.set", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  const argType = compileExpression(ctx, fctx, callExpr.arguments[0], { kind: "i32" });
  if (argType && argType.kind === "f64") {
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  }
  fctx.body.push({ op: "local.set", index: idxTmp });
  fctx.body.push({ op: "local.get", index: idxTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "i32.lt_s" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "local.get", index: idxTmp },
      { op: "local.get", index: lenTmp },
      { op: "i32.add" },
      { op: "local.set", index: idxTmp },
    ],
  });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.get", index: idxTmp });
  emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elemType);
  return elemType;
}
function compileArrayIndexOf(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 1) {
    ctx.errors.push({ message: "indexOf requires 1 argument", line: getLine(callExpr), column: getCol(callExpr) });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__arr_iof_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_iof_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__arr_iof_i_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__arr_iof_len_${fctx.locals.length}`, { kind: "i32" });
  const valTmp = allocLocal(fctx, `__arr_iof_val_${fctx.locals.length}`, elemType);
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  compileExpression(ctx, fctx, callExpr.arguments[0], elemType);
  fctx.body.push({ op: "local.set", index: valTmp });
  if (callExpr.arguments.length >= 2) {
    if (ctx.fast) {
      compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "i32" });
    } else {
      compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "f64" });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
    const fromTmp = allocLocal(fctx, `__arr_iof_from_${fctx.locals.length}`, { kind: "i32" });
    fctx.body.push({ op: "local.tee", index: fromTmp });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "i32.lt_s" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: lenTmp },
        { op: "local.get", index: fromTmp },
        { op: "i32.add" },
        { op: "local.tee", index: fromTmp },
        { op: "i32.const", value: 0 },
        { op: "i32.lt_s" },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "i32.const", value: 0 },
            { op: "local.set", index: fromTmp },
          ],
        },
      ],
    });
    fctx.body.push({ op: "local.get", index: fromTmp });
  } else {
    fctx.body.push({ op: "i32.const", value: 0 });
  }
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  let eqInstrs;
  if (elemType.kind === "externref") {
    addStringImports(ctx);
    const equalsIdx = ctx.funcMap.get("equals");
    eqInstrs = [{ op: "call", funcIdx: equalsIdx }];
  } else if (elemType.kind === "ref" || elemType.kind === "ref_null") {
    eqInstrs = [{ op: "ref.eq" }];
  } else {
    const eqOp = elemType.kind === "f64" ? "f64.eq" : "i32.eq";
    eqInstrs = [{ op: eqOp }];
  }
  const resType = ctx.fast ? { kind: "i32" } : { kind: "f64" };
  const resTmp = allocLocal(fctx, `__arr_iof_res_${fctx.locals.length}`, resType);
  if (ctx.fast) {
    fctx.body.push({ op: "i32.const", value: -1 });
  } else {
    fctx.body.push({ op: "f64.const", value: -1 });
  }
  fctx.body.push({ op: "local.set", index: resTmp });
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    { op: "local.get", index: valTmp },
    ...eqInstrs,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: ctx.fast
        ? [
            { op: "local.get", index: iTmp },
            { op: "local.set", index: resTmp },
            { op: "br", depth: 2 },
            // break out of block
          ]
        : [
            { op: "local.get", index: iTmp },
            { op: "f64.convert_i32_s" },
            { op: "local.set", index: resTmp },
            { op: "br", depth: 2 },
            // break out of block
          ],
    },
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: resTmp });
  if (ctx.fast) {
    return { kind: "i32" };
  }
  return { kind: "f64" };
}
function compileArrayIncludes(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 1) {
    ctx.errors.push({ message: "includes requires 1 argument", line: getLine(callExpr), column: getCol(callExpr) });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__arr_inc_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_inc_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__arr_inc_i_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__arr_inc_len_${fctx.locals.length}`, { kind: "i32" });
  const valTmp = allocLocal(fctx, `__arr_inc_val_${fctx.locals.length}`, elemType);
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  compileExpression(ctx, fctx, callExpr.arguments[0], elemType);
  fctx.body.push({ op: "local.set", index: valTmp });
  if (callExpr.arguments.length >= 2) {
    if (ctx.fast) {
      compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "i32" });
    } else {
      compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "f64" });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
    const fromTmp = allocLocal(fctx, `__arr_inc_from_${fctx.locals.length}`, { kind: "i32" });
    fctx.body.push({ op: "local.tee", index: fromTmp });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "i32.lt_s" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: lenTmp },
        { op: "local.get", index: fromTmp },
        { op: "i32.add" },
        { op: "local.tee", index: fromTmp },
        { op: "i32.const", value: 0 },
        { op: "i32.lt_s" },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "i32.const", value: 0 },
            { op: "local.set", index: fromTmp },
          ],
        },
      ],
    });
    fctx.body.push({ op: "local.get", index: fromTmp });
  } else {
    fctx.body.push({ op: "i32.const", value: 0 });
  }
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  let incNeedsElemTmp = false;
  let incElemTmp;
  if (elemType.kind === "f64") {
    incNeedsElemTmp = true;
    incElemTmp = allocLocal(fctx, `__arr_inc_el_${fctx.locals.length}`, { kind: "f64" });
  }
  const resTmp = allocLocal(fctx, `__arr_inc_res_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: resTmp });
  let comparisonInstrs;
  if (elemType.kind === "f64") {
    comparisonInstrs = [
      // Load element and save to temp
      { op: "local.get", index: dataTmp },
      { op: "local.get", index: iTmp },
      { op: getOp, typeIdx: arrTypeIdx },
      { op: "local.tee", index: incElemTmp },
      // elem == val
      { op: "local.get", index: valTmp },
      { op: "f64.eq" },
      // isNaN(elem) = elem != elem
      { op: "local.get", index: incElemTmp },
      { op: "local.get", index: incElemTmp },
      { op: "f64.ne" },
      // isNaN(val) = val != val
      { op: "local.get", index: valTmp },
      { op: "local.get", index: valTmp },
      { op: "f64.ne" },
      // isNaN(elem) & isNaN(val)
      { op: "i32.and" },
      // (elem == val) | (both NaN)
      { op: "i32.or" },
    ];
  } else if (elemType.kind === "externref") {
    addStringImports(ctx);
    const equalsIdx = ctx.funcMap.get("equals");
    comparisonInstrs = [
      { op: "local.get", index: dataTmp },
      { op: "local.get", index: iTmp },
      { op: getOp, typeIdx: arrTypeIdx },
      { op: "local.get", index: valTmp },
      { op: "call", funcIdx: equalsIdx },
    ];
  } else if (elemType.kind === "ref" || elemType.kind === "ref_null") {
    comparisonInstrs = [
      { op: "local.get", index: dataTmp },
      { op: "local.get", index: iTmp },
      { op: getOp, typeIdx: arrTypeIdx },
      { op: "local.get", index: valTmp },
      { op: "ref.eq" },
    ];
  } else {
    const eqOp = "i32.eq";
    comparisonInstrs = [
      { op: "local.get", index: dataTmp },
      { op: "local.get", index: iTmp },
      { op: getOp, typeIdx: arrTypeIdx },
      { op: "local.get", index: valTmp },
      { op: eqOp },
    ];
  }
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    ...comparisonInstrs,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: 1 },
        { op: "local.set", index: resTmp },
        { op: "br", depth: 2 },
        // break out of block
      ],
    },
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: resTmp });
  return { kind: "i32" };
}
function compileArrayReverse(ctx, fctx, propAccess, _callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  const vecTmp = allocLocal(fctx, `__arr_rev_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_rev_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__arr_rev_i_${fctx.locals.length}`, { kind: "i32" });
  const jTmp = allocLocal(fctx, `__arr_rev_j_${fctx.locals.length}`, { kind: "i32" });
  const swapTmp = allocLocal(fctx, `__arr_rev_sw_${fctx.locals.length}`, elemType);
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "local.set", index: jTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: jTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    // swap = data[i]
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    { op: "local.set", index: swapTmp },
    // data[i] = data[j]
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: jTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    { op: "array.set", typeIdx: arrTypeIdx },
    // data[j] = swap
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: jTmp },
    { op: "local.get", index: swapTmp },
    { op: "array.set", typeIdx: arrTypeIdx },
    // i++, j--
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "local.get", index: jTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.sub" },
    { op: "local.set", index: jTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: vecTmp });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compileArrayPush(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length === 0) {
    const vecTmp0 = allocLocal(fctx, `__arr_push_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
    compileExpression(ctx, fctx, propAccess.expression);
    fctx.body.push({ op: "local.tee", index: vecTmp0 });
    emitReceiverNullGuard(ctx, fctx, vecTmp0, propAccess.expression);
    fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
    if (!ctx.fast) fctx.body.push({ op: "f64.convert_i32_s" });
    return ctx.fast ? { kind: "i32" } : { kind: "f64" };
  }
  const argCount = callExpr.arguments.length;
  const vecTmp = allocLocal(fctx, `__arr_push_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_push_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenTmp = allocLocal(fctx, `__arr_push_len_${fctx.locals.length}`, { kind: "i32" });
  const newCapTmp = allocLocal(fctx, `__arr_push_ncap_${fctx.locals.length}`, { kind: "i32" });
  const newDataTmp = allocLocal(fctx, `__arr_push_ndata_${fctx.locals.length}`, {
    kind: "ref_null",
    typeIdx: arrTypeIdx,
  });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp, propAccess.expression);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.tee", index: dataTmp });
  fctx.body.push({ op: "array.len" });
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "i32.const", value: argCount });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "i32.lt_s" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      // newCap = max((len + argCount) * 2, 4)
      { op: "local.get", index: lenTmp },
      { op: "i32.const", value: argCount },
      { op: "i32.add" },
      { op: "i32.const", value: 1 },
      { op: "i32.shl" },
      // (len + argCount) * 2
      { op: "i32.const", value: 4 },
      // select: if (len+argCount)*2 > 4 then (len+argCount)*2 else 4
      { op: "local.get", index: lenTmp },
      { op: "i32.const", value: argCount },
      { op: "i32.add" },
      { op: "i32.const", value: 1 },
      { op: "i32.shl" },
      { op: "i32.const", value: 4 },
      { op: "i32.gt_s" },
      { op: "select" },
      { op: "local.set", index: newCapTmp },
      // newData = array.new_default(newCap)
      { op: "local.get", index: newCapTmp },
      { op: "array.new_default", typeIdx: arrTypeIdx },
      { op: "local.set", index: newDataTmp },
      // array.copy newData[0..len] = data[0..len]
      { op: "local.get", index: newDataTmp },
      { op: "i32.const", value: 0 },
      { op: "local.get", index: dataTmp },
      { op: "i32.const", value: 0 },
      { op: "local.get", index: lenTmp },
      { op: "array.copy", dstTypeIdx: arrTypeIdx, srcTypeIdx: arrTypeIdx },
      // Update vec struct data field
      { op: "local.get", index: vecTmp },
      { op: "local.get", index: newDataTmp },
      { op: "ref.as_non_null" },
      { op: "struct.set", typeIdx: vecTypeIdx, fieldIdx: 1 },
      // Update local data pointer
      { op: "local.get", index: newDataTmp },
      { op: "local.set", index: dataTmp },
    ],
  });
  for (let i = 0; i < argCount; i++) {
    fctx.body.push({ op: "local.get", index: dataTmp });
    fctx.body.push({ op: "local.get", index: lenTmp });
    if (i > 0) {
      fctx.body.push({ op: "i32.const", value: i });
      fctx.body.push({ op: "i32.add" });
    }
    compileExpression(ctx, fctx, callExpr.arguments[i], elemType);
    fctx.body.push({ op: "array.set", typeIdx: arrTypeIdx });
  }
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "i32.const", value: argCount });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "struct.set", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "i32.const", value: argCount });
  fctx.body.push({ op: "i32.add" });
  if (!ctx.fast) fctx.body.push({ op: "f64.convert_i32_s" });
  return ctx.fast ? { kind: "i32" } : { kind: "f64" };
}
function compileArrayPop(ctx, fctx, propAccess, _callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  const vecTmp = allocLocal(fctx, `__arr_pop_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const newLenTmp = allocLocal(fctx, `__arr_pop_nl_${fctx.locals.length}`, { kind: "i32" });
  const resultTmp = allocLocal(fctx, `__arr_pop_res_${fctx.locals.length}`, elemType);
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  const lenTmp = allocLocal(fctx, `__arr_pop_len_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "i32.gt_s" });
  const thenInstrs = [
    // newLen = length - 1
    { op: "local.get", index: lenTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.sub" },
    { op: "local.set", index: newLenTmp },
    // result = data[newLen]
    { op: "local.get", index: vecTmp },
    { op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 },
    { op: "local.get", index: newLenTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    { op: "local.set", index: resultTmp },
    // Decrement length: vec.length = newLen
    { op: "local.get", index: vecTmp },
    { op: "local.get", index: newLenTmp },
    { op: "struct.set", typeIdx: vecTypeIdx, fieldIdx: 0 },
  ];
  fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
  fctx.body.push({ op: "local.get", index: resultTmp });
  return elemType;
}
function compileArrayShift(ctx, fctx, propAccess, _callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  const vecTmp = allocLocal(fctx, `__arr_sft_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_sft_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenTmp = allocLocal(fctx, `__arr_sft_len_${fctx.locals.length}`, { kind: "i32" });
  const newLenTmp = allocLocal(fctx, `__arr_sft_nl_${fctx.locals.length}`, { kind: "i32" });
  const resultTmp = allocLocal(fctx, `__arr_sft_res_${fctx.locals.length}`, elemType);
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "i32.gt_s" });
  const thenInstrs = [
    // result = data[0]
    { op: "local.get", index: dataTmp },
    { op: "i32.const", value: 0 },
    { op: getOp, typeIdx: arrTypeIdx },
    { op: "local.set", index: resultTmp },
    // newLen = len - 1
    { op: "local.get", index: lenTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.sub" },
    { op: "local.set", index: newLenTmp },
    // Shift left: array.copy data[0..newLen] = data[1..len]
    { op: "local.get", index: dataTmp },
    { op: "i32.const", value: 0 },
    { op: "local.get", index: dataTmp },
    { op: "i32.const", value: 1 },
    { op: "local.get", index: newLenTmp },
    { op: "array.copy", dstTypeIdx: arrTypeIdx, srcTypeIdx: arrTypeIdx },
    // Decrement length: vec.length = newLen
    { op: "local.get", index: vecTmp },
    { op: "local.get", index: newLenTmp },
    { op: "struct.set", typeIdx: vecTypeIdx, fieldIdx: 0 },
  ];
  fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
  fctx.body.push({ op: "local.get", index: resultTmp });
  return elemType;
}
function compileArraySlice(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, _elemType) {
  const vecTmp = allocLocal(fctx, `__arr_slc_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_slc_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const newData = allocLocal(fctx, `__arr_slc_ndata_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const startTmp = allocLocal(fctx, `__arr_slc_s_${fctx.locals.length}`, { kind: "i32" });
  const endTmp = allocLocal(fctx, `__arr_slc_e_${fctx.locals.length}`, { kind: "i32" });
  const lenTmp = allocLocal(fctx, `__arr_slc_len_${fctx.locals.length}`, { kind: "i32" });
  const sliceLenTmp = allocLocal(fctx, `__arr_slc_sl_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  if (callExpr.arguments.length >= 1) {
    compileExpression(ctx, fctx, callExpr.arguments[0], { kind: "f64" });
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  } else {
    fctx.body.push({ op: "i32.const", value: 0 });
  }
  fctx.body.push({ op: "local.set", index: startTmp });
  emitClampIndex(fctx, startTmp, lenTmp);
  if (callExpr.arguments.length >= 2) {
    compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "f64" });
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  } else {
    fctx.body.push({ op: "local.get", index: lenTmp });
  }
  fctx.body.push({ op: "local.set", index: endTmp });
  emitClampIndex(fctx, endTmp, lenTmp);
  fctx.body.push({ op: "local.get", index: endTmp });
  fctx.body.push({ op: "local.get", index: startTmp });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "local.set", index: sliceLenTmp });
  emitClampNonNeg(fctx, sliceLenTmp);
  fctx.body.push({ op: "local.get", index: sliceLenTmp });
  fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: newData });
  emitArrayCopy(fctx, arrTypeIdx, newData, null, dataTmp, startTmp, sliceLenTmp);
  fctx.body.push({ op: "local.get", index: sliceLenTmp });
  fctx.body.push({ op: "local.get", index: newData });
  fctx.body.push({ op: "ref.as_non_null" });
  fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compileArrayConcat(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, _elemType) {
  if (callExpr.arguments.length === 0) {
    const vecA2 = allocLocal(fctx, `__arr_cat_va_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
    const dataA2 = allocLocal(fctx, `__arr_cat_da_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
    const newData2 = allocLocal(fctx, `__arr_cat_nd_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
    const lenA2 = allocLocal(fctx, `__arr_cat_la_${fctx.locals.length}`, { kind: "i32" });
    compileExpression(ctx, fctx, propAccess.expression);
    fctx.body.push({ op: "local.tee", index: vecA2 });
    emitReceiverNullGuard(ctx, fctx, vecA2);
    fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
    fctx.body.push({ op: "local.set", index: lenA2 });
    fctx.body.push({ op: "local.get", index: vecA2 });
    fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
    fctx.body.push({ op: "local.set", index: dataA2 });
    fctx.body.push({ op: "local.get", index: lenA2 });
    fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
    fctx.body.push({ op: "local.set", index: newData2 });
    emitArrayCopy(fctx, arrTypeIdx, newData2, null, dataA2, null, lenA2);
    fctx.body.push({ op: "local.get", index: lenA2 });
    fctx.body.push({ op: "local.get", index: newData2 });
    fctx.body.push({ op: "ref.as_non_null" });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  const vecA = allocLocal(fctx, `__arr_cat_va_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const vecB = allocLocal(fctx, `__arr_cat_vb_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataA = allocLocal(fctx, `__arr_cat_da_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const dataB = allocLocal(fctx, `__arr_cat_db_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const newData = allocLocal(fctx, `__arr_cat_nd_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenA = allocLocal(fctx, `__arr_cat_la_${fctx.locals.length}`, { kind: "i32" });
  const lenB = allocLocal(fctx, `__arr_cat_lb_${fctx.locals.length}`, { kind: "i32" });
  const totalLen = allocLocal(fctx, `__arr_cat_tl_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecA });
  emitReceiverNullGuard(ctx, fctx, vecA);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenA });
  fctx.body.push({ op: "local.get", index: vecA });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataA });
  compileExpression(ctx, fctx, callExpr.arguments[0]);
  fctx.body.push({ op: "local.tee", index: vecB });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenB });
  fctx.body.push({ op: "local.get", index: vecB });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataB });
  fctx.body.push({ op: "local.get", index: lenA });
  fctx.body.push({ op: "local.get", index: lenB });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: totalLen });
  fctx.body.push({ op: "local.get", index: totalLen });
  fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: newData });
  emitArrayCopy(fctx, arrTypeIdx, newData, null, dataA, null, lenA);
  emitArrayCopy(fctx, arrTypeIdx, newData, lenA, dataB, null, lenB);
  fctx.body.push({ op: "local.get", index: totalLen });
  fctx.body.push({ op: "local.get", index: newData });
  fctx.body.push({ op: "ref.as_non_null" });
  fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compileArrayJoin(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  const concatIdx = ctx.funcMap.get("concat");
  const toStrIdx = ctx.funcMap.get("number_toString");
  if (concatIdx === void 0) {
    ctx.errors.push({
      message: "join requires string support (wasm:js-string concat)",
      line: getLine(callExpr),
      column: getCol(callExpr),
    });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__arr_join_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_join_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenTmp = allocLocal(fctx, `__arr_join_len_${fctx.locals.length}`, { kind: "i32" });
  const iTmp = allocLocal(fctx, `__arr_join_i_${fctx.locals.length}`, { kind: "i32" });
  const resultTmp = allocLocal(fctx, `__arr_join_res_${fctx.locals.length}`, { kind: "externref" });
  const sepTmp = allocLocal(fctx, `__arr_join_sep_${fctx.locals.length}`, { kind: "externref" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  if (callExpr.arguments.length >= 1) {
    compileExpression(ctx, fctx, callExpr.arguments[0]);
  } else {
    const commaGlobalIdx = ctx.stringGlobalMap.get(",");
    if (commaGlobalIdx !== void 0) {
      fctx.body.push({ op: "global.get", index: commaGlobalIdx });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
  }
  fctx.body.push({ op: "local.set", index: sepTmp });
  fctx.body.push({ op: "ref.null.extern" });
  fctx.body.push({ op: "local.set", index: resultTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  const elemToStr = [
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
  ];
  if (elemType.kind === "f64" && toStrIdx !== void 0) {
    elemToStr.push({ op: "call", funcIdx: toStrIdx });
  } else if (elemType.kind === "i32" && toStrIdx !== void 0) {
    elemToStr.push({ op: "f64.convert_i32_s" });
    elemToStr.push({ op: "call", funcIdx: toStrIdx });
  }
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 0 },
    { op: "i32.eq" },
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [...elemToStr, { op: "local.set", index: resultTmp }],
      else: [
        { op: "local.get", index: resultTmp },
        { op: "local.get", index: sepTmp },
        { op: "call", funcIdx: concatIdx },
        ...elemToStr,
        { op: "call", funcIdx: concatIdx },
        { op: "local.set", index: resultTmp },
      ],
    },
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: resultTmp });
  return { kind: "externref" };
}
function compileArraySplice(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, _elemType) {
  if (callExpr.arguments.length === 0) {
    compileExpression(ctx, fctx, propAccess.expression);
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  const vecTmp = allocLocal(fctx, `__arr_spl_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_spl_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const delData = allocLocal(fctx, `__arr_spl_deld_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenTmp = allocLocal(fctx, `__arr_spl_len_${fctx.locals.length}`, { kind: "i32" });
  const startTmp = allocLocal(fctx, `__arr_spl_s_${fctx.locals.length}`, { kind: "i32" });
  const delCountTmp = allocLocal(fctx, `__arr_spl_dc_${fctx.locals.length}`, { kind: "i32" });
  const newLenTmp = allocLocal(fctx, `__arr_spl_nl_${fctx.locals.length}`, { kind: "i32" });
  const tailCountTmp = allocLocal(fctx, `__arr_spl_tc_${fctx.locals.length}`, { kind: "i32" });
  const tailStartTmp = allocLocal(fctx, `__arr_spl_ts_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  compileExpression(ctx, fctx, callExpr.arguments[0], { kind: "f64" });
  fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  fctx.body.push({ op: "local.set", index: startTmp });
  emitClampIndex(fctx, startTmp, lenTmp);
  if (callExpr.arguments.length >= 2) {
    compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "f64" });
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  } else {
    fctx.body.push({ op: "local.get", index: lenTmp });
    fctx.body.push({ op: "local.get", index: startTmp });
    fctx.body.push({ op: "i32.sub" });
  }
  fctx.body.push({ op: "local.set", index: delCountTmp });
  emitClampNonNeg(fctx, delCountTmp);
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "local.get", index: startTmp });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "local.set", index: tailCountTmp });
  fctx.body.push({ op: "local.get", index: delCountTmp });
  fctx.body.push({ op: "local.get", index: tailCountTmp });
  fctx.body.push({ op: "i32.gt_s" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "local.get", index: tailCountTmp },
      { op: "local.set", index: delCountTmp },
    ],
  });
  emitClampNonNeg(fctx, delCountTmp);
  fctx.body.push({ op: "local.get", index: delCountTmp });
  fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: delData });
  emitArrayCopy(fctx, arrTypeIdx, delData, null, dataTmp, startTmp, delCountTmp);
  fctx.body.push({ op: "local.get", index: startTmp });
  fctx.body.push({ op: "local.get", index: delCountTmp });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: tailStartTmp });
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "local.get", index: tailStartTmp });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "local.set", index: tailCountTmp });
  emitClampNonNeg(fctx, tailCountTmp);
  emitArrayCopy(fctx, arrTypeIdx, dataTmp, startTmp, dataTmp, tailStartTmp, tailCountTmp);
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "local.get", index: delCountTmp });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "local.set", index: newLenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "local.get", index: newLenTmp });
  fctx.body.push({ op: "struct.set", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.get", index: delCountTmp });
  fctx.body.push({ op: "local.get", index: delData });
  fctx.body.push({ op: "ref.as_non_null" });
  fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function setupArrayCallback(ctx, fctx, callExpr, methodName, tag, bridgeName) {
  const cbArg = callExpr.arguments[0];
  const cbResult =
    ts4.isArrowFunction(cbArg) || ts4.isFunctionExpression(cbArg)
      ? compileArrowAsClosure(ctx, fctx, cbArg)
      : compileExpression(ctx, fctx, cbArg);
  let closureInfo;
  let closureTypeIdx;
  let closureTmp;
  if (cbResult && (cbResult.kind === "ref" || cbResult.kind === "ref_null")) {
    closureTypeIdx = cbResult.typeIdx;
    closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
    if (closureInfo) {
      closureTmp = allocLocal(fctx, `__arr_${tag}_clcb_${fctx.locals.length}`, cbResult);
      fctx.body.push({ op: "local.set", index: closureTmp });
    }
  }
  let callBridgeIdx;
  let cbTmp;
  if (!closureInfo) {
    const bridge = bridgeName ?? (ctx.fast ? "__call_1_i32" : "__call_1_f64");
    callBridgeIdx = ctx.funcMap.get(bridge);
    if (callBridgeIdx === void 0) {
      ctx.errors.push({
        message: `Missing ${bridge} import for ${methodName}`,
        line: getLine(callExpr),
        column: getCol(callExpr),
      });
      return null;
    }
    cbTmp = allocLocal(fctx, `__arr_${tag}_cb_${fctx.locals.length}`, { kind: "externref" });
    fctx.body.push({ op: "local.set", index: cbTmp });
  }
  return { closureInfo, closureTypeIdx, closureTmp, callBridgeIdx, cbTmp };
}
function setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, tag) {
  const vecTmp = allocLocal(fctx, `__arr_${tag}_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_${tag}_data_${fctx.locals.length}`, {
    kind: "ref_null",
    typeIdx: arrTypeIdx,
  });
  const lenTmp = allocLocal(fctx, `__arr_${tag}_len_${fctx.locals.length}`, { kind: "i32" });
  const iTmp = allocLocal(fctx, `__arr_${tag}_i_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  return { vecTmp, dataTmp, lenTmp, iTmp, getOp };
}
function buildClosureCallInstrs(ctx, fctx, setup, elemType, vecTypeIdx, arrTypeIdx, loop, elemSource) {
  const { closureInfo, closureTypeIdx, closureTmp } = setup;
  if (!closureInfo || closureTypeIdx === void 0 || closureTmp === void 0) return [];
  const numParams = closureInfo.paramTypes.length;
  const elemCoerce = closureInfo.paramTypes[0] ? coercionInstrs(ctx, elemType, closureInfo.paramTypes[0], fctx) : [];
  return [
    { op: "local.get", index: closureTmp },
    // Element value
    ...(elemSource.kind === "local"
      ? [{ op: "local.get", index: elemSource.index }]
      : [
          { op: "local.get", index: loop.dataTmp },
          { op: "local.get", index: loop.iTmp },
          { op: loop.getOp, typeIdx: arrTypeIdx },
        ]),
    ...elemCoerce,
    // Index (2nd user param)
    ...(numParams >= 2
      ? [
          { op: "local.get", index: loop.iTmp },
          ...coercionInstrs(ctx, { kind: "i32" }, closureInfo.paramTypes[1] ?? { kind: "i32" }, fctx),
        ]
      : []),
    // Array (3rd user param)
    ...(numParams >= 3
      ? [
          { op: "local.get", index: loop.vecTmp },
          ...coercionInstrs(
            ctx,
            { kind: "ref_null", typeIdx: vecTypeIdx },
            closureInfo.paramTypes[2] ?? { kind: "ref_null", typeIdx: vecTypeIdx },
            fctx,
          ),
        ]
      : []),
    { op: "local.get", index: closureTmp },
    { op: "struct.get", typeIdx: closureTypeIdx, fieldIdx: 0 },
    ...guardedFuncRefCastInstrs(fctx, closureInfo.funcTypeIdx),
    { op: "ref.as_non_null" },
    { op: "call_ref", typeIdx: closureInfo.funcTypeIdx },
  ];
}
function buildBridgeCallInstrs(ctx, setup, elemType, arrTypeIdx, loop, elemSource) {
  return [
    { op: "local.get", index: setup.cbTmp },
    ...(elemSource.kind === "local"
      ? [
          { op: "local.get", index: elemSource.index },
          ...(!ctx.fast && elemType.kind === "i32" ? [{ op: "f64.convert_i32_s" }] : []),
        ]
      : [
          { op: "local.get", index: loop.dataTmp },
          { op: "local.get", index: loop.iTmp },
          { op: loop.getOp, typeIdx: arrTypeIdx },
          ...(!ctx.fast && elemType.kind === "i32" ? [{ op: "f64.convert_i32_s" }] : []),
        ]),
    { op: "call", funcIdx: setup.callBridgeIdx },
  ];
}
function buildTruthyCheck(ctx, setup) {
  if (setup.closureInfo) {
    const retKind = setup.closureInfo.returnType?.kind;
    if (retKind === "f64") {
      return [{ op: "f64.const", value: 0 }, { op: "f64.ne" }];
    }
    if (retKind === "i32") {
      return [];
    }
    if (retKind === "externref" || retKind === "ref" || retKind === "ref_null") {
      return [{ op: "ref.is_null" }, { op: "i32.eqz" }];
    }
    return [];
  }
  return ctx.fast ? [] : [{ op: "f64.const", value: 0 }, { op: "f64.ne" }];
}
function buildFalsyCheck(ctx, setup) {
  if (setup.closureInfo) {
    const retKind = setup.closureInfo.returnType?.kind;
    if (retKind === "f64") {
      return [{ op: "f64.const", value: 0 }, { op: "f64.eq" }];
    }
    if (retKind === "i32") {
      return [{ op: "i32.eqz" }];
    }
    if (retKind === "externref" || retKind === "ref" || retKind === "ref_null") {
      return [{ op: "ref.is_null" }];
    }
    return [{ op: "i32.eqz" }];
  }
  return ctx.fast ? [{ op: "i32.eqz" }] : [{ op: "f64.const", value: 0 }, { op: "f64.eq" }];
}
function emitArrayLoop(fctx, loopBody) {
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
}
function loopExitCheck(loop) {
  return [
    { op: "local.get", index: loop.iTmp },
    { op: "local.get", index: loop.lenTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
  ];
}
function loopIncrement(loop) {
  return [
    { op: "local.get", index: loop.iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: loop.iTmp },
    { op: "br", depth: 0 },
  ];
}
function buildCallAndCheck(ctx, fctx, setup, elemType, vecTypeIdx, arrTypeIdx, loop, elemSource, check) {
  const callInstrs = setup.closureInfo
    ? buildClosureCallInstrs(ctx, fctx, setup, elemType, vecTypeIdx, arrTypeIdx, loop, elemSource)
    : buildBridgeCallInstrs(ctx, setup, elemType, arrTypeIdx, loop, elemSource);
  const checkInstrs =
    check === "truthy" ? buildTruthyCheck(ctx, setup) : check === "falsy" ? buildFalsyCheck(ctx, setup) : [];
  return [...callInstrs, ...checkInstrs];
}
function compileArrayFilter(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.filter")) {
    fctx.body.push({ op: "unreachable" });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  const setup = setupArrayCallback(ctx, fctx, callExpr, "filter", "flt");
  if (!setup) return null;
  const resData = allocLocal(fctx, `__arr_flt_rd_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const resLen = allocLocal(fctx, `__arr_flt_rl_${fctx.locals.length}`, { kind: "i32" });
  const elemTmp = allocLocal(fctx, `__arr_flt_el_${fctx.locals.length}`, elemType);
  const loop = setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, "flt");
  fctx.body.push({ op: "local.get", index: loop.lenTmp });
  fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: resData });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: resLen });
  const callAndCheck = buildCallAndCheck(
    ctx,
    fctx,
    setup,
    elemType,
    vecTypeIdx,
    arrTypeIdx,
    loop,
    { kind: "local", index: elemTmp },
    "truthy",
  );
  const loopBody = [
    ...loopExitCheck(loop),
    // elem = data[i]
    { op: "local.get", index: loop.dataTmp },
    { op: "local.get", index: loop.iTmp },
    { op: loop.getOp, typeIdx: arrTypeIdx },
    { op: "local.set", index: elemTmp },
    ...callAndCheck,
    // if result is truthy, add element to result
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: resData },
        { op: "local.get", index: resLen },
        { op: "local.get", index: elemTmp },
        { op: "array.set", typeIdx: arrTypeIdx },
        { op: "local.get", index: resLen },
        { op: "i32.const", value: 1 },
        { op: "i32.add" },
        { op: "local.set", index: resLen },
      ],
    },
    ...loopIncrement(loop),
  ];
  emitArrayLoop(fctx, loopBody);
  fctx.body.push({ op: "local.get", index: resLen });
  fctx.body.push({ op: "local.get", index: resData });
  fctx.body.push({ op: "ref.as_non_null" });
  fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compileArrayMap(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.map")) {
    fctx.body.push({ op: "unreachable" });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  const cbArg = callExpr.arguments[0];
  let mapResultElemType = elemType;
  let mapArrTypeIdx = arrTypeIdx;
  let mapVecTypeIdx = vecTypeIdx;
  if (ts4.isArrowFunction(cbArg) || ts4.isFunctionExpression(cbArg)) {
    const cbSig = ctx.checker.getSignatureFromDeclaration(cbArg);
    if (cbSig) {
      const retType = ctx.checker.getReturnTypeOfSignature(cbSig);
      const mapped = resolveWasmType(ctx, retType);
      if (mapped.kind !== elemType.kind) {
        mapResultElemType = mapped;
        mapArrTypeIdx = getOrRegisterArrayType(ctx, mapResultElemType.kind, mapResultElemType);
        mapVecTypeIdx = getOrRegisterVecType(ctx, mapResultElemType.kind, mapResultElemType);
      }
    }
  }
  const setup = setupArrayCallback(ctx, fctx, callExpr, "map", "map");
  if (!setup) return null;
  if (setup.closureInfo?.returnType && setup.closureInfo.returnType.kind !== mapResultElemType.kind) {
    mapResultElemType = setup.closureInfo.returnType;
    mapArrTypeIdx = getOrRegisterArrayType(ctx, mapResultElemType.kind, mapResultElemType);
    mapVecTypeIdx = getOrRegisterVecType(ctx, mapResultElemType.kind, mapResultElemType);
  }
  const loop = setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, "map");
  const resData = allocLocal(fctx, `__arr_map_rd_${fctx.locals.length}`, { kind: "ref_null", typeIdx: mapArrTypeIdx });
  fctx.body.push({ op: "local.get", index: loop.lenTmp });
  fctx.body.push({ op: "array.new_default", typeIdx: mapArrTypeIdx });
  fctx.body.push({ op: "local.set", index: resData });
  let callInstrs;
  if (setup.closureInfo) {
    const retType = setup.closureInfo.returnType;
    callInstrs = [
      ...buildClosureCallInstrs(ctx, fctx, setup, elemType, vecTypeIdx, arrTypeIdx, loop, { kind: "inline" }),
      // Coerce closure return type to map result element type if needed
      ...(retType && retType.kind !== mapResultElemType.kind
        ? coercionInstrs(ctx, retType, mapResultElemType, fctx)
        : []),
    ];
  } else {
    callInstrs = [
      ...buildBridgeCallInstrs(ctx, setup, elemType, arrTypeIdx, loop, { kind: "inline" }),
      // Convert result to target element type if needed
      ...(!ctx.fast && mapResultElemType.kind === "i32" ? [{ op: "i32.trunc_sat_f64_s" }] : []),
    ];
  }
  const loopBody = [
    ...loopExitCheck(loop),
    // resData[i] = cb(data[i])
    { op: "local.get", index: resData },
    { op: "local.get", index: loop.iTmp },
    ...callInstrs,
    { op: "array.set", typeIdx: mapArrTypeIdx },
    ...loopIncrement(loop),
  ];
  emitArrayLoop(fctx, loopBody);
  fctx.body.push({ op: "local.get", index: loop.lenTmp });
  fctx.body.push({ op: "local.get", index: resData });
  fctx.body.push({ op: "ref.as_non_null" });
  fctx.body.push({ op: "struct.new", typeIdx: mapVecTypeIdx });
  return { kind: "ref_null", typeIdx: mapVecTypeIdx };
}
function compileArrayReduce(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.reduce")) {
    fctx.body.push({ op: "unreachable" });
    return elemType;
  }
  const numKind = ctx.fast ? "i32" : "f64";
  const bridgeName = ctx.fast ? "__call_2_i32" : "__call_2_f64";
  const setup = setupArrayCallback(ctx, fctx, callExpr, "reduce", "red", bridgeName);
  if (!setup) return null;
  const loop = setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, "red");
  const accTmp = allocLocal(fctx, `__arr_red_acc_${fctx.locals.length}`, { kind: numKind });
  if (callExpr.arguments.length >= 2) {
    compileExpression(ctx, fctx, callExpr.arguments[1], { kind: numKind });
    fctx.body.push({ op: "local.set", index: accTmp });
  } else {
    fctx.body.push({ op: "local.get", index: loop.lenTmp });
    fctx.body.push({ op: "i32.eqz" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: throwStringInstrs(ctx, "TypeError: Reduce of empty array with no initial value"),
    });
    fctx.body.push({ op: "local.get", index: loop.dataTmp });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: elemType.kind === "i16" ? "array.get_s" : "array.get", typeIdx: arrTypeIdx });
    fctx.body.push({ op: "local.set", index: accTmp });
    fctx.body.push({ op: "i32.const", value: 1 });
    fctx.body.push({ op: "local.set", index: loop.iTmp });
  }
  let callInstrs;
  if (setup.closureInfo && setup.closureTypeIdx !== void 0 && setup.closureTmp !== void 0) {
    const ci = setup.closureInfo;
    const numParams = ci.paramTypes.length;
    const accCoerce = ci.paramTypes[0] ? coercionInstrs(ctx, { kind: numKind }, ci.paramTypes[0], fctx) : [];
    const elemCoerce = ci.paramTypes[1] ? coercionInstrs(ctx, elemType, ci.paramTypes[1], fctx) : [];
    callInstrs = [
      { op: "local.get", index: setup.closureTmp },
      { op: "local.get", index: accTmp },
      ...accCoerce,
      { op: "local.get", index: loop.dataTmp },
      { op: "local.get", index: loop.iTmp },
      { op: loop.getOp, typeIdx: arrTypeIdx },
      ...elemCoerce,
      ...(numParams >= 3
        ? [
            { op: "local.get", index: loop.iTmp },
            ...coercionInstrs(ctx, { kind: "i32" }, ci.paramTypes[2] ?? { kind: "i32" }, fctx),
          ]
        : []),
      ...(numParams >= 4
        ? [
            { op: "local.get", index: loop.vecTmp },
            ...coercionInstrs(
              ctx,
              { kind: "ref_null", typeIdx: vecTypeIdx },
              ci.paramTypes[3] ?? { kind: "ref_null", typeIdx: vecTypeIdx },
              fctx,
            ),
          ]
        : []),
      { op: "local.get", index: setup.closureTmp },
      { op: "struct.get", typeIdx: setup.closureTypeIdx, fieldIdx: 0 },
      ...guardedFuncRefCastInstrs(fctx, ci.funcTypeIdx),
      { op: "ref.as_non_null" },
      { op: "call_ref", typeIdx: ci.funcTypeIdx },
      // Coerce closure return type to accumulator type if needed
      ...(ci.returnType && ci.returnType.kind !== numKind
        ? coercionInstrs(ctx, ci.returnType, { kind: numKind }, fctx)
        : []),
      { op: "local.set", index: accTmp },
    ];
  } else {
    callInstrs = [
      { op: "local.get", index: setup.cbTmp },
      { op: "local.get", index: accTmp },
      { op: "local.get", index: loop.dataTmp },
      { op: "local.get", index: loop.iTmp },
      { op: loop.getOp, typeIdx: arrTypeIdx },
      ...(!ctx.fast && elemType.kind === "i32" ? [{ op: "f64.convert_i32_s" }] : []),
      { op: "call", funcIdx: setup.callBridgeIdx },
      { op: "local.set", index: accTmp },
    ];
  }
  const loopBody = [...loopExitCheck(loop), ...callInstrs, ...loopIncrement(loop)];
  emitArrayLoop(fctx, loopBody);
  fctx.body.push({ op: "local.get", index: accTmp });
  return { kind: numKind };
}
function compileArrayReduceRight(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.reduceRight")) {
    fctx.body.push({ op: "unreachable" });
    return elemType;
  }
  const numKind = ctx.fast ? "i32" : "f64";
  const bridgeName = ctx.fast ? "__call_2_i32" : "__call_2_f64";
  const setup = setupArrayCallback(ctx, fctx, callExpr, "reduceRight", "rr", bridgeName);
  if (!setup) return null;
  const vecTmp = allocLocal(fctx, `__arr_rr_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_rr_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenTmp = allocLocal(fctx, `__arr_rr_len_${fctx.locals.length}`, { kind: "i32" });
  const iTmp = allocLocal(fctx, `__arr_rr_i_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  const accTmp = allocLocal(fctx, `__arr_rr_acc_${fctx.locals.length}`, { kind: numKind });
  if (callExpr.arguments.length >= 2) {
    compileExpression(ctx, fctx, callExpr.arguments[1], { kind: numKind });
    fctx.body.push({ op: "local.set", index: accTmp });
    fctx.body.push({ op: "local.get", index: lenTmp });
    fctx.body.push({ op: "i32.const", value: 1 });
    fctx.body.push({ op: "i32.sub" });
    fctx.body.push({ op: "local.set", index: iTmp });
  } else {
    fctx.body.push({ op: "local.get", index: lenTmp });
    fctx.body.push({ op: "i32.eqz" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: throwStringInstrs(ctx, "TypeError: Reduce of empty array with no initial value"),
    });
    fctx.body.push({ op: "local.get", index: dataTmp });
    fctx.body.push({ op: "local.get", index: lenTmp });
    fctx.body.push({ op: "i32.const", value: 1 });
    fctx.body.push({ op: "i32.sub" });
    fctx.body.push({ op: getOp, typeIdx: arrTypeIdx });
    fctx.body.push({ op: "local.set", index: accTmp });
    fctx.body.push({ op: "local.get", index: lenTmp });
    fctx.body.push({ op: "i32.const", value: 2 });
    fctx.body.push({ op: "i32.sub" });
    fctx.body.push({ op: "local.set", index: iTmp });
  }
  const loop = { vecTmp, dataTmp, lenTmp, iTmp, getOp };
  let callInstrs;
  if (setup.closureInfo && setup.closureTypeIdx !== void 0 && setup.closureTmp !== void 0) {
    const ci = setup.closureInfo;
    const numParams = ci.paramTypes.length;
    const accCoerce = ci.paramTypes[0] ? coercionInstrs(ctx, { kind: numKind }, ci.paramTypes[0], fctx) : [];
    const elemCoerce = ci.paramTypes[1] ? coercionInstrs(ctx, elemType, ci.paramTypes[1], fctx) : [];
    callInstrs = [
      { op: "local.get", index: setup.closureTmp },
      { op: "local.get", index: accTmp },
      ...accCoerce,
      { op: "local.get", index: dataTmp },
      { op: "local.get", index: iTmp },
      { op: getOp, typeIdx: arrTypeIdx },
      ...elemCoerce,
      ...(numParams >= 3
        ? [
            { op: "local.get", index: iTmp },
            ...coercionInstrs(ctx, { kind: "i32" }, ci.paramTypes[2] ?? { kind: "i32" }, fctx),
          ]
        : []),
      ...(numParams >= 4
        ? [
            { op: "local.get", index: vecTmp },
            ...coercionInstrs(
              ctx,
              { kind: "ref_null", typeIdx: vecTypeIdx },
              ci.paramTypes[3] ?? { kind: "ref_null", typeIdx: vecTypeIdx },
              fctx,
            ),
          ]
        : []),
      { op: "local.get", index: setup.closureTmp },
      { op: "struct.get", typeIdx: setup.closureTypeIdx, fieldIdx: 0 },
      ...guardedFuncRefCastInstrs(fctx, ci.funcTypeIdx),
      { op: "ref.as_non_null" },
      { op: "call_ref", typeIdx: ci.funcTypeIdx },
      // Coerce closure return type to accumulator type if needed
      ...(ci.returnType && ci.returnType.kind !== numKind
        ? coercionInstrs(ctx, ci.returnType, { kind: numKind }, fctx)
        : []),
      { op: "local.set", index: accTmp },
    ];
  } else {
    callInstrs = [
      { op: "local.get", index: setup.cbTmp },
      { op: "local.get", index: accTmp },
      { op: "local.get", index: dataTmp },
      { op: "local.get", index: iTmp },
      { op: getOp, typeIdx: arrTypeIdx },
      ...(!ctx.fast && elemType.kind === "i32" ? [{ op: "f64.convert_i32_s" }] : []),
      { op: "call", funcIdx: setup.callBridgeIdx },
      { op: "local.set", index: accTmp },
    ];
  }
  const loopBody = [
    // Exit check: if (i < 0) break
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 0 },
    { op: "i32.lt_s" },
    { op: "br_if", depth: 1 },
    // Callback
    ...callInstrs,
    // i--
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.sub" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  emitArrayLoop(fctx, loopBody);
  fctx.body.push({ op: "local.get", index: accTmp });
  return { kind: numKind };
}
function compileArrayForEach(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.forEach")) {
    fctx.body.push({ op: "unreachable" });
    return null;
  }
  const setup = setupArrayCallback(ctx, fctx, callExpr, "forEach", "fe");
  if (!setup) return null;
  const loop = setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, "fe");
  if (setup.closureInfo) {
    const callInstrs = buildClosureCallInstrs(ctx, fctx, setup, elemType, vecTypeIdx, arrTypeIdx, loop, {
      kind: "inline",
    });
    const dropInstrs = setup.closureInfo.returnType ? [{ op: "drop" }] : [];
    const loopBody = [...loopExitCheck(loop), ...callInstrs, ...dropInstrs, ...loopIncrement(loop)];
    emitArrayLoop(fctx, loopBody);
  } else {
    const callInstrs = buildBridgeCallInstrs(ctx, setup, elemType, arrTypeIdx, loop, { kind: "inline" });
    const loopBody = [...loopExitCheck(loop), ...callInstrs, { op: "drop" }, ...loopIncrement(loop)];
    emitArrayLoop(fctx, loopBody);
  }
  return null;
}
function compileArrayFind(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.find")) {
    fctx.body.push({ op: "unreachable" });
    return elemType;
  }
  const setup = setupArrayCallback(ctx, fctx, callExpr, "find", "find");
  if (!setup) return null;
  const elemTmpLocal = allocLocal(fctx, `__arr_find_el_${fctx.locals.length}`, elemType);
  const loop = setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, "find");
  const callAndCheck = buildCallAndCheck(
    ctx,
    fctx,
    setup,
    elemType,
    vecTypeIdx,
    arrTypeIdx,
    loop,
    { kind: "local", index: elemTmpLocal },
    "truthy",
  );
  const findResType = ctx.fast ? elemType : { kind: "f64" };
  const findResTmp = allocLocal(fctx, `__arr_find_res_${fctx.locals.length}`, findResType);
  if (ctx.fast) {
    fctx.body.push({ op: "i32.const", value: 0 });
  } else {
    fctx.body.push({ op: "f64.const", value: 0 });
    fctx.body.push({ op: "f64.const", value: 0 });
    fctx.body.push({ op: "f64.div" });
  }
  fctx.body.push({ op: "local.set", index: findResTmp });
  const loopBody = [
    ...loopExitCheck(loop),
    { op: "local.get", index: loop.dataTmp },
    { op: "local.get", index: loop.iTmp },
    { op: loop.getOp, typeIdx: arrTypeIdx },
    { op: "local.set", index: elemTmpLocal },
    ...callAndCheck,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: elemTmpLocal },
        ...(!ctx.fast && elemType.kind === "i32" ? [{ op: "f64.convert_i32_s" }] : []),
        { op: "local.set", index: findResTmp },
        { op: "br", depth: 2 },
      ],
    },
    ...loopIncrement(loop),
  ];
  emitArrayLoop(fctx, loopBody);
  fctx.body.push({ op: "local.get", index: findResTmp });
  return ctx.fast ? elemType : { kind: "f64" };
}
function compileArrayFindIndex(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.findIndex")) {
    fctx.body.push({ op: "unreachable" });
    return { kind: "i32" };
  }
  const setup = setupArrayCallback(ctx, fctx, callExpr, "findIndex", "fi");
  if (!setup) return null;
  const loop = setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, "fi");
  const callAndCheck = buildCallAndCheck(
    ctx,
    fctx,
    setup,
    elemType,
    vecTypeIdx,
    arrTypeIdx,
    loop,
    { kind: "inline" },
    "truthy",
  );
  const fiResType = ctx.fast ? { kind: "i32" } : { kind: "f64" };
  const fiResTmp = allocLocal(fctx, `__arr_fi_res_${fctx.locals.length}`, fiResType);
  if (ctx.fast) {
    fctx.body.push({ op: "i32.const", value: -1 });
  } else {
    fctx.body.push({ op: "f64.const", value: -1 });
  }
  fctx.body.push({ op: "local.set", index: fiResTmp });
  const loopBody = [
    ...loopExitCheck(loop),
    ...callAndCheck,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: loop.iTmp },
        ...(ctx.fast ? [] : [{ op: "f64.convert_i32_s" }]),
        { op: "local.set", index: fiResTmp },
        { op: "br", depth: 2 },
      ],
    },
    ...loopIncrement(loop),
  ];
  emitArrayLoop(fctx, loopBody);
  fctx.body.push({ op: "local.get", index: fiResTmp });
  return ctx.fast ? { kind: "i32" } : { kind: "f64" };
}
function compileArraySome(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.some")) {
    fctx.body.push({ op: "unreachable" });
    return { kind: "i32" };
  }
  const setup = setupArrayCallback(ctx, fctx, callExpr, "some", "some");
  if (!setup) return null;
  const loop = setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, "some");
  const callAndCheck = buildCallAndCheck(
    ctx,
    fctx,
    setup,
    elemType,
    vecTypeIdx,
    arrTypeIdx,
    loop,
    { kind: "inline" },
    "truthy",
  );
  const resTmp = allocLocal(fctx, `__arr_some_res_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: resTmp });
  const loopBody = [
    ...loopExitCheck(loop),
    ...callAndCheck,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: 1 },
        { op: "local.set", index: resTmp },
        { op: "br", depth: 2 },
      ],
    },
    ...loopIncrement(loop),
  ];
  emitArrayLoop(fctx, loopBody);
  fctx.body.push({ op: "local.get", index: resTmp });
  return { kind: "i32" };
}
function compileArrayEvery(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (emitCallbackTypeCheck(ctx, fctx, callExpr, "Array.prototype.every")) {
    fctx.body.push({ op: "unreachable" });
    return { kind: "i32" };
  }
  const setup = setupArrayCallback(ctx, fctx, callExpr, "every", "evr");
  if (!setup) return null;
  const loop = setupArrayLoop(ctx, fctx, propAccess, vecTypeIdx, arrTypeIdx, elemType, "evr");
  const callAndCheck = buildCallAndCheck(
    ctx,
    fctx,
    setup,
    elemType,
    vecTypeIdx,
    arrTypeIdx,
    loop,
    { kind: "inline" },
    "falsy",
  );
  const resTmp = allocLocal(fctx, `__arr_evr_res_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "local.set", index: resTmp });
  const loopBody = [
    ...loopExitCheck(loop),
    ...callAndCheck,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: 0 },
        { op: "local.set", index: resTmp },
        { op: "br", depth: 2 },
      ],
    },
    ...loopIncrement(loop),
  ];
  emitArrayLoop(fctx, loopBody);
  fctx.body.push({ op: "local.get", index: resTmp });
  return { kind: "i32" };
}
function compileArraySort(ctx, fctx, propAccess, _callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  const elemKind = elemType.kind;
  const timsortIdx = ensureTimsortHelper(ctx, vecTypeIdx, arrTypeIdx, elemKind);
  const vecTmp = allocLocal(fctx, `__arr_sort_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "call", funcIdx: timsortIdx });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "ref.as_non_null" });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compileArrayFill(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 1) {
    ctx.errors.push({
      message: "fill requires at least 1 argument",
      line: getLine(callExpr),
      column: getCol(callExpr),
    });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__arr_fill_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_fill_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenTmp = allocLocal(fctx, `__arr_fill_len_${fctx.locals.length}`, { kind: "i32" });
  const valTmp = allocLocal(fctx, `__arr_fill_val_${fctx.locals.length}`, elemType);
  const startTmp = allocLocal(fctx, `__arr_fill_s_${fctx.locals.length}`, { kind: "i32" });
  const endTmp = allocLocal(fctx, `__arr_fill_e_${fctx.locals.length}`, { kind: "i32" });
  const iTmp = allocLocal(fctx, `__arr_fill_i_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  compileExpression(ctx, fctx, callExpr.arguments[0], elemType);
  fctx.body.push({ op: "local.set", index: valTmp });
  if (callExpr.arguments.length >= 2) {
    if (ctx.fast) {
      compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "i32" });
    } else {
      compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "f64" });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
  } else {
    fctx.body.push({ op: "i32.const", value: 0 });
  }
  fctx.body.push({ op: "local.set", index: startTmp });
  emitClampIndex(fctx, startTmp, lenTmp);
  if (callExpr.arguments.length >= 3) {
    if (ctx.fast) {
      compileExpression(ctx, fctx, callExpr.arguments[2], { kind: "i32" });
    } else {
      compileExpression(ctx, fctx, callExpr.arguments[2], { kind: "f64" });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
  } else {
    fctx.body.push({ op: "local.get", index: lenTmp });
  }
  fctx.body.push({ op: "local.set", index: endTmp });
  emitClampIndex(fctx, endTmp, lenTmp);
  fctx.body.push({ op: "local.get", index: startTmp });
  fctx.body.push({ op: "local.set", index: iTmp });
  const loopBody = [
    { op: "local.get", index: iTmp },
    { op: "local.get", index: endTmp },
    { op: "i32.ge_s" },
    { op: "br_if", depth: 1 },
    // data[i] = value
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: "local.get", index: valTmp },
    { op: "array.set", typeIdx: arrTypeIdx },
    // i++
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.add" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: vecTmp });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compileArrayCopyWithin(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, _elemType) {
  if (callExpr.arguments.length < 2) {
    ctx.errors.push({
      message: "copyWithin requires at least 2 arguments (target, start)",
      line: getLine(callExpr),
      column: getCol(callExpr),
    });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__arr_cw_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_cw_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const lenTmp = allocLocal(fctx, `__arr_cw_len_${fctx.locals.length}`, { kind: "i32" });
  const targetTmp = allocLocal(fctx, `__arr_cw_tgt_${fctx.locals.length}`, { kind: "i32" });
  const startTmp = allocLocal(fctx, `__arr_cw_s_${fctx.locals.length}`, { kind: "i32" });
  const endTmp = allocLocal(fctx, `__arr_cw_e_${fctx.locals.length}`, { kind: "i32" });
  const countTmp = allocLocal(fctx, `__arr_cw_cnt_${fctx.locals.length}`, { kind: "i32" });
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenTmp });
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  if (ctx.fast) {
    compileExpression(ctx, fctx, callExpr.arguments[0], { kind: "i32" });
  } else {
    compileExpression(ctx, fctx, callExpr.arguments[0], { kind: "f64" });
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  }
  fctx.body.push({ op: "local.set", index: targetTmp });
  emitClampIndex(fctx, targetTmp, lenTmp);
  if (ctx.fast) {
    compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "i32" });
  } else {
    compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "f64" });
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  }
  fctx.body.push({ op: "local.set", index: startTmp });
  emitClampIndex(fctx, startTmp, lenTmp);
  if (callExpr.arguments.length >= 3) {
    if (ctx.fast) {
      compileExpression(ctx, fctx, callExpr.arguments[2], { kind: "i32" });
    } else {
      compileExpression(ctx, fctx, callExpr.arguments[2], { kind: "f64" });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
  } else {
    fctx.body.push({ op: "local.get", index: lenTmp });
  }
  fctx.body.push({ op: "local.set", index: endTmp });
  emitClampIndex(fctx, endTmp, lenTmp);
  fctx.body.push({ op: "local.get", index: endTmp });
  fctx.body.push({ op: "local.get", index: startTmp });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "local.get", index: targetTmp });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "local.get", index: endTmp });
  fctx.body.push({ op: "local.get", index: startTmp });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "local.get", index: lenTmp });
  fctx.body.push({ op: "local.get", index: targetTmp });
  fctx.body.push({ op: "i32.sub" });
  fctx.body.push({ op: "i32.lt_s" });
  fctx.body.push({ op: "select" });
  fctx.body.push({ op: "local.set", index: countTmp });
  emitClampNonNeg(fctx, countTmp);
  emitArrayCopy(fctx, arrTypeIdx, dataTmp, targetTmp, dataTmp, startTmp, countTmp);
  fctx.body.push({ op: "local.get", index: vecTmp });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compileArrayLastIndexOf(ctx, fctx, propAccess, callExpr, vecTypeIdx, arrTypeIdx, elemType) {
  if (callExpr.arguments.length < 1) {
    ctx.errors.push({ message: "lastIndexOf requires 1 argument", line: getLine(callExpr), column: getCol(callExpr) });
    return null;
  }
  const vecTmp = allocLocal(fctx, `__arr_liof_vec_${fctx.locals.length}`, { kind: "ref_null", typeIdx: vecTypeIdx });
  const dataTmp = allocLocal(fctx, `__arr_liof_data_${fctx.locals.length}`, { kind: "ref_null", typeIdx: arrTypeIdx });
  const iTmp = allocLocal(fctx, `__arr_liof_i_${fctx.locals.length}`, { kind: "i32" });
  const valTmp = allocLocal(fctx, `__arr_liof_val_${fctx.locals.length}`, elemType);
  compileExpression(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.tee", index: vecTmp });
  emitReceiverNullGuard(ctx, fctx, vecTmp);
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  const lenTmp = allocLocal(fctx, `__arr_liof_len_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.set", index: lenTmp });
  if (callExpr.arguments.length >= 2) {
    if (ctx.fast) {
      compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "i32" });
    } else {
      compileExpression(ctx, fctx, callExpr.arguments[1], { kind: "f64" });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
    fctx.body.push({ op: "local.set", index: iTmp });
    fctx.body.push({ op: "local.get", index: iTmp });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "i32.lt_s" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: lenTmp },
        { op: "local.get", index: iTmp },
        { op: "i32.add" },
        { op: "local.set", index: iTmp },
      ],
    });
    fctx.body.push({ op: "local.get", index: iTmp });
    fctx.body.push({ op: "local.get", index: lenTmp });
    fctx.body.push({ op: "i32.const", value: 1 });
    fctx.body.push({ op: "i32.sub" });
    fctx.body.push({ op: "i32.gt_s" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: lenTmp },
        { op: "i32.const", value: 1 },
        { op: "i32.sub" },
        { op: "local.set", index: iTmp },
      ],
    });
  } else {
    fctx.body.push({ op: "local.get", index: lenTmp });
    fctx.body.push({ op: "i32.const", value: 1 });
    fctx.body.push({ op: "i32.sub" });
    fctx.body.push({ op: "local.set", index: iTmp });
  }
  fctx.body.push({ op: "local.get", index: vecTmp });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataTmp });
  compileExpression(ctx, fctx, callExpr.arguments[0], elemType);
  fctx.body.push({ op: "local.set", index: valTmp });
  const getOp = elemType.kind === "i16" ? "array.get_s" : "array.get";
  let liofEqInstrs;
  if (elemType.kind === "externref") {
    addStringImports(ctx);
    const equalsIdx = ctx.funcMap.get("equals");
    liofEqInstrs = [{ op: "call", funcIdx: equalsIdx }];
  } else if (elemType.kind === "ref" || elemType.kind === "ref_null") {
    liofEqInstrs = [{ op: "ref.eq" }];
  } else {
    const eqOp = elemType.kind === "f64" ? "f64.eq" : "i32.eq";
    liofEqInstrs = [{ op: eqOp }];
  }
  const liofResType = ctx.fast ? { kind: "i32" } : { kind: "f64" };
  const liofResTmp = allocLocal(fctx, `__arr_liof_res_${fctx.locals.length}`, liofResType);
  if (ctx.fast) {
    fctx.body.push({ op: "i32.const", value: -1 });
  } else {
    fctx.body.push({ op: "f64.const", value: -1 });
  }
  fctx.body.push({ op: "local.set", index: liofResTmp });
  const loopBody = [
    // if (i < 0) break
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 0 },
    { op: "i32.lt_s" },
    { op: "br_if", depth: 1 },
    // if (data[i] == val) store result and break
    { op: "local.get", index: dataTmp },
    { op: "local.get", index: iTmp },
    { op: getOp, typeIdx: arrTypeIdx },
    { op: "local.get", index: valTmp },
    ...liofEqInstrs,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: ctx.fast
        ? [
            { op: "local.get", index: iTmp },
            { op: "local.set", index: liofResTmp },
            { op: "br", depth: 2 },
            // break out of block
          ]
        : [
            { op: "local.get", index: iTmp },
            { op: "f64.convert_i32_s" },
            { op: "local.set", index: liofResTmp },
            { op: "br", depth: 2 },
            // break out of block
          ],
    },
    // i--
    { op: "local.get", index: iTmp },
    { op: "i32.const", value: 1 },
    { op: "i32.sub" },
    { op: "local.set", index: iTmp },
    { op: "br", depth: 0 },
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
  });
  fctx.body.push({ op: "local.get", index: liofResTmp });
  if (ctx.fast) {
    return { kind: "i32" };
  }
  return { kind: "f64" };
}

// src/codegen/binary-ops.ts
import ts6 from "typescript";

// src/codegen/string-ops.ts
import ts5 from "typescript";
function emitGuardedFuncRefCast2(fctx, funcTypeIdx) {
  const tmpFunc = allocLocal(fctx, `__gfc_${fctx.locals.length}`, { kind: "funcref" });
  fctx.body.push({ op: "local.tee", index: tmpFunc });
  fctx.body.push({ op: "ref.test", typeIdx: funcTypeIdx });
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: { kind: "ref_null", typeIdx: funcTypeIdx } },
    then: [
      { op: "local.get", index: tmpFunc },
      { op: "ref.cast_null", typeIdx: funcTypeIdx },
    ],
    else: [{ op: "ref.null", typeIdx: funcTypeIdx }],
  });
}
function compileStringLiteral(ctx, fctx, value, node) {
  if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
    return compileNativeStringLiteral(ctx, fctx, value);
  }
  const globalIdx = ctx.stringGlobalMap.get(value);
  if (globalIdx !== void 0) {
    fctx.body.push({ op: "global.get", index: globalIdx });
    return { kind: "externref" };
  }
  addStringImports(ctx);
  addStringConstantGlobal(ctx, value);
  const lateGlobalIdx = ctx.stringGlobalMap.get(value);
  if (lateGlobalIdx !== void 0) {
    fctx.body.push({ op: "global.get", index: lateGlobalIdx });
    return { kind: "externref" };
  }
  ctx.errors.push({
    message: `String literal not registered: "${value}"`,
    line: node ? getLine(node) : 0,
    column: node ? getCol(node) : 0,
  });
  return null;
}
function compileNativeStringLiteral(ctx, fctx, value) {
  const strDataTypeIdx = ctx.nativeStrDataTypeIdx;
  const strTypeIdx = ctx.nativeStrTypeIdx;
  fctx.body.push({ op: "i32.const", value: value.length });
  fctx.body.push({ op: "i32.const", value: 0 });
  for (let i = 0; i < value.length; i++) {
    fctx.body.push({ op: "i32.const", value: value.charCodeAt(i) });
  }
  fctx.body.push({ op: "array.new_fixed", typeIdx: strDataTypeIdx, length: value.length });
  fctx.body.push({ op: "struct.new", typeIdx: strTypeIdx });
  return nativeStringType(ctx);
}
function compileTemplateExpression(ctx, fctx, expr) {
  if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
    return compileNativeTemplateExpression(ctx, fctx, expr);
  }
  addStringImports(ctx);
  const concatIdx = ctx.funcMap.get("concat");
  const toStrIdx = ctx.funcMap.get("number_toString");
  if (concatIdx === void 0) return null;
  if (expr.head.text) {
    compileStringLiteral(ctx, fctx, expr.head.text, expr.head);
  } else {
  }
  for (let i = 0; i < expr.templateSpans.length; i++) {
    const span = expr.templateSpans[i];
    const spanType = compileExpression(ctx, fctx, span.expression);
    if (spanType && spanType.kind === "f64" && toStrIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: toStrIdx });
    } else if (spanType && spanType.kind === "i32" && toStrIdx !== void 0) {
      fctx.body.push({ op: "f64.convert_i32_s" });
      fctx.body.push({ op: "call", funcIdx: toStrIdx });
    } else if (spanType && spanType.kind === "i64" && toStrIdx !== void 0) {
      fctx.body.push({ op: "f64.convert_i64_s" });
      fctx.body.push({ op: "call", funcIdx: toStrIdx });
    } else if (spanType && (spanType.kind === "ref" || spanType.kind === "ref_null")) {
      coerceType2(ctx, fctx, spanType, { kind: "externref" }, "string");
    }
    if (i === 0 && !expr.head.text) {
    } else {
      fctx.body.push({ op: "call", funcIdx: concatIdx });
    }
    if (span.literal.text) {
      compileStringLiteral(ctx, fctx, span.literal.text, span.literal);
      fctx.body.push({ op: "call", funcIdx: concatIdx });
    }
  }
  return { kind: "externref" };
}
function compileNativeTemplateExpression(ctx, fctx, expr) {
  const concatIdx = ctx.nativeStrHelpers.get("__str_concat");
  const toStrIdx = ctx.funcMap.get("number_toString");
  const fromExternIdx = ctx.nativeStrHelpers.get("__str_from_extern");
  if (concatIdx === void 0) return null;
  if (expr.head.text) {
    compileStringLiteral(ctx, fctx, expr.head.text, expr.head);
  }
  for (let i = 0; i < expr.templateSpans.length; i++) {
    const span = expr.templateSpans[i];
    const spanType = compileExpression(ctx, fctx, span.expression);
    if (spanType && spanType.kind === "f64" && toStrIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: toStrIdx });
      if (fromExternIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: fromExternIdx });
      }
    } else if (spanType && spanType.kind === "i32" && toStrIdx !== void 0) {
      fctx.body.push({ op: "f64.convert_i32_s" });
      fctx.body.push({ op: "call", funcIdx: toStrIdx });
      if (fromExternIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: fromExternIdx });
      }
    } else if (spanType && spanType.kind === "i64" && toStrIdx !== void 0) {
      fctx.body.push({ op: "f64.convert_i64_s" });
      fctx.body.push({ op: "call", funcIdx: toStrIdx });
      if (fromExternIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: fromExternIdx });
      }
    } else if (spanType && (spanType.kind === "ref" || spanType.kind === "ref_null") && toStrIdx !== void 0) {
      coerceType2(ctx, fctx, spanType, { kind: "externref" }, "string");
      if (fromExternIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: fromExternIdx });
      }
    }
    if (i === 0 && !expr.head.text) {
    } else {
      fctx.body.push({ op: "call", funcIdx: concatIdx });
    }
    if (span.literal.text) {
      compileStringLiteral(ctx, fctx, span.literal.text, span.literal);
      fctx.body.push({ op: "call", funcIdx: concatIdx });
    }
  }
  return nativeStringType(ctx);
}
function compileTaggedTemplateExpression(ctx, fctx, expr) {
  const stringParts = [];
  const rawParts = [];
  const substitutions = [];
  if (ts5.isNoSubstitutionTemplateLiteral(expr.template)) {
    stringParts.push(expr.template.text);
    rawParts.push(expr.template.rawText ?? expr.template.text);
  } else {
    const tmpl = expr.template;
    stringParts.push(tmpl.head.text);
    rawParts.push(tmpl.head.rawText ?? tmpl.head.text);
    for (const span of tmpl.templateSpans) {
      substitutions.push(span.expression);
      stringParts.push(span.literal.text);
      rawParts.push(span.literal.rawText ?? span.literal.text);
    }
  }
  const elemKind = "externref";
  const elemWasm = { kind: "externref" };
  const baseVecTypeIdx = getOrRegisterVecType(ctx, elemKind, elemWasm);
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, baseVecTypeIdx);
  if (arrTypeIdx < 0) {
    ctx.errors.push({
      message: "Tagged template: invalid vec type for strings array",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const templateVecTypeIdx = getOrRegisterTemplateVecType(ctx);
  const cacheId = ctx.templateCacheCounter++;
  const cacheGlobalType = { kind: "ref_null", typeIdx: templateVecTypeIdx };
  const cacheGlobalIdx = nextModuleGlobalIdx(ctx);
  ctx.mod.globals.push({
    name: `__tt_cache_${cacheId}`,
    type: cacheGlobalType,
    mutable: true,
    init: [{ op: "ref.null", typeIdx: templateVecTypeIdx }],
  });
  const stringsVecType = { kind: "ref_null", typeIdx: templateVecTypeIdx };
  const stringsLocal = allocLocal(fctx, `__tt_strings_${fctx.locals.length}`, stringsVecType);
  const savedBody = pushBody(fctx);
  for (const raw of rawParts) {
    compileStringLiteral(ctx, fctx, raw, expr);
  }
  fctx.body.push({ op: "array.new_fixed", typeIdx: arrTypeIdx, length: rawParts.length });
  const tmpRawData = allocLocal(fctx, `__tt_raw_data_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: tmpRawData });
  fctx.body.push({ op: "i32.const", value: rawParts.length });
  fctx.body.push({ op: "local.get", index: tmpRawData });
  fctx.body.push({ op: "struct.new", typeIdx: baseVecTypeIdx });
  const tmpRawVec = allocLocal(fctx, `__tt_raw_vec_${fctx.locals.length}`, { kind: "ref", typeIdx: baseVecTypeIdx });
  fctx.body.push({ op: "local.set", index: tmpRawVec });
  for (const str of stringParts) {
    compileStringLiteral(ctx, fctx, str, expr);
  }
  fctx.body.push({ op: "array.new_fixed", typeIdx: arrTypeIdx, length: stringParts.length });
  const tmpData = allocLocal(fctx, `__tt_arr_data_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: tmpData });
  fctx.body.push({ op: "i32.const", value: stringParts.length });
  fctx.body.push({ op: "local.get", index: tmpData });
  fctx.body.push({ op: "local.get", index: tmpRawVec });
  fctx.body.push({ op: "struct.new", typeIdx: templateVecTypeIdx });
  fctx.body.push({ op: "global.set", index: cacheGlobalIdx });
  const thenBody = fctx.body;
  fctx.body = savedBody;
  fctx.body.push({ op: "global.get", index: cacheGlobalIdx });
  fctx.body.push({ op: "ref.is_null" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: thenBody,
  });
  fctx.body.push({ op: "global.get", index: cacheGlobalIdx });
  fctx.body.push({ op: "local.set", index: stringsLocal });
  if (ts5.isIdentifier(expr.tag)) {
    const tagName = expr.tag.text;
    const closureInfo = ctx.closureMap.get(tagName);
    if (closureInfo) {
      const localIdx = fctx.localMap.get(tagName);
      if (localIdx === void 0) {
        ctx.errors.push({
          message: `Tagged template: closure variable '${tagName}' not found`,
          line: getLine(expr),
          column: getCol(expr),
        });
        return null;
      }
      fctx.body.push({ op: "local.get", index: localIdx });
      const paramType0 = closureInfo.paramTypes[0];
      fctx.body.push({ op: "local.get", index: stringsLocal });
      if (paramType0 && paramType0.kind === "externref") {
        fctx.body.push({ op: "extern.convert_any" });
      }
      const closureMaxSubs = Math.min(substitutions.length, closureInfo.paramTypes.length - 1);
      for (let i = 0; i < closureMaxSubs; i++) {
        const expectedParamType = closureInfo.paramTypes[i + 1];
        compileExpression(ctx, fctx, substitutions[i], expectedParamType);
      }
      fctx.body.push({ op: "local.get", index: localIdx });
      fctx.body.push({ op: "struct.get", typeIdx: closureInfo.structTypeIdx, fieldIdx: 0 });
      emitGuardedFuncRefCast2(fctx, closureInfo.funcTypeIdx);
      fctx.body.push({ op: "ref.as_non_null" });
      fctx.body.push({ op: "call_ref", typeIdx: closureInfo.funcTypeIdx });
      return closureInfo.returnType ?? null;
    }
    const funcIdx = ctx.funcMap.get(tagName);
    if (funcIdx !== void 0) {
      const nestedCaptures = ctx.nestedFuncCaptures.get(tagName);
      if (nestedCaptures) {
        for (const cap of nestedCaptures) {
          fctx.body.push({ op: "local.get", index: cap.outerLocalIdx });
        }
      }
      const restInfo = ctx.funcRestParams.get(tagName);
      const paramTypes = getFuncParamTypes(ctx, funcIdx);
      fctx.body.push({ op: "local.get", index: stringsLocal });
      if (paramTypes?.[0] && paramTypes[0].kind === "externref") {
        fctx.body.push({ op: "extern.convert_any" });
      }
      if (restInfo) {
        const captureCount = nestedCaptures ? nestedCaptures.length : 0;
        const restIdx = restInfo.restIndex - captureCount;
        for (let i = 0; i < Math.min(substitutions.length, restIdx - 1); i++) {
          compileExpression(ctx, fctx, substitutions[i], paramTypes?.[i + 1 + captureCount]);
        }
        const restStart = Math.max(0, restIdx - 1);
        const restSubs = substitutions.slice(restStart);
        const restArgCount = restSubs.length;
        fctx.body.push({ op: "i32.const", value: restArgCount });
        for (const sub2 of restSubs) {
          compileExpression(ctx, fctx, sub2, restInfo.elemType);
        }
        fctx.body.push({ op: "array.new_fixed", typeIdx: restInfo.arrayTypeIdx, length: restArgCount });
        fctx.body.push({ op: "struct.new", typeIdx: restInfo.vecTypeIdx });
      } else {
        const captureCount = nestedCaptures ? nestedCaptures.length : 0;
        const maxSubs = paramTypes
          ? Math.min(substitutions.length, paramTypes.length - 1 - captureCount)
          : substitutions.length;
        for (let i = 0; i < maxSubs; i++) {
          compileExpression(ctx, fctx, substitutions[i], paramTypes?.[i + 1 + captureCount]);
        }
        const optInfo = ctx.funcOptionalParams.get(tagName);
        if (optInfo) {
          const numProvided = maxSubs + 1 + captureCount;
          for (const opt of optInfo) {
            if (opt.index >= numProvided) {
              pushParamSentinel(fctx, opt.type, ctx, opt);
            }
          }
        }
      }
      const finalFuncIdx = ctx.funcMap.get(tagName) ?? funcIdx;
      fctx.body.push({ op: "call", funcIdx: finalFuncIdx });
      const sig = ctx.checker.getResolvedSignature(expr);
      if (sig) {
        const retType = ctx.checker.getReturnTypeOfSignature(sig);
        if (isVoidType(retType)) return null;
        return resolveWasmType(ctx, retType);
      }
      return { kind: "externref" };
    }
  }
  {
    const tagTsType = ctx.checker.getTypeAtLocation(expr.tag);
    const callSigs = tagTsType.getCallSignatures?.();
    let matchedClosureInfo;
    let matchedStructTypeIdx;
    if (callSigs && callSigs.length > 0) {
      const sig = callSigs[0];
      const sigParamCount = sig.parameters.length;
      const sigRetType = ctx.checker.getReturnTypeOfSignature(sig);
      const sigRetWasm = isVoidType(sigRetType) ? null : resolveWasmType(ctx, sigRetType);
      const sigParamWasmTypes = [];
      for (let i = 0; i < sigParamCount; i++) {
        const paramType = ctx.checker.getTypeOfSymbol(sig.parameters[i]);
        sigParamWasmTypes.push(resolveWasmType(ctx, paramType));
      }
      for (const [typeIdx, info] of ctx.closureInfoByTypeIdx) {
        if (info.paramTypes.length !== sigParamCount) continue;
        if (sigRetWasm === null && info.returnType !== null) continue;
        if (sigRetWasm !== null && info.returnType === null) continue;
        if (sigRetWasm !== null && info.returnType !== null && sigRetWasm.kind !== info.returnType.kind) continue;
        let paramsMatch = true;
        for (let i = 0; i < sigParamCount; i++) {
          if (sigParamWasmTypes[i].kind !== info.paramTypes[i].kind) {
            paramsMatch = false;
            break;
          }
        }
        if (paramsMatch) {
          matchedClosureInfo = info;
          matchedStructTypeIdx = typeIdx;
          break;
        }
      }
    }
    if (matchedClosureInfo && matchedStructTypeIdx !== void 0) {
      const tagResult = compileExpression(ctx, fctx, expr.tag);
      let closureLocal;
      if (tagResult?.kind === "externref") {
        const closureRefType = { kind: "ref_null", typeIdx: matchedStructTypeIdx };
        closureLocal = allocLocal(fctx, `__tt_tag_${fctx.locals.length}`, closureRefType);
        fctx.body.push({ op: "any.convert_extern" });
        emitGuardedRefCast(fctx, matchedStructTypeIdx);
        fctx.body.push({ op: "local.set", index: closureLocal });
      } else {
        const closureRefType = tagResult ?? { kind: "ref", typeIdx: matchedStructTypeIdx };
        closureLocal = allocLocal(fctx, `__tt_tag_${fctx.locals.length}`, closureRefType);
        fctx.body.push({ op: "local.set", index: closureLocal });
      }
      fctx.body.push({ op: "local.get", index: closureLocal });
      fctx.body.push({ op: "ref.as_non_null" });
      fctx.body.push({ op: "local.get", index: stringsLocal });
      if (matchedClosureInfo.paramTypes[0] && matchedClosureInfo.paramTypes[0].kind === "externref") {
        fctx.body.push({ op: "extern.convert_any" });
      }
      const closureMaxSubs = Math.min(substitutions.length, matchedClosureInfo.paramTypes.length - 1);
      for (let i = 0; i < closureMaxSubs; i++) {
        const expectedParamType = matchedClosureInfo.paramTypes[i + 1];
        compileExpression(ctx, fctx, substitutions[i], expectedParamType);
      }
      for (let i = substitutions.length + 1; i < matchedClosureInfo.paramTypes.length; i++) {
        pushDefaultValue(fctx, matchedClosureInfo.paramTypes[i], ctx);
      }
      fctx.body.push({ op: "local.get", index: closureLocal });
      fctx.body.push({ op: "ref.as_non_null" });
      fctx.body.push({ op: "struct.get", typeIdx: matchedStructTypeIdx, fieldIdx: 0 });
      emitGuardedFuncRefCast2(fctx, matchedClosureInfo.funcTypeIdx);
      fctx.body.push({ op: "ref.as_non_null" });
      fctx.body.push({ op: "call_ref", typeIdx: matchedClosureInfo.funcTypeIdx });
      return matchedClosureInfo.returnType ?? null;
    }
    {
      const tagResult = compileExpression(ctx, fctx, expr.tag);
      if (tagResult && (tagResult.kind === "ref" || tagResult.kind === "ref_null")) {
        const closureTypeIdx = tagResult.typeIdx;
        const closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
        if (closureInfo) {
          const closureLocal = allocLocal(fctx, `__tt_tag_${fctx.locals.length}`, tagResult);
          fctx.body.push({ op: "local.set", index: closureLocal });
          fctx.body.push({ op: "local.get", index: closureLocal });
          fctx.body.push({ op: "local.get", index: stringsLocal });
          if (closureInfo.paramTypes[0] && closureInfo.paramTypes[0].kind === "externref") {
            fctx.body.push({ op: "extern.convert_any" });
          }
          const closureMaxSubs = Math.min(substitutions.length, closureInfo.paramTypes.length - 1);
          for (let i = 0; i < closureMaxSubs; i++) {
            const expectedParamType = closureInfo.paramTypes[i + 1];
            compileExpression(ctx, fctx, substitutions[i], expectedParamType);
          }
          for (let i = substitutions.length + 1; i < closureInfo.paramTypes.length; i++) {
            pushDefaultValue(fctx, closureInfo.paramTypes[i], ctx);
          }
          fctx.body.push({ op: "local.get", index: closureLocal });
          fctx.body.push({ op: "struct.get", typeIdx: closureInfo.structTypeIdx, fieldIdx: 0 });
          emitGuardedFuncRefCast2(fctx, closureInfo.funcTypeIdx);
          fctx.body.push({ op: "ref.as_non_null" });
          fctx.body.push({ op: "call_ref", typeIdx: closureInfo.funcTypeIdx });
          return closureInfo.returnType ?? null;
        }
      }
      if (tagResult) {
        if (tagResult.kind !== "externref") {
          coerceType2(ctx, fctx, tagResult, { kind: "externref" });
        }
        const tagLocal = allocLocal(fctx, `__tt_dyn_tag_${fctx.locals.length}`, { kind: "externref" });
        fctx.body.push({ op: "local.set", index: tagLocal });
        const ext = { kind: "externref" };
        ensureLateImport(ctx, "__tagged_template", [ext, ext, ext], [ext]);
        ensureLateImport(ctx, "__js_array_new", [], [ext]);
        ensureLateImport(ctx, "__js_array_push", [ext, ext], []);
        flushLateImportShifts(ctx, fctx);
        const ttIdx = ctx.funcMap.get("__tagged_template");
        const arrNewIdx = ctx.funcMap.get("__js_array_new");
        const arrPushIdx = ctx.funcMap.get("__js_array_push");
        fctx.body.push({ op: "call", funcIdx: arrNewIdx });
        const subsArrLocal = allocLocal(fctx, `__tt_subs_arr_${fctx.locals.length}`, ext);
        fctx.body.push({ op: "local.set", index: subsArrLocal });
        for (const sub2 of substitutions) {
          fctx.body.push({ op: "local.get", index: subsArrLocal });
          const subResult = compileExpression(ctx, fctx, sub2);
          if (subResult && subResult.kind !== "externref") {
            coerceType2(ctx, fctx, subResult, ext);
          }
          fctx.body.push({ op: "call", funcIdx: arrPushIdx });
        }
        fctx.body.push({ op: "local.get", index: tagLocal });
        fctx.body.push({ op: "local.get", index: stringsLocal });
        fctx.body.push({ op: "extern.convert_any" });
        fctx.body.push({ op: "local.get", index: subsArrLocal });
        fctx.body.push({ op: "call", funcIdx: ttIdx });
        return { kind: "externref" };
      }
    }
  }
  ctx.errors.push({
    message: `Tagged template: unsupported tag expression kind ${ts5.SyntaxKind[expr.tag.kind]}`,
    line: getLine(expr),
    column: getCol(expr),
  });
  fctx.body.push({ op: "ref.null.extern" });
  return { kind: "externref" };
}
function emitBoolToString(ctx, fctx) {
  addStringConstantGlobal(ctx, "true");
  addStringConstantGlobal(ctx, "false");
  const trueIdx = ctx.stringGlobalMap.get("true");
  const falseIdx = ctx.stringGlobalMap.get("false");
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: { kind: "externref" } },
    then: [{ op: "global.get", index: trueIdx }],
    else: [{ op: "global.get", index: falseIdx }],
  });
}
function compileStringBinaryOp(ctx, fctx, expr, op) {
  if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
    const strFlattenIdx = ctx.nativeStrHelpers.get("__str_flatten");
    switch (op) {
      case ts5.SyntaxKind.PlusToken: {
        compileExpression(ctx, fctx, expr.left);
        compileExpression(ctx, fctx, expr.right);
        const funcIdx = ctx.nativeStrHelpers.get("__str_concat");
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          return nativeStringType(ctx);
        }
        break;
      }
      case ts5.SyntaxKind.EqualsEqualsEqualsToken:
      case ts5.SyntaxKind.EqualsEqualsToken: {
        compileExpression(ctx, fctx, expr.left);
        fctx.body.push({ op: "call", funcIdx: strFlattenIdx });
        compileExpression(ctx, fctx, expr.right);
        fctx.body.push({ op: "call", funcIdx: strFlattenIdx });
        const funcIdx = ctx.nativeStrHelpers.get("__str_equals");
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          return { kind: "i32" };
        }
        break;
      }
      case ts5.SyntaxKind.ExclamationEqualsEqualsToken:
      case ts5.SyntaxKind.ExclamationEqualsToken: {
        compileExpression(ctx, fctx, expr.left);
        fctx.body.push({ op: "call", funcIdx: strFlattenIdx });
        compileExpression(ctx, fctx, expr.right);
        fctx.body.push({ op: "call", funcIdx: strFlattenIdx });
        const funcIdx = ctx.nativeStrHelpers.get("__str_equals");
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          fctx.body.push({ op: "i32.eqz" });
          return { kind: "i32" };
        }
        break;
      }
      case ts5.SyntaxKind.LessThanToken:
      case ts5.SyntaxKind.LessThanEqualsToken:
      case ts5.SyntaxKind.GreaterThanToken:
      case ts5.SyntaxKind.GreaterThanEqualsToken: {
        compileExpression(ctx, fctx, expr.left);
        compileExpression(ctx, fctx, expr.right);
        const funcIdx = ctx.nativeStrHelpers.get("__str_compare");
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          fctx.body.push({ op: "i32.const", value: 0 });
          const cmpOp =
            op === ts5.SyntaxKind.LessThanToken
              ? "i32.lt_s"
              : op === ts5.SyntaxKind.LessThanEqualsToken
                ? "i32.le_s"
                : op === ts5.SyntaxKind.GreaterThanToken
                  ? "i32.gt_s"
                  : "i32.ge_s";
          fctx.body.push({ op: cmpOp });
          return { kind: "i32" };
        }
        break;
      }
      default: {
        const leftType2 = compileExpression(ctx, fctx, expr.left);
        if (leftType2 && leftType2.kind === "f64") {
        } else if (leftType2 && leftType2.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
        } else if (leftType2 && (leftType2.kind === "ref" || leftType2.kind === "ref_null")) {
          fctx.body.push({ op: "extern.convert_any" });
          const pfIdx1 = ctx.funcMap.get("parseFloat");
          if (pfIdx1 !== void 0) {
            fctx.body.push({ op: "call", funcIdx: pfIdx1 });
          } else {
            addUnionImports(ctx);
            fctx.body.push({ op: "call", funcIdx: ctx.funcMap.get("__unbox_number") });
          }
        } else {
          const pfIdx1 = ctx.funcMap.get("parseFloat");
          if (pfIdx1 !== void 0) {
            fctx.body.push({ op: "call", funcIdx: pfIdx1 });
          } else {
            addUnionImports(ctx);
            fctx.body.push({ op: "call", funcIdx: ctx.funcMap.get("__unbox_number") });
          }
        }
        const rightType2 = compileExpression(ctx, fctx, expr.right);
        if (rightType2 && rightType2.kind === "f64") {
        } else if (rightType2 && rightType2.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
        } else if (rightType2 && (rightType2.kind === "ref" || rightType2.kind === "ref_null")) {
          fctx.body.push({ op: "extern.convert_any" });
          const pfIdx2 = ctx.funcMap.get("parseFloat");
          if (pfIdx2 !== void 0) {
            fctx.body.push({ op: "call", funcIdx: pfIdx2 });
          } else {
            addUnionImports(ctx);
            fctx.body.push({ op: "call", funcIdx: ctx.funcMap.get("__unbox_number") });
          }
        } else {
          const pfIdx2 = ctx.funcMap.get("parseFloat");
          if (pfIdx2 !== void 0) {
            fctx.body.push({ op: "call", funcIdx: pfIdx2 });
          } else {
            addUnionImports(ctx);
            fctx.body.push({ op: "call", funcIdx: ctx.funcMap.get("__unbox_number") });
          }
        }
        return compileNumericBinaryOp(ctx, fctx, op, expr);
      }
    }
    ctx.errors.push({
      message: `Unsupported string operator: ${ts5.SyntaxKind[op]}`,
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  addStringImports(ctx);
  const isArithmeticOrBitwise =
    op === ts5.SyntaxKind.MinusToken ||
    op === ts5.SyntaxKind.AsteriskToken ||
    op === ts5.SyntaxKind.AsteriskAsteriskToken ||
    op === ts5.SyntaxKind.SlashToken ||
    op === ts5.SyntaxKind.PercentToken ||
    op === ts5.SyntaxKind.AmpersandToken ||
    op === ts5.SyntaxKind.BarToken ||
    op === ts5.SyntaxKind.CaretToken ||
    op === ts5.SyntaxKind.LessThanLessThanToken ||
    op === ts5.SyntaxKind.GreaterThanGreaterThanToken ||
    op === ts5.SyntaxKind.GreaterThanGreaterThanGreaterThanToken;
  if (isArithmeticOrBitwise) {
    const leftArithType = compileExpression(ctx, fctx, expr.left);
    if (leftArithType && leftArithType.kind === "f64") {
    } else if (leftArithType && leftArithType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
    } else {
      const pfIdx = ctx.funcMap.get("parseFloat");
      if (pfIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: pfIdx });
      } else {
        addUnionImports(ctx);
        fctx.body.push({ op: "call", funcIdx: ctx.funcMap.get("__unbox_number") });
      }
    }
    const rightArithType = compileExpression(ctx, fctx, expr.right);
    if (rightArithType && rightArithType.kind === "f64") {
    } else if (rightArithType && rightArithType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
    } else {
      const pfIdx2 = ctx.funcMap.get("parseFloat");
      if (pfIdx2 !== void 0) {
        fctx.body.push({ op: "call", funcIdx: pfIdx2 });
      } else {
        addUnionImports(ctx);
        fctx.body.push({ op: "call", funcIdx: ctx.funcMap.get("__unbox_number") });
      }
    }
    return compileNumericBinaryOp(ctx, fctx, op, expr);
  }
  const leftTsType = ctx.checker.getTypeAtLocation(expr.left);
  const leftType = compileExpression(ctx, fctx, expr.left);
  if (op === ts5.SyntaxKind.PlusToken && !leftType) {
    addStringConstantGlobal(ctx, "undefined");
    const undefGIdx = ctx.stringGlobalMap.get("undefined");
    fctx.body.push({ op: "global.get", index: undefGIdx });
  } else if (
    op === ts5.SyntaxKind.PlusToken &&
    leftType &&
    (leftType.kind === "f64" || leftType.kind === "i32" || leftType.kind === "i64")
  ) {
    if (isBooleanType(leftTsType) && leftType.kind === "i32") {
      emitBoolToString(ctx, fctx);
    } else {
      if (leftType.kind === "i32") fctx.body.push({ op: "f64.convert_i32_s" });
      else if (leftType.kind === "i64") fctx.body.push({ op: "f64.convert_i64_s" });
      const toStr = ctx.funcMap.get("number_toString");
      if (toStr !== void 0) fctx.body.push({ op: "call", funcIdx: toStr });
    }
  } else if (op === ts5.SyntaxKind.PlusToken && leftType && leftType.kind === "externref") {
    const leftIsNull = (leftTsType.flags & ts5.TypeFlags.Null) !== 0;
    const leftIsUndef = (leftTsType.flags & (ts5.TypeFlags.Undefined | ts5.TypeFlags.Void)) !== 0;
    if (leftIsNull) {
      fctx.body.push({ op: "drop" });
      addStringConstantGlobal(ctx, "null");
      fctx.body.push({ op: "global.get", index: ctx.stringGlobalMap.get("null") });
    } else if (leftIsUndef) {
      fctx.body.push({ op: "drop" });
      addStringConstantGlobal(ctx, "undefined");
      fctx.body.push({ op: "global.get", index: ctx.stringGlobalMap.get("undefined") });
    }
  } else if (op === ts5.SyntaxKind.PlusToken && leftType && (leftType.kind === "ref" || leftType.kind === "ref_null")) {
    fctx.body.push({ op: "extern.convert_any" });
  }
  const rightTsType = ctx.checker.getTypeAtLocation(expr.right);
  const rightType = compileExpression(ctx, fctx, expr.right);
  if (op === ts5.SyntaxKind.PlusToken && !rightType) {
    addStringConstantGlobal(ctx, "undefined");
    const undefGIdx = ctx.stringGlobalMap.get("undefined");
    fctx.body.push({ op: "global.get", index: undefGIdx });
  } else if (
    op === ts5.SyntaxKind.PlusToken &&
    rightType &&
    (rightType.kind === "f64" || rightType.kind === "i32" || rightType.kind === "i64")
  ) {
    if (isBooleanType(rightTsType) && rightType.kind === "i32") {
      emitBoolToString(ctx, fctx);
    } else {
      if (rightType.kind === "i32") fctx.body.push({ op: "f64.convert_i32_s" });
      else if (rightType.kind === "i64") fctx.body.push({ op: "f64.convert_i64_s" });
      const toStr = ctx.funcMap.get("number_toString");
      if (toStr !== void 0) fctx.body.push({ op: "call", funcIdx: toStr });
    }
  } else if (op === ts5.SyntaxKind.PlusToken && rightType && rightType.kind === "externref") {
    const rightIsNull = (rightTsType.flags & ts5.TypeFlags.Null) !== 0;
    const rightIsUndef = (rightTsType.flags & (ts5.TypeFlags.Undefined | ts5.TypeFlags.Void)) !== 0;
    if (rightIsNull) {
      fctx.body.push({ op: "drop" });
      addStringConstantGlobal(ctx, "null");
      fctx.body.push({ op: "global.get", index: ctx.stringGlobalMap.get("null") });
    } else if (rightIsUndef) {
      fctx.body.push({ op: "drop" });
      addStringConstantGlobal(ctx, "undefined");
      fctx.body.push({ op: "global.get", index: ctx.stringGlobalMap.get("undefined") });
    }
  } else if (
    op === ts5.SyntaxKind.PlusToken &&
    rightType &&
    (rightType.kind === "ref" || rightType.kind === "ref_null")
  ) {
    fctx.body.push({ op: "extern.convert_any" });
  }
  switch (op) {
    case ts5.SyntaxKind.PlusToken: {
      const funcIdx = ctx.funcMap.get("concat");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "externref" };
      }
      break;
    }
    case ts5.SyntaxKind.EqualsEqualsEqualsToken:
    case ts5.SyntaxKind.EqualsEqualsToken: {
      const funcIdx = ctx.funcMap.get("equals");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "i32" };
      }
      break;
    }
    case ts5.SyntaxKind.ExclamationEqualsEqualsToken:
    case ts5.SyntaxKind.ExclamationEqualsToken: {
      const funcIdx = ctx.funcMap.get("equals");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        fctx.body.push({ op: "i32.eqz" });
        return { kind: "i32" };
      }
      break;
    }
    case ts5.SyntaxKind.LessThanToken:
    case ts5.SyntaxKind.LessThanEqualsToken:
    case ts5.SyntaxKind.GreaterThanToken:
    case ts5.SyntaxKind.GreaterThanEqualsToken: {
      const funcIdx = ctx.funcMap.get("string_compare");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        fctx.body.push({ op: "i32.const", value: 0 });
        const cmpOp =
          op === ts5.SyntaxKind.LessThanToken
            ? "i32.lt_s"
            : op === ts5.SyntaxKind.LessThanEqualsToken
              ? "i32.le_s"
              : op === ts5.SyntaxKind.GreaterThanToken
                ? "i32.gt_s"
                : "i32.ge_s";
        fctx.body.push({ op: cmpOp });
        return { kind: "i32" };
      }
      break;
    }
  }
  ctx.errors.push({
    message: `Unsupported string operator: ${ts5.SyntaxKind[op]}`,
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}
function compileNativeStringMethodCall(ctx, fctx, expr, propAccess, method) {
  const strTypeIdx = ctx.nativeStrTypeIdx;
  const strDataTypeIdx = ctx.nativeStrDataTypeIdx;
  const flattenIdx = ctx.nativeStrHelpers.get("__str_flatten");
  const emitFlatten = () => fctx.body.push({ op: "call", funcIdx: flattenIdx });
  if (method === "charCodeAt") {
    compileExpression(ctx, fctx, propAccess.expression);
    const flattenIdx2 = ctx.nativeStrHelpers.get("__str_flatten");
    fctx.body.push({ op: "call", funcIdx: flattenIdx2 });
    const tmpLocal = allocLocal(fctx, "__charCodeAt_tmp", flatStringType(ctx));
    fctx.body.push({ op: "local.set", index: tmpLocal });
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 });
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 });
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    fctx.body.push({ op: "i32.add" });
    fctx.body.push({ op: "array.get_u", typeIdx: strDataTypeIdx });
    return { kind: "i32" };
  }
  if (method === "charAt") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_charAt");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "at") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    const strTmp = allocLocal(fctx, `__str_at_tmp_${fctx.locals.length}`, flatStringType(ctx));
    fctx.body.push({ op: "local.tee", index: strTmp });
    fctx.body.push({ op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 });
    const lenTmp = allocLocal(fctx, `__str_at_len_${fctx.locals.length}`, { kind: "i32" });
    fctx.body.push({ op: "local.set", index: lenTmp });
    const idxTmp = allocLocal(fctx, `__str_at_idx_${fctx.locals.length}`, { kind: "i32" });
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    fctx.body.push({ op: "local.set", index: idxTmp });
    fctx.body.push({ op: "local.get", index: idxTmp });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "i32.lt_s" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: idxTmp },
        { op: "local.get", index: lenTmp },
        { op: "i32.add" },
        { op: "local.set", index: idxTmp },
      ],
    });
    fctx.body.push({ op: "local.get", index: strTmp });
    fctx.body.push({ op: "local.get", index: idxTmp });
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_charAt");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "substring") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    if (expr.arguments.length > 1) {
      const argType = compileExpression(ctx, fctx, expr.arguments[1], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 2147483647 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 2147483647 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_substring");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "slice") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    if (expr.arguments.length > 1) {
      const argType = compileExpression(ctx, fctx, expr.arguments[1], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 2147483647 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 2147483647 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_slice");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "indexOf") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      compileExpression(ctx, fctx, expr.arguments[0]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "ref.null", typeIdx: strTypeIdx });
    }
    if (expr.arguments.length > 1) {
      const argType = compileExpression(ctx, fctx, expr.arguments[1], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_indexOf");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return { kind: "i32" };
  }
  if (method === "lastIndexOf") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      compileExpression(ctx, fctx, expr.arguments[0]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "ref.null", typeIdx: strTypeIdx });
    }
    if (expr.arguments.length > 1) {
      const argType = compileExpression(ctx, fctx, expr.arguments[1], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 2147483647 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 2147483647 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_lastIndexOf");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return { kind: "i32" };
  }
  if (method === "includes") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      compileExpression(ctx, fctx, expr.arguments[0]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "ref.null", typeIdx: strTypeIdx });
    }
    if (expr.arguments.length > 1) {
      const argType = compileExpression(ctx, fctx, expr.arguments[1]);
      if (argType && argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_includes");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return { kind: "i32" };
  }
  if (method === "startsWith") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      compileExpression(ctx, fctx, expr.arguments[0]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "ref.null", typeIdx: strTypeIdx });
    }
    if (expr.arguments.length > 1) {
      const argType = compileExpression(ctx, fctx, expr.arguments[1]);
      if (argType && argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_startsWith");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return { kind: "i32" };
  }
  if (method === "endsWith") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      compileExpression(ctx, fctx, expr.arguments[0]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "ref.null", typeIdx: strTypeIdx });
    }
    if (expr.arguments.length > 1) {
      const argType = compileExpression(ctx, fctx, expr.arguments[1]);
      if (argType && argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 2147483647 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_endsWith");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return { kind: "i32" };
  }
  if (method === "trim" || method === "trimStart" || method === "trimEnd") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    const helperName = `__str_${method}`;
    const funcIdx2 = ctx.nativeStrHelpers.get(helperName);
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "repeat") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0]);
      if (argType && argType.kind === "f64") {
        const countLocal = allocLocal(fctx, `__repeat_count_${fctx.locals.length}`, { kind: "f64" });
        fctx.body.push({ op: "local.tee", index: countLocal });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.lt" });
        fctx.body.push({ op: "local.get", index: countLocal });
        fctx.body.push({ op: "f64.const", value: Infinity });
        fctx.body.push({ op: "f64.eq" });
        fctx.body.push({ op: "i32.or" });
        {
          const rangeErrMsg = "RangeError: Invalid count value";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "global.get", index: strIdx },
              { op: "throw", tagIdx },
            ],
            else: [],
          });
        }
        fctx.body.push({ op: "local.get", index: countLocal });
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_repeat");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "padStart") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0]);
      if (argType && argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    if (expr.arguments.length > 1) {
      compileExpression(ctx, fctx, expr.arguments[1]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "i32.const", value: 1 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 32 });
      fctx.body.push({ op: "array.new_fixed", typeIdx: ctx.nativeStrDataTypeIdx, length: 1 });
      fctx.body.push({ op: "struct.new", typeIdx: ctx.nativeStrTypeIdx });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_padStart");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "padEnd") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0]);
      if (argType && argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    if (expr.arguments.length > 1) {
      compileExpression(ctx, fctx, expr.arguments[1]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "i32.const", value: 1 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 32 });
      fctx.body.push({ op: "array.new_fixed", typeIdx: ctx.nativeStrDataTypeIdx, length: 1 });
      fctx.body.push({ op: "struct.new", typeIdx: ctx.nativeStrTypeIdx });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_padEnd");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "toLowerCase" || method === "toUpperCase") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    const helperName = `__str_${method}`;
    const funcIdx2 = ctx.nativeStrHelpers.get(helperName);
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  const firstArgIsRegExp =
    (method === "replace" || method === "replaceAll" || method === "split") &&
    expr.arguments.length > 0 &&
    (() => {
      const argType = ctx.checker.getTypeAtLocation(expr.arguments[0]);
      const symName = argType.getSymbol()?.getName();
      return symName === "RegExp";
    })();
  if (method === "replace" && !firstArgIsRegExp) {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      compileExpression(ctx, fctx, expr.arguments[0]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "ref.null", typeIdx: ctx.nativeStrTypeIdx });
    }
    if (expr.arguments.length > 1) {
      compileExpression(ctx, fctx, expr.arguments[1]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "array.new_default", typeIdx: ctx.nativeStrDataTypeIdx });
      fctx.body.push({ op: "struct.new", typeIdx: ctx.nativeStrTypeIdx });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_replace");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "replaceAll" && !firstArgIsRegExp) {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      compileExpression(ctx, fctx, expr.arguments[0]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "ref.null", typeIdx: ctx.nativeStrTypeIdx });
    }
    if (expr.arguments.length > 1) {
      compileExpression(ctx, fctx, expr.arguments[1]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "array.new_default", typeIdx: ctx.nativeStrDataTypeIdx });
      fctx.body.push({ op: "struct.new", typeIdx: ctx.nativeStrTypeIdx });
    }
    const funcIdx2 = ctx.nativeStrHelpers.get("__str_replaceAll");
    fctx.body.push({ op: "call", funcIdx: funcIdx2 });
    return nativeStringType(ctx);
  }
  if (method === "split" && !firstArgIsRegExp) {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    if (expr.arguments.length > 0) {
      compileExpression(ctx, fctx, expr.arguments[0]);
      emitFlatten();
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "array.new_default", typeIdx: ctx.nativeStrDataTypeIdx });
      fctx.body.push({ op: "struct.new", typeIdx: ctx.nativeStrTypeIdx });
    }
    const splitIdx = ctx.nativeStrHelpers.get("__str_split");
    fctx.body.push({ op: "call", funcIdx: splitIdx });
    const nstrVecTypeIdx = ctx.vecTypeMap.get(`ref_${ctx.anyStrTypeIdx}`);
    return { kind: "ref", typeIdx: nstrVecTypeIdx };
  }
  if (method === "codePointAt") {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    const tmpLocal = allocLocal(fctx, "__codePointAt_tmp", flatStringType(ctx));
    fctx.body.push({ op: "local.set", index: tmpLocal });
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 });
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 });
    if (expr.arguments.length > 0) {
      const argType = compileExpression(ctx, fctx, expr.arguments[0], { kind: "f64" });
      if (!argType) {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (argType.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    fctx.body.push({ op: "i32.add" });
    fctx.body.push({ op: "array.get_u", typeIdx: strDataTypeIdx });
    fctx.body.push({ op: "f64.convert_i32_u" });
    return { kind: "f64" };
  }
  if (method === "normalize") {
    if (expr.arguments.length > 0) {
      const formArg = expr.arguments[0];
      if (ts5.isStringLiteral(formArg)) {
        const form = formArg.text;
        if (form !== "NFC" && form !== "NFD" && form !== "NFKC" && form !== "NFKD") {
          const rangeErrMsg = "RangeError: The normalization form should be one of NFC, NFD, NFKC, NFKD";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({ op: "global.get", index: strIdx });
          fctx.body.push({ op: "throw", tagIdx });
          return null;
        }
      }
      const argType = compileExpression(ctx, fctx, formArg);
      if (argType) {
        fctx.body.push({ op: "drop" });
      }
    }
    return compileExpression(ctx, fctx, propAccess.expression);
  }
  const importName = `string_${method}`;
  const funcIdx = ctx.funcMap.get(importName);
  if (funcIdx !== void 0) {
    compileExpression(ctx, fctx, propAccess.expression);
    emitFlatten();
    const toExternIdx = ctx.nativeStrHelpers.get("__str_to_extern");
    fctx.body.push({ op: "call", funcIdx: toExternIdx });
    for (const arg of expr.arguments) {
      const argType = compileExpression(ctx, fctx, arg);
      if (
        argType &&
        argType.kind === "ref" &&
        (argType.typeIdx === strTypeIdx || argType.typeIdx === ctx.anyStrTypeIdx)
      ) {
        emitFlatten();
        fctx.body.push({ op: "call", funcIdx: toExternIdx });
      }
    }
    fctx.body.push({ op: "call", funcIdx });
    const returnsBool = method === "includes" || method === "startsWith" || method === "endsWith";
    const returnsNum = method === "indexOf" || method === "lastIndexOf" || method === "search";
    const returnsExternRef = method === "match";
    if (returnsBool) {
      return { kind: "i32" };
    } else if (returnsNum) {
      return { kind: "f64" };
    } else if (returnsExternRef) {
      return { kind: "externref" };
    } else {
      const fromExternIdx = ctx.nativeStrHelpers.get("__str_from_extern");
      fctx.body.push({ op: "call", funcIdx: fromExternIdx });
      return nativeStringType(ctx);
    }
  }
  ctx.errors.push({
    message: `Unknown string method: ${method}`,
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}

// src/codegen/binary-ops.ts
var FLATTENABLE_OPS = /* @__PURE__ */ new Set([
  ts6.SyntaxKind.PlusToken,
  ts6.SyntaxKind.MinusToken,
  ts6.SyntaxKind.AsteriskToken,
  ts6.SyntaxKind.SlashToken,
  ts6.SyntaxKind.PercentToken,
  ts6.SyntaxKind.AmpersandToken,
  ts6.SyntaxKind.BarToken,
  ts6.SyntaxKind.CaretToken,
  ts6.SyntaxKind.LessThanLessThanToken,
  ts6.SyntaxKind.GreaterThanGreaterThanToken,
  ts6.SyntaxKind.GreaterThanGreaterThanGreaterThanToken,
]);
function tryFlattenBinaryChain(ctx, fctx, expr, op) {
  if (!FLATTENABLE_OPS.has(op)) return null;
  if (!ts6.isBinaryExpression(expr.left) || expr.left.operatorToken.kind !== op) {
    return null;
  }
  const operands = [];
  let node = expr;
  while (ts6.isBinaryExpression(node) && node.operatorToken.kind === op) {
    operands.push(node.right);
    node = node.left;
  }
  operands.push(node);
  operands.reverse();
  for (const operand of operands) {
    const tsType = ctx.checker.getTypeAtLocation(operand);
    if (isStringType(tsType)) return null;
    if (isBigIntType(tsType)) return null;
    if ((tsType.flags & ts6.TypeFlags.Any) !== 0) return null;
  }
  const isDivOrPow = op === ts6.SyntaxKind.SlashToken || op === ts6.SyntaxKind.AsteriskAsteriskToken;
  let allNativeI32 = !isDivOrPow;
  if (allNativeI32 && !ctx.fast) {
    for (const operand of operands) {
      const tsType = ctx.checker.getTypeAtLocation(operand);
      const native = resolveNativeTypeAnnotation(tsType);
      if (native?.kind !== "i32") {
        allNativeI32 = false;
        break;
      }
    }
  }
  const numericHint = { kind: (ctx.fast || allNativeI32) && !isDivOrPow ? "i32" : "f64" };
  let resultType = compileExpression(ctx, fctx, operands[0], numericHint);
  if (!resultType) return null;
  for (let i = 1; i < operands.length; i++) {
    let rightType = compileExpression(ctx, fctx, operands[i], numericHint);
    if (!rightType) return null;
    const leftIsRef = resultType.kind === "externref" || resultType.kind === "ref" || resultType.kind === "ref_null";
    const rightIsRef = rightType.kind === "externref" || rightType.kind === "ref" || rightType.kind === "ref_null";
    if (leftIsRef || rightIsRef) {
      if (rightIsRef) {
        const tmpR = allocTempLocal(fctx, rightType);
        fctx.body.push({ op: "local.set", index: tmpR });
        if (leftIsRef) {
          coerceType3(ctx, fctx, resultType, { kind: "f64" });
        }
        fctx.body.push({ op: "local.get", index: tmpR });
        coerceType3(ctx, fctx, rightType, { kind: "f64" });
        releaseTempLocal(fctx, tmpR);
      } else {
        const tmpR = allocTempLocal(fctx, rightType);
        fctx.body.push({ op: "local.set", index: tmpR });
        coerceType3(ctx, fctx, resultType, { kind: "f64" });
        fctx.body.push({ op: "local.get", index: tmpR });
        releaseTempLocal(fctx, tmpR);
      }
      resultType = { kind: "f64" };
      rightType = { kind: "f64" };
    }
    if (resultType.kind === "i32" && rightType.kind === "f64") {
      const tmpR = allocTempLocal(fctx, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: tmpR });
      fctx.body.push({ op: "f64.convert_i32_s" });
      fctx.body.push({ op: "local.get", index: tmpR });
      releaseTempLocal(fctx, tmpR);
      resultType = { kind: "f64" };
      rightType = { kind: "f64" };
    } else if (resultType.kind === "f64" && rightType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
      rightType = { kind: "f64" };
    }
    if ((ctx.fast || allNativeI32) && resultType.kind === "i32" && rightType.kind === "i32") {
      resultType = compileI32BinaryOp(ctx, fctx, op, expr);
    } else {
      resultType = compileNumericBinaryOp(ctx, fctx, op, expr);
    }
  }
  return resultType;
}
function compileBinaryExpression(ctx, fctx, expr) {
  const op = expr.operatorToken.kind;
  if (op === ts6.SyntaxKind.EqualsToken) {
    return compileAssignment(ctx, fctx, expr);
  }
  if (
    op === ts6.SyntaxKind.QuestionQuestionEqualsToken ||
    op === ts6.SyntaxKind.BarBarEqualsToken ||
    op === ts6.SyntaxKind.AmpersandAmpersandEqualsToken
  ) {
    return compileLogicalAssignment(ctx, fctx, expr, op);
  }
  if (isCompoundAssignment(op)) {
    return compileCompoundAssignment(ctx, fctx, expr, op);
  }
  if (op === ts6.SyntaxKind.AmpersandAmpersandToken) {
    return compileLogicalAnd(ctx, fctx, expr);
  }
  if (op === ts6.SyntaxKind.BarBarToken) {
    return compileLogicalOr(ctx, fctx, expr);
  }
  if (op === ts6.SyntaxKind.QuestionQuestionToken) {
    return compileNullishCoalescing(ctx, fctx, expr);
  }
  if (op === ts6.SyntaxKind.CommaToken) {
    const leftType2 = compileExpression(ctx, fctx, expr.left);
    if (leftType2) {
      fctx.body.push({ op: "drop" });
    }
    return compileExpression(ctx, fctx, expr.right);
  }
  if (op === ts6.SyntaxKind.InstanceOfKeyword) {
    return compileInstanceOf(ctx, fctx, expr);
  }
  if (
    op === ts6.SyntaxKind.EqualsEqualsEqualsToken ||
    op === ts6.SyntaxKind.EqualsEqualsToken ||
    op === ts6.SyntaxKind.ExclamationEqualsEqualsToken ||
    op === ts6.SyntaxKind.ExclamationEqualsToken
  ) {
    const typeofResult = compileTypeofComparison(ctx, fctx, expr);
    if (typeofResult !== null) return typeofResult;
  }
  const isEqOp = op === ts6.SyntaxKind.EqualsEqualsEqualsToken || op === ts6.SyntaxKind.EqualsEqualsToken;
  const isNeqOp = op === ts6.SyntaxKind.ExclamationEqualsEqualsToken || op === ts6.SyntaxKind.ExclamationEqualsToken;
  const isStrictEqOp = op === ts6.SyntaxKind.EqualsEqualsEqualsToken;
  const isStrictNeqOp = op === ts6.SyntaxKind.ExclamationEqualsEqualsToken;
  const isLooseEqOp = op === ts6.SyntaxKind.EqualsEqualsToken;
  const isLooseNeqOp = op === ts6.SyntaxKind.ExclamationEqualsToken;
  if (isEqOp || isNeqOp) {
    const rightIsNullKeyword = expr.right.kind === ts6.SyntaxKind.NullKeyword;
    const rightIsUndefinedId = ts6.isIdentifier(expr.right) && expr.right.text === "undefined";
    const rightIsNullish = rightIsNullKeyword || rightIsUndefinedId;
    const leftIsNullKeyword = expr.left.kind === ts6.SyntaxKind.NullKeyword;
    const leftIsUndefinedId = ts6.isIdentifier(expr.left) && expr.left.text === "undefined";
    const leftIsNullish = leftIsNullKeyword || leftIsUndefinedId;
    if (rightIsNullish || leftIsNullish) {
      const nonNullExpr = rightIsNullish ? expr.left : expr.right;
      const nonNullIsNullKeyword = rightIsNullish ? leftIsNullKeyword : rightIsNullKeyword;
      const nonNullIsUndefinedId = rightIsNullish ? leftIsUndefinedId : rightIsUndefinedId;
      const nullSideIsNullKeyword = rightIsNullish ? rightIsNullKeyword : leftIsNullKeyword;
      const nullSideIsUndefinedId = rightIsNullish ? rightIsUndefinedId : leftIsUndefinedId;
      if (nonNullIsNullKeyword || nonNullIsUndefinedId) {
        if (isStrictEqOp || isStrictNeqOp) {
          const sameKind =
            (nonNullIsNullKeyword && nullSideIsNullKeyword) || (nonNullIsUndefinedId && nullSideIsUndefinedId);
          fctx.body.push({ op: "i32.const", value: isStrictEqOp ? (sameKind ? 1 : 0) : sameKind ? 0 : 1 });
          return { kind: "i32" };
        }
        fctx.body.push({ op: "i32.const", value: isLooseEqOp ? 1 : 0 });
        return { kind: "i32" };
      }
      const nonNullTsType = ctx.checker.getTypeAtLocation(nonNullExpr);
      const nonNullIsUndefinedType =
        (nonNullTsType.flags & ts6.TypeFlags.Undefined) !== 0 || (nonNullTsType.flags & ts6.TypeFlags.Void) !== 0;
      const nonNullIsNullType = (nonNullTsType.flags & ts6.TypeFlags.Null) !== 0;
      const valType = compileExpression(ctx, fctx, nonNullExpr);
      if (valType === null) {
        if (isStrictEqOp || isStrictNeqOp) {
          const sameKind = nullSideIsUndefinedId;
          fctx.body.push({ op: "i32.const", value: isStrictEqOp ? (sameKind ? 1 : 0) : sameKind ? 0 : 1 });
        } else {
          fctx.body.push({ op: "i32.const", value: isEqOp ? 1 : 0 });
        }
        return { kind: "i32" };
      }
      if (valType.kind === "externref") {
        if (isStrictEqOp || isStrictNeqOp) {
          if (nullSideIsNullKeyword) {
            fctx.body.push({ op: "ref.is_null" });
            if (isStrictNeqOp) fctx.body.push({ op: "i32.eqz" });
            return { kind: "i32" };
          }
          const isUndefIdx2 = ensureExternIsUndefinedImport(ctx);
          if (isUndefIdx2 !== void 0) {
            flushLateImportShifts2(ctx, fctx);
            fctx.body.push({ op: "call", funcIdx: isUndefIdx2 });
            if (isStrictNeqOp) fctx.body.push({ op: "i32.eqz" });
            return { kind: "i32" };
          }
          fctx.body.push({ op: "ref.is_null" });
          if (isStrictNeqOp) fctx.body.push({ op: "i32.eqz" });
          return { kind: "i32" };
        }
        const isUndefIdx = ensureExternIsUndefinedImport(ctx);
        if (isUndefIdx !== void 0) {
          flushLateImportShifts2(ctx, fctx);
          const tmpLocal = allocTempLocal(fctx, { kind: "externref" });
          fctx.body.push({ op: "local.tee", index: tmpLocal });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "call", funcIdx: isUndefIdx });
          fctx.body.push({ op: "i32.or" });
          releaseTempLocal(fctx, tmpLocal);
          if (isNeqOp) fctx.body.push({ op: "i32.eqz" });
          return { kind: "i32" };
        }
        fctx.body.push({ op: "ref.is_null" });
        if (isNeqOp) fctx.body.push({ op: "i32.eqz" });
        return { kind: "i32" };
      }
      if (nonNullIsUndefinedType || nonNullIsNullType) {
        fctx.body.push({ op: "drop" });
        if (isLooseEqOp || isLooseNeqOp) {
          fctx.body.push({ op: "i32.const", value: isLooseEqOp ? 1 : 0 });
          return { kind: "i32" };
        }
        const sameKind =
          (nonNullIsUndefinedType && nullSideIsUndefinedId) || (nonNullIsNullType && nullSideIsNullKeyword);
        fctx.body.push({ op: "i32.const", value: isStrictEqOp ? (sameKind ? 1 : 0) : sameKind ? 0 : 1 });
        return { kind: "i32" };
      }
      if (valType.kind === "ref" || valType.kind === "ref_null") {
        if ((isStrictEqOp || isStrictNeqOp) && nullSideIsUndefinedId) {
          fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "i32.const", value: isStrictNeqOp ? 1 : 0 });
          return { kind: "i32" };
        }
        fctx.body.push({ op: "ref.is_null" });
        if (isNeqOp) fctx.body.push({ op: "i32.eqz" });
        return { kind: "i32" };
      }
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "i32.const", value: isNeqOp ? 1 : 0 });
      return { kind: "i32" };
    }
  }
  if (op === ts6.SyntaxKind.InKeyword) {
    const rightType2 = ctx.checker.getTypeAtLocation(expr.right);
    const rightWasm = resolveWasmType(ctx, rightType2);
    let structFieldNames = null;
    let isVecType = false;
    let vecTypeIdx = -1;
    if (rightWasm.kind === "ref" || rightWasm.kind === "ref_null") {
      const typeIdx = rightWasm.typeIdx;
      const structDef = ctx.mod.types[typeIdx];
      if (structDef?.kind === "struct") {
        if (structDef.name?.startsWith("__vec_")) {
          isVecType = true;
          vecTypeIdx = typeIdx;
        } else {
          structFieldNames = structDef.fields.map((f) => f.name).filter((n) => n !== void 0);
        }
      }
    }
    let staticKey = null;
    const leftExpr = expr.left;
    if (ts6.isPrivateIdentifier(leftExpr)) {
      staticKey = leftExpr.text.startsWith("#") ? "__priv_" + leftExpr.text.slice(1) : leftExpr.text;
    } else if (ts6.isStringLiteral(leftExpr)) {
      staticKey = leftExpr.text;
    } else if (ts6.isNumericLiteral(leftExpr)) {
      staticKey = leftExpr.text;
    } else if (ts6.isBinaryExpression(leftExpr) && leftExpr.operatorToken.kind === ts6.SyntaxKind.CommaToken) {
      let last = leftExpr.right;
      while (ts6.isBinaryExpression(last) && last.operatorToken.kind === ts6.SyntaxKind.CommaToken) {
        last = last.right;
      }
      if (ts6.isStringLiteral(last)) {
        staticKey = last.text;
      } else if (ts6.isNumericLiteral(last)) {
        staticKey = last.text;
      }
    } else if (ts6.isParenthesizedExpression(leftExpr)) {
      const inner = leftExpr.expression;
      if (ts6.isBinaryExpression(inner) && inner.operatorToken.kind === ts6.SyntaxKind.CommaToken) {
        let last = inner.right;
        while (ts6.isBinaryExpression(last) && last.operatorToken.kind === ts6.SyntaxKind.CommaToken) {
          last = last.right;
        }
        if (ts6.isStringLiteral(last)) {
          staticKey = last.text;
        } else if (ts6.isNumericLiteral(last)) {
          staticKey = last.text;
        }
      } else if (ts6.isStringLiteral(inner)) {
        staticKey = inner.text;
      } else if (ts6.isNumericLiteral(inner)) {
        staticKey = inner.text;
      }
    }
    let tsTypeHasProperty = false;
    if (staticKey !== null) {
      const prop = rightType2.getProperty(staticKey);
      if (prop) {
        tsTypeHasProperty = true;
      }
      if (
        !tsTypeHasProperty &&
        ts6.isBinaryExpression(expr.right) &&
        expr.right.operatorToken.kind === ts6.SyntaxKind.CommaToken
      ) {
        let lastRight = expr.right.right;
        while (ts6.isBinaryExpression(lastRight) && lastRight.operatorToken.kind === ts6.SyntaxKind.CommaToken) {
          lastRight = lastRight.right;
        }
        const lastRightType = ctx.checker.getTypeAtLocation(lastRight);
        const prop2 = lastRightType.getProperty(staticKey);
        if (prop2) tsTypeHasProperty = true;
      }
      if (!tsTypeHasProperty) {
        const apparentType = ctx.checker.getApparentType(rightType2);
        const apparentProp = apparentType.getProperty(staticKey);
        if (apparentProp) tsTypeHasProperty = true;
      }
    }
    if (isVecType && staticKey !== null) {
      const numIdx = Number(staticKey);
      if (Number.isFinite(numIdx) && numIdx >= 0 && Number.isInteger(numIdx)) {
        const leftResult = compileExpression(ctx, fctx, expr.left);
        if (leftResult) {
          fctx.body.push({ op: "drop" });
        }
        const rightResult = compileExpression(ctx, fctx, expr.right);
        if (rightResult) {
          fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
          fctx.body.push({ op: "i32.const", value: numIdx });
          fctx.body.push({ op: "i32.gt_s" });
        } else {
          fctx.body.push({ op: "i32.const", value: 0 });
        }
        return { kind: "i32" };
      }
      if (staticKey === "length") {
        const leftResult = compileExpression(ctx, fctx, expr.left);
        if (leftResult) {
          fctx.body.push({ op: "drop" });
        }
        const rightResult = compileExpression(ctx, fctx, expr.right);
        if (rightResult) {
          fctx.body.push({ op: "drop" });
        }
        fctx.body.push({ op: "i32.const", value: 1 });
        return { kind: "i32" };
      }
    }
    if (staticKey !== null) {
      const hasInStruct = structFieldNames !== null && structFieldNames.includes(staticKey);
      const has = hasInStruct || tsTypeHasProperty;
      const leftResult = compileExpression(ctx, fctx, expr.left);
      if (leftResult) {
        fctx.body.push({ op: "drop" });
      }
      const rightResult = compileExpression(ctx, fctx, expr.right);
      if (rightResult) {
        fctx.body.push({ op: "drop" });
      }
      fctx.body.push({ op: "i32.const", value: has ? 1 : 0 });
      return { kind: "i32" };
    }
    if (structFieldNames !== null && structFieldNames.length > 0) {
      const keyType = compileExpression(ctx, fctx, expr.left);
      if (keyType) {
        const equalsIdx = ctx.funcMap.get("__str_eq") ?? ctx.funcMap.get("string_equals");
        const jsStrEquals = ctx.mod.imports.findIndex(
          (imp) => imp.module === "wasm:js-string" && imp.name === "equals",
        );
        const eqFunc = jsStrEquals >= 0 ? jsStrEquals : equalsIdx;
        if (eqFunc !== void 0 && eqFunc >= 0) {
          const keyLocal = allocLocal(fctx, `__in_key_${fctx.locals.length}`, keyType);
          fctx.body.push({ op: "local.set", index: keyLocal });
          fctx.body.push({ op: "i32.const", value: 0 });
          for (const fieldName of structFieldNames) {
            fctx.body.push({ op: "local.get", index: keyLocal });
            const strGlobal = ctx.stringGlobalMap.get(fieldName);
            if (strGlobal !== void 0) {
              fctx.body.push({ op: "global.get", index: strGlobal });
              fctx.body.push({ op: "call", funcIdx: eqFunc });
              fctx.body.push({ op: "i32.or" });
            }
          }
          return { kind: "i32" };
        }
      }
    }
    {
      const leftResult = compileExpression(ctx, fctx, expr.left);
      if (leftResult) {
        fctx.body.push({ op: "drop" });
      }
      const rightResult = compileExpression(ctx, fctx, expr.right);
      if (rightResult) {
        fctx.body.push({ op: "drop" });
      }
      const leftType2 = ctx.checker.getTypeAtLocation(expr.left);
      if (leftType2.isStringLiteral()) {
        const key = leftType2.value;
        const prop = rightType2.getProperty(key);
        const apparentType = ctx.checker.getApparentType(rightType2);
        const apparentProp = apparentType.getProperty(key);
        const has = !!(prop || apparentProp || (structFieldNames && structFieldNames.includes(key)));
        fctx.body.push({ op: "i32.const", value: has ? 1 : 0 });
        return { kind: "i32" };
      }
      fctx.body.push({ op: "i32.const", value: 0 });
      return { kind: "i32" };
    }
  }
  {
    const flatResult = tryFlattenBinaryChain(ctx, fctx, expr, op);
    if (flatResult !== null) return flatResult;
  }
  {
    const folded = tryStaticToNumber(ctx, expr);
    if (folded !== void 0) {
      fctx.body.push({ op: "f64.const", value: folded });
      return { kind: "f64" };
    }
  }
  const leftTsType = ctx.checker.getTypeAtLocation(expr.left);
  const rightTsType = ctx.checker.getTypeAtLocation(expr.right);
  const isLooseEq = op === ts6.SyntaxKind.EqualsEqualsToken;
  const isLooseNeq = op === ts6.SyntaxKind.ExclamationEqualsToken;
  if (isLooseEq || isLooseNeq) {
    const leftIsNum = isNumberType(leftTsType);
    const leftIsBool = isBooleanType(leftTsType);
    const leftIsStr = isStringType(leftTsType);
    const rightIsNum = isNumberType(rightTsType);
    const rightIsBool = isBooleanType(rightTsType);
    const rightIsStr = isStringType(rightTsType);
    if (leftIsNum && rightIsBool) {
      compileExpression(ctx, fctx, expr.left, { kind: "f64" });
      compileExpression(ctx, fctx, expr.right);
      fctx.body.push({ op: "f64.convert_i32_s" });
      fctx.body.push({ op: isLooseEq ? "f64.eq" : "f64.ne" });
      return { kind: "i32" };
    }
    if (leftIsBool && rightIsNum) {
      compileExpression(ctx, fctx, expr.left);
      fctx.body.push({ op: "f64.convert_i32_s" });
      compileExpression(ctx, fctx, expr.right, { kind: "f64" });
      fctx.body.push({ op: isLooseEq ? "f64.eq" : "f64.ne" });
      return { kind: "i32" };
    }
    if ((leftIsStr && rightIsNum) || (leftIsNum && rightIsStr)) {
      const pfIdx = ctx.funcMap.get("parseFloat");
      if (pfIdx !== void 0) {
        if (leftIsStr) {
          compileExpression(ctx, fctx, expr.left);
          fctx.body.push({ op: "call", funcIdx: pfIdx });
          compileExpression(ctx, fctx, expr.right, { kind: "f64" });
        } else {
          compileExpression(ctx, fctx, expr.left, { kind: "f64" });
          compileExpression(ctx, fctx, expr.right);
          fctx.body.push({ op: "call", funcIdx: pfIdx });
        }
        fctx.body.push({ op: isLooseEq ? "f64.eq" : "f64.ne" });
        return { kind: "i32" };
      }
    }
    if ((leftIsStr && rightIsBool) || (leftIsBool && rightIsStr)) {
      const pfIdx = ctx.funcMap.get("parseFloat");
      if (pfIdx !== void 0) {
        if (leftIsStr) {
          compileExpression(ctx, fctx, expr.left);
          fctx.body.push({ op: "call", funcIdx: pfIdx });
          compileExpression(ctx, fctx, expr.right);
          fctx.body.push({ op: "f64.convert_i32_s" });
        } else {
          compileExpression(ctx, fctx, expr.left);
          fctx.body.push({ op: "f64.convert_i32_s" });
          compileExpression(ctx, fctx, expr.right);
          fctx.body.push({ op: "call", funcIdx: pfIdx });
        }
        fctx.body.push({ op: isLooseEq ? "f64.eq" : "f64.ne" });
        return { kind: "i32" };
      }
    }
  }
  if (ctx.anyValueTypeIdx >= 0) {
    const leftIsAny = (leftTsType.flags & ts6.TypeFlags.Any) !== 0;
    const rightIsAny = (rightTsType.flags & ts6.TypeFlags.Any) !== 0;
    if (leftIsAny && rightIsAny) {
      const isPlusOp = op === ts6.SyntaxKind.PlusToken;
      const isEqualityOp =
        op === ts6.SyntaxKind.EqualsEqualsToken ||
        op === ts6.SyntaxKind.ExclamationEqualsToken ||
        op === ts6.SyntaxKind.EqualsEqualsEqualsToken ||
        op === ts6.SyntaxKind.ExclamationEqualsEqualsToken;
      if (isPlusOp || isEqualityOp) {
        const anyDispatch = compileAnyBinaryDispatch(ctx, fctx, expr, op);
        if (anyDispatch !== null) return anyDispatch;
      }
    }
  }
  const isRelational =
    op === ts6.SyntaxKind.LessThanToken ||
    op === ts6.SyntaxKind.LessThanEqualsToken ||
    op === ts6.SyntaxKind.GreaterThanToken ||
    op === ts6.SyntaxKind.GreaterThanEqualsToken;
  if (
    isStringType(leftTsType) &&
    (isStringType(rightTsType) ||
      op === ts6.SyntaxKind.PlusToken ||
      (!isRelational && !isNumberType(rightTsType) && !isBooleanType(rightTsType) && !isBigIntType(rightTsType)))
  ) {
    return compileStringBinaryOp(ctx, fctx, expr, op);
  }
  if (op === ts6.SyntaxKind.PlusToken && isStringType(rightTsType) && !isBigIntType(leftTsType)) {
    return compileStringBinaryOp(ctx, fctx, expr, op);
  }
  if (isBigIntType(leftTsType) || isBigIntType(rightTsType)) {
    const leftIsBigInt = isBigIntType(leftTsType);
    const rightIsBigInt = isBigIntType(rightTsType);
    if (leftIsBigInt !== rightIsBigInt) {
      const isStrictEq = op === ts6.SyntaxKind.EqualsEqualsEqualsToken;
      const isStrictNeq = op === ts6.SyntaxKind.ExclamationEqualsEqualsToken;
      if (isStrictEq || isStrictNeq) {
        const lt2 = compileExpression(ctx, fctx, expr.left);
        if (lt2) fctx.body.push({ op: "drop" });
        const rt2 = compileExpression(ctx, fctx, expr.right);
        if (rt2) fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "i32.const", value: isStrictNeq ? 1 : 0 });
        return { kind: "i32" };
      }
      const isLooseEq2 = op === ts6.SyntaxKind.EqualsEqualsToken;
      const isLooseNeq2 = op === ts6.SyntaxKind.ExclamationEqualsToken;
      const isComparison =
        op === ts6.SyntaxKind.LessThanToken ||
        op === ts6.SyntaxKind.LessThanEqualsToken ||
        op === ts6.SyntaxKind.GreaterThanToken ||
        op === ts6.SyntaxKind.GreaterThanEqualsToken;
      if (isLooseEq2 || isLooseNeq2 || isComparison) {
        const leftIsStr = isStringType(leftTsType);
        const rightIsStr = isStringType(rightTsType);
        const leftType3 = compileExpression(ctx, fctx, expr.left, leftIsBigInt ? { kind: "i64" } : void 0);
        if (!leftType3) return null;
        if (leftType3.kind === "i64") {
          fctx.body.push({ op: "f64.convert_i64_s" });
        } else if (leftType3.kind === "externref") {
          const pfIdx = ctx.funcMap.get("parseFloat");
          if (pfIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: pfIdx });
          } else {
            addUnionImports(ctx);
            const unboxIdx = ctx.funcMap.get("__unbox_number");
            fctx.body.push({ op: "call", funcIdx: unboxIdx });
          }
        } else if (leftType3.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
        }
        const rightType3 = compileExpression(ctx, fctx, expr.right, rightIsBigInt ? { kind: "i64" } : void 0);
        if (!rightType3) return null;
        if (rightType3.kind === "i64") {
          fctx.body.push({ op: "f64.convert_i64_s" });
        } else if (rightType3.kind === "externref") {
          const pfIdx = ctx.funcMap.get("parseFloat");
          if (pfIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: pfIdx });
          } else {
            addUnionImports(ctx);
            const unboxIdx = ctx.funcMap.get("__unbox_number");
            fctx.body.push({ op: "call", funcIdx: unboxIdx });
          }
        } else if (rightType3.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
        }
        if (isLooseEq2) {
          fctx.body.push({ op: "f64.eq" });
        } else if (isLooseNeq2) {
          fctx.body.push({ op: "f64.ne" });
        } else {
          return compileNumericBinaryOp(ctx, fctx, op, expr);
        }
        return { kind: "i32" };
      }
      const lt = compileExpression(ctx, fctx, expr.left);
      if (lt) fctx.body.push({ op: "drop" });
      const rt = compileExpression(ctx, fctx, expr.right);
      if (rt) fctx.body.push({ op: "drop" });
      emitThrowString2(ctx, fctx, "Cannot mix BigInt and other types, use explicit conversions");
      return { kind: "i32" };
    }
    const i64Hint = { kind: "i64" };
    const leftType2 = compileExpression(ctx, fctx, expr.left, i64Hint);
    const rightType2 = compileExpression(ctx, fctx, expr.right, i64Hint);
    if (!leftType2 || !rightType2) return null;
    return compileI64BinaryOp(ctx, fctx, op, expr);
  }
  const isNumericOp =
    op === ts6.SyntaxKind.PlusToken ||
    op === ts6.SyntaxKind.MinusToken ||
    op === ts6.SyntaxKind.AsteriskToken ||
    op === ts6.SyntaxKind.AsteriskAsteriskToken ||
    op === ts6.SyntaxKind.SlashToken ||
    op === ts6.SyntaxKind.PercentToken ||
    op === ts6.SyntaxKind.LessThanToken ||
    op === ts6.SyntaxKind.LessThanEqualsToken ||
    op === ts6.SyntaxKind.GreaterThanToken ||
    op === ts6.SyntaxKind.GreaterThanEqualsToken ||
    op === ts6.SyntaxKind.AmpersandToken ||
    op === ts6.SyntaxKind.BarToken ||
    op === ts6.SyntaxKind.CaretToken ||
    op === ts6.SyntaxKind.LessThanLessThanToken ||
    op === ts6.SyntaxKind.GreaterThanGreaterThanToken ||
    op === ts6.SyntaxKind.GreaterThanGreaterThanGreaterThanToken;
  const isDivOrPow = op === ts6.SyntaxKind.SlashToken || op === ts6.SyntaxKind.AsteriskAsteriskToken;
  const leftNativeType = resolveNativeTypeAnnotation(leftTsType);
  const rightNativeType = resolveNativeTypeAnnotation(rightTsType);
  const bothNativeI32 = leftNativeType?.kind === "i32" && rightNativeType?.kind === "i32";
  const numericHint = isNumericOp ? { kind: (ctx.fast || bothNativeI32) && !isDivOrPow ? "i32" : "f64" } : void 0;
  let leftType = compileExpression(ctx, fctx, expr.left, numericHint);
  let rightType = compileExpression(ctx, fctx, expr.right, numericHint);
  if (!leftType || !rightType) return null;
  if (leftType.kind === "i32" && rightType.kind === "f64") {
    const tmpR = allocTempLocal(fctx, { kind: "f64" });
    fctx.body.push({ op: "local.set", index: tmpR });
    fctx.body.push({ op: "f64.convert_i32_s" });
    fctx.body.push({ op: "local.get", index: tmpR });
    releaseTempLocal(fctx, tmpR);
    leftType = { kind: "f64" };
  } else if (leftType.kind === "f64" && rightType.kind === "i32") {
    fctx.body.push({ op: "f64.convert_i32_s" });
    rightType = { kind: "f64" };
  }
  {
    const leftIsRef = leftType.kind === "ref" || leftType.kind === "ref_null";
    const rightIsRef = rightType.kind === "ref" || rightType.kind === "ref_null";
    if (leftIsRef || rightIsRef) {
      const isStrictEq = op === ts6.SyntaxKind.EqualsEqualsEqualsToken;
      const isStrictNeq = op === ts6.SyntaxKind.ExclamationEqualsEqualsToken;
      if (isStrictEq || isStrictNeq) {
        if (leftIsRef && rightIsRef) {
          fctx.body.push({ op: "ref.eq" });
          if (isStrictNeq) fctx.body.push({ op: "i32.eqz" });
          return { kind: "i32" };
        }
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "i32.const", value: isStrictNeq ? 1 : 0 });
        return { kind: "i32" };
      }
      if (isNumericOp || isEqOp || isNeqOp) {
        const hint = op === ts6.SyntaxKind.PlusToken ? "default" : "number";
        if (rightIsRef) {
          coerceType3(ctx, fctx, rightType, { kind: "f64" }, hint);
          rightType = { kind: "f64" };
        }
        if (leftIsRef) {
          const tmpR = allocTempLocal(fctx, rightType);
          fctx.body.push({ op: "local.set", index: tmpR });
          coerceType3(ctx, fctx, leftType, { kind: "f64" }, hint);
          fctx.body.push({ op: "local.get", index: tmpR });
          releaseTempLocal(fctx, tmpR);
          leftType = { kind: "f64" };
        }
        if (leftType.kind === "i32" && rightType.kind === "f64") {
          const tmpR = allocTempLocal(fctx, { kind: "f64" });
          fctx.body.push({ op: "local.set", index: tmpR });
          fctx.body.push({ op: "f64.convert_i32_s" });
          fctx.body.push({ op: "local.get", index: tmpR });
          releaseTempLocal(fctx, tmpR);
          leftType = { kind: "f64" };
        } else if (leftType.kind === "f64" && rightType.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
          rightType = { kind: "f64" };
        }
      }
    }
  }
  if (
    leftType.kind === "i32" &&
    rightType.kind === "i32" &&
    ((ctx.fast && isNumberType(leftTsType)) || bothNativeI32)
  ) {
    return compileI32BinaryOp(ctx, fctx, op, expr);
  }
  if (leftType.kind === "i64" && rightType.kind === "i64") {
    return compileI64BinaryOp(ctx, fctx, op, expr);
  }
  if ((leftType.kind === "i64" && rightType.kind === "f64") || (leftType.kind === "f64" && rightType.kind === "i64")) {
    const isStrictEq = op === ts6.SyntaxKind.EqualsEqualsEqualsToken;
    const isStrictNeq = op === ts6.SyntaxKind.ExclamationEqualsEqualsToken;
    if (isStrictEq || isStrictNeq) {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "i32.const", value: isStrictNeq ? 1 : 0 });
      return { kind: "i32" };
    }
    if (rightType.kind === "i64") {
      fctx.body.push({ op: "f64.convert_i64_s" });
    } else {
      const tmpR = allocTempLocal(fctx, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: tmpR });
      fctx.body.push({ op: "f64.convert_i64_s" });
      fctx.body.push({ op: "local.get", index: tmpR });
      releaseTempLocal(fctx, tmpR);
    }
    const isLooseEq2 = op === ts6.SyntaxKind.EqualsEqualsToken;
    const isLooseNeq2 = op === ts6.SyntaxKind.ExclamationEqualsToken;
    if (isLooseEq2) {
      fctx.body.push({ op: "f64.eq" });
      return { kind: "i32" };
    }
    if (isLooseNeq2) {
      fctx.body.push({ op: "f64.ne" });
      return { kind: "i32" };
    }
    return compileNumericBinaryOp(ctx, fctx, op, expr);
  }
  if (
    (isNumberType(leftTsType) || leftType.kind === "f64") &&
    leftType.kind !== "externref" &&
    rightType.kind !== "externref"
  ) {
    if (rightType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
    }
    return compileNumericBinaryOp(ctx, fctx, op, expr);
  }
  if (
    (isBooleanType(leftTsType) || leftType.kind === "i32") &&
    leftType.kind !== "externref" &&
    rightType.kind !== "externref"
  ) {
    if (rightType.kind === "f64") {
      const tmpR = allocTempLocal(fctx, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: tmpR });
      fctx.body.push({ op: "f64.convert_i32_s" });
      fctx.body.push({ op: "local.get", index: tmpR });
      releaseTempLocal(fctx, tmpR);
      return compileNumericBinaryOp(ctx, fctx, op, expr);
    }
    return compileBooleanBinaryOp(ctx, fctx, op);
  }
  if ((leftType.kind === "externref" || rightType.kind === "externref") && isNumericOp) {
    addUnionImports(ctx);
    const unboxIdx = ctx.funcMap.get("__unbox_number");
    if (rightType.kind === "externref") {
      fctx.body.push({ op: "call", funcIdx: unboxIdx });
    }
    if (leftType.kind === "externref") {
      const tmpR = allocTempLocal(fctx, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: tmpR });
      fctx.body.push({ op: "call", funcIdx: unboxIdx });
      fctx.body.push({ op: "local.get", index: tmpR });
      releaseTempLocal(fctx, tmpR);
    }
    return compileNumericBinaryOp(ctx, fctx, op, expr);
  }
  if ((leftType.kind === "externref" || rightType.kind === "externref") && (isEqOp || isNeqOp)) {
    const isStrict =
      op === ts6.SyntaxKind.EqualsEqualsEqualsToken || op === ts6.SyntaxKind.ExclamationEqualsEqualsToken;
    const isStrictNeq = op === ts6.SyntaxKind.ExclamationEqualsEqualsToken;
    const leftIsString = isStringType(leftTsType);
    const rightIsString = isStringType(rightTsType);
    const leftIsNumber = isNumberType(leftTsType);
    const rightIsNumber = isNumberType(rightTsType);
    const leftIsBool = isBooleanType(leftTsType);
    const rightIsBool = isBooleanType(rightTsType);
    if (isStrict) {
      const leftJsKind = leftIsString ? "string" : leftIsNumber ? "number" : leftIsBool ? "boolean" : "other";
      const rightJsKind = rightIsString ? "string" : rightIsNumber ? "number" : rightIsBool ? "boolean" : "other";
      if (leftJsKind !== "other" && rightJsKind !== "other" && leftJsKind !== rightJsKind) {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "i32.const", value: isStrictNeq ? 1 : 0 });
        return { kind: "i32" };
      }
    }
    const eitherIsString = leftIsString || rightIsString;
    if (eitherIsString) {
      if (rightType.kind !== "externref") {
        coerceType3(ctx, fctx, rightType, { kind: "externref" });
      }
      if (leftType.kind !== "externref") {
        const tmpR = allocTempLocal(fctx, { kind: "externref" });
        fctx.body.push({ op: "local.set", index: tmpR });
        coerceType3(ctx, fctx, leftType, { kind: "externref" });
        fctx.body.push({ op: "local.get", index: tmpR });
        releaseTempLocal(fctx, tmpR);
      }
      addStringImports(ctx);
      const equalsIdx = ctx.funcMap.get("equals");
      if (equalsIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: equalsIdx });
        if (isNeqOp) fctx.body.push({ op: "i32.eqz" });
        return { kind: "i32" };
      }
    }
    const EQ_HEAP_TYPE = -19;
    if (
      leftType.kind === "externref" &&
      rightType.kind === "externref" &&
      !leftIsString &&
      !rightIsString &&
      !leftIsNumber &&
      !rightIsNumber &&
      !leftIsBool &&
      !rightIsBool
    ) {
      const tmpRight = allocTempLocal(fctx, { kind: "externref" });
      const tmpLeft = allocTempLocal(fctx, { kind: "externref" });
      fctx.body.push({ op: "local.set", index: tmpRight });
      fctx.body.push({ op: "local.set", index: tmpLeft });
      fctx.body.push({ op: "local.get", index: tmpLeft });
      fctx.body.push({ op: "any.convert_extern" });
      const tmpAnyLeft = allocTempLocal(fctx, { kind: "anyref" });
      fctx.body.push({ op: "local.tee", index: tmpAnyLeft });
      fctx.body.push({ op: "ref.test", typeIdx: EQ_HEAP_TYPE });
      fctx.body.push({
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [
          // Left is eqref-compatible — check right too
          { op: "local.get", index: tmpRight },
          { op: "any.convert_extern" },
          ...(() => {
            const tmpAnyRight = allocTempLocal(fctx, { kind: "anyref" });
            const instrs = [
              { op: "local.tee", index: tmpAnyRight },
              { op: "ref.test", typeIdx: EQ_HEAP_TYPE },
              {
                op: "if",
                blockType: { kind: "val", type: { kind: "i32" } },
                then: [
                  // Both are eqref — cast and compare with ref.eq
                  { op: "local.get", index: tmpAnyLeft },
                  { op: "ref.cast", typeIdx: EQ_HEAP_TYPE },
                  { op: "local.get", index: tmpAnyRight },
                  { op: "ref.cast", typeIdx: EQ_HEAP_TYPE },
                  { op: "ref.eq" },
                ],
                else: [
                  // Right is not eqref — cannot be equal to a GC ref
                  { op: "i32.const", value: 0 },
                ],
              },
            ];
            releaseTempLocal(fctx, tmpAnyRight);
            return instrs;
          })(),
        ],
        else: [
          // Left is not eqref — fall through to numeric comparison
          // by pushing -1 as sentinel to indicate "not handled"
          { op: "i32.const", value: -1 },
        ],
      });
      releaseTempLocal(fctx, tmpAnyLeft);
      const identityResult = allocTempLocal(fctx, { kind: "i32" });
      fctx.body.push({ op: "local.tee", index: identityResult });
      fctx.body.push({ op: "i32.const", value: -1 });
      fctx.body.push({ op: "i32.ne" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [
          // Identity check produced 0 or 1 — use it directly
          // For != / !==, negate
          { op: "local.get", index: identityResult },
          ...(isNeqOp ? [{ op: "i32.eqz" }] : []),
        ],
        else: (() => {
          addUnionImports(ctx);
          const unboxIdx2 = ctx.funcMap.get("__unbox_number");
          return [
            { op: "local.get", index: tmpLeft },
            { op: "call", funcIdx: unboxIdx2 },
            { op: "local.get", index: tmpRight },
            { op: "call", funcIdx: unboxIdx2 },
            { op: isEqOp ? "f64.eq" : "f64.ne" },
          ];
        })(),
      });
      releaseTempLocal(fctx, identityResult);
      releaseTempLocal(fctx, tmpRight);
      releaseTempLocal(fctx, tmpLeft);
      return { kind: "i32" };
    }
    addUnionImports(ctx);
    const unboxIdx = ctx.funcMap.get("__unbox_number");
    if (rightType.kind === "externref") {
      fctx.body.push({ op: "call", funcIdx: unboxIdx });
    } else if (rightType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
    }
    if (leftType.kind === "externref") {
      const tmpR = allocTempLocal(fctx, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: tmpR });
      fctx.body.push({ op: "call", funcIdx: unboxIdx });
      fctx.body.push({ op: "local.get", index: tmpR });
      releaseTempLocal(fctx, tmpR);
    } else if (leftType.kind === "i32") {
      const tmpR = allocTempLocal(fctx, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: tmpR });
      fctx.body.push({ op: "f64.convert_i32_s" });
      fctx.body.push({ op: "local.get", index: tmpR });
      releaseTempLocal(fctx, tmpR);
    }
    fctx.body.push({ op: isEqOp ? "f64.eq" : "f64.ne" });
    return { kind: "i32" };
  }
  if (isNumericOp) {
    if (rightType.kind === "externref") {
      addUnionImports(ctx);
      const unboxIdx = ctx.funcMap.get("__unbox_number");
      fctx.body.push({ op: "call", funcIdx: unboxIdx });
    } else if (rightType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
    } else if (rightType.kind === "i64") {
      fctx.body.push({ op: "f64.convert_i64_s" });
    } else if (rightType.kind === "ref" || rightType.kind === "ref_null") {
      coerceType3(ctx, fctx, rightType, { kind: "f64" });
    }
    if (
      leftType.kind === "externref" ||
      leftType.kind === "i32" ||
      leftType.kind === "i64" ||
      leftType.kind === "ref" ||
      leftType.kind === "ref_null"
    ) {
      const tmpR = allocTempLocal(fctx, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: tmpR });
      if (leftType.kind === "externref") {
        addUnionImports(ctx);
        const unboxIdx = ctx.funcMap.get("__unbox_number");
        fctx.body.push({ op: "call", funcIdx: unboxIdx });
      } else if (leftType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
      } else if (leftType.kind === "i64") {
        fctx.body.push({ op: "f64.convert_i64_s" });
      } else if (leftType.kind === "ref" || leftType.kind === "ref_null") {
        coerceType3(ctx, fctx, leftType, { kind: "f64" });
      }
      fctx.body.push({ op: "local.get", index: tmpR });
      releaseTempLocal(fctx, tmpR);
    }
    return compileNumericBinaryOp(ctx, fctx, op, expr);
  }
  ctx.errors.push({
    message: `Unsupported binary operator for type`,
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}
function compileAnyBinaryDispatch(ctx, fctx, expr, op) {
  let helperName = null;
  let resultIsI32 = false;
  switch (op) {
    case ts6.SyntaxKind.PlusToken:
      helperName = "__any_add";
      break;
    case ts6.SyntaxKind.MinusToken:
      helperName = "__any_sub";
      break;
    case ts6.SyntaxKind.AsteriskToken:
      helperName = "__any_mul";
      break;
    case ts6.SyntaxKind.SlashToken:
      helperName = "__any_div";
      break;
    case ts6.SyntaxKind.PercentToken:
      helperName = "__any_mod";
      break;
    case ts6.SyntaxKind.EqualsEqualsToken:
      helperName = "__any_eq";
      resultIsI32 = true;
      break;
    case ts6.SyntaxKind.EqualsEqualsEqualsToken:
      helperName = "__any_strict_eq";
      resultIsI32 = true;
      break;
    case ts6.SyntaxKind.ExclamationEqualsToken:
      helperName = "__any_eq";
      resultIsI32 = true;
      break;
    case ts6.SyntaxKind.ExclamationEqualsEqualsToken:
      helperName = "__any_strict_eq";
      resultIsI32 = true;
      break;
    case ts6.SyntaxKind.LessThanToken:
      helperName = "__any_lt";
      resultIsI32 = true;
      break;
    case ts6.SyntaxKind.GreaterThanToken:
      helperName = "__any_gt";
      resultIsI32 = true;
      break;
    case ts6.SyntaxKind.LessThanEqualsToken:
      helperName = "__any_le";
      resultIsI32 = true;
      break;
    case ts6.SyntaxKind.GreaterThanEqualsToken:
      helperName = "__any_ge";
      resultIsI32 = true;
      break;
    default:
      return null;
  }
  ensureAnyHelpers(ctx);
  const funcIdx = ctx.funcMap.get(helperName);
  if (funcIdx === void 0) return null;
  const leftType = compileExpression(ctx, fctx, expr.left);
  const rightType = compileExpression(ctx, fctx, expr.right);
  if (!leftType || !rightType) return null;
  fctx.body.push({ op: "call", funcIdx });
  if (op === ts6.SyntaxKind.ExclamationEqualsToken || op === ts6.SyntaxKind.ExclamationEqualsEqualsToken) {
    fctx.body.push({ op: "i32.eqz" });
  }
  if (resultIsI32) {
    return { kind: "i32" };
  }
  return { kind: "ref", typeIdx: ctx.anyValueTypeIdx };
}
function compileNumericBinaryOp(ctx, fctx, op, expr) {
  switch (op) {
    case ts6.SyntaxKind.PlusToken:
      fctx.body.push({ op: "f64.add" });
      return { kind: "f64" };
    case ts6.SyntaxKind.MinusToken:
      fctx.body.push({ op: "f64.sub" });
      return { kind: "f64" };
    case ts6.SyntaxKind.AsteriskToken:
      fctx.body.push({ op: "f64.mul" });
      return { kind: "f64" };
    case ts6.SyntaxKind.AsteriskAsteriskToken: {
      const funcIdx = ctx.funcMap.get("Math_pow");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "f64" };
      }
      ctx.errors.push({
        message: "Math_pow import not found for ** operator",
        line: getLine(expr),
        column: getCol(expr),
      });
      return { kind: "f64" };
    }
    case ts6.SyntaxKind.SlashToken:
      fctx.body.push({ op: "f64.div" });
      return { kind: "f64" };
    case ts6.SyntaxKind.PercentToken:
      return compileModulo(ctx, fctx, expr);
    case ts6.SyntaxKind.EqualsEqualsEqualsToken:
      fctx.body.push({ op: "f64.eq" });
      return { kind: "i32" };
    case ts6.SyntaxKind.ExclamationEqualsEqualsToken:
      fctx.body.push({ op: "f64.ne" });
      return { kind: "i32" };
    case ts6.SyntaxKind.EqualsEqualsToken:
      fctx.body.push({ op: "f64.eq" });
      return { kind: "i32" };
    case ts6.SyntaxKind.ExclamationEqualsToken:
      fctx.body.push({ op: "f64.ne" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanToken:
      fctx.body.push({ op: "f64.lt" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanEqualsToken:
      fctx.body.push({ op: "f64.le" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanToken:
      fctx.body.push({ op: "f64.gt" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanEqualsToken:
      fctx.body.push({ op: "f64.ge" });
      return { kind: "i32" };
    case ts6.SyntaxKind.AmpersandToken:
      return compileBitwiseBinaryOp(fctx, "i32.and", false);
    case ts6.SyntaxKind.BarToken:
      return compileBitwiseBinaryOp(fctx, "i32.or", false);
    case ts6.SyntaxKind.CaretToken:
      return compileBitwiseBinaryOp(fctx, "i32.xor", false);
    case ts6.SyntaxKind.LessThanLessThanToken:
      return compileBitwiseBinaryOp(fctx, "i32.shl", false);
    case ts6.SyntaxKind.GreaterThanGreaterThanToken:
      return compileBitwiseBinaryOp(fctx, "i32.shr_s", false);
    case ts6.SyntaxKind.GreaterThanGreaterThanGreaterThanToken:
      return compileBitwiseBinaryOp(fctx, "i32.shr_u", true);
    default:
      ctx.errors.push({
        message: `Unsupported numeric binary operator: ${ts6.SyntaxKind[op]}`,
        line: getLine(expr),
        column: getCol(expr),
      });
      return { kind: "f64" };
  }
}
function compileI32BinaryOp(ctx, fctx, op, expr) {
  switch (op) {
    case ts6.SyntaxKind.PlusToken:
      fctx.body.push({ op: "i32.add" });
      return { kind: "i32" };
    case ts6.SyntaxKind.MinusToken:
      fctx.body.push({ op: "i32.sub" });
      return { kind: "i32" };
    case ts6.SyntaxKind.AsteriskToken:
      fctx.body.push({ op: "i32.mul" });
      return { kind: "i32" };
    case ts6.SyntaxKind.PercentToken:
      fctx.body.push({ op: "i32.rem_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.EqualsEqualsEqualsToken:
    case ts6.SyntaxKind.EqualsEqualsToken:
      fctx.body.push({ op: "i32.eq" });
      return { kind: "i32" };
    case ts6.SyntaxKind.ExclamationEqualsEqualsToken:
    case ts6.SyntaxKind.ExclamationEqualsToken:
      fctx.body.push({ op: "i32.ne" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanToken:
      fctx.body.push({ op: "i32.lt_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanEqualsToken:
      fctx.body.push({ op: "i32.le_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanToken:
      fctx.body.push({ op: "i32.gt_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanEqualsToken:
      fctx.body.push({ op: "i32.ge_s" });
      return { kind: "i32" };
    // Bitwise — direct i32 ops (no conversion needed!)
    case ts6.SyntaxKind.AmpersandToken:
      fctx.body.push({ op: "i32.and" });
      return { kind: "i32" };
    case ts6.SyntaxKind.BarToken:
      fctx.body.push({ op: "i32.or" });
      return { kind: "i32" };
    case ts6.SyntaxKind.CaretToken:
      fctx.body.push({ op: "i32.xor" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanLessThanToken:
      fctx.body.push({ op: "i32.shl" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanGreaterThanToken:
      fctx.body.push({ op: "i32.shr_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanGreaterThanGreaterThanToken:
      fctx.body.push({ op: "i32.shr_u" });
      return { kind: "i32" };
    default:
      return compileNumericBinaryOp(ctx, fctx, op, expr);
  }
}
function compileI64BinaryOp(ctx, fctx, op, expr) {
  switch (op) {
    case ts6.SyntaxKind.PlusToken:
      fctx.body.push({ op: "i64.add" });
      return { kind: "i64" };
    case ts6.SyntaxKind.MinusToken:
      fctx.body.push({ op: "i64.sub" });
      return { kind: "i64" };
    case ts6.SyntaxKind.AsteriskToken:
      fctx.body.push({ op: "i64.mul" });
      return { kind: "i64" };
    case ts6.SyntaxKind.SlashToken:
      fctx.body.push({ op: "i64.div_s" });
      return { kind: "i64" };
    case ts6.SyntaxKind.PercentToken:
      fctx.body.push({ op: "i64.rem_s" });
      return { kind: "i64" };
    case ts6.SyntaxKind.AsteriskAsteriskToken: {
      const expLocal = allocTempLocal(fctx, { kind: "i64" });
      const baseLocal = allocTempLocal(fctx, { kind: "i64" });
      const resultLocal = allocTempLocal(fctx, { kind: "i64" });
      fctx.body.push({ op: "local.set", index: expLocal });
      fctx.body.push({ op: "local.set", index: baseLocal });
      fctx.body.push({ op: "i64.const", value: 1n });
      fctx.body.push({ op: "local.set", index: resultLocal });
      fctx.body.push({
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              // if exp <= 0 then break
              { op: "local.get", index: expLocal },
              { op: "i64.const", value: 0n },
              { op: "i64.le_s" },
              { op: "br_if", depth: 1 },
              // break out of block
              // result = result * base
              { op: "local.get", index: resultLocal },
              { op: "local.get", index: baseLocal },
              { op: "i64.mul" },
              { op: "local.set", index: resultLocal },
              // exp = exp - 1
              { op: "local.get", index: expLocal },
              { op: "i64.const", value: 1n },
              { op: "i64.sub" },
              { op: "local.set", index: expLocal },
              // continue loop
              { op: "br", depth: 0 },
            ],
          },
        ],
      });
      fctx.body.push({ op: "local.get", index: resultLocal });
      releaseTempLocal(fctx, expLocal);
      releaseTempLocal(fctx, baseLocal);
      releaseTempLocal(fctx, resultLocal);
      return { kind: "i64" };
    }
    case ts6.SyntaxKind.EqualsEqualsEqualsToken:
    case ts6.SyntaxKind.EqualsEqualsToken:
      fctx.body.push({ op: "i64.eq" });
      return { kind: "i32" };
    case ts6.SyntaxKind.ExclamationEqualsEqualsToken:
    case ts6.SyntaxKind.ExclamationEqualsToken:
      fctx.body.push({ op: "i64.ne" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanToken:
      fctx.body.push({ op: "i64.lt_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanEqualsToken:
      fctx.body.push({ op: "i64.le_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanToken:
      fctx.body.push({ op: "i64.gt_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanEqualsToken:
      fctx.body.push({ op: "i64.ge_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.AmpersandToken:
      fctx.body.push({ op: "i64.and" });
      return { kind: "i64" };
    case ts6.SyntaxKind.BarToken:
      fctx.body.push({ op: "i64.or" });
      return { kind: "i64" };
    case ts6.SyntaxKind.CaretToken:
      fctx.body.push({ op: "i64.xor" });
      return { kind: "i64" };
    case ts6.SyntaxKind.LessThanLessThanToken:
      fctx.body.push({ op: "i64.shl" });
      return { kind: "i64" };
    case ts6.SyntaxKind.GreaterThanGreaterThanToken:
      fctx.body.push({ op: "i64.shr_s" });
      return { kind: "i64" };
    case ts6.SyntaxKind.GreaterThanGreaterThanGreaterThanToken:
      fctx.body.push({ op: "i64.shr_u" });
      return { kind: "i64" };
    default:
      ctx.errors.push({
        message: `Unsupported BigInt binary operator: ${ts6.SyntaxKind[op]}`,
        line: getLine(expr),
        column: getCol(expr),
      });
      return { kind: "i64" };
  }
}
function emitToInt32(fctx) {
  fctx.body.push({ op: "f64.trunc" });
  const tmp = allocTempLocal(fctx, { kind: "f64" });
  fctx.body.push({ op: "local.tee", index: tmp });
  fctx.body.push({ op: "local.get", index: tmp });
  fctx.body.push({ op: "f64.const", value: 4294967296 });
  fctx.body.push({ op: "f64.div" });
  fctx.body.push({ op: "f64.floor" });
  fctx.body.push({ op: "f64.const", value: 4294967296 });
  fctx.body.push({ op: "f64.mul" });
  fctx.body.push({ op: "f64.sub" });
  fctx.body.push({ op: "i32.trunc_sat_f64_u" });
  releaseTempLocal(fctx, tmp);
}
function compileBitwiseBinaryOp(fctx, i32op, unsigned) {
  const tmpR = allocTempLocal(fctx, { kind: "f64" });
  fctx.body.push({ op: "local.set", index: tmpR });
  emitToInt32(fctx);
  fctx.body.push({ op: "local.get", index: tmpR });
  releaseTempLocal(fctx, tmpR);
  emitToInt32(fctx);
  fctx.body.push({ op: i32op });
  fctx.body.push({ op: unsigned ? "f64.convert_i32_u" : "f64.convert_i32_s" });
  return { kind: "f64" };
}
function compileModulo(ctx, fctx, expr) {
  emitModulo(fctx);
  return { kind: "f64" };
}
function emitModulo(fctx) {
  const tmpB = allocTempLocal(fctx, { kind: "f64" });
  const tmpA = allocTempLocal(fctx, { kind: "f64" });
  fctx.body.push({ op: "local.set", index: tmpB });
  fctx.body.push({ op: "local.set", index: tmpA });
  const thenInstrs = [{ op: "local.get", index: tmpA }];
  const elseInstrs = [
    { op: "local.get", index: tmpA },
    { op: "local.get", index: tmpA },
    { op: "local.get", index: tmpB },
    { op: "f64.div" },
    { op: "f64.trunc" },
    // JS % uses truncation toward zero, not floor
    { op: "local.get", index: tmpB },
    { op: "f64.mul" },
    { op: "f64.sub" },
    // Preserve sign of dividend for zero results (-0 % x should be -0)
    { op: "local.get", index: tmpA },
    { op: "f64.copysign" },
  ];
  fctx.body.push({ op: "local.get", index: tmpB });
  fctx.body.push({ op: "f64.abs" });
  fctx.body.push({ op: "f64.const", value: Infinity });
  fctx.body.push({ op: "f64.eq" });
  fctx.body.push({ op: "local.get", index: tmpA });
  fctx.body.push({ op: "f64.abs" });
  fctx.body.push({ op: "f64.const", value: Infinity });
  fctx.body.push({ op: "f64.ne" });
  fctx.body.push({ op: "i32.and" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: { kind: "f64" } },
    then: thenInstrs,
    else: elseInstrs,
  });
  releaseTempLocal(fctx, tmpA);
  releaseTempLocal(fctx, tmpB);
}
function compileBooleanBinaryOp(ctx, fctx, op) {
  switch (op) {
    case ts6.SyntaxKind.EqualsEqualsEqualsToken:
    case ts6.SyntaxKind.EqualsEqualsToken:
      fctx.body.push({ op: "i32.eq" });
      return { kind: "i32" };
    case ts6.SyntaxKind.ExclamationEqualsEqualsToken:
    case ts6.SyntaxKind.ExclamationEqualsToken:
      fctx.body.push({ op: "i32.ne" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanToken:
      fctx.body.push({ op: "i32.lt_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.LessThanEqualsToken:
      fctx.body.push({ op: "i32.le_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanToken:
      fctx.body.push({ op: "i32.gt_s" });
      return { kind: "i32" };
    case ts6.SyntaxKind.GreaterThanEqualsToken:
      fctx.body.push({ op: "i32.ge_s" });
      return { kind: "i32" };
    default:
      return { kind: "i32" };
  }
}

// src/codegen/literals.ts
import ts9 from "typescript";

// src/codegen/statements.ts
import ts8 from "typescript";

// src/codegen/closures.ts
import ts7 from "typescript";
function collectReferencedIdentifiers(node, names) {
  if (ts7.isIdentifier(node)) {
    names.add(node.text);
  }
  if (node.kind === ts7.SyntaxKind.ThisKeyword || node.kind === ts7.SyntaxKind.SuperKeyword) {
    names.add("this");
  }
  ts7.forEachChild(node, (child) => collectReferencedIdentifiers(child, names));
}
function collectWrittenIdentifiers(node, names) {
  if (ts7.isBinaryExpression(node)) {
    const op = node.operatorToken.kind;
    if (
      op === ts7.SyntaxKind.EqualsToken ||
      op === ts7.SyntaxKind.PlusEqualsToken ||
      op === ts7.SyntaxKind.MinusEqualsToken ||
      op === ts7.SyntaxKind.AsteriskEqualsToken ||
      op === ts7.SyntaxKind.SlashEqualsToken ||
      op === ts7.SyntaxKind.PercentEqualsToken ||
      op === ts7.SyntaxKind.AmpersandEqualsToken ||
      op === ts7.SyntaxKind.BarEqualsToken ||
      op === ts7.SyntaxKind.CaretEqualsToken ||
      op === ts7.SyntaxKind.LessThanLessThanEqualsToken ||
      op === ts7.SyntaxKind.GreaterThanGreaterThanEqualsToken ||
      op === ts7.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken ||
      op === ts7.SyntaxKind.AsteriskAsteriskEqualsToken ||
      op === ts7.SyntaxKind.BarBarEqualsToken ||
      op === ts7.SyntaxKind.AmpersandAmpersandEqualsToken ||
      op === ts7.SyntaxKind.QuestionQuestionEqualsToken
    ) {
      if (ts7.isIdentifier(node.left)) {
        names.add(node.left.text);
      }
    }
  } else if (ts7.isPrefixUnaryExpression(node) || ts7.isPostfixUnaryExpression(node)) {
    const op = node.operator;
    if (op === ts7.SyntaxKind.PlusPlusToken || op === ts7.SyntaxKind.MinusMinusToken) {
      if (ts7.isIdentifier(node.operand)) {
        names.add(node.operand.text);
      }
    }
  }
  ts7.forEachChild(node, (child) => collectWrittenIdentifiers(child, names));
}
function promoteAccessorCapturesToGlobals(ctx, fctx, accessorBody) {
  if (!accessorBody) return;
  const referencedNames = /* @__PURE__ */ new Set();
  for (const stmt of accessorBody.statements) {
    collectReferencedIdentifiers(stmt, referencedNames);
  }
  for (const name of referencedNames) {
    if (ctx.capturedGlobals.has(name)) continue;
    if (ctx.moduleGlobals.has(name)) continue;
    const localIdx = fctx.localMap.get(name);
    if (localIdx === void 0) continue;
    if (name === "this") continue;
    if (ctx.funcMap.has(name)) continue;
    const localType =
      localIdx < fctx.params.length
        ? fctx.params[localIdx].type
        : (fctx.locals[localIdx - fctx.params.length]?.type ?? { kind: "f64" });
    const globalType = localType.kind === "ref" ? { kind: "ref_null", typeIdx: localType.typeIdx } : localType;
    const init =
      globalType.kind === "f64"
        ? [{ op: "f64.const", value: 0 }]
        : globalType.kind === "i32"
          ? [{ op: "i32.const", value: 0 }]
          : globalType.kind === "externref"
            ? [{ op: "ref.null.extern" }]
            : globalType.kind === "ref_null"
              ? [{ op: "ref.null", typeIdx: globalType.typeIdx }]
              : [{ op: "i32.const", value: 0 }];
    const globalIdx = nextModuleGlobalIdx(ctx);
    ctx.mod.globals.push({
      name: `__captured_${name}`,
      type: globalType,
      mutable: true,
      init,
    });
    fctx.body.push({ op: "local.get", index: localIdx });
    fctx.body.push({ op: "global.set", index: globalIdx });
    ctx.capturedGlobals.set(name, globalIdx);
    if (localType.kind === "ref") {
      ctx.capturedGlobalsWidened.add(name);
    }
    const tdzFlagLocalIdx = fctx.tdzFlagLocals?.get(name);
    if (tdzFlagLocalIdx !== void 0) {
      const tdzGlobalIdx = nextModuleGlobalIdx(ctx);
      ctx.mod.globals.push({
        name: `__tdz_${name}`,
        type: { kind: "i32" },
        mutable: true,
        init: [{ op: "i32.const", value: 0 }],
      });
      fctx.body.push({ op: "local.get", index: tdzFlagLocalIdx });
      fctx.body.push({ op: "global.set", index: tdzGlobalIdx });
      ctx.tdzGlobals.set(name, tdzGlobalIdx);
    }
    fctx.localMap.delete(name);
  }
}
function collectBindingPatternNames(pattern, names) {
  for (const element of pattern.elements) {
    if (ts7.isOmittedExpression(element)) continue;
    if (ts7.isIdentifier(element.name)) {
      names.add(element.name.text);
    } else if (ts7.isObjectBindingPattern(element.name) || ts7.isArrayBindingPattern(element.name)) {
      collectBindingPatternNames(element.name, names);
    }
  }
}
function isOwnParamName(arrow, name) {
  for (const p of arrow.parameters) {
    if (ts7.isIdentifier(p.name) && p.name.text === name) return true;
    if (ts7.isObjectBindingPattern(p.name) || ts7.isArrayBindingPattern(p.name)) {
      const names = /* @__PURE__ */ new Set();
      collectBindingPatternNames(p.name, names);
      if (names.has(name)) return true;
    }
  }
  return false;
}
function emitArrowParamDestructuring(ctx, fctx, param, paramIdx, paramType) {
  if (ts7.isObjectBindingPattern(param.name)) {
    const pattern = param.name;
    const tsParamType = ctx.checker.getTypeAtLocation(param);
    ensureStructForType(ctx, tsParamType);
    const symName = tsParamType.symbol?.name;
    let typeName =
      symName && symName !== "__type" && symName !== "__object" && ctx.structMap.has(symName)
        ? symName
        : (ctx.anonTypeMap.get(tsParamType) ?? symName);
    if (
      typeName &&
      (typeName === "__type" || typeName === "__object") &&
      !ctx.anonTypeMap.has(tsParamType) &&
      tsParamType.getProperties().length > 0
    ) {
      ensureStructForType(ctx, tsParamType);
      typeName = ctx.anonTypeMap.get(tsParamType) ?? typeName;
    }
    if (!typeName) return;
    const structTypeIdx = ctx.structMap.get(typeName);
    const fields = ctx.structFields.get(typeName);
    if (structTypeIdx === void 0 || !fields) return;
    if (paramType.kind === "externref") {
      const testLocal = allocLocal(fctx, `__destr_test_${fctx.locals.length}`, { kind: "i32" });
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "any.convert_extern" });
      fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx });
      fctx.body.push({ op: "local.set", index: testLocal });
      const structRefType = { kind: "ref_null", typeIdx: structTypeIdx };
      const structPath = collectInstrs(fctx, () => {
        const convertedIdx = allocLocal(fctx, `__destr_ref_${fctx.locals.length}`, structRefType);
        fctx.body.push({ op: "local.get", index: paramIdx });
        fctx.body.push({ op: "any.convert_extern" });
        emitGuardedRefCast(fctx, structTypeIdx);
        fctx.body.push({ op: "local.set", index: convertedIdx });
        for (const element of pattern.elements) {
          if (!ts7.isBindingElement(element)) continue;
          if (ts7.isOmittedExpression(element)) continue;
          if (!ts7.isIdentifier(element.name)) continue;
          const localName = element.name.text;
          const propNameNode = element.propertyName ?? element.name;
          if (!ts7.isIdentifier(propNameNode) && !ts7.isStringLiteral(propNameNode)) continue;
          const propName = propNameNode.text;
          const fieldIdx = fields.findIndex((f) => f.name === propName);
          if (fieldIdx === -1) continue;
          const fieldType = fields[fieldIdx].type;
          const localIdx = allocLocal(fctx, localName, fieldType);
          fctx.body.push({ op: "local.get", index: convertedIdx });
          fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      });
      const externPath = collectInstrs(fctx, () => {
        fctx.body.push({ op: "local.get", index: paramIdx });
        compileExternrefObjectDestructuringDecl(ctx, fctx, pattern, paramType);
      });
      fctx.body.push({ op: "local.get", index: testLocal });
      fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: structPath, else: externPath });
      return;
    }
    const savedBodyAPD = fctx.body;
    const apdInstrs = [];
    fctx.body = apdInstrs;
    const structParamIdx = paramIdx;
    for (const element of pattern.elements) {
      if (!ts7.isBindingElement(element)) continue;
      if (ts7.isOmittedExpression(element)) continue;
      const propNameNode = element.propertyName ?? element.name;
      if (!ts7.isIdentifier(element.name)) {
        continue;
      }
      if (!ts7.isIdentifier(propNameNode) && !ts7.isStringLiteral(propNameNode)) {
        continue;
      }
      const propName = propNameNode;
      const localName = element.name.text;
      const fieldIdx = fields.findIndex((f) => f.name === propName.text);
      if (fieldIdx === -1) continue;
      const fieldType = fields[fieldIdx].type;
      const localIdx = allocLocal(fctx, localName, fieldType);
      fctx.body.push({ op: "local.get", index: structParamIdx });
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
      if (element.initializer) {
        if (fieldType.kind === "externref") {
          const tmpField = allocLocal(fctx, `__dflt_${fctx.locals.length}`, fieldType);
          fctx.body.push({ op: "local.tee", index: tmpField });
          const isUndefIdx = ensureLateImport(ctx, "__extern_is_undefined", [{ kind: "externref" }], [{ kind: "i32" }]);
          flushLateImportShifts(ctx, fctx);
          if (isUndefIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: isUndefIdx });
          } else {
            fctx.body.push({ op: "ref.is_null" });
          }
          const savedBody = pushBody(fctx);
          compileExpression(ctx, fctx, element.initializer, fieldType);
          fctx.body.push({ op: "local.set", index: localIdx });
          const thenInstrs = fctx.body;
          fctx.body = savedBody;
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
            else: [
              { op: "local.get", index: tmpField },
              { op: "local.set", index: localIdx },
            ],
          });
        } else if (fieldType.kind === "ref_null" || fieldType.kind === "ref") {
          const tmpField = allocLocal(fctx, `__dflt_${fctx.locals.length}`, fieldType);
          fctx.body.push({ op: "local.tee", index: tmpField });
          fctx.body.push({ op: "ref.is_null" });
          const savedBody = pushBody(fctx);
          compileExpression(ctx, fctx, element.initializer, fieldType);
          fctx.body.push({ op: "local.set", index: localIdx });
          const thenInstrs = fctx.body;
          fctx.body = savedBody;
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
            else: [
              { op: "local.get", index: tmpField },
              { op: "local.set", index: localIdx },
            ],
          });
        } else if (fieldType.kind === "f64") {
          const tmpField = allocLocal(fctx, `__dflt_${fctx.locals.length}`, fieldType);
          fctx.body.push({ op: "local.tee", index: tmpField });
          fctx.body.push({ op: "local.get", index: tmpField });
          fctx.body.push({ op: "f64.ne" });
          const savedBody = pushBody(fctx);
          compileExpression(ctx, fctx, element.initializer, fieldType);
          fctx.body.push({ op: "local.set", index: localIdx });
          const thenInstrs = fctx.body;
          fctx.body = savedBody;
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
            else: [
              { op: "local.get", index: tmpField },
              { op: "local.set", index: localIdx },
            ],
          });
        } else {
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      } else {
        fctx.body.push({ op: "local.set", index: localIdx });
      }
    }
    fctx.body = savedBodyAPD;
    if (paramType.kind === "ref_null" && apdInstrs.length > 0) {
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: [], else: apdInstrs });
    } else {
      fctx.body.push(...apdInstrs);
    }
  } else if (ts7.isArrayBindingPattern(param.name)) {
    const pattern = param.name;
    if (paramType.kind === "externref") {
      fctx.body.push({ op: "local.get", index: paramIdx });
      compileExternrefArrayDestructuringDecl(ctx, fctx, pattern, paramType);
      return;
    }
    if (paramType.kind !== "ref" && paramType.kind !== "ref_null") return;
    const vecTypeIdx = paramType.typeIdx;
    const innerArrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
    const arrDef = ctx.mod.types[innerArrTypeIdx];
    if (!arrDef || arrDef.kind !== "array") return;
    const innerElemType = arrDef.element;
    const savedBodyAPDA = fctx.body;
    const apdaInstrs = [];
    fctx.body = apdaInstrs;
    for (let i = 0; i < pattern.elements.length; i++) {
      const element = pattern.elements[i];
      if (ts7.isOmittedExpression(element)) continue;
      const bindingElem = element;
      if (!ts7.isIdentifier(bindingElem.name)) continue;
      const localName = bindingElem.name.text;
      const bindingTsType = ctx.checker.getTypeAtLocation(element);
      const bindingWasmType = resolveWasmType(ctx, bindingTsType);
      const localIdx = allocLocal(fctx, localName, bindingWasmType);
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
      fctx.body.push({ op: "i32.const", value: i });
      emitBoundsCheckedArrayGet(fctx, innerArrTypeIdx, innerElemType);
      if (!valTypesMatch(innerElemType, bindingWasmType)) {
        coerceType(ctx, fctx, innerElemType, bindingWasmType);
      }
      if (bindingElem.initializer) {
        if (bindingWasmType.kind === "externref") {
          const tmpElem = allocLocal(fctx, `__ary_dflt_${fctx.locals.length}`, bindingWasmType);
          fctx.body.push({ op: "local.tee", index: tmpElem });
          const isUndefIdx = ensureLateImport(ctx, "__extern_is_undefined", [{ kind: "externref" }], [{ kind: "i32" }]);
          flushLateImportShifts(ctx, fctx);
          if (isUndefIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: isUndefIdx });
          } else {
            fctx.body.push({ op: "ref.is_null" });
          }
          const savedBody = pushBody(fctx);
          compileExpression(ctx, fctx, bindingElem.initializer, bindingWasmType);
          fctx.body.push({ op: "local.set", index: localIdx });
          const thenInstrs = fctx.body;
          fctx.body = savedBody;
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
            else: [
              { op: "local.get", index: tmpElem },
              { op: "local.set", index: localIdx },
            ],
          });
        } else if (bindingWasmType.kind === "ref_null" || bindingWasmType.kind === "ref") {
          const tmpElem = allocLocal(fctx, `__ary_dflt_${fctx.locals.length}`, bindingWasmType);
          fctx.body.push({ op: "local.tee", index: tmpElem });
          fctx.body.push({ op: "ref.is_null" });
          const savedBody = pushBody(fctx);
          compileExpression(ctx, fctx, bindingElem.initializer, bindingWasmType);
          fctx.body.push({ op: "local.set", index: localIdx });
          const thenInstrs = fctx.body;
          fctx.body = savedBody;
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
            else: [
              { op: "local.get", index: tmpElem },
              { op: "local.set", index: localIdx },
            ],
          });
        } else if (bindingWasmType.kind === "f64") {
          const tmpElem = allocLocal(fctx, `__ary_dflt_${fctx.locals.length}`, bindingWasmType);
          fctx.body.push({ op: "local.tee", index: tmpElem });
          fctx.body.push({ op: "local.get", index: tmpElem });
          fctx.body.push({ op: "f64.ne" });
          const savedBody = pushBody(fctx);
          compileExpression(ctx, fctx, bindingElem.initializer, bindingWasmType);
          fctx.body.push({ op: "local.set", index: localIdx });
          const thenInstrs = fctx.body;
          fctx.body = savedBody;
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
            else: [
              { op: "local.get", index: tmpElem },
              { op: "local.set", index: localIdx },
            ],
          });
        } else {
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      } else {
        fctx.body.push({ op: "local.set", index: localIdx });
      }
    }
    fctx.body = savedBodyAPDA;
    if (paramType.kind === "ref_null" && apdaInstrs.length > 0) {
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: [], else: apdaInstrs });
    } else {
      fctx.body.push(...apdaInstrs);
    }
  }
}
function emitParamDefaultCheckInline(fctx, paramIdx, paramType, thenInstrs) {
  if (paramType.kind === "externref") {
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
  } else if (paramType.kind === "ref_null" || paramType.kind === "ref") {
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
  } else if (paramType.kind === "i32") {
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "i32.eqz" });
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
  } else if (paramType.kind === "f64") {
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "f64.ne" });
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
  }
}
function emitArrowParamDefaults(ctx, fctx, arrow, paramOffset) {
  const hasDefaults = arrow.parameters.some((p) => !!p.initializer);
  let tdzFlags;
  if (hasDefaults) {
    if (!fctx.tdzFlagLocals) fctx.tdzFlagLocals = /* @__PURE__ */ new Map();
    tdzFlags = [];
    for (let i = 0; i < arrow.parameters.length; i++) {
      const param = arrow.parameters[i];
      const paramName = ts7.isIdentifier(param.name) ? param.name.text : `__param${i}`;
      const flagIdx = allocLocal(fctx, `__tdz_param_${paramName}`, { kind: "i32" });
      tdzFlags.push(flagIdx);
      fctx.tdzFlagLocals.set(paramName, flagIdx);
    }
  }
  for (let i = 0; i < arrow.parameters.length; i++) {
    const param = arrow.parameters[i];
    if (!param.initializer) {
      if (tdzFlags) {
        fctx.body.push({ op: "i32.const", value: 1 });
        fctx.body.push({ op: "local.set", index: tdzFlags[i] });
      }
      continue;
    }
    const paramIdx = paramOffset + i;
    const paramType = fctx.params[paramIdx]?.type;
    if (!paramType) continue;
    const savedBody = pushBody(fctx);
    compileExpression(ctx, fctx, param.initializer, paramType);
    fctx.body.push({ op: "local.set", index: paramIdx });
    const thenInstrs = fctx.body;
    fctx.body = savedBody;
    emitParamDefaultCheckInline(fctx, paramIdx, paramType, thenInstrs);
    if (tdzFlags) {
      fctx.body.push({ op: "i32.const", value: 1 });
      fctx.body.push({ op: "local.set", index: tdzFlags[i] });
    }
  }
  if (tdzFlags) {
    for (let i = 0; i < arrow.parameters.length; i++) {
      const param = arrow.parameters[i];
      const paramName = ts7.isIdentifier(param.name) ? param.name.text : `__param${i}`;
      fctx.tdzFlagLocals?.delete(paramName);
    }
    if (fctx.tdzFlagLocals?.size === 0) fctx.tdzFlagLocals = void 0;
  }
}
function emitMethodParamDefaults(ctx, fctx, params, paramOffset) {
  const hasDefaults = params.some((p) => !!p.initializer);
  let tdzFlags;
  if (hasDefaults) {
    if (!fctx.tdzFlagLocals) fctx.tdzFlagLocals = /* @__PURE__ */ new Map();
    tdzFlags = [];
    for (let i = 0; i < params.length; i++) {
      const param = params[i];
      const paramName = ts7.isIdentifier(param.name) ? param.name.text : `__param${i}`;
      const flagIdx = allocLocal(fctx, `__tdz_param_${paramName}`, { kind: "i32" });
      tdzFlags.push(flagIdx);
      fctx.tdzFlagLocals.set(paramName, flagIdx);
    }
  }
  for (let i = 0; i < params.length; i++) {
    const param = params[i];
    if (!param.initializer) {
      if (tdzFlags) {
        fctx.body.push({ op: "i32.const", value: 1 });
        fctx.body.push({ op: "local.set", index: tdzFlags[i] });
      }
      continue;
    }
    const paramIdx = paramOffset + i;
    const paramType = fctx.params[paramIdx]?.type;
    if (!paramType) continue;
    const savedBody = pushBody(fctx);
    compileExpression(ctx, fctx, param.initializer, paramType);
    fctx.body.push({ op: "local.set", index: paramIdx });
    const thenInstrs = fctx.body;
    fctx.body = savedBody;
    emitParamDefaultCheckInline(fctx, paramIdx, paramType, thenInstrs);
    if (tdzFlags) {
      fctx.body.push({ op: "i32.const", value: 1 });
      fctx.body.push({ op: "local.set", index: tdzFlags[i] });
    }
  }
  if (tdzFlags) {
    for (let i = 0; i < params.length; i++) {
      const param = params[i];
      const paramName = ts7.isIdentifier(param.name) ? param.name.text : `__param${i}`;
      fctx.tdzFlagLocals?.delete(paramName);
    }
    if (fctx.tdzFlagLocals?.size === 0) fctx.tdzFlagLocals = void 0;
  }
}
function isHostCallbackArgument(node, ctx) {
  const parent = node.parent;
  if (!parent) return false;
  if (ts7.isCallExpression(parent)) {
    if (!parent.arguments.some((arg) => arg === node)) return false;
    if (ts7.isIdentifier(parent.expression)) {
      const calleeName = parent.expression.text;
      const funcIdx = ctx.funcMap.get(calleeName);
      if (funcIdx !== void 0 && funcIdx >= ctx.numImportFuncs) {
        return false;
      }
    }
    return true;
  }
  if (ts7.isNewExpression(parent)) {
    if (!parent.arguments?.some((arg) => arg === node)) return false;
    if (ts7.isIdentifier(parent.expression)) {
      const ctorName = parent.expression.text;
      const newFuncIdx = ctx.funcMap.get(`${ctorName}_new`);
      if (newFuncIdx !== void 0 && newFuncIdx >= ctx.numImportFuncs) {
        return false;
      }
    }
    return true;
  }
  return false;
}
function compileArrowFunction(ctx, fctx, arrow) {
  if (isHostCallbackArgument(arrow, ctx)) {
    return compileArrowAsCallback(ctx, fctx, arrow);
  }
  return compileArrowAsClosure2(ctx, fctx, arrow);
}
function compileArrowAsClosure2(ctx, fctx, arrow) {
  const closureId = ctx.closureCounter++;
  const closureName = `__closure_${closureId}`;
  const body = arrow.body;
  const isGenerator = ts7.isFunctionExpression(arrow) && arrow.asteriskToken !== void 0;
  if (isGenerator) {
    ctx.generatorFunctions.add(closureName);
  }
  const arrowParams = [];
  for (const p of arrow.parameters) {
    const paramType = ctx.checker.getTypeAtLocation(p);
    let wasmType = resolveWasmType(ctx, paramType);
    if (p.initializer && wasmType.kind === "ref") {
      wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
    }
    arrowParams.push(wasmType);
  }
  const sig = ctx.checker.getSignatureFromDeclaration(arrow);
  let closureReturnType = null;
  if (isGenerator) {
    closureReturnType = { kind: "externref" };
  } else if (sig) {
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    if (!isVoidType(retType) && !(retType.flags & ts7.TypeFlags.Never)) {
      closureReturnType = resolveWasmType(ctx, retType);
    }
  }
  if (closureReturnType !== null) {
    const ctxType = ctx.checker.getContextualType(arrow);
    if (ctxType) {
      const ctxCallSigs = ctxType.getCallSignatures?.();
      if (ctxCallSigs && ctxCallSigs.length > 0) {
        const ctxRetType = ctx.checker.getReturnTypeOfSignature(ctxCallSigs[0]);
        if (isVoidType(ctxRetType)) {
          closureReturnType = null;
        }
      }
    }
  }
  const referencedNames = /* @__PURE__ */ new Set();
  if (ts7.isBlock(body)) {
    for (const stmt of body.statements) {
      collectReferencedIdentifiers(stmt, referencedNames);
    }
  } else {
    collectReferencedIdentifiers(body, referencedNames);
  }
  const writtenInClosure = /* @__PURE__ */ new Set();
  if (ts7.isBlock(body)) {
    for (const stmt of body.statements) {
      collectWrittenIdentifiers(stmt, writtenInClosure);
    }
  } else {
    collectWrittenIdentifiers(body, writtenInClosure);
  }
  const writtenInOuter = /* @__PURE__ */ new Set();
  for (const name of referencedNames) {
    if (writtenInClosure.has(name)) continue;
    try {
      const sym = ctx.checker.getSymbolAtLocation(ts7.isBlock(body) ? (body.statements[0] ?? body) : body);
      let enclosing = arrow.parent;
      while (
        enclosing &&
        !ts7.isFunctionDeclaration(enclosing) &&
        !ts7.isFunctionExpression(enclosing) &&
        !ts7.isArrowFunction(enclosing) &&
        !ts7.isMethodDeclaration(enclosing) &&
        !ts7.isConstructorDeclaration(enclosing) &&
        !ts7.isSourceFile(enclosing)
      ) {
        enclosing = enclosing.parent;
      }
      if (enclosing) {
        const outerBody = ts7.isSourceFile(enclosing) ? enclosing : enclosing.body;
        if (outerBody) {
          const outerWrites = /* @__PURE__ */ new Set();
          const collectOuterWrites = (node) => {
            if (node === arrow) return;
            if (ts7.isBinaryExpression(node) && node.operatorToken.kind === ts7.SyntaxKind.EqualsToken) {
              if (ts7.isIdentifier(node.left) && node.left.text === name) {
                outerWrites.add(name);
              }
            }
            if (ts7.isPrefixUnaryExpression(node) || ts7.isPostfixUnaryExpression(node)) {
              if (ts7.isIdentifier(node.operand) && node.operand.text === name) {
                outerWrites.add(name);
              }
            }
            if (
              ts7.isBinaryExpression(node) &&
              node.operatorToken.kind >= ts7.SyntaxKind.PlusEqualsToken &&
              node.operatorToken.kind <= ts7.SyntaxKind.CaretEqualsToken
            ) {
              if (ts7.isIdentifier(node.left) && node.left.text === name) {
                outerWrites.add(name);
              }
            }
            ts7.forEachChild(node, collectOuterWrites);
          };
          if (ts7.isBlock(outerBody)) {
            for (const stmt of outerBody.statements) {
              collectOuterWrites(stmt);
            }
          } else {
            collectOuterWrites(outerBody);
          }
          if (outerWrites.has(name)) {
            writtenInOuter.add(name);
          }
        }
      }
    } catch {}
  }
  const captures = [];
  for (const name of referencedNames) {
    const localIdx = fctx.localMap.get(name);
    if (localIdx === void 0) continue;
    if (ctx.funcMap.has(name)) continue;
    if (isOwnParamName(arrow, name)) continue;
    if (ts7.isFunctionExpression(arrow) && arrow.name && arrow.name.text === name) continue;
    const type =
      localIdx < fctx.params.length
        ? fctx.params[localIdx].type
        : (fctx.locals[localIdx - fctx.params.length]?.type ?? { kind: "f64" });
    const isMutable = writtenInClosure.has(name) || writtenInOuter.has(name);
    const alreadyBoxed = !!fctx.boxedCaptures?.has(name);
    captures.push({ name, type, localIdx, mutable: isMutable, alreadyBoxed });
  }
  const closureResults = closureReturnType ? [closureReturnType] : [];
  let structTypeIdx;
  let liftedFuncTypeIdx;
  let liftedParams;
  const isNamedFuncExpr = ts7.isFunctionExpression(arrow) && arrow.name;
  if (captures.length === 0 && !isNamedFuncExpr) {
    const wrapperTypes = getOrCreateFuncRefWrapperTypes(ctx, arrowParams, closureResults);
    if (wrapperTypes) {
      structTypeIdx = wrapperTypes.structTypeIdx;
      liftedFuncTypeIdx = wrapperTypes.liftedFuncTypeIdx;
      liftedParams = [{ kind: "ref", typeIdx: structTypeIdx }, ...arrowParams];
    } else {
      const structFields = [{ name: "func", type: { kind: "funcref" }, mutable: false }];
      structTypeIdx = ctx.mod.types.length;
      ctx.mod.types.push({
        kind: "struct",
        name: `${closureName}_struct`,
        fields: structFields,
      });
      liftedParams = [{ kind: "ref", typeIdx: structTypeIdx }, ...arrowParams];
      liftedFuncTypeIdx = addFuncType(ctx, liftedParams, closureResults, `${closureName}_type`);
    }
  } else {
    const structFields = [
      { name: "func", type: { kind: "funcref" }, mutable: false },
      ...captures.map((c) => {
        if (c.mutable && !c.alreadyBoxed) {
          const refCellTypeIdx = getOrRegisterRefCellType(ctx, c.type);
          return {
            name: c.name,
            type: { kind: "ref_null", typeIdx: refCellTypeIdx },
            mutable: false,
          };
        }
        if (c.mutable && c.alreadyBoxed) {
          return {
            name: c.name,
            type: c.type,
            mutable: false,
          };
        }
        return {
          name: c.name,
          type: c.type,
          mutable: false,
        };
      }),
    ];
    const wrapperTypes = !isNamedFuncExpr ? getOrCreateFuncRefWrapperTypes(ctx, arrowParams, closureResults) : null;
    structTypeIdx = ctx.mod.types.length;
    if (wrapperTypes) {
      ctx.mod.types.push({
        kind: "struct",
        name: `${closureName}_struct`,
        fields: structFields,
        superTypeIdx: wrapperTypes.structTypeIdx,
      });
      liftedFuncTypeIdx = wrapperTypes.liftedFuncTypeIdx;
      liftedParams = [{ kind: "ref_null", typeIdx: structTypeIdx }, ...arrowParams];
    } else {
      ctx.mod.types.push({
        kind: "struct",
        name: `${closureName}_struct`,
        fields: structFields,
      });
      liftedParams = [{ kind: "ref_null", typeIdx: structTypeIdx }, ...arrowParams];
      liftedFuncTypeIdx = addFuncType(ctx, liftedParams, closureResults, `${closureName}_type`);
    }
  }
  const usesWrapperFuncType =
    captures.length > 0 && !isNamedFuncExpr && !!getOrCreateFuncRefWrapperTypes(ctx, arrowParams, closureResults);
  const selfParamKind = isNamedFuncExpr ? "ref_null" : "ref";
  const selfTypeIdx = usesWrapperFuncType
    ? getOrCreateFuncRefWrapperTypes(ctx, arrowParams, closureResults).structTypeIdx
    : structTypeIdx;
  const liftedFctx = {
    name: closureName,
    params: [
      { name: "__self", type: { kind: selfParamKind, typeIdx: selfTypeIdx } },
      ...arrow.parameters.map((p, i) => ({
        name: ts7.isIdentifier(p.name) ? p.name.text : `__param${i}`,
        type: arrowParams[i] ?? { kind: "f64" },
      })),
    ],
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: closureReturnType,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    labelMap: /* @__PURE__ */ new Map(),
    savedBodies: [],
    enclosingClassName: fctx.enclosingClassName ?? resolveEnclosingClassName(fctx),
    isGenerator,
  };
  for (let i = 0; i < liftedFctx.params.length; i++) {
    liftedFctx.localMap.set(liftedFctx.params[i].name, i);
  }
  let selfLocalForCaptures = 0;
  if (usesWrapperFuncType && captures.length > 0) {
    const castLocal = allocLocal(liftedFctx, "__self_cast", { kind: "ref", typeIdx: structTypeIdx });
    liftedFctx.body.push({ op: "local.get", index: 0 });
    liftedFctx.body.push({ op: "ref.cast", typeIdx: structTypeIdx });
    liftedFctx.body.push({ op: "local.set", index: castLocal });
    selfLocalForCaptures = castLocal;
  }
  for (let i = 0; i < captures.length; i++) {
    const cap = captures[i];
    if (cap.mutable) {
      let refCellTypeIdx;
      let valType;
      if (cap.alreadyBoxed && (cap.type.kind === "ref" || cap.type.kind === "ref_null")) {
        refCellTypeIdx = cap.type.typeIdx;
        const outerBoxed = fctx.boxedCaptures?.get(cap.name);
        valType = outerBoxed?.valType ?? { kind: "f64" };
      } else {
        refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
        valType = cap.type;
      }
      const refCellType = { kind: "ref_null", typeIdx: refCellTypeIdx };
      const localIdx = allocLocal(liftedFctx, cap.name, refCellType);
      liftedFctx.body.push({ op: "local.get", index: selfLocalForCaptures });
      liftedFctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: i + 1 });
      liftedFctx.body.push({ op: "local.set", index: localIdx });
      if (!liftedFctx.boxedCaptures) liftedFctx.boxedCaptures = /* @__PURE__ */ new Map();
      liftedFctx.boxedCaptures.set(cap.name, { refCellTypeIdx, valType });
    } else if (cap.alreadyBoxed && (cap.type.kind === "ref" || cap.type.kind === "ref_null")) {
      const refCellTypeIdx = cap.type.typeIdx;
      const outerBoxed = fctx.boxedCaptures?.get(cap.name);
      const valType = outerBoxed?.valType ?? { kind: "f64" };
      const refCellType = { kind: "ref_null", typeIdx: refCellTypeIdx };
      const localIdx = allocLocal(liftedFctx, cap.name, refCellType);
      liftedFctx.body.push({ op: "local.get", index: selfLocalForCaptures });
      liftedFctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: i + 1 });
      liftedFctx.body.push({ op: "local.set", index: localIdx });
      if (!liftedFctx.boxedCaptures) liftedFctx.boxedCaptures = /* @__PURE__ */ new Map();
      liftedFctx.boxedCaptures.set(cap.name, { refCellTypeIdx, valType });
    } else {
      const localIdx = allocLocal(liftedFctx, cap.name, cap.type);
      liftedFctx.body.push({ op: "local.get", index: selfLocalForCaptures });
      liftedFctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: i + 1 });
      liftedFctx.body.push({ op: "local.set", index: localIdx });
    }
  }
  let funcExprName;
  if (ts7.isFunctionExpression(arrow) && arrow.name) {
    funcExprName = arrow.name.text;
    liftedFctx.localMap.set(funcExprName, 0);
    if (!liftedFctx.readOnlyBindings) liftedFctx.readOnlyBindings = /* @__PURE__ */ new Set();
    liftedFctx.readOnlyBindings.add(funcExprName);
  }
  const savedFunc = ctx.currentFunc;
  if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
  if (savedFunc) ctx.funcStack.push(savedFunc);
  ctx.currentFunc = liftedFctx;
  const closureInfoForSelf = {
    structTypeIdx,
    funcTypeIdx: liftedFuncTypeIdx,
    returnType: closureReturnType,
    paramTypes: arrowParams,
  };
  if (funcExprName) {
    ctx.closureMap.set(funcExprName, closureInfoForSelf);
  }
  emitArrowParamDefaults(
    ctx,
    liftedFctx,
    arrow,
    1,
    /* skip __self */
  );
  for (let pi = 0; pi < arrow.parameters.length; pi++) {
    const param = arrow.parameters[pi];
    if (ts7.isIdentifier(param.name)) continue;
    const paramIdx = pi + 1;
    const paramType = arrowParams[pi];
    const allocBindingLocals = (pattern) => {
      for (const element of pattern.elements) {
        if (ts7.isOmittedExpression(element)) continue;
        if (ts7.isIdentifier(element.name)) {
          const localName = element.name.text;
          if (!liftedFctx.localMap.has(localName)) {
            const elemTsType = ctx.checker.getTypeAtLocation(element);
            const elemWasmType = resolveWasmType(ctx, elemTsType);
            allocLocal(liftedFctx, localName, elemWasmType);
          }
        } else if (ts7.isObjectBindingPattern(element.name) || ts7.isArrayBindingPattern(element.name)) {
          allocBindingLocals(element.name);
        }
      }
    };
    if (ts7.isArrayBindingPattern(param.name)) {
      let handled = false;
      if (paramType.kind === "externref") {
        destructureParamArray(ctx, liftedFctx, paramIdx, param.name, paramType);
        handled = true;
      }
      let resolvedParamType = paramType;
      let srcParamIdx = paramIdx;
      if (!handled && (paramType.kind === "ref" || paramType.kind === "ref_null")) {
        resolvedParamType = paramType;
        srcParamIdx = paramIdx;
      }
      if (resolvedParamType.kind === "ref" || resolvedParamType.kind === "ref_null") {
        const typeIdx = resolvedParamType.typeIdx;
        const typeDef = ctx.mod.types[typeIdx];
        if (typeDef && typeDef.kind === "struct") {
          const arrTypeIdx = getArrTypeIdxFromVec(ctx, typeIdx);
          const arrDef = ctx.mod.types[arrTypeIdx];
          if (arrDef && arrDef.kind === "array") {
            const elemType = arrDef.element;
            const savedBodyFPAD = liftedFctx.body;
            const fpadInstrs = [];
            liftedFctx.body = fpadInstrs;
            for (let ei = 0; ei < param.name.elements.length; ei++) {
              const element = param.name.elements[ei];
              if (ts7.isOmittedExpression(element)) continue;
              if (!ts7.isBindingElement(element)) continue;
              if (element.dotDotDotToken && ts7.isIdentifier(element.name)) {
                const restName = element.name.text;
                const restLenLocal = allocLocal(liftedFctx, `__rest_len_${liftedFctx.locals.length}`, { kind: "i32" });
                liftedFctx.body.push({ op: "local.get", index: srcParamIdx });
                liftedFctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 0 });
                liftedFctx.body.push({ op: "i32.const", value: ei });
                liftedFctx.body.push({ op: "i32.sub" });
                liftedFctx.body.push({ op: "local.set", index: restLenLocal });
                liftedFctx.body.push({ op: "i32.const", value: 0 });
                liftedFctx.body.push({ op: "local.get", index: restLenLocal });
                liftedFctx.body.push({ op: "local.get", index: restLenLocal });
                liftedFctx.body.push({ op: "i32.const", value: 0 });
                liftedFctx.body.push({ op: "i32.lt_s" });
                liftedFctx.body.push({ op: "select" });
                liftedFctx.body.push({ op: "local.set", index: restLenLocal });
                const restArrLocal = allocLocal(liftedFctx, `__rest_arr_${liftedFctx.locals.length}`, {
                  kind: "ref",
                  typeIdx: arrTypeIdx,
                });
                liftedFctx.body.push({ op: "local.get", index: restLenLocal });
                liftedFctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
                liftedFctx.body.push({ op: "local.set", index: restArrLocal });
                liftedFctx.body.push({ op: "local.get", index: restArrLocal });
                liftedFctx.body.push({ op: "i32.const", value: 0 });
                liftedFctx.body.push({ op: "local.get", index: srcParamIdx });
                liftedFctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
                liftedFctx.body.push({ op: "i32.const", value: ei });
                liftedFctx.body.push({ op: "local.get", index: restLenLocal });
                liftedFctx.body.push({ op: "array.copy", dstTypeIdx: arrTypeIdx, srcTypeIdx: arrTypeIdx });
                liftedFctx.body.push({ op: "local.get", index: restLenLocal });
                liftedFctx.body.push({ op: "local.get", index: restArrLocal });
                liftedFctx.body.push({ op: "struct.new", typeIdx });
                const vecType = { kind: "ref_null", typeIdx };
                const restLocal = allocLocal(liftedFctx, restName, vecType);
                liftedFctx.body.push({ op: "local.set", index: restLocal });
                continue;
              }
              if (!ts7.isIdentifier(element.name)) continue;
              const localName = element.name.text;
              const localIdx = allocLocal(liftedFctx, localName, elemType);
              liftedFctx.body.push({ op: "local.get", index: srcParamIdx });
              liftedFctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
              liftedFctx.body.push({ op: "i32.const", value: ei });
              emitBoundsCheckedArrayGet(liftedFctx, arrTypeIdx, elemType);
              liftedFctx.body.push({ op: "local.set", index: localIdx });
            }
            liftedFctx.body = savedBodyFPAD;
            if (resolvedParamType.kind === "ref_null" && fpadInstrs.length > 0) {
              liftedFctx.body.push({ op: "local.get", index: srcParamIdx });
              liftedFctx.body.push({ op: "ref.is_null" });
              liftedFctx.body.push({ op: "if", blockType: { kind: "empty" }, then: [], else: fpadInstrs });
            } else {
              liftedFctx.body.push(...fpadInstrs);
            }
            handled = true;
          } else if (typeDef.fields.length > 0 && typeDef.fields[0].name === "_0") {
            const savedBodyFPAD = liftedFctx.body;
            const fpadInstrs = [];
            liftedFctx.body = fpadInstrs;
            for (let ei = 0; ei < param.name.elements.length; ei++) {
              const element = param.name.elements[ei];
              if (ts7.isOmittedExpression(element)) continue;
              if (!ts7.isBindingElement(element)) continue;
              if (ei >= typeDef.fields.length) break;
              const fieldType = typeDef.fields[ei].type;
              if (!ts7.isIdentifier(element.name)) continue;
              const localName = element.name.text;
              const localIdx = allocLocal(liftedFctx, localName, fieldType);
              liftedFctx.body.push({ op: "local.get", index: srcParamIdx });
              liftedFctx.body.push({ op: "struct.get", typeIdx, fieldIdx: ei });
              liftedFctx.body.push({ op: "local.set", index: localIdx });
            }
            liftedFctx.body = savedBodyFPAD;
            if (resolvedParamType.kind === "ref_null" && fpadInstrs.length > 0) {
              liftedFctx.body.push({ op: "local.get", index: srcParamIdx });
              liftedFctx.body.push({ op: "ref.is_null" });
              liftedFctx.body.push({ op: "if", blockType: { kind: "empty" }, then: [], else: fpadInstrs });
            } else {
              liftedFctx.body.push(...fpadInstrs);
            }
            handled = true;
          }
        }
      }
      if (!handled) {
        allocBindingLocals(param.name);
      }
    } else if (ts7.isObjectBindingPattern(param.name)) {
      let handled = false;
      if (paramType.kind === "ref" || paramType.kind === "ref_null") {
        const typeIdx = paramType.typeIdx;
        const typeDef = ctx.mod.types[typeIdx];
        if (typeDef && typeDef.kind === "struct") {
          let allFound = true;
          const savedBodyFPOD = liftedFctx.body;
          const fpodInstrs = [];
          liftedFctx.body = fpodInstrs;
          for (const element of param.name.elements) {
            if (ts7.isOmittedExpression(element)) continue;
            if (!ts7.isIdentifier(element.name)) continue;
            const localName = element.name.text;
            const propName = element.propertyName
              ? ts7.isIdentifier(element.propertyName)
                ? element.propertyName.text
                : localName
              : localName;
            const fieldIdx = typeDef.fields.findIndex((f) => f.name === propName);
            if (fieldIdx < 0) {
              allFound = false;
              continue;
            }
            const fieldType = typeDef.fields[fieldIdx].type;
            const localIdx = allocLocal(liftedFctx, localName, fieldType);
            liftedFctx.body.push({ op: "local.get", index: paramIdx });
            liftedFctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
            liftedFctx.body.push({ op: "local.set", index: localIdx });
          }
          liftedFctx.body = savedBodyFPOD;
          if (paramType.kind === "ref_null" && fpodInstrs.length > 0) {
            liftedFctx.body.push({ op: "local.get", index: paramIdx });
            liftedFctx.body.push({ op: "ref.is_null" });
            liftedFctx.body.push({ op: "if", blockType: { kind: "empty" }, then: [], else: fpodInstrs });
          } else {
            liftedFctx.body.push(...fpodInstrs);
          }
          handled = allFound;
        }
      }
      if (!handled) {
        allocBindingLocals(param.name);
      }
    }
  }
  if (ts7.isFunctionExpression(arrow) && ts7.isBlock(body) && closureBodyUsesArguments(body)) {
    const hasNumericParam = arrowParams.some((pt) => pt.kind === "f64" || pt.kind === "i32");
    if (hasNumericParam) {
      ensureLateImport(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
      flushLateImportShifts(ctx, liftedFctx);
    }
    const numArgs = arrowParams.length;
    const elemType = { kind: "externref" };
    const vti = getOrRegisterVecType(ctx, "externref", elemType);
    const ati = getArrTypeIdxFromVec(ctx, vti);
    const vecRef = { kind: "ref", typeIdx: vti };
    const argsLocal = allocLocal(liftedFctx, "arguments", vecRef);
    const arrTmp = allocLocal(liftedFctx, "__args_arr_tmp", { kind: "ref", typeIdx: ati });
    for (let i = 0; i < numArgs; i++) {
      liftedFctx.body.push({ op: "local.get", index: i + 1 });
      const pt = arrowParams[i];
      if (pt.kind === "f64") {
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          liftedFctx.body.push({ op: "call", funcIdx: boxIdx });
        } else {
          liftedFctx.body.push({ op: "drop" });
          liftedFctx.body.push({ op: "ref.null.extern" });
        }
      } else if (pt.kind === "i32") {
        liftedFctx.body.push({ op: "f64.convert_i32_s" });
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          liftedFctx.body.push({ op: "call", funcIdx: boxIdx });
        } else {
          liftedFctx.body.push({ op: "drop" });
          liftedFctx.body.push({ op: "ref.null.extern" });
        }
      } else if (pt.kind === "ref" || pt.kind === "ref_null") {
        liftedFctx.body.push({ op: "extern.convert_any" });
      }
    }
    liftedFctx.body.push({ op: "array.new_fixed", typeIdx: ati, length: numArgs });
    liftedFctx.body.push({ op: "local.set", index: arrTmp });
    liftedFctx.body.push({ op: "i32.const", value: numArgs });
    liftedFctx.body.push({ op: "local.get", index: arrTmp });
    liftedFctx.body.push({ op: "struct.new", typeIdx: vti });
    liftedFctx.body.push({ op: "local.set", index: argsLocal });
  }
  let conciseBodyHasValue = false;
  if (ts7.isBlock(body)) {
    hoistLetConstWithTdz(ctx, liftedFctx, body.statements);
  }
  if (isGenerator && ts7.isBlock(body)) {
    const bufferLocal = allocLocal(liftedFctx, "__gen_buffer", { kind: "externref" });
    const createBufIdx = ctx.funcMap.get("__gen_create_buffer");
    liftedFctx.body.push({ op: "call", funcIdx: createBufIdx });
    liftedFctx.body.push({ op: "local.set", index: bufferLocal });
    const bodyInstrs = [];
    const outerBody = liftedFctx.body;
    liftedFctx.body = bodyInstrs;
    liftedFctx.generatorReturnDepth = 0;
    liftedFctx.blockDepth++;
    for (let i = 0; i < liftedFctx.breakStack.length; i++) liftedFctx.breakStack[i]++;
    for (let i = 0; i < liftedFctx.continueStack.length; i++) liftedFctx.continueStack[i]++;
    for (const stmt of body.statements) {
      compileStatement(ctx, liftedFctx, stmt);
    }
    liftedFctx.blockDepth--;
    for (let i = 0; i < liftedFctx.breakStack.length; i++) liftedFctx.breakStack[i]--;
    for (let i = 0; i < liftedFctx.continueStack.length; i++) liftedFctx.continueStack[i]--;
    liftedFctx.generatorReturnDepth = void 0;
    liftedFctx.body = outerBody;
    liftedFctx.body.push({
      op: "block",
      blockType: { kind: "empty" },
      body: bodyInstrs,
    });
    const isAsyncGen = arrow.modifiers?.some((m) => m.kind === 134) ?? false;
    const createGenIdx = ctx.funcMap.get(isAsyncGen ? "__create_async_generator" : "__create_generator");
    liftedFctx.body.push({ op: "local.get", index: bufferLocal });
    liftedFctx.body.push({ op: "call", funcIdx: createGenIdx });
    conciseBodyHasValue = true;
  } else if (ts7.isBlock(body)) {
    for (const stmt of body.statements) {
      compileStatement(ctx, liftedFctx, stmt);
    }
  } else {
    const exprType = compileExpression(ctx, liftedFctx, body);
    if (exprType !== null && closureReturnType) {
      conciseBodyHasValue = true;
      if (exprType.kind !== closureReturnType.kind) {
        const instrs = coercionInstrs(ctx, exprType, closureReturnType, liftedFctx);
        if (instrs.length > 0) {
          liftedFctx.body.push(...instrs);
        } else if (closureReturnType.kind === "externref" && exprType.kind === "f64") {
          closureReturnType = exprType;
          liftedFctx.returnType = exprType;
          closureResults[0] = exprType;
          liftedFuncTypeIdx = addFuncType(ctx, liftedParams, closureResults, `${closureName}_type`);
          closureInfoForSelf.returnType = exprType;
          closureInfoForSelf.funcTypeIdx = liftedFuncTypeIdx;
        }
      }
    } else if (exprType !== null) {
      liftedFctx.body.push({ op: "drop" });
    }
  }
  if (funcExprName) {
    ctx.closureMap.delete(funcExprName);
  }
  if (closureReturnType && !conciseBodyHasValue) {
    const lastInstr = liftedFctx.body[liftedFctx.body.length - 1];
    if (!lastInstr || lastInstr.op !== "return") {
      if (closureReturnType.kind === "f64") {
        liftedFctx.body.push({ op: "f64.const", value: 0 });
      } else if (closureReturnType.kind === "i32") {
        liftedFctx.body.push({ op: "i32.const", value: 0 });
      } else if (closureReturnType.kind === "externref") {
        liftedFctx.body.push({ op: "ref.null.extern" });
      }
    }
  }
  if (savedFunc) ctx.funcStack.pop();
  if (savedFunc) ctx.parentBodiesStack.pop();
  ctx.currentFunc = savedFunc;
  const liftedFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.mod.functions.push({
    name: closureName,
    typeIdx: liftedFuncTypeIdx,
    locals: liftedFctx.locals,
    body: liftedFctx.body,
    exported: false,
  });
  ctx.funcMap.set(closureName, liftedFuncIdx);
  fctx.body.push({ op: "ref.func", funcIdx: liftedFuncIdx });
  for (const cap of captures) {
    if (cap.mutable) {
      if (fctx.boxedCaptures?.has(cap.name)) {
        fctx.body.push({ op: "local.get", index: cap.localIdx });
      } else {
        const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
        fctx.body.push({ op: "local.get", index: cap.localIdx });
        fctx.body.push({ op: "struct.new", typeIdx: refCellTypeIdx });
        const boxedLocalIdx = allocLocal(fctx, `__boxed_${cap.name}`, { kind: "ref_null", typeIdx: refCellTypeIdx });
        fctx.body.push({ op: "local.tee", index: boxedLocalIdx });
        fctx.localMap.set(cap.name, boxedLocalIdx);
        if (!fctx.boxedCaptures) fctx.boxedCaptures = /* @__PURE__ */ new Map();
        fctx.boxedCaptures.set(cap.name, { refCellTypeIdx, valType: cap.type });
      }
    } else {
      fctx.body.push({ op: "local.get", index: cap.localIdx });
    }
  }
  fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
  const closureInfo = {
    structTypeIdx,
    funcTypeIdx: liftedFuncTypeIdx,
    returnType: closureReturnType,
    paramTypes: arrowParams,
  };
  ctx.closureInfoByTypeIdx.set(structTypeIdx, closureInfo);
  const parent = arrow.parent;
  if (ts7.isVariableDeclaration(parent) && ts7.isIdentifier(parent.name)) {
    ctx.closureMap.set(parent.name.text, closureInfo);
  } else if (
    ts7.isBinaryExpression(parent) &&
    parent.operatorToken.kind === ts7.SyntaxKind.EqualsToken &&
    ts7.isIdentifier(parent.left)
  ) {
    const assignName = parent.left.text;
    const currentFctx = ctx.currentFunc;
    const localIdx = currentFctx.localMap.get(assignName);
    if (localIdx !== void 0 && !currentFctx.boxedCaptures?.has(assignName)) {
      ctx.closureMap.set(assignName, closureInfo);
    }
  } else if (ts7.isPropertyAssignment(parent) && ts7.isIdentifier(parent.name)) {
  }
  return { kind: "ref", typeIdx: structTypeIdx };
}
function compileArrowAsCallback(ctx, fctx, arrow) {
  const cbId = ctx.callbackCounter++;
  const cbName = `__cb_${cbId}`;
  const body = arrow.body;
  const referencedNames = /* @__PURE__ */ new Set();
  if (ts7.isBlock(body)) {
    for (const stmt of body.statements) {
      collectReferencedIdentifiers(stmt, referencedNames);
    }
  } else {
    collectReferencedIdentifiers(body, referencedNames);
  }
  const writtenInCallback = /* @__PURE__ */ new Set();
  if (ts7.isBlock(body)) {
    for (const stmt of body.statements) {
      collectWrittenIdentifiers(stmt, writtenInCallback);
    }
  } else {
    collectWrittenIdentifiers(body, writtenInCallback);
  }
  const captures = [];
  for (const name of referencedNames) {
    const localIdx = fctx.localMap.get(name);
    if (localIdx === void 0) continue;
    if (ctx.funcMap.has(name)) continue;
    if (isOwnParamName(arrow, name)) continue;
    const type =
      localIdx < fctx.params.length
        ? fctx.params[localIdx].type
        : (fctx.locals[localIdx - fctx.params.length]?.type ?? { kind: "f64" });
    const isMutable = writtenInCallback.has(name);
    const alreadyBoxed = !!fctx.boxedCaptures?.has(name);
    captures.push({ name, type, localIdx, mutable: isMutable, alreadyBoxed });
  }
  let capStructTypeIdx = -1;
  if (captures.length > 0) {
    const fields = captures.map((cap) => {
      if (cap.mutable && !cap.alreadyBoxed) {
        const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
        return {
          name: cap.name,
          type: { kind: "ref_null", typeIdx: refCellTypeIdx },
          mutable: false,
        };
      }
      if (cap.mutable && cap.alreadyBoxed) {
        return {
          name: cap.name,
          type: cap.type,
          mutable: false,
        };
      }
      return {
        name: cap.name,
        type: cap.type,
        mutable: false,
      };
    });
    capStructTypeIdx = ctx.mod.types.length;
    ctx.mod.types.push({
      kind: "struct",
      name: `__cb_cap_${cbId}`,
      fields,
    });
  }
  const cbResolvedParams = [];
  const cbParams = [{ kind: "externref" }];
  for (const p of arrow.parameters) {
    const paramType = ctx.checker.getTypeAtLocation(p);
    const resolved = resolveWasmType(ctx, paramType);
    cbResolvedParams.push(resolved);
    if (resolved.kind === "ref" || resolved.kind === "ref_null") {
      cbParams.push({ kind: "externref" });
    } else {
      cbParams.push(resolved);
    }
  }
  const sig = ctx.checker.getSignatureFromDeclaration(arrow);
  let cbReturnType = null;
  if (sig) {
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    if (!isVoidType(retType)) {
      cbReturnType = resolveWasmType(ctx, retType);
    }
  }
  const cbResults = cbReturnType ? [cbReturnType] : [];
  const cbTypeIdx = addFuncType(ctx, cbParams, cbResults, `${cbName}_type`);
  const cbFctx = {
    name: cbName,
    params: [
      { name: "__captures", type: { kind: "externref" } },
      ...arrow.parameters.map((p, i) => ({
        name: ts7.isIdentifier(p.name) ? p.name.text : `__param${i}`,
        type: cbParams[i + 1] ?? { kind: "f64" },
      })),
    ],
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: cbReturnType,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    labelMap: /* @__PURE__ */ new Map(),
    savedBodies: [],
    enclosingClassName: fctx.enclosingClassName ?? resolveEnclosingClassName(fctx),
  };
  for (let i = 0; i < cbFctx.params.length; i++) {
    cbFctx.localMap.set(cbFctx.params[i].name, i);
  }
  if (captures.length > 0) {
    const capLocal = allocLocal(cbFctx, `__cap_ref`, { kind: "ref", typeIdx: capStructTypeIdx });
    cbFctx.body.push({ op: "local.get", index: 0 });
    cbFctx.body.push({ op: "any.convert_extern" });
    cbFctx.body.push({ op: "ref.cast", typeIdx: capStructTypeIdx });
    cbFctx.body.push({ op: "local.set", index: capLocal });
    for (let i = 0; i < captures.length; i++) {
      const cap = captures[i];
      const outerBoxed = fctx.boxedCaptures?.get(cap.name);
      if (cap.mutable) {
        let refCellTypeIdx;
        let valType;
        if (cap.alreadyBoxed && (cap.type.kind === "ref" || cap.type.kind === "ref_null")) {
          refCellTypeIdx = cap.type.typeIdx;
          const outerInfo = fctx.boxedCaptures?.get(cap.name);
          valType = outerInfo?.valType ?? { kind: "f64" };
        } else {
          refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
          valType = cap.type;
        }
        const refCellType = { kind: "ref_null", typeIdx: refCellTypeIdx };
        const localIdx = allocLocal(cbFctx, cap.name, refCellType);
        cbFctx.body.push({ op: "local.get", index: capLocal });
        cbFctx.body.push({ op: "struct.get", typeIdx: capStructTypeIdx, fieldIdx: i });
        cbFctx.body.push({ op: "local.set", index: localIdx });
        if (!cbFctx.boxedCaptures) cbFctx.boxedCaptures = /* @__PURE__ */ new Map();
        cbFctx.boxedCaptures.set(cap.name, { refCellTypeIdx, valType });
      } else if (outerBoxed && (cap.type.kind === "ref" || cap.type.kind === "ref_null")) {
        const refCellType = { kind: "ref_null", typeIdx: outerBoxed.refCellTypeIdx };
        const localIdx = allocLocal(cbFctx, cap.name, refCellType);
        cbFctx.body.push({ op: "local.get", index: capLocal });
        cbFctx.body.push({ op: "struct.get", typeIdx: capStructTypeIdx, fieldIdx: i });
        cbFctx.body.push({ op: "local.set", index: localIdx });
        if (!cbFctx.boxedCaptures) cbFctx.boxedCaptures = /* @__PURE__ */ new Map();
        cbFctx.boxedCaptures.set(cap.name, { refCellTypeIdx: outerBoxed.refCellTypeIdx, valType: outerBoxed.valType });
      } else {
        const localIdx = allocLocal(cbFctx, cap.name, cap.type);
        cbFctx.body.push({ op: "local.get", index: capLocal });
        cbFctx.body.push({ op: "struct.get", typeIdx: capStructTypeIdx, fieldIdx: i });
        cbFctx.body.push({ op: "local.set", index: localIdx });
      }
    }
  }
  for (let i = 0; i < cbResolvedParams.length; i++) {
    const resolved = cbResolvedParams[i];
    if (resolved.kind === "ref" || resolved.kind === "ref_null") {
      const paramIdx = i + 1;
      const paramName = cbFctx.params[paramIdx].name;
      const convertedIdx = allocLocal(cbFctx, `__converted_${paramName}`, resolved);
      cbFctx.body.push({ op: "local.get", index: paramIdx });
      coerceType(ctx, cbFctx, { kind: "externref" }, resolved);
      cbFctx.body.push({ op: "local.set", index: convertedIdx });
      cbFctx.localMap.set(paramName, convertedIdx);
    }
  }
  const savedFunc = ctx.currentFunc;
  if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
  if (savedFunc) ctx.funcStack.push(savedFunc);
  ctx.currentFunc = cbFctx;
  emitArrowParamDefaults(
    ctx,
    cbFctx,
    arrow,
    1,
    /* skip __captures */
  );
  for (let i = 0; i < arrow.parameters.length; i++) {
    const param = arrow.parameters[i];
    if (ts7.isObjectBindingPattern(param.name) || ts7.isArrayBindingPattern(param.name)) {
      const resolved = cbResolvedParams[i] ?? { kind: "f64" };
      const paramName = cbFctx.params[1 + i]?.name ?? `__param${i}`;
      const effectiveIdx = cbFctx.localMap.get(paramName) ?? 1 + i;
      emitArrowParamDestructuring(ctx, cbFctx, param, effectiveIdx, resolved);
    }
  }
  if (ts7.isBlock(body)) {
    hoistLetConstWithTdz(ctx, cbFctx, body.statements);
  }
  let exprBodyHasReturnValue = false;
  if (ts7.isBlock(body)) {
    for (const stmt of body.statements) {
      compileStatement(ctx, cbFctx, stmt);
    }
  } else {
    const exprType = compileExpression(ctx, cbFctx, body);
    if (exprType !== null && cbReturnType) {
      exprBodyHasReturnValue = true;
      if (exprType.kind !== cbReturnType.kind) {
        const instrs = coercionInstrs(ctx, exprType, cbReturnType, cbFctx);
        if (instrs.length > 0) {
          cbFctx.body.push(...instrs);
        }
      }
    } else if (exprType !== null) {
      cbFctx.body.push({ op: "drop" });
    }
  }
  if (cbReturnType && !exprBodyHasReturnValue) {
    const lastInstr = cbFctx.body[cbFctx.body.length - 1];
    if (!lastInstr || lastInstr.op !== "return") {
      if (cbReturnType.kind === "f64") {
        cbFctx.body.push({ op: "f64.const", value: 0 });
      } else if (cbReturnType.kind === "i32") {
        cbFctx.body.push({ op: "i32.const", value: 0 });
      } else if (cbReturnType.kind === "externref") {
        cbFctx.body.push({ op: "ref.null.extern" });
      }
    }
  }
  if (savedFunc) ctx.funcStack.pop();
  if (savedFunc) ctx.parentBodiesStack.pop();
  ctx.currentFunc = savedFunc;
  const cbFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.mod.functions.push({
    name: cbName,
    typeIdx: cbTypeIdx,
    locals: cbFctx.locals,
    body: cbFctx.body,
    exported: true,
  });
  ctx.funcMap.set(cbName, cbFuncIdx);
  ctx.mod.exports.push({
    name: cbName,
    desc: { kind: "func", index: cbFuncIdx },
  });
  const makeCallbackIdx = ctx.funcMap.get("__make_callback");
  if (makeCallbackIdx === void 0) {
    ctx.errors.push({
      message: "Missing __make_callback import",
      line: getLine(arrow),
      column: getCol(arrow),
    });
    return null;
  }
  fctx.body.push({ op: "i32.const", value: cbId });
  if (captures.length > 0) {
    const refCellLocals = [];
    for (const cap of captures) {
      if (cap.mutable && !cap.alreadyBoxed) {
        const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
        fctx.body.push({ op: "local.get", index: cap.localIdx });
        fctx.body.push({ op: "struct.new", typeIdx: refCellTypeIdx });
        const refCellLocal = allocLocal(fctx, `__cb_rc_${cap.name}_${cbId}`, {
          kind: "ref_null",
          typeIdx: refCellTypeIdx,
        });
        fctx.body.push({ op: "local.tee", index: refCellLocal });
        refCellLocals.push({ refCellLocal, outerLocalIdx: cap.localIdx, refCellTypeIdx, valType: cap.type });
      } else {
        fctx.body.push({ op: "local.get", index: cap.localIdx });
      }
    }
    fctx.body.push({ op: "struct.new", typeIdx: capStructTypeIdx });
    fctx.body.push({ op: "extern.convert_any" });
    if (refCellLocals.length > 0) {
      const writebacks = [];
      for (const rc of refCellLocals) {
        writebacks.push({ op: "local.get", index: rc.refCellLocal });
        writebacks.push({ op: "ref.as_non_null" });
        writebacks.push({ op: "struct.get", typeIdx: rc.refCellTypeIdx, fieldIdx: 0 });
        writebacks.push({ op: "local.set", index: rc.outerLocalIdx });
      }
      if (!fctx.pendingCallbackWritebacks) fctx.pendingCallbackWritebacks = [];
      fctx.pendingCallbackWritebacks.push(...writebacks);
    }
  } else {
    fctx.body.push({ op: "ref.null.extern" });
  }
  fctx.body.push({ op: "call", funcIdx: makeCallbackIdx });
  return { kind: "externref" };
}
function getFuncSignature(ctx, funcIdx) {
  if (funcIdx < ctx.numImportFuncs) {
    let importFuncCount = 0;
    for (const imp of ctx.mod.imports) {
      if (imp.desc.kind === "func") {
        if (importFuncCount === funcIdx) {
          const typeDef = ctx.mod.types[imp.desc.typeIdx];
          if (typeDef?.kind === "func") return { params: typeDef.params, results: typeDef.results };
          return null;
        }
        importFuncCount++;
      }
    }
  } else {
    const localIdx = funcIdx - ctx.numImportFuncs;
    const func = ctx.mod.functions[localIdx];
    if (func) {
      const typeDef = ctx.mod.types[func.typeIdx];
      if (typeDef?.kind === "func") return { params: typeDef.params, results: typeDef.results };
    }
  }
  return null;
}
function getOrCreateFuncRefWrapperTypes(ctx, userParams, resultTypes) {
  const sigKey = `${userParams.map((p) => p.kind + (p.typeIdx ?? "")).join(",")}->${resultTypes.map((r) => r.kind + (r.typeIdx ?? "")).join(",")}`;
  const cached = ctx.funcRefWrapperCache.get(sigKey);
  if (cached) {
    return { structTypeIdx: cached.structTypeIdx, liftedFuncTypeIdx: cached.funcTypeIdx, closureInfo: cached };
  }
  const closureName = `__fn_wrap_${ctx.closureCounter++}`;
  const structFields = [{ name: "func", type: { kind: "funcref" }, mutable: false }];
  const structTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: `${closureName}_struct`,
    fields: structFields,
    superTypeIdx: -1,
    // non-final, no parent — allows subtypes
  });
  const liftedParams = [{ kind: "ref", typeIdx: structTypeIdx }, ...userParams];
  const liftedFuncTypeIdx = addFuncType(ctx, liftedParams, resultTypes, `${closureName}_type`);
  const closureInfo = {
    structTypeIdx,
    funcTypeIdx: liftedFuncTypeIdx,
    returnType: resultTypes.length > 0 ? resultTypes[0] : null,
    paramTypes: userParams,
  };
  ctx.closureInfoByTypeIdx.set(structTypeIdx, closureInfo);
  ctx.funcRefWrapperCache.set(sigKey, closureInfo);
  return { structTypeIdx, liftedFuncTypeIdx, closureInfo };
}
function emitFuncRefAsClosure(ctx, fctx, funcName, funcIdx) {
  const sig = getFuncSignature(ctx, funcIdx);
  if (!sig) return null;
  const nestedCaptures = ctx.nestedFuncCaptures.get(funcName);
  if (nestedCaptures && nestedCaptures.length > 0) {
    const numCaptures = nestedCaptures.length;
    const userParams2 = sig.params.slice(numCaptures);
    const results = sig.results;
    const wrapperTypes2 = getOrCreateFuncRefWrapperTypes(ctx, userParams2, results);
    if (!wrapperTypes2) return null;
    const captureFields = nestedCaptures.map((_cap, i) => {
      const capParamType = sig.params[i];
      return { name: `cap${i}`, type: capParamType, mutable: false };
    });
    const closureName = `__fn_cap_${funcName}_${ctx.closureCounter++}`;
    const structTypeIdx2 = ctx.mod.types.length;
    ctx.mod.types.push({
      kind: "struct",
      name: `${closureName}_struct`,
      fields: [{ name: "func", type: { kind: "funcref" }, mutable: false }, ...captureFields],
      superTypeIdx: wrapperTypes2.structTypeIdx,
    });
    const liftedFuncTypeIdx2 = wrapperTypes2.liftedFuncTypeIdx;
    const trampolineName2 = `__fn_tramp_${funcName}_${ctx.closureCounter++}`;
    const trampolineBody2 = [];
    const trampolineLocals = [];
    if (numCaptures > 1) {
      trampolineLocals.push({ name: "__casted_self", type: { kind: "ref", typeIdx: structTypeIdx2 } });
    }
    const castedSelfLocal = 1 + userParams2.length;
    trampolineBody2.push({ op: "local.get", index: 0 });
    trampolineBody2.push({ op: "ref.cast", typeIdx: structTypeIdx2 });
    if (numCaptures === 1) {
      trampolineBody2.push({ op: "struct.get", typeIdx: structTypeIdx2, fieldIdx: 1 });
    } else {
      trampolineBody2.push({ op: "local.set", index: castedSelfLocal });
      for (let i = 0; i < numCaptures; i++) {
        trampolineBody2.push({ op: "local.get", index: castedSelfLocal });
        trampolineBody2.push({ op: "struct.get", typeIdx: structTypeIdx2, fieldIdx: i + 1 });
      }
    }
    for (let i = 0; i < userParams2.length; i++) {
      trampolineBody2.push({ op: "local.get", index: i + 1 });
    }
    trampolineBody2.push({ op: "call", funcIdx });
    const trampolineFuncIdx2 = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name: trampolineName2,
      typeIdx: liftedFuncTypeIdx2,
      locals: trampolineLocals,
      body: trampolineBody2,
      exported: false,
    });
    ctx.funcMap.set(trampolineName2, trampolineFuncIdx2);
    const closureInfo2 = {
      structTypeIdx: structTypeIdx2,
      funcTypeIdx: wrapperTypes2.closureInfo.funcTypeIdx,
      returnType: results.length > 0 ? results[0] : null,
      paramTypes: userParams2,
    };
    ctx.closureInfoByTypeIdx.set(structTypeIdx2, closureInfo2);
    fctx.body.push({ op: "ref.func", funcIdx: trampolineFuncIdx2 });
    for (const cap of nestedCaptures) {
      if (cap.mutable && cap.valType) {
        const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.valType);
        if (fctx.boxedCaptures?.has(cap.name)) {
          const currentLocalIdx = fctx.localMap.get(cap.name);
          fctx.body.push({ op: "local.get", index: currentLocalIdx });
        } else {
          fctx.body.push({ op: "local.get", index: cap.outerLocalIdx });
          fctx.body.push({ op: "struct.new", typeIdx: refCellTypeIdx });
          const boxedLocalIdx = allocLocal(fctx, `__boxed_${cap.name}`, {
            kind: "ref",
            typeIdx: refCellTypeIdx,
          });
          fctx.body.push({ op: "local.tee", index: boxedLocalIdx });
          fctx.localMap.set(cap.name, boxedLocalIdx);
          if (!fctx.boxedCaptures) fctx.boxedCaptures = /* @__PURE__ */ new Map();
          fctx.boxedCaptures.set(cap.name, { refCellTypeIdx, valType: cap.valType });
        }
      } else {
        fctx.body.push({ op: "local.get", index: cap.outerLocalIdx });
      }
    }
    fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx2 });
    return { kind: "ref", typeIdx: structTypeIdx2 };
  }
  const userParams = sig.params;
  const wrapperTypes = getOrCreateFuncRefWrapperTypes(ctx, userParams, sig.results);
  if (!wrapperTypes) return null;
  const { structTypeIdx, liftedFuncTypeIdx, closureInfo } = wrapperTypes;
  const trampolineName = `__fn_tramp_${funcName}_${ctx.closureCounter++}`;
  const trampolineBody = [];
  for (let i = 0; i < userParams.length; i++) {
    trampolineBody.push({ op: "local.get", index: i + 1 });
  }
  trampolineBody.push({ op: "call", funcIdx });
  const trampolineFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.mod.functions.push({
    name: trampolineName,
    typeIdx: liftedFuncTypeIdx,
    locals: [],
    body: trampolineBody,
    exported: false,
  });
  ctx.funcMap.set(trampolineName, trampolineFuncIdx);
  fctx.body.push({ op: "ref.func", funcIdx: trampolineFuncIdx });
  fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
  return { kind: "ref", typeIdx: structTypeIdx };
}
function closureBodyUsesArguments(node) {
  if (ts7.isIdentifier(node) && node.text === "arguments") return true;
  if (ts7.isFunctionDeclaration(node) || ts7.isFunctionExpression(node)) {
    return false;
  }
  return ts7.forEachChild(node, closureBodyUsesArguments) ?? false;
}
registerCompileArrowAsClosure(compileArrowAsClosure2);

// src/codegen/statements.ts
function adjustRethrowDepth(fctx, delta) {
  if (fctx.catchRethrowStack) {
    for (let i = 0; i < fctx.catchRethrowStack.length; i++) {
      fctx.catchRethrowStack[i].depth += delta;
    }
  }
}
function emitTdzInit(ctx, fctx, name) {
  const flagIdx = ctx.tdzGlobals.get(name);
  if (flagIdx === void 0) return;
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "global.set", index: flagIdx });
}
function emitTdzCheck(ctx, fctx, name) {
  const flagIdx = ctx.tdzGlobals.get(name);
  if (flagIdx === void 0) return;
  const tagIdx = ensureExnTag(ctx);
  fctx.body.push({ op: "global.get", index: flagIdx });
  fctx.body.push({ op: "i32.eqz" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      // Push error message as externref string, then throw
      emitTdzErrorString(ctx, name),
      { op: "throw", tagIdx },
    ],
    else: [],
  });
}
function emitTdzErrorString(_ctx, _name) {
  return { op: "ref.null.extern" };
}
function inferArrayVecType(ctx, decl) {
  if (!ts8.isIdentifier(decl.name)) return null;
  const varName = decl.name.text;
  let scope = decl;
  while (
    scope &&
    !ts8.isFunctionDeclaration(scope) &&
    !ts8.isFunctionExpression(scope) &&
    !ts8.isArrowFunction(scope) &&
    !ts8.isMethodDeclaration(scope) &&
    !ts8.isSourceFile(scope)
  ) {
    scope = scope.parent;
  }
  if (!scope) return null;
  let inferredElemType = null;
  function visit(node) {
    if (inferredElemType) return;
    if (
      ts8.isBinaryExpression(node) &&
      node.operatorToken.kind === ts8.SyntaxKind.EqualsToken &&
      ts8.isElementAccessExpression(node.left) &&
      ts8.isIdentifier(node.left.expression) &&
      node.left.expression.text === varName
    ) {
      const valType = ctx.checker.getTypeAtLocation(node.right);
      if (!(valType.flags & ts8.TypeFlags.Any)) {
        inferredElemType = valType;
        return;
      }
    }
    if (
      ts8.isCallExpression(node) &&
      ts8.isPropertyAccessExpression(node.expression) &&
      node.expression.name.text === "push" &&
      ts8.isIdentifier(node.expression.expression) &&
      node.expression.expression.text === varName &&
      node.arguments.length >= 1
    ) {
      const valType = ctx.checker.getTypeAtLocation(node.arguments[0]);
      if (!(valType.flags & ts8.TypeFlags.Any)) {
        inferredElemType = valType;
        return;
      }
    }
    ts8.forEachChild(node, visit);
  }
  visit(scope);
  if (!inferredElemType) return null;
  const elemWasm = resolveWasmType(ctx, inferredElemType);
  const elemKey = elemWasm.kind === "ref" || elemWasm.kind === "ref_null" ? `ref_${elemWasm.typeIdx}` : elemWasm.kind;
  const vecTypeIdx = getOrRegisterVecType(ctx, elemKey, elemWasm);
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function markStatementPos(ctx, fctx, stmt, compile2) {
  const pos = getSourcePos(ctx, stmt);
  const bodyLenBefore = fctx.body.length;
  compile2();
  if (pos && fctx.body.length > bodyLenBefore) {
    attachSourcePos(fctx.body[bodyLenBefore], pos);
  }
}
function collectBlockScopedNames(stmt) {
  const names = [];
  for (const s of stmt.statements) {
    if (!ts8.isVariableStatement(s)) continue;
    const flags = s.declarationList.flags;
    if (!(flags & ts8.NodeFlags.Let) && !(flags & ts8.NodeFlags.Const)) continue;
    for (const decl of s.declarationList.declarations) {
      if (ts8.isIdentifier(decl.name)) {
        names.push(decl.name.text);
      } else if (ts8.isObjectBindingPattern(decl.name) || ts8.isArrayBindingPattern(decl.name)) {
        collectBindingPatternNames2(decl.name, names);
      }
    }
  }
  return names;
}
function collectBindingPatternNames2(pattern, names) {
  for (const el of pattern.elements) {
    if (ts8.isOmittedExpression(el)) continue;
    if (ts8.isIdentifier(el.name)) {
      names.push(el.name.text);
    } else if (ts8.isObjectBindingPattern(el.name) || ts8.isArrayBindingPattern(el.name)) {
      collectBindingPatternNames2(el.name, names);
    }
  }
}
function saveBlockScopedShadows(fctx, block) {
  const blockNames = collectBlockScopedNames(block);
  if (blockNames.length === 0) return null;
  let savedLocals = null;
  let savedTdz = null;
  let savedConstBindings = null;
  for (const name of blockNames) {
    if (!savedConstBindings) savedConstBindings = /* @__PURE__ */ new Map();
    savedConstBindings.set(name, fctx.constBindings?.has(name) ?? false);
    fctx.constBindings?.delete(name);
    const existing = fctx.localMap.get(name);
    if (existing !== void 0) {
      if (!savedLocals) savedLocals = /* @__PURE__ */ new Map();
      savedLocals.set(name, existing);
      fctx.localMap.delete(name);
      if (fctx.tdzFlagLocals) {
        const tdzIdx = fctx.tdzFlagLocals.get(name);
        if (tdzIdx !== void 0) {
          if (!savedTdz) savedTdz = /* @__PURE__ */ new Map();
          savedTdz.set(name, tdzIdx);
          fctx.tdzFlagLocals.delete(name);
        }
      }
    }
  }
  if (!savedLocals && !savedTdz && !savedConstBindings) return null;
  return { locals: savedLocals, tdzFlags: savedTdz, constBindings: savedConstBindings };
}
function restoreBlockScopedShadows(fctx, saved) {
  if (!saved) return;
  if (saved.locals) {
    for (const [name, idx] of saved.locals) {
      fctx.localMap.set(name, idx);
    }
  }
  if (saved.tdzFlags) {
    if (!fctx.tdzFlagLocals) fctx.tdzFlagLocals = /* @__PURE__ */ new Map();
    for (const [name, idx] of saved.tdzFlags) {
      fctx.tdzFlagLocals.set(name, idx);
    }
  }
  if (saved.constBindings) {
    if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
    for (const [name, hadConstBinding] of saved.constBindings) {
      if (hadConstBinding) fctx.constBindings.add(name);
      else fctx.constBindings.delete(name);
    }
  }
}
function compileStatement(ctx, fctx, stmt) {
  if (!stmt) {
    ctx.errors.push({
      message: "unexpected undefined AST node in compileStatement",
      line: 0,
      column: 0,
    });
    return;
  }
  try {
    compileStatementInner(ctx, fctx, stmt);
  } catch (e) {
    const msg = e instanceof Error ? e.message : String(e);
    ctx.errors.push({
      message: `Internal error compiling statement: ${msg}`,
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
  }
}
function compileStatementInner(ctx, fctx, stmt) {
  if (ts8.isImportDeclaration(stmt)) return;
  if (ts8.isExportDeclaration(stmt)) return;
  if (ts8.isExportAssignment(stmt)) {
    const resultType = compileExpression2(ctx, fctx, stmt.expression);
    if (resultType !== null) {
      fctx.body.push({ op: "drop" });
    }
    return;
  }
  if (ts8.isVariableStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileVariableStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isReturnStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileReturnStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isIfStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileIfStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isWhileStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileWhileStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isForStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileForStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isBlock(stmt)) {
    const savedLocals = saveBlockScopedShadows(fctx, stmt);
    for (const s of stmt.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedLocals);
    return;
  }
  if (ts8.isExpressionStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => {
      const resultType = compileExpression2(ctx, fctx, stmt.expression);
      if (resultType !== null) {
        fctx.body.push({ op: "drop" });
      }
    });
    return;
  }
  if (ts8.isDoStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileDoWhileStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isSwitchStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileSwitchStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isForOfStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileForOfStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isForInStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileForInStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isLabeledStatement(stmt)) {
    compileLabeledStatement(ctx, fctx, stmt);
    return;
  }
  if (ts8.isBreakStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileBreakStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isContinueStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileContinueStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isThrowStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileThrowStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isTryStatement(stmt)) {
    markStatementPos(ctx, fctx, stmt, () => compileTryStatement(ctx, fctx, stmt));
    return;
  }
  if (ts8.isFunctionDeclaration(stmt)) {
    if (stmt.name && ctx.funcMap.has(stmt.name.text)) return;
    compileNestedFunctionDeclaration(ctx, fctx, stmt);
    return;
  }
  if (ts8.isClassDeclaration(stmt)) {
    compileNestedClassDeclaration(ctx, fctx, stmt);
    return;
  }
  if (stmt.kind === ts8.SyntaxKind.EmptyStatement) {
    return;
  }
  if (stmt.kind === ts8.SyntaxKind.PropertyDeclaration) {
    return;
  }
  if (stmt.kind === ts8.SyntaxKind.SemicolonClassElement) {
    return;
  }
  if (stmt.kind === ts8.SyntaxKind.ClassStaticBlockDeclaration) {
    const staticBlock = stmt;
    if (staticBlock.body) {
      for (const s of staticBlock.body.statements) {
        compileStatement(ctx, fctx, s);
      }
    }
    return;
  }
  ctx.errors.push({
    message: `Unsupported statement: ${ts8.SyntaxKind[stmt.kind]}`,
    line: getLine2(stmt),
    column: getCol2(stmt),
  });
}
var HOST_ARRAY_STRING_METHODS = /* @__PURE__ */ new Set(["split"]);
function isStringMethodReturningHostArray(ctx, expr) {
  if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) return false;
  if (!ts8.isCallExpression(expr)) return false;
  if (!ts8.isPropertyAccessExpression(expr.expression)) return false;
  const method = expr.expression.name.text;
  if (!HOST_ARRAY_STRING_METHODS.has(method)) return false;
  const receiverType = ctx.checker.getTypeAtLocation(expr.expression.expression);
  return isStringType(receiverType);
}
function isPromiseHostCall(ctx, expr) {
  if (!ts8.isCallExpression(expr)) return false;
  if (ts8.isPropertyAccessExpression(expr.expression)) {
    const method = expr.expression.name.text;
    if (
      ts8.isIdentifier(expr.expression.expression) &&
      expr.expression.expression.text === "Promise" &&
      (method === "resolve" ||
        method === "reject" ||
        method === "all" ||
        method === "race" ||
        method === "allSettled" ||
        method === "any")
    ) {
      return true;
    }
    if (method === "then" || method === "catch" || method === "finally") {
      const receiverType = ctx.checker.getTypeAtLocation(expr.expression.expression);
      const symName = receiverType.getSymbol()?.name;
      if (symName === "Promise") return true;
      const apparent = ctx.checker.getApparentType(receiverType);
      if (apparent.getSymbol()?.name === "Promise") return true;
    }
  }
  if (ts8.isNewExpression(expr) && ts8.isIdentifier(expr.expression) && expr.expression.text === "Promise") {
    return true;
  }
  return false;
}
function compileVariableStatement(ctx, fctx, stmt) {
  for (const decl of stmt.declarationList.declarations) {
    if (ts8.isObjectBindingPattern(decl.name)) {
      compileObjectDestructuring(ctx, fctx, decl);
      continue;
    }
    if (ts8.isArrayBindingPattern(decl.name)) {
      compileArrayDestructuring(ctx, fctx, decl);
      continue;
    }
    if (!ts8.isIdentifier(decl.name)) {
      ctx.errors.push({
        message: "Destructuring not supported",
        line: getLine2(decl),
        column: getCol2(decl),
      });
      continue;
    }
    const name = decl.name.text;
    if (stmt.declarationList.flags & ts8.NodeFlags.Const) {
      if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
      fctx.constBindings.add(name);
    }
    if (decl.initializer && ts8.isClassExpression(decl.initializer)) {
      continue;
    }
    if (decl.initializer && (ts8.isArrowFunction(decl.initializer) || ts8.isFunctionExpression(decl.initializer))) {
      const actualType = compileExpression2(ctx, fctx, decl.initializer);
      const closureType = actualType ?? { kind: "externref" };
      const modGlobalIdx = ctx.moduleGlobals.get(name);
      if (modGlobalIdx !== void 0) {
        const globalDef = ctx.mod.globals[localGlobalIdx(ctx, modGlobalIdx)];
        if (globalDef) {
          const nullableType =
            closureType.kind === "ref" ? { kind: "ref_null", typeIdx: closureType.typeIdx } : closureType;
          globalDef.type = nullableType;
          if (nullableType.kind === "ref_null") {
            globalDef.init = [{ op: "ref.null", typeIdx: nullableType.typeIdx }];
          }
        }
        const localIdx2 = allocLocal(fctx, name, closureType);
        fctx.body.push({ op: "local.tee", index: localIdx2 });
        fctx.body.push({ op: "global.set", index: modGlobalIdx });
        emitTdzInit(ctx, fctx, name);
      } else {
        const localIdx2 = allocLocal(fctx, name, closureType);
        emitCoercedLocalSet(ctx, fctx, localIdx2, closureType);
      }
      continue;
    }
    if (
      decl.initializer &&
      ts8.isObjectLiteralExpression(decl.initializer) &&
      decl.initializer.properties.some(
        (p) => ts8.isPropertyAssignment(p) && p.name && ts8.isComputedPropertyName(p.name),
      )
    ) {
      const varType2 = ctx.checker.getTypeAtLocation(decl);
      const tsProps = varType2.getProperties();
      const hasUnresolvedComputed = tsProps.length < decl.initializer.properties.length;
      if (hasUnresolvedComputed) {
        const allComputedResolvable = decl.initializer.properties.every((p) => {
          if (!ts8.isPropertyAssignment(p) || !p.name || !ts8.isComputedPropertyName(p.name)) return true;
          return resolveComputedKeyExpression(ctx, p.name.expression) !== void 0;
        });
        if (!allComputedResolvable) {
          const actualType = compileExpression2(ctx, fctx, decl.initializer);
          const objType = actualType ?? { kind: "externref" };
          const modGlobal = ctx.moduleGlobals.get(name);
          if (modGlobal !== void 0) {
            fctx.body.push({ op: "global.set", index: modGlobal });
            emitTdzInit(ctx, fctx, name);
          } else {
            const localIdx2 = allocLocal(fctx, name, objType);
            fctx.body.push({ op: "local.set", index: localIdx2 });
          }
          continue;
        }
      }
    }
    const moduleGlobalIdx = ctx.moduleGlobals.get(name);
    if (moduleGlobalIdx !== void 0) {
      const shapeInfo = ctx.shapeMap.get(name);
      if (shapeInfo && decl.initializer) {
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "i32.const", value: 4 });
        fctx.body.push({ op: "array.new_default", typeIdx: shapeInfo.arrTypeIdx });
        fctx.body.push({ op: "struct.new", typeIdx: shapeInfo.vecTypeIdx });
        fctx.body.push({ op: "global.set", index: moduleGlobalIdx });
        emitTdzInit(ctx, fctx, name);
        continue;
      }
      if (decl.initializer) {
        const globalDef = ctx.mod.globals[localGlobalIdx(ctx, moduleGlobalIdx)];
        const wasmType2 = globalDef?.type ?? resolveWasmType(ctx, ctx.checker.getTypeAtLocation(decl));
        compileExpression2(ctx, fctx, decl.initializer, wasmType2);
        const moduleGlobalIdxPost = ctx.moduleGlobals.get(name);
        fctx.body.push({ op: "global.set", index: moduleGlobalIdxPost });
      } else {
        const globalDef = ctx.mod.globals[localGlobalIdx(ctx, moduleGlobalIdx)];
        if (globalDef?.type.kind === "externref") {
          emitUndefined(ctx, fctx);
          fctx.body.push({ op: "global.set", index: moduleGlobalIdx });
        }
      }
      emitTdzInit(ctx, fctx, name);
      continue;
    }
    const varType = ctx.checker.getTypeAtLocation(decl);
    let inferredVecType = null;
    if (varType.flags & ts8.TypeFlags.Object) {
      const sym = varType.symbol ?? varType.symbol;
      if (sym?.name === "Array") {
        const typeArgs = ctx.checker.getTypeArguments(varType);
        if (typeArgs?.[0] && typeArgs[0].flags & ts8.TypeFlags.Any) {
          inferredVecType = inferArrayVecType(ctx, decl);
        }
      }
    }
    const widenedStructName = ctx.widenedVarStructMap.get(name);
    const widenedTypeIdx = widenedStructName !== void 0 ? ctx.structMap.get(widenedStructName) : void 0;
    const wasmType =
      widenedTypeIdx !== void 0
        ? { kind: "ref_null", typeIdx: widenedTypeIdx }
        : (inferredVecType ??
          (decl.initializer && isStringMethodReturningHostArray(ctx, decl.initializer)
            ? { kind: "externref" }
            : decl.initializer && isPromiseHostCall(ctx, decl.initializer)
              ? { kind: "externref" }
              : resolveWasmType(ctx, varType)));
    const existingIdx = fctx.localMap.get(name);
    const isVar = !(decl.parent.flags & (ts8.NodeFlags.Let | ts8.NodeFlags.Const));
    const isHoistedLetConst =
      !isVar && existingIdx !== void 0 && existingIdx >= fctx.params.length && fctx.tdzFlagLocals?.has(name);
    const localIdx =
      (isVar || isHoistedLetConst) && existingIdx !== void 0 && existingIdx >= fctx.params.length
        ? existingIdx
        : allocLocal(fctx, name, wasmType);
    if (isVar && existingIdx !== void 0 && existingIdx >= fctx.params.length) {
      const localSlot = fctx.locals[existingIdx - fctx.params.length];
      if (localSlot && (wasmType.kind !== localSlot.type.kind || wasmType.typeIdx !== localSlot.type.typeIdx)) {
        const existingIsRef = localSlot.type.kind === "ref" || localSlot.type.kind === "ref_null";
        const newIsPrimitive =
          wasmType.kind === "f64" ||
          wasmType.kind === "i32" ||
          wasmType.kind === "i64" ||
          wasmType.kind === "externref";
        if (!(existingIsRef && newIsPrimitive)) {
          localSlot.type = wasmType;
        }
      }
    }
    if (decl.initializer) {
      const callSigs = varType.getCallSignatures?.();
      const isCallable = callSigs && callSigs.length > 0 && wasmType.kind === "externref";
      let stackType = wasmType;
      if (isCallable) {
        const actualType = compileExpression2(ctx, fctx, decl.initializer);
        const closureType = actualType ?? { kind: "externref" };
        if (
          (closureType.kind === "ref" || closureType.kind === "ref_null") &&
          ctx.closureInfoByTypeIdx.has(closureType.typeIdx)
        ) {
          if (localIdx >= fctx.params.length) {
            const localSlot = fctx.locals[localIdx - fctx.params.length];
            if (localSlot) localSlot.type = closureType;
          }
          stackType = closureType;
        } else if (closureType.kind === "externref" && callSigs.length > 0) {
          const sig = callSigs[0];
          const sigParamCount = sig.parameters.length;
          const sigRetType = ctx.checker.getReturnTypeOfSignature(sig);
          const sigRetWasm = isVoidType(sigRetType) ? null : resolveWasmType(ctx, sigRetType);
          const sigParamWasmTypes = [];
          for (let i = 0; i < sigParamCount; i++) {
            const paramType = ctx.checker.getTypeOfSymbol(sig.parameters[i]);
            sigParamWasmTypes.push(resolveWasmType(ctx, paramType));
          }
          let matchedClosureInfo;
          for (const [typeIdx, info] of ctx.closureInfoByTypeIdx) {
            if (info.paramTypes.length !== sigParamCount) continue;
            if (sigRetWasm === null && info.returnType !== null) continue;
            if (sigRetWasm !== null && info.returnType === null) continue;
            if (sigRetWasm !== null && info.returnType !== null && sigRetWasm.kind !== info.returnType.kind) continue;
            let paramsMatch = true;
            for (let i = 0; i < sigParamCount; i++) {
              if (sigParamWasmTypes[i].kind !== info.paramTypes[i].kind) {
                paramsMatch = false;
                break;
              }
            }
            if (paramsMatch) {
              matchedClosureInfo = { structTypeIdx: typeIdx, info };
              break;
            }
          }
          if (matchedClosureInfo) {
            fctx.body.push({ op: "any.convert_extern" });
            emitGuardedRefCast(fctx, matchedClosureInfo.structTypeIdx);
            const castType = { kind: "ref_null", typeIdx: matchedClosureInfo.structTypeIdx };
            if (localIdx >= fctx.params.length) {
              const localSlot = fctx.locals[localIdx - fctx.params.length];
              if (localSlot) localSlot.type = castType;
            }
            stackType = castType;
          } else {
            stackType = closureType;
          }
        } else {
          stackType = closureType;
        }
      } else {
        const resultType = compileExpression2(ctx, fctx, decl.initializer, wasmType);
        stackType = resultType ?? wasmType;
        if (resultType && !valTypesMatch(resultType, wasmType)) {
          const bodyLenBeforeCoerce = fctx.body.length;
          coerceType3(ctx, fctx, resultType, wasmType);
          if (fctx.body.length > bodyLenBeforeCoerce) {
            stackType = wasmType;
          }
        }
      }
      emitCoercedLocalSet(ctx, fctx, localIdx, stackType);
    } else if (wasmType.kind === "externref") {
      emitUndefined(ctx, fctx);
      fctx.body.push({ op: "local.set", index: localIdx });
    }
    emitLocalTdzInit(fctx, name);
  }
}
function emitLocalTdzInit(fctx, name) {
  const flagIdx = fctx.tdzFlagLocals?.get(name);
  if (flagIdx === void 0) return;
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "local.set", index: flagIdx });
}
function ensureBindingLocals(ctx, fctx, pattern) {
  for (const element of pattern.elements) {
    if (ts8.isOmittedExpression(element)) continue;
    if (ts8.isIdentifier(element.name)) {
      const name = element.name.text;
      if (fctx.localMap.has(name)) continue;
      const elemType = ctx.checker.getTypeAtLocation(element);
      const wasmType = resolveWasmType(ctx, elemType);
      allocLocal(fctx, name, wasmType);
    } else if (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name)) {
      ensureBindingLocals(ctx, fctx, element.name);
    }
  }
}
function syncDestructuredLocalsToGlobals(ctx, fctx, pattern) {
  for (const element of pattern.elements) {
    if (ts8.isOmittedExpression(element)) continue;
    if (ts8.isBindingElement(element)) {
      if (ts8.isIdentifier(element.name)) {
        const name = element.name.text;
        const moduleGlobalIdx = ctx.moduleGlobals.get(name);
        const localIdx = fctx.localMap.get(name);
        if (moduleGlobalIdx !== void 0 && localIdx !== void 0) {
          const localType = getLocalType(fctx, localIdx);
          const globalDef = ctx.mod.globals[localGlobalIdx(ctx, moduleGlobalIdx)];
          const globalType = globalDef?.type;
          fctx.body.push({ op: "local.get", index: localIdx });
          if (localType && globalType && !valTypesMatch(localType, globalType)) {
            coerceType3(ctx, fctx, localType, globalType);
          }
          fctx.body.push({ op: "global.set", index: moduleGlobalIdx });
        }
      } else if (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name)) {
        syncDestructuredLocalsToGlobals(ctx, fctx, element.name);
      }
    }
  }
}
function collectInstrs(fctx, emitFn) {
  const saved = fctx.body;
  fctx.savedBodies.push(saved);
  fctx.body = [];
  emitFn();
  const instrs = fctx.body;
  fctx.body = saved;
  fctx.savedBodies.pop();
  return instrs;
}
function emitNullGuard(ctx, fctx, srcLocal, isNullable, emitFn) {
  const guardInstrs = collectInstrs(fctx, emitFn);
  if (isNullable && guardInstrs.length > 0) {
    const msg = "TypeError: Cannot destructure 'null' or 'undefined'";
    addStringConstantGlobal(ctx, msg);
    const strIdx = ctx.stringGlobalMap.get(msg);
    const tagIdx = ensureExnTag(ctx);
    const throwInstrs = [
      { op: "global.get", index: strIdx },
      { op: "throw", tagIdx },
    ];
    fctx.body.push({ op: "local.get", index: srcLocal });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: throwInstrs, else: guardInstrs });
  } else {
    fctx.body.push(...guardInstrs);
  }
}
function ensureAsyncIterator(ctx, fctx) {
  const idx = ctx.funcMap.get("__async_iterator");
  if (idx !== void 0) return idx;
  const importsBefore = ctx.numImportFuncs;
  const fnType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
  addImport(ctx, "env", "__async_iterator", { kind: "func", typeIdx: fnType });
  shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
  return ctx.funcMap.get("__async_iterator");
}
function ensureExternIsUndefined(ctx, fctx) {
  const idx = ctx.funcMap.get("__extern_is_undefined");
  if (idx !== void 0) return idx;
  const importsBefore = ctx.numImportFuncs;
  const fnType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }]);
  addImport(ctx, "env", "__extern_is_undefined", { kind: "func", typeIdx: fnType });
  shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
  return ctx.funcMap.get("__extern_is_undefined");
}
function emitExternrefDefaultCheck(ctx, fctx, tmpLocal) {
  const isUndefIdx = ensureExternIsUndefined(ctx, fctx);
  if (isUndefIdx !== void 0) {
    fctx.body.push({ op: "call", funcIdx: isUndefIdx });
  } else {
    fctx.body.push({ op: "ref.is_null" });
  }
}
function emitNestedBindingDefault(ctx, fctx, nestedLocal, valueType, initializer) {
  if (valueType.kind === "ref" || valueType.kind === "ref_null") {
    fctx.body.push({ op: "local.get", index: nestedLocal });
    fctx.body.push({ op: "ref.is_null" });
    const defaultInstrs = collectInstrs(fctx, () => {
      const initType = compileExpression2(ctx, fctx, initializer, valueType);
      if (initType && !valTypesMatch(initType, valueType)) {
        coerceType3(ctx, fctx, initType, valueType);
      }
      fctx.body.push({ op: "local.set", index: nestedLocal });
    });
    if (defaultInstrs.length > 0) {
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: defaultInstrs,
        else: [],
      });
    }
  } else if (valueType.kind === "externref") {
    fctx.body.push({ op: "local.get", index: nestedLocal });
    emitExternrefDefaultCheck(ctx, fctx, nestedLocal);
    const defaultInstrs = collectInstrs(fctx, () => {
      const initType = compileExpression2(ctx, fctx, initializer, valueType);
      if (initType && initType.kind !== "externref") {
        if (initType.kind === "ref" || initType.kind === "ref_null") {
          fctx.body.push({ op: "extern.convert_any" });
        } else if (initType.kind === "f64") {
          const bIdx = ctx.funcMap.get("__box_number");
          if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
        } else if (initType.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
          const bIdx = ctx.funcMap.get("__box_number");
          if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
        }
      }
      fctx.body.push({ op: "local.set", index: nestedLocal });
    });
    if (defaultInstrs.length > 0) {
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: defaultInstrs,
        else: [],
      });
    }
  } else if (valueType.kind === "f64") {
    fctx.body.push({ op: "local.get", index: nestedLocal });
    fctx.body.push({ op: "i64.reinterpret_f64" });
    fctx.body.push({ op: "i64.const", value: 0x7ff00000deadc0den });
    fctx.body.push({ op: "i64.eq" });
    const defaultInstrs = collectInstrs(fctx, () => {
      compileExpression2(ctx, fctx, initializer, valueType);
      fctx.body.push({ op: "local.set", index: nestedLocal });
    });
    if (defaultInstrs.length > 0) {
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: defaultInstrs,
        else: [],
      });
    }
  }
}
function emitDefaultValueCheck(ctx, fctx, fieldType, localIdx, initializer, targetType) {
  const hintType = targetType ?? fieldType;
  const buildElseBranch = (tmpField) => {
    if (targetType && !valTypesMatch(fieldType, targetType)) {
      return collectInstrs(fctx, () => {
        fctx.body.push({ op: "local.get", index: tmpField });
        coerceType3(ctx, fctx, fieldType, targetType);
        fctx.body.push({ op: "local.set", index: localIdx });
      });
    }
    return [
      { op: "local.get", index: tmpField },
      { op: "local.set", index: localIdx },
    ];
  };
  if (fieldType.kind === "externref") {
    const tmpField = allocLocal(fctx, `__dflt_${fctx.locals.length}`, fieldType);
    fctx.body.push({ op: "local.tee", index: tmpField });
    emitExternrefDefaultCheck(ctx, fctx, tmpField);
    const thenInstrs = collectInstrs(fctx, () => {
      compileExpression2(ctx, fctx, initializer, hintType);
      fctx.body.push({ op: "local.set", index: localIdx });
    });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: thenInstrs,
      else: buildElseBranch(tmpField),
    });
  } else if (fieldType.kind === "f64") {
    const tmpField = allocLocal(fctx, `__dflt_${fctx.locals.length}`, fieldType);
    fctx.body.push({ op: "local.tee", index: tmpField });
    fctx.body.push({ op: "i64.reinterpret_f64" });
    fctx.body.push({ op: "i64.const", value: 0x7ff00000deadc0den });
    fctx.body.push({ op: "i64.eq" });
    const thenInstrs = collectInstrs(fctx, () => {
      compileExpression2(ctx, fctx, initializer, hintType);
      fctx.body.push({ op: "local.set", index: localIdx });
    });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: thenInstrs,
      else: buildElseBranch(tmpField),
    });
  } else if (fieldType.kind === "ref_null" || fieldType.kind === "ref") {
    const tmpField = allocLocal(fctx, `__dflt_${fctx.locals.length}`, fieldType);
    fctx.body.push({ op: "local.tee", index: tmpField });
    fctx.body.push({ op: "ref.is_null" });
    const thenInstrs = collectInstrs(fctx, () => {
      compileExpression2(ctx, fctx, initializer, hintType);
      fctx.body.push({ op: "local.set", index: localIdx });
    });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: thenInstrs,
      else: buildElseBranch(tmpField),
    });
  } else {
    if (targetType && !valTypesMatch(fieldType, targetType)) {
      coerceType3(ctx, fctx, fieldType, targetType);
    }
    fctx.body.push({ op: "local.set", index: localIdx });
  }
}
function compileObjectDestructuring(ctx, fctx, decl) {
  if (!decl.initializer) return;
  const pattern = decl.name;
  const bodyLenBefore = fctx.body.length;
  const resultType = compileExpression2(ctx, fctx, decl.initializer);
  if (!resultType) return;
  if (resultType.kind === "externref") {
    compileExternrefObjectDestructuringDecl(ctx, fctx, pattern, resultType);
    return;
  }
  if (resultType.kind === "f64" || resultType.kind === "i32") {
    if (resultType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
    }
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: boxIdx });
      compileExternrefObjectDestructuringDecl(ctx, fctx, pattern, { kind: "externref" });
      return;
    }
  }
  let structTypeIdx;
  let fields;
  let typeName;
  if (resultType.kind === "ref" || resultType.kind === "ref_null") {
    const actualTypeIdx = resultType.typeIdx;
    typeName = ctx.typeIdxToStructName.get(actualTypeIdx);
    if (typeName !== void 0) {
      structTypeIdx = actualTypeIdx;
      fields = ctx.structFields.get(typeName);
    }
  }
  if (structTypeIdx === void 0 || !fields) {
    const initType = ctx.checker.getTypeAtLocation(decl.initializer);
    const symName = initType.symbol?.name;
    typeName =
      symName && symName !== "__type" && symName !== "__object" && ctx.structMap.has(symName)
        ? symName
        : (ctx.anonTypeMap.get(initType) ?? symName);
    if (
      (!typeName || typeName === "__type" || typeName === "__object") &&
      !ctx.anonTypeMap.has(initType) &&
      initType.getProperties().length > 0
    ) {
      ensureStructForType(ctx, initType);
      typeName = ctx.anonTypeMap.get(initType) ?? typeName;
    }
    if (!typeName) {
      if (resultType.kind === "ref" || resultType.kind === "ref_null") {
        fctx.body.push({ op: "extern.convert_any" });
        compileExternrefObjectDestructuringDecl(ctx, fctx, pattern, { kind: "externref" });
        return;
      }
      fctx.body.length = bodyLenBefore;
      ensureBindingLocals(ctx, fctx, pattern);
      ctx.errors.push({
        message: "Cannot destructure: unknown type",
        line: getLine2(decl),
        column: getCol2(decl),
      });
      return;
    }
    structTypeIdx = ctx.structMap.get(typeName);
    fields = ctx.structFields.get(typeName);
    if (structTypeIdx === void 0 || !fields) {
      if (resultType.kind === "ref" || resultType.kind === "ref_null") {
        fctx.body.push({ op: "extern.convert_any" });
        compileExternrefObjectDestructuringDecl(ctx, fctx, pattern, { kind: "externref" });
        return;
      }
      fctx.body.length = bodyLenBefore;
      ensureBindingLocals(ctx, fctx, pattern);
      ctx.errors.push({
        message: `Cannot destructure: not a known struct type: ${typeName}`,
        line: getLine2(decl),
        column: getCol2(decl),
      });
      return;
    }
  }
  const tmpLocal = allocLocal(fctx, `__destruct_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  emitNullGuard(ctx, fctx, tmpLocal, resultType.kind === "ref_null", () => {
    for (const element of pattern.elements) {
      if (!ts8.isBindingElement(element)) continue;
      const propNameNode = element.propertyName ?? element.name;
      const propName = ts8.isIdentifier(propNameNode)
        ? propNameNode
        : ts8.isStringLiteral(propNameNode)
          ? propNameNode
          : ts8.isNumericLiteral(propNameNode)
            ? propNameNode
            : void 0;
      let propNameResolvedText;
      if (!propName && ts8.isComputedPropertyName(propNameNode)) {
        propNameResolvedText = resolveComputedKeyExpression(ctx, propNameNode.expression);
      }
      if (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name)) {
        const nestedPropName =
          element.propertyName && ts8.isIdentifier(element.propertyName) ? element.propertyName : void 0;
        let nestedPropText;
        if (!nestedPropName && element.propertyName && ts8.isComputedPropertyName(element.propertyName)) {
          nestedPropText = resolveComputedKeyExpression(ctx, element.propertyName.expression);
        }
        if (!nestedPropName && !nestedPropText) {
          ensureBindingLocals(ctx, fctx, element.name);
          continue;
        }
        const nFieldIdx = fields.findIndex((f) => f.name === (nestedPropName ? nestedPropName.text : nestedPropText));
        if (nFieldIdx === -1) {
          ensureBindingLocals(ctx, fctx, element.name);
          continue;
        }
        const nField = fields[nFieldIdx];
        if (!nField) {
          ensureBindingLocals(ctx, fctx, element.name);
          continue;
        }
        const nFieldType = nField.type;
        const nestedTmp = allocLocal(fctx, `__destruct_nested_${fctx.locals.length}`, nFieldType);
        fctx.body.push({ op: "local.get", index: tmpLocal });
        fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: nFieldIdx });
        fctx.body.push({ op: "local.set", index: nestedTmp });
        if (ts8.isObjectBindingPattern(element.name) && (nFieldType.kind === "ref" || nFieldType.kind === "ref_null")) {
          const nestedTypeIdx = nFieldType.typeIdx;
          const nestedStructName = ctx.typeIdxToStructName.get(nestedTypeIdx);
          const nestedFields = nestedStructName ? ctx.structFields.get(nestedStructName) : void 0;
          if (nestedFields) {
            emitNullGuard(ctx, fctx, nestedTmp, nFieldType.kind === "ref_null", () => {
              for (const ne of element.name.elements) {
                if (!ts8.isBindingElement(ne)) continue;
                if (!ts8.isIdentifier(ne.name)) continue;
                const nePropNode = ne.propertyName ?? ne.name;
                const nePropText = ts8.isIdentifier(nePropNode)
                  ? nePropNode.text
                  : ts8.isStringLiteral(nePropNode)
                    ? nePropNode.text
                    : void 0;
                if (!nePropText) continue;
                const neLocalName = ne.name.text;
                const neFieldIdx = nestedFields.findIndex((f) => f.name === nePropText);
                if (neFieldIdx === -1) continue;
                const neField = nestedFields[neFieldIdx];
                if (!neField) continue;
                const neFieldType = neField.type;
                const neLocalIdx = allocLocal(fctx, neLocalName, neFieldType);
                fctx.body.push({ op: "local.get", index: nestedTmp });
                fctx.body.push({ op: "struct.get", typeIdx: nestedTypeIdx, fieldIdx: neFieldIdx });
                fctx.body.push({ op: "local.set", index: neLocalIdx });
              }
            });
          } else {
            ensureBindingLocals(ctx, fctx, element.name);
          }
        } else if (
          ts8.isArrayBindingPattern(element.name) &&
          (nFieldType.kind === "ref" || nFieldType.kind === "ref_null")
        ) {
          const nestedVecTypeIdx = nFieldType.typeIdx;
          const nestedArrTypeIdx = getArrTypeIdxFromVec(ctx, nestedVecTypeIdx);
          const nestedArrDef = ctx.mod.types[nestedArrTypeIdx];
          if (nestedArrDef && nestedArrDef.kind === "array") {
            const nestedElemType = nestedArrDef.element;
            emitNullGuard(ctx, fctx, nestedTmp, nFieldType.kind === "ref_null", () => {
              for (let j = 0; j < element.name.elements.length; j++) {
                const ne = element.name.elements[j];
                if (ts8.isOmittedExpression(ne)) continue;
                if (!ts8.isIdentifier(ne.name)) continue;
                const neName = ne.name.text;
                const neLocalIdx = allocLocal(fctx, neName, nestedElemType);
                fctx.body.push({ op: "local.get", index: nestedTmp });
                fctx.body.push({ op: "struct.get", typeIdx: nestedVecTypeIdx, fieldIdx: 1 });
                fctx.body.push({ op: "i32.const", value: j });
                emitBoundsCheckedArrayGet2(fctx, nestedArrTypeIdx, nestedElemType);
                fctx.body.push({ op: "local.set", index: neLocalIdx });
              }
            });
          } else {
            ensureBindingLocals(ctx, fctx, element.name);
          }
        } else {
          ensureBindingLocals(ctx, fctx, element.name);
        }
        continue;
      }
      if (element.dotDotDotToken) {
        if (ts8.isIdentifier(element.name)) {
          const restName = element.name.text;
          let restIdx = fctx.localMap.get(restName);
          if (restIdx === void 0) {
            restIdx = allocLocal(fctx, restName, { kind: "externref" });
          }
          const excludedKeys = [];
          for (const el of pattern.elements) {
            if (!ts8.isBindingElement(el) || el.dotDotDotToken) continue;
            const pn = el.propertyName ?? el.name;
            if (ts8.isIdentifier(pn)) excludedKeys.push(pn.text);
            else if (ts8.isStringLiteral(pn)) excludedKeys.push(pn.text);
            else if (ts8.isNumericLiteral(pn)) excludedKeys.push(pn.text);
          }
          let restObjIdx = ctx.funcMap.get("__extern_rest_object");
          if (restObjIdx === void 0) {
            const importsBefore = ctx.numImportFuncs;
            const restObjType = addFuncType(
              ctx,
              [{ kind: "externref" }, { kind: "externref" }],
              [{ kind: "externref" }],
            );
            addImport(ctx, "env", "__extern_rest_object", { kind: "func", typeIdx: restObjType });
            shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
            restObjIdx = ctx.funcMap.get("__extern_rest_object");
          }
          if (restObjIdx !== void 0) {
            const excludedStr = excludedKeys.join(",");
            addStringConstantGlobal(ctx, excludedStr);
            const excludedStrIdx = ctx.stringGlobalMap.get(excludedStr);
            if (excludedStrIdx !== void 0) {
              fctx.body.push({ op: "local.get", index: tmpLocal });
              fctx.body.push({ op: "extern.convert_any" });
              fctx.body.push({ op: "global.get", index: excludedStrIdx });
              fctx.body.push({ op: "call", funcIdx: restObjIdx });
              fctx.body.push({ op: "local.set", index: restIdx });
            }
          }
        }
        continue;
      }
      if (!ts8.isIdentifier(element.name)) continue;
      const localName = element.name.text;
      if (!propName && !propNameResolvedText) continue;
      const propNameText = propName ? propName.text : propNameResolvedText;
      const fieldIdx = fields.findIndex((f) => f.name === propNameText);
      if (fieldIdx === -1) {
        ctx.errors.push({
          message: `Unknown field in destructuring: ${propNameText}`,
          line: getLine2(element),
          column: getCol2(element),
        });
        continue;
      }
      const field = fields[fieldIdx];
      if (!field) continue;
      const fieldType = field.type;
      const localIdx = allocLocal(fctx, localName, fieldType);
      fctx.body.push({ op: "local.get", index: tmpLocal });
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
      if (element.initializer) {
        emitDefaultValueCheck(ctx, fctx, fieldType, localIdx, element.initializer);
      } else {
        fctx.body.push({ op: "local.set", index: localIdx });
      }
    }
  });
  syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
}
function compileExternrefObjectDestructuringDecl(ctx, fctx, pattern, resultType) {
  const tmpLocal = allocLocal(fctx, `__ext_obj_destruct_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  let getIdx = ctx.funcMap.get("__extern_get");
  if (getIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const getType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__extern_get", { kind: "func", typeIdx: getType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (getIdx === void 0) {
    ensureBindingLocals(ctx, fctx, pattern);
    return;
  }
  ensureBindingLocals(ctx, fctx, pattern);
  const isNullable = resultType.kind === "externref" || resultType.kind === "ref_null";
  emitNullGuard(ctx, fctx, tmpLocal, isNullable, () => {
    const excludedKeys = [];
    for (const element of pattern.elements) {
      if (!ts8.isBindingElement(element) || element.dotDotDotToken) continue;
      const pn = element.propertyName ?? element.name;
      if (ts8.isIdentifier(pn)) excludedKeys.push(pn.text);
      else if (ts8.isStringLiteral(pn)) excludedKeys.push(pn.text);
      else if (ts8.isNumericLiteral(pn)) excludedKeys.push(pn.text);
    }
    for (const element of pattern.elements) {
      if (!ts8.isBindingElement(element)) continue;
      if (element.dotDotDotToken) {
        if (ts8.isIdentifier(element.name)) {
          const restName = element.name.text;
          let restIdx = fctx.localMap.get(restName);
          if (restIdx === void 0) {
            restIdx = allocLocal(fctx, restName, { kind: "externref" });
          }
          let restObjIdx = ctx.funcMap.get("__extern_rest_object");
          if (restObjIdx === void 0) {
            const importsBefore = ctx.numImportFuncs;
            const restObjType = addFuncType(
              ctx,
              [{ kind: "externref" }, { kind: "externref" }],
              [{ kind: "externref" }],
            );
            addImport(ctx, "env", "__extern_rest_object", { kind: "func", typeIdx: restObjType });
            shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
            restObjIdx = ctx.funcMap.get("__extern_rest_object");
            getIdx = ctx.funcMap.get("__extern_get");
          }
          if (restObjIdx !== void 0) {
            const excludedStr = excludedKeys.join(",");
            addStringConstantGlobal(ctx, excludedStr);
            const excludedStrIdx = ctx.stringGlobalMap.get(excludedStr);
            if (excludedStrIdx !== void 0) {
              fctx.body.push({ op: "local.get", index: tmpLocal });
              fctx.body.push({ op: "global.get", index: excludedStrIdx });
              fctx.body.push({ op: "call", funcIdx: restObjIdx });
              fctx.body.push({ op: "local.set", index: restIdx });
            }
          }
        }
        continue;
      }
      const propNameNode = element.propertyName ?? element.name;
      let propNameText;
      if (ts8.isIdentifier(propNameNode)) {
        propNameText = propNameNode.text;
      } else if (ts8.isStringLiteral(propNameNode)) {
        propNameText = propNameNode.text;
      } else if (ts8.isNumericLiteral(propNameNode)) {
        propNameText = propNameNode.text;
      }
      if (!propNameText) continue;
      addStringConstantGlobal(ctx, propNameText);
      const strGlobalIdx = ctx.stringGlobalMap.get(propNameText);
      if (strGlobalIdx === void 0) continue;
      getIdx = ctx.funcMap.get("__extern_get");
      if (getIdx === void 0) continue;
      fctx.body.push({ op: "local.get", index: tmpLocal });
      fctx.body.push({ op: "global.get", index: strGlobalIdx });
      fctx.body.push({ op: "call", funcIdx: getIdx });
      const elemType = { kind: "externref" };
      if (ts8.isIdentifier(element.name)) {
        const localName = element.name.text;
        let localIdx = fctx.localMap.get(localName);
        if (localIdx === void 0) {
          localIdx = allocLocal(fctx, localName, elemType);
        }
        const localType = getLocalType(fctx, localIdx);
        if (element.initializer) {
          const tmpElem = allocLocal(fctx, `__ext_obj_dflt_${fctx.locals.length}`, elemType);
          fctx.body.push({ op: "local.tee", index: tmpElem });
          emitExternrefDefaultCheck(ctx, fctx, tmpElem);
          const thenInstrs = collectInstrs(fctx, () => {
            compileExpression2(ctx, fctx, element.initializer, localType ?? elemType);
            fctx.body.push({ op: "local.set", index: localIdx });
          });
          const elseCoerce =
            localType && !valTypesMatch(elemType, localType)
              ? collectInstrs(fctx, () => {
                  coerceType3(ctx, fctx, elemType, localType);
                })
              : [];
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
            else: [{ op: "local.get", index: tmpElem }, ...elseCoerce, { op: "local.set", index: localIdx }],
          });
        } else {
          if (localType && !valTypesMatch(elemType, localType)) {
            coerceType3(ctx, fctx, elemType, localType);
          }
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      } else if (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name)) {
        const nestedLocal = allocLocal(fctx, `__ext_nested_${fctx.locals.length}`, elemType);
        fctx.body.push({ op: "local.set", index: nestedLocal });
        ensureBindingLocals(ctx, fctx, element.name);
        if (ts8.isObjectBindingPattern(element.name)) {
          fctx.body.push({ op: "local.get", index: nestedLocal });
          compileExternrefObjectDestructuringDecl(ctx, fctx, element.name, elemType);
        } else {
          fctx.body.push({ op: "local.get", index: nestedLocal });
          compileExternrefArrayDestructuringDecl(ctx, fctx, element.name, elemType);
        }
      }
    }
  });
  syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
}
function compileExternrefArrayDestructuringDecl(ctx, fctx, pattern, resultType) {
  const tmpLocal = allocLocal(fctx, `__ext_arr_destruct_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  let getIdx = ctx.funcMap.get("__extern_get");
  if (getIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const getType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__extern_get", { kind: "func", typeIdx: getType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (getIdx === void 0) {
    ensureBindingLocals(ctx, fctx, pattern);
    return;
  }
  let boxIdx = ctx.funcMap.get("__box_number");
  if (boxIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const boxType = addFuncType(ctx, [{ kind: "f64" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__box_number", { kind: "func", typeIdx: boxType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    boxIdx = ctx.funcMap.get("__box_number");
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (boxIdx === void 0 || getIdx === void 0) {
    ensureBindingLocals(ctx, fctx, pattern);
    return;
  }
  ensureBindingLocals(ctx, fctx, pattern);
  for (let i = 0; i < pattern.elements.length; i++) {
    const element = pattern.elements[i];
    if (ts8.isOmittedExpression(element)) continue;
    if (!ts8.isBindingElement(element)) continue;
    if (element.dotDotDotToken) {
      if (ts8.isIdentifier(element.name)) {
        const restName = element.name.text;
        let restIdx = fctx.localMap.get(restName);
        if (restIdx === void 0) {
          restIdx = allocLocal(fctx, restName, { kind: "externref" });
        }
        let sliceIdx = ctx.funcMap.get("__extern_slice");
        if (sliceIdx === void 0) {
          const importsBefore = ctx.numImportFuncs;
          const sliceType = addFuncType(ctx, [{ kind: "externref" }, { kind: "f64" }], [{ kind: "externref" }]);
          addImport(ctx, "env", "__extern_slice", { kind: "func", typeIdx: sliceType });
          shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
          sliceIdx = ctx.funcMap.get("__extern_slice");
          boxIdx = ctx.funcMap.get("__box_number");
          getIdx = ctx.funcMap.get("__extern_get");
        }
        if (sliceIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "f64.const", value: i });
          fctx.body.push({ op: "call", funcIdx: sliceIdx });
          fctx.body.push({ op: "local.set", index: restIdx });
        }
      }
      continue;
    }
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "f64.const", value: i });
    fctx.body.push({ op: "call", funcIdx: boxIdx });
    fctx.body.push({ op: "call", funcIdx: getIdx });
    const elemType = { kind: "externref" };
    if (ts8.isIdentifier(element.name)) {
      const localName = element.name.text;
      let localIdx = fctx.localMap.get(localName);
      if (localIdx === void 0) {
        localIdx = allocLocal(fctx, localName, elemType);
      }
      const localType = getLocalType(fctx, localIdx);
      if (element.initializer) {
        const tmpElem = allocLocal(fctx, `__ext_dflt_${fctx.locals.length}`, elemType);
        fctx.body.push({ op: "local.tee", index: tmpElem });
        emitExternrefDefaultCheck(ctx, fctx, tmpElem);
        const thenInstrs = collectInstrs(fctx, () => {
          compileExpression2(ctx, fctx, element.initializer, localType ?? elemType);
          fctx.body.push({ op: "local.set", index: localIdx });
        });
        const elseCoerce =
          localType && !valTypesMatch(elemType, localType)
            ? collectInstrs(fctx, () => {
                coerceType3(ctx, fctx, elemType, localType);
              })
            : [];
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: thenInstrs,
          else: [{ op: "local.get", index: tmpElem }, ...elseCoerce, { op: "local.set", index: localIdx }],
        });
      } else {
        if (localType && !valTypesMatch(elemType, localType)) {
          coerceType3(ctx, fctx, elemType, localType);
        }
        fctx.body.push({ op: "local.set", index: localIdx });
      }
    } else if (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name)) {
      const nestedLocal = allocLocal(fctx, `__ext_arr_nested_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.set", index: nestedLocal });
      if (element.initializer) {
        fctx.body.push({ op: "local.get", index: nestedLocal });
        emitExternrefDefaultCheck(ctx, fctx, nestedLocal);
        const defaultInstrs = collectInstrs(fctx, () => {
          const initType = compileExpression2(ctx, fctx, element.initializer, elemType);
          if (initType && initType.kind !== "externref") {
            if (initType.kind === "ref" || initType.kind === "ref_null") {
              fctx.body.push({ op: "extern.convert_any" });
            } else if (initType.kind === "f64") {
              const bIdx = ctx.funcMap.get("__box_number");
              if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
            } else if (initType.kind === "i32") {
              fctx.body.push({ op: "f64.convert_i32_s" });
              const bIdx = ctx.funcMap.get("__box_number");
              if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
            }
          }
          fctx.body.push({ op: "local.set", index: nestedLocal });
        });
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: defaultInstrs,
          else: [],
        });
      }
      ensureBindingLocals(ctx, fctx, element.name);
      if (ts8.isObjectBindingPattern(element.name)) {
        fctx.body.push({ op: "local.get", index: nestedLocal });
        compileExternrefObjectDestructuringDecl(ctx, fctx, element.name, elemType);
      } else {
        fctx.body.push({ op: "local.get", index: nestedLocal });
        compileExternrefArrayDestructuringDecl(ctx, fctx, element.name, elemType);
      }
    }
  }
}
function compileArrayDestructuring(ctx, fctx, decl) {
  if (!decl.initializer) return;
  const pattern = decl.name;
  const bodyLenBefore = fctx.body.length;
  const patternHasRest = pattern.elements.some((el) => ts8.isBindingElement(el) && el.dotDotDotToken);
  if (patternHasRest) ctx._arrayLiteralForceVec = true;
  const resultType = compileExpression2(ctx, fctx, decl.initializer);
  if (patternHasRest) ctx._arrayLiteralForceVec = false;
  if (!resultType) return;
  if (resultType.kind !== "ref" && resultType.kind !== "ref_null") {
    if (resultType.kind === "externref") {
      compileExternrefArrayDestructuringDecl(ctx, fctx, pattern, resultType);
      syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
      return;
    }
    if (resultType.kind === "f64" || resultType.kind === "i32") {
      if (resultType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
      }
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
        compileExternrefArrayDestructuringDecl(ctx, fctx, pattern, { kind: "externref" });
        syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
        return;
      }
    }
    fctx.body.length = bodyLenBefore;
    ensureBindingLocals(ctx, fctx, pattern);
    ctx.errors.push({
      message: "Cannot destructure: not an array type",
      line: getLine2(decl),
      column: getCol2(decl),
    });
    return;
  }
  const typeIdx = resultType.typeIdx;
  const typeDef = ctx.mod.types[typeIdx];
  if (!typeDef || typeDef.kind !== "struct") {
    fctx.body.push({ op: "extern.convert_any" });
    compileExternrefArrayDestructuringDecl(ctx, fctx, pattern, { kind: "externref" });
    syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
    return;
  }
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, typeIdx);
  const arrDef = ctx.mod.types[arrTypeIdx];
  const isVecArray = arrDef && arrDef.kind === "array";
  const isTupleStruct =
    !isVecArray &&
    typeDef.kind === "struct" &&
    (typeDef.fields.length === 0 || typeDef.fields.every((f, idx) => f.name === `_${idx}`));
  const isStringStruct =
    ctx.nativeStrings &&
    ctx.anyStrTypeIdx >= 0 &&
    (typeIdx === ctx.anyStrTypeIdx || typeIdx === ctx.nativeStrTypeIdx || typeIdx === ctx.consStrTypeIdx);
  if (!isVecArray && !isTupleStruct && !isStringStruct) {
    fctx.body.push({ op: "extern.convert_any" });
    compileExternrefArrayDestructuringDecl(ctx, fctx, pattern, { kind: "externref" });
    syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
    return;
  }
  if (isStringStruct) {
    compileStringDestructuring(ctx, fctx, pattern, resultType, bodyLenBefore);
    return;
  }
  const tmpLocal = allocLocal(fctx, `__destruct_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  const isNullableArr = resultType.kind === "ref_null";
  const hasRestElement = pattern.elements.some((el) => ts8.isBindingElement(el) && el.dotDotDotToken);
  if (isTupleStruct && hasRestElement) {
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "extern.convert_any" });
    compileExternrefArrayDestructuringDecl(ctx, fctx, pattern, { kind: "externref" });
    syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
    return;
  }
  if (isTupleStruct) {
    const tupleFields = typeDef.fields;
    ensureBindingLocals(ctx, fctx, pattern);
    emitNullGuard(ctx, fctx, tmpLocal, isNullableArr, () => {
      for (let i = 0; i < pattern.elements.length; i++) {
        const element = pattern.elements[i];
        if (ts8.isOmittedExpression(element)) continue;
        if (i >= tupleFields.length) {
          if (ts8.isBindingElement(element) && element.initializer) {
            if (ts8.isIdentifier(element.name)) {
              const localName2 = element.name.text;
              const localIdx2 = fctx.localMap.get(localName2);
              if (localIdx2 !== void 0) {
                const localType2 = fctx.locals[localIdx2].type;
                compileExpression2(ctx, fctx, element.initializer, localType2);
                fctx.body.push({ op: "local.set", index: localIdx2 });
              }
            } else if (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name)) {
              ensureBindingLocals(ctx, fctx, element.name);
              ctx._arrayLiteralForceVec = true;
              let initType;
              try {
                initType = compileExpression2(ctx, fctx, element.initializer);
              } finally {
                ctx._arrayLiteralForceVec = false;
              }
              if (initType) {
                if (
                  (initType.kind === "ref" || initType.kind === "ref_null") &&
                  ts8.isObjectBindingPattern(element.name)
                ) {
                  const initTypeIdx = initType.typeIdx;
                  const tmpObjLocal = allocLocal(fctx, `__dflt_obj_${fctx.locals.length}`, initType);
                  fctx.body.push({ op: "local.set", index: tmpObjLocal });
                  const nestedStructName = ctx.typeIdxToStructName.get(initTypeIdx);
                  const nestedFields = nestedStructName ? ctx.structFields.get(nestedStructName) : void 0;
                  if (nestedFields) {
                    for (const nestedElem of element.name.elements) {
                      if (!ts8.isBindingElement(nestedElem)) continue;
                      const propNNode = nestedElem.propertyName ?? nestedElem.name;
                      const propNText = ts8.isIdentifier(propNNode)
                        ? propNNode.text
                        : ts8.isStringLiteral(propNNode)
                          ? propNNode.text
                          : ts8.isNumericLiteral(propNNode)
                            ? propNNode.text
                            : void 0;
                      if (!ts8.isIdentifier(nestedElem.name)) continue;
                      if (!propNText) continue;
                      const nLocalName = nestedElem.name.text;
                      const nFieldIdx = nestedFields.findIndex((f) => f.name === propNText);
                      if (nFieldIdx === -1) continue;
                      const nFieldEntry = nestedFields[nFieldIdx];
                      if (!nFieldEntry) continue;
                      const nLocalIdx = fctx.localMap.get(nLocalName);
                      if (nLocalIdx === void 0) continue;
                      fctx.body.push({ op: "local.get", index: tmpObjLocal });
                      fctx.body.push({ op: "struct.get", typeIdx: initTypeIdx, fieldIdx: nFieldIdx });
                      const localType2 = getLocalType(fctx, nLocalIdx);
                      const fType = nFieldEntry.type;
                      if (localType2 && !valTypesMatch(fType, localType2)) {
                        coerceType3(ctx, fctx, fType, localType2);
                      }
                      fctx.body.push({ op: "local.set", index: nLocalIdx });
                    }
                  }
                } else if (
                  (initType.kind === "ref" || initType.kind === "ref_null") &&
                  ts8.isArrayBindingPattern(element.name)
                ) {
                  const initTypeIdx = initType.typeIdx;
                  const initTypeDef = ctx.mod.types[initTypeIdx];
                  if (initTypeDef && initTypeDef.kind === "struct") {
                    const initArrTypeIdx = getArrTypeIdxFromVec(ctx, initTypeIdx);
                    const initArrDef = ctx.mod.types[initArrTypeIdx];
                    if (initArrDef && initArrDef.kind === "array") {
                      const tmpVecLocal = allocLocal(fctx, `__dflt_vec_${fctx.locals.length}`, initType);
                      fctx.body.push({ op: "local.set", index: tmpVecLocal });
                      const initElemType = initArrDef.element;
                      for (let j = 0; j < element.name.elements.length; j++) {
                        const ne = element.name.elements[j];
                        if (ts8.isOmittedExpression(ne)) continue;
                        if (!ts8.isBindingElement(ne) || !ts8.isIdentifier(ne.name)) continue;
                        const nName = ne.name.text;
                        const nLocalIdx = fctx.localMap.get(nName);
                        if (nLocalIdx === void 0) continue;
                        fctx.body.push({ op: "local.get", index: tmpVecLocal });
                        fctx.body.push({ op: "struct.get", typeIdx: initTypeIdx, fieldIdx: 1 });
                        fctx.body.push({ op: "i32.const", value: j });
                        emitBoundsCheckedArrayGet2(fctx, initArrTypeIdx, initElemType);
                        const localType2 = getLocalType(fctx, nLocalIdx);
                        if (localType2 && !valTypesMatch(initElemType, localType2)) {
                          coerceType3(ctx, fctx, initElemType, localType2);
                        }
                        fctx.body.push({ op: "local.set", index: nLocalIdx });
                      }
                    } else {
                      const tupleDefFields = initTypeDef.fields;
                      const tmpTupleLocal = allocLocal(fctx, `__dflt_tuple_${fctx.locals.length}`, initType);
                      fctx.body.push({ op: "local.set", index: tmpTupleLocal });
                      for (let j = 0; j < element.name.elements.length; j++) {
                        const ne = element.name.elements[j];
                        if (ts8.isOmittedExpression(ne)) continue;
                        if (!ts8.isBindingElement(ne) || !ts8.isIdentifier(ne.name)) continue;
                        if (j >= tupleDefFields.length) break;
                        const nName = ne.name.text;
                        const nLocalIdx = fctx.localMap.get(nName);
                        if (nLocalIdx === void 0) continue;
                        const tfType = tupleDefFields[j].type;
                        fctx.body.push({ op: "local.get", index: tmpTupleLocal });
                        fctx.body.push({ op: "struct.get", typeIdx: initTypeIdx, fieldIdx: j });
                        const localType2 = getLocalType(fctx, nLocalIdx);
                        if (localType2 && !valTypesMatch(tfType, localType2)) {
                          coerceType3(ctx, fctx, tfType, localType2);
                        }
                        fctx.body.push({ op: "local.set", index: nLocalIdx });
                      }
                    }
                  } else {
                    fctx.body.push({ op: "drop" });
                  }
                } else {
                  if (initType.kind === "f64") {
                    const bIdx = ctx.funcMap.get("__box_number");
                    if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
                  } else if (initType.kind === "i32") {
                    fctx.body.push({ op: "f64.convert_i32_s" });
                    const bIdx = ctx.funcMap.get("__box_number");
                    if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
                  } else if (initType.kind !== "externref") {
                    if (initType.kind === "ref" || initType.kind === "ref_null") {
                      fctx.body.push({ op: "extern.convert_any" });
                    }
                  }
                  if (ts8.isArrayBindingPattern(element.name)) {
                    compileExternrefArrayDestructuringDecl(ctx, fctx, element.name, { kind: "externref" });
                  } else {
                    compileExternrefObjectDestructuringDecl(ctx, fctx, element.name, { kind: "externref" });
                  }
                }
              }
            }
          }
          continue;
        }
        const fieldType = tupleFields[i].type;
        if (ts8.isBindingElement(element) && element.dotDotDotToken) {
          const restName = ts8.isIdentifier(element.name) ? element.name.text : `__rest_${fctx.locals.length}`;
          allocLocal(fctx, restName, { kind: "externref" });
          continue;
        }
        if (
          ts8.isBindingElement(element) &&
          (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name))
        ) {
          const nestedLocal = allocLocal(fctx, `__destruct_nested_${fctx.locals.length}`, fieldType);
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: i });
          fctx.body.push({ op: "local.set", index: nestedLocal });
          if (element.initializer) {
            ctx._arrayLiteralForceVec = true;
            try {
              emitNestedBindingDefault(ctx, fctx, nestedLocal, fieldType, element.initializer);
            } finally {
              ctx._arrayLiteralForceVec = false;
            }
          }
          ensureBindingLocals(ctx, fctx, element.name);
          if (fieldType.kind === "ref" || fieldType.kind === "ref_null") {
            const nestedTypeIdx = fieldType.typeIdx;
            const nestedTypeDef = ctx.mod.types[nestedTypeIdx];
            if (ts8.isObjectBindingPattern(element.name)) {
              const nestedStructName = ctx.typeIdxToStructName.get(nestedTypeIdx);
              const nestedFields = nestedStructName ? ctx.structFields.get(nestedStructName) : void 0;
              if (nestedFields) {
                for (const nestedElem of element.name.elements) {
                  if (!ts8.isBindingElement(nestedElem)) continue;
                  const propNNode = nestedElem.propertyName ?? nestedElem.name;
                  const propNText = ts8.isIdentifier(propNNode)
                    ? propNNode.text
                    : ts8.isStringLiteral(propNNode)
                      ? propNNode.text
                      : ts8.isNumericLiteral(propNNode)
                        ? propNNode.text
                        : void 0;
                  if (!ts8.isIdentifier(nestedElem.name)) continue;
                  if (!propNText) continue;
                  const nLocalName = nestedElem.name.text;
                  const nFieldIdx = nestedFields.findIndex((f) => f.name === propNText);
                  if (nFieldIdx === -1) continue;
                  const nFieldEntry = nestedFields[nFieldIdx];
                  if (!nFieldEntry) continue;
                  const nLocalIdx = fctx.localMap.get(nLocalName);
                  if (nLocalIdx === void 0) continue;
                  fctx.body.push({ op: "local.get", index: nestedLocal });
                  fctx.body.push({ op: "struct.get", typeIdx: nestedTypeIdx, fieldIdx: nFieldIdx });
                  const localType2 = getLocalType(fctx, nLocalIdx);
                  const fType = nFieldEntry.type;
                  if (localType2 && !valTypesMatch(fType, localType2)) {
                    coerceType3(ctx, fctx, fType, localType2);
                  }
                  fctx.body.push({ op: "local.set", index: nLocalIdx });
                }
                continue;
              }
            } else if (ts8.isArrayBindingPattern(element.name)) {
              const isNestedTuple =
                nestedTypeDef &&
                nestedTypeDef.kind === "struct" &&
                nestedTypeDef.fields.length > 0 &&
                nestedTypeDef.fields.every((f, idx) => f.name === `_${idx}`);
              if (isNestedTuple) {
                const nestedFields = nestedTypeDef.fields;
                for (let j = 0; j < element.name.elements.length; j++) {
                  const ne = element.name.elements[j];
                  if (ts8.isOmittedExpression(ne)) continue;
                  if (!ts8.isBindingElement(ne) || !ts8.isIdentifier(ne.name)) continue;
                  if (j >= nestedFields.length) continue;
                  const nName = ne.name.text;
                  let nLocalIdx = fctx.localMap.get(nName);
                  if (nLocalIdx === void 0) {
                    const nTsType = ctx.checker.getTypeAtLocation(ne);
                    nLocalIdx = allocLocal(fctx, nName, resolveWasmType(ctx, nTsType));
                  }
                  const nLocalType = getLocalType(fctx, nLocalIdx);
                  const nFieldType = nestedFields[j].type;
                  fctx.body.push({ op: "local.get", index: nestedLocal });
                  fctx.body.push({ op: "struct.get", typeIdx: nestedTypeIdx, fieldIdx: j });
                  if (nLocalType && !valTypesMatch(nFieldType, nLocalType)) {
                    coerceType3(ctx, fctx, nFieldType, nLocalType);
                  }
                  fctx.body.push({ op: "local.set", index: nLocalIdx });
                }
                continue;
              }
              const nestedArrTypeIdx = getArrTypeIdxFromVec(ctx, nestedTypeIdx);
              const nestedArrDef = ctx.mod.types[nestedArrTypeIdx];
              if (nestedArrDef && nestedArrDef.kind === "array") {
                const nestedElemType = nestedArrDef.element;
                for (let j = 0; j < element.name.elements.length; j++) {
                  const ne = element.name.elements[j];
                  if (ts8.isOmittedExpression(ne)) continue;
                  if (!ts8.isBindingElement(ne) || !ts8.isIdentifier(ne.name)) continue;
                  const nName = ne.name.text;
                  const nLocalIdx = fctx.localMap.get(nName);
                  if (nLocalIdx === void 0) continue;
                  fctx.body.push({ op: "local.get", index: nestedLocal });
                  fctx.body.push({ op: "struct.get", typeIdx: nestedTypeIdx, fieldIdx: 1 });
                  fctx.body.push({ op: "i32.const", value: j });
                  emitBoundsCheckedArrayGet2(fctx, nestedArrTypeIdx, nestedElemType);
                  const localType2 = getLocalType(fctx, nLocalIdx);
                  if (localType2 && !valTypesMatch(nestedElemType, localType2)) {
                    coerceType3(ctx, fctx, nestedElemType, localType2);
                  }
                  fctx.body.push({ op: "local.set", index: nLocalIdx });
                }
                continue;
              }
            }
          }
          fctx.body.push({ op: "local.get", index: nestedLocal });
          if (fieldType.kind === "ref" || fieldType.kind === "ref_null") {
            fctx.body.push({ op: "extern.convert_any" });
          } else if (fieldType.kind === "f64") {
            const bIdx = ctx.funcMap.get("__box_number");
            if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
          } else if (fieldType.kind === "i32") {
            fctx.body.push({ op: "f64.convert_i32_s" });
            const bIdx = ctx.funcMap.get("__box_number");
            if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
          }
          if (ts8.isArrayBindingPattern(element.name)) {
            compileExternrefArrayDestructuringDecl(ctx, fctx, element.name, { kind: "externref" });
          } else {
            compileExternrefObjectDestructuringDecl(ctx, fctx, element.name, { kind: "externref" });
          }
          continue;
        }
        if (!ts8.isIdentifier(element.name)) continue;
        const localName = element.name.text;
        let localIdx = fctx.localMap.get(localName);
        if (localIdx === void 0) {
          const elemTsType = ctx.checker.getTypeAtLocation(element);
          const resolvedType = resolveWasmType(ctx, elemTsType);
          localIdx = allocLocal(fctx, localName, resolvedType);
        }
        const localType = getLocalType(fctx, localIdx);
        fctx.body.push({ op: "local.get", index: tmpLocal });
        fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: i });
        if (localType && !valTypesMatch(fieldType, localType)) {
          coerceType3(ctx, fctx, fieldType, localType);
        }
        if (element.initializer) {
          emitDefaultValueCheck(ctx, fctx, localType ?? fieldType, localIdx, element.initializer);
        } else {
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      }
    });
    syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
    return;
  }
  if (!arrDef || arrDef.kind !== "array") return;
  const elemType = arrDef.element;
  emitNullGuard(ctx, fctx, tmpLocal, isNullableArr, () => {
    for (let i = 0; i < pattern.elements.length; i++) {
      const element = pattern.elements[i];
      if (ts8.isOmittedExpression(element)) continue;
      if (ts8.isBindingElement(element) && element.dotDotDotToken) {
        const restLenLocal = allocLocal(fctx, `__rest_len_${fctx.locals.length}`, { kind: "i32" });
        fctx.body.push({ op: "local.get", index: tmpLocal });
        fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 0 });
        fctx.body.push({ op: "i32.const", value: i });
        fctx.body.push({ op: "i32.sub" });
        fctx.body.push({ op: "local.set", index: restLenLocal });
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "local.get", index: restLenLocal });
        fctx.body.push({ op: "local.get", index: restLenLocal });
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "i32.lt_s" });
        fctx.body.push({ op: "select" });
        fctx.body.push({ op: "local.set", index: restLenLocal });
        const restArrLocal = allocLocal(fctx, `__rest_arr_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
        fctx.body.push({ op: "local.get", index: restLenLocal });
        fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
        fctx.body.push({ op: "local.set", index: restArrLocal });
        fctx.body.push({ op: "local.get", index: restArrLocal });
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "local.get", index: tmpLocal });
        fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
        fctx.body.push({ op: "i32.const", value: i });
        fctx.body.push({ op: "local.get", index: restLenLocal });
        fctx.body.push({ op: "array.copy", dstTypeIdx: arrTypeIdx, srcTypeIdx: arrTypeIdx });
        fctx.body.push({ op: "local.get", index: restLenLocal });
        fctx.body.push({ op: "local.get", index: restArrLocal });
        fctx.body.push({ op: "struct.new", typeIdx });
        if (ts8.isIdentifier(element.name)) {
          const restName = element.name.text;
          const restLocal = allocLocal(fctx, restName, resultType);
          fctx.body.push({ op: "local.set", index: restLocal });
        } else if (ts8.isArrayBindingPattern(element.name)) {
          const nestedTmpLocal = allocLocal(fctx, `__rest_nested_${fctx.locals.length}`, resultType);
          fctx.body.push({ op: "local.set", index: nestedTmpLocal });
          ensureBindingLocals(ctx, fctx, element.name);
          for (let j = 0; j < element.name.elements.length; j++) {
            const ne = element.name.elements[j];
            if (ts8.isOmittedExpression(ne)) continue;
            const neBinding = ne;
            if (neBinding.dotDotDotToken && ts8.isIdentifier(neBinding.name)) {
              const innerRestLenLocal = allocLocal(fctx, `__inner_rest_len_${fctx.locals.length}`, { kind: "i32" });
              fctx.body.push({ op: "local.get", index: nestedTmpLocal });
              fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 0 });
              fctx.body.push({ op: "i32.const", value: j });
              fctx.body.push({ op: "i32.sub" });
              fctx.body.push({ op: "local.set", index: innerRestLenLocal });
              fctx.body.push({ op: "i32.const", value: 0 });
              fctx.body.push({ op: "local.get", index: innerRestLenLocal });
              fctx.body.push({ op: "local.get", index: innerRestLenLocal });
              fctx.body.push({ op: "i32.const", value: 0 });
              fctx.body.push({ op: "i32.lt_s" });
              fctx.body.push({ op: "select" });
              fctx.body.push({ op: "local.set", index: innerRestLenLocal });
              const innerRestArrLocal = allocLocal(fctx, `__inner_rest_arr_${fctx.locals.length}`, {
                kind: "ref",
                typeIdx: arrTypeIdx,
              });
              fctx.body.push({ op: "local.get", index: innerRestLenLocal });
              fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
              fctx.body.push({ op: "local.set", index: innerRestArrLocal });
              fctx.body.push({ op: "local.get", index: innerRestArrLocal });
              fctx.body.push({ op: "i32.const", value: 0 });
              fctx.body.push({ op: "local.get", index: nestedTmpLocal });
              fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
              fctx.body.push({ op: "i32.const", value: j });
              fctx.body.push({ op: "local.get", index: innerRestLenLocal });
              fctx.body.push({ op: "array.copy", dstTypeIdx: arrTypeIdx, srcTypeIdx: arrTypeIdx });
              fctx.body.push({ op: "local.get", index: innerRestLenLocal });
              fctx.body.push({ op: "local.get", index: innerRestArrLocal });
              fctx.body.push({ op: "struct.new", typeIdx });
              const innerRestLocal = fctx.localMap.get(neBinding.name.text);
              if (innerRestLocal === void 0) continue;
              fctx.body.push({ op: "local.set", index: innerRestLocal });
            } else if (ts8.isIdentifier(neBinding.name)) {
              const nLocalIdx = fctx.localMap.get(neBinding.name.text);
              if (nLocalIdx === void 0) continue;
              fctx.body.push({ op: "local.get", index: nestedTmpLocal });
              fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
              fctx.body.push({ op: "i32.const", value: j });
              emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elemType);
              fctx.body.push({ op: "local.set", index: nLocalIdx });
            }
          }
        } else {
          fctx.body.push({ op: "drop" });
          ensureBindingLocals(ctx, fctx, element.name);
        }
        continue;
      }
      if (
        ts8.isBindingElement(element) &&
        (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name))
      ) {
        const hasDefault = !!element.initializer;
        if (hasDefault && elemType.kind === "externref") {
          ensureBindingLocals(ctx, fctx, element.name);
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
          fctx.body.push({ op: "i32.const", value: i });
          emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elemType);
          const nestedExtLocal = allocLocal(fctx, `__ext_nested_${fctx.locals.length}`, elemType);
          fctx.body.push({ op: "local.set", index: nestedExtLocal });
          fctx.body.push({ op: "local.get", index: nestedExtLocal });
          emitExternrefDefaultCheck(ctx, fctx, nestedExtLocal);
          ctx._arrayLiteralForceVec = true;
          const defaultBranch = collectInstrs(fctx, () => {
            const initType = compileExpression2(ctx, fctx, element.initializer);
            ctx._arrayLiteralForceVec = false;
            if (initType && (initType.kind === "ref" || initType.kind === "ref_null")) {
              const initTypeIdx = initType.typeIdx;
              const initTypeDef = ctx.mod.types[initTypeIdx];
              if (initTypeDef && initTypeDef.kind === "struct") {
                const initArrTypeIdx = getArrTypeIdxFromVec(ctx, initTypeIdx);
                const initArrDef = ctx.mod.types[initArrTypeIdx];
                if (ts8.isArrayBindingPattern(element.name) && initArrDef && initArrDef.kind === "array") {
                  const tmpVecLocal = allocLocal(fctx, `__dflt_vec_${fctx.locals.length}`, initType);
                  fctx.body.push({ op: "local.set", index: tmpVecLocal });
                  const initElemType = initArrDef.element;
                  for (let j = 0; j < element.name.elements.length; j++) {
                    const ne = element.name.elements[j];
                    if (ts8.isOmittedExpression(ne)) continue;
                    if (!ts8.isBindingElement(ne) || !ts8.isIdentifier(ne.name)) continue;
                    const nName = ne.name.text;
                    const nLocalIdx = fctx.localMap.get(nName);
                    if (nLocalIdx === void 0) continue;
                    fctx.body.push({ op: "local.get", index: tmpVecLocal });
                    fctx.body.push({ op: "struct.get", typeIdx: initTypeIdx, fieldIdx: 1 });
                    fctx.body.push({ op: "i32.const", value: j });
                    emitBoundsCheckedArrayGet2(fctx, initArrTypeIdx, initElemType);
                    const localType = getLocalType(fctx, nLocalIdx);
                    if (localType && !valTypesMatch(initElemType, localType)) {
                      coerceType3(ctx, fctx, initElemType, localType);
                    }
                    fctx.body.push({ op: "local.set", index: nLocalIdx });
                  }
                  return;
                }
                if (ts8.isObjectBindingPattern(element.name)) {
                  const tmpObjLocal = allocLocal(fctx, `__dflt_obj_${fctx.locals.length}`, initType);
                  fctx.body.push({ op: "local.set", index: tmpObjLocal });
                  const nestedStructName = ctx.typeIdxToStructName.get(initTypeIdx);
                  const nestedFields = nestedStructName ? ctx.structFields.get(nestedStructName) : void 0;
                  if (nestedFields) {
                    for (const nestedElem of element.name.elements) {
                      if (!ts8.isBindingElement(nestedElem)) continue;
                      const propNNode = nestedElem.propertyName ?? nestedElem.name;
                      const propNText = ts8.isIdentifier(propNNode)
                        ? propNNode.text
                        : ts8.isStringLiteral(propNNode)
                          ? propNNode.text
                          : ts8.isNumericLiteral(propNNode)
                            ? propNNode.text
                            : void 0;
                      if (!ts8.isIdentifier(nestedElem.name)) continue;
                      if (!propNText) continue;
                      const nLocalName = nestedElem.name.text;
                      const nFieldIdx = nestedFields.findIndex((f) => f.name === propNText);
                      if (nFieldIdx === -1) continue;
                      const nLocalIdx = fctx.localMap.get(nLocalName);
                      if (nLocalIdx === void 0) continue;
                      fctx.body.push({ op: "local.get", index: tmpObjLocal });
                      fctx.body.push({ op: "struct.get", typeIdx: initTypeIdx, fieldIdx: nFieldIdx });
                      const localType = getLocalType(fctx, nLocalIdx);
                      const fType = nestedFields[nFieldIdx].type;
                      if (localType && !valTypesMatch(fType, localType)) {
                        coerceType3(ctx, fctx, fType, localType);
                      }
                      fctx.body.push({ op: "local.set", index: nLocalIdx });
                    }
                    return;
                  }
                }
              }
            }
            if (initType && initType.kind !== "externref") {
              if (initType.kind === "ref" || initType.kind === "ref_null") {
                fctx.body.push({ op: "extern.convert_any" });
              } else if (initType.kind === "f64") {
                const bIdx = ctx.funcMap.get("__box_number");
                if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
              }
            }
            fctx.body.push({ op: "local.set", index: nestedExtLocal });
          });
          const elseBranch = collectInstrs(fctx, () => {
            if (ts8.isArrayBindingPattern(element.name)) {
              fctx.body.push({ op: "local.get", index: nestedExtLocal });
              compileExternrefArrayDestructuringDecl(ctx, fctx, element.name, elemType);
            }
            if (ts8.isObjectBindingPattern(element.name)) {
              fctx.body.push({ op: "local.get", index: nestedExtLocal });
              compileExternrefObjectDestructuringDecl(ctx, fctx, element.name, elemType);
            }
          });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: defaultBranch,
            else: elseBranch,
          });
        } else if (hasDefault) {
          ensureBindingLocals(ctx, fctx, element.name);
          const nullableElemType = elemType.kind === "ref" ? { kind: "ref_null", typeIdx: elemType.typeIdx } : elemType;
          const nestedLocal = allocLocal(fctx, `__destruct_nested_${fctx.locals.length}`, nullableElemType);
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
          fctx.body.push({ op: "i32.const", value: i });
          emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, nullableElemType);
          ctx._arrayLiteralForceVec = true;
          try {
            emitDefaultValueCheck(ctx, fctx, nullableElemType, nestedLocal, element.initializer);
          } finally {
            ctx._arrayLiteralForceVec = false;
          }
          if (elemType.kind === "ref" || elemType.kind === "ref_null") {
            if (ts8.isObjectBindingPattern(element.name)) {
              const nestedTypeIdx = elemType.typeIdx;
              const nestedStructName = ctx.typeIdxToStructName.get(nestedTypeIdx);
              const nestedFields = nestedStructName ? ctx.structFields.get(nestedStructName) : void 0;
              if (nestedFields) {
                for (const nestedElem of element.name.elements) {
                  if (!ts8.isBindingElement(nestedElem)) continue;
                  const propNNode = nestedElem.propertyName ?? nestedElem.name;
                  const propNText = ts8.isIdentifier(propNNode)
                    ? propNNode.text
                    : ts8.isStringLiteral(propNNode)
                      ? propNNode.text
                      : ts8.isNumericLiteral(propNNode)
                        ? propNNode.text
                        : void 0;
                  if (!ts8.isIdentifier(nestedElem.name)) continue;
                  if (!propNText) continue;
                  const nLocalName = nestedElem.name.text;
                  const nFieldIdx = nestedFields.findIndex((f) => f.name === propNText);
                  if (nFieldIdx === -1) continue;
                  const nFieldEntry = nestedFields[nFieldIdx];
                  if (!nFieldEntry) continue;
                  const nLocalIdx = fctx.localMap.get(nLocalName);
                  if (nLocalIdx === void 0) continue;
                  fctx.body.push({ op: "local.get", index: nestedLocal });
                  fctx.body.push({ op: "struct.get", typeIdx: nestedTypeIdx, fieldIdx: nFieldIdx });
                  fctx.body.push({ op: "local.set", index: nLocalIdx });
                }
              }
            } else if (ts8.isArrayBindingPattern(element.name)) {
              const nestedVecTypeIdx = elemType.typeIdx;
              const nestedArrTypeIdx = getArrTypeIdxFromVec(ctx, nestedVecTypeIdx);
              const nestedArrDef = ctx.mod.types[nestedArrTypeIdx];
              if (nestedArrDef && nestedArrDef.kind === "array") {
                const nestedElemType = nestedArrDef.element;
                for (let j = 0; j < element.name.elements.length; j++) {
                  const ne = element.name.elements[j];
                  if (ts8.isOmittedExpression(ne)) continue;
                  if (!ts8.isBindingElement(ne) || !ts8.isIdentifier(ne.name)) continue;
                  const nName = ne.name.text;
                  const nLocalIdx = fctx.localMap.get(nName);
                  if (nLocalIdx === void 0) continue;
                  fctx.body.push({ op: "local.get", index: nestedLocal });
                  fctx.body.push({ op: "struct.get", typeIdx: nestedVecTypeIdx, fieldIdx: 1 });
                  fctx.body.push({ op: "i32.const", value: j });
                  emitBoundsCheckedArrayGet2(fctx, nestedArrTypeIdx, nestedElemType);
                  fctx.body.push({ op: "local.set", index: nLocalIdx });
                }
              }
            }
          }
        } else {
          const nestedLocal = allocLocal(fctx, `__destruct_nested_${fctx.locals.length}`, elemType);
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
          fctx.body.push({ op: "i32.const", value: i });
          emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elemType);
          fctx.body.push({ op: "local.set", index: nestedLocal });
          ensureBindingLocals(ctx, fctx, element.name);
          if (elemType.kind === "ref" || elemType.kind === "ref_null") {
            if (ts8.isObjectBindingPattern(element.name)) {
              const nestedTypeIdx = elemType.typeIdx;
              const nestedStructName = ctx.typeIdxToStructName.get(nestedTypeIdx);
              const nestedFields = nestedStructName ? ctx.structFields.get(nestedStructName) : void 0;
              if (nestedFields) {
                for (const nestedElem of element.name.elements) {
                  if (!ts8.isBindingElement(nestedElem)) continue;
                  const propNNode = nestedElem.propertyName ?? nestedElem.name;
                  const propNText = ts8.isIdentifier(propNNode)
                    ? propNNode.text
                    : ts8.isStringLiteral(propNNode)
                      ? propNNode.text
                      : ts8.isNumericLiteral(propNNode)
                        ? propNNode.text
                        : void 0;
                  if (!ts8.isIdentifier(nestedElem.name)) continue;
                  if (!propNText) continue;
                  const nLocalName = nestedElem.name.text;
                  const nFieldIdx = nestedFields.findIndex((f) => f.name === propNText);
                  if (nFieldIdx === -1) continue;
                  const nFieldEntry = nestedFields[nFieldIdx];
                  if (!nFieldEntry) continue;
                  const nLocalIdx = fctx.localMap.get(nLocalName);
                  if (nLocalIdx === void 0) continue;
                  fctx.body.push({ op: "local.get", index: nestedLocal });
                  fctx.body.push({ op: "struct.get", typeIdx: nestedTypeIdx, fieldIdx: nFieldIdx });
                  fctx.body.push({ op: "local.set", index: nLocalIdx });
                }
              }
            } else if (ts8.isArrayBindingPattern(element.name)) {
              const nestedVecTypeIdx = elemType.typeIdx;
              const nestedArrTypeIdx = getArrTypeIdxFromVec(ctx, nestedVecTypeIdx);
              const nestedArrDef = ctx.mod.types[nestedArrTypeIdx];
              if (nestedArrDef && nestedArrDef.kind === "array") {
                const nestedElemType = nestedArrDef.element;
                for (let j = 0; j < element.name.elements.length; j++) {
                  const ne = element.name.elements[j];
                  if (ts8.isOmittedExpression(ne)) continue;
                  if (!ts8.isBindingElement(ne) || !ts8.isIdentifier(ne.name)) continue;
                  const nName = ne.name.text;
                  const nLocalIdx = fctx.localMap.get(nName);
                  if (nLocalIdx === void 0) continue;
                  fctx.body.push({ op: "local.get", index: nestedLocal });
                  fctx.body.push({ op: "struct.get", typeIdx: nestedVecTypeIdx, fieldIdx: 1 });
                  fctx.body.push({ op: "i32.const", value: j });
                  emitBoundsCheckedArrayGet2(fctx, nestedArrTypeIdx, nestedElemType);
                  fctx.body.push({ op: "local.set", index: nLocalIdx });
                }
              }
            }
          } else if (elemType.kind === "externref") {
            if (ts8.isArrayBindingPattern(element.name)) {
              fctx.body.push({ op: "local.get", index: nestedLocal });
              compileExternrefArrayDestructuringDecl(ctx, fctx, element.name, elemType);
            }
            if (ts8.isObjectBindingPattern(element.name)) {
              fctx.body.push({ op: "local.get", index: nestedLocal });
              compileExternrefObjectDestructuringDecl(ctx, fctx, element.name, elemType);
            }
          }
        }
        continue;
      }
      if (!ts8.isIdentifier(element.name)) continue;
      const localName = element.name.text;
      const localIdx = allocLocal(fctx, localName, elemType);
      fctx.body.push({ op: "local.get", index: tmpLocal });
      fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
      fctx.body.push({ op: "i32.const", value: i });
      emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elemType);
      if (element.initializer) {
        emitDefaultValueCheck(ctx, fctx, elemType, localIdx, element.initializer);
      } else {
        fctx.body.push({ op: "local.set", index: localIdx });
      }
    }
  });
  syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
}
function compileStringDestructuring(ctx, fctx, pattern, resultType, bodyLenBefore) {
  ensureNativeStringHelpers(ctx);
  const charAtIdx = ctx.nativeStrHelpers.get("__str_charAt");
  if (charAtIdx === void 0) {
    fctx.body.length = bodyLenBefore;
    ensureBindingLocals(ctx, fctx, pattern);
    ctx.errors.push({
      message: "Cannot destructure string: __str_charAt helper not available",
      line: 0,
      column: 0,
    });
    return;
  }
  const strType = nativeStringType(ctx);
  const tmpLocal = allocLocal(fctx, `__destruct_str_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  const isNullable = resultType.kind === "ref_null";
  const savedBody = fctx.body;
  const destructInstrs = [];
  fctx.body = destructInstrs;
  ensureBindingLocals(ctx, fctx, pattern);
  for (let i = 0; i < pattern.elements.length; i++) {
    const element = pattern.elements[i];
    if (ts8.isOmittedExpression(element)) continue;
    if (ts8.isBindingElement(element) && element.dotDotDotToken) {
      if (ts8.isIdentifier(element.name)) {
        const restName = element.name.text;
        let restIdx = fctx.localMap.get(restName);
        if (restIdx === void 0) {
          restIdx = allocLocal(fctx, restName, { kind: "externref" });
        }
        let sliceIdx = ctx.funcMap.get("__extern_slice");
        if (sliceIdx === void 0) {
          const importsBefore = ctx.numImportFuncs;
          const sliceType = addFuncType(ctx, [{ kind: "externref" }, { kind: "f64" }], [{ kind: "externref" }]);
          addImport(ctx, "env", "__extern_slice", { kind: "func", typeIdx: sliceType });
          shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
          sliceIdx = ctx.funcMap.get("__extern_slice");
        }
        if (sliceIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "extern.convert_any" });
          fctx.body.push({ op: "f64.const", value: i });
          fctx.body.push({ op: "call", funcIdx: sliceIdx });
          fctx.body.push({ op: "local.set", index: restIdx });
        }
      }
      continue;
    }
    if (
      ts8.isBindingElement(element) &&
      (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name))
    ) {
      ensureBindingLocals(ctx, fctx, element.name);
      continue;
    }
    if (!ts8.isIdentifier(element.name)) continue;
    const localName = element.name.text;
    const localIdx = allocLocal(fctx, localName, strType);
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "i32.const", value: i });
    fctx.body.push({ op: "call", funcIdx: charAtIdx });
    fctx.body.push({ op: "local.set", index: localIdx });
  }
  fctx.body = savedBody;
  if (isNullable && destructInstrs.length > 0) {
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: [], else: destructInstrs });
  } else {
    fctx.body.push(...destructInstrs);
  }
}
function canTailCall(ctx, fctx, calleeIdx) {
  let calleeTypeIdx;
  if (calleeIdx < ctx.numImportFuncs) {
    const imp = ctx.mod.imports[calleeIdx];
    if (imp?.desc.kind === "func") calleeTypeIdx = imp.desc.typeIdx;
  } else {
    const localIdx = calleeIdx - ctx.numImportFuncs;
    const func = ctx.mod.functions[localIdx];
    if (func) calleeTypeIdx = func.typeIdx;
  }
  if (calleeTypeIdx === void 0) return false;
  const typeDef = ctx.mod.types[calleeTypeIdx];
  if (!typeDef || typeDef.kind !== "func") return false;
  if (typeDef.params.length !== fctx.params.length) return false;
  const calleeResults = typeDef.results;
  if (!fctx.returnType) {
    return calleeResults.length === 0;
  }
  if (calleeResults.length !== 1) return false;
  const calleeRet = calleeResults[0];
  const callerRet = fctx.returnType;
  if (calleeRet.kind === callerRet.kind) return true;
  if (
    (calleeRet.kind === "ref" || calleeRet.kind === "ref_null") &&
    (callerRet.kind === "ref" || callerRet.kind === "ref_null")
  )
    return true;
  return false;
}
function canTailCallRef(ctx, fctx, typeIdx) {
  const typeDef = ctx.mod.types[typeIdx];
  if (!typeDef || typeDef.kind !== "func") return false;
  if (typeDef.params.length !== fctx.params.length) return false;
  const calleeResults = typeDef.results;
  if (!fctx.returnType) return calleeResults.length === 0;
  if (calleeResults.length !== 1) return false;
  const calleeRet = calleeResults[0];
  const callerRet = fctx.returnType;
  if (calleeRet.kind === callerRet.kind) return true;
  if (
    (calleeRet.kind === "ref" || calleeRet.kind === "ref_null") &&
    (callerRet.kind === "ref" || callerRet.kind === "ref_null")
  )
    return true;
  return false;
}
function compileReturnStatement(ctx, fctx, stmt) {
  if (fctx.isGenerator) {
    if (stmt.expression) {
      const bufferIdx = fctx.localMap.get("__gen_buffer");
      const resultType = compileExpression2(ctx, fctx, stmt.expression);
      if (resultType !== null && bufferIdx !== void 0) {
        const tmpLocal = allocLocal(fctx, `__gen_ret_${fctx.locals.length}`, resultType);
        fctx.body.push({ op: "local.set", index: tmpLocal });
        fctx.body.push({ op: "local.get", index: bufferIdx });
        fctx.body.push({ op: "local.get", index: tmpLocal });
        if (resultType.kind === "f64") {
          const pushIdx = ctx.funcMap.get("__gen_push_f64");
          if (pushIdx !== void 0) fctx.body.push({ op: "call", funcIdx: pushIdx });
        } else if (resultType.kind === "i32") {
          const pushIdx = ctx.funcMap.get("__gen_push_i32");
          if (pushIdx !== void 0) fctx.body.push({ op: "call", funcIdx: pushIdx });
        } else {
          const pushIdx = ctx.funcMap.get("__gen_push_ref");
          if (pushIdx !== void 0) fctx.body.push({ op: "call", funcIdx: pushIdx });
        }
      } else if (resultType !== null) {
        fctx.body.push({ op: "drop" });
      }
    }
    const genReturnDepth = fctx.generatorReturnDepth ?? fctx.blockDepth;
    fctx.body.push({ op: "br", depth: genReturnDepth });
    return;
  }
  const hasPendingFinally = fctx.finallyStack && fctx.finallyStack.length > 0;
  if (stmt.expression) {
    const exprType = compileExpression2(ctx, fctx, stmt.expression, fctx.returnType ?? void 0);
    if (exprType && fctx.returnType && !valTypesMatch(exprType, fctx.returnType)) {
      coerceType3(ctx, fctx, exprType, fctx.returnType);
    }
    if (exprType && !fctx.returnType) {
      fctx.body.push({ op: "drop" });
    }
  } else if (fctx.returnType) {
    if (fctx.returnType.kind === "f64") fctx.body.push({ op: "f64.const", value: 0 });
    else if (fctx.returnType.kind === "i32") fctx.body.push({ op: "i32.const", value: 0 });
    else if (fctx.returnType.kind === "i64") fctx.body.push({ op: "i64.const", value: 0n });
    else if (fctx.returnType.kind === "externref") fctx.body.push({ op: "ref.null.extern" });
    else if (fctx.returnType.kind === "ref_null") fctx.body.push({ op: "ref.null", typeIdx: fctx.returnType.typeIdx });
    else if (fctx.returnType.kind === "ref") fctx.body.push({ op: "ref.null", typeIdx: fctx.returnType.typeIdx });
  }
  if (hasPendingFinally) {
    let retTmpIdx;
    if (fctx.returnType) {
      retTmpIdx = allocLocal(fctx, `__finally_ret_${fctx.locals.length}`, fctx.returnType);
      fctx.body.push({ op: "local.set", index: retTmpIdx });
    }
    for (let i = fctx.finallyStack.length - 1; i >= 0; i--) {
      fctx.body.push(...fctx.finallyStack[i].cloneFinally());
    }
    if (retTmpIdx !== void 0) {
      fctx.body.push({ op: "local.get", index: retTmpIdx });
    }
    fctx.body.push({ op: "return" });
    return;
  }
  const lastInstr = fctx.body[fctx.body.length - 1];
  if (lastInstr && lastInstr.op === "call") {
    const calleeIdx = lastInstr.funcIdx;
    if (canTailCall(ctx, fctx, calleeIdx)) {
      lastInstr.op = "return_call";
      return;
    }
  }
  if (lastInstr && lastInstr.op === "call_ref") {
    const typeIdx = lastInstr.typeIdx;
    if (typeIdx !== void 0 && canTailCallRef(ctx, fctx, typeIdx)) {
      lastInstr.op = "return_call_ref";
      return;
    }
  }
  fctx.body.push({ op: "return" });
}
function detectNullNarrowing(expr) {
  if (!ts8.isBinaryExpression(expr)) return null;
  const op = expr.operatorToken.kind;
  const isNeq = op === ts8.SyntaxKind.ExclamationEqualsEqualsToken || op === ts8.SyntaxKind.ExclamationEqualsToken;
  const isEq = op === ts8.SyntaxKind.EqualsEqualsEqualsToken || op === ts8.SyntaxKind.EqualsEqualsToken;
  if (!isNeq && !isEq) return null;
  const rightIsNull =
    expr.right.kind === ts8.SyntaxKind.NullKeyword || (ts8.isIdentifier(expr.right) && expr.right.text === "undefined");
  const leftIsNull =
    expr.left.kind === ts8.SyntaxKind.NullKeyword || (ts8.isIdentifier(expr.left) && expr.left.text === "undefined");
  if (!rightIsNull && !leftIsNull) return null;
  const nonNullSide = rightIsNull ? expr.left : expr.right;
  if (!ts8.isIdentifier(nonNullSide)) return null;
  return {
    varName: nonNullSide.text,
    narrowedBranch: isNeq ? "then" : "else",
  };
}
function detectTypeofNarrowing(expr) {
  if (!ts8.isBinaryExpression(expr)) return null;
  const op = expr.operatorToken.kind;
  const isEq = op === ts8.SyntaxKind.EqualsEqualsEqualsToken || op === ts8.SyntaxKind.EqualsEqualsToken;
  const isNeq = op === ts8.SyntaxKind.ExclamationEqualsEqualsToken || op === ts8.SyntaxKind.ExclamationEqualsToken;
  if (!isEq && !isNeq) return null;
  let typeofExpr = null;
  let stringLiteral = null;
  if (ts8.isTypeOfExpression(expr.left) && ts8.isStringLiteral(expr.right)) {
    typeofExpr = expr.left;
    stringLiteral = expr.right.text;
  } else if (ts8.isTypeOfExpression(expr.right) && ts8.isStringLiteral(expr.left)) {
    typeofExpr = expr.right;
    stringLiteral = expr.left.text;
  }
  if (!typeofExpr || !stringLiteral) return null;
  const operand = typeofExpr.expression;
  if (!ts8.isIdentifier(operand)) return null;
  if (stringLiteral !== "string" && stringLiteral !== "number") return null;
  return {
    varName: operand.text,
    typeLiteral: stringLiteral,
    narrowedBranch: isEq ? "then" : "else",
  };
}
function applyTypeofNarrowing(ctx, fctx, varName, typeLiteral) {
  const originalLocalIdx = fctx.localMap.get(varName);
  if (originalLocalIdx === void 0) return null;
  const localType = getLocalType(fctx, originalLocalIdx);
  if (!localType || !isAnyValue(localType, ctx)) return null;
  ensureAnyHelpers(ctx);
  let narrowedType;
  let unboxHelper;
  if (typeLiteral === "number") {
    narrowedType = { kind: "f64" };
    unboxHelper = "__any_unbox_f64";
  } else if (typeLiteral === "string") {
    narrowedType = { kind: "externref" };
    unboxHelper = "__any_unbox_extern";
  } else {
    return null;
  }
  const funcIdx = ctx.funcMap.get(unboxHelper);
  if (funcIdx === void 0) return null;
  const narrowedLocalIdx = allocLocal(fctx, `__typeof_${varName}`, narrowedType);
  fctx.body.push({ op: "local.get", index: originalLocalIdx });
  fctx.body.push({ op: "call", funcIdx });
  fctx.body.push({ op: "local.set", index: narrowedLocalIdx });
  fctx.localMap.set(varName, narrowedLocalIdx);
  return { originalLocalIdx, narrowedLocalIdx };
}
function compileIfStatement(ctx, fctx, stmt) {
  const narrowing = detectNullNarrowing(stmt.expression);
  const typeofNarrowing = detectTypeofNarrowing(stmt.expression);
  const condType = compileExpression2(ctx, fctx, stmt.expression);
  ensureI32Condition(fctx, condType, ctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]++;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]++;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth++;
  adjustRethrowDepth(fctx, 1);
  const savedNarrowedNonNull = fctx.narrowedNonNull ? new Set(fctx.narrowedNonNull) : void 0;
  if (narrowing && narrowing.narrowedBranch === "then") {
    if (!fctx.narrowedNonNull) fctx.narrowedNonNull = /* @__PURE__ */ new Set();
    fctx.narrowedNonNull.add(narrowing.varName);
  }
  const savedBody = pushBody(fctx);
  let typeofNarrowResult = null;
  if (typeofNarrowing && typeofNarrowing.narrowedBranch === "then") {
    typeofNarrowResult = applyTypeofNarrowing(ctx, fctx, typeofNarrowing.varName, typeofNarrowing.typeLiteral);
  }
  if (ts8.isBlock(stmt.thenStatement)) {
    for (const s of stmt.thenStatement.statements) {
      compileStatement(ctx, fctx, s);
    }
  } else {
    compileStatement(ctx, fctx, stmt.thenStatement);
  }
  const thenInstrs = fctx.body;
  if (typeofNarrowResult) {
    fctx.localMap.set(typeofNarrowing.varName, typeofNarrowResult.originalLocalIdx);
  }
  fctx.narrowedNonNull = savedNarrowedNonNull ? new Set(savedNarrowedNonNull) : void 0;
  if (narrowing && narrowing.narrowedBranch === "else") {
    if (!fctx.narrowedNonNull) fctx.narrowedNonNull = /* @__PURE__ */ new Set();
    fctx.narrowedNonNull.add(narrowing.varName);
  }
  let elseInstrs;
  let typeofNarrowResultElse = null;
  if (stmt.elseStatement) {
    fctx.body = [];
    if (typeofNarrowing && typeofNarrowing.narrowedBranch === "else") {
      typeofNarrowResultElse = applyTypeofNarrowing(ctx, fctx, typeofNarrowing.varName, typeofNarrowing.typeLiteral);
    }
    if (ts8.isBlock(stmt.elseStatement)) {
      for (const s of stmt.elseStatement.statements) {
        compileStatement(ctx, fctx, s);
      }
    } else {
      compileStatement(ctx, fctx, stmt.elseStatement);
    }
    elseInstrs = fctx.body;
    if (typeofNarrowResultElse) {
      fctx.localMap.set(typeofNarrowing.varName, typeofNarrowResultElse.originalLocalIdx);
    }
  }
  popBody(fctx, savedBody);
  fctx.narrowedNonNull = savedNarrowedNonNull;
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]--;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]--;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth--;
  adjustRethrowDepth(fctx, -1);
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: thenInstrs,
    else: elseInstrs,
  });
}
function compileWhileStatement(ctx, fctx, stmt) {
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] += 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] += 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth += 2;
  adjustRethrowDepth(fctx, 2);
  fctx.breakStack.push(1);
  fctx.continueStack.push(0);
  const condType = compileExpression2(ctx, fctx, stmt.expression);
  ensureI32Condition(fctx, condType, ctx);
  fctx.body.push({ op: "i32.eqz" });
  fctx.body.push({ op: "br_if", depth: 1 });
  if (ts8.isBlock(stmt.statement)) {
    const savedScope = saveBlockScopedShadows(fctx, stmt.statement);
    for (const s of stmt.statement.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedScope);
  } else {
    compileStatement(ctx, fctx, stmt.statement);
  }
  fctx.body.push({ op: "br", depth: 0 });
  const loopBody = fctx.body;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] -= 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] -= 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth -= 2;
  adjustRethrowDepth(fctx, -2);
  popBody(fctx, savedBody);
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
}
function detectI32LoopVar(stmt) {
  if (!stmt.initializer || !ts8.isVariableDeclarationList(stmt.initializer)) return null;
  const decls = stmt.initializer.declarations;
  if (decls.length !== 1) return null;
  const decl = decls[0];
  if (!ts8.isIdentifier(decl.name)) return null;
  const name = decl.name.text;
  if (!decl.initializer || !ts8.isNumericLiteral(decl.initializer)) return null;
  const initValue = Number(decl.initializer.text.replace(/_/g, ""));
  if (!Number.isInteger(initValue) || initValue < -2147483648 || initValue > 2147483647) return null;
  if (!stmt.condition || !ts8.isBinaryExpression(stmt.condition)) return null;
  const cond = stmt.condition;
  const op = cond.operatorToken.kind;
  let isValidCondition = false;
  if (
    (op === ts8.SyntaxKind.LessThanToken || op === ts8.SyntaxKind.LessThanEqualsToken) &&
    ts8.isIdentifier(cond.left) &&
    cond.left.text === name
  ) {
    isValidCondition = true;
  }
  if (
    (op === ts8.SyntaxKind.GreaterThanToken || op === ts8.SyntaxKind.GreaterThanEqualsToken) &&
    ts8.isIdentifier(cond.right) &&
    cond.right.text === name
  ) {
    isValidCondition = true;
  }
  if (!isValidCondition) return null;
  if (!stmt.incrementor) return null;
  const incr = stmt.incrementor;
  if (ts8.isPostfixUnaryExpression(incr)) {
    if (!ts8.isIdentifier(incr.operand) || incr.operand.text !== name) return null;
    if (incr.operator !== ts8.SyntaxKind.PlusPlusToken && incr.operator !== ts8.SyntaxKind.MinusMinusToken) return null;
  } else if (ts8.isPrefixUnaryExpression(incr)) {
    if (!ts8.isIdentifier(incr.operand) || incr.operand.text !== name) return null;
    if (incr.operator !== ts8.SyntaxKind.PlusPlusToken && incr.operator !== ts8.SyntaxKind.MinusMinusToken) return null;
  } else if (ts8.isBinaryExpression(incr)) {
    if (!ts8.isIdentifier(incr.left) || incr.left.text !== name) return null;
    if (
      incr.operatorToken.kind !== ts8.SyntaxKind.PlusEqualsToken &&
      incr.operatorToken.kind !== ts8.SyntaxKind.MinusEqualsToken
    )
      return null;
    if (!ts8.isNumericLiteral(incr.right)) return null;
    const stepVal = Number(incr.right.text.replace(/_/g, ""));
    if (!Number.isInteger(stepVal)) return null;
  } else {
    return null;
  }
  return { name, initValue };
}
function compileForStatement(ctx, fctx, stmt) {
  let savedForScope = null;
  let savedForTdz = null;
  let savedForConstBindings = null;
  if (
    stmt.initializer &&
    ts8.isVariableDeclarationList(stmt.initializer) &&
    stmt.initializer.flags & (ts8.NodeFlags.Let | ts8.NodeFlags.Const)
  ) {
    for (const decl of stmt.initializer.declarations) {
      if (ts8.isIdentifier(decl.name)) {
        const name = decl.name.text;
        if (!savedForConstBindings) savedForConstBindings = /* @__PURE__ */ new Map();
        savedForConstBindings.set(name, fctx.constBindings?.has(name) ?? false);
        fctx.constBindings?.delete(name);
        const existing = fctx.localMap.get(name);
        if (existing !== void 0) {
          if (!savedForScope) savedForScope = /* @__PURE__ */ new Map();
          savedForScope.set(name, existing);
          fctx.localMap.delete(name);
        }
        const existingTdz = fctx.tdzFlagLocals?.get(name);
        if (existingTdz !== void 0) {
          if (!savedForTdz) savedForTdz = /* @__PURE__ */ new Map();
          savedForTdz.set(name, existingTdz);
          fctx.tdzFlagLocals?.delete(name);
        }
      }
    }
  }
  if (stmt.initializer) {
    if (ts8.isVariableDeclarationList(stmt.initializer)) {
      const isVar = !(stmt.initializer.flags & (ts8.NodeFlags.Let | ts8.NodeFlags.Const));
      for (const decl of stmt.initializer.declarations) {
        if (ts8.isObjectBindingPattern(decl.name)) {
          compileObjectDestructuring(ctx, fctx, decl);
          continue;
        }
        if (ts8.isArrayBindingPattern(decl.name)) {
          compileArrayDestructuring(ctx, fctx, decl);
          continue;
        }
        if (!ts8.isIdentifier(decl.name)) continue;
        const name = decl.name.text;
        const moduleGlobalIdx = ctx.moduleGlobals.get(name);
        if (moduleGlobalIdx !== void 0) {
          if (decl.initializer) {
            const globalDef = ctx.mod.globals[localGlobalIdx(ctx, moduleGlobalIdx)];
            const wasmType2 = globalDef?.type ?? resolveWasmType(ctx, ctx.checker.getTypeAtLocation(decl));
            compileExpression2(ctx, fctx, decl.initializer, wasmType2);
            fctx.body.push({ op: "global.set", index: moduleGlobalIdx });
          }
          continue;
        }
        if (decl.initializer && ts8.isClassExpression(decl.initializer)) {
          continue;
        }
        if (decl.initializer && (ts8.isArrowFunction(decl.initializer) || ts8.isFunctionExpression(decl.initializer))) {
          const actualType = compileExpression2(ctx, fctx, decl.initializer);
          const closureType = actualType ?? { kind: "externref" };
          const existingIdx2 = fctx.localMap.get(name);
          const localIdx2 =
            isVar && existingIdx2 !== void 0 && existingIdx2 >= fctx.params.length
              ? existingIdx2
              : allocLocal(fctx, name, closureType);
          if (isVar && existingIdx2 !== void 0 && existingIdx2 >= fctx.params.length) {
            const localSlot = fctx.locals[localIdx2 - fctx.params.length];
            if (localSlot) localSlot.type = closureType;
          }
          emitCoercedLocalSet(ctx, fctx, localIdx2, closureType);
          continue;
        }
        const varType = ctx.checker.getTypeAtLocation(decl);
        let wasmType = resolveWasmType(ctx, varType);
        const i32LoopInfo = detectI32LoopVar(stmt);
        const isI32LoopVar = i32LoopInfo !== null && i32LoopInfo.name === name && wasmType.kind === "f64";
        if (isI32LoopVar) {
          wasmType = { kind: "i32" };
        }
        const existingIdx = fctx.localMap.get(name);
        const localIdx =
          isVar && existingIdx !== void 0 && existingIdx >= fctx.params.length
            ? existingIdx
            : allocLocal(fctx, name, wasmType);
        if (isVar && existingIdx !== void 0 && existingIdx >= fctx.params.length) {
          const localSlot = fctx.locals[localIdx - fctx.params.length];
          if (localSlot && !valTypesMatch(wasmType, localSlot.type)) {
            localSlot.type = wasmType;
          }
        }
        if (decl.initializer) {
          if (isI32LoopVar) {
            fctx.body.push({ op: "i32.const", value: i32LoopInfo.initValue });
            fctx.body.push({ op: "local.set", index: localIdx });
          } else {
            const forInitType = compileExpression2(ctx, fctx, decl.initializer, wasmType);
            if (forInitType && !valTypesMatch(forInitType, wasmType)) {
              coerceType3(ctx, fctx, forInitType, wasmType);
            }
            emitCoercedLocalSet(ctx, fctx, localIdx, forInitType ?? wasmType);
          }
        }
        if (!isVar) {
          const tdzFlagIdx = fctx.tdzFlagLocals?.get(name);
          if (tdzFlagIdx !== void 0) {
            fctx.body.push({ op: "i32.const", value: 1 });
            fctx.body.push({ op: "local.set", index: tdzFlagIdx });
          }
        }
      }
    } else {
      const resultType = compileExpression2(ctx, fctx, stmt.initializer);
      if (resultType !== null) fctx.body.push({ op: "drop" });
    }
  }
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] += 3;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] += 3;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth += 3;
  adjustRethrowDepth(fctx, 3);
  fctx.breakStack.push(2);
  fctx.continueStack.push(0);
  const condInstrs = [];
  if (stmt.condition) {
    const condBody = fctx.body;
    fctx.body = [];
    const condType = compileExpression2(ctx, fctx, stmt.condition);
    ensureI32Condition(fctx, condType, ctx);
    fctx.body.push({ op: "i32.eqz" });
    fctx.body.push({ op: "br_if", depth: 1 });
    condInstrs.push(...fctx.body);
    fctx.body = condBody;
  }
  const savedSafeIndexed = fctx.safeIndexedArrays;
  if (stmt.condition && ts8.isBinaryExpression(stmt.condition)) {
    const cond = stmt.condition;
    const op = cond.operatorToken.kind;
    let indexExpr;
    let lengthExpr;
    if (op === ts8.SyntaxKind.LessThanToken || op === ts8.SyntaxKind.LessThanEqualsToken) {
      indexExpr = cond.left;
      lengthExpr = cond.right;
    }
    if (op === ts8.SyntaxKind.GreaterThanToken || op === ts8.SyntaxKind.GreaterThanEqualsToken) {
      indexExpr = cond.right;
      lengthExpr = cond.left;
    }
    if (
      indexExpr &&
      lengthExpr &&
      ts8.isIdentifier(indexExpr) &&
      ts8.isPropertyAccessExpression(lengthExpr) &&
      ts8.isIdentifier(lengthExpr.name) &&
      lengthExpr.name.text === "length" &&
      ts8.isIdentifier(lengthExpr.expression)
    ) {
      const indexVar = indexExpr.text;
      const arrayVar = lengthExpr.expression.text;
      if (!fctx.safeIndexedArrays) {
        fctx.safeIndexedArrays = /* @__PURE__ */ new Set();
      }
      fctx.safeIndexedArrays.add(`${arrayVar}:${indexVar}`);
    }
  }
  if (ts8.isBlock(stmt.statement)) {
    const savedScope = saveBlockScopedShadows(fctx, stmt.statement);
    for (const s of stmt.statement.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedScope);
  } else {
    compileStatement(ctx, fctx, stmt.statement);
  }
  const bodyInstrs = fctx.body;
  fctx.safeIndexedArrays = savedSafeIndexed;
  fctx.body = [];
  if (stmt.incrementor) {
    const resultType = compileExpression2(ctx, fctx, stmt.incrementor);
    if (resultType !== null) fctx.body.push({ op: "drop" });
  }
  const incrInstrs = fctx.body;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] -= 3;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] -= 3;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth -= 3;
  adjustRethrowDepth(fctx, -3);
  popBody(fctx, savedBody);
  const loopBody = [
    ...condInstrs,
    {
      op: "block",
      blockType: { kind: "empty" },
      body: bodyInstrs,
    },
    ...incrInstrs,
    { op: "br", depth: 0 },
    // restart $loop
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
  if (savedForScope) {
    for (const [name, idx] of savedForScope) {
      fctx.localMap.set(name, idx);
    }
  }
  if (savedForTdz) {
    if (!fctx.tdzFlagLocals) fctx.tdzFlagLocals = /* @__PURE__ */ new Map();
    for (const [name, idx] of savedForTdz) {
      fctx.tdzFlagLocals.set(name, idx);
    }
  }
  if (savedForConstBindings) {
    if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
    for (const [name, hadConstBinding] of savedForConstBindings) {
      if (hadConstBinding) fctx.constBindings.add(name);
      else fctx.constBindings.delete(name);
    }
  }
}
function compileDoWhileStatement(ctx, fctx, stmt) {
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] += 3;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] += 3;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth += 3;
  adjustRethrowDepth(fctx, 3);
  fctx.breakStack.push(2);
  fctx.continueStack.push(0);
  if (ts8.isBlock(stmt.statement)) {
    const savedScope = saveBlockScopedShadows(fctx, stmt.statement);
    for (const s of stmt.statement.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedScope);
  } else {
    compileStatement(ctx, fctx, stmt.statement);
  }
  const bodyInstrs = fctx.body;
  fctx.body = [];
  const condType = compileExpression2(ctx, fctx, stmt.expression);
  ensureI32Condition(fctx, condType, ctx);
  fctx.body.push({ op: "br_if", depth: 0 });
  const condInstrs = fctx.body;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] -= 3;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] -= 3;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth -= 3;
  adjustRethrowDepth(fctx, -3);
  popBody(fctx, savedBody);
  const loopBody = [
    {
      op: "block",
      blockType: { kind: "empty" },
      body: bodyInstrs,
    },
    ...condInstrs,
  ];
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
}
function compileSwitchStatement(ctx, fctx, stmt) {
  const exprType = ctx.checker.getTypeAtLocation(stmt.expression);
  let wasmType = resolveWasmType(ctx, exprType);
  let switchIsString = isStringType(exprType);
  if (!switchIsString) {
    for (const clause of stmt.caseBlock.clauses) {
      if (ts8.isCaseClause(clause)) {
        const caseType = ctx.checker.getTypeAtLocation(clause.expression);
        if (isStringType(caseType)) {
          switchIsString = true;
          break;
        }
      }
    }
  }
  let strEqFuncIdx;
  if (switchIsString) {
    if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
      ensureNativeStringHelpers(ctx);
      const flattenIdx = ctx.nativeStrHelpers.get("__str_flatten");
      const equalsIdx = ctx.nativeStrHelpers.get("__str_equals");
      strEqFuncIdx = equalsIdx;
      wasmType = { kind: "ref", typeIdx: ctx.anyStrTypeIdx };
    } else {
      addStringImports(ctx);
      strEqFuncIdx = ctx.funcMap.get("equals");
      wasmType = { kind: "externref" };
    }
  } else if (wasmType.kind === "externref") {
    wasmType = { kind: "f64" };
  }
  const tmpLocalIdx = allocLocal(fctx, `__sw_${fctx.locals.length}`, wasmType);
  compileExpression2(ctx, fctx, stmt.expression, wasmType);
  fctx.body.push({ op: "local.set", index: tmpLocalIdx });
  const clauses = stmt.caseBlock.clauses;
  const noMatchSentinel = clauses.length;
  const targetLocalIdx = allocLocal(fctx, `__sw_target_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: noMatchSentinel });
  fctx.body.push({ op: "local.set", index: targetLocalIdx });
  const eqOp = wasmType.kind === "i32" ? "i32.eq" : "f64.eq";
  let defaultIdx = -1;
  for (let ci = 0; ci < clauses.length; ci++) {
    const clause = clauses[ci];
    if (ts8.isDefaultClause(clause)) {
      defaultIdx = ci;
      continue;
    }
    const caseClause = clause;
    const checkBody = [];
    const outerBody = fctx.body;
    fctx.body = checkBody;
    fctx.body.push({ op: "local.get", index: tmpLocalIdx });
    if (switchIsString && ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
      const flattenIdx = ctx.nativeStrHelpers.get("__str_flatten");
      fctx.body.push({ op: "call", funcIdx: flattenIdx });
    }
    compileExpression2(ctx, fctx, caseClause.expression, wasmType);
    if (switchIsString && ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
      const flattenIdx = ctx.nativeStrHelpers.get("__str_flatten");
      fctx.body.push({ op: "call", funcIdx: flattenIdx });
    }
    if (switchIsString && strEqFuncIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: strEqFuncIdx });
    } else {
      fctx.body.push({ op: eqOp });
    }
    const setTarget = [
      { op: "i32.const", value: ci },
      { op: "local.set", index: targetLocalIdx },
    ];
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: setTarget,
    });
    fctx.body = outerBody;
    fctx.body.push({ op: "local.get", index: targetLocalIdx });
    fctx.body.push({ op: "i32.const", value: noMatchSentinel });
    fctx.body.push({ op: "i32.eq" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: checkBody,
    });
  }
  if (defaultIdx >= 0) {
    const setDefault = [
      { op: "i32.const", value: defaultIdx },
      { op: "local.set", index: targetLocalIdx },
    ];
    fctx.body.push({ op: "local.get", index: targetLocalIdx });
    fctx.body.push({ op: "i32.const", value: noMatchSentinel });
    fctx.body.push({ op: "i32.eq" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: setDefault,
    });
  }
  const runningLocalIdx = allocLocal(fctx, `__sw_running_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: runningLocalIdx });
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]++;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]++;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth++;
  adjustRethrowDepth(fctx, 1);
  const switchBreakIdx = fctx.breakStack.length;
  fctx.breakStack.push(1);
  for (let ci = 0; ci < clauses.length; ci++) {
    const clause = clauses[ci];
    const activateBody = [
      { op: "i32.const", value: 1 },
      { op: "local.set", index: runningLocalIdx },
    ];
    fctx.body.push({ op: "local.get", index: targetLocalIdx });
    fctx.body.push({ op: "i32.const", value: ci });
    fctx.body.push({ op: "i32.eq" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: activateBody,
    });
    if (clause.statements.length > 0) {
      const bodyInstrs = [];
      const outerBody = fctx.body;
      fctx.body = bodyInstrs;
      for (let i = 0; i < switchBreakIdx; i++) fctx.breakStack[i]++;
      for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]++;
      if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth++;
      adjustRethrowDepth(fctx, 1);
      for (const s of clause.statements) {
        compileStatement(ctx, fctx, s);
      }
      for (let i = 0; i < switchBreakIdx; i++) fctx.breakStack[i]--;
      for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]--;
      if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth--;
      adjustRethrowDepth(fctx, -1);
      fctx.body = outerBody;
      fctx.body.push({ op: "local.get", index: runningLocalIdx });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: bodyInstrs,
      });
    }
  }
  fctx.breakStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]--;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]--;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth--;
  adjustRethrowDepth(fctx, -1);
  const switchBody = fctx.body;
  popBody(fctx, savedBody);
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: switchBody,
  });
}
function compileForOfDestructuring(ctx, fctx, pattern, elemLocal, elemType, stmt) {
  if (ts8.isObjectBindingPattern(pattern)) {
    if (elemType.kind !== "ref" && elemType.kind !== "ref_null") {
      if (elemType.kind === "externref") {
        fctx.body.push({ op: "local.get", index: elemLocal });
        compileExternrefObjectDestructuringDecl(ctx, fctx, pattern, elemType);
        syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
        return;
      }
      for (const element of pattern.elements) {
        if (!ts8.isBindingElement(element)) continue;
        if (!ts8.isIdentifier(element.name)) continue;
        const localName = element.name.text;
        const bindingTsType = ctx.checker.getTypeAtLocation(element);
        const bindingType = resolveWasmType(ctx, bindingTsType);
        const localIdx = allocLocal(fctx, localName, bindingType);
        if (element.initializer) {
          const instrs = collectInstrs(fctx, () => {
            compileExpression2(ctx, fctx, element.initializer, bindingType);
            fctx.body.push({ op: "local.set", index: localIdx });
          });
          fctx.body.push(...instrs);
        } else {
          if (bindingType.kind === "f64") {
            fctx.body.push({ op: "f64.const", value: NaN });
          } else if (bindingType.kind === "i32") {
            fctx.body.push({ op: "i32.const", value: 0 });
          } else if (bindingType.kind === "ref_null" || bindingType.kind === "ref") {
            const refTypeIdx = bindingType.typeIdx;
            fctx.body.push({ op: "ref.null", typeIdx: refTypeIdx });
          } else {
            fctx.body.push({ op: "ref.null.extern" });
          }
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      }
      return;
    }
    const structTypeIdx = elemType.typeIdx;
    const typeDef = ctx.mod.types[structTypeIdx];
    if (!typeDef || typeDef.kind !== "struct") {
      ctx.errors.push({
        message: "for-of destructuring: element type is not a struct",
        line: getLine2(stmt),
        column: getCol2(stmt),
      });
      return;
    }
    const structName = ctx.typeIdxToStructName.get(structTypeIdx);
    const fields = structName ? ctx.structFields.get(structName) : void 0;
    if (!fields) {
      ctx.errors.push({
        message: "for-of destructuring: cannot find struct fields",
        line: getLine2(stmt),
        column: getCol2(stmt),
      });
      return;
    }
    emitNullGuard(ctx, fctx, elemLocal, elemType.kind === "ref_null", () => {
      for (const element of pattern.elements) {
        if (!ts8.isBindingElement(element)) continue;
        if (element.dotDotDotToken) {
          if (ts8.isIdentifier(element.name)) {
            const restName = element.name.text;
            let restIdx = fctx.localMap.get(restName);
            if (restIdx === void 0) {
              restIdx = allocLocal(fctx, restName, { kind: "externref" });
            }
            const excludedKeys = [];
            for (const el of pattern.elements) {
              if (!ts8.isBindingElement(el) || el.dotDotDotToken) continue;
              const pn = el.propertyName ?? el.name;
              if (ts8.isIdentifier(pn)) excludedKeys.push(pn.text);
              else if (ts8.isStringLiteral(pn)) excludedKeys.push(pn.text);
              else if (ts8.isNumericLiteral(pn)) excludedKeys.push(pn.text);
            }
            let restObjIdx = ctx.funcMap.get("__extern_rest_object");
            if (restObjIdx === void 0) {
              const importsBefore = ctx.numImportFuncs;
              const restObjType = addFuncType(
                ctx,
                [{ kind: "externref" }, { kind: "externref" }],
                [{ kind: "externref" }],
              );
              addImport(ctx, "env", "__extern_rest_object", { kind: "func", typeIdx: restObjType });
              shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
              restObjIdx = ctx.funcMap.get("__extern_rest_object");
            }
            if (restObjIdx !== void 0) {
              const excludedStr = excludedKeys.join(",");
              addStringConstantGlobal(ctx, excludedStr);
              const excludedStrIdx = ctx.stringGlobalMap.get(excludedStr);
              if (excludedStrIdx !== void 0) {
                fctx.body.push({ op: "local.get", index: elemLocal });
                fctx.body.push({ op: "extern.convert_any" });
                fctx.body.push({ op: "global.get", index: excludedStrIdx });
                fctx.body.push({ op: "call", funcIdx: restObjIdx });
                fctx.body.push({ op: "local.set", index: restIdx });
              }
            }
          }
          continue;
        }
        const propNameNode = element.propertyName ?? element.name;
        let propNameText = ts8.isIdentifier(propNameNode)
          ? propNameNode.text
          : ts8.isStringLiteral(propNameNode)
            ? propNameNode.text
            : ts8.isNumericLiteral(propNameNode)
              ? propNameNode.text
              : void 0;
        if (!propNameText && ts8.isComputedPropertyName(propNameNode)) {
          propNameText = resolveComputedKeyExpression(ctx, propNameNode.expression);
        }
        if (!ts8.isIdentifier(element.name)) continue;
        const localName = element.name.text;
        if (!propNameText) continue;
        const fieldIdx = fields.findIndex((f) => f.name === propNameText);
        if (fieldIdx === -1) {
          const bindingTsType = ctx.checker.getTypeAtLocation(element);
          const bindingType = resolveWasmType(ctx, bindingTsType);
          const localIdx2 = allocLocal(fctx, localName, bindingType);
          if (element.initializer) {
            const instrs = collectInstrs(fctx, () => {
              compileExpression2(ctx, fctx, element.initializer, bindingType);
              fctx.body.push({ op: "local.set", index: localIdx2 });
            });
            fctx.body.push(...instrs);
          } else {
            if (bindingType.kind === "f64") {
              fctx.body.push({ op: "f64.const", value: NaN });
            } else if (bindingType.kind === "i32") {
              fctx.body.push({ op: "i32.const", value: 0 });
            } else if (bindingType.kind === "ref_null" || bindingType.kind === "ref") {
              const refTypeIdx = bindingType.typeIdx;
              fctx.body.push({ op: "ref.null", typeIdx: refTypeIdx });
            } else {
              fctx.body.push({ op: "ref.null.extern" });
            }
            fctx.body.push({ op: "local.set", index: localIdx2 });
          }
          continue;
        }
        const fieldEntry = fields[fieldIdx];
        if (!fieldEntry) continue;
        const fieldType = fieldEntry.type;
        const localIdx = allocLocal(fctx, localName, fieldType);
        fctx.body.push({ op: "local.get", index: elemLocal });
        fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
        if (element.initializer) {
          emitDefaultValueCheck(ctx, fctx, fieldType, localIdx, element.initializer);
        } else {
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      }
    });
  } else if (ts8.isArrayBindingPattern(pattern)) {
    if (elemType.kind !== "ref" && elemType.kind !== "ref_null") {
      if (elemType.kind === "externref") {
        fctx.body.push({ op: "local.get", index: elemLocal });
        compileExternrefArrayDestructuringDecl(ctx, fctx, pattern, elemType);
        syncDestructuredLocalsToGlobals(ctx, fctx, pattern);
        return;
      }
      for (const element of pattern.elements) {
        if (ts8.isOmittedExpression(element)) continue;
        if (!ts8.isBindingElement(element)) continue;
        if (!ts8.isIdentifier(element.name)) continue;
        const localName = element.name.text;
        const bindingTsType = ctx.checker.getTypeAtLocation(element);
        const bindingType = resolveWasmType(ctx, bindingTsType);
        const localIdx = allocLocal(fctx, localName, bindingType);
        if (element.initializer) {
          const instrs = collectInstrs(fctx, () => {
            compileExpression2(ctx, fctx, element.initializer, bindingType);
            fctx.body.push({ op: "local.set", index: localIdx });
          });
          fctx.body.push(...instrs);
        } else {
          if (bindingType.kind === "f64") {
            fctx.body.push({ op: "f64.const", value: NaN });
          } else if (bindingType.kind === "i32") {
            fctx.body.push({ op: "i32.const", value: 0 });
          } else if (bindingType.kind === "ref_null" || bindingType.kind === "ref") {
            const refTypeIdx = bindingType.typeIdx;
            fctx.body.push({ op: "ref.null", typeIdx: refTypeIdx });
          } else {
            fctx.body.push({ op: "ref.null.extern" });
          }
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      }
      return;
    }
    const structTypeIdx = elemType.typeIdx;
    const structDef = ctx.mod.types[structTypeIdx];
    const isTupleStruct =
      structDef &&
      structDef.kind === "struct" &&
      structDef.fields.length > 0 &&
      structDef.fields.every((f, idx) => f.name === `_${idx}`);
    if (isTupleStruct) {
      const tupleFields = structDef.fields;
      emitNullGuard(ctx, fctx, elemLocal, elemType.kind === "ref_null", () => {
        for (let i = 0; i < pattern.elements.length; i++) {
          const element = pattern.elements[i];
          if (ts8.isOmittedExpression(element)) continue;
          if (i >= tupleFields.length) break;
          const fieldType = tupleFields[i].type;
          if (ts8.isBindingElement(element) && element.dotDotDotToken) {
            const restName = ts8.isIdentifier(element.name) ? element.name.text : `__rest_${fctx.locals.length}`;
            let restIdx = fctx.localMap.get(restName);
            if (restIdx === void 0) {
              restIdx = allocLocal(fctx, restName, { kind: "externref" });
            }
            let sliceIdx = ctx.funcMap.get("__extern_slice");
            if (sliceIdx === void 0) {
              const importsBefore = ctx.numImportFuncs;
              const sliceType = addFuncType(ctx, [{ kind: "externref" }, { kind: "f64" }], [{ kind: "externref" }]);
              addImport(ctx, "env", "__extern_slice", { kind: "func", typeIdx: sliceType });
              shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
              sliceIdx = ctx.funcMap.get("__extern_slice");
            }
            if (sliceIdx !== void 0) {
              fctx.body.push({ op: "local.get", index: elemLocal });
              fctx.body.push({ op: "extern.convert_any" });
              fctx.body.push({ op: "f64.const", value: i });
              fctx.body.push({ op: "call", funcIdx: sliceIdx });
              fctx.body.push({ op: "local.set", index: restIdx });
            }
            continue;
          }
          if (
            ts8.isBindingElement(element) &&
            (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name))
          ) {
            const nestedLocal = allocLocal(fctx, `__forof_nested_${fctx.locals.length}`, fieldType);
            fctx.body.push({ op: "local.get", index: elemLocal });
            fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: i });
            fctx.body.push({ op: "local.set", index: nestedLocal });
            compileForOfDestructuring(ctx, fctx, element.name, nestedLocal, fieldType, stmt);
            continue;
          }
          if (!ts8.isIdentifier(element.name)) continue;
          const localName = element.name.text;
          const bindingTsType = ctx.checker.getTypeAtLocation(element);
          const bindingWasmType = resolveWasmType(ctx, bindingTsType);
          const localIdx = allocLocal(fctx, localName, bindingWasmType);
          fctx.body.push({ op: "local.get", index: elemLocal });
          fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: i });
          if (!valTypesMatch(fieldType, bindingWasmType)) {
            coerceType3(ctx, fctx, fieldType, bindingWasmType);
          }
          if (element.initializer) {
            emitDefaultValueCheck(ctx, fctx, bindingWasmType, localIdx, element.initializer);
          } else {
            fctx.body.push({ op: "local.set", index: localIdx });
          }
        }
      });
      return;
    }
    const innerArrTypeIdx = getArrTypeIdxFromVec(ctx, structTypeIdx);
    const arrDef = ctx.mod.types[innerArrTypeIdx];
    if (!arrDef || arrDef.kind !== "array") {
      ctx.errors.push({
        message: "for-of array destructuring: element is not an array type",
        line: getLine2(stmt),
        column: getCol2(stmt),
      });
      return;
    }
    const innerElemType = arrDef.element;
    emitNullGuard(ctx, fctx, elemLocal, elemType.kind === "ref_null", () => {
      for (let i = 0; i < pattern.elements.length; i++) {
        const element = pattern.elements[i];
        if (ts8.isOmittedExpression(element)) continue;
        if (
          ts8.isBindingElement(element) &&
          (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name))
        ) {
          const nestedLocal = allocLocal(fctx, `__forof_nested_${fctx.locals.length}`, innerElemType);
          fctx.body.push({ op: "local.get", index: elemLocal });
          fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: 1 });
          fctx.body.push({ op: "i32.const", value: i });
          emitBoundsCheckedArrayGet2(fctx, innerArrTypeIdx, innerElemType);
          fctx.body.push({ op: "local.set", index: nestedLocal });
          compileForOfDestructuring(ctx, fctx, element.name, nestedLocal, innerElemType, stmt);
          continue;
        }
        if (ts8.isBindingElement(element) && element.dotDotDotToken) {
          const restName = ts8.isIdentifier(element.name) ? element.name.text : `__rest_${fctx.locals.length}`;
          const restLenLocal = allocLocal(fctx, `__rest_len_${fctx.locals.length}`, { kind: "i32" });
          fctx.body.push({ op: "local.get", index: elemLocal });
          fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: 0 });
          fctx.body.push({ op: "i32.const", value: i });
          fctx.body.push({ op: "i32.sub" });
          fctx.body.push({ op: "local.set", index: restLenLocal });
          fctx.body.push({ op: "i32.const", value: 0 });
          fctx.body.push({ op: "local.get", index: restLenLocal });
          fctx.body.push({ op: "local.get", index: restLenLocal });
          fctx.body.push({ op: "i32.const", value: 0 });
          fctx.body.push({ op: "i32.lt_s" });
          fctx.body.push({ op: "select" });
          fctx.body.push({ op: "local.set", index: restLenLocal });
          const restArrLocal = allocLocal(fctx, `__rest_arr_${fctx.locals.length}`, {
            kind: "ref",
            typeIdx: innerArrTypeIdx,
          });
          fctx.body.push({ op: "local.get", index: restLenLocal });
          fctx.body.push({ op: "array.new_default", typeIdx: innerArrTypeIdx });
          fctx.body.push({ op: "local.set", index: restArrLocal });
          fctx.body.push({ op: "local.get", index: restArrLocal });
          fctx.body.push({ op: "i32.const", value: 0 });
          fctx.body.push({ op: "local.get", index: elemLocal });
          fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: 1 });
          fctx.body.push({ op: "i32.const", value: i });
          fctx.body.push({ op: "local.get", index: restLenLocal });
          fctx.body.push({ op: "array.copy", dstTypeIdx: innerArrTypeIdx, srcTypeIdx: innerArrTypeIdx });
          const restVecType = { kind: "ref", typeIdx: structTypeIdx };
          fctx.body.push({ op: "local.get", index: restLenLocal });
          fctx.body.push({ op: "local.get", index: restArrLocal });
          fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
          let restIdx = fctx.localMap.get(restName);
          if (restIdx === void 0) {
            restIdx = allocLocal(fctx, restName, restVecType);
          }
          fctx.body.push({ op: "local.set", index: restIdx });
          continue;
        }
        if (!ts8.isIdentifier(element.name)) continue;
        const localName = element.name.text;
        const bindingTsType = ctx.checker.getTypeAtLocation(element);
        const bindingWasmType = resolveWasmType(ctx, bindingTsType);
        const localIdx = allocLocal(fctx, localName, bindingWasmType);
        fctx.body.push({ op: "local.get", index: elemLocal });
        fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: 1 });
        fctx.body.push({ op: "i32.const", value: i });
        emitBoundsCheckedArrayGet2(fctx, innerArrTypeIdx, innerElemType);
        if (!valTypesMatch(innerElemType, bindingWasmType)) {
          coerceType3(ctx, fctx, innerElemType, bindingWasmType);
        }
        if (element.initializer) {
          emitDefaultValueCheck(ctx, fctx, bindingWasmType, localIdx, element.initializer);
        } else {
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      }
    });
  }
}
function compileForOfAssignDestructuring(ctx, fctx, expr, elemLocal, elemType, vecTypeIdx, arrTypeIdx, stmt) {
  if (ts8.isObjectLiteralExpression(expr)) {
    if (elemType.kind !== "ref" && elemType.kind !== "ref_null") {
      for (const prop of expr.properties) {
        if (ts8.isSpreadAssignment(prop)) continue;
        if (!ts8.isShorthandPropertyAssignment(prop) && !ts8.isPropertyAssignment(prop)) continue;
        const targetName = ts8.isShorthandPropertyAssignment(prop)
          ? prop.name.text
          : ts8.isPropertyAssignment(prop) && ts8.isIdentifier(prop.initializer)
            ? prop.initializer.text
            : ts8.isIdentifier(prop.name)
              ? prop.name.text
              : void 0;
        if (!targetName) continue;
        const targetLocal = fctx.localMap.get(targetName);
        if (targetLocal === void 0) continue;
        const init = ts8.isShorthandPropertyAssignment(prop) ? prop.objectAssignmentInitializer : void 0;
        if (init) {
          const targetType = getLocalType(fctx, targetLocal);
          const instrs = collectInstrs(fctx, () => {
            compileExpression2(ctx, fctx, init, targetType ?? { kind: "externref" });
            fctx.body.push({ op: "local.set", index: targetLocal });
          });
          fctx.body.push(...instrs);
        }
      }
      return;
    }
    const structTypeIdx = elemType.typeIdx;
    const typeDef = ctx.mod.types[structTypeIdx];
    if (!typeDef || typeDef.kind !== "struct") return;
    const structName = ctx.typeIdxToStructName.get(structTypeIdx);
    const fields = structName ? ctx.structFields.get(structName) : void 0;
    if (!fields) return;
    for (const prop of expr.properties) {
      if (!ts8.isShorthandPropertyAssignment(prop) && !ts8.isPropertyAssignment(prop)) continue;
      let propName = ts8.isShorthandPropertyAssignment(prop)
        ? prop.name.text
        : ts8.isIdentifier(prop.name)
          ? prop.name.text
          : ts8.isStringLiteral(prop.name)
            ? prop.name.text
            : void 0;
      if (!propName && ts8.isPropertyAssignment(prop) && ts8.isComputedPropertyName(prop.name)) {
        propName = resolveComputedKeyExpression(ctx, prop.name.expression);
      }
      if (!propName) continue;
      const targetName = ts8.isShorthandPropertyAssignment(prop)
        ? prop.name.text
        : ts8.isPropertyAssignment(prop) && ts8.isIdentifier(prop.initializer)
          ? prop.initializer.text
          : propName;
      const fieldIdx = fields.findIndex((f) => f.name === propName);
      if (fieldIdx === -1) continue;
      const targetLocal = fctx.localMap.get(targetName);
      if (targetLocal === void 0) continue;
      const fieldEntry2 = fields[fieldIdx];
      if (!fieldEntry2) continue;
      const fieldType = fieldEntry2.type;
      const targetType = getLocalType(fctx, targetLocal);
      fctx.body.push({ op: "local.get", index: elemLocal });
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
      if (targetType && !valTypesMatch(fieldType, targetType)) {
        coerceType3(ctx, fctx, fieldType, targetType);
      }
      emitCoercedLocalSet(ctx, fctx, targetLocal, fieldType);
    }
  } else if (ts8.isArrayLiteralExpression(expr)) {
    if (elemType.kind !== "ref" && elemType.kind !== "ref_null") {
      if (elemType.kind === "externref") {
        compileForOfAssignDestructuringExternref(ctx, fctx, expr, elemLocal);
      }
      return;
    }
    const innerVecTypeIdx = elemType.typeIdx;
    const innerStructDef = ctx.mod.types[innerVecTypeIdx];
    const isTuple =
      innerStructDef &&
      innerStructDef.kind === "struct" &&
      innerStructDef.fields.length > 0 &&
      innerStructDef.fields.every((f, idx) => f.name === `_${idx}`);
    if (innerStructDef && innerStructDef.kind === "struct" && innerStructDef.fields.length === 0) {
      for (let i = 0; i < expr.elements.length; i++) {
        const el = expr.elements[i];
        if (ts8.isOmittedExpression(el)) continue;
        let oobTarget = el;
        let oobInit;
        if (ts8.isBinaryExpression(el) && el.operatorToken.kind === ts8.SyntaxKind.EqualsToken) {
          oobTarget = el.left;
          oobInit = el.right;
        }
        if (oobInit && ts8.isIdentifier(oobTarget)) {
          const oobLocal = fctx.localMap.get(oobTarget.text);
          if (oobLocal !== void 0) {
            const oobType = getLocalType(fctx, oobLocal);
            const instrs = collectInstrs(fctx, () => {
              compileExpression2(ctx, fctx, oobInit, oobType ?? { kind: "f64" });
              fctx.body.push({ op: "local.set", index: oobLocal });
            });
            fctx.body.push(...instrs);
          }
        }
      }
      return;
    }
    if (isTuple) {
      const tupleFields = innerStructDef.fields;
      for (let i = 0; i < expr.elements.length; i++) {
        const el = expr.elements[i];
        if (ts8.isOmittedExpression(el)) continue;
        if (i >= tupleFields.length) {
          let oobTarget = el;
          let oobInit;
          if (ts8.isBinaryExpression(el) && el.operatorToken.kind === ts8.SyntaxKind.EqualsToken) {
            oobTarget = el.left;
            oobInit = el.right;
          }
          if (oobInit && ts8.isIdentifier(oobTarget)) {
            const oobLocal = fctx.localMap.get(oobTarget.text);
            if (oobLocal !== void 0) {
              const oobType = getLocalType(fctx, oobLocal);
              const instrs = collectInstrs(fctx, () => {
                compileExpression2(ctx, fctx, oobInit, oobType ?? { kind: "f64" });
                fctx.body.push({ op: "local.set", index: oobLocal });
              });
              fctx.body.push(...instrs);
            }
          }
          continue;
        }
        const fieldType = tupleFields[i].type;
        if (ts8.isObjectLiteralExpression(el) || ts8.isArrayLiteralExpression(el)) {
          const nestedLocal = allocLocal(fctx, `__forof_nested_${fctx.locals.length}`, fieldType);
          fctx.body.push({ op: "local.get", index: elemLocal });
          fctx.body.push({ op: "struct.get", typeIdx: innerVecTypeIdx, fieldIdx: i });
          fctx.body.push({ op: "local.set", index: nestedLocal });
          compileForOfAssignDestructuring(ctx, fctx, el, nestedLocal, fieldType, vecTypeIdx, arrTypeIdx, stmt);
          continue;
        }
        let targetEl = el;
        let defaultInit;
        if (ts8.isBinaryExpression(el) && el.operatorToken.kind === ts8.SyntaxKind.EqualsToken) {
          targetEl = el.left;
          defaultInit = el.right;
        }
        if (!ts8.isIdentifier(targetEl)) continue;
        const targetLocal = fctx.localMap.get(targetEl.text);
        if (targetLocal === void 0) continue;
        const targetType = getLocalType(fctx, targetLocal);
        fctx.body.push({ op: "local.get", index: elemLocal });
        fctx.body.push({ op: "struct.get", typeIdx: innerVecTypeIdx, fieldIdx: i });
        if (defaultInit) {
          emitDefaultValueCheck(ctx, fctx, fieldType, targetLocal, defaultInit, targetType ?? void 0);
        } else {
          if (targetType && !valTypesMatch(fieldType, targetType)) {
            coerceType3(ctx, fctx, fieldType, targetType);
          }
          fctx.body.push({ op: "local.set", index: targetLocal });
        }
      }
    } else {
      const innerArrTypeIdx = getArrTypeIdxFromVec(ctx, innerVecTypeIdx);
      const innerArrDef = ctx.mod.types[innerArrTypeIdx];
      if (!innerArrDef || innerArrDef.kind !== "array") return;
      const innerElemType = innerArrDef.element;
      for (let i = 0; i < expr.elements.length; i++) {
        const el = expr.elements[i];
        if (ts8.isOmittedExpression(el)) continue;
        if (ts8.isObjectLiteralExpression(el) || ts8.isArrayLiteralExpression(el)) {
          const nestedLocal = allocLocal(fctx, `__forof_nested_${fctx.locals.length}`, innerElemType);
          fctx.body.push({ op: "local.get", index: elemLocal });
          fctx.body.push({ op: "struct.get", typeIdx: innerVecTypeIdx, fieldIdx: 1 });
          fctx.body.push({ op: "i32.const", value: i });
          emitBoundsCheckedArrayGet2(fctx, innerArrTypeIdx, innerElemType);
          fctx.body.push({ op: "local.set", index: nestedLocal });
          compileForOfAssignDestructuring(ctx, fctx, el, nestedLocal, innerElemType, vecTypeIdx, arrTypeIdx, stmt);
          continue;
        }
        let targetEl = el;
        let defaultInit;
        if (ts8.isBinaryExpression(el) && el.operatorToken.kind === ts8.SyntaxKind.EqualsToken) {
          targetEl = el.left;
          defaultInit = el.right;
        }
        if (!ts8.isIdentifier(targetEl)) continue;
        const targetLocal = fctx.localMap.get(targetEl.text);
        if (targetLocal === void 0) continue;
        const targetType = getLocalType(fctx, targetLocal);
        if (defaultInit && innerElemType.kind === "externref") {
          const arrDataLocal = allocLocal(fctx, `__forof_arr_${fctx.locals.length}`, {
            kind: "ref",
            typeIdx: innerArrTypeIdx,
          });
          fctx.body.push({ op: "local.get", index: elemLocal });
          fctx.body.push({ op: "struct.get", typeIdx: innerVecTypeIdx, fieldIdx: 1 });
          fctx.body.push({ op: "local.tee", index: arrDataLocal });
          fctx.body.push({ op: "array.len" });
          fctx.body.push({ op: "i32.const", value: i });
          fctx.body.push({ op: "i32.gt_s" });
          const hintType = targetType ?? innerElemType;
          const thenInstrs = collectInstrs(fctx, () => {
            fctx.body.push({ op: "local.get", index: arrDataLocal });
            fctx.body.push({ op: "i32.const", value: i });
            fctx.body.push({ op: "array.get", typeIdx: innerArrTypeIdx });
            emitDefaultValueCheck(ctx, fctx, innerElemType, targetLocal, defaultInit, targetType ?? void 0);
          });
          const elseInstrs = collectInstrs(fctx, () => {
            compileExpression2(ctx, fctx, defaultInit, hintType);
            fctx.body.push({ op: "local.set", index: targetLocal });
          });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
            else: elseInstrs,
          });
        } else {
          fctx.body.push({ op: "local.get", index: elemLocal });
          fctx.body.push({ op: "struct.get", typeIdx: innerVecTypeIdx, fieldIdx: 1 });
          fctx.body.push({ op: "i32.const", value: i });
          emitBoundsCheckedArrayGet2(fctx, innerArrTypeIdx, innerElemType);
          if (defaultInit) {
            emitDefaultValueCheck(ctx, fctx, innerElemType, targetLocal, defaultInit, targetType ?? void 0);
          } else {
            if (targetType && !valTypesMatch(innerElemType, targetType)) {
              coerceType3(ctx, fctx, innerElemType, targetType);
            }
            fctx.body.push({ op: "local.set", index: targetLocal });
          }
        }
      }
    }
  }
}
function compileForOfAssignDestructuringExternref(ctx, fctx, expr, elemLocal) {
  let getIdx = ctx.funcMap.get("__extern_get");
  if (getIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const getType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__extern_get", { kind: "func", typeIdx: getType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (getIdx === void 0) return;
  let boxIdx = ctx.funcMap.get("__box_number");
  if (boxIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const boxType = addFuncType(ctx, [{ kind: "f64" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__box_number", { kind: "func", typeIdx: boxType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    boxIdx = ctx.funcMap.get("__box_number");
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (boxIdx === void 0 || getIdx === void 0) return;
  for (let i = 0; i < expr.elements.length; i++) {
    const el = expr.elements[i];
    if (ts8.isOmittedExpression(el)) continue;
    if (ts8.isSpreadElement(el)) continue;
    let targetEl = el;
    let defaultInit;
    if (ts8.isBinaryExpression(el) && el.operatorToken.kind === ts8.SyntaxKind.EqualsToken) {
      targetEl = el.left;
      defaultInit = el.right;
    }
    if (!ts8.isIdentifier(targetEl)) continue;
    const targetLocal = fctx.localMap.get(targetEl.text);
    if (targetLocal === void 0) continue;
    fctx.body.push({ op: "local.get", index: elemLocal });
    fctx.body.push({ op: "f64.const", value: i });
    fctx.body.push({ op: "call", funcIdx: boxIdx });
    fctx.body.push({ op: "call", funcIdx: getIdx });
    if (defaultInit) {
      const targetType = getLocalType(fctx, targetLocal);
      emitDefaultValueCheck(ctx, fctx, { kind: "externref" }, targetLocal, defaultInit, targetType ?? void 0);
    } else {
      emitCoercedLocalSet(ctx, fctx, targetLocal, { kind: "externref" });
    }
  }
}
function collectBindingNames(pattern) {
  const names = [];
  for (const element of pattern.elements) {
    if (ts8.isOmittedExpression(element)) continue;
    if (ts8.isBindingElement(element)) {
      if (ts8.isIdentifier(element.name)) {
        names.push(element.name.text);
      } else if (ts8.isObjectBindingPattern(element.name) || ts8.isArrayBindingPattern(element.name)) {
        names.push(...collectBindingNames(element.name));
      }
    }
  }
  return names;
}
function compileForOfStatement(ctx, fctx, stmt) {
  const exprTsType = ctx.checker.getTypeAtLocation(stmt.expression);
  if (isStringType(exprTsType) && ctx.nativeStrings && ctx.anyStrTypeIdx >= 0) {
    compileForOfString(ctx, fctx, stmt);
    return;
  }
  const sym = exprTsType.symbol ?? exprTsType.symbol;
  const isArray = sym?.name === "Array";
  if (isArray) {
    compileForOfArray(ctx, fctx, stmt);
  } else {
    if (!compileForOfArrayTentative(ctx, fctx, stmt)) {
      compileForOfIterator(ctx, fctx, stmt);
    }
  }
}
function compileForOfString(ctx, fctx, stmt) {
  ensureNativeStringHelpers(ctx);
  const charAtIdx = ctx.nativeStrHelpers.get("__str_charAt");
  if (charAtIdx === void 0) {
    ctx.errors.push({
      message: "for-of on string: __str_charAt helper not available",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  const strType = nativeStringType(ctx);
  const anyStrTypeIdx = ctx.anyStrTypeIdx;
  const bodyLenBefore = fctx.body.length;
  const compiledType = compileExpression2(ctx, fctx, stmt.expression);
  if (!compiledType) {
    fctx.body.length = bodyLenBefore;
    ctx.errors.push({
      message: "for-of: failed to compile string expression",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  const strLocal = allocLocal(fctx, `__forof_str_${fctx.locals.length}`, strType);
  fctx.body.push({ op: "local.set", index: strLocal });
  const strNullGuardStart = fctx.body.length;
  const lenLocal = allocLocal(fctx, `__forof_len_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.get", index: strLocal });
  fctx.body.push({ op: "struct.get", typeIdx: anyStrTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenLocal });
  const iLocal = allocLocal(fctx, `__forof_i_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iLocal });
  const elemType = strType;
  let elemLocal;
  if (ts8.isVariableDeclarationList(stmt.initializer)) {
    const decl = stmt.initializer.declarations[0];
    const varName = ts8.isIdentifier(decl.name) ? decl.name.text : `__forof_elem_${fctx.locals.length}`;
    elemLocal = allocLocal(fctx, varName, elemType);
    if (stmt.initializer.flags & ts8.NodeFlags.Const && ts8.isIdentifier(decl.name)) {
      if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
      fctx.constBindings.add(decl.name.text);
    }
  } else if (ts8.isIdentifier(stmt.initializer)) {
    const varName = stmt.initializer.text;
    elemLocal = fctx.localMap.get(varName) ?? allocLocal(fctx, varName, elemType);
  } else {
    elemLocal = allocLocal(fctx, `__forof_elem_${fctx.locals.length}`, elemType);
  }
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] += 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] += 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth += 2;
  adjustRethrowDepth(fctx, 2);
  fctx.breakStack.push(1);
  fctx.continueStack.push(0);
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "local.get", index: lenLocal });
  fctx.body.push({ op: "i32.ge_s" });
  fctx.body.push({ op: "br_if", depth: 1 });
  fctx.body.push({ op: "local.get", index: strLocal });
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "call", funcIdx: charAtIdx });
  fctx.body.push({ op: "local.set", index: elemLocal });
  if (ts8.isBlock(stmt.statement)) {
    const savedScope = saveBlockScopedShadows(fctx, stmt.statement);
    for (const s of stmt.statement.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedScope);
  } else {
    compileStatement(ctx, fctx, stmt.statement);
  }
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: iLocal });
  fctx.body.push({ op: "br", depth: 0 });
  const loopBody = fctx.body;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] -= 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] -= 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth -= 2;
  adjustRethrowDepth(fctx, -2);
  popBody(fctx, savedBody);
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
  if (strType.kind === "ref_null") {
    const guardedInstrs = fctx.body.splice(strNullGuardStart);
    const tagIdx = ensureExnTag(ctx);
    fctx.body.push({ op: "local.get", index: strLocal });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [{ op: "ref.null.extern" }, { op: "throw", tagIdx }],
      else: guardedInstrs,
    });
  }
}
function compileForOfArrayTentative(ctx, fctx, stmt) {
  const bodyLenBefore = fctx.body.length;
  const localsLenBefore = fctx.locals.length;
  const exprType = compileExpression2(ctx, fctx, stmt.expression);
  if (exprType && (exprType.kind === "ref" || exprType.kind === "ref_null")) {
    const typeDef = ctx.mod.types[exprType.typeIdx];
    if (typeDef && typeDef.kind === "struct" && getArrTypeIdxFromVec(ctx, exprType.typeIdx) >= 0) {
      fctx.body.length = bodyLenBefore;
      fctx.locals.length = localsLenBefore;
      compileForOfArray(ctx, fctx, stmt);
      return true;
    }
  }
  fctx.body.length = bodyLenBefore;
  fctx.locals.length = localsLenBefore;
  return false;
}
function compileForOfArray(ctx, fctx, stmt) {
  const bodyLenBefore = fctx.body.length;
  const vecType = compileExpression2(ctx, fctx, stmt.expression);
  if (!vecType || (vecType.kind !== "ref" && vecType.kind !== "ref_null")) {
    fctx.body.length = bodyLenBefore;
    ctx.errors.push({
      message: "for-of requires an array expression",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  const vecTypeIdx = vecType.typeIdx;
  const vecDef = ctx.mod.types[vecTypeIdx];
  if (!vecDef || vecDef.kind !== "struct") {
    fctx.body.length = bodyLenBefore;
    ctx.errors.push({
      message: "for-of requires an array type",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
  const arrDef = ctx.mod.types[arrTypeIdx];
  if (!arrDef || arrDef.kind !== "array") {
    fctx.body.length = bodyLenBefore;
    ctx.errors.push({
      message: "for-of requires an array type",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  const elemType = arrDef.element;
  const vecLocal = allocLocal(fctx, `__forof_vec_${fctx.locals.length}`, vecType);
  fctx.body.push({ op: "local.set", index: vecLocal });
  const nullGuardStart = fctx.body.length;
  const dataLocal = allocLocal(fctx, `__forof_data_${fctx.locals.length}`, {
    kind: "ref_null",
    typeIdx: arrTypeIdx,
  });
  fctx.body.push({ op: "local.get", index: vecLocal });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "local.set", index: dataLocal });
  const lenLocal = allocLocal(fctx, `__forof_len_${fctx.locals.length}`, {
    kind: "i32",
  });
  fctx.body.push({ op: "local.get", index: vecLocal });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
  fctx.body.push({ op: "local.set", index: lenLocal });
  const iLocal = allocLocal(fctx, `__forof_i_${fctx.locals.length}`, {
    kind: "i32",
  });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iLocal });
  let elemLocal;
  let destructPattern = null;
  let assignDestructExpr = null;
  if (ts8.isVariableDeclarationList(stmt.initializer)) {
    const decl = stmt.initializer.declarations[0];
    const isConst = !!(stmt.initializer.flags & ts8.NodeFlags.Const);
    if (ts8.isObjectBindingPattern(decl.name) || ts8.isArrayBindingPattern(decl.name)) {
      destructPattern = decl.name;
      elemLocal = allocLocal(fctx, `__forof_elem_${fctx.locals.length}`, elemType);
      if (isConst) {
        collectBindingNames(decl.name).forEach((n) => {
          if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
          fctx.constBindings.add(n);
        });
      }
    } else {
      const varName = ts8.isIdentifier(decl.name) ? decl.name.text : `__forof_elem_${fctx.locals.length}`;
      elemLocal = allocLocal(fctx, varName, elemType);
      if (isConst && ts8.isIdentifier(decl.name)) {
        if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
        fctx.constBindings.add(decl.name.text);
      }
    }
  } else if (ts8.isObjectLiteralExpression(stmt.initializer) || ts8.isArrayLiteralExpression(stmt.initializer)) {
    assignDestructExpr = stmt.initializer;
    elemLocal = allocLocal(fctx, `__forof_elem_${fctx.locals.length}`, elemType);
  } else if (ts8.isIdentifier(stmt.initializer)) {
    const varName = stmt.initializer.text;
    elemLocal = fctx.localMap.get(varName) ?? allocLocal(fctx, varName, elemType);
  } else {
    elemLocal = allocLocal(fctx, `__forof_elem_${fctx.locals.length}`, elemType);
  }
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] += 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] += 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth += 2;
  adjustRethrowDepth(fctx, 2);
  fctx.breakStack.push(1);
  fctx.continueStack.push(0);
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "local.get", index: lenLocal });
  fctx.body.push({ op: "i32.ge_s" });
  fctx.body.push({ op: "br_if", depth: 1 });
  fctx.body.push({ op: "local.get", index: dataLocal });
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "array.get", typeIdx: arrTypeIdx });
  const elemLocalType = getLocalType(fctx, elemLocal);
  if (elemLocalType && !valTypesMatch(elemType, elemLocalType)) {
    coerceType3(ctx, fctx, elemType, elemLocalType);
  }
  emitCoercedLocalSet(ctx, fctx, elemLocal, elemType);
  if (destructPattern) {
    compileForOfDestructuring(ctx, fctx, destructPattern, elemLocal, elemType, stmt);
  }
  if (assignDestructExpr) {
    compileForOfAssignDestructuring(ctx, fctx, assignDestructExpr, elemLocal, elemType, vecTypeIdx, arrTypeIdx, stmt);
  }
  if (ts8.isBlock(stmt.statement)) {
    const savedScope = saveBlockScopedShadows(fctx, stmt.statement);
    for (const s of stmt.statement.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedScope);
  } else {
    compileStatement(ctx, fctx, stmt.statement);
  }
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: iLocal });
  fctx.body.push({ op: "br", depth: 0 });
  const loopBody = fctx.body;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] -= 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] -= 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth -= 2;
  adjustRethrowDepth(fctx, -2);
  popBody(fctx, savedBody);
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
  if (vecType.kind === "ref_null") {
    const guardedInstrs = fctx.body.splice(nullGuardStart);
    const backupLocal = fctx.__lastGuardedCastBackup;
    fctx.body.push({ op: "local.get", index: vecLocal });
    fctx.body.push({ op: "ref.is_null" });
    if (backupLocal !== void 0) {
      const tagIdx = ensureExnTag(ctx);
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: backupLocal },
          { op: "ref.is_null" },
          {
            op: "if",
            blockType: { kind: "empty" },
            then: [{ op: "ref.null.extern" }, { op: "throw", tagIdx }],
            else: [],
            // wrong struct type → skip loop
          },
        ],
        else: guardedInstrs,
      });
    } else {
      const tagIdx = ensureExnTag(ctx);
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [{ op: "ref.null.extern" }, { op: "throw", tagIdx }],
        else: guardedInstrs,
      });
    }
  }
}
function compileForOfIteratorAssignDestructuring(ctx, fctx, expr, elemLocal, stmt) {
  let getIdx = ctx.funcMap.get("__extern_get");
  if (getIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const getType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__extern_get", { kind: "func", typeIdx: getType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (getIdx === void 0) return;
  if (ts8.isObjectLiteralExpression(expr)) {
    for (const prop of expr.properties) {
      if (ts8.isSpreadAssignment(prop)) continue;
      if (!ts8.isShorthandPropertyAssignment(prop) && !ts8.isPropertyAssignment(prop)) continue;
      const propName = ts8.isShorthandPropertyAssignment(prop)
        ? prop.name.text
        : ts8.isIdentifier(prop.name)
          ? prop.name.text
          : ts8.isStringLiteral(prop.name)
            ? prop.name.text
            : void 0;
      if (!propName) continue;
      const targetName = ts8.isShorthandPropertyAssignment(prop)
        ? prop.name.text
        : ts8.isPropertyAssignment(prop) && ts8.isIdentifier(prop.initializer)
          ? prop.initializer.text
          : propName;
      const targetLocal = fctx.localMap.get(targetName);
      if (targetLocal === void 0) continue;
      addStringConstantGlobal(ctx, propName);
      const strGlobalIdx = ctx.stringGlobalMap.get(propName);
      if (strGlobalIdx === void 0) continue;
      getIdx = ctx.funcMap.get("__extern_get");
      if (getIdx === void 0) continue;
      fctx.body.push({ op: "local.get", index: elemLocal });
      fctx.body.push({ op: "global.get", index: strGlobalIdx });
      fctx.body.push({ op: "call", funcIdx: getIdx });
      emitCoercedLocalSet(ctx, fctx, targetLocal, { kind: "externref" });
    }
  } else if (ts8.isArrayLiteralExpression(expr)) {
    let boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx === void 0) {
      const importsBefore = ctx.numImportFuncs;
      const boxType = addFuncType(ctx, [{ kind: "f64" }], [{ kind: "externref" }]);
      addImport(ctx, "env", "__box_number", { kind: "func", typeIdx: boxType });
      shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
      boxIdx = ctx.funcMap.get("__box_number");
      getIdx = ctx.funcMap.get("__extern_get");
    }
    if (boxIdx === void 0 || getIdx === void 0) return;
    for (let i = 0; i < expr.elements.length; i++) {
      const el = expr.elements[i];
      if (ts8.isOmittedExpression(el)) continue;
      if (ts8.isSpreadElement(el)) continue;
      if (!ts8.isIdentifier(el)) continue;
      const targetLocal = fctx.localMap.get(el.text);
      if (targetLocal === void 0) continue;
      fctx.body.push({ op: "local.get", index: elemLocal });
      fctx.body.push({ op: "f64.const", value: i });
      fctx.body.push({ op: "call", funcIdx: boxIdx });
      fctx.body.push({ op: "call", funcIdx: getIdx });
      emitCoercedLocalSet(ctx, fctx, targetLocal, { kind: "externref" });
    }
  }
}
function compileForOfDirectIterator(ctx, fctx, stmt, iterableType, iterMethodIdx) {
  const iterMethodDef = ctx.mod.functions[iterMethodIdx - ctx.numImportFuncs];
  if (!iterMethodDef) return false;
  const iterMethodType = ctx.mod.types[iterMethodDef.typeIdx];
  if (!iterMethodType || iterMethodType.kind !== "func" || iterMethodType.results.length === 0) return false;
  const iterResultType = iterMethodType.results[0];
  if (iterResultType.kind !== "ref" && iterResultType.kind !== "ref_null") return false;
  const iterStructTypeIdx = iterResultType.typeIdx;
  const iterStructDef = ctx.mod.types[iterStructTypeIdx];
  if (!iterStructDef || iterStructDef.kind !== "struct") return false;
  let iterStructName;
  for (const [name, idx] of ctx.structMap) {
    if (idx === iterStructTypeIdx) {
      iterStructName = name;
      break;
    }
  }
  if (!iterStructName) return false;
  const nextMethodIdx = ctx.funcMap.get(`${iterStructName}_next`);
  if (nextMethodIdx === void 0) return false;
  const nextMethodDef = ctx.mod.functions[nextMethodIdx - ctx.numImportFuncs];
  if (!nextMethodDef) return false;
  const nextMethodType = ctx.mod.types[nextMethodDef.typeIdx];
  if (!nextMethodType || nextMethodType.kind !== "func" || nextMethodType.results.length === 0) return false;
  const nextResultType = nextMethodType.results[0];
  if (nextResultType.kind !== "ref" && nextResultType.kind !== "ref_null") return false;
  const resultStructTypeIdx = nextResultType.typeIdx;
  const resultStructDef = ctx.mod.types[resultStructTypeIdx];
  if (!resultStructDef || resultStructDef.kind !== "struct") return false;
  const resultFields =
    ctx.structFields.get(`${iterStructName}_next_result`) ?? findStructFieldsByTypeIdx(ctx, resultStructTypeIdx);
  if (!resultFields) return false;
  let doneFieldIdx = -1;
  let valueFieldIdx = -1;
  let doneFieldType;
  let valueFieldType;
  for (let i = 0; i < resultFields.length; i++) {
    const f = resultFields[i];
    if (f.name === "done") {
      doneFieldIdx = i;
      doneFieldType = f.type;
    }
    if (f.name === "value") {
      valueFieldIdx = i;
      valueFieldType = f.type;
    }
  }
  if (doneFieldIdx < 0 || valueFieldIdx < 0 || !doneFieldType || !valueFieldType) return false;
  const nullTmp = allocLocal(fctx, `__forit_stmp_${fctx.locals.length}`, iterableType);
  fctx.body.push({ op: "local.tee", index: nullTmp });
  fctx.body.push({ op: "ref.is_null" });
  const tagIdx = ensureExnTag(ctx);
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [{ op: "ref.null.extern" }, { op: "throw", tagIdx }],
    else: [],
  });
  fctx.body.push({ op: "local.get", index: nullTmp });
  if (iterableType.kind === "ref_null") {
    fctx.body.push({ op: "ref.as_non_null" });
  }
  fctx.body.push({ op: "call", funcIdx: iterMethodIdx });
  const iterLocal = allocLocal(fctx, `__forit_iter_${fctx.locals.length}`, iterResultType);
  fctx.body.push({ op: "local.set", index: iterLocal });
  const resultLocal = allocLocal(fctx, `__forit_res_${fctx.locals.length}`, nextResultType);
  const elemType = valueFieldType;
  let elemLocal;
  let destructPatternIter = null;
  let assignDestructExprIter = null;
  if (ts8.isVariableDeclarationList(stmt.initializer)) {
    const decl = stmt.initializer.declarations[0];
    const isConst = !!(stmt.initializer.flags & ts8.NodeFlags.Const);
    if (ts8.isObjectBindingPattern(decl.name) || ts8.isArrayBindingPattern(decl.name)) {
      destructPatternIter = decl.name;
      elemLocal = allocLocal(fctx, `__forit_elem_${fctx.locals.length}`, elemType);
      if (isConst) {
        collectBindingNames(decl.name).forEach((n) => {
          if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
          fctx.constBindings.add(n);
        });
      }
    } else {
      const varName = ts8.isIdentifier(decl.name) ? decl.name.text : `__forit_elem_${fctx.locals.length}`;
      elemLocal = allocLocal(fctx, varName, elemType);
      if (isConst && ts8.isIdentifier(decl.name)) {
        if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
        fctx.constBindings.add(decl.name.text);
      }
    }
  } else if (ts8.isObjectLiteralExpression(stmt.initializer) || ts8.isArrayLiteralExpression(stmt.initializer)) {
    assignDestructExprIter = stmt.initializer;
    elemLocal = allocLocal(fctx, `__forit_elem_${fctx.locals.length}`, elemType);
  } else if (ts8.isIdentifier(stmt.initializer)) {
    const varName = stmt.initializer.text;
    elemLocal = fctx.localMap.get(varName) ?? allocLocal(fctx, varName, elemType);
  } else {
    elemLocal = allocLocal(fctx, `__forit_elem_${fctx.locals.length}`, elemType);
  }
  const returnMethodIdx = ctx.funcMap.get(`${iterStructName}_return`);
  const doneFlagDirect = allocLocal(fctx, `__forit_done_${fctx.locals.length}`, { kind: "i32" });
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] += 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] += 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth += 2;
  adjustRethrowDepth(fctx, 2);
  fctx.breakStack.push(1);
  fctx.continueStack.push(0);
  const iterCountLocal = allocLocal(fctx, `__forit_guard_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.get", index: iterCountLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.tee", index: iterCountLocal });
  fctx.body.push({ op: "i32.const", value: 1e6 });
  fctx.body.push({ op: "i32.gt_s" });
  fctx.body.push({ op: "br_if", depth: 1 });
  fctx.body.push({ op: "local.get", index: iterLocal });
  if (iterResultType.kind === "ref_null") {
    fctx.body.push({ op: "ref.as_non_null" });
  }
  fctx.body.push({ op: "call", funcIdx: nextMethodIdx });
  fctx.body.push({ op: "local.set", index: resultLocal });
  fctx.body.push({ op: "local.get", index: resultLocal });
  if (nextResultType.kind === "ref_null") {
    fctx.body.push({ op: "ref.as_non_null" });
  }
  fctx.body.push({ op: "struct.get", typeIdx: resultStructTypeIdx, fieldIdx: doneFieldIdx });
  if (doneFieldType.kind === "f64") {
    fctx.body.push({ op: "i32.trunc_f64_s" });
  }
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "i32.const", value: 1 },
      { op: "local.set", index: doneFlagDirect },
      { op: "br", depth: 2 },
      // break out of block (if + loop = depth 2)
    ],
    else: [],
  });
  fctx.body.push({ op: "local.get", index: resultLocal });
  if (nextResultType.kind === "ref_null") {
    fctx.body.push({ op: "ref.as_non_null" });
  }
  fctx.body.push({ op: "struct.get", typeIdx: resultStructTypeIdx, fieldIdx: valueFieldIdx });
  const targetElemType = getLocalType(fctx, elemLocal) ?? elemType;
  if (!valTypesMatch(valueFieldType, targetElemType)) {
    coerceType3(ctx, fctx, valueFieldType, targetElemType);
  }
  fctx.body.push({ op: "local.set", index: elemLocal });
  if (destructPatternIter) {
    compileForOfDestructuring(ctx, fctx, destructPatternIter, elemLocal, elemType, stmt);
  }
  if (assignDestructExprIter) {
    compileForOfIteratorAssignDestructuring(ctx, fctx, assignDestructExprIter, elemLocal, stmt);
  }
  if (ts8.isBlock(stmt.statement)) {
    const savedScope = saveBlockScopedShadows(fctx, stmt.statement);
    for (const s of stmt.statement.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedScope);
  } else {
    compileStatement(ctx, fctx, stmt.statement);
  }
  fctx.body.push({ op: "br", depth: 0 });
  const loopBody = fctx.body;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] -= 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] -= 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth -= 2;
  adjustRethrowDepth(fctx, -2);
  popBody(fctx, savedBody);
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
  if (returnMethodIdx !== void 0) {
    fctx.body.push({ op: "local.get", index: doneFlagDirect });
    fctx.body.push({ op: "i32.eqz" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: iterLocal },
        ...(iterResultType.kind === "ref_null" ? [{ op: "ref.as_non_null" }] : []),
        { op: "call", funcIdx: returnMethodIdx },
        // Drop the return value (return() returns {value, done})
        { op: "drop" },
      ],
      else: [],
    });
  }
  return true;
}
function findStructFieldsByTypeIdx(ctx, typeIdx) {
  for (const [name, fields] of ctx.structFields) {
    const idx = ctx.structMap.get(name);
    if (idx === typeIdx) return fields;
  }
  const typeDef = ctx.mod.types[typeIdx];
  if (typeDef && typeDef.kind === "struct") {
    return typeDef.fields.map((f, i) => ({
      name: f.name ?? `field_${i}`,
      type: f.type,
    }));
  }
  return void 0;
}
function compileForOfIterator(ctx, fctx, stmt) {
  const iterableType = compileExpression2(ctx, fctx, stmt.expression);
  if (!iterableType) {
    ctx.errors.push({
      message: "for-of: failed to compile iterable expression",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  if (iterableType.kind === "ref" || iterableType.kind === "ref_null") {
    let structName;
    for (const [name, idx] of ctx.structMap) {
      if (idx === iterableType.typeIdx) {
        structName = name;
        break;
      }
    }
    if (structName) {
      const methodFullName = `${structName}_@@iterator`;
      const iterMethodIdx = ctx.funcMap.get(methodFullName);
      if (iterMethodIdx !== void 0) {
        if (compileForOfDirectIterator(ctx, fctx, stmt, iterableType, iterMethodIdx)) {
          return;
        }
      }
    }
  }
  if (iterableType.kind !== "externref") {
    coerceType3(ctx, fctx, iterableType, { kind: "externref" });
  }
  {
    const backupLocal = fctx.__lastGuardedCastBackup;
    const tagIdx = ensureExnTag(ctx);
    const iterTmp = allocLocal(fctx, `__forit_null_${fctx.locals.length}`, { kind: "externref" });
    fctx.body.push({ op: "local.tee", index: iterTmp });
    fctx.body.push({ op: "ref.is_null" });
    if (backupLocal !== void 0) {
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: backupLocal },
          { op: "ref.is_null" },
          {
            op: "if",
            blockType: { kind: "empty" },
            then: [{ op: "ref.null.extern" }, { op: "throw", tagIdx }],
            else: [],
          },
        ],
        else: [],
      });
    } else {
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [{ op: "ref.null.extern" }, { op: "throw", tagIdx }],
        else: [],
      });
    }
    fctx.body.push({ op: "local.get", index: iterTmp });
  }
  let iteratorIdx;
  if (stmt.awaitModifier) {
    iteratorIdx = ensureAsyncIterator(ctx, fctx);
  }
  if (iteratorIdx === void 0) {
    iteratorIdx = ctx.funcMap.get("__iterator");
  }
  if (iteratorIdx === void 0) {
    ctx.errors.push({
      message: "for-of on non-array type requires iterator imports",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  fctx.body.push({ op: "call", funcIdx: iteratorIdx });
  const nextIdx = ctx.funcMap.get("__iterator_next");
  const doneIdx = ctx.funcMap.get("__iterator_done");
  const valueIdx = ctx.funcMap.get("__iterator_value");
  const returnIdx = ctx.funcMap.get("__iterator_return");
  if (nextIdx === void 0 || doneIdx === void 0 || valueIdx === void 0) {
    ctx.errors.push({
      message: "for-of on non-array type requires iterator imports",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  const iterLocal = allocLocal(fctx, `__forof_iter_${fctx.locals.length}`, { kind: "externref" });
  fctx.body.push({ op: "local.set", index: iterLocal });
  const resultLocal = allocLocal(fctx, `__forof_result_${fctx.locals.length}`, { kind: "externref" });
  const elemType = { kind: "externref" };
  let elemLocal;
  let destructPatternIter = null;
  let assignDestructExprIter = null;
  if (ts8.isVariableDeclarationList(stmt.initializer)) {
    const decl = stmt.initializer.declarations[0];
    const isConst = !!(stmt.initializer.flags & ts8.NodeFlags.Const);
    if (ts8.isObjectBindingPattern(decl.name) || ts8.isArrayBindingPattern(decl.name)) {
      destructPatternIter = decl.name;
      elemLocal = allocLocal(fctx, `__forof_elem_${fctx.locals.length}`, elemType);
      if (isConst) {
        collectBindingNames(decl.name).forEach((n) => {
          if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
          fctx.constBindings.add(n);
        });
      }
    } else {
      const varName = ts8.isIdentifier(decl.name) ? decl.name.text : `__forof_elem_${fctx.locals.length}`;
      elemLocal = allocLocal(fctx, varName, elemType);
      if (isConst && ts8.isIdentifier(decl.name)) {
        if (!fctx.constBindings) fctx.constBindings = /* @__PURE__ */ new Set();
        fctx.constBindings.add(decl.name.text);
      }
    }
  } else if (ts8.isObjectLiteralExpression(stmt.initializer) || ts8.isArrayLiteralExpression(stmt.initializer)) {
    assignDestructExprIter = stmt.initializer;
    elemLocal = allocLocal(fctx, `__forof_elem_${fctx.locals.length}`, elemType);
  } else if (ts8.isIdentifier(stmt.initializer)) {
    const varName = stmt.initializer.text;
    elemLocal = fctx.localMap.get(varName) ?? allocLocal(fctx, varName, elemType);
  } else {
    elemLocal = allocLocal(fctx, `__forof_elem_${fctx.locals.length}`, elemType);
  }
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] += 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] += 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth += 2;
  adjustRethrowDepth(fctx, 2);
  fctx.breakStack.push(1);
  fctx.continueStack.push(0);
  const doneFlag = allocLocal(fctx, `__forof_done_${fctx.locals.length}`, { kind: "i32" });
  const iterCountLocal = allocLocal(fctx, `__forof_guard_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.get", index: iterCountLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.tee", index: iterCountLocal });
  fctx.body.push({ op: "i32.const", value: 1e6 });
  fctx.body.push({ op: "i32.gt_s" });
  fctx.body.push({ op: "br_if", depth: 1 });
  fctx.body.push({ op: "local.get", index: iterLocal });
  fctx.body.push({ op: "call", funcIdx: nextIdx });
  fctx.body.push({ op: "local.set", index: resultLocal });
  fctx.body.push({ op: "local.get", index: resultLocal });
  fctx.body.push({ op: "call", funcIdx: doneIdx });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "i32.const", value: 1 },
      { op: "local.set", index: doneFlag },
      { op: "br", depth: 2 },
      // break out of block (if + loop = depth 2)
    ],
    else: [],
  });
  fctx.body.push({ op: "local.get", index: resultLocal });
  fctx.body.push({ op: "call", funcIdx: valueIdx });
  fctx.body.push({ op: "local.set", index: elemLocal });
  if (destructPatternIter) {
    compileForOfDestructuring(ctx, fctx, destructPatternIter, elemLocal, elemType, stmt);
  }
  if (assignDestructExprIter) {
    compileForOfIteratorAssignDestructuring(ctx, fctx, assignDestructExprIter, elemLocal, stmt);
  }
  if (ts8.isBlock(stmt.statement)) {
    const savedScope = saveBlockScopedShadows(fctx, stmt.statement);
    for (const s of stmt.statement.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedScope);
  } else {
    compileStatement(ctx, fctx, stmt.statement);
  }
  fctx.body.push({ op: "br", depth: 0 });
  const loopBody = fctx.body;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] -= 2;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] -= 2;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth -= 2;
  adjustRethrowDepth(fctx, -2);
  popBody(fctx, savedBody);
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
  if (returnIdx !== void 0) {
    fctx.body.push({ op: "local.get", index: doneFlag });
    fctx.body.push({ op: "i32.eqz" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: iterLocal },
        { op: "call", funcIdx: returnIdx },
      ],
      else: [],
    });
  }
}
function compileForInStatement(ctx, fctx, stmt) {
  const init = stmt.initializer;
  let varName;
  let keyLocal;
  if (ts8.isVariableDeclarationList(init)) {
    const decl = init.declarations[0];
    if (!ts8.isIdentifier(decl.name)) {
      ctx.errors.push({
        message: "for-in variable must be an identifier",
        line: getLine2(decl),
        column: getCol2(decl),
      });
      return;
    }
    varName = decl.name.text;
    keyLocal = allocLocal(fctx, varName, { kind: "externref" });
  } else if (ts8.isIdentifier(init)) {
    varName = init.text;
    const existingLocal = fctx.localMap.get(varName);
    if (existingLocal !== void 0) {
      keyLocal = existingLocal;
    } else {
      keyLocal = allocLocal(fctx, varName, { kind: "externref" });
    }
  } else if (
    ts8.isBinaryExpression(init) &&
    init.operatorToken.kind === ts8.SyntaxKind.EqualsToken &&
    ts8.isIdentifier(init.left)
  ) {
    varName = init.left.text;
    const existingLocal = fctx.localMap.get(varName);
    if (existingLocal !== void 0) {
      keyLocal = existingLocal;
    } else {
      keyLocal = allocLocal(fctx, varName, { kind: "externref" });
    }
    compileExpression2(ctx, fctx, init.right);
    fctx.body.push({ op: "local.set", index: keyLocal });
  } else {
    ctx.errors.push({
      message: "for-in requires a variable declaration or identifier",
      line: getLine2(stmt),
      column: getCol2(stmt),
    });
    return;
  }
  const keysIdx = ctx.funcMap.get("__for_in_keys");
  const lenIdx = ctx.funcMap.get("__for_in_len");
  const getIdx = ctx.funcMap.get("__for_in_get");
  if (keysIdx === void 0 || lenIdx === void 0 || getIdx === void 0) {
    const exprType2 = ctx.checker.getTypeAtLocation(stmt.expression);
    const props = exprType2.getProperties();
    if (props.length === 0) return;
    for (const prop of props) {
      const globalIdx = ctx.stringGlobalMap.get(prop.name);
      if (globalIdx === void 0) continue;
      fctx.body.push({ op: "global.get", index: globalIdx });
      fctx.body.push({ op: "local.set", index: keyLocal });
      compileStatement(ctx, fctx, stmt.statement);
    }
    return;
  }
  const exprType = compileExpression2(ctx, fctx, stmt.expression);
  if (exprType && exprType.kind !== "externref") {
    coerceType3(ctx, fctx, exprType, { kind: "externref" });
  }
  fctx.body.push({ op: "call", funcIdx: keysIdx });
  const keysLocal = allocLocal(fctx, `__forin_keys_${fctx.locals.length}`, { kind: "externref" });
  fctx.body.push({ op: "local.set", index: keysLocal });
  const lenLocal = allocLocal(fctx, `__forin_len_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.get", index: keysLocal });
  fctx.body.push({ op: "call", funcIdx: lenIdx });
  fctx.body.push({ op: "local.set", index: lenLocal });
  const iLocal = allocLocal(fctx, `__forin_i_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iLocal });
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] += 3;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] += 3;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth += 3;
  adjustRethrowDepth(fctx, 3);
  fctx.breakStack.push(2);
  fctx.continueStack.push(0);
  if (ts8.isBlock(stmt.statement)) {
    const savedScope = saveBlockScopedShadows(fctx, stmt.statement);
    for (const s of stmt.statement.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedScope);
  } else {
    compileStatement(ctx, fctx, stmt.statement);
  }
  const userBody = fctx.body;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i] -= 3;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i] -= 3;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth -= 3;
  adjustRethrowDepth(fctx, -3);
  popBody(fctx, savedBody);
  const loopBody = [];
  loopBody.push({ op: "local.get", index: iLocal });
  loopBody.push({ op: "local.get", index: lenLocal });
  loopBody.push({ op: "i32.ge_s" });
  loopBody.push({ op: "br_if", depth: 1 });
  loopBody.push({ op: "local.get", index: keysLocal });
  loopBody.push({ op: "local.get", index: iLocal });
  loopBody.push({ op: "call", funcIdx: getIdx });
  loopBody.push({ op: "local.set", index: keyLocal });
  loopBody.push({
    op: "block",
    blockType: { kind: "empty" },
    body: userBody,
  });
  loopBody.push({ op: "local.get", index: iLocal });
  loopBody.push({ op: "i32.const", value: 1 });
  loopBody.push({ op: "i32.add" });
  loopBody.push({ op: "local.set", index: iLocal });
  loopBody.push({ op: "br", depth: 0 });
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
}
function compileLabeledStatement(ctx, fctx, stmt) {
  const labelName = stmt.label.text;
  const innerStmt = stmt.statement;
  const isLoop =
    ts8.isWhileStatement(innerStmt) ||
    ts8.isDoStatement(innerStmt) ||
    ts8.isForStatement(innerStmt) ||
    ts8.isForInStatement(innerStmt) ||
    ts8.isForOfStatement(innerStmt);
  if (isLoop) {
    const breakIdx = fctx.breakStack.length;
    const continueIdx = fctx.continueStack.length;
    fctx.labelMap.set(labelName, { breakIdx, continueIdx });
    compileStatement(ctx, fctx, innerStmt);
    fctx.labelMap.delete(labelName);
  } else {
    const savedBody = pushBody(fctx);
    for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]++;
    for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]++;
    if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth++;
    adjustRethrowDepth(fctx, 1);
    const breakIdx = fctx.breakStack.length;
    const continueIdx = fctx.continueStack.length;
    fctx.breakStack.push(0);
    fctx.labelMap.set(labelName, { breakIdx, continueIdx });
    compileStatement(ctx, fctx, innerStmt);
    const bodyInstrs = fctx.body;
    fctx.breakStack.pop();
    fctx.labelMap.delete(labelName);
    for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]--;
    for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]--;
    if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth--;
    adjustRethrowDepth(fctx, -1);
    popBody(fctx, savedBody);
    fctx.body.push({
      op: "block",
      blockType: { kind: "empty" },
      body: bodyInstrs,
    });
  }
}
function compileBreakStatement(_ctx, fctx, stmt) {
  let breakIdx;
  if (stmt.label) {
    const labelName = stmt.label.text;
    const labelInfo = fctx.labelMap.get(labelName);
    if (labelInfo === void 0) return;
    breakIdx = labelInfo.breakIdx;
  } else {
    breakIdx = fctx.breakStack.length - 1;
  }
  const depth = fctx.breakStack[breakIdx];
  if (depth === void 0) return;
  if (fctx.finallyStack) {
    for (let i = fctx.finallyStack.length - 1; i >= 0; i--) {
      const entry = fctx.finallyStack[i];
      if (breakIdx < entry.breakStackLen) {
        fctx.body.push(...entry.cloneFinally());
      }
    }
  }
  fctx.body.push({ op: "br", depth });
}
function compileContinueStatement(_ctx, fctx, stmt) {
  let contIdx;
  if (stmt.label) {
    const labelName = stmt.label.text;
    const labelInfo = fctx.labelMap.get(labelName);
    if (labelInfo === void 0) return;
    contIdx = labelInfo.continueIdx;
  } else {
    contIdx = fctx.continueStack.length - 1;
  }
  const depth = fctx.continueStack[contIdx];
  if (depth === void 0) return;
  if (fctx.finallyStack) {
    for (let i = fctx.finallyStack.length - 1; i >= 0; i--) {
      const entry = fctx.finallyStack[i];
      if (contIdx < entry.continueStackLen) {
        fctx.body.push(...entry.cloneFinally());
      }
    }
  }
  fctx.body.push({ op: "br", depth });
}
function compileExternrefCatchDestructure(ctx, fctx, pattern, exnLocalIdx) {
  fctx.body.push({ op: "drop" });
  if (ts8.isObjectBindingPattern(pattern)) {
    let getIdx = ensureLateImport2(
      ctx,
      "__extern_get",
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    for (const element of pattern.elements) {
      if (!ts8.isBindingElement(element)) continue;
      const propNameNode = element.propertyName ?? element.name;
      let propNameText;
      if (ts8.isIdentifier(propNameNode)) propNameText = propNameNode.text;
      else if (ts8.isStringLiteral(propNameNode)) propNameText = propNameNode.text;
      if (!propNameText) continue;
      const localName = ts8.isIdentifier(element.name) ? element.name.text : void 0;
      if (!localName) continue;
      const localIdx = fctx.localMap.get(localName);
      if (localIdx === void 0) continue;
      addStringConstantGlobal(ctx, propNameText);
      const strGlobalIdx = ctx.stringGlobalMap.get(propNameText);
      if (strGlobalIdx === void 0) continue;
      getIdx = ctx.funcMap.get("__extern_get");
      fctx.body.push({ op: "local.get", index: exnLocalIdx });
      fctx.body.push({ op: "global.get", index: strGlobalIdx });
      fctx.body.push({ op: "call", funcIdx: getIdx });
      const localType = getLocalType(fctx, localIdx);
      if (localType && localType.kind !== "externref") {
        coerceType3(ctx, fctx, { kind: "externref" }, localType);
      }
      fctx.body.push({ op: "local.set", index: localIdx });
    }
  } else if (ts8.isArrayBindingPattern(pattern)) {
    addUnionImports(ctx);
    let getIdx = ensureLateImport2(
      ctx,
      "__extern_get",
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    const boxIdx = ctx.funcMap.get("__box_number");
    let idx = 0;
    for (const element of pattern.elements) {
      if (ts8.isOmittedExpression(element)) {
        idx++;
        continue;
      }
      if (!ts8.isBindingElement(element)) {
        idx++;
        continue;
      }
      const localName = ts8.isIdentifier(element.name) ? element.name.text : void 0;
      if (!localName) {
        idx++;
        continue;
      }
      const localIdx = fctx.localMap.get(localName);
      if (localIdx === void 0) {
        idx++;
        continue;
      }
      getIdx = ctx.funcMap.get("__extern_get");
      fctx.body.push({ op: "local.get", index: exnLocalIdx });
      fctx.body.push({ op: "f64.const", value: idx });
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      }
      fctx.body.push({ op: "call", funcIdx: getIdx });
      const localType = getLocalType(fctx, localIdx);
      if (localType && localType.kind !== "externref") {
        coerceType3(ctx, fctx, { kind: "externref" }, localType);
      }
      fctx.body.push({ op: "local.set", index: localIdx });
      idx++;
    }
  }
}
function compileThrowStatement(ctx, fctx, stmt) {
  if (
    stmt.expression &&
    ts8.isIdentifier(stmt.expression) &&
    fctx.catchRethrowStack &&
    fctx.catchRethrowStack.length > 0
  ) {
    const thrownName = stmt.expression.text;
    for (let i = fctx.catchRethrowStack.length - 1; i >= 0; i--) {
      const entry = fctx.catchRethrowStack[i];
      if (entry.varName === thrownName) {
        fctx.body.push({ op: "rethrow", depth: entry.depth });
        return;
      }
    }
  }
  const tagIdx = ensureExnTag(ctx);
  if (stmt.expression) {
    const resultType = compileExpression2(ctx, fctx, stmt.expression, {
      kind: "externref",
    });
    if (resultType && resultType.kind !== "externref") {
      coerceType3(ctx, fctx, resultType, { kind: "externref" });
    } else if (!resultType) {
      fctx.body.push({ op: "ref.null.extern" });
    }
  } else {
    fctx.body.push({ op: "ref.null.extern" });
  }
  fctx.body.push({ op: "throw", tagIdx });
}
function compileTryStatement(ctx, fctx, stmt) {
  const tagIdx = ensureExnTag(ctx);
  let finallyInstrs = null;
  if (stmt.finallyBlock) {
    const savedForFinally = pushBody(fctx);
    const savedFinallyScope = saveBlockScopedShadows(fctx, stmt.finallyBlock);
    for (const s of stmt.finallyBlock.statements) {
      compileStatement(ctx, fctx, s);
    }
    restoreBlockScopedShadows(fctx, savedFinallyScope);
    finallyInstrs = fctx.body;
    popBody(fctx, savedForFinally);
  }
  function cloneFinally() {
    return JSON.parse(JSON.stringify(finallyInstrs));
  }
  if (finallyInstrs) {
    fctx.savedBodies.push(finallyInstrs);
  }
  const savedBody = pushBody(fctx);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]++;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]++;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth++;
  adjustRethrowDepth(fctx, 1);
  if (finallyInstrs) {
    if (!fctx.finallyStack) fctx.finallyStack = [];
    fctx.finallyStack.push({
      cloneFinally,
      breakStackLen: fctx.breakStack.length,
      continueStackLen: fctx.continueStack.length,
    });
  }
  const savedTryScope = saveBlockScopedShadows(fctx, stmt.tryBlock);
  for (const s of stmt.tryBlock.statements) {
    compileStatement(ctx, fctx, s);
  }
  restoreBlockScopedShadows(fctx, savedTryScope);
  if (finallyInstrs) {
    fctx.finallyStack.pop();
  }
  if (finallyInstrs) {
    fctx.body.push(...cloneFinally());
  }
  const tryBody = fctx.body;
  let catches = [];
  let catchAllBody;
  if (finallyInstrs && !stmt.catchClause) {
    fctx.body = [];
    fctx.body.push(...cloneFinally());
    fctx.body.push({ op: "rethrow", depth: 0 });
    catchAllBody = fctx.body;
  }
  if (stmt.catchClause) {
    let cloneCatchBody2 = function () {
      return JSON.parse(JSON.stringify(catchBodyInstrs));
    };
    var cloneCatchBody = cloneCatchBody2;
    let exnLocalIdx = null;
    let savedCatchVarIdx;
    if (stmt.catchClause.variableDeclaration && ts8.isIdentifier(stmt.catchClause.variableDeclaration.name)) {
      const varName = stmt.catchClause.variableDeclaration.name.text;
      savedCatchVarIdx = fctx.localMap.get(varName);
      exnLocalIdx = allocLocal(fctx, varName, { kind: "externref" });
    } else if (
      stmt.catchClause.variableDeclaration &&
      (ts8.isObjectBindingPattern(stmt.catchClause.variableDeclaration.name) ||
        ts8.isArrayBindingPattern(stmt.catchClause.variableDeclaration.name))
    ) {
      ensureBindingLocals(ctx, fctx, stmt.catchClause.variableDeclaration.name);
      exnLocalIdx = allocLocal(fctx, `__catch_destruct_${fctx.locals.length}`, { kind: "externref" });
    }
    let catchVarName;
    if (stmt.catchClause.variableDeclaration && ts8.isIdentifier(stmt.catchClause.variableDeclaration.name)) {
      catchVarName = stmt.catchClause.variableDeclaration.name.text;
    }
    let catchBodyInstrs;
    {
      const prevBody = fctx.body;
      fctx.savedBodies.push(tryBody);
      fctx.body = [];
      if (catchVarName) {
        if (!fctx.catchRethrowStack) fctx.catchRethrowStack = [];
        fctx.catchRethrowStack.push({ varName: catchVarName, depth: 0 });
      }
      if (finallyInstrs) {
        for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]++;
        for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]++;
        adjustRethrowDepth(fctx, 1);
        if (!fctx.finallyStack) fctx.finallyStack = [];
        fctx.finallyStack.push({
          cloneFinally,
          breakStackLen: fctx.breakStack.length,
          continueStackLen: fctx.continueStack.length,
        });
      }
      if (
        exnLocalIdx !== null &&
        stmt.catchClause.variableDeclaration &&
        (ts8.isObjectBindingPattern(stmt.catchClause.variableDeclaration.name) ||
          ts8.isArrayBindingPattern(stmt.catchClause.variableDeclaration.name))
      ) {
        fctx.body.push({ op: "local.get", index: exnLocalIdx });
        compileExternrefCatchDestructure(ctx, fctx, stmt.catchClause.variableDeclaration.name, exnLocalIdx);
      }
      const savedCatchScope = saveBlockScopedShadows(fctx, stmt.catchClause.block);
      for (const s of stmt.catchClause.block.statements) {
        compileStatement(ctx, fctx, s);
      }
      restoreBlockScopedShadows(fctx, savedCatchScope);
      if (finallyInstrs) {
        fctx.finallyStack.pop();
        for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]--;
        for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]--;
        adjustRethrowDepth(fctx, -1);
      }
      if (catchVarName) {
        fctx.catchRethrowStack.pop();
      }
      catchBodyInstrs = fctx.body;
      fctx.body = prevBody;
      const tbIdx = fctx.savedBodies.lastIndexOf(tryBody);
      if (tbIdx >= 0) fctx.savedBodies.splice(tbIdx, 1);
    }
    fctx.body = [];
    if (exnLocalIdx !== null) {
      fctx.body.push({ op: "local.set", index: exnLocalIdx });
    } else {
      fctx.body.push({ op: "drop" });
    }
    if (finallyInstrs) {
      const innerCatchAllBody = [...cloneFinally(), { op: "rethrow", depth: 0 }];
      fctx.body.push({
        op: "try",
        blockType: { kind: "empty" },
        body: catchBodyInstrs,
        catches: [],
        catchAll: innerCatchAllBody,
      });
      fctx.body.push(...cloneFinally());
    } else {
      fctx.body.push(...catchBodyInstrs);
    }
    catches = [{ tagIdx, body: fctx.body }];
    {
      fctx.savedBodies.push(tryBody);
      for (const c of catches) fctx.savedBodies.push(c.body);
      fctx.body = [];
      if (exnLocalIdx !== null) {
        const getCaughtIdx = ensureLateImport2(ctx, "__get_caught_exception", [], [{ kind: "externref" }]);
        flushLateImportShifts2(ctx, fctx);
        if (getCaughtIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: getCaughtIdx });
          fctx.body.push({ op: "local.set", index: exnLocalIdx });
        }
      }
      if (finallyInstrs) {
        const innerCatchAllBody = [...cloneFinally(), { op: "rethrow", depth: 0 }];
        fctx.body.push({
          op: "try",
          blockType: { kind: "empty" },
          body: cloneCatchBody2(),
          catches: [],
          catchAll: innerCatchAllBody,
        });
        fctx.body.push(...cloneFinally());
      } else {
        fctx.body.push(...cloneCatchBody2());
      }
      catchAllBody = fctx.body;
      for (const c of catches) {
        const ci = fctx.savedBodies.lastIndexOf(c.body);
        if (ci >= 0) fctx.savedBodies.splice(ci, 1);
      }
      const tbIdx2 = fctx.savedBodies.lastIndexOf(tryBody);
      if (tbIdx2 >= 0) fctx.savedBodies.splice(tbIdx2, 1);
    }
    if (stmt.catchClause.variableDeclaration && ts8.isIdentifier(stmt.catchClause.variableDeclaration.name)) {
      const varName = stmt.catchClause.variableDeclaration.name.text;
      if (savedCatchVarIdx !== void 0) {
        fctx.localMap.set(varName, savedCatchVarIdx);
      }
    }
  }
  if (finallyInstrs) {
    const fiIdx = fctx.savedBodies.lastIndexOf(finallyInstrs);
    if (fiIdx >= 0) fctx.savedBodies.splice(fiIdx, 1);
  }
  popBody(fctx, savedBody);
  for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]--;
  for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]--;
  if (fctx.generatorReturnDepth !== void 0) fctx.generatorReturnDepth--;
  adjustRethrowDepth(fctx, -1);
  fctx.body.push({
    op: "try",
    blockType: { kind: "empty" },
    body: tryBody,
    catches,
    catchAll: catchAllBody,
  });
}
function compileNestedClassDeclaration(ctx, fctx, decl) {
  if (!decl.name) return;
  const className = decl.name.text;
  const isDeferred = ctx.deferredClassBodies.has(className);
  if (ctx.structMap.has(className) && !isDeferred) {
    if (ctx.classThrowsOnEval.has(className)) {
      emitThrowString2(ctx, fctx, "TypeError: Classes may not have a static property named 'prototype'");
      return;
    }
    return;
  }
  try {
    if (!ctx.structMap.has(className)) {
      collectClassDeclaration(ctx, decl);
    }
    if (ctx.classThrowsOnEval.has(className)) {
      emitThrowString2(ctx, fctx, "TypeError: Classes may not have a static property named 'prototype'");
      return;
    }
    for (const member of decl.members) {
      if (ts8.isMethodDeclaration(member) && member.body) {
        promoteAccessorCapturesToGlobals(ctx, fctx, member.body);
      }
      if (ts8.isConstructorDeclaration(member) && member.body) {
        promoteAccessorCapturesToGlobals(ctx, fctx, member.body);
      }
    }
    const funcByName = /* @__PURE__ */ new Map();
    for (let i = 0; i < ctx.mod.functions.length; i++) {
      funcByName.set(ctx.mod.functions[i].name, i);
    }
    compileClassBodies(ctx, decl, funcByName);
    if (isDeferred) ctx.deferredClassBodies.delete(className);
  } catch (e) {
    const msg = e instanceof Error ? e.message : String(e);
    reportError(ctx, decl, `Internal error compiling nested class '${className}': ${msg}`);
  }
}
function compileNestedFunctionDeclaration(ctx, fctx, stmt) {
  if (!stmt.name || !stmt.body) return;
  const funcName = stmt.name.text;
  const paramTypes = [];
  for (const p of stmt.parameters) {
    const paramType = ctx.checker.getTypeAtLocation(p);
    let wasmType = resolveWasmType(ctx, paramType);
    if (p.initializer && wasmType.kind === "ref") {
      wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
    }
    paramTypes.push(wasmType);
  }
  const isGenerator = stmt.asteriskToken !== void 0;
  if (isGenerator) {
    ctx.generatorFunctions.add(funcName);
  }
  const sig = ctx.checker.getSignatureFromDeclaration(stmt);
  let returnType = null;
  if (isGenerator) {
    returnType = { kind: "externref" };
  } else if (sig) {
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    if (!isVoidType(retType)) {
      returnType = resolveWasmType(ctx, retType);
    }
  }
  const referencedNames = /* @__PURE__ */ new Set();
  for (const s of stmt.body.statements) {
    collectReferencedIdentifiers(s, referencedNames);
  }
  const writtenInBody = /* @__PURE__ */ new Set();
  for (const s of stmt.body.statements) {
    collectWrittenIdentifiers(s, writtenInBody);
  }
  const ownParamNames = new Set(stmt.parameters.filter((p) => ts8.isIdentifier(p.name)).map((p) => p.name.text));
  const captures = [];
  for (const name of referencedNames) {
    if (ownParamNames.has(name)) continue;
    const localIdx = fctx.localMap.get(name);
    if (localIdx === void 0) continue;
    if (ctx.funcMap.has(name)) continue;
    const type =
      localIdx < fctx.params.length
        ? fctx.params[localIdx].type
        : (fctx.locals[localIdx - fctx.params.length]?.type ?? { kind: "f64" });
    const isMutable = writtenInBody.has(name);
    captures.push({ name, type, localIdx, mutable: isMutable });
  }
  const results = returnType ? [returnType] : [];
  const optionalParams = [];
  for (let i = 0; i < stmt.parameters.length; i++) {
    const param = stmt.parameters[i];
    if (param.questionToken || param.initializer) {
      const info = { index: i, type: paramTypes[i] };
      if (param.initializer) {
        const cd = extractConstantDefault(param.initializer, paramTypes[i]);
        if (cd) {
          info.constantDefault = cd;
        } else {
          info.hasExpressionDefault = true;
        }
      }
      optionalParams.push(info);
    }
  }
  if (optionalParams.length > 0) {
    ctx.funcOptionalParams.set(funcName, optionalParams);
  }
  if (captures.length === 0) {
    const funcTypeIdx = addFuncType(ctx, paramTypes, results, `${funcName}_type`);
    const liftedFctx = {
      name: funcName,
      params: stmt.parameters.map((p, i) => ({
        name: ts8.isIdentifier(p.name) ? p.name.text : `__param${i}`,
        type: paramTypes[i],
      })),
      locals: [],
      localMap: /* @__PURE__ */ new Map(),
      returnType,
      body: [],
      blockDepth: 0,
      breakStack: [],
      continueStack: [],
      labelMap: /* @__PURE__ */ new Map(),
      savedBodies: [],
      isGenerator,
    };
    for (let i = 0; i < liftedFctx.params.length; i++) {
      liftedFctx.localMap.set(liftedFctx.params[i].name, i);
    }
    const savedFunc = ctx.currentFunc;
    if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
    if (savedFunc) ctx.funcStack.push(savedFunc);
    ctx.currentFunc = liftedFctx;
    emitDefaultParamInit(ctx, liftedFctx, stmt, paramTypes, 0);
    for (let pi = 0; pi < stmt.parameters.length; pi++) {
      const param = stmt.parameters[pi];
      if (ts8.isObjectBindingPattern(param.name)) {
        destructureParamObject2(ctx, liftedFctx, pi, param.name, paramTypes[pi]);
      } else if (ts8.isArrayBindingPattern(param.name)) {
        destructureParamArray(ctx, liftedFctx, pi, param.name, paramTypes[pi]);
      }
    }
    if (stmt.body && bodyUsesArguments(stmt.body)) {
      emitArgumentsObject(ctx, liftedFctx, paramTypes, 0);
    }
    if (isGenerator) {
      const bufferLocal = allocLocal(liftedFctx, "__gen_buffer", { kind: "externref" });
      const createBufIdx = ctx.funcMap.get("__gen_create_buffer");
      liftedFctx.body.push({ op: "call", funcIdx: createBufIdx });
      liftedFctx.body.push({ op: "local.set", index: bufferLocal });
      const bodyInstrs = [];
      const outerBody = liftedFctx.body;
      liftedFctx.body = bodyInstrs;
      liftedFctx.generatorReturnDepth = 0;
      liftedFctx.blockDepth++;
      for (let i = 0; i < liftedFctx.breakStack.length; i++) liftedFctx.breakStack[i]++;
      for (let i = 0; i < liftedFctx.continueStack.length; i++) liftedFctx.continueStack[i]++;
      for (const s of stmt.body.statements) {
        compileStatement(ctx, liftedFctx, s);
      }
      liftedFctx.blockDepth--;
      for (let i = 0; i < liftedFctx.breakStack.length; i++) liftedFctx.breakStack[i]--;
      for (let i = 0; i < liftedFctx.continueStack.length; i++) liftedFctx.continueStack[i]--;
      liftedFctx.generatorReturnDepth = void 0;
      liftedFctx.body = outerBody;
      liftedFctx.body.push({
        op: "block",
        blockType: { kind: "empty" },
        body: bodyInstrs,
      });
      const isAsyncStmt = stmt.modifiers?.some((m) => m.kind === 134) ?? false;
      const createGenIdx = ctx.funcMap.get(isAsyncStmt ? "__create_async_generator" : "__create_generator");
      liftedFctx.body.push({ op: "local.get", index: bufferLocal });
      liftedFctx.body.push({ op: "call", funcIdx: createGenIdx });
    } else {
      for (const s of stmt.body.statements) {
        compileStatement(ctx, liftedFctx, s);
      }
      appendDefaultReturn(liftedFctx, returnType);
    }
    if (savedFunc) ctx.funcStack.pop();
    if (savedFunc) ctx.parentBodiesStack.pop();
    ctx.currentFunc = savedFunc;
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name: funcName,
      typeIdx: funcTypeIdx,
      locals: liftedFctx.locals,
      body: liftedFctx.body,
      exported: false,
    });
    ctx.funcMap.set(funcName, funcIdx);
  } else {
    const captureParamTypes = captures.map((c) => {
      if (c.mutable) {
        const refCellTypeIdx = getOrRegisterRefCellType(ctx, c.type);
        return { kind: "ref", typeIdx: refCellTypeIdx };
      }
      return c.type;
    });
    const allParamTypes = [...captureParamTypes, ...paramTypes];
    const funcTypeIdx = addFuncType(ctx, allParamTypes, results, `${funcName}_type`);
    const liftedFctx = {
      name: funcName,
      params: [
        ...captures.map((c, i) => ({ name: c.name, type: captureParamTypes[i] })),
        ...stmt.parameters.map((p, i) => ({
          name: ts8.isIdentifier(p.name) ? p.name.text : `__param${i}`,
          type: paramTypes[i],
        })),
      ],
      locals: [],
      localMap: /* @__PURE__ */ new Map(),
      returnType,
      body: [],
      blockDepth: 0,
      breakStack: [],
      continueStack: [],
      labelMap: /* @__PURE__ */ new Map(),
      savedBodies: [],
      isGenerator,
    };
    for (let i = 0; i < liftedFctx.params.length; i++) {
      liftedFctx.localMap.set(liftedFctx.params[i].name, i);
    }
    for (const cap of captures) {
      if (cap.mutable) {
        const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
        if (!liftedFctx.boxedCaptures) liftedFctx.boxedCaptures = /* @__PURE__ */ new Map();
        liftedFctx.boxedCaptures.set(cap.name, { refCellTypeIdx, valType: cap.type });
      } else {
        const outerBoxed = fctx.boxedCaptures?.get(cap.name);
        if (outerBoxed && (cap.type.kind === "ref" || cap.type.kind === "ref_null")) {
          if (!liftedFctx.boxedCaptures) liftedFctx.boxedCaptures = /* @__PURE__ */ new Map();
          liftedFctx.boxedCaptures.set(cap.name, {
            refCellTypeIdx: outerBoxed.refCellTypeIdx,
            valType: outerBoxed.valType,
          });
        }
      }
    }
    const savedFunc = ctx.currentFunc;
    if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
    if (savedFunc) ctx.funcStack.push(savedFunc);
    ctx.currentFunc = liftedFctx;
    emitDefaultParamInit(ctx, liftedFctx, stmt, paramTypes, captures.length);
    for (let pi = 0; pi < stmt.parameters.length; pi++) {
      const param = stmt.parameters[pi];
      const paramIdx = captures.length + pi;
      if (ts8.isObjectBindingPattern(param.name)) {
        destructureParamObject2(ctx, liftedFctx, paramIdx, param.name, paramTypes[pi]);
      } else if (ts8.isArrayBindingPattern(param.name)) {
        destructureParamArray(ctx, liftedFctx, paramIdx, param.name, paramTypes[pi]);
      }
    }
    if (stmt.body && bodyUsesArguments(stmt.body)) {
      emitArgumentsObject(ctx, liftedFctx, paramTypes, captures.length);
    }
    if (isGenerator) {
      const bufferLocal = allocLocal(liftedFctx, "__gen_buffer", { kind: "externref" });
      const createBufIdx = ctx.funcMap.get("__gen_create_buffer");
      liftedFctx.body.push({ op: "call", funcIdx: createBufIdx });
      liftedFctx.body.push({ op: "local.set", index: bufferLocal });
      const bodyInstrs = [];
      const outerBody = liftedFctx.body;
      liftedFctx.body = bodyInstrs;
      liftedFctx.generatorReturnDepth = 0;
      liftedFctx.blockDepth++;
      for (let i = 0; i < liftedFctx.breakStack.length; i++) liftedFctx.breakStack[i]++;
      for (let i = 0; i < liftedFctx.continueStack.length; i++) liftedFctx.continueStack[i]++;
      for (const s of stmt.body.statements) {
        compileStatement(ctx, liftedFctx, s);
      }
      liftedFctx.blockDepth--;
      for (let i = 0; i < liftedFctx.breakStack.length; i++) liftedFctx.breakStack[i]--;
      for (let i = 0; i < liftedFctx.continueStack.length; i++) liftedFctx.continueStack[i]--;
      liftedFctx.generatorReturnDepth = void 0;
      liftedFctx.body = outerBody;
      liftedFctx.body.push({
        op: "block",
        blockType: { kind: "empty" },
        body: bodyInstrs,
      });
      const isAsyncStmt = stmt.modifiers?.some((m) => m.kind === 134) ?? false;
      const createGenIdx = ctx.funcMap.get(isAsyncStmt ? "__create_async_generator" : "__create_generator");
      liftedFctx.body.push({ op: "local.get", index: bufferLocal });
      liftedFctx.body.push({ op: "call", funcIdx: createGenIdx });
    } else {
      for (const s of stmt.body.statements) {
        compileStatement(ctx, liftedFctx, s);
      }
      appendDefaultReturn(liftedFctx, returnType);
    }
    if (savedFunc) ctx.funcStack.pop();
    if (savedFunc) ctx.parentBodiesStack.pop();
    ctx.currentFunc = savedFunc;
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name: funcName,
      typeIdx: funcTypeIdx,
      locals: liftedFctx.locals,
      body: liftedFctx.body,
      exported: false,
    });
    ctx.funcMap.set(funcName, funcIdx);
    ctx.nestedFuncCaptures.set(
      funcName,
      captures.map((c) => ({
        name: c.name,
        outerLocalIdx: c.localIdx,
        mutable: c.mutable,
        valType: c.type,
      })),
    );
  }
}
function hoistFunctionDeclarations(ctx, fctx, stmts) {
  for (const stmt of stmts) {
    if (ts8.isFunctionDeclaration(stmt) && stmt.name && stmt.body) {
      if (!ctx.funcMap.has(stmt.name.text)) {
        const errorsBefore = ctx.errors.length;
        const funcsBefore = ctx.mod.functions.length;
        const funcName = stmt.name.text;
        compileNestedFunctionDeclaration(ctx, fctx, stmt);
        if (ctx.errors.length > errorsBefore) {
          ctx.errors.length = errorsBefore;
          ctx.mod.functions.length = funcsBefore;
          ctx.funcMap.delete(funcName);
          ctx.nestedFuncCaptures.delete(funcName);
          ctx.funcOptionalParams.delete(funcName);
          if (!ctx.hoistFailedFuncs) ctx.hoistFailedFuncs = /* @__PURE__ */ new Set();
          ctx.hoistFailedFuncs.add(funcName);
        }
      }
    }
    if (ts8.isIfStatement(stmt)) {
      if (ts8.isBlock(stmt.thenStatement)) {
        hoistFunctionDeclarations(ctx, fctx, stmt.thenStatement.statements);
      }
      if (stmt.elseStatement) {
        if (ts8.isBlock(stmt.elseStatement)) {
          hoistFunctionDeclarations(ctx, fctx, stmt.elseStatement.statements);
        } else if (ts8.isIfStatement(stmt.elseStatement)) {
          hoistFunctionDeclarations(ctx, fctx, [stmt.elseStatement]);
        }
      }
    }
    if (ts8.isTryStatement(stmt)) {
      hoistFunctionDeclarations(ctx, fctx, stmt.tryBlock.statements);
      if (stmt.catchClause) {
        hoistFunctionDeclarations(ctx, fctx, stmt.catchClause.block.statements);
      }
      if (stmt.finallyBlock) {
        hoistFunctionDeclarations(ctx, fctx, stmt.finallyBlock.statements);
      }
    }
    if (ts8.isBlock(stmt)) {
      hoistFunctionDeclarations(ctx, fctx, stmt.statements);
    }
    if (ts8.isForStatement(stmt) || ts8.isWhileStatement(stmt) || ts8.isDoStatement(stmt)) {
      if (ts8.isBlock(stmt.statement)) {
        hoistFunctionDeclarations(ctx, fctx, stmt.statement.statements);
      } else {
        hoistFunctionDeclarations(ctx, fctx, [stmt.statement]);
      }
    }
    if (ts8.isForInStatement(stmt) || ts8.isForOfStatement(stmt)) {
      if (ts8.isBlock(stmt.statement)) {
        hoistFunctionDeclarations(ctx, fctx, stmt.statement.statements);
      } else {
        hoistFunctionDeclarations(ctx, fctx, [stmt.statement]);
      }
    }
    if (ts8.isSwitchStatement(stmt)) {
      for (const clause of stmt.caseBlock.clauses) {
        hoistFunctionDeclarations(ctx, fctx, clause.statements);
      }
    }
    if (ts8.isLabeledStatement(stmt)) {
      if (ts8.isBlock(stmt.statement)) {
        hoistFunctionDeclarations(ctx, fctx, stmt.statement.statements);
      } else {
        hoistFunctionDeclarations(ctx, fctx, [stmt.statement]);
      }
    }
  }
}
function emitDefaultParamInit(ctx, liftedFctx, stmt, paramTypes, paramOffset) {
  for (let i = 0; i < stmt.parameters.length; i++) {
    const param = stmt.parameters[i];
    if (!param.initializer) continue;
    const paramIdx = paramOffset + i;
    const paramType = paramTypes[i];
    const savedBody = pushBody(liftedFctx);
    compileExpression2(ctx, liftedFctx, param.initializer, paramType);
    liftedFctx.body.push({ op: "local.set", index: paramIdx });
    const thenInstrs = liftedFctx.body;
    popBody(liftedFctx, savedBody);
    if (paramType.kind === "externref" || paramType.kind === "ref_null" || paramType.kind === "ref") {
      liftedFctx.body.push({ op: "local.get", index: paramIdx });
      liftedFctx.body.push({ op: "ref.is_null" });
      liftedFctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: thenInstrs,
      });
    } else if (paramType.kind === "i32") {
      liftedFctx.body.push({ op: "local.get", index: paramIdx });
      liftedFctx.body.push({ op: "i32.eqz" });
      liftedFctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: thenInstrs,
      });
    } else if (paramType.kind === "f64") {
      liftedFctx.body.push({ op: "local.get", index: paramIdx });
      liftedFctx.body.push({ op: "i64.reinterpret_f64" });
      liftedFctx.body.push({ op: "i64.const", value: 0x7ff00000deadc0den });
      liftedFctx.body.push({ op: "i64.eq" });
      liftedFctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: thenInstrs,
      });
    }
  }
}
function appendDefaultReturn(fctx, returnType) {
  if (!returnType) return;
  const lastInstr = fctx.body[fctx.body.length - 1];
  if (lastInstr && lastInstr.op === "return") return;
  if (returnType.kind === "f64") fctx.body.push({ op: "f64.const", value: 0 });
  else if (returnType.kind === "i32") fctx.body.push({ op: "i32.const", value: 0 });
  else if (returnType.kind === "externref") fctx.body.push({ op: "ref.null.extern" });
}
function getLine2(node) {
  try {
    const sf = node.getSourceFile();
    if (!sf) return 0;
    const { line } = sf.getLineAndCharacterOfPosition(node.getStart());
    return line + 1;
  } catch {
    return 0;
  }
}
function getCol2(node) {
  try {
    const sf = node.getSourceFile();
    if (!sf) return 0;
    const { character } = sf.getLineAndCharacterOfPosition(node.getStart());
    return character + 1;
  } catch {
    return 0;
  }
}
function bodyUsesArguments(node) {
  if (ts8.isIdentifier(node) && node.text === "arguments") return true;
  if (ts8.isFunctionDeclaration(node) || ts8.isFunctionExpression(node)) {
    return false;
  }
  return ts8.forEachChild(node, bodyUsesArguments) ?? false;
}
function emitArgumentsObject(ctx, fctx, paramTypes, paramOffset) {
  const numArgs = paramTypes.length;
  const elemType = { kind: "externref" };
  const vti = getOrRegisterVecType(ctx, "externref", elemType);
  const ati = getArrTypeIdxFromVec(ctx, vti);
  const vecRef = { kind: "ref", typeIdx: vti };
  const argsLocal = allocLocal(fctx, "arguments", vecRef);
  const arrTmp = allocLocal(fctx, "__args_arr_tmp", { kind: "ref", typeIdx: ati });
  const hasNumericParams = paramTypes.some((pt) => pt.kind === "f64" || pt.kind === "i32");
  if (hasNumericParams) {
    ensureLateImport2(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
    ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
    flushLateImportShifts2(ctx, fctx);
  }
  fctx.mappedArgsInfo = {
    argsLocalIdx: argsLocal,
    arrTypeIdx: ati,
    vecTypeIdx: vti,
    paramCount: numArgs,
    paramOffset,
    paramTypes: paramTypes.slice(),
  };
  for (let i = 0; i < numArgs; i++) {
    fctx.body.push({ op: "local.get", index: i + paramOffset });
    const pt = paramTypes[i];
    if (pt.kind === "f64") {
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      } else {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "ref.null.extern" });
      }
    } else if (pt.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      } else {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "ref.null.extern" });
      }
    } else if (pt.kind === "ref" || pt.kind === "ref_null") {
      fctx.body.push({ op: "extern.convert_any" });
    }
  }
  fctx.body.push({ op: "array.new_fixed", typeIdx: ati, length: numArgs });
  fctx.body.push({ op: "local.set", index: arrTmp });
  fctx.body.push({ op: "i32.const", value: numArgs });
  fctx.body.push({ op: "local.get", index: arrTmp });
  fctx.body.push({ op: "struct.new", typeIdx: vti });
  fctx.body.push({ op: "local.set", index: argsLocal });
}

// src/codegen/literals.ts
function ensureComputedPropertyFields(ctx, fctx, expr, tsType) {
  const existingName = resolveStructName(ctx, tsType);
  if (!existingName) return;
  const existingFields = ctx.structFields.get(existingName);
  if (!existingFields) return;
  const resolvedProps = [];
  for (const prop of expr.properties) {
    if (!ts9.isPropertyAssignment(prop)) continue;
    const propName = resolvePropertyNameText(ctx, prop);
    if (propName === void 0) continue;
    if (existingFields.some((f) => f.name === propName)) continue;
    resolvedProps.push({ name: propName, valueExpr: prop.initializer });
  }
  if (resolvedProps.length === 0) return;
  const fields = [...existingFields];
  for (const rp of resolvedProps) {
    const propType = ctx.checker.getTypeAtLocation(rp.valueExpr);
    const wasmType = resolveWasmType(ctx, propType);
    fields.push({ name: rp.name, type: wasmType, mutable: true });
  }
  const structTypeIdx = ctx.structMap.get(existingName);
  const typeDef = ctx.mod.types[structTypeIdx];
  typeDef.fields = fields;
  ctx.structFields.set(existingName, fields);
  for (const rp of resolvedProps) {
    const propType = ctx.checker.getTypeAtLocation(rp.valueExpr);
    const wasmType = resolveWasmType(ctx, propType);
    patchStructNewForAddedField(ctx, fctx, structTypeIdx, wasmType);
  }
}
function compileObjectLiteral(ctx, fctx, expr) {
  if (expr.properties.length === 0 && ts9.isVariableDeclaration(expr.parent) && ts9.isIdentifier(expr.parent.name)) {
    const widenedProps = ctx.widenedTypeProperties.get(expr.parent.name.text);
    if (widenedProps && widenedProps.length > 0) {
      return compileWidenedEmptyObject(ctx, fctx, expr, widenedProps);
    }
  }
  const contextType = ctx.checker.getContextualType(expr);
  if (!contextType) {
    const type = ctx.checker.getTypeAtLocation(expr);
    let typeName2 = resolveStructName(ctx, type);
    if (!typeName2) {
      ensureStructForType(ctx, type);
      typeName2 = resolveStructName(ctx, type);
    }
    if (typeName2) {
      ensureComputedPropertyFields(ctx, fctx, expr, type);
      return compileObjectLiteralForStruct(ctx, fctx, expr, typeName2);
    }
    ctx.errors.push({
      message: "Cannot determine struct type for object literal",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  let typeName = resolveStructName(ctx, contextType);
  if (!typeName) {
    ensureStructForType(ctx, contextType);
    typeName = resolveStructName(ctx, contextType);
  }
  if (typeName) {
    ensureComputedPropertyFields(ctx, fctx, expr, contextType);
    return compileObjectLiteralForStruct(ctx, fctx, expr, typeName);
  }
  const inferredType = ctx.checker.getTypeAtLocation(expr);
  let inferredName = resolveStructName(ctx, inferredType);
  if (!inferredName) {
    ensureStructForType(ctx, inferredType);
    inferredName = resolveStructName(ctx, inferredType);
  }
  if (inferredName) {
    ensureComputedPropertyFields(ctx, fctx, expr, inferredType);
    return compileObjectLiteralForStruct(ctx, fctx, expr, inferredName);
  }
  ctx.errors.push({
    message: "Object literal type not mapped to struct",
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}
function resolveConstantExpression(ctx, expr) {
  if (ts9.isNumericLiteral(expr)) return Number(expr.text);
  if (expr.kind === ts9.SyntaxKind.TrueKeyword) return 1;
  if (expr.kind === ts9.SyntaxKind.FalseKeyword) return 0;
  if (expr.kind === ts9.SyntaxKind.NullKeyword) return "null";
  if (ts9.isStringLiteral(expr)) return expr.text;
  if (ts9.isParenthesizedExpression(expr)) {
    return resolveConstantExpression(ctx, expr.expression);
  }
  if (ts9.isIdentifier(expr)) {
    const sym = ctx.checker.getSymbolAtLocation(expr);
    if (sym) {
      const decl = sym.valueDeclaration;
      if (decl && ts9.isVariableDeclaration(decl) && decl.initializer) {
        const declList = decl.parent;
        if (ts9.isVariableDeclarationList(declList) && (declList.flags & ts9.NodeFlags.Const) !== 0) {
          return resolveConstantExpression(ctx, decl.initializer);
        }
        if (ts9.isVariableDeclarationList(declList) && decl.initializer) {
          if (ts9.isStringLiteral(decl.initializer) || ts9.isNumericLiteral(decl.initializer)) {
            return ts9.isStringLiteral(decl.initializer)
              ? decl.initializer.text
              : String(Number(decl.initializer.text));
          }
        }
      }
    }
    return void 0;
  }
  if (ts9.isBinaryExpression(expr) && expr.operatorToken.kind === ts9.SyntaxKind.EqualsToken) {
    return resolveConstantExpression(ctx, expr.right);
  }
  if (ts9.isBinaryExpression(expr)) {
    const left = resolveConstantExpression(ctx, expr.left);
    const right = resolveConstantExpression(ctx, expr.right);
    if (left === void 0 || right === void 0) return void 0;
    if (typeof left === "string" || typeof right === "string") {
      if (expr.operatorToken.kind === ts9.SyntaxKind.PlusToken) {
        return String(left) + String(right);
      }
      return void 0;
    }
    switch (expr.operatorToken.kind) {
      case ts9.SyntaxKind.PlusToken:
        return left + right;
      case ts9.SyntaxKind.MinusToken:
        return left - right;
      case ts9.SyntaxKind.AsteriskToken:
        return left * right;
      case ts9.SyntaxKind.SlashToken:
        return right !== 0 ? left / right : void 0;
      case ts9.SyntaxKind.PercentToken:
        return right !== 0 ? left % right : void 0;
      case ts9.SyntaxKind.AsteriskAsteriskToken:
        return left ** right;
      default:
        return void 0;
    }
  }
  if (ts9.isPrefixUnaryExpression(expr)) {
    const operand = resolveConstantExpression(ctx, expr.operand);
    if (typeof operand !== "number") return void 0;
    switch (expr.operator) {
      case ts9.SyntaxKind.MinusToken:
        return -operand;
      case ts9.SyntaxKind.PlusToken:
        return operand;
      default:
        return void 0;
    }
  }
  if (ts9.isConditionalExpression(expr)) {
    const cond = resolveConstantExpression(ctx, expr.condition);
    if (cond === void 0) return void 0;
    const isTruthy = typeof cond === "string" ? cond.length > 0 : cond !== 0 && !isNaN(cond);
    return resolveConstantExpression(ctx, isTruthy ? expr.whenTrue : expr.whenFalse);
  }
  if (ts9.isBinaryExpression(expr) && expr.operatorToken.kind === ts9.SyntaxKind.QuestionQuestionToken) {
    const left = resolveConstantExpression(ctx, expr.left);
    if (left !== void 0) return left;
    return resolveConstantExpression(ctx, expr.right);
  }
  if (ts9.isTemplateExpression(expr)) {
    let result = expr.head.text;
    for (const span of expr.templateSpans) {
      const val = resolveConstantExpression(ctx, span.expression);
      if (val === void 0) return void 0;
      result += String(val) + span.literal.text;
    }
    return result;
  }
  if (ts9.isNoSubstitutionTemplateLiteral(expr)) {
    return expr.text;
  }
  if (ts9.isCallExpression(expr) && ts9.isIdentifier(expr.expression) && expr.arguments.length === 1) {
    const funcName = expr.expression.text;
    const argVal = resolveConstantExpression(ctx, expr.arguments[0]);
    if (argVal !== void 0) {
      if (funcName === "String") return String(argVal);
      if (funcName === "Number") return typeof argVal === "string" ? Number(argVal) : argVal;
    }
  }
  return void 0;
}
function resolvePropertyNameText(ctx, prop) {
  if (!ts9.isPropertyAssignment(prop)) return void 0;
  const name = prop.name;
  if (ts9.isIdentifier(name)) return name.text;
  if (ts9.isStringLiteral(name)) return name.text;
  if (ts9.isNumericLiteral(name)) return String(Number(name.text));
  if (ts9.isComputedPropertyName(name)) {
    return resolveComputedKeyExpression(ctx, name.expression);
  }
  return void 0;
}
var WELL_KNOWN_SYMBOLS = {
  iterator: 1,
  hasInstance: 2,
  toPrimitive: 3,
  toStringTag: 4,
  species: 5,
  isConcatSpreadable: 6,
  match: 7,
  replace: 8,
  search: 9,
  split: 10,
  unscopables: 11,
  asyncIterator: 12,
};
function resolveWellKnownSymbol(name) {
  if (name in WELL_KNOWN_SYMBOLS) return `@@${name}`;
  return void 0;
}
function getWellKnownSymbolId(name) {
  return WELL_KNOWN_SYMBOLS[name];
}
function ensureSymbolCounter(ctx) {
  if (ctx.symbolCounterGlobalIdx >= 0) return ctx.symbolCounterGlobalIdx;
  const idx = nextModuleGlobalIdx(ctx);
  ctx.mod.globals.push({
    name: "__symbol_counter",
    type: { kind: "i32" },
    mutable: true,
    init: [{ op: "i32.const", value: 100 }],
  });
  ctx.symbolCounterGlobalIdx = idx;
  return idx;
}
function compileSymbolCall(ctx, fctx, args) {
  if (args.length > 0) {
    const argType = compileExpression(ctx, fctx, args[0]);
    if (argType !== null) {
      fctx.body.push({ op: "drop" });
    }
  }
  const counterIdx = ensureSymbolCounter(ctx);
  fctx.body.push({ op: "global.get", index: counterIdx });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "global.set", index: counterIdx });
  fctx.body.push({ op: "global.get", index: counterIdx });
  return { kind: "i32" };
}
function resolveComputedKeyExpression(ctx, expr) {
  if (ts9.isPropertyAccessExpression(expr) && ts9.isIdentifier(expr.expression)) {
    const objName = expr.expression.text;
    const propName = expr.name.text;
    if (objName === "Symbol") {
      const wellKnown = resolveWellKnownSymbol(propName);
      if (wellKnown !== void 0) return wellKnown;
    }
    const enumKey = `${objName}.${propName}`;
    const enumStrVal = ctx.enumStringValues.get(enumKey);
    if (enumStrVal !== void 0) return enumStrVal;
    const enumNumVal = ctx.enumValues.get(enumKey);
    if (enumNumVal !== void 0) return String(enumNumVal);
  }
  const constVal = resolveConstantExpression(ctx, expr);
  if (constVal !== void 0) return String(constVal);
  return void 0;
}
function resolveAccessorPropName(ctx, name) {
  if (ts9.isIdentifier(name)) return name.text;
  if (ts9.isStringLiteral(name)) return name.text;
  if (ts9.isNumericLiteral(name)) return String(Number(name.text));
  if (ts9.isComputedPropertyName(name)) {
    return resolveComputedKeyExpression(ctx, name.expression);
  }
  return void 0;
}
function compileWidenedEmptyObject(ctx, fctx, expr, widenedProps) {
  const type = ctx.checker.getTypeAtLocation(expr);
  let typeName = ctx.anonTypeMap.get(type);
  if (!typeName && ts9.isVariableDeclaration(expr.parent) && ts9.isIdentifier(expr.parent.name)) {
    const varType = ctx.checker.getTypeAtLocation(expr.parent.name);
    typeName = ctx.anonTypeMap.get(varType);
  }
  if (!typeName) {
    if (ts9.isVariableDeclaration(expr.parent) && ts9.isIdentifier(expr.parent.name)) {
      const fields2 = widenedProps.map((wp) => ({
        name: wp.name,
        type: wp.type.kind === "ref" ? { kind: "ref_null", typeIdx: wp.type.typeIdx } : wp.type,
        mutable: true,
      }));
      typeName = `__anon_${ctx.anonTypeCounter++}`;
      const typeIdx = ctx.mod.types.length;
      ctx.mod.types.push({
        kind: "struct",
        name: typeName,
        fields: fields2,
      });
      ctx.structMap.set(typeName, typeIdx);
      ctx.typeIdxToStructName.set(typeIdx, typeName);
      ctx.structFields.set(typeName, fields2);
      ctx.anonTypeMap.set(type, typeName);
      const varType = ctx.checker.getTypeAtLocation(expr.parent.name);
      ctx.anonTypeMap.set(varType, typeName);
    }
  }
  if (!typeName) return null;
  const structTypeIdx = ctx.structMap.get(typeName);
  const fields = ctx.structFields.get(typeName);
  if (structTypeIdx === void 0 || !fields) return null;
  for (const field of fields) {
    switch (field.type.kind) {
      case "f64":
        fctx.body.push({ op: "f64.const", value: 0 });
        break;
      case "i32":
        fctx.body.push({ op: "i32.const", value: 0 });
        break;
      case "externref":
        fctx.body.push({ op: "ref.null.extern" });
        break;
      default:
        if (field.type.kind === "ref" || field.type.kind === "ref_null") {
          fctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
        } else {
          fctx.body.push({ op: "f64.const", value: 0 });
        }
    }
  }
  fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
  return { kind: "ref", typeIdx: structTypeIdx };
}
function compileObjectLiteralForStruct(ctx, fctx, expr, typeName) {
  const structTypeIdx = ctx.structMap.get(typeName);
  const fields = ctx.structFields.get(typeName);
  if (structTypeIdx === void 0 || !fields) {
    ctx.errors.push({
      message: `Unknown struct type: ${typeName}`,
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const spreadSources = [];
  for (const prop of expr.properties) {
    if (ts9.isSpreadAssignment(prop)) {
      const srcType = ctx.checker.getTypeAtLocation(prop.expression);
      const srcStructName = resolveStructName(ctx, srcType);
      if (srcStructName) {
        const srcStructTypeIdx = ctx.structMap.get(srcStructName);
        const srcFields = ctx.structFields.get(srcStructName);
        if (srcStructTypeIdx !== void 0 && srcFields) {
          const srcValType = { kind: "ref", typeIdx: srcStructTypeIdx };
          const srcLocal = allocLocal(fctx, `__spread_obj_${fctx.locals.length}`, srcValType);
          const spreadResult = compileExpression(ctx, fctx, prop.expression);
          if (!spreadResult) continue;
          fctx.body.push({ op: "local.set", index: srcLocal });
          spreadSources.push({ local: srcLocal, srcStructTypeIdx, srcFields });
        }
      }
    }
  }
  for (const field of fields) {
    const prop = expr.properties.find((p) => resolvePropertyNameText(ctx, p) === field.name);
    const shorthandProp = !prop
      ? expr.properties.find((p) => ts9.isShorthandPropertyAssignment(p) && p.name.text === field.name)
      : void 0;
    if (prop && ts9.isPropertyAssignment(prop)) {
      const bodyLenBefore = fctx.body.length;
      compileExpression(ctx, fctx, prop.initializer, field.type);
      if ((field.name === "valueOf" || field.name === "toString") && field.type.kind === "eqref") {
        for (let bi = bodyLenBefore; bi < fctx.body.length; bi++) {
          const instr = fctx.body[bi];
          if (instr.op === "struct.new" && ctx.closureInfoByTypeIdx.has(instr.typeIdx)) {
            const closureTypeIdx = instr.typeIdx;
            const existing = ctx.valueOfClosureTypes.get(typeName) ?? [];
            if (!existing.includes(closureTypeIdx)) {
              existing.push(closureTypeIdx);
              ctx.valueOfClosureTypes.set(typeName, existing);
            }
          }
        }
      }
    } else if (shorthandProp && ts9.isShorthandPropertyAssignment(shorthandProp)) {
      compileExpression(ctx, fctx, shorthandProp.name, field.type);
    } else {
      let found = false;
      for (let si = spreadSources.length - 1; si >= 0; si--) {
        const src = spreadSources[si];
        const fieldIdx = src.srcFields.findIndex((f) => f.name === field.name);
        if (fieldIdx >= 0) {
          fctx.body.push({ op: "local.get", index: src.local });
          fctx.body.push({ op: "struct.get", typeIdx: src.srcStructTypeIdx, fieldIdx });
          found = true;
          break;
        }
      }
      if (!found) {
        if (field.type.kind === "f64") {
          fctx.body.push({ op: "i64.const", value: 0x7ff00000deadc0den });
          fctx.body.push({ op: "f64.reinterpret_i64" });
        } else if (field.type.kind === "externref") {
          fctx.body.push({ op: "ref.null.extern" });
        } else if (field.type.kind === "eqref") {
          fctx.body.push({ op: "ref.null.eq" });
        } else if (field.type.kind === "ref" || field.type.kind === "ref_null") {
          fctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
        } else {
          fctx.body.push({ op: "i32.const", value: 0 });
        }
      }
    }
  }
  fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
  for (const prop of expr.properties) {
    if (
      ts9.isGetAccessorDeclaration(prop) &&
      prop.name &&
      (ts9.isIdentifier(prop.name) ||
        ts9.isStringLiteral(prop.name) ||
        ts9.isComputedPropertyName(prop.name) ||
        ts9.isNumericLiteral(prop.name))
    ) {
      const propName = resolveAccessorPropName(ctx, prop.name);
      if (propName === void 0) continue;
      const accessorKey = `${typeName}_${propName}`;
      ctx.classAccessorSet.add(accessorKey);
      const getterName = `${typeName}_get_${propName}`;
      const getterParams = [{ kind: "ref", typeIdx: structTypeIdx }];
      const sig = ctx.checker.getSignatureFromDeclaration(prop);
      let getterResults = [];
      if (sig) {
        const retType = ctx.checker.getReturnTypeOfSignature(sig);
        if (!isVoidType(retType)) {
          getterResults = [resolveWasmType(ctx, retType)];
        }
      }
      const getterTypeIdx = addFuncType(ctx, getterParams, getterResults, `${getterName}_type`);
      const getterFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
      ctx.funcMap.set(getterName, getterFuncIdx);
      const getterFunc = {
        name: getterName,
        typeIdx: getterTypeIdx,
        locals: [],
        body: [],
        exported: false,
      };
      ctx.mod.functions.push(getterFunc);
      promoteAccessorCapturesToGlobals(ctx, fctx, prop.body);
      const getterFctx = {
        name: getterName,
        params: [{ name: "this", type: { kind: "ref", typeIdx: structTypeIdx } }],
        locals: [],
        localMap: /* @__PURE__ */ new Map(),
        returnType: getterResults.length > 0 ? getterResults[0] : null,
        body: [],
        blockDepth: 0,
        breakStack: [],
        continueStack: [],
        labelMap: /* @__PURE__ */ new Map(),
        savedBodies: [],
      };
      getterFctx.localMap.set("this", 0);
      const savedFunc = ctx.currentFunc;
      if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
      if (savedFunc) ctx.funcStack.push(savedFunc);
      ctx.currentFunc = getterFctx;
      if (prop.body) {
        for (const stmt of prop.body.statements) {
          compileStatement(ctx, getterFctx, stmt);
        }
      }
      if (getterFctx.returnType) {
        const lastInstr = getterFctx.body[getterFctx.body.length - 1];
        if (!lastInstr || lastInstr.op !== "return") {
          if (getterFctx.returnType.kind === "f64") {
            getterFctx.body.push({ op: "f64.const", value: 0 });
          } else if (getterFctx.returnType.kind === "i32") {
            getterFctx.body.push({ op: "i32.const", value: 0 });
          } else if (getterFctx.returnType.kind === "externref") {
            getterFctx.body.push({ op: "ref.null.extern" });
          } else if (getterFctx.returnType.kind === "ref" || getterFctx.returnType.kind === "ref_null") {
            getterFctx.body.push({ op: "ref.null", typeIdx: getterFctx.returnType.typeIdx });
          }
        }
      }
      cacheStringLiterals(ctx, getterFctx);
      getterFunc.locals = getterFctx.locals;
      getterFunc.body = getterFctx.body;
      if (savedFunc) ctx.funcStack.pop();
      if (savedFunc) ctx.parentBodiesStack.pop();
      ctx.currentFunc = savedFunc;
    }
    if (
      ts9.isSetAccessorDeclaration(prop) &&
      prop.name &&
      (ts9.isIdentifier(prop.name) ||
        ts9.isStringLiteral(prop.name) ||
        ts9.isComputedPropertyName(prop.name) ||
        ts9.isNumericLiteral(prop.name))
    ) {
      const propName = resolveAccessorPropName(ctx, prop.name);
      if (propName === void 0) continue;
      const accessorKey = `${typeName}_${propName}`;
      ctx.classAccessorSet.add(accessorKey);
      const setterName = `${typeName}_set_${propName}`;
      const setterParams = [{ kind: "ref", typeIdx: structTypeIdx }];
      for (const param of prop.parameters) {
        const paramType = ctx.checker.getTypeAtLocation(param);
        setterParams.push(resolveWasmType(ctx, paramType));
      }
      const setterTypeIdx = addFuncType(ctx, setterParams, [], `${setterName}_type`);
      const setterFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
      ctx.funcMap.set(setterName, setterFuncIdx);
      const setterFunc = {
        name: setterName,
        typeIdx: setterTypeIdx,
        locals: [],
        body: [],
        exported: false,
      };
      ctx.mod.functions.push(setterFunc);
      promoteAccessorCapturesToGlobals(ctx, fctx, prop.body);
      const setterFctxParams = [{ name: "this", type: { kind: "ref", typeIdx: structTypeIdx } }];
      for (let pi = 0; pi < prop.parameters.length; pi++) {
        const param = prop.parameters[pi];
        const paramName = ts9.isIdentifier(param.name) ? param.name.text : `__param${pi}`;
        const paramType = ctx.checker.getTypeAtLocation(param);
        setterFctxParams.push({ name: paramName, type: resolveWasmType(ctx, paramType) });
      }
      const setterFctx = {
        name: setterName,
        params: setterFctxParams,
        locals: [],
        localMap: /* @__PURE__ */ new Map(),
        returnType: null,
        body: [],
        blockDepth: 0,
        breakStack: [],
        continueStack: [],
        labelMap: /* @__PURE__ */ new Map(),
        savedBodies: [],
      };
      for (let i = 0; i < setterFctxParams.length; i++) {
        setterFctx.localMap.set(setterFctxParams[i].name, i);
      }
      const savedFunc = ctx.currentFunc;
      if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
      if (savedFunc) ctx.funcStack.push(savedFunc);
      ctx.currentFunc = setterFctx;
      for (let pi = 0; pi < prop.parameters.length; pi++) {
        const param = prop.parameters[pi];
        if (!param.initializer) continue;
        const paramLocalIdx = pi + 1;
        const paramType = setterFctxParams[paramLocalIdx].type;
        const savedBody = pushBody(setterFctx);
        compileExpression(ctx, setterFctx, param.initializer, paramType);
        setterFctx.body.push({ op: "local.set", index: paramLocalIdx });
        const thenInstrs = setterFctx.body;
        popBody(setterFctx, savedBody);
        if (paramType.kind === "externref") {
          setterFctx.body.push({ op: "local.get", index: paramLocalIdx });
          setterFctx.body.push({ op: "ref.is_null" });
          setterFctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
        } else if (paramType.kind === "ref_null" || paramType.kind === "ref") {
          setterFctx.body.push({ op: "local.get", index: paramLocalIdx });
          setterFctx.body.push({ op: "ref.is_null" });
          setterFctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
        } else if (paramType.kind === "i32") {
          setterFctx.body.push({ op: "local.get", index: paramLocalIdx });
          setterFctx.body.push({ op: "i32.eqz" });
          setterFctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
        } else if (paramType.kind === "f64") {
          setterFctx.body.push({ op: "local.get", index: paramLocalIdx });
          setterFctx.body.push({ op: "f64.const", value: 0 });
          setterFctx.body.push({ op: "f64.eq" });
          setterFctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
        }
      }
      if (prop.body) {
        for (const stmt of prop.body.statements) {
          compileStatement(ctx, setterFctx, stmt);
        }
      }
      cacheStringLiterals(ctx, setterFctx);
      setterFunc.locals = setterFctx.locals;
      setterFunc.body = setterFctx.body;
      if (savedFunc) ctx.funcStack.pop();
      if (savedFunc) ctx.parentBodiesStack.pop();
      ctx.currentFunc = savedFunc;
    }
    if (
      ts9.isMethodDeclaration(prop) &&
      prop.name &&
      (ts9.isIdentifier(prop.name) ||
        ts9.isStringLiteral(prop.name) ||
        ts9.isNumericLiteral(prop.name) ||
        ts9.isComputedPropertyName(prop.name))
    ) {
      const methodName = resolveAccessorPropName(ctx, prop.name);
      if (methodName === void 0) continue;
      const fullName = `${typeName}_${methodName}`;
      ctx.classMethodSet.add(fullName);
      const isGeneratorMethod = prop.asteriskToken !== void 0;
      if (isGeneratorMethod) {
        ctx.generatorFunctions.add(fullName);
      }
      const methodParams = [{ kind: "ref", typeIdx: structTypeIdx }];
      for (const param of prop.parameters) {
        const paramType = ctx.checker.getTypeAtLocation(param);
        let wasmType = resolveWasmType(ctx, paramType);
        if (param.initializer && wasmType.kind === "ref") {
          wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
        }
        methodParams.push(wasmType);
      }
      const sig = ctx.checker.getSignatureFromDeclaration(prop);
      const retType = sig ? ctx.checker.getReturnTypeOfSignature(sig) : void 0;
      const methodResults = isGeneratorMethod
        ? [{ kind: "externref" }]
        : retType && !isVoidType(retType)
          ? [resolveWasmType(ctx, retType)]
          : [];
      const methodTypeIdx = addFuncType(ctx, methodParams, methodResults, `${fullName}_type`);
      const existingFuncIdx = ctx.funcMap.get(fullName);
      let methodFunc;
      if (existingFuncIdx !== void 0) {
        const localIdx = existingFuncIdx - ctx.numImportFuncs;
        methodFunc = ctx.mod.functions[localIdx];
        methodFunc.typeIdx = methodTypeIdx;
      } else {
        const methodFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
        ctx.funcMap.set(fullName, methodFuncIdx);
        methodFunc = {
          name: fullName,
          typeIdx: methodTypeIdx,
          locals: [],
          body: [],
          exported: false,
        };
        ctx.mod.functions.push(methodFunc);
      }
      promoteAccessorCapturesToGlobals(ctx, fctx, prop.body);
      const methodFctxParams = [{ name: "this", type: { kind: "ref", typeIdx: structTypeIdx } }];
      for (let pi = 0; pi < prop.parameters.length; pi++) {
        const param = prop.parameters[pi];
        const paramName = ts9.isIdentifier(param.name) ? param.name.text : `__param${pi}`;
        const paramType = ctx.checker.getTypeAtLocation(param);
        let wasmType = resolveWasmType(ctx, paramType);
        if ((param.initializer || param.questionToken) && wasmType.kind === "ref") {
          wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
        }
        methodFctxParams.push({ name: paramName, type: wasmType });
      }
      const methodFctx = {
        name: fullName,
        params: methodFctxParams,
        locals: [],
        localMap: /* @__PURE__ */ new Map(),
        returnType: methodResults.length > 0 ? methodResults[0] : null,
        body: [],
        blockDepth: 0,
        breakStack: [],
        continueStack: [],
        labelMap: /* @__PURE__ */ new Map(),
        savedBodies: [],
        isGenerator: isGeneratorMethod,
      };
      for (let i = 0; i < methodFctxParams.length; i++) {
        methodFctx.localMap.set(methodFctxParams[i].name, i);
      }
      const savedFunc = ctx.currentFunc;
      if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
      if (savedFunc) ctx.funcStack.push(savedFunc);
      ctx.currentFunc = methodFctx;
      emitMethodParamDefaults(ctx, methodFctx, prop.parameters, 1);
      for (let pi = 0; pi < prop.parameters.length; pi++) {
        const param = prop.parameters[pi];
        const paramLocalIdx = pi + 1;
        if (ts9.isObjectBindingPattern(param.name)) {
          destructureParamObject2(ctx, methodFctx, paramLocalIdx, param.name, methodFctxParams[paramLocalIdx].type);
        } else if (ts9.isArrayBindingPattern(param.name)) {
          destructureParamArray(ctx, methodFctx, paramLocalIdx, param.name, methodFctxParams[paramLocalIdx].type);
        }
      }
      if (prop.body && bodyUsesArguments(prop.body)) {
        const methodParamTypes = methodFctxParams.slice(1).map((p) => p.type);
        emitArgumentsObject(ctx, methodFctx, methodParamTypes, 1);
      }
      if (isGeneratorMethod && prop.body) {
        const bufferLocal = allocLocal(methodFctx, "__gen_buffer", { kind: "externref" });
        const createBufIdx = ctx.funcMap.get("__gen_create_buffer");
        methodFctx.body.push({ op: "call", funcIdx: createBufIdx });
        methodFctx.body.push({ op: "local.set", index: bufferLocal });
        const bodyInstrs = [];
        const outerBody = methodFctx.body;
        methodFctx.body = bodyInstrs;
        methodFctx.generatorReturnDepth = 0;
        methodFctx.blockDepth++;
        for (let i = 0; i < methodFctx.breakStack.length; i++) methodFctx.breakStack[i]++;
        for (let i = 0; i < methodFctx.continueStack.length; i++) methodFctx.continueStack[i]++;
        for (const stmt of prop.body.statements) {
          compileStatement(ctx, methodFctx, stmt);
        }
        methodFctx.blockDepth--;
        for (let i = 0; i < methodFctx.breakStack.length; i++) methodFctx.breakStack[i]--;
        for (let i = 0; i < methodFctx.continueStack.length; i++) methodFctx.continueStack[i]--;
        methodFctx.generatorReturnDepth = void 0;
        methodFctx.body = outerBody;
        methodFctx.body.push({
          op: "block",
          blockType: { kind: "empty" },
          body: bodyInstrs,
        });
        const isAsyncProp = prop.modifiers?.some((m) => m.kind === 134) ?? false;
        const createGenIdx = ctx.funcMap.get(isAsyncProp ? "__create_async_generator" : "__create_generator");
        methodFctx.body.push({ op: "local.get", index: bufferLocal });
        methodFctx.body.push({ op: "call", funcIdx: createGenIdx });
      } else if (prop.body) {
        for (const stmt of prop.body.statements) {
          compileStatement(ctx, methodFctx, stmt);
        }
      }
      if (methodFctx.returnType && !isGeneratorMethod) {
        const lastInstr = methodFctx.body[methodFctx.body.length - 1];
        if (!lastInstr || lastInstr.op !== "return") {
          if (methodFctx.returnType.kind === "f64") {
            methodFctx.body.push({ op: "f64.const", value: 0 });
          } else if (methodFctx.returnType.kind === "i32") {
            methodFctx.body.push({ op: "i32.const", value: 0 });
          } else if (methodFctx.returnType.kind === "externref") {
            methodFctx.body.push({ op: "ref.null.extern" });
          } else if (methodFctx.returnType.kind === "ref" || methodFctx.returnType.kind === "ref_null") {
            methodFctx.body.push({ op: "ref.null", typeIdx: methodFctx.returnType.typeIdx });
          }
        }
      }
      cacheStringLiterals(ctx, methodFctx);
      methodFunc.locals = methodFctx.locals;
      methodFunc.body = methodFctx.body;
      if (savedFunc) ctx.funcStack.pop();
      if (savedFunc) ctx.parentBodiesStack.pop();
      ctx.currentFunc = savedFunc;
    }
  }
  return { kind: "ref", typeIdx: structTypeIdx };
}
function compileTupleLiteral(ctx, fctx, expr, tupleType) {
  const elemTypes = getTupleElementTypes(ctx, tupleType);
  const tupleIdx = getOrRegisterTupleType(ctx, elemTypes);
  for (let i = 0; i < elemTypes.length; i++) {
    const expectedType = elemTypes[i] ?? { kind: "externref" };
    if (i < expr.elements.length) {
      compileExpression(ctx, fctx, expr.elements[i], expectedType);
    } else {
      if (expectedType.kind === "f64") {
        fctx.body.push({ op: "i64.const", value: 0x7ff00000deadc0den });
        fctx.body.push({ op: "f64.reinterpret_i64" });
      } else if (expectedType.kind === "i32") {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (expectedType.kind === "externref") {
        fctx.body.push({ op: "ref.null.extern" });
      } else if (expectedType.kind === "ref_null" || expectedType.kind === "ref") {
        const typeIdx = expectedType.typeIdx;
        fctx.body.push({ op: "ref.null", typeIdx });
      } else {
        pushDefaultValue(fctx, expectedType, ctx);
      }
    }
  }
  fctx.body.push({ op: "struct.new", typeIdx: tupleIdx });
  return { kind: "ref", typeIdx: tupleIdx };
}
function compileArrayLiteral(ctx, fctx, expr) {
  const ctxTupleType = ctx.checker.getContextualType(expr) ?? ctx.checker.getTypeAtLocation(expr);
  if (ctxTupleType && isTupleType(ctxTupleType) && !ctx._arrayLiteralForceVec) {
    let tupleType = ctxTupleType;
    if (expr.elements.length > 1) {
      const ctxElemTypes = getTupleElementTypes(ctx, ctxTupleType);
      const allSameKind = ctxElemTypes.length > 0 && ctxElemTypes.every((t) => t.kind === ctxElemTypes[0].kind);
      if (allSameKind) {
        const actualType = ctx.checker.getTypeAtLocation(expr);
        if (actualType && isTupleType(actualType)) {
          const actualElemTypes = getTupleElementTypes(ctx, actualType);
          const actualHeterogeneous =
            actualElemTypes.length > 1 && !actualElemTypes.every((t) => t.kind === actualElemTypes[0].kind);
          if (actualHeterogeneous) {
            tupleType = actualType;
          }
        }
      }
    }
    return compileTupleLiteral(ctx, fctx, expr, tupleType);
  }
  if (expr.elements.length === 0) {
    let emptyElemKind = "externref";
    const ctxType = ctx.checker.getContextualType(expr) ?? ctx.checker.getTypeAtLocation(expr);
    if (ctxType) {
      const sym = ctxType.symbol ?? ctxType.symbol;
      if (sym?.name === "Array") {
        const typeArgs = ctx.checker.getTypeArguments(ctxType);
        if (typeArgs[0]) {
          const elemWasmType = resolveWasmType(ctx, typeArgs[0]);
          emptyElemKind =
            elemWasmType.kind === "ref" || elemWasmType.kind === "ref_null"
              ? `ref_${elemWasmType.typeIdx}`
              : elemWasmType.kind;
        }
      }
    }
    const vecTypeIdx2 = getOrRegisterVecType(ctx, emptyElemKind);
    const arrTypeIdx2 = getArrTypeIdxFromVec(ctx, vecTypeIdx2);
    if (arrTypeIdx2 < 0) {
      ctx.errors.push({ message: "Empty array literal: invalid vec type", line: getLine(expr), column: getCol(expr) });
      return null;
    }
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx2 });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx2 });
    return { kind: "ref_null", typeIdx: vecTypeIdx2 };
  }
  const hasSpread = expr.elements.some((el) => ts9.isSpreadElement(el));
  let elemWasm;
  let elemKind;
  const firstSignificantElem = expr.elements.find((el) => !ts9.isOmittedExpression(el));
  const firstElem = firstSignificantElem ?? expr.elements[0];
  if (ts9.isSpreadElement(firstElem)) {
    const spreadType = ctx.checker.getTypeAtLocation(firstElem.expression);
    const typeArgs = ctx.checker.getTypeArguments(spreadType);
    const innerType = typeArgs[0];
    elemWasm = innerType ? resolveWasmType(ctx, innerType) : { kind: "f64" };
  } else if (ts9.isOmittedExpression(firstElem)) {
    elemWasm = { kind: "externref" };
  } else {
    const firstElemType = ctx.checker.getTypeAtLocation(firstElem);
    elemWasm = resolveWasmType(ctx, firstElemType);
  }
  elemKind = elemWasm.kind === "ref" || elemWasm.kind === "ref_null" ? `ref_${elemWasm.typeIdx}` : elemWasm.kind;
  const vecTypeIdx = getOrRegisterVecType(ctx, elemKind, elemWasm);
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
  if (arrTypeIdx < 0) {
    ctx.errors.push({ message: "Array literal: invalid vec type", line: getLine(expr), column: getCol(expr) });
    return null;
  }
  if (!hasSpread) {
    for (const el of expr.elements) {
      compileExpression(ctx, fctx, el, elemWasm);
    }
    fctx.body.push({ op: "array.new_fixed", typeIdx: arrTypeIdx, length: expr.elements.length });
    const tmpData = allocLocal(fctx, `__arr_data_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
    fctx.body.push({ op: "local.set", index: tmpData });
    fctx.body.push({ op: "i32.const", value: expr.elements.length });
    fctx.body.push({ op: "local.get", index: tmpData });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  const spreadLocals = [];
  const nonSpreadCount = expr.elements.filter((el) => !ts9.isSpreadElement(el)).length;
  fctx.body.push({ op: "i32.const", value: nonSpreadCount });
  for (let i = 0; i < expr.elements.length; i++) {
    const el = expr.elements[i];
    if (ts9.isSpreadElement(el)) {
      const srcType = compileExpression(ctx, fctx, el.expression);
      if (!srcType || (srcType.kind !== "ref" && srcType.kind !== "ref_null")) {
        if (srcType) {
          fctx.body.push({ op: "drop" });
        }
        continue;
      }
      const srcVecTypeIdx = srcType.typeIdx;
      const srcLocal = allocLocal(fctx, `__spread_src_${fctx.locals.length}`, srcType);
      fctx.body.push({ op: "local.tee", index: srcLocal });
      fctx.body.push({ op: "struct.get", typeIdx: srcVecTypeIdx, fieldIdx: 0 });
      fctx.body.push({ op: "i32.add" });
      spreadLocals.push({ local: srcLocal, elemIdx: i, srcVecTypeIdx });
    }
  }
  const resultArrType = { kind: "ref", typeIdx: arrTypeIdx };
  fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
  const resultLocal = allocLocal(fctx, `__spread_result_${fctx.locals.length}`, resultArrType);
  fctx.body.push({ op: "local.set", index: resultLocal });
  const writeIdx = allocLocal(fctx, `__spread_wi_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: writeIdx });
  for (let i = 0; i < expr.elements.length; i++) {
    const el = expr.elements[i];
    if (ts9.isSpreadElement(el)) {
      const spreadInfo = spreadLocals.find((s) => s.elemIdx === i);
      if (!spreadInfo) continue;
      const srcArrTypeIdx = getArrTypeIdxFromVec(ctx, spreadInfo.srcVecTypeIdx);
      if (srcArrTypeIdx < 0) continue;
      const readIdx = allocLocal(fctx, `__spread_ri_${fctx.locals.length}`, { kind: "i32" });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "local.set", index: readIdx });
      const loopBody = [];
      loopBody.push({ op: "local.get", index: readIdx });
      loopBody.push({ op: "local.get", index: spreadInfo.local });
      loopBody.push({ op: "struct.get", typeIdx: spreadInfo.srcVecTypeIdx, fieldIdx: 0 });
      loopBody.push({ op: "i32.ge_s" });
      loopBody.push({ op: "br_if", depth: 1 });
      loopBody.push({ op: "local.get", index: resultLocal });
      loopBody.push({ op: "local.get", index: writeIdx });
      loopBody.push({ op: "local.get", index: spreadInfo.local });
      loopBody.push({ op: "struct.get", typeIdx: spreadInfo.srcVecTypeIdx, fieldIdx: 1 });
      loopBody.push({ op: "local.get", index: readIdx });
      loopBody.push({ op: "array.get", typeIdx: srcArrTypeIdx });
      loopBody.push({ op: "array.set", typeIdx: arrTypeIdx });
      loopBody.push({ op: "local.get", index: writeIdx });
      loopBody.push({ op: "i32.const", value: 1 });
      loopBody.push({ op: "i32.add" });
      loopBody.push({ op: "local.set", index: writeIdx });
      loopBody.push({ op: "local.get", index: readIdx });
      loopBody.push({ op: "i32.const", value: 1 });
      loopBody.push({ op: "i32.add" });
      loopBody.push({ op: "local.set", index: readIdx });
      loopBody.push({ op: "br", depth: 0 });
      fctx.body.push({
        op: "block",
        blockType: { kind: "empty" },
        body: [{ op: "loop", blockType: { kind: "empty" }, body: loopBody }],
      });
    } else {
      fctx.body.push({ op: "local.get", index: resultLocal });
      fctx.body.push({ op: "local.get", index: writeIdx });
      compileExpression(ctx, fctx, el, elemWasm);
      fctx.body.push({ op: "array.set", typeIdx: arrTypeIdx });
      fctx.body.push({ op: "local.get", index: writeIdx });
      fctx.body.push({ op: "i32.const", value: 1 });
      fctx.body.push({ op: "i32.add" });
      fctx.body.push({ op: "local.set", index: writeIdx });
    }
  }
  fctx.body.push({ op: "local.get", index: writeIdx });
  fctx.body.push({ op: "local.get", index: resultLocal });
  fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compileArrayConstructorCall(ctx, fctx, expr) {
  const args = expr.arguments;
  const ctxType = ctx.checker.getContextualType(expr);
  const exprType = ctxType ?? ctx.checker.getTypeAtLocation(expr);
  let elemWasm;
  const rawTypeArgs = ctx.checker.getTypeArguments(exprType);
  const elemTsType = rawTypeArgs?.[0];
  if (elemTsType && !(elemTsType.flags & ts9.TypeFlags.Any)) {
    elemWasm = resolveWasmType(ctx, elemTsType);
  } else {
    elemWasm = { kind: "f64" };
  }
  const elemKind = elemWasm.kind === "ref" || elemWasm.kind === "ref_null" ? `ref_${elemWasm.typeIdx}` : elemWasm.kind;
  const vecTypeIdx = getOrRegisterVecType(ctx, elemKind, elemWasm);
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
  if (arrTypeIdx < 0) {
    ctx.errors.push({ message: "Array(): invalid vec type", line: getLine(expr), column: getCol(expr) });
    return null;
  }
  if (args.length === 0) {
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  if (args.length === 1) {
    compileExpression(ctx, fctx, args[0], { kind: "f64" });
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    const sizeLocal = allocLocal(fctx, `__arr_size_${fctx.locals.length}`, { kind: "i32" });
    fctx.body.push({ op: "local.tee", index: sizeLocal });
    fctx.body.push({ op: "local.get", index: sizeLocal });
    fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  for (const arg of args) {
    compileExpression(ctx, fctx, arg, elemWasm);
  }
  fctx.body.push({ op: "array.new_fixed", typeIdx: arrTypeIdx, length: args.length });
  const tmpData = allocLocal(fctx, `__arr_data_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: tmpData });
  fctx.body.push({ op: "i32.const", value: args.length });
  fctx.body.push({ op: "local.get", index: tmpData });
  fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}

// src/codegen/object-ops.ts
import ts10 from "typescript";
function emitNonObjectArgGuard(ctx, fctx, argExpr, methodName) {
  const tsType = ctx.checker.getTypeAtLocation(argExpr);
  const flags = tsType.flags;
  const NON_OBJECT_FLAGS =
    ts10.TypeFlags.Undefined |
    ts10.TypeFlags.Void |
    ts10.TypeFlags.Null |
    ts10.TypeFlags.BooleanLike |
    ts10.TypeFlags.NumberLike |
    ts10.TypeFlags.StringLike |
    ts10.TypeFlags.BigIntLike;
  if (flags & NON_OBJECT_FLAGS) {
    const argType = compileExpression(ctx, fctx, argExpr);
    if (argType) fctx.body.push({ op: "drop" });
    emitThrowString2(ctx, fctx, `TypeError: ${methodName} called on non-object`);
    return true;
  }
  if (
    argExpr.kind === ts10.SyntaxKind.UndefinedKeyword ||
    argExpr.kind === ts10.SyntaxKind.NullKeyword ||
    argExpr.kind === ts10.SyntaxKind.TrueKeyword ||
    argExpr.kind === ts10.SyntaxKind.FalseKeyword ||
    ts10.isNumericLiteral(argExpr) ||
    (ts10.isIdentifier(argExpr) && argExpr.text === "undefined")
  ) {
    emitThrowString2(ctx, fctx, `TypeError: ${methodName} called on non-object`);
    return true;
  }
  return false;
}
function emitObjectArgNullGuard(ctx, fctx, localIdx) {
  const message = "TypeError: Object method called on null or undefined";
  addStringConstantGlobal(ctx, message);
  const strIdx = ctx.stringGlobalMap.get(message);
  const tagIdx = ensureExnTag(ctx);
  fctx.body.push({ op: "local.get", index: localIdx });
  fctx.body.push({ op: "ref.is_null" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "global.get", index: strIdx },
      { op: "throw", tagIdx },
    ],
    else: [],
  });
}
var PROP_FLAG_WRITABLE = 1 << 0;
var PROP_FLAG_ENUMERABLE = 1 << 1;
var PROP_FLAG_CONFIGURABLE = 1 << 2;
var PROP_FLAG_DEFINED = 1 << 3;
var PROP_FLAG_ACCESSOR = 1 << 4;
function computeDescriptorFlags(writable, enumerable, configurable, isAccessor) {
  let flags = PROP_FLAG_DEFINED;
  if (writable) flags |= PROP_FLAG_WRITABLE;
  if (enumerable) flags |= PROP_FLAG_ENUMERABLE;
  if (configurable) flags |= PROP_FLAG_CONFIGURABLE;
  if (isAccessor) flags |= PROP_FLAG_ACCESSOR;
  return flags;
}
function compileObjectDefineProperty(ctx, fctx, expr) {
  const objArg = expr.arguments[0];
  const propArg = expr.arguments[1];
  const descArg = expr.arguments[2];
  if (emitNonObjectArgGuard(ctx, fctx, objArg, "Object.defineProperty")) {
    fctx.body.push({ op: "unreachable" });
    return { kind: "externref" };
  }
  let valueExpr;
  let getNode;
  let setNode;
  if (ts10.isObjectLiteralExpression(descArg)) {
    for (const prop of descArg.properties) {
      if (ts10.isPropertyAssignment(prop) && ts10.isIdentifier(prop.name) && prop.name.text === "value") {
        valueExpr = prop.initializer;
      }
      if (
        ts10.isPropertyAssignment(prop) &&
        ts10.isIdentifier(prop.name) &&
        prop.name.text === "get" &&
        (ts10.isFunctionExpression(prop.initializer) || ts10.isArrowFunction(prop.initializer))
      ) {
        getNode = prop.initializer;
      }
      if (ts10.isMethodDeclaration(prop) && prop.name && ts10.isIdentifier(prop.name) && prop.name.text === "get") {
        getNode = prop;
      }
      if (
        ts10.isPropertyAssignment(prop) &&
        ts10.isIdentifier(prop.name) &&
        prop.name.text === "set" &&
        (ts10.isFunctionExpression(prop.initializer) || ts10.isArrowFunction(prop.initializer))
      ) {
        setNode = prop.initializer;
      }
      if (ts10.isMethodDeclaration(prop) && prop.name && ts10.isIdentifier(prop.name) && prop.name.text === "set") {
        setNode = prop;
      }
    }
  }
  let descWritable;
  let descEnumerable;
  let descConfigurable;
  if (ts10.isObjectLiteralExpression(descArg)) {
    for (const prop of descArg.properties) {
      if (ts10.isPropertyAssignment(prop) && ts10.isIdentifier(prop.name)) {
        const name = prop.name.text;
        if (name === "writable" || name === "enumerable" || name === "configurable") {
          let boolVal;
          if (prop.initializer.kind === ts10.SyntaxKind.TrueKeyword) boolVal = true;
          else if (prop.initializer.kind === ts10.SyntaxKind.FalseKeyword) boolVal = false;
          if (name === "writable") descWritable = boolVal;
          else if (name === "enumerable") descEnumerable = boolVal;
          else if (name === "configurable") descConfigurable = boolVal;
        }
      }
    }
  }
  let propName;
  if (ts10.isStringLiteral(propArg)) {
    propName = propArg.text;
  }
  const objTsType = ctx.checker.getTypeAtLocation(objArg);
  let structName =
    resolveStructName(ctx, objTsType) ||
    (ts10.isIdentifier(objArg) ? ctx.widenedVarStructMap.get(objArg.text) : void 0);
  if (!structName && ts10.isIdentifier(objArg)) {
    const localIdx = fctx.localMap.get(objArg.text);
    if (localIdx !== void 0) {
      const localType =
        localIdx < fctx.params.length ? fctx.params[localIdx].type : fctx.locals[localIdx - fctx.params.length]?.type;
      if (localType && (localType.kind === "ref" || localType.kind === "ref_null")) {
        structName = ctx.typeIdxToStructName.get(localType.typeIdx);
      }
    }
  }
  if (!structName && ts10.isIdentifier(objArg)) {
    const sym = ctx.checker.getSymbolAtLocation(objArg);
    const decl = sym?.valueDeclaration;
    if (decl && ts10.isVariableDeclaration(decl) && decl.initializer) {
      const initType = ctx.checker.getTypeAtLocation(decl.initializer);
      structName = resolveStructName(ctx, initType);
      if (!structName && ts10.isObjectLiteralExpression(decl.initializer)) {
        const litProps = decl.initializer.properties
          .filter((p) => ts10.isPropertyAssignment(p) && ts10.isIdentifier(p.name))
          .map((p) => p.name.text)
          .sort();
        if (litProps.length > 0) {
          for (const [sName, sFields] of ctx.structFields) {
            const fieldNames = sFields.map((f) => f.name).sort();
            if (fieldNames.length === litProps.length && fieldNames.every((n, i) => n === litProps[i])) {
              structName = sName;
              break;
            }
          }
        }
      }
    }
  }
  const structTypeIdx = structName ? ctx.structMap.get(structName) : void 0;
  const fields = structName ? ctx.structFields.get(structName) : void 0;
  const fieldIdx = fields && propName ? fields.findIndex((f) => f.name === propName) : -1;
  const useStruct = structTypeIdx !== void 0 && fields && fieldIdx >= 0 && valueExpr;
  if ((getNode || setNode) && !valueExpr && structName && structTypeIdx !== void 0 && propName) {
    const objType = compileExpression(ctx, fctx, objArg);
    if (!objType) return null;
    const objLocal = allocLocal(fctx, `__defprop_obj_${fctx.locals.length}`, objType);
    fctx.body.push({ op: "local.set", index: objLocal });
    emitObjectArgNullGuard(ctx, fctx, objLocal);
    const accessorKey = `${structName}_${propName}`;
    ctx.classAccessorSet.add(accessorKey);
    const getBodyStatements = (node) => {
      if (ts10.isArrowFunction(node) && !ts10.isBlock(node.body)) {
        return [];
      }
      const body = ts10.isArrowFunction(node) ? node.body : node.body;
      return body ? [...body.statements] : [];
    };
    const getParams = (node) => {
      return node.parameters;
    };
    if (getNode) {
      const getterName = `${structName}_get_${propName}`;
      if (!ctx.funcMap.has(getterName)) {
        const getterParams = [{ kind: "ref_null", typeIdx: structTypeIdx }];
        const sig = ctx.checker.getSignatureFromDeclaration(getNode);
        let getterResults = [];
        if (sig) {
          const retType = ctx.checker.getReturnTypeOfSignature(sig);
          if (!isVoidType(retType)) {
            getterResults = [resolveWasmType(ctx, retType)];
          }
        }
        const getterTypeIdx = addFuncType(ctx, getterParams, getterResults, `${getterName}_type`);
        const getterFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
        ctx.funcMap.set(getterName, getterFuncIdx);
        const getterFunc = {
          name: getterName,
          typeIdx: getterTypeIdx,
          locals: [],
          body: [],
          exported: false,
        };
        ctx.mod.functions.push(getterFunc);
        const getterFctx = {
          name: getterName,
          params: [{ name: "this", type: { kind: "ref_null", typeIdx: structTypeIdx } }],
          locals: [],
          localMap: /* @__PURE__ */ new Map(),
          returnType: getterResults.length > 0 ? getterResults[0] : null,
          body: [],
          blockDepth: 0,
          breakStack: [],
          continueStack: [],
          labelMap: /* @__PURE__ */ new Map(),
          savedBodies: [],
        };
        getterFctx.localMap.set("this", 0);
        const savedFunc = ctx.currentFunc;
        if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
        if (savedFunc) ctx.funcStack.push(savedFunc);
        ctx.currentFunc = getterFctx;
        if (ts10.isArrowFunction(getNode) && !ts10.isBlock(getNode.body)) {
          const retType = compileExpression(ctx, getterFctx, getNode.body, getterFctx.returnType ?? void 0);
          if (retType && getterFctx.returnType && retType.kind !== getterFctx.returnType.kind) {
            coerceType(ctx, getterFctx, retType, getterFctx.returnType);
          }
        } else {
          const stmts = getBodyStatements(getNode);
          for (const stmt of stmts) {
            compileStatement(ctx, getterFctx, stmt);
          }
        }
        if (getterFctx.returnType) {
          const lastInstr = getterFctx.body[getterFctx.body.length - 1];
          if (!lastInstr || lastInstr.op !== "return") {
            if (getterFctx.returnType.kind === "f64") {
              getterFctx.body.push({ op: "f64.const", value: 0 });
            } else if (getterFctx.returnType.kind === "i32") {
              getterFctx.body.push({ op: "i32.const", value: 0 });
            } else if (getterFctx.returnType.kind === "externref") {
              getterFctx.body.push({ op: "ref.null.extern" });
            } else if (getterFctx.returnType.kind === "ref" || getterFctx.returnType.kind === "ref_null") {
              getterFctx.body.push({ op: "ref.null", typeIdx: getterFctx.returnType.typeIdx });
            }
          }
        }
        cacheStringLiterals(ctx, getterFctx);
        getterFunc.locals = getterFctx.locals;
        getterFunc.body = getterFctx.body;
        if (savedFunc) ctx.funcStack.pop();
        if (savedFunc) ctx.parentBodiesStack.pop();
        ctx.currentFunc = savedFunc;
      }
    }
    if (setNode) {
      const setterName = `${structName}_set_${propName}`;
      if (!ctx.funcMap.has(setterName)) {
        const setterParams = [{ kind: "ref_null", typeIdx: structTypeIdx }];
        const allNodeParams = getParams(setNode);
        const nodeParams = allNodeParams.filter((p) => !(ts10.isIdentifier(p.name) && p.name.text === "this"));
        for (const param of nodeParams) {
          const paramType = ctx.checker.getTypeAtLocation(param);
          setterParams.push(resolveWasmType(ctx, paramType));
        }
        const setterTypeIdx = addFuncType(ctx, setterParams, [], `${setterName}_type`);
        const setterFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
        ctx.funcMap.set(setterName, setterFuncIdx);
        const setterFunc = {
          name: setterName,
          typeIdx: setterTypeIdx,
          locals: [],
          body: [],
          exported: false,
        };
        ctx.mod.functions.push(setterFunc);
        const setterFctxParams = [{ name: "this", type: { kind: "ref_null", typeIdx: structTypeIdx } }];
        for (let pi = 0; pi < nodeParams.length; pi++) {
          const param = nodeParams[pi];
          const paramName = ts10.isIdentifier(param.name) ? param.name.text : `__param${pi}`;
          const paramType = ctx.checker.getTypeAtLocation(param);
          setterFctxParams.push({ name: paramName, type: resolveWasmType(ctx, paramType) });
        }
        const setterFctx = {
          name: setterName,
          params: setterFctxParams,
          locals: [],
          localMap: /* @__PURE__ */ new Map(),
          returnType: null,
          body: [],
          blockDepth: 0,
          breakStack: [],
          continueStack: [],
          labelMap: /* @__PURE__ */ new Map(),
          savedBodies: [],
        };
        for (let i = 0; i < setterFctxParams.length; i++) {
          setterFctx.localMap.set(setterFctxParams[i].name, i);
        }
        const savedFunc = ctx.currentFunc;
        if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
        if (savedFunc) ctx.funcStack.push(savedFunc);
        ctx.currentFunc = setterFctx;
        if (ts10.isArrowFunction(setNode) && !ts10.isBlock(setNode.body)) {
          const retType = compileExpression(ctx, setterFctx, setNode.body);
          if (retType) setterFctx.body.push({ op: "drop" });
        } else {
          const stmts = getBodyStatements(setNode);
          for (const stmt of stmts) {
            compileStatement(ctx, setterFctx, stmt);
          }
        }
        cacheStringLiterals(ctx, setterFctx);
        setterFunc.locals = setterFctx.locals;
        setterFunc.body = setterFctx.body;
        if (savedFunc) ctx.funcStack.pop();
        if (savedFunc) ctx.parentBodiesStack.pop();
        ctx.currentFunc = savedFunc;
      }
    }
    fctx.body.push({ op: "local.get", index: objLocal });
    return objType;
  }
  if (valueExpr && useStruct) {
    let objType = compileExpression(ctx, fctx, objArg);
    if (!objType) return null;
    if (objType.kind === "externref" && structTypeIdx !== void 0) {
      fctx.body.push({ op: "any.convert_extern" });
      const tmpAny = allocTempLocal(fctx, { kind: "anyref" });
      fctx.body.push({ op: "local.tee", index: tmpAny });
      fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx });
      fctx.body.push({
        op: "if",
        blockType: { kind: "val", type: { kind: "ref_null", typeIdx: structTypeIdx } },
        then: [
          { op: "local.get", index: tmpAny },
          { op: "ref.cast_null", typeIdx: structTypeIdx },
        ],
        else: [{ op: "ref.null", typeIdx: structTypeIdx }],
      });
      releaseTempLocal(fctx, tmpAny);
      objType = { kind: "ref_null", typeIdx: structTypeIdx };
    }
    const objLocal = allocLocal(fctx, `__defprop_obj_${fctx.locals.length}`, objType);
    fctx.body.push({ op: "local.set", index: objLocal });
    emitObjectArgNullGuard(ctx, fctx, objLocal);
    let priorExistingFlags;
    if (propName) {
      const varName = ts10.isIdentifier(objArg) ? objArg.text : void 0;
      if (varName) {
        const isAccessor = !!(getNode || setNode);
        const newFlags = computeDescriptorFlags(descWritable, descEnumerable, descConfigurable, isAccessor);
        const key = `${varName}:${propName}`;
        priorExistingFlags = ctx.definedPropertyFlags.get(key);
        if (ctx.nonExtensibleVars.has(varName) && !ctx.definedPropertyFlags.has(key)) {
          emitThrowString2(ctx, fctx, "TypeError: Cannot define property, object is not extensible");
        }
        const existingFlags = ctx.definedPropertyFlags.get(key);
        if (existingFlags !== void 0) {
          const isExistingConfigurable = !!(existingFlags & PROP_FLAG_CONFIGURABLE);
          if (!isExistingConfigurable) {
            if (newFlags & PROP_FLAG_CONFIGURABLE) {
              emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
            }
            const existingEnumerable = existingFlags & PROP_FLAG_ENUMERABLE;
            const newEnumerable = newFlags & PROP_FLAG_ENUMERABLE;
            if (existingEnumerable !== newEnumerable) {
              emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
            }
            if (!(existingFlags & PROP_FLAG_ACCESSOR) && !isAccessor) {
              if (!(existingFlags & PROP_FLAG_WRITABLE)) {
                if (newFlags & PROP_FLAG_WRITABLE) {
                  emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
                }
              }
            }
            if (isAccessor && !(existingFlags & PROP_FLAG_ACCESSOR)) {
              emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
            }
            if (!isAccessor && existingFlags & PROP_FLAG_ACCESSOR) {
              emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
            }
          }
        }
        ctx.definedPropertyFlags.set(key, newFlags);
        if (structTypeIdx !== void 0 && fields) {
          const userFieldsList = fields
            .map((f, idx) => ({ field: f, fieldIdx: idx }))
            .filter((e) => !e.field.name.startsWith("__"));
          const userIdx = userFieldsList.findIndex((e) => e.field.name === propName);
          if (userIdx >= 0) {
            const flagsArr = ctx.shapePropFlags.get(structTypeIdx);
            if (flagsArr && userIdx < flagsArr.length) {
              flagsArr[userIdx] = newFlags & 7;
            }
          }
        }
      }
    }
    for (const prop of descArg.properties) {
      if (ts10.isPropertyAssignment(prop) && ts10.isIdentifier(prop.name) && prop.name.text === "value") continue;
      if (ts10.isPropertyAssignment(prop)) {
        const sideType = compileExpression(ctx, fctx, prop.initializer);
        if (sideType) fctx.body.push({ op: "drop" });
      }
    }
    const needsValueCompare =
      priorExistingFlags !== void 0 &&
      !(priorExistingFlags & PROP_FLAG_CONFIGURABLE) &&
      !(priorExistingFlags & PROP_FLAG_WRITABLE) &&
      !(priorExistingFlags & PROP_FLAG_ACCESSOR);
    const fieldType = fields[fieldIdx].type;
    if (needsValueCompare) {
      const oldValLocal = allocLocal(fctx, `__defprop_oldval_${fctx.locals.length}`, fieldType);
      fctx.body.push({ op: "local.get", index: objLocal });
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
      fctx.body.push({ op: "local.set", index: oldValLocal });
      const newValLocal = allocLocal(fctx, `__defprop_newval_${fctx.locals.length}`, fieldType);
      const valType = compileExpression(ctx, fctx, valueExpr, fieldType);
      if (!valType) {
        fctx.body.push({ op: "local.get", index: objLocal });
        return objType;
      }
      if (valType.kind !== fieldType.kind) {
        coerceType(ctx, fctx, valType, fieldType);
      }
      fctx.body.push({ op: "local.set", index: newValLocal });
      const tagIdx = ensureExnTag(ctx);
      const errMsg = "TypeError: Cannot redefine property";
      addStringConstantGlobal(ctx, errMsg);
      const errMsgGlobal = ctx.stringGlobalMap.get(errMsg);
      if (fieldType.kind === "f64") {
        const compareBody = [
          { op: "global.get", index: errMsgGlobal },
          { op: "throw", tagIdx },
        ];
        fctx.body.push({ op: "local.get", index: oldValLocal });
        fctx.body.push({ op: "local.get", index: newValLocal });
        fctx.body.push({ op: "f64.ne" });
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: compareBody,
        });
      } else if (fieldType.kind === "i32") {
        const compareBody = [
          { op: "global.get", index: errMsgGlobal },
          { op: "throw", tagIdx },
        ];
        fctx.body.push({ op: "local.get", index: oldValLocal });
        fctx.body.push({ op: "local.get", index: newValLocal });
        fctx.body.push({ op: "i32.ne" });
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: compareBody,
        });
      }
      fctx.body.push({ op: "local.get", index: objLocal });
      fctx.body.push({ op: "local.get", index: newValLocal });
      fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
    } else {
      fctx.body.push({ op: "local.get", index: objLocal });
      const valType = compileExpression(ctx, fctx, valueExpr, fieldType);
      if (!valType) {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "local.get", index: objLocal });
        return objType;
      }
      if (valType.kind !== fieldType.kind) {
        coerceType(ctx, fctx, valType, fieldType);
      }
      fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
    }
    fctx.body.push({ op: "local.get", index: objLocal });
    return objType;
  } else if (valueExpr) {
    return emitExternDefinePropertyValue(
      ctx,
      fctx,
      objArg,
      propArg,
      descArg,
      valueExpr,
      descWritable,
      descEnumerable,
      descConfigurable,
    );
  } else {
    return emitExternDefinePropertyNoValue(
      ctx,
      fctx,
      objArg,
      propArg,
      descArg,
      descWritable,
      descEnumerable,
      descConfigurable,
      getNode,
      setNode,
    );
  }
}
function computeRuntimeFlags(descWritable, descEnumerable, descConfigurable, hasValue) {
  let flags = 0;
  if (descWritable !== void 0) {
    flags |= 1 << 3;
    if (descWritable) flags |= 1;
  }
  if (descEnumerable !== void 0) {
    flags |= 1 << 4;
    if (descEnumerable) flags |= 1 << 1;
  }
  if (descConfigurable !== void 0) {
    flags |= 1 << 5;
    if (descConfigurable) flags |= 1 << 2;
  }
  if (hasValue) flags |= 1 << 7;
  return flags;
}
function emitExternDefinePropertyValue(
  ctx,
  fctx,
  objArg,
  propArg,
  descArg,
  valueExpr,
  descWritable,
  descEnumerable,
  descConfigurable,
) {
  const objType = compileExpression(ctx, fctx, objArg, { kind: "externref" });
  if (!objType) return null;
  if (objType.kind !== "externref") {
    coerceType(ctx, fctx, objType, { kind: "externref" });
  }
  const objLocal = allocLocal(fctx, `__defprop_obj_${fctx.locals.length}`, { kind: "externref" });
  fctx.body.push({ op: "local.set", index: objLocal });
  emitObjectArgNullGuard(ctx, fctx, objLocal);
  const propType = compileExpression(ctx, fctx, propArg, { kind: "externref" });
  if (!propType) {
    fctx.body.push({ op: "local.get", index: objLocal });
    return { kind: "externref" };
  }
  if (propType.kind !== "externref") {
    coerceType(ctx, fctx, propType, { kind: "externref" });
  }
  const propLocal = allocLocal(fctx, `__defprop_key_${fctx.locals.length}`, { kind: "externref" });
  fctx.body.push({ op: "local.set", index: propLocal });
  const valType = compileExpression(ctx, fctx, valueExpr, { kind: "externref" });
  if (!valType) {
    fctx.body.push({ op: "local.get", index: objLocal });
    return { kind: "externref" };
  }
  if (valType.kind !== "externref") {
    coerceType(ctx, fctx, valType, { kind: "externref" });
  }
  const valLocal = allocLocal(fctx, `__defprop_val_${fctx.locals.length}`, { kind: "externref" });
  fctx.body.push({ op: "local.set", index: valLocal });
  if (ts10.isObjectLiteralExpression(descArg)) {
    for (const prop of descArg.properties) {
      if (ts10.isPropertyAssignment(prop) && ts10.isIdentifier(prop.name) && prop.name.text === "value") continue;
      if (
        ts10.isPropertyAssignment(prop) &&
        ts10.isIdentifier(prop.name) &&
        (prop.name.text === "writable" || prop.name.text === "enumerable" || prop.name.text === "configurable")
      )
        continue;
      if (ts10.isPropertyAssignment(prop)) {
        const sideType = compileExpression(ctx, fctx, prop.initializer);
        if (sideType) fctx.body.push({ op: "drop" });
      }
    }
  }
  const runtimeFlags = computeRuntimeFlags(descWritable, descEnumerable, descConfigurable, true);
  fctx.body.push({ op: "local.get", index: objLocal });
  fctx.body.push({ op: "local.get", index: propLocal });
  fctx.body.push({ op: "local.get", index: valLocal });
  fctx.body.push({ op: "f64.const", value: runtimeFlags });
  const funcIdx = ensureLateImport2(
    ctx,
    "__defineProperty_value",
    [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }, { kind: "f64" }],
    [{ kind: "externref" }],
  );
  flushLateImportShifts2(ctx, fctx);
  if (funcIdx !== void 0) {
    fctx.body.push({ op: "call", funcIdx });
  }
  return { kind: "externref" };
}
function emitExternDefinePropertyNoValue(
  ctx,
  fctx,
  objArg,
  propArg,
  descArg,
  descWritable,
  descEnumerable,
  descConfigurable,
  getNode,
  setNode,
) {
  const objType = compileExpression(ctx, fctx, objArg);
  if (!objType) return null;
  const objLocal = allocLocal(fctx, `__defprop_obj_${fctx.locals.length}`, objType);
  fctx.body.push({ op: "local.set", index: objLocal });
  if (objType.kind === "externref" || objType.kind === "ref_null") {
    emitObjectArgNullGuard(ctx, fctx, objLocal);
  }
  const propType = compileExpression(ctx, fctx, propArg, { kind: "externref" });
  let propLocal;
  if (propType) {
    if (propType.kind !== "externref") {
      coerceType(ctx, fctx, propType, { kind: "externref" });
    }
    propLocal = allocLocal(fctx, `__defprop_key_${fctx.locals.length}`, { kind: "externref" });
    fctx.body.push({ op: "local.set", index: propLocal });
  }
  const descType = compileExpression(ctx, fctx, descArg);
  if (descType) fctx.body.push({ op: "drop" });
  const objTsType = ctx.checker.getTypeAtLocation(objArg);
  const _structName =
    resolveStructName(ctx, objTsType) ||
    (ts10.isIdentifier(objArg) ? ctx.widenedVarStructMap.get(objArg.text) : void 0);
  const _propName = ts10.isStringLiteral(propArg) ? propArg.text : void 0;
  const _structTypeIdx = _structName ? ctx.structMap.get(_structName) : void 0;
  const _fields = _structName ? ctx.structFields.get(_structName) : void 0;
  const _fieldIdx = _fields && _propName ? _fields.findIndex((f) => f.name === _propName) : -1;
  const isKnownStructField = _structTypeIdx !== void 0 && _fields !== void 0 && _fieldIdx >= 0;
  if (!isKnownStructField && propLocal !== void 0) {
    const propName2 = ts10.isStringLiteral(propArg) ? propArg.text : void 0;
    if (propName2 && ts10.isObjectLiteralExpression(descArg)) {
      const isAccessor = !!(getNode || setNode);
      const newFlags = computeDescriptorFlags(descWritable, descEnumerable, descConfigurable, isAccessor);
      const varName = ts10.isIdentifier(objArg) ? objArg.text : void 0;
      if (varName) {
        const key = `${varName}:${propName2}`;
        ctx.definedPropertyFlags.set(key, newFlags);
      }
    }
    const runtimeFlags = computeRuntimeFlags(descWritable, descEnumerable, descConfigurable, false);
    fctx.body.push({ op: "local.get", index: objLocal });
    if (objType.kind !== "externref") {
      coerceType(ctx, fctx, objType, { kind: "externref" });
    }
    fctx.body.push({ op: "local.get", index: propLocal });
    fctx.body.push({ op: "ref.null.extern" });
    fctx.body.push({ op: "f64.const", value: runtimeFlags });
    const funcIdx = ensureLateImport2(
      ctx,
      "__defineProperty_value",
      [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }, { kind: "f64" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
    }
    return { kind: "externref" };
  }
  const propName = ts10.isStringLiteral(propArg) ? propArg.text : void 0;
  if (propName && ts10.isObjectLiteralExpression(descArg)) {
    const isAccessor = !!(getNode || setNode);
    const newFlags = computeDescriptorFlags(descWritable, descEnumerable, descConfigurable, isAccessor);
    const varName = ts10.isIdentifier(objArg) ? objArg.text : void 0;
    if (varName) {
      const key = `${varName}:${propName}`;
      if (ctx.nonExtensibleVars.has(varName) && !ctx.definedPropertyFlags.has(key)) {
        emitThrowString2(ctx, fctx, "TypeError: Cannot define property, object is not extensible");
      }
      const existingFlags = ctx.definedPropertyFlags.get(key);
      if (existingFlags !== void 0) {
        const isExistingConfigurable = !!(existingFlags & PROP_FLAG_CONFIGURABLE);
        if (!isExistingConfigurable) {
          if (newFlags & PROP_FLAG_CONFIGURABLE) {
            emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
          }
          if ((existingFlags & PROP_FLAG_ENUMERABLE) !== (newFlags & PROP_FLAG_ENUMERABLE)) {
            emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
          }
          if (!(existingFlags & PROP_FLAG_ACCESSOR) && !isAccessor) {
            if (!(existingFlags & PROP_FLAG_WRITABLE)) {
              if (newFlags & PROP_FLAG_WRITABLE) {
                emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
              }
            }
          }
          if (isAccessor && !(existingFlags & PROP_FLAG_ACCESSOR)) {
            emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
          }
          if (!isAccessor && existingFlags & PROP_FLAG_ACCESSOR) {
            emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
          }
        }
      }
      ctx.definedPropertyFlags.set(key, newFlags);
    }
  }
  fctx.body.push({ op: "local.get", index: objLocal });
  return objType;
}
function compileObjectDefineProperties(ctx, fctx, expr) {
  const objArg = expr.arguments[0];
  const descsArg = expr.arguments[1];
  if (emitNonObjectArgGuard(ctx, fctx, objArg, "Object.defineProperties")) {
    fctx.body.push({ op: "unreachable" });
    return { kind: "externref" };
  }
  if (ts10.isObjectLiteralExpression(descsArg)) {
    const objType2 = compileExpression(ctx, fctx, objArg);
    if (!objType2) return null;
    const objLocal = allocLocal(fctx, `__defprops_obj_${fctx.locals.length}`, objType2);
    fctx.body.push({ op: "local.set", index: objLocal });
    for (const prop of descsArg.properties) {
      if (!ts10.isPropertyAssignment(prop)) continue;
      const propName = ts10.isIdentifier(prop.name)
        ? prop.name.text
        : ts10.isStringLiteral(prop.name)
          ? prop.name.text
          : void 0;
      if (propName === void 0) continue;
      const syntheticPropAccess = ts10.factory.createPropertyAccessExpression(
        ts10.factory.createIdentifier("Object"),
        "defineProperty",
      );
      const syntheticCall = ts10.factory.createCallExpression(syntheticPropAccess, void 0, [
        ts10.factory.createIdentifier(`__defprops_obj_placeholder_${objLocal}`),
        ts10.factory.createStringLiteral(propName),
        prop.initializer,
      ]);
      ts10.setTextRange(syntheticCall, expr);
      syntheticCall.parent = expr.parent;
      const descExpr = prop.initializer;
      let valueExpr;
      let descWritable;
      let descEnumerable;
      let descConfigurable;
      if (ts10.isObjectLiteralExpression(descExpr)) {
        for (const dp of descExpr.properties) {
          if (ts10.isPropertyAssignment(dp) && ts10.isIdentifier(dp.name)) {
            if (dp.name.text === "value") valueExpr = dp.initializer;
            if (dp.name.text === "writable") {
              if (dp.initializer.kind === ts10.SyntaxKind.TrueKeyword) descWritable = true;
              else if (dp.initializer.kind === ts10.SyntaxKind.FalseKeyword) descWritable = false;
            }
            if (dp.name.text === "enumerable") {
              if (dp.initializer.kind === ts10.SyntaxKind.TrueKeyword) descEnumerable = true;
              else if (dp.initializer.kind === ts10.SyntaxKind.FalseKeyword) descEnumerable = false;
            }
            if (dp.name.text === "configurable") {
              if (dp.initializer.kind === ts10.SyntaxKind.TrueKeyword) descConfigurable = true;
              else if (dp.initializer.kind === ts10.SyntaxKind.FalseKeyword) descConfigurable = false;
            }
          }
        }
      }
      const objTsType = ctx.checker.getTypeAtLocation(objArg);
      const structName =
        resolveStructName(ctx, objTsType) ||
        (ts10.isIdentifier(objArg) ? ctx.widenedVarStructMap.get(objArg.text) : void 0);
      const structTypeIdx = structName ? ctx.structMap.get(structName) : void 0;
      const fields = structName ? ctx.structFields.get(structName) : void 0;
      const fieldIdx = fields && propName ? fields.findIndex((f) => f.name === propName) : -1;
      if (structTypeIdx !== void 0 && fields && fieldIdx >= 0 && valueExpr) {
        const fieldType = fields[fieldIdx].type;
        let priorExistingFlags;
        if (ts10.isIdentifier(objArg)) {
          const isAccessor = false;
          const newFlags = computeDescriptorFlags(descWritable, descEnumerable, descConfigurable, isAccessor);
          const key = `${objArg.text}:${propName}`;
          priorExistingFlags = ctx.definedPropertyFlags.get(key);
          const existingFlags = ctx.definedPropertyFlags.get(key);
          if (existingFlags !== void 0) {
            const isExistingConfigurable = !!(existingFlags & PROP_FLAG_CONFIGURABLE);
            if (!isExistingConfigurable) {
              if (newFlags & PROP_FLAG_CONFIGURABLE) {
                emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
              }
              const existingEnumerable = existingFlags & PROP_FLAG_ENUMERABLE;
              const newEnumerable = newFlags & PROP_FLAG_ENUMERABLE;
              if (existingEnumerable !== newEnumerable) {
                emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
              }
              if (!(existingFlags & PROP_FLAG_ACCESSOR) && !isAccessor) {
                if (!(existingFlags & PROP_FLAG_WRITABLE)) {
                  if (newFlags & PROP_FLAG_WRITABLE) {
                    emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
                  }
                }
              }
              if (isAccessor && !(existingFlags & PROP_FLAG_ACCESSOR)) {
                emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
              }
              if (!isAccessor && existingFlags & PROP_FLAG_ACCESSOR) {
                emitThrowString2(ctx, fctx, "TypeError: Cannot redefine property");
              }
            }
          }
        }
        const needsValueCompare =
          priorExistingFlags !== void 0 &&
          !(priorExistingFlags & PROP_FLAG_CONFIGURABLE) &&
          !(priorExistingFlags & PROP_FLAG_WRITABLE) &&
          !(priorExistingFlags & PROP_FLAG_ACCESSOR);
        fctx.body.push({ op: "local.get", index: objLocal });
        let needsGuard = false;
        if (objType2.kind === "externref") {
          fctx.body.push({ op: "any.convert_extern" });
          needsGuard = true;
        } else if (
          (objType2.kind === "ref_null" || objType2.kind === "ref") &&
          "typeIdx" in objType2 &&
          objType2.typeIdx !== structTypeIdx
        ) {
          needsGuard = true;
        }
        if (needsValueCompare) {
          if (needsGuard) {
            const defpTmp = allocLocal(fctx, `__defp_tmp_${fctx.locals.length}`, { kind: "anyref" });
            fctx.body.push({ op: "local.set", index: defpTmp });
            const oldValLocal = allocLocal(fctx, `__defps_oldval_${fctx.locals.length}`, fieldType);
            fctx.body.push({ op: "local.get", index: defpTmp });
            fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx });
            if (fieldType.kind === "f64") {
              fctx.body.push({
                op: "if",
                blockType: { kind: "val", type: { kind: "f64" } },
                then: [
                  { op: "local.get", index: defpTmp },
                  { op: "ref.cast", typeIdx: structTypeIdx },
                  { op: "struct.get", typeIdx: structTypeIdx, fieldIdx },
                ],
                else: [{ op: "f64.const", value: 0 }],
              });
            } else if (fieldType.kind === "i32") {
              fctx.body.push({
                op: "if",
                blockType: { kind: "val", type: { kind: "i32" } },
                then: [
                  { op: "local.get", index: defpTmp },
                  { op: "ref.cast", typeIdx: structTypeIdx },
                  { op: "struct.get", typeIdx: structTypeIdx, fieldIdx },
                ],
                else: [{ op: "i32.const", value: 0 }],
              });
            }
            fctx.body.push({ op: "local.set", index: oldValLocal });
            const valType = compileExpression(ctx, fctx, valueExpr, fieldType);
            if (valType) {
              const newValLocal = allocLocal(fctx, `__defps_newval_${fctx.locals.length}`, fieldType);
              if (valType.kind !== fieldType.kind) {
                coerceType(ctx, fctx, valType, fieldType);
              }
              fctx.body.push({ op: "local.set", index: newValLocal });
              const tagIdx = ensureExnTag(ctx);
              const errMsg = "TypeError: Cannot redefine property";
              addStringConstantGlobal(ctx, errMsg);
              const errMsgGlobal = ctx.stringGlobalMap.get(errMsg);
              if (fieldType.kind === "f64") {
                fctx.body.push({ op: "local.get", index: oldValLocal });
                fctx.body.push({ op: "local.get", index: newValLocal });
                fctx.body.push({ op: "f64.ne" });
                fctx.body.push({
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "global.get", index: errMsgGlobal },
                    { op: "throw", tagIdx },
                  ],
                });
              } else if (fieldType.kind === "i32") {
                fctx.body.push({ op: "local.get", index: oldValLocal });
                fctx.body.push({ op: "local.get", index: newValLocal });
                fctx.body.push({ op: "i32.ne" });
                fctx.body.push({
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "global.get", index: errMsgGlobal },
                    { op: "throw", tagIdx },
                  ],
                });
              }
              fctx.body.push({ op: "local.get", index: defpTmp });
              fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx });
              fctx.body.push({
                op: "if",
                blockType: { kind: "empty" },
                then: [
                  { op: "local.get", index: defpTmp },
                  { op: "ref.cast", typeIdx: structTypeIdx },
                  { op: "local.get", index: newValLocal },
                  { op: "struct.set", typeIdx: structTypeIdx, fieldIdx },
                ],
                else: [],
              });
            }
          } else {
            const oldValLocal = allocLocal(fctx, `__defps_oldval_${fctx.locals.length}`, fieldType);
            fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
            fctx.body.push({ op: "local.set", index: oldValLocal });
            const newValLocal = allocLocal(fctx, `__defps_newval_${fctx.locals.length}`, fieldType);
            const valType = compileExpression(ctx, fctx, valueExpr, fieldType);
            if (valType) {
              if (valType.kind !== fieldType.kind) {
                coerceType(ctx, fctx, valType, fieldType);
              }
              fctx.body.push({ op: "local.set", index: newValLocal });
              const tagIdx = ensureExnTag(ctx);
              const errMsg = "TypeError: Cannot redefine property";
              addStringConstantGlobal(ctx, errMsg);
              const errMsgGlobal = ctx.stringGlobalMap.get(errMsg);
              if (fieldType.kind === "f64") {
                fctx.body.push({ op: "local.get", index: oldValLocal });
                fctx.body.push({ op: "local.get", index: newValLocal });
                fctx.body.push({ op: "f64.ne" });
                fctx.body.push({
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "global.get", index: errMsgGlobal },
                    { op: "throw", tagIdx },
                  ],
                });
              } else if (fieldType.kind === "i32") {
                fctx.body.push({ op: "local.get", index: oldValLocal });
                fctx.body.push({ op: "local.get", index: newValLocal });
                fctx.body.push({ op: "i32.ne" });
                fctx.body.push({
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "global.get", index: errMsgGlobal },
                    { op: "throw", tagIdx },
                  ],
                });
              }
              fctx.body.push({ op: "local.get", index: objLocal });
              fctx.body.push({ op: "local.get", index: newValLocal });
              fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
            } else {
              fctx.body.push({ op: "drop" });
            }
          }
        } else if (needsGuard) {
          const defpTmp = allocLocal(fctx, `__defp_tmp_${fctx.locals.length}`, { kind: "anyref" });
          fctx.body.push({ op: "local.set", index: defpTmp });
          const valType = compileExpression(ctx, fctx, valueExpr, fieldType);
          if (valType) {
            const valLocal = allocLocal(fctx, `__defp_val_${fctx.locals.length}`, fieldType);
            if (valType.kind !== fieldType.kind) {
              coerceType(ctx, fctx, valType, fieldType);
            }
            fctx.body.push({ op: "local.set", index: valLocal });
            fctx.body.push({ op: "local.get", index: defpTmp });
            fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx });
            fctx.body.push({
              op: "if",
              blockType: { kind: "empty" },
              then: [
                { op: "local.get", index: defpTmp },
                { op: "ref.cast", typeIdx: structTypeIdx },
                { op: "local.get", index: valLocal },
                { op: "struct.set", typeIdx: structTypeIdx, fieldIdx },
              ],
              else: [],
            });
          }
        } else {
          const valType = compileExpression(ctx, fctx, valueExpr, fieldType);
          if (valType) {
            if (valType.kind !== fieldType.kind) {
              coerceType(ctx, fctx, valType, fieldType);
            }
            fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
          } else {
            fctx.body.push({ op: "drop" });
          }
        }
        if (ts10.isIdentifier(objArg)) {
          const isAccessor = false;
          const newFlags = computeDescriptorFlags(descWritable, descEnumerable, descConfigurable, isAccessor);
          const key = `${objArg.text}:${propName}`;
          ctx.definedPropertyFlags.set(key, newFlags);
        }
        const userFields = fields
          .map((f, idx) => ({ field: f, fieldIdx: idx }))
          .filter((e) => !e.field.name.startsWith("__"));
        const userFieldIdx = userFields.findIndex((e) => e.fieldIdx === fieldIdx);
        if (userFieldIdx >= 0) {
          const flagsArr = ctx.shapePropFlags.get(structTypeIdx);
          if (flagsArr && userFieldIdx < flagsArr.length) {
            const isAccessor = false;
            const newFlags = computeDescriptorFlags(descWritable, descEnumerable, descConfigurable, isAccessor);
            flagsArr[userFieldIdx] = newFlags & 7;
          }
        }
        continue;
      }
      if (objType2.kind !== "externref") {
        fctx.body.push({ op: "local.get", index: objLocal });
        coerceType(ctx, fctx, objType2, { kind: "externref" });
      } else {
        fctx.body.push({ op: "local.get", index: objLocal });
      }
      const objExtLocal = allocLocal(fctx, `__defprops_ext_${fctx.locals.length}`, { kind: "externref" });
      fctx.body.push({ op: "local.set", index: objExtLocal });
      fctx.body.push({ op: "local.get", index: objExtLocal });
      compileExpression(ctx, fctx, ts10.factory.createStringLiteral(propName), { kind: "externref" });
      if (valueExpr) {
        const vt = compileExpression(ctx, fctx, valueExpr, { kind: "externref" });
        if (vt && vt.kind !== "externref") {
          coerceType(ctx, fctx, vt, { kind: "externref" });
        } else if (!vt) {
          fctx.body.push({ op: "ref.null.extern" });
        }
      } else {
        fctx.body.push({ op: "ref.null.extern" });
      }
      const runtimeFlags = computeRuntimeFlags(descWritable, descEnumerable, descConfigurable, !!valueExpr);
      fctx.body.push({ op: "f64.const", value: runtimeFlags });
      const funcIdx2 = ensureLateImport2(
        ctx,
        "__defineProperty_value",
        [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }, { kind: "f64" }],
        [{ kind: "externref" }],
      );
      flushLateImportShifts2(ctx, fctx);
      if (funcIdx2 !== void 0) {
        fctx.body.push({ op: "call", funcIdx: funcIdx2 });
        fctx.body.push({ op: "drop" });
      }
      if (ts10.isIdentifier(objArg)) {
        const isAccessor = false;
        const newFlags = computeDescriptorFlags(descWritable, descEnumerable, descConfigurable, isAccessor);
        const key = `${objArg.text}:${propName}`;
        ctx.definedPropertyFlags.set(key, newFlags);
      }
    }
    fctx.body.push({ op: "local.get", index: objLocal });
    return objType2;
  }
  const objType = compileExpression(ctx, fctx, objArg, { kind: "externref" });
  if (!objType) return null;
  if (objType.kind !== "externref") {
    coerceType(ctx, fctx, objType, { kind: "externref" });
  }
  const descsType = compileExpression(ctx, fctx, descsArg, { kind: "externref" });
  if (!descsType) {
    return { kind: "externref" };
  }
  if (descsType.kind !== "externref") {
    coerceType(ctx, fctx, descsType, { kind: "externref" });
  }
  const funcIdx = ensureLateImport2(
    ctx,
    "__defineProperties",
    [{ kind: "externref" }, { kind: "externref" }],
    [{ kind: "externref" }],
  );
  flushLateImportShifts2(ctx, fctx);
  if (funcIdx !== void 0) {
    fctx.body.push({ op: "call", funcIdx });
  }
  return { kind: "externref" };
}
function compileObjectKeysOrValues(ctx, fctx, method, expr) {
  const arg = expr.arguments[0];
  const argType = ctx.checker.getTypeAtLocation(arg);
  const structName = resolveStructName(ctx, argType);
  if (!structName) {
    const isAnyOrUnknown = (argType.flags & (ts10.TypeFlags.Any | ts10.TypeFlags.Unknown)) !== 0;
    const tsProps = argType.getProperties?.();
    if (!isAnyOrUnknown && tsProps && tsProps.length === 0) {
      const argResult3 = compileExpression(ctx, fctx, arg);
      if (argResult3) {
        fctx.body.push({ op: "drop" });
      }
      const elemKind2 = "externref";
      const vecTypeIdx2 = getOrRegisterVecType(ctx, elemKind2);
      const arrTypeIdx2 = getArrTypeIdxFromVec(ctx, vecTypeIdx2);
      if (arrTypeIdx2 < 0) return null;
      fctx.body.push({ op: "array.new_fixed", typeIdx: arrTypeIdx2, length: 0 });
      const tmpData2 = allocLocal(fctx, `__obj_${method}_empty_data_${fctx.locals.length}`, {
        kind: "ref",
        typeIdx: arrTypeIdx2,
      });
      fctx.body.push({ op: "local.set", index: tmpData2 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "local.get", index: tmpData2 });
      fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx2 });
      return { kind: "ref_null", typeIdx: vecTypeIdx2 };
    }
    const argResult2 = compileExpression(ctx, fctx, arg);
    if (!argResult2) return null;
    if (argResult2.kind !== "externref") {
      coerceType(ctx, fctx, argResult2, { kind: "externref" });
    }
    const importName = `__object_${method}`;
    const funcIdx = ensureLateImport2(ctx, importName, [{ kind: "externref" }], [{ kind: "externref" }]);
    flushLateImportShifts2(ctx, fctx);
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "externref" };
    }
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  const structTypeIdx = ctx.structMap.get(structName);
  const fields = ctx.structFields.get(structName);
  if (structTypeIdx === void 0 || !fields) {
    ctx.errors.push({
      message: `Object.${method}(): unknown struct "${structName}"`,
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const userFields = fields
    .map((f, idx) => ({ field: f, fieldIdx: idx }))
    .filter((e) => !e.field.name.startsWith("__"));
  if (method === "keys") {
    const elemKind2 = "externref";
    const vecTypeIdx2 = getOrRegisterVecType(ctx, elemKind2);
    const arrTypeIdx2 = getArrTypeIdxFromVec(ctx, vecTypeIdx2);
    if (arrTypeIdx2 < 0) {
      ctx.errors.push({
        message: `Object.keys(): cannot resolve array type for string[]`,
        line: getLine(expr),
        column: getCol(expr),
      });
      return null;
    }
    for (const entry of userFields) {
      if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
        compileNativeStringLiteral(ctx, fctx, entry.field.name);
        fctx.body.push({ op: "extern.convert_any" });
      } else {
        const globalIdx = ctx.stringGlobalMap.get(entry.field.name);
        if (globalIdx !== void 0) {
          fctx.body.push({ op: "global.get", index: globalIdx });
        } else {
          const importName = ctx.stringLiteralMap.get(entry.field.name);
          if (importName) {
            const funcIdx = ctx.funcMap.get(importName);
            if (funcIdx !== void 0) {
              fctx.body.push({ op: "call", funcIdx });
            }
          }
        }
      }
    }
    const count2 = userFields.length;
    fctx.body.push({ op: "array.new_fixed", typeIdx: arrTypeIdx2, length: count2 });
    const tmpData2 = allocLocal(fctx, `__obj_keys_data_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx2 });
    fctx.body.push({ op: "local.set", index: tmpData2 });
    fctx.body.push({ op: "i32.const", value: count2 });
    fctx.body.push({ op: "local.get", index: tmpData2 });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx2 });
    return { kind: "ref_null", typeIdx: vecTypeIdx2 };
  }
  if (method === "entries") {
    const argResult2 = compileExpression(ctx, fctx, arg);
    if (!argResult2) return null;
    const objLocal2 = allocLocal(fctx, `__obj_entries_src_${fctx.locals.length}`, {
      kind: "ref",
      typeIdx: structTypeIdx,
    });
    fctx.body.push({ op: "local.set", index: objLocal2 });
    emitObjectArgNullGuard(ctx, fctx, objLocal2);
    const sig = ctx.checker.getResolvedSignature(expr);
    const retType = sig ? ctx.checker.getReturnTypeOfSignature(sig) : void 0;
    const resolvedRet = retType ? resolveWasmType(ctx, retType) : void 0;
    let outerVecTypeIdx;
    let outerArrTypeIdx;
    let entryTupleTypeIdx;
    if (resolvedRet && (resolvedRet.kind === "ref" || resolvedRet.kind === "ref_null") && "typeIdx" in resolvedRet) {
      outerVecTypeIdx = resolvedRet.typeIdx;
      outerArrTypeIdx = getArrTypeIdxFromVec(ctx, outerVecTypeIdx);
      const arrTypeDef = ctx.mod.types[outerArrTypeIdx];
      if (
        arrTypeDef &&
        arrTypeDef.kind === "array" &&
        arrTypeDef.element &&
        (arrTypeDef.element.kind === "ref" || arrTypeDef.element.kind === "ref_null")
      ) {
        entryTupleTypeIdx = arrTypeDef.element.typeIdx;
      } else {
        entryTupleTypeIdx = getOrRegisterTupleType(ctx, [{ kind: "externref" }, { kind: "externref" }]);
      }
    } else {
      entryTupleTypeIdx = getOrRegisterTupleType(ctx, [{ kind: "externref" }, { kind: "externref" }]);
      const entryElemKind = `ref_${entryTupleTypeIdx}`;
      outerVecTypeIdx = getOrRegisterVecType(ctx, entryElemKind, { kind: "ref", typeIdx: entryTupleTypeIdx });
      outerArrTypeIdx = getArrTypeIdxFromVec(ctx, outerVecTypeIdx);
    }
    if (outerArrTypeIdx < 0) {
      ctx.errors.push({
        message: `Object.entries(): cannot resolve outer array type`,
        line: getLine(expr),
        column: getCol(expr),
      });
      return null;
    }
    const tupleTypeDef = ctx.mod.types[entryTupleTypeIdx];
    const tupleFields = tupleTypeDef && tupleTypeDef.kind === "struct" ? tupleTypeDef.fields : void 0;
    const valueFieldType = tupleFields?.[1]?.type;
    addUnionImports(ctx);
    for (const entry of userFields) {
      if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
        compileNativeStringLiteral(ctx, fctx, entry.field.name);
        if (tupleFields && tupleFields[0]?.type?.kind === "externref") {
          fctx.body.push({ op: "extern.convert_any" });
        }
      } else {
        const globalIdx = ctx.stringGlobalMap.get(entry.field.name);
        if (globalIdx !== void 0) {
          fctx.body.push({ op: "global.get", index: globalIdx });
        } else {
          const importName = ctx.stringLiteralMap.get(entry.field.name);
          if (importName) {
            const funcIdx = ctx.funcMap.get(importName);
            if (funcIdx !== void 0) {
              fctx.body.push({ op: "call", funcIdx });
            }
          }
        }
      }
      fctx.body.push({ op: "local.get", index: objLocal2 });
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: entry.fieldIdx });
      const fieldKind = entry.field.type.kind;
      const targetKind = valueFieldType?.kind ?? "externref";
      if (targetKind === "externref") {
        if (fieldKind === "f64") {
          const boxIdx = ctx.funcMap.get("__box_number");
          if (boxIdx !== void 0) fctx.body.push({ op: "call", funcIdx: boxIdx });
        } else if (fieldKind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
          const boxIdx = ctx.funcMap.get("__box_number");
          if (boxIdx !== void 0) fctx.body.push({ op: "call", funcIdx: boxIdx });
        } else if (fieldKind === "ref" || fieldKind === "ref_null") {
          fctx.body.push({ op: "extern.convert_any" });
        }
      }
      fctx.body.push({ op: "struct.new", typeIdx: entryTupleTypeIdx });
    }
    const count2 = userFields.length;
    fctx.body.push({ op: "array.new_fixed", typeIdx: outerArrTypeIdx, length: count2 });
    const outerData = allocLocal(fctx, `__obj_entries_data_${fctx.locals.length}`, {
      kind: "ref",
      typeIdx: outerArrTypeIdx,
    });
    fctx.body.push({ op: "local.set", index: outerData });
    fctx.body.push({ op: "i32.const", value: count2 });
    fctx.body.push({ op: "local.get", index: outerData });
    fctx.body.push({ op: "struct.new", typeIdx: outerVecTypeIdx });
    return { kind: "ref_null", typeIdx: outerVecTypeIdx };
  }
  const argResult = compileExpression(ctx, fctx, arg);
  if (!argResult) return null;
  const objLocal = allocLocal(fctx, `__obj_vals_src_${fctx.locals.length}`, { kind: "ref", typeIdx: structTypeIdx });
  fctx.body.push({ op: "local.set", index: objLocal });
  emitObjectArgNullGuard(ctx, fctx, objLocal);
  const elemKind = "externref";
  const vecTypeIdx = getOrRegisterVecType(ctx, elemKind);
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
  if (arrTypeIdx < 0) {
    ctx.errors.push({
      message: `Object.values(): cannot resolve array type for values[]`,
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  addUnionImports(ctx);
  for (const entry of userFields) {
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: entry.fieldIdx });
    if (entry.field.type.kind === "f64") {
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      }
    } else if (entry.field.type.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      }
    } else if (entry.field.type.kind === "ref" || entry.field.type.kind === "ref_null") {
      fctx.body.push({ op: "extern.convert_any" });
    }
  }
  const count = userFields.length;
  fctx.body.push({ op: "array.new_fixed", typeIdx: arrTypeIdx, length: count });
  const tmpData = allocLocal(fctx, `__obj_vals_data_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
  fctx.body.push({ op: "local.set", index: tmpData });
  fctx.body.push({ op: "i32.const", value: count });
  fctx.body.push({ op: "local.get", index: tmpData });
  fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
  return { kind: "ref_null", typeIdx: vecTypeIdx };
}
function compilePropertyIntrospection(ctx, fctx, propAccess, expr) {
  const receiverType = ctx.checker.getTypeAtLocation(propAccess.expression);
  const receiverWasm = resolveWasmType(ctx, receiverType);
  if (receiverWasm.kind === "externref") {
    const isHOP = propAccess.name.text === "hasOwnProperty";
    const importName = isHOP ? "__hasOwnProperty" : "__propertyIsEnumerable";
    const hopIdx = ensureLateImport2(
      ctx,
      importName,
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "i32" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (hopIdx !== void 0) {
      compileExpression(ctx, fctx, propAccess.expression);
      if (expr.arguments[0]) {
        const argType = compileExpression(ctx, fctx, expr.arguments[0]);
        if (argType && argType.kind !== "externref") {
          coerceType(ctx, fctx, argType, { kind: "externref" });
        }
      } else {
        fctx.body.push({ op: "ref.null.extern" });
      }
      fctx.body.push({ op: "call", funcIdx: hopIdx });
      return { kind: "i32" };
    }
  }
  const privateNames = /* @__PURE__ */ new Set();
  for (const prop of receiverType.getProperties()) {
    if (prop.name.startsWith("#")) {
      privateNames.add(prop.name.slice(1));
    }
  }
  let structFieldNames = null;
  if (receiverWasm.kind === "ref" || receiverWasm.kind === "ref_null") {
    const structDef = ctx.mod.types[receiverWasm.typeIdx];
    if (structDef?.kind === "struct") {
      structFieldNames = structDef.fields
        .map((f) => f.name)
        .filter((n) => n !== void 0 && !n.startsWith("__") && !privateNames.has(n));
    }
  }
  const isPrototypeReceiver =
    ts10.isPropertyAccessExpression(propAccess.expression) && propAccess.expression.name.text === "prototype";
  const isConstructorReceiver = !isPrototypeReceiver && receiverType.getConstructSignatures().length > 0;
  if (isPrototypeReceiver || isConstructorReceiver) {
    structFieldNames = null;
  }
  const tsProps = /* @__PURE__ */ new Set();
  for (const prop of receiverType.getProperties()) {
    if (prop.name.startsWith("#")) continue;
    const decls = prop.getDeclarations();
    const isMethod =
      decls && decls.length > 0 && decls.every((d) => ts10.isMethodDeclaration(d) || ts10.isMethodSignature(d));
    const isAccessor =
      decls &&
      decls.length > 0 &&
      decls.every((d) => ts10.isGetAccessorDeclaration(d) || ts10.isSetAccessorDeclaration(d));
    if (isPrototypeReceiver) {
      if (!isMethod && !isAccessor) continue;
    } else if (isConstructorReceiver) {
      if (decls && decls.length > 0) {
        const hasStatic = decls.some((d) =>
          ts10.canHaveModifiers(d)
            ? (ts10.getModifiers(d)?.some((m) => m.kind === ts10.SyntaxKind.StaticKeyword) ?? false)
            : false,
        );
        if (!hasStatic) continue;
      }
    } else {
      if (isMethod || isAccessor) continue;
    }
    tsProps.add(prop.name);
  }
  const callSigs = receiverType.getCallSignatures();
  const constructSigs = receiverType.getConstructSignatures();
  if (callSigs.length > 0 || constructSigs.length > 0) {
    tsProps.add("length");
    tsProps.add("name");
    if (constructSigs.length > 0) {
      tsProps.add("prototype");
    }
    const symbol = receiverType.getSymbol();
    if (symbol && symbol.flags & ts10.SymbolFlags.Class) {
      tsProps.add("prototype");
    }
  }
  const arg = expr.arguments[0];
  if (!arg) {
    const recvType2 = compileExpression(ctx, fctx, propAccess.expression);
    if (recvType2) {
      fctx.body.push({ op: "drop" });
    }
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  let staticKey = null;
  if (ts10.isStringLiteral(arg)) {
    staticKey = arg.text;
  } else if (ts10.isNumericLiteral(arg)) {
    staticKey = arg.text;
  } else {
    const argTsType = ctx.checker.getTypeAtLocation(arg);
    if (argTsType.isStringLiteral()) {
      staticKey = argTsType.value;
    }
  }
  if (staticKey !== null) {
    const hasInStruct = structFieldNames !== null && structFieldNames.includes(staticKey);
    const hasInTs = tsProps.has(staticKey);
    const has = hasInStruct || hasInTs;
    const recvType2 = compileExpression(ctx, fctx, propAccess.expression);
    if (recvType2) {
      fctx.body.push({ op: "drop" });
    }
    const argResultType2 = compileExpression(ctx, fctx, arg);
    if (argResultType2) {
      fctx.body.push({ op: "drop" });
    }
    fctx.body.push({ op: "i32.const", value: has ? 1 : 0 });
    return { kind: "i32" };
  }
  const allFieldNames = /* @__PURE__ */ new Set();
  if (structFieldNames) {
    for (const f of structFieldNames) allFieldNames.add(f);
  }
  for (const p of tsProps) allFieldNames.add(p);
  if (allFieldNames.size > 0) {
    for (const fieldName of allFieldNames) {
      if (!ctx.stringGlobalMap.has(fieldName)) {
        addStringConstantGlobal(ctx, fieldName);
      }
    }
    const recvType2 = compileExpression(ctx, fctx, propAccess.expression);
    if (recvType2) {
      fctx.body.push({ op: "drop" });
    }
    const keyType = compileExpression(ctx, fctx, arg);
    if (keyType) {
      const equalsIdx = ctx.funcMap.get("__str_eq") ?? ctx.funcMap.get("string_equals");
      const jsStrEquals = ctx.mod.imports.findIndex((imp) => imp.module === "wasm:js-string" && imp.name === "equals");
      const eqFunc = jsStrEquals >= 0 ? jsStrEquals : equalsIdx;
      if (eqFunc !== void 0 && eqFunc >= 0) {
        const keyLocal = allocLocal(fctx, `__hop_key_${fctx.locals.length}`, keyType);
        fctx.body.push({ op: "local.set", index: keyLocal });
        fctx.body.push({ op: "i32.const", value: 0 });
        for (const fieldName of allFieldNames) {
          const strGlobal = ctx.stringGlobalMap.get(fieldName);
          if (strGlobal !== void 0) {
            fctx.body.push({ op: "local.get", index: keyLocal });
            fctx.body.push({ op: "global.get", index: strGlobal });
            fctx.body.push({ op: "call", funcIdx: eqFunc });
            fctx.body.push({ op: "i32.or" });
          }
        }
        return { kind: "i32" };
      }
    }
  }
  const recvType = compileExpression(ctx, fctx, propAccess.expression);
  if (recvType) {
    fctx.body.push({ op: "drop" });
  }
  const argResultType = compileExpression(ctx, fctx, arg);
  if (argResultType) {
    fctx.body.push({ op: "drop" });
  }
  fctx.body.push({ op: "i32.const", value: 0 });
  return { kind: "i32" };
}

// src/codegen/typeof-delete.ts
import ts11 from "typescript";
function emitDeleteSentinel(fctx, fieldType) {
  switch (fieldType.kind) {
    case "ref":
    case "ref_null":
      fctx.body.push({ op: "ref.null", typeIdx: fieldType.typeIdx });
      break;
    case "externref":
      fctx.body.push({ op: "ref.null.extern" });
      break;
    case "f64":
      fctx.body.push({ op: "f64.const", value: NaN });
      break;
    case "i32":
      fctx.body.push({ op: "i32.const", value: 0 });
      break;
    default:
      fctx.body.push({ op: "ref.null.extern" });
      break;
  }
}
function compileDeleteExpression(ctx, fctx, expr) {
  const operand = expr.expression;
  let inner = operand;
  while (
    ts11.isParenthesizedExpression(inner) ||
    ts11.isAsExpression(inner) ||
    ts11.isNonNullExpression(inner) ||
    ts11.isTypeAssertionExpression(inner)
  ) {
    inner = ts11.isParenthesizedExpression(inner)
      ? inner.expression
      : ts11.isAsExpression(inner)
        ? inner.expression
        : ts11.isNonNullExpression(inner)
          ? inner.expression
          : inner.expression;
  }
  if (ts11.isIdentifier(inner)) {
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  if (ts11.isPropertyAccessExpression(inner)) {
    const objType = ctx.checker.getTypeAtLocation(inner.expression);
    let typeName = resolveStructName(ctx, objType);
    if (!typeName && ts11.isIdentifier(inner.expression)) {
      typeName = ctx.widenedVarStructMap.get(inner.expression.text);
    }
    if (typeName) {
      const structTypeIdx = ctx.structMap.get(typeName);
      const fields = ctx.structFields.get(typeName);
      const fieldName = ts11.isPrivateIdentifier(inner.name) ? "__priv_" + inner.name.text.slice(1) : inner.name.text;
      if (structTypeIdx !== void 0 && fields) {
        const fieldIdx = fields.findIndex((f) => f.name === fieldName);
        if (fieldIdx !== -1 && fields[fieldIdx].mutable) {
          const fieldType = fields[fieldIdx].type;
          compileExpression(ctx, fctx, inner.expression);
          emitDeleteSentinel(fctx, fieldType);
          fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
          fctx.body.push({ op: "i32.const", value: 1 });
          return { kind: "i32" };
        }
      }
    }
  }
  if (ts11.isElementAccessExpression(inner) && ts11.isStringLiteral(inner.argumentExpression)) {
    const objType = ctx.checker.getTypeAtLocation(inner.expression);
    let typeName = resolveStructName(ctx, objType);
    if (!typeName && ts11.isIdentifier(inner.expression)) {
      typeName = ctx.widenedVarStructMap.get(inner.expression.text);
    }
    if (typeName) {
      const structTypeIdx = ctx.structMap.get(typeName);
      const fields = ctx.structFields.get(typeName);
      const fieldName = inner.argumentExpression.text;
      if (structTypeIdx !== void 0 && fields) {
        const fieldIdx = fields.findIndex((f) => f.name === fieldName);
        if (fieldIdx !== -1 && fields[fieldIdx].mutable) {
          const fieldType = fields[fieldIdx].type;
          compileExpression(ctx, fctx, inner.expression);
          emitDeleteSentinel(fctx, fieldType);
          fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
          fctx.body.push({ op: "i32.const", value: 1 });
          return { kind: "i32" };
        }
      }
    }
  }
  const operandType = compileExpression(ctx, fctx, operand);
  if (operandType !== null) {
    fctx.body.push({ op: "drop" });
  }
  fctx.body.push({ op: "i32.const", value: 1 });
  return { kind: "i32" };
}
function compileRegExpLiteral(ctx, fctx, expr) {
  const { pattern, flags } = parseRegExpLiteral(expr.getText());
  const patternResult = compileStringLiteral(ctx, fctx, pattern, expr);
  if (!patternResult) return null;
  const flagsStr = flags ?? "";
  const flagsResult = compileStringLiteral(ctx, fctx, flagsStr, expr);
  if (!flagsResult) return null;
  let funcIdx = ctx.funcMap.get("RegExp_new");
  if (funcIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const regexpNewType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "RegExp_new", { kind: "func", typeIdx: regexpNewType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    funcIdx = ctx.funcMap.get("RegExp_new");
  }
  if (funcIdx === void 0) {
    ctx.errors.push({
      message: "Missing RegExp_new import for regex literal",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  fctx.body.push({ op: "call", funcIdx });
  return { kind: "externref" };
}
function collectInstanceOfTags(ctx, className) {
  const ownTag = ctx.classTagMap.get(className);
  if (ownTag === void 0) return [];
  const tags = [ownTag];
  for (const [child, parent] of ctx.classParentMap) {
    if (parent === className) {
      tags.push(...collectInstanceOfTags(ctx, child));
    }
  }
  return tags;
}
function resolveInstanceOfClassName(ctx, rightExpr) {
  if (ts11.isIdentifier(rightExpr)) {
    const name = rightExpr.text;
    if (ctx.classTagMap.has(name)) return name;
    const mapped = ctx.classExprNameMap.get(name);
    if (mapped && ctx.classTagMap.has(mapped)) return mapped;
  }
  const tsType = ctx.checker.getTypeAtLocation(rightExpr);
  const constructSigs = tsType.getConstructSignatures?.();
  if (constructSigs && constructSigs.length > 0) {
    const instanceType = constructSigs[0].getReturnType();
    const symbolName2 = instanceType.getSymbol()?.name;
    if (symbolName2) {
      if (ctx.classTagMap.has(symbolName2)) return symbolName2;
      const mapped = ctx.classExprNameMap.get(symbolName2);
      if (mapped && ctx.classTagMap.has(mapped)) return mapped;
    }
  }
  const symbolName = tsType.getSymbol()?.name;
  if (symbolName) {
    if (ctx.classTagMap.has(symbolName)) return symbolName;
    const mapped = ctx.classExprNameMap.get(symbolName);
    if (mapped && ctx.classTagMap.has(mapped)) return mapped;
  }
  return void 0;
}
function compileInstanceOf(ctx, fctx, expr) {
  const className = resolveInstanceOfClassName(ctx, expr.right);
  if (className === void 0) {
    const leftType2 = compileExpression(ctx, fctx, expr.left);
    if (leftType2) {
      fctx.body.push({ op: "drop" });
    }
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  const compatibleTags = collectInstanceOfTags(ctx, className);
  if (compatibleTags.length === 0) {
    const leftType2 = compileExpression(ctx, fctx, expr.left);
    if (leftType2) {
      fctx.body.push({ op: "drop" });
    }
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  const leftType = compileExpression(ctx, fctx, expr.left);
  if (!leftType) return null;
  const rightStructTypeIdx = ctx.structMap.get(className);
  let rootClass = className;
  while (ctx.classParentMap.has(rootClass)) {
    rootClass = ctx.classParentMap.get(rootClass);
  }
  const rootStructTypeIdx = ctx.structMap.get(rootClass) ?? rightStructTypeIdx;
  if (leftType.kind === "externref") {
    if (rootStructTypeIdx === void 0) {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "i32.const", value: 0 });
      return { kind: "i32" };
    }
    fctx.body.push({ op: "any.convert_extern" });
    const anyLocalIdx = allocTempLocal(fctx, { kind: "anyref" });
    fctx.body.push({ op: "local.set", index: anyLocalIdx });
    const thenBody = [{ op: "i32.const", value: 0 }];
    const elseBody = [
      { op: "local.get", index: anyLocalIdx },
      { op: "ref.cast", typeIdx: rootStructTypeIdx },
      { op: "struct.get", typeIdx: rootStructTypeIdx, fieldIdx: 0 },
    ];
    if (compatibleTags.length === 1) {
      elseBody.push({ op: "i32.const", value: compatibleTags[0] });
      elseBody.push({ op: "i32.eq" });
    } else {
      const tagLocalIdx = allocLocal(fctx, `__instanceof_tag_${fctx.locals.length}`, { kind: "i32" });
      elseBody.push({ op: "local.set", index: tagLocalIdx });
      elseBody.push({ op: "local.get", index: tagLocalIdx });
      elseBody.push({ op: "i32.const", value: compatibleTags[0] });
      elseBody.push({ op: "i32.eq" });
      for (let i = 1; i < compatibleTags.length; i++) {
        elseBody.push({ op: "local.get", index: tagLocalIdx });
        elseBody.push({ op: "i32.const", value: compatibleTags[i] });
        elseBody.push({ op: "i32.eq" });
        elseBody.push({ op: "i32.or" });
      }
    }
    fctx.body.push({ op: "local.get", index: anyLocalIdx });
    fctx.body.push({ op: "ref.test", typeIdx: rootStructTypeIdx });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "i32" } },
      then: elseBody,
      // ref.test passed -> check tag
      else: thenBody,
      // ref.test failed -> false
    });
    releaseTempLocal(fctx, anyLocalIdx);
    return { kind: "i32" };
  }
  if (leftType.kind === "i32" || leftType.kind === "f64") {
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  const leftTsType = ctx.checker.getTypeAtLocation(expr.left);
  let leftClassName = leftTsType.getSymbol()?.name;
  if (leftClassName && !ctx.structMap.has(leftClassName)) {
    leftClassName = ctx.classExprNameMap.get(leftClassName) ?? leftClassName;
  }
  let leftStructTypeIdx = leftClassName ? ctx.structMap.get(leftClassName) : void 0;
  if (leftStructTypeIdx === void 0) {
    leftStructTypeIdx = rootStructTypeIdx;
  }
  if (leftStructTypeIdx === void 0) {
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  const isNullable = leftType.kind === "ref_null";
  if (isNullable) {
    const refLocalIdx = allocLocal(fctx, `__instanceof_ref_${fctx.locals.length}`, leftType);
    fctx.body.push({ op: "local.set", index: refLocalIdx });
    const thenBody = [{ op: "i32.const", value: 0 }];
    const tagCheckBody = [
      { op: "local.get", index: refLocalIdx },
      { op: "ref.cast", typeIdx: leftStructTypeIdx },
      { op: "struct.get", typeIdx: leftStructTypeIdx, fieldIdx: 0 },
    ];
    if (compatibleTags.length === 1) {
      tagCheckBody.push({ op: "i32.const", value: compatibleTags[0] });
      tagCheckBody.push({ op: "i32.eq" });
    } else {
      const tagLocalIdx = allocLocal(fctx, `__instanceof_tag_${fctx.locals.length}`, { kind: "i32" });
      tagCheckBody.push({ op: "local.set", index: tagLocalIdx });
      tagCheckBody.push({ op: "local.get", index: tagLocalIdx });
      tagCheckBody.push({ op: "i32.const", value: compatibleTags[0] });
      tagCheckBody.push({ op: "i32.eq" });
      for (let i = 1; i < compatibleTags.length; i++) {
        tagCheckBody.push({ op: "local.get", index: tagLocalIdx });
        tagCheckBody.push({ op: "i32.const", value: compatibleTags[i] });
        tagCheckBody.push({ op: "i32.eq" });
        tagCheckBody.push({ op: "i32.or" });
      }
    }
    const elseBody = [
      { op: "local.get", index: refLocalIdx },
      { op: "ref.test", typeIdx: leftStructTypeIdx },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: tagCheckBody,
        else: [{ op: "i32.const", value: 0 }],
        // wrong struct type → false
      },
    ];
    fctx.body.push({ op: "local.get", index: refLocalIdx });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "i32" } },
      then: thenBody,
      else: elseBody,
    });
    return { kind: "i32" };
  }
  fctx.body.push({ op: "struct.get", typeIdx: leftStructTypeIdx, fieldIdx: 0 });
  if (compatibleTags.length === 1) {
    fctx.body.push({ op: "i32.const", value: compatibleTags[0] });
    fctx.body.push({ op: "i32.eq" });
  } else {
    const tagLocalIdx = allocLocal(fctx, `__instanceof_tag_${fctx.locals.length}`, { kind: "i32" });
    fctx.body.push({ op: "local.set", index: tagLocalIdx });
    fctx.body.push({ op: "local.get", index: tagLocalIdx });
    fctx.body.push({ op: "i32.const", value: compatibleTags[0] });
    fctx.body.push({ op: "i32.eq" });
    for (let i = 1; i < compatibleTags.length; i++) {
      fctx.body.push({ op: "local.get", index: tagLocalIdx });
      fctx.body.push({ op: "i32.const", value: compatibleTags[i] });
      fctx.body.push({ op: "i32.eq" });
      fctx.body.push({ op: "i32.or" });
    }
  }
  return { kind: "i32" };
}
function staticTypeofForType(ctx, tsType) {
  if (tsType.flags & ts11.TypeFlags.Null) return "object";
  if (tsType.flags & ts11.TypeFlags.Undefined || tsType.flags & ts11.TypeFlags.Void) return "undefined";
  if (tsType.flags & ts11.TypeFlags.BigInt || tsType.flags & ts11.TypeFlags.BigIntLiteral) return "bigint";
  if (isStringType(tsType)) return "string";
  const wasmType = resolveWasmType(ctx, tsType);
  if (wasmType.kind === "f64") return "number";
  if (wasmType.kind === "i32") {
    if (isSymbolType(tsType)) return "symbol";
    if (isBooleanType(tsType)) return "boolean";
    return "number";
  }
  if (wasmType.kind === "ref" || wasmType.kind === "ref_null") {
    if (isAnyValue(wasmType, ctx)) return null;
    const callSigs = tsType.getCallSignatures?.();
    if (callSigs && callSigs.length > 0) return "function";
    const ctorSigs = tsType.getConstructSignatures?.();
    if (ctorSigs && ctorSigs.length > 0) return "function";
    return "object";
  }
  if (wasmType.kind === "externref") {
    const callSigs = tsType.getCallSignatures?.();
    if (callSigs && callSigs.length > 0) return "function";
    const ctorSigs = tsType.getConstructSignatures?.();
    if (ctorSigs && ctorSigs.length > 0) return "function";
    if (tsType.flags & ts11.TypeFlags.Object) return "object";
  }
  if (tsType.isUnion?.()) {
    const results = /* @__PURE__ */ new Set();
    for (const member of tsType.types) {
      const r = staticTypeofForType(ctx, member);
      if (r === null) return null;
      results.add(r);
    }
    if (results.size === 1) return [...results][0];
  }
  return null;
}
function compileTypeofExpression(ctx, fctx, expr) {
  const operand = expr.expression;
  if (
    ts11.isPropertyAccessExpression(operand) &&
    ts11.isIdentifier(operand.expression) &&
    operand.expression.text === "Math"
  ) {
    const mathConstants = /* @__PURE__ */ new Set(["PI", "E", "LN2", "LN10", "SQRT2", "SQRT1_2", "LOG2E", "LOG10E"]);
    if (mathConstants.has(operand.name.text)) {
      return compileStringLiteral(ctx, fctx, "number");
    }
    return compileStringLiteral(ctx, fctx, "function");
  }
  if (
    ts11.isMetaProperty(operand) &&
    operand.keywordToken === ts11.SyntaxKind.ImportKeyword &&
    operand.name.text === "meta"
  ) {
    return compileStringLiteral(ctx, fctx, "object");
  }
  if (
    ts11.isMetaProperty(operand) &&
    operand.keywordToken === ts11.SyntaxKind.NewKeyword &&
    operand.name.text === "target"
  ) {
    if (fctx.isConstructor) {
      return compileStringLiteral(ctx, fctx, "function");
    } else {
      return compileStringLiteral(ctx, fctx, "undefined");
    }
  }
  const tsType = ctx.checker.getTypeAtLocation(operand);
  const staticResult = staticTypeofForType(ctx, tsType);
  if (staticResult !== null) {
    return compileStringLiteral(ctx, fctx, staticResult);
  }
  const wasmType = resolveWasmType(ctx, tsType);
  if (ctx.fast && (wasmType.kind === "ref" || wasmType.kind === "ref_null") && isAnyValue(wasmType, ctx)) {
    ensureAnyHelpers(ctx);
    const typeofIdx = ctx.funcMap.get("__any_typeof");
    if (typeofIdx !== void 0) {
      const operandType2 = compileExpression(ctx, fctx, operand);
      if (operandType2 === null) return null;
      fctx.body.push({ op: "call", funcIdx: typeofIdx });
      return { kind: "ref", typeIdx: ctx.anyStrTypeIdx };
    }
  }
  addUnionImports(ctx);
  const funcIdx = ctx.funcMap.get("__typeof");
  if (funcIdx === void 0) return null;
  const operandType = compileExpression(ctx, fctx, operand);
  if (operandType === null) return null;
  if (operandType.kind === "f64") {
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx !== void 0) fctx.body.push({ op: "call", funcIdx: boxIdx });
  } else if (operandType.kind === "i32") {
    const boxIdx = ctx.funcMap.get("__box_boolean");
    if (boxIdx !== void 0) fctx.body.push({ op: "call", funcIdx: boxIdx });
  } else if (operandType.kind === "ref" || operandType.kind === "ref_null") {
    fctx.body.push({ op: "extern.convert_any" });
  }
  fctx.body.push({ op: "call", funcIdx });
  return { kind: "externref" };
}
function compileTypeofComparison(ctx, fctx, expr) {
  const op = expr.operatorToken.kind;
  const isEq = op === ts11.SyntaxKind.EqualsEqualsEqualsToken || op === ts11.SyntaxKind.EqualsEqualsToken;
  const isNeq = op === ts11.SyntaxKind.ExclamationEqualsEqualsToken || op === ts11.SyntaxKind.ExclamationEqualsToken;
  if (!isEq && !isNeq) return null;
  let typeofExpr = null;
  let stringLiteral = null;
  if (ts11.isTypeOfExpression(expr.left) && ts11.isStringLiteral(expr.right)) {
    typeofExpr = expr.left;
    stringLiteral = expr.right.text;
  } else if (ts11.isTypeOfExpression(expr.right) && ts11.isStringLiteral(expr.left)) {
    typeofExpr = expr.right;
    stringLiteral = expr.left.text;
  }
  if (!typeofExpr || !stringLiteral) return null;
  const operand = typeofExpr.expression;
  const tsType = ctx.checker.getTypeAtLocation(operand);
  let staticTypeof = null;
  if (
    ts11.isPropertyAccessExpression(operand) &&
    ts11.isIdentifier(operand.expression) &&
    operand.expression.text === "Math"
  ) {
    const mathConstants = /* @__PURE__ */ new Set(["PI", "E", "LN2", "LN10", "SQRT2", "SQRT1_2", "LOG2E", "LOG10E"]);
    staticTypeof = mathConstants.has(operand.name.text) ? "number" : "function";
  } else {
    staticTypeof = staticTypeofForType(ctx, tsType);
  }
  if (staticTypeof !== null) {
    const matches = staticTypeof === stringLiteral;
    const result = isEq ? (matches ? 1 : 0) : matches ? 0 : 1;
    fctx.body.push({ op: "i32.const", value: result });
    return { kind: "i32" };
  }
  if (isAnyValue(resolveWasmType(ctx, tsType), ctx)) {
    ensureAnyHelpers(ctx);
    let tagChecks = null;
    if (stringLiteral === "number") tagChecks = [2, 3];
    else if (stringLiteral === "boolean") tagChecks = [4];
    else if (stringLiteral === "string") tagChecks = [5, 6];
    else if (stringLiteral === "undefined") tagChecks = [1];
    else if (stringLiteral === "object") tagChecks = [0];
    if (tagChecks !== null) {
      const operandType = compileExpression(ctx, fctx, operand);
      if (!operandType) return null;
      fctx.body.push({ op: "struct.get", typeIdx: ctx.anyValueTypeIdx, fieldIdx: 0 });
      if (tagChecks.length === 1) {
        fctx.body.push({ op: "i32.const", value: tagChecks[0] });
        fctx.body.push({ op: "i32.eq" });
      } else {
        const tagLocal = allocTempLocal(fctx, { kind: "i32" });
        fctx.body.push({ op: "local.set", index: tagLocal });
        fctx.body.push({ op: "local.get", index: tagLocal });
        fctx.body.push({ op: "i32.const", value: tagChecks[0] });
        fctx.body.push({ op: "i32.eq" });
        for (let i = 1; i < tagChecks.length; i++) {
          fctx.body.push({ op: "local.get", index: tagLocal });
          fctx.body.push({ op: "i32.const", value: tagChecks[i] });
          fctx.body.push({ op: "i32.eq" });
          fctx.body.push({ op: "i32.or" });
        }
        releaseTempLocal(fctx, tagLocal);
      }
      if (isNeq) {
        fctx.body.push({ op: "i32.eqz" });
      }
      return { kind: "i32" };
    }
  }
  addUnionImports(ctx);
  let helperName = null;
  if (stringLiteral === "number") helperName = "__typeof_number";
  else if (stringLiteral === "string") helperName = "__typeof_string";
  else if (stringLiteral === "boolean") helperName = "__typeof_boolean";
  else if (stringLiteral === "undefined") helperName = "__typeof_undefined";
  else if (stringLiteral === "object") helperName = "__typeof_object";
  else if (stringLiteral === "function") helperName = "__typeof_function";
  if (!helperName) return null;
  const funcIdx = ctx.funcMap.get(helperName);
  if (funcIdx === void 0) return null;
  if (ts11.isIdentifier(operand)) {
    const localIdx = fctx.localMap.get(operand.text);
    if (localIdx !== void 0) {
      fctx.body.push({ op: "local.get", index: localIdx });
    } else {
      const valType = compileExpression(ctx, fctx, operand);
      if (!valType) return null;
    }
  } else {
    const valType = compileExpression(ctx, fctx, operand);
    if (!valType) return null;
  }
  fctx.body.push({ op: "call", funcIdx });
  if (isNeq) {
    fctx.body.push({ op: "i32.eqz" });
  }
  return { kind: "i32" };
}

// src/codegen/property-access.ts
import ts12 from "typescript";
function emitDummyStruct(ctx, fctx, className) {
  const structTypeIdx = ctx.structMap.get(className);
  const fields = ctx.structFields.get(className);
  if (structTypeIdx === void 0 || !fields) return false;
  for (const field of fields) {
    if (field.name === "__tag") {
      const tag = ctx.classTagMap.get(className) ?? 0;
      fctx.body.push({ op: "i32.const", value: tag });
    } else {
      switch (field.type.kind) {
        case "f64":
          fctx.body.push({ op: "f64.const", value: 0 });
          break;
        case "i32":
          fctx.body.push({ op: "i32.const", value: 0 });
          break;
        case "externref":
          fctx.body.push({ op: "ref.null.extern" });
          break;
        case "ref_null":
          fctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
          break;
        case "ref":
          fctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
          break;
        default:
          fctx.body.push({ op: "i32.const", value: 0 });
          break;
      }
    }
  }
  fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
  return true;
}
function emitGetterCallWithDummy(ctx, fctx, className, getterName, funcIdx) {
  if (!emitDummyStruct(ctx, fctx, className)) return null;
  fctx.body.push({ op: "call", funcIdx });
  const localIdx = funcIdx - ctx.numImportFuncs;
  const funcDef = localIdx >= 0 ? ctx.mod.functions[localIdx] : void 0;
  if (funcDef) {
    const funcType = ctx.mod.types[funcDef.typeIdx];
    if (funcType?.kind === "func" && funcType.results.length > 0) {
      return funcType.results[0];
    }
  }
  return { kind: "externref" };
}
function isProvablyNonNull(expr, checker) {
  let inner = expr;
  while (ts12.isParenthesizedExpression(inner)) {
    inner = inner.expression;
  }
  switch (inner.kind) {
    case ts12.SyntaxKind.NewExpression:
    case ts12.SyntaxKind.ObjectLiteralExpression:
    case ts12.SyntaxKind.ArrayLiteralExpression:
    case ts12.SyntaxKind.StringLiteral:
    case ts12.SyntaxKind.NoSubstitutionTemplateLiteral:
    case ts12.SyntaxKind.TemplateExpression:
      return true;
    default:
      break;
  }
  if (checker && ts12.isIdentifier(inner)) {
    const sym = checker.getSymbolAtLocation(inner);
    if (sym) {
      const decl = sym.valueDeclaration;
      if (decl && ts12.isVariableDeclaration(decl) && decl.initializer) {
        const declList = decl.parent;
        if (ts12.isVariableDeclarationList(declList) && (declList.flags & ts12.NodeFlags.Const) !== 0) {
          return isProvablyNonNull(decl.initializer, checker);
        }
      }
    }
  }
  return false;
}
function typeErrorThrowInstrs(ctx, node) {
  const line = node ? getLine(node) : 0;
  const col = node ? getCol(node) : 0;
  const message =
    line > 0 && col > 0
      ? `TypeError: Cannot access property on null or undefined at ${line}:${col}`
      : "TypeError: Cannot access property on null or undefined";
  addStringConstantGlobal(ctx, message);
  const strIdx = ctx.stringGlobalMap.get(message);
  const tagIdx = ensureExnTag(ctx);
  return [
    { op: "global.get", index: strIdx },
    { op: "throw", tagIdx },
  ];
}
function emitNullCheckThrow2(ctx, fctx, refType, node) {
  const backupLocal = fctx.__lastGuardedCastBackup;
  const tmp = allocTempLocal(fctx, refType);
  fctx.body.push({ op: "local.tee", index: tmp });
  fctx.body.push({ op: "ref.is_null" });
  if (backupLocal !== void 0) {
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: backupLocal },
        { op: "ref.is_null" },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: typeErrorThrowInstrs(ctx, node),
          else: [],
          // wrong struct type — don't throw
        },
      ],
      else: [],
    });
  } else {
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: typeErrorThrowInstrs(ctx, node),
      else: [],
    });
  }
  fctx.body.push({ op: "local.get", index: tmp });
  releaseTempLocal(fctx, tmp);
}
function findAlternateStructsForField(ctx, propName, excludeTypeIdx) {
  const result = [];
  for (const [typeName, fields] of ctx.structFields) {
    const sIdx = ctx.structMap.get(typeName);
    if (sIdx === void 0 || sIdx === excludeTypeIdx) continue;
    const fIdx = fields.findIndex((f) => f.name === propName);
    if (fIdx !== -1) {
      result.push({ structTypeIdx: sIdx, fieldIdx: fIdx, fieldType: fields[fIdx].type });
    }
  }
  return result;
}
function emitNullGuardedStructGet(ctx, fctx, objType, fieldType, typeIdx, fieldIdx, propName, throwOnNull = false) {
  const resultType = fieldType.kind === "ref" ? { kind: "ref_null", typeIdx: fieldType.typeIdx } : fieldType;
  if (propName) {
    const tmpAny = allocLocal(fctx, `__ng_any_${fctx.locals.length}`, { kind: "anyref" });
    fctx.body.push({ op: "local.set", index: tmpAny });
    const resultLocal = allocLocal(fctx, `__ng_res_${fctx.locals.length}`, resultType);
    const alternates = findAlternateStructsForField(ctx, propName, typeIdx);
    const buildFallback = (srcLocal, altIdx) => {
      if (altIdx < alternates.length) {
        const alt = alternates[altIdx];
        const altCoerce = coercionInstrs(ctx, alt.fieldType, resultType, fctx);
        return [
          { op: "local.get", index: srcLocal },
          { op: "ref.test", typeIdx: alt.structTypeIdx },
          {
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "local.get", index: srcLocal },
              { op: "ref.cast", typeIdx: alt.structTypeIdx },
              { op: "struct.get", typeIdx: alt.structTypeIdx, fieldIdx: alt.fieldIdx },
              ...altCoerce,
              { op: "local.set", index: resultLocal },
            ],
            else: buildFallback(srcLocal, altIdx + 1),
          },
        ];
      }
      return [...defaultValueInstrs(resultType), { op: "local.set", index: resultLocal }];
    };
    const backupLocal = fctx.__lastGuardedCastBackup;
    fctx.body.push({ op: "local.get", index: tmpAny });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then:
        backupLocal !== void 0
          ? [
              // Value is null — could be wrong struct type or genuinely null.
              // Check the backup anyref to distinguish.
              { op: "local.get", index: backupLocal },
              { op: "ref.is_null" },
              {
                op: "if",
                blockType: { kind: "empty" },
                // Backup is also null → genuinely null, throw TypeError
                then: typeErrorThrowInstrs(ctx),
                // Backup is non-null → wrong struct type, try primary + alternates on backup
                else: [
                  { op: "local.get", index: backupLocal },
                  { op: "ref.test", typeIdx },
                  {
                    op: "if",
                    blockType: { kind: "empty" },
                    then: [
                      { op: "local.get", index: backupLocal },
                      { op: "ref.cast", typeIdx },
                      { op: "struct.get", typeIdx, fieldIdx },
                      { op: "local.set", index: resultLocal },
                    ],
                    else: buildFallback(backupLocal, 0),
                  },
                ],
              },
            ]
          : typeErrorThrowInstrs(ctx),
      else: [],
    });
    fctx.body.push({ op: "local.get", index: tmpAny });
    fctx.body.push({ op: "ref.test", typeIdx });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: tmpAny },
        { op: "ref.cast", typeIdx },
        { op: "struct.get", typeIdx, fieldIdx },
        { op: "local.set", index: resultLocal },
      ],
      else: buildFallback(tmpAny, 0),
    });
    fctx.body.push({ op: "local.get", index: resultLocal });
    return;
  }
  const tmp = allocLocal(fctx, `__ng_${fctx.locals.length}`, objType);
  fctx.body.push({ op: "local.tee", index: tmp });
  fctx.body.push({ op: "ref.is_null" });
  const nullBranch = throwOnNull ? typeErrorThrowInstrs(ctx) : defaultValueInstrs(resultType);
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: resultType },
    then: nullBranch,
    else: [
      { op: "local.get", index: tmp },
      { op: "struct.get", typeIdx, fieldIdx },
    ],
  });
}
function emitExternrefToStructGet(ctx, fctx, fieldType, structTypeIdx, fieldIdx, propName, throwOnNull = false) {
  const resultType = fieldType.kind === "ref" ? { kind: "ref_null", typeIdx: fieldType.typeIdx } : fieldType;
  fctx.body.push({ op: "any.convert_extern" });
  const tmpAny = allocTempLocal(fctx, { kind: "anyref" });
  fctx.body.push({ op: "local.tee", index: tmpAny });
  const resultLocal = allocTempLocal(fctx, resultType);
  if (throwOnNull) {
    fctx.body.push({ op: "local.get", index: tmpAny });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: typeErrorThrowInstrs(ctx),
      else: [],
    });
  }
  let externGetFallback;
  if (propName) {
    const getIdx = ensureLateImport2(
      ctx,
      "__extern_get",
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (getIdx !== void 0) {
      externGetFallback = [];
      externGetFallback.push({ op: "local.get", index: tmpAny });
      externGetFallback.push({ op: "extern.convert_any" });
      addStringConstantGlobal(ctx, propName);
      const strGlobalIdx = ctx.stringGlobalMap.get(propName);
      if (strGlobalIdx !== void 0) {
        externGetFallback.push({ op: "global.get", index: strGlobalIdx });
      } else {
        externGetFallback.push({ op: "ref.null.extern" });
      }
      externGetFallback.push({ op: "call", funcIdx: getIdx });
      if (resultType.kind === "f64") {
        const unboxIdx = ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
        flushLateImportShifts2(ctx, fctx);
        if (unboxIdx !== void 0) {
          externGetFallback.push({ op: "call", funcIdx: unboxIdx });
        }
      } else if (resultType.kind === "i32") {
        const unboxIdx = ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
        flushLateImportShifts2(ctx, fctx);
        if (unboxIdx !== void 0) {
          externGetFallback.push({ op: "call", funcIdx: unboxIdx });
          externGetFallback.push({ op: "i32.trunc_sat_f64_s" });
        }
      }
      if (resultType.kind === "ref_null") {
        const tmpExtResult = allocTempLocal(fctx, { kind: "anyref" });
        externGetFallback.push({ op: "any.convert_extern" });
        externGetFallback.push({ op: "local.tee", index: tmpExtResult });
        externGetFallback.push({ op: "ref.test", typeIdx: resultType.typeIdx });
        externGetFallback.push({
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "local.get", index: tmpExtResult },
            { op: "ref.cast", typeIdx: resultType.typeIdx },
            { op: "local.set", index: resultLocal },
          ],
          else: [...defaultValueInstrs(resultType), { op: "local.set", index: resultLocal }],
        });
        releaseTempLocal(fctx, tmpExtResult);
      } else {
        externGetFallback.push({ op: "local.set", index: resultLocal });
      }
    }
  }
  fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx });
  const alternates = propName ? findAlternateStructsForField(ctx, propName, structTypeIdx) : [];
  const buildFallbackChain = (altIdx) => {
    if (altIdx < alternates.length) {
      const alt = alternates[altIdx];
      const altCoerce = coercionInstrs(ctx, alt.fieldType, resultType, fctx);
      return [
        { op: "local.get", index: tmpAny },
        { op: "ref.test", typeIdx: alt.structTypeIdx },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "local.get", index: tmpAny },
            { op: "ref.cast", typeIdx: alt.structTypeIdx },
            { op: "struct.get", typeIdx: alt.structTypeIdx, fieldIdx: alt.fieldIdx },
            ...altCoerce,
            { op: "local.set", index: resultLocal },
          ],
          else: buildFallbackChain(altIdx + 1),
        },
      ];
    }
    if (externGetFallback) {
      return externGetFallback;
    }
    return [...defaultValueInstrs(resultType), { op: "local.set", index: resultLocal }];
  };
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "local.get", index: tmpAny },
      { op: "ref.cast", typeIdx: structTypeIdx },
      { op: "struct.get", typeIdx: structTypeIdx, fieldIdx },
      { op: "local.set", index: resultLocal },
    ],
    else: buildFallbackChain(0),
  });
  fctx.body.push({ op: "local.get", index: resultLocal });
  releaseTempLocal(fctx, tmpAny);
  releaseTempLocal(fctx, resultLocal);
}
function compileOptionalPropertyAccess(ctx, fctx, expr) {
  const objType = compileExpression(ctx, fctx, expr.expression);
  if (!objType) return null;
  const tmp = allocLocal(fctx, `__opt_${fctx.locals.length}`, objType);
  fctx.body.push({ op: "local.tee", index: tmp });
  fctx.body.push({ op: "ref.is_null" });
  const tsPropType = ctx.checker.getTypeAtLocation(expr);
  let resultType = resolveWasmType(ctx, tsPropType);
  if (resultType.kind === "ref" || resultType.kind === "ref_null") {
    resultType = { kind: "externref" };
  }
  const savedBody = fctx.body;
  fctx.savedBodies.push(savedBody);
  let thenInstrs;
  if (resultType.kind === "f64") {
    thenInstrs = [{ op: "f64.const", value: 0 }];
  } else if (resultType.kind === "i32") {
    thenInstrs = [{ op: "i32.const", value: 0 }];
  } else {
    thenInstrs = [{ op: "ref.null.extern" }];
  }
  fctx.body = [];
  fctx.body.push({ op: "local.get", index: tmp });
  const tsObjType = ctx.checker.getTypeAtLocation(expr.expression);
  const propName = expr.name.text;
  let elseResultType = null;
  if (isExternalDeclaredClass(tsObjType, ctx.checker)) {
    compileExternPropertyGetFromStack(ctx, fctx, tsObjType, propName);
    elseResultType = { kind: "externref" };
  } else if (isStringType(tsObjType) && propName === "length") {
    if (ctx.nativeStrings && ctx.anyStrTypeIdx >= 0) {
      fctx.body.push({ op: "struct.get", typeIdx: ctx.anyStrTypeIdx, fieldIdx: 0 });
    } else {
      const funcIdx = ctx.funcMap.get("length");
      if (funcIdx !== void 0) fctx.body.push({ op: "call", funcIdx });
    }
    elseResultType = { kind: "i32" };
  } else {
    const structName = resolveStructName(ctx, tsObjType);
    if (structName) {
      const structTypeIdx = ctx.structMap.get(structName);
      const fields = ctx.structFields.get(structName);
      if (structTypeIdx !== void 0 && fields) {
        const accessorKey = `${structName}_${propName}`;
        const getterName = `${structName}_get_${propName}`;
        const getterIdx = ctx.funcMap.get(getterName);
        if (ctx.classAccessorSet.has(accessorKey) && getterIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: getterIdx });
          const funcDef = ctx.mod.functions[getterIdx - ctx.numImportFuncs];
          if (funcDef) {
            const typeDef = ctx.mod.types[funcDef.typeIdx];
            if (typeDef && typeDef.kind === "func" && typeDef.results.length > 0) {
              elseResultType = typeDef.results[0];
            }
          }
        } else {
          const fieldIdx = fields.findIndex((f) => f.name === propName);
          if (fieldIdx >= 0) {
            if (objType.kind !== "ref" || objType.typeIdx !== structTypeIdx) {
              const castTmp = allocLocal(fctx, `__optcast_tmp_${fctx.locals.length}`, objType);
              fctx.body.push({ op: "local.tee", index: castTmp });
              fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx });
              fctx.body.push({
                op: "if",
                blockType: { kind: "val", type: fields[fieldIdx].type },
                then: [
                  { op: "local.get", index: castTmp },
                  { op: "ref.cast", typeIdx: structTypeIdx },
                  { op: "struct.get", typeIdx: structTypeIdx, fieldIdx },
                ],
                else: [
                  // Type mismatch at runtime — emit a safe default (sNaN sentinel for f64 #866)
                  ...(fields[fieldIdx].type.kind === "f64"
                    ? [{ op: "i64.const", value: 0x7ff00000deadc0den }, { op: "f64.reinterpret_i64" }]
                    : fields[fieldIdx].type.kind === "i32"
                      ? [{ op: "i32.const", value: 0 }]
                      : [{ op: "ref.null.extern" }]),
                ],
              });
            } else {
              fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
            }
            elseResultType = fields[fieldIdx].type;
          }
        }
      }
    }
  }
  if (elseResultType && !valTypesMatch(elseResultType, resultType)) {
    coerceType3(ctx, fctx, elseResultType, resultType);
  }
  const elseInstrs = fctx.body;
  popBody(fctx, savedBody);
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: resultType },
    then: thenInstrs,
    else: elseInstrs,
  });
  return resultType;
}
function compileExternPropertyGetFromStack(ctx, fctx, objType, propName) {
  const className = objType.getSymbol()?.name;
  if (!className) return;
  let current = className;
  while (current) {
    const info = ctx.externClasses.get(current);
    if (info?.properties.has(propName)) {
      const importName = `${info.importPrefix}_get_${propName}`;
      const funcIdx = ctx.funcMap.get(importName);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
      }
      return;
    }
    current = ctx.externClassParent?.get(current);
  }
}
function compilePropertyAccess(ctx, fctx, expr) {
  if (expr.questionDotToken) {
    return compileOptionalPropertyAccess(ctx, fctx, expr);
  }
  const objType = ctx.checker.getTypeAtLocation(expr.expression);
  const propName = ts12.isPrivateIdentifier(expr.name) ? "__priv_" + expr.name.text.slice(1) : expr.name.text;
  if (expr.expression.kind === ts12.SyntaxKind.SuperKeyword) {
    return compileSuperPropertyAccess(ctx, fctx, expr, propName);
  }
  if (
    ts12.isMetaProperty(expr.expression) &&
    expr.expression.keywordToken === ts12.SyntaxKind.ImportKeyword &&
    expr.expression.name.text === "meta"
  ) {
    if (propName === "url") {
      return compileStringLiteral(ctx, fctx, "module.wasm");
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  if (ts12.isIdentifier(expr.expression) && expr.expression.text === "globalThis") {
    const gtFuncIdx = ensureLateImport2(ctx, "__get_globalThis", [], [{ kind: "externref" }]);
    const getIdx = ensureLateImport2(
      ctx,
      "__extern_get",
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (gtFuncIdx === void 0 || getIdx === void 0) {
      fctx.body.push({ op: "ref.null.extern" });
      return { kind: "externref" };
    }
    fctx.body.push({ op: "call", funcIdx: gtFuncIdx });
    addStringConstantGlobal(ctx, propName);
    const strGlobalIdx = ctx.stringGlobalMap.get(propName);
    if (strGlobalIdx !== void 0) {
      fctx.body.push({ op: "global.get", index: strGlobalIdx });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    if (getIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: getIdx });
    }
    const accessType2 = ctx.checker.getTypeAtLocation(expr);
    const accessWasm2 = resolveWasmType(ctx, accessType2);
    if (accessWasm2.kind === "f64") {
      const unboxIdx = ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
      flushLateImportShifts2(ctx, fctx);
      if (unboxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: unboxIdx });
      }
      return { kind: "f64" };
    }
    if (accessWasm2.kind === "i32") {
      const unboxIdx = ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
      flushLateImportShifts2(ctx, fctx);
      if (unboxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: unboxIdx });
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
      return { kind: "i32" };
    }
    return { kind: "externref" };
  }
  if (ts12.isIdentifier(expr.expression)) {
    const objName = expr.expression.text;
    const enumKey = `${objName}.${propName}`;
    const enumVal = ctx.enumValues.get(enumKey);
    if (enumVal !== void 0) {
      fctx.body.push({ op: "f64.const", value: enumVal });
      return { kind: "f64" };
    }
    const enumStrVal = ctx.enumStringValues.get(enumKey);
    if (enumStrVal !== void 0) {
      return compileStringLiteral(ctx, fctx, enumStrVal);
    }
  }
  if (ts12.isIdentifier(expr.expression)) {
    const objName = expr.expression.text;
    if (ctx.classSet.has(objName)) {
      const fullName = `${objName}_${propName}`;
      const globalIdx = ctx.staticProps.get(fullName);
      if (globalIdx !== void 0) {
        fctx.body.push({ op: "global.get", index: globalIdx });
        const globalDef = ctx.mod.globals[localGlobalIdx(ctx, globalIdx)];
        return globalDef?.type ?? { kind: "f64" };
      }
      if (propName === "prototype") {
        if (emitLazyProtoGet(ctx, fctx, objName)) {
          return { kind: "externref" };
        }
        fctx.body.push({ op: "ref.null.extern" });
        return { kind: "externref" };
      }
      if (propName === "constructor") {
        const ctorName = `${objName}_constructor`;
        const funcIdx = ctx.funcMap.get(ctorName);
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "ref.func", funcIdx });
          fctx.body.push({ op: "extern.convert_any" });
          return { kind: "externref" };
        }
        fctx.body.push({ op: "ref.null.extern" });
        return { kind: "externref" };
      }
      if (ctx.staticMethodSet.has(fullName) || ctx.classMethodSet.has(fullName)) {
        const funcIdx = ctx.funcMap.get(fullName);
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "ref.null.extern" });
          return { kind: "externref" };
        }
      }
      const accessorKey = `${objName}_${propName}`;
      if (ctx.classAccessorSet.has(accessorKey)) {
        const getterName = `${objName}_get_${propName}`;
        const funcIdx = ctx.funcMap.get(getterName);
        if (funcIdx !== void 0) {
          const retType = emitGetterCallWithDummy(ctx, fctx, objName, getterName, funcIdx);
          return retType ?? { kind: "externref" };
        }
      }
    }
  }
  if (
    propName === "length" &&
    ts12.isPropertyAccessExpression(expr.expression) &&
    ts12.isIdentifier(expr.expression.expression) &&
    expr.expression.expression.text === "Math"
  ) {
    const mathMethodArity = {
      abs: 1,
      ceil: 1,
      floor: 1,
      round: 1,
      trunc: 1,
      sign: 1,
      sqrt: 1,
      cbrt: 1,
      clz32: 1,
      fround: 1,
      exp: 1,
      expm1: 1,
      log: 1,
      log2: 1,
      log10: 1,
      log1p: 1,
      sin: 1,
      cos: 1,
      tan: 1,
      asin: 1,
      acos: 1,
      atan: 1,
      sinh: 1,
      cosh: 1,
      tanh: 1,
      asinh: 1,
      acosh: 1,
      atanh: 1,
      min: 2,
      max: 2,
      pow: 2,
      atan2: 2,
      imul: 2,
      hypot: 2,
      random: 0,
    };
    const method = expr.expression.name.text;
    if (method in mathMethodArity) {
      fctx.body.push({ op: "f64.const", value: mathMethodArity[method] });
      return { kind: "f64" };
    }
  }
  if (propName === "length") {
    const callSigs = objType.getCallSignatures?.();
    const constructSigs2 = objType.getConstructSignatures?.();
    const lengthSigs =
      callSigs && callSigs.length > 0 ? callSigs : constructSigs2 && constructSigs2.length > 0 ? constructSigs2 : null;
    if (lengthSigs && lengthSigs.length > 0) {
      const sig = lengthSigs[0];
      const paramCount = sig.parameters.filter((p) => {
        const decl = p.valueDeclaration;
        return !decl || !ts12.isParameter(decl) || !decl.dotDotDotToken;
      }).length;
      fctx.body.push({ op: "f64.const", value: paramCount });
      return { kind: "f64" };
    }
  }
  if (propName === "name") {
    const callSigs = objType.getCallSignatures?.();
    const constructSigs = objType.getConstructSignatures?.();
    if ((callSigs && callSigs.length > 0) || (constructSigs && constructSigs.length > 0)) {
      let funcName = objType.getSymbol()?.name ?? "";
      if (funcName === "__type" || funcName === "__function" || funcName === "__class" || funcName === "__object")
        funcName = "";
      if (funcName === "") {
        if (ts12.isIdentifier(expr.expression)) {
          funcName = expr.expression.text;
        } else if (ts12.isPropertyAccessExpression(expr.expression)) {
          funcName = expr.expression.name.text;
        } else if (
          ts12.isElementAccessExpression(expr.expression) &&
          ts12.isStringLiteral(expr.expression.argumentExpression)
        ) {
          funcName = expr.expression.argumentExpression.text;
        }
      }
      addStringConstantGlobal(ctx, funcName);
      return compileStringLiteral(ctx, fctx, funcName);
    }
  }
  if (propName === "length") {
    if (ts12.isIdentifier(expr.expression)) {
      const shapeInfo = ctx.shapeMap.get(expr.expression.text);
      if (shapeInfo) {
        compileExpression(ctx, fctx, expr.expression);
        fctx.body.push({ op: "struct.get", typeIdx: shapeInfo.vecTypeIdx, fieldIdx: 0 });
        if (!ctx.fast) fctx.body.push({ op: "f64.convert_i32_s" });
        return ctx.fast ? { kind: "i32" } : { kind: "f64" };
      }
    }
    if (ts12.isIdentifier(expr.expression)) {
      const localIdx = fctx.localMap.get(expr.expression.text);
      if (localIdx !== void 0) {
        const localType =
          localIdx < fctx.params.length ? fctx.params[localIdx].type : fctx.locals[localIdx - fctx.params.length]?.type;
        if (localType?.kind === "externref") {
          const funcIdx = ctx.funcMap.get("__extern_length");
          if (funcIdx !== void 0) {
            fctx.body.push({ op: "local.get", index: localIdx });
            fctx.body.push({ op: "call", funcIdx });
            return { kind: "f64" };
          }
        }
        if ((localType?.kind === "ref" || localType?.kind === "ref_null") && localType.typeIdx !== void 0) {
          const vecTypeIdx = localType.typeIdx;
          const typeDef = ctx.mod.types[vecTypeIdx];
          if (
            typeDef?.kind === "struct" &&
            typeDef.fields[0]?.name === "length" &&
            typeDef.fields[1]?.name === "data"
          ) {
            fctx.body.push({ op: "local.get", index: localIdx });
            fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
            if (!ctx.fast) fctx.body.push({ op: "f64.convert_i32_s" });
            return ctx.fast ? { kind: "i32" } : { kind: "f64" };
          }
        }
      }
    }
    const objWasmType = resolveWasmType(ctx, objType);
    if (objWasmType.kind === "ref" || objWasmType.kind === "ref_null") {
      const vecTypeIdx = objWasmType.typeIdx;
      const typeDef = ctx.mod.types[vecTypeIdx];
      if (typeDef?.kind === "struct" && typeDef.fields[1]?.name === "data") {
        const exprResult = compileExpression(ctx, fctx, expr.expression);
        if (
          exprResult &&
          (exprResult.kind === "ref" || exprResult.kind === "ref_null") &&
          exprResult.typeIdx !== vecTypeIdx
        ) {
          const lenTmp = allocLocal(fctx, `__len_tmp_${fctx.locals.length}`, { kind: "anyref" });
          fctx.body.push({ op: "local.set", index: lenTmp });
          fctx.body.push({ op: "local.get", index: lenTmp });
          fctx.body.push({ op: "ref.test", typeIdx: vecTypeIdx });
          const lenResult = ctx.fast ? { kind: "i32" } : { kind: "f64" };
          fctx.body.push({
            op: "if",
            blockType: { kind: "val", type: lenResult },
            then: [
              { op: "local.get", index: lenTmp },
              { op: "ref.cast", typeIdx: vecTypeIdx },
              { op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 },
              ...(ctx.fast ? [] : [{ op: "f64.convert_i32_s" }]),
            ],
            else: [{ op: ctx.fast ? "i32.const" : "f64.const", value: 0 }],
          });
          return lenResult;
        }
        fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
        if (!ctx.fast) fctx.body.push({ op: "f64.convert_i32_s" });
        return ctx.fast ? { kind: "i32" } : { kind: "f64" };
      }
    }
    {
      const savedLen = fctx.body.length;
      const exprType = compileExpression(ctx, fctx, expr.expression);
      if (exprType && (exprType.kind === "ref" || exprType.kind === "ref_null") && exprType.typeIdx !== void 0) {
        const vecTypeIdx = exprType.typeIdx;
        const typeDef = ctx.mod.types[vecTypeIdx];
        if (typeDef?.kind === "struct" && typeDef.fields[0]?.name === "length" && typeDef.fields[1]?.name === "data") {
          fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
          if (!ctx.fast) fctx.body.push({ op: "f64.convert_i32_s" });
          return ctx.fast ? { kind: "i32" } : { kind: "f64" };
        }
      }
      fctx.body.length = savedLen;
    }
  }
  if (propName === "raw" && ctx.templateVecTypeIdx >= 0) {
    const templateVecTypeIdx = ctx.templateVecTypeIdx;
    let isVecLike = false;
    if (ts12.isIdentifier(expr.expression)) {
      const localIdx = fctx.localMap.get(expr.expression.text);
      if (localIdx !== void 0) {
        const localType =
          localIdx < fctx.params.length ? fctx.params[localIdx].type : fctx.locals[localIdx - fctx.params.length]?.type;
        if ((localType?.kind === "ref" || localType?.kind === "ref_null") && localType.typeIdx !== void 0) {
          const typeIdx = localType.typeIdx;
          const typeDef = ctx.mod.types[typeIdx];
          if (
            typeDef?.kind === "struct" &&
            typeDef.fields[0]?.name === "length" &&
            typeDef.fields[1]?.name === "data"
          ) {
            isVecLike = true;
          }
        }
      }
    }
    if (!isVecLike) {
      const objWasmType = resolveWasmType(ctx, objType);
      if (objWasmType.kind === "ref" || objWasmType.kind === "ref_null") {
        const typeIdx = objWasmType.typeIdx;
        const typeDef = ctx.mod.types[typeIdx];
        if (typeDef?.kind === "struct" && typeDef.fields[0]?.name === "length" && typeDef.fields[1]?.name === "data") {
          isVecLike = true;
        }
      }
    }
    if (isVecLike) {
      compileExpression(ctx, fctx, expr.expression);
      const baseVecTypeIdx = getOrRegisterVecType(ctx, "externref", { kind: "externref" });
      const rawTmp = allocLocal(fctx, `__raw_tmp_${fctx.locals.length}`, { kind: "ref_null", typeIdx: baseVecTypeIdx });
      const rawObj = allocLocal(fctx, `__raw_obj_${fctx.locals.length}`, { kind: "anyref" });
      fctx.body.push({ op: "local.set", index: rawObj });
      fctx.body.push({ op: "local.get", index: rawObj });
      fctx.body.push({ op: "ref.test", typeIdx: templateVecTypeIdx });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: rawObj },
          { op: "ref.cast", typeIdx: templateVecTypeIdx },
          { op: "struct.get", typeIdx: templateVecTypeIdx, fieldIdx: 2 },
          { op: "local.set", index: rawTmp },
        ],
        else: [
          // Not a template vec — return null (no raw field available)
          { op: "ref.null", typeIdx: baseVecTypeIdx },
          { op: "local.set", index: rawTmp },
        ],
      });
      fctx.body.push({ op: "local.get", index: rawTmp });
      return { kind: "ref_null", typeIdx: baseVecTypeIdx };
    }
  }
  if (ts12.isIdentifier(expr.expression) && expr.expression.text === "Math") {
    const mathConstants = {
      PI: Math.PI,
      E: Math.E,
      LN2: Math.LN2,
      LN10: Math.LN10,
      SQRT2: Math.SQRT2,
      SQRT1_2: Math.SQRT1_2,
      LOG2E: Math.LOG2E,
      LOG10E: Math.LOG10E,
    };
    if (propName in mathConstants) {
      fctx.body.push({ op: "f64.const", value: mathConstants[propName] });
      return { kind: "f64" };
    }
  }
  if (ts12.isIdentifier(expr.expression) && expr.expression.text === "Number") {
    const numberConstants = {
      EPSILON: Number.EPSILON,
      MAX_SAFE_INTEGER: Number.MAX_SAFE_INTEGER,
      MIN_SAFE_INTEGER: Number.MIN_SAFE_INTEGER,
      MAX_VALUE: Number.MAX_VALUE,
      MIN_VALUE: Number.MIN_VALUE,
      POSITIVE_INFINITY: Infinity,
      NEGATIVE_INFINITY: -Infinity,
      NaN: NaN,
    };
    if (propName in numberConstants) {
      fctx.body.push({ op: "f64.const", value: numberConstants[propName] });
      return { kind: "f64" };
    }
  }
  if (ts12.isIdentifier(expr.expression) && expr.expression.text === "Symbol") {
    const symId = getWellKnownSymbolId(propName);
    if (symId !== void 0) {
      fctx.body.push({ op: "i32.const", value: symId });
      return { kind: "i32" };
    }
  }
  if (isStringType(objType) && propName === "length") {
    compileExpression(ctx, fctx, expr.expression);
    if (ctx.nativeStrings && ctx.anyStrTypeIdx >= 0) {
      fctx.body.push({ op: "struct.get", typeIdx: ctx.anyStrTypeIdx, fieldIdx: 0 });
      return { kind: "i32" };
    }
    const funcIdx = ctx.funcMap.get("length");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "i32" };
    }
  }
  if (isIteratorResultType(objType) || isGeneratorIteratorResultLike(ctx, objType, propName)) {
    if (propName === "value") {
      compileExpression(ctx, fctx, expr.expression);
      const valueType = getIteratorResultValueType(ctx, objType);
      if (valueType && valueType.kind === "f64") {
        const funcIdx2 = ctx.funcMap.get("__gen_result_value_f64");
        if (funcIdx2 !== void 0) {
          fctx.body.push({ op: "call", funcIdx: funcIdx2 });
          return { kind: "f64" };
        }
      }
      const funcIdx = ctx.funcMap.get("__gen_result_value");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "externref" };
      }
    }
    if (propName === "done") {
      compileExpression(ctx, fctx, expr.expression);
      const funcIdx = ctx.funcMap.get("__gen_result_done");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "i32" };
      }
    }
  }
  if (isExternalDeclaredClass(objType, ctx.checker)) {
    const externResult = compileExternPropertyGet(ctx, fctx, expr, objType, propName);
    if (externResult !== null) return externResult;
  }
  let typeName = resolveStructName(ctx, objType);
  if (!typeName && ts12.isIdentifier(expr.expression)) {
    typeName = ctx.widenedVarStructMap.get(expr.expression.text);
  }
  if (!typeName && expr.expression.kind === ts12.SyntaxKind.ThisKeyword) {
    typeName = resolveThisStructName(ctx, fctx);
  }
  if (typeName) {
    const accessorKey = `${typeName}_${propName}`;
    if (ctx.classAccessorSet.has(accessorKey)) {
      const getterName = `${typeName}_get_${propName}`;
      const funcIdx = ctx.funcMap.get(getterName);
      if (funcIdx !== void 0) {
        compileExpression(ctx, fctx, expr.expression);
        fctx.body.push({ op: "call", funcIdx });
        const getterLocalIdx = funcIdx - ctx.numImportFuncs;
        const getterDef = getterLocalIdx >= 0 ? ctx.mod.functions[getterLocalIdx] : void 0;
        if (getterDef) {
          const getterType = ctx.mod.types[getterDef.typeIdx];
          if (getterType?.kind === "func" && getterType.results.length > 0) {
            return getterType.results[0];
          }
        }
        const propType = ctx.checker.getTypeAtLocation(expr);
        return resolveWasmType(ctx, propType);
      }
    }
    if (ctx.classSet.has(typeName)) {
      const methodFullName = `${typeName}_${propName}`;
      if (ctx.classMethodSet.has(methodFullName) || ctx.staticMethodSet.has(methodFullName)) {
        const funcIdx = ctx.funcMap.get(methodFullName);
        if (funcIdx !== void 0) {
          const objResult = compileExpression(ctx, fctx, expr.expression);
          if (objResult) {
            fctx.body.push({ op: "drop" });
          }
          fctx.body.push({ op: "ref.null.extern" });
          return { kind: "externref" };
        }
      }
    }
    if (propName === "constructor" && ctx.classSet.has(typeName)) {
      const objResult = compileExpression(ctx, fctx, expr.expression);
      if (objResult) {
        fctx.body.push({ op: "drop" });
      }
      const ctorName = `${typeName}_constructor`;
      const funcIdx = ctx.funcMap.get(ctorName);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "ref.func", funcIdx });
        fctx.body.push({ op: "extern.convert_any" });
        return { kind: "externref" };
      }
      fctx.body.push({ op: "ref.null.extern" });
      return { kind: "externref" };
    }
    if (propName === "prototype" && ctx.classSet.has(typeName)) {
      const objResult = compileExpression(ctx, fctx, expr.expression);
      if (objResult) {
        fctx.body.push({ op: "drop" });
      }
      if (emitLazyProtoGet(ctx, fctx, typeName)) {
        return { kind: "externref" };
      }
      fctx.body.push({ op: "ref.null.extern" });
      return { kind: "externref" };
    }
    const structTypeIdx = ctx.structMap.get(typeName);
    const fields = ctx.structFields.get(typeName);
    if (structTypeIdx !== void 0 && fields) {
      const fieldIdx = fields.findIndex((f) => f.name === propName);
      if (fieldIdx !== -1) {
        const objResult = compileExpression(ctx, fctx, expr.expression);
        const fieldType = fields[fieldIdx].type;
        const exprNonNull = isProvablyNonNull(expr.expression, ctx.checker);
        if (objResult && objResult.kind === "ref_null") {
          emitNullGuardedStructGet(ctx, fctx, objResult, fieldType, structTypeIdx, fieldIdx, propName);
          if (fieldType.kind === "ref") {
            return { kind: "ref_null", typeIdx: fieldType.typeIdx };
          }
          return fieldType;
        } else if (objResult && objResult.kind === "externref") {
          emitExternrefToStructGet(
            ctx,
            fctx,
            fieldType,
            structTypeIdx,
            fieldIdx,
            propName,
            true,
            /* throwOnNull */
          );
          if (fieldType.kind === "ref") {
            return { kind: "ref_null", typeIdx: fieldType.typeIdx };
          }
          return fieldType;
        } else if (objResult && objResult.kind === "ref") {
          const nullableObj = { kind: "ref_null", typeIdx: objResult.typeIdx ?? structTypeIdx };
          emitNullGuardedStructGet(ctx, fctx, nullableObj, fieldType, structTypeIdx, fieldIdx, propName);
          if (fieldType.kind === "ref") {
            return { kind: "ref_null", typeIdx: fieldType.typeIdx };
          }
          return fieldType;
        } else {
          fctx.body.push({
            op: "struct.get",
            typeIdx: structTypeIdx,
            fieldIdx,
          });
        }
        return fieldType;
      }
      const protoFieldIdx = fields.findIndex((f) => f.name === "__proto__");
      if (protoFieldIdx !== -1) {
        const protoAccessType = ctx.checker.getTypeAtLocation(expr);
        const protoResultWasm = resolveWasmType(ctx, protoAccessType);
        const effectiveResult =
          protoResultWasm.kind === "f64" || protoResultWasm.kind === "i32" ? protoResultWasm : { kind: "externref" };
        const getIdx = ensureLateImport2(
          ctx,
          "__extern_get",
          [{ kind: "externref" }, { kind: "externref" }],
          [{ kind: "externref" }],
        );
        let unboxIdx;
        if (effectiveResult.kind === "f64" || effectiveResult.kind === "i32") {
          unboxIdx = ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
        }
        flushLateImportShifts2(ctx, fctx);
        if (getIdx !== void 0) {
          const objResult = compileExpression(ctx, fctx, expr.expression);
          const objLocal = allocLocal(fctx, `__pobj_${fctx.locals.length}`, { kind: "anyref" });
          if (objResult && objResult.kind === "externref") {
            fctx.body.push({ op: "any.convert_extern" });
          }
          fctx.body.push({ op: "local.set", index: objLocal });
          const protoLocal = allocLocal(fctx, `__proto_${fctx.locals.length}`, { kind: "externref" });
          fctx.body.push({ op: "local.get", index: objLocal });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              // Null object → null proto
              { op: "ref.null.extern" },
              { op: "local.set", index: protoLocal },
            ],
            else: [
              // Try to cast to expected struct type and get __proto__
              { op: "local.get", index: objLocal },
              { op: "ref.test", typeIdx: structTypeIdx },
              {
                op: "if",
                blockType: { kind: "empty" },
                then: [
                  { op: "local.get", index: objLocal },
                  { op: "ref.cast", typeIdx: structTypeIdx },
                  { op: "struct.get", typeIdx: structTypeIdx, fieldIdx: protoFieldIdx },
                  { op: "local.set", index: protoLocal },
                ],
                else: [
                  // Wrong struct type — try alternate structs that have __proto__
                  { op: "ref.null.extern" },
                  { op: "local.set", index: protoLocal },
                ],
              },
            ],
          });
          addStringConstantGlobal(ctx, propName);
          const strGlobalIdx = ctx.stringGlobalMap.get(propName);
          fctx.body.push({ op: "local.get", index: protoLocal });
          fctx.body.push({ op: "ref.is_null" });
          const protoDefaultInstrs = defaultValueInstrs(effectiveResult);
          fctx.body.push({
            op: "if",
            blockType: { kind: "val", type: effectiveResult },
            then: protoDefaultInstrs,
            else: [
              { op: "local.get", index: protoLocal },
              ...(strGlobalIdx !== void 0 ? [{ op: "global.get", index: strGlobalIdx }] : [{ op: "ref.null.extern" }]),
              { op: "call", funcIdx: getIdx },
              ...(effectiveResult.kind === "f64" && unboxIdx !== void 0
                ? [{ op: "call", funcIdx: unboxIdx }]
                : effectiveResult.kind === "i32" && unboxIdx !== void 0
                  ? [{ op: "call", funcIdx: unboxIdx }, { op: "i32.trunc_sat_f64_s" }]
                  : []),
            ],
          });
          return effectiveResult;
        }
      }
    }
  }
  const accessType = ctx.checker.getTypeAtLocation(expr);
  const accessWasm = resolveWasmType(ctx, accessType);
  if (typeName && !ctx.classSet.has(typeName)) {
    const props = objType.getProperties?.();
    if (props) {
      const tsProp = props.find((p) => p.name === propName);
      if (tsProp) {
        const propTsType = ctx.checker.getTypeOfSymbolAtLocation(tsProp, expr);
        const propWasmType = resolveWasmType(ctx, propTsType);
        const structTypeIdx = ctx.structMap.get(typeName);
        const fields = ctx.structFields.get(typeName);
        if (structTypeIdx !== void 0 && fields) {
          const typeDef = ctx.mod.types[structTypeIdx];
          if (typeDef?.kind === "struct") {
            const fieldType =
              propWasmType.kind === "ref" ? { kind: "ref_null", typeIdx: propWasmType.typeIdx } : propWasmType;
            const newField = { name: propName, type: fieldType, mutable: true };
            fields.push(newField);
            patchStructNewForAddedField(ctx, fctx, structTypeIdx, propWasmType);
            const fieldIdx = fields.length - 1;
            if (fieldIdx !== -1) {
              const fieldType2 = fields[fieldIdx].type;
              const objResult = compileExpression(ctx, fctx, expr.expression);
              const exprNonNull2 = isProvablyNonNull(expr.expression, ctx.checker);
              if (objResult && objResult.kind === "ref_null") {
                emitNullGuardedStructGet(ctx, fctx, objResult, fieldType2, structTypeIdx, fieldIdx, propName);
                if (fieldType2.kind === "ref") {
                  return { kind: "ref_null", typeIdx: fieldType2.typeIdx };
                }
                return fieldType2;
              } else if (objResult && objResult.kind === "externref") {
                emitExternrefToStructGet(ctx, fctx, fieldType2, structTypeIdx, fieldIdx, propName, true);
              } else if (objResult && objResult.kind === "ref") {
                const nullableObj = { kind: "ref_null", typeIdx: objResult.typeIdx ?? structTypeIdx };
                emitNullGuardedStructGet(ctx, fctx, nullableObj, fieldType2, structTypeIdx, fieldIdx, propName);
                if (fieldType2.kind === "ref") {
                  return { kind: "ref_null", typeIdx: fieldType2.typeIdx };
                }
              } else {
                fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
              }
              return fieldType2;
            }
          }
        }
      }
    }
  }
  {
    const objWasmType = resolveWasmType(ctx, objType);
    const isExternObj =
      objWasmType.kind === "externref" ||
      (ts12.isIdentifier(expr.expression) &&
        (() => {
          const localIdx = fctx.localMap.get(expr.expression.text);
          if (localIdx === void 0) return false;
          const localType =
            localIdx < fctx.params.length
              ? fctx.params[localIdx].type
              : fctx.locals[localIdx - fctx.params.length]?.type;
          return localType?.kind === "externref";
        })());
    if (isExternObj) {
      const getIdx = ensureLateImport2(
        ctx,
        "__extern_get",
        [{ kind: "externref" }, { kind: "externref" }],
        [{ kind: "externref" }],
      );
      let unboxIdx;
      if (accessWasm.kind === "f64" || accessWasm.kind === "i32") {
        unboxIdx = ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
      }
      flushLateImportShifts2(ctx, fctx);
      if (getIdx !== void 0) {
        const objExprType = compileExpression(ctx, fctx, expr.expression);
        if (objExprType && (objExprType.kind === "ref" || objExprType.kind === "ref_null")) {
          fctx.body.push({ op: "extern.convert_any" });
        }
        if (objExprType && objExprType.kind === "f64") {
          addUnionImports(ctx);
          const boxIdx = ctx.funcMap.get("__box_number");
          if (boxIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: boxIdx });
          }
        }
        if (objExprType && objExprType.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
          addUnionImports(ctx);
          const boxIdx = ctx.funcMap.get("__box_number");
          if (boxIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: boxIdx });
          }
        }
        const objTmp = allocLocal(fctx, `__nullchk_${fctx.locals.length}`, { kind: "externref" });
        fctx.body.push({ op: "local.tee", index: objTmp });
        fctx.body.push({ op: "ref.is_null" });
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: typeErrorThrowInstrs(ctx, expr),
          else: [],
        });
        const structCandidates = findAlternateStructsForField(ctx, propName, -1);
        if (structCandidates.length > 0) {
          const tmpAnyExt = allocLocal(fctx, `__sd_any_${fctx.locals.length}`, { kind: "anyref" });
          fctx.body.push({ op: "local.get", index: objTmp });
          fctx.body.push({ op: "any.convert_extern" });
          fctx.body.push({ op: "local.set", index: tmpAnyExt });
          const resultWasm =
            accessWasm.kind === "f64" || accessWasm.kind === "i32" ? accessWasm : { kind: "externref" };
          const resultLocal = allocLocal(fctx, `__sd_res_${fctx.locals.length}`, resultWasm);
          const externGetFallback = [{ op: "local.get", index: objTmp }];
          addStringConstantGlobal(ctx, propName);
          const strGlobalIdxExt = ctx.stringGlobalMap.get(propName);
          if (strGlobalIdxExt !== void 0) {
            externGetFallback.push({ op: "global.get", index: strGlobalIdxExt });
          } else {
            externGetFallback.push({ op: "ref.null.extern" });
          }
          externGetFallback.push({ op: "call", funcIdx: getIdx });
          if (resultWasm.kind === "f64" && unboxIdx !== void 0) {
            externGetFallback.push({ op: "call", funcIdx: unboxIdx });
          } else if (resultWasm.kind === "i32" && unboxIdx !== void 0) {
            externGetFallback.push({ op: "call", funcIdx: unboxIdx });
            externGetFallback.push({ op: "i32.trunc_sat_f64_s" });
          }
          externGetFallback.push({ op: "local.set", index: resultLocal });
          const buildStructDispatch = (idx) => {
            if (idx >= structCandidates.length) {
              return externGetFallback;
            }
            const cand = structCandidates[idx];
            const getFieldInstrs = [
              { op: "local.get", index: tmpAnyExt },
              { op: "ref.cast", typeIdx: cand.structTypeIdx },
              { op: "struct.get", typeIdx: cand.structTypeIdx, fieldIdx: cand.fieldIdx },
            ];
            const coerce = coercionInstrs(ctx, cand.fieldType, resultWasm, fctx);
            getFieldInstrs.push(...coerce);
            getFieldInstrs.push({ op: "local.set", index: resultLocal });
            return [
              { op: "local.get", index: tmpAnyExt },
              { op: "ref.test", typeIdx: cand.structTypeIdx },
              {
                op: "if",
                blockType: { kind: "empty" },
                then: getFieldInstrs,
                else: buildStructDispatch(idx + 1),
              },
            ];
          };
          fctx.body.push(...buildStructDispatch(0));
          fctx.body.push({ op: "local.get", index: resultLocal });
          if (accessWasm.kind === "f64") {
            return { kind: "f64" };
          }
          if (accessWasm.kind === "i32") {
            return { kind: "i32" };
          }
          return { kind: "externref" };
        }
        fctx.body.push({ op: "local.get", index: objTmp });
        addStringConstantGlobal(ctx, propName);
        compileStringLiteral(ctx, fctx, propName);
        fctx.body.push({ op: "call", funcIdx: getIdx });
        if (accessWasm.kind === "f64") {
          if (unboxIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: unboxIdx });
          }
          return { kind: "f64" };
        }
        if (accessWasm.kind === "i32") {
          if (unboxIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: unboxIdx });
          }
          fctx.body.push({ op: "i32.trunc_sat_f64_s" });
          return { kind: "i32" };
        }
        return { kind: "externref" };
      }
    }
  }
  if (accessWasm.kind === "f64" || accessWasm.kind === "i32") {
    fctx.body.push({ op: accessWasm.kind === "f64" ? "f64.const" : "i32.const", value: 0 });
    return accessWasm;
  }
  if (accessWasm.kind === "externref") {
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  if (accessWasm.kind === "ref" || accessWasm.kind === "ref_null") {
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  fctx.body.push({ op: "ref.null.extern" });
  return { kind: "externref" };
}
function compileExternPropertyGet(ctx, fctx, expr, objType, propName) {
  const className = objType.getSymbol()?.name;
  if (!className) return null;
  const resolvedInfo = findExternInfoForMember(ctx, className, propName, "property");
  const propOwner = resolvedInfo ?? ctx.externClasses.get(className);
  if (!propOwner) return null;
  const importName = `${propOwner.importPrefix}_get_${propName}`;
  const funcIdx = ctx.funcMap.get(importName);
  if (funcIdx === void 0) {
    return null;
  }
  compileExpression(ctx, fctx, expr.expression);
  fctx.body.push({ op: "call", funcIdx });
  const propInfo = propOwner.properties.get(propName);
  return propInfo?.type ?? { kind: "externref" };
}
function emitBoundsGuardedArraySet(fctx, vecLocal, vecTypeIdx, idxLocal, valLocal, arrTypeIdx) {
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "local.get", index: vecLocal });
  fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
  fctx.body.push({ op: "array.len" });
  fctx.body.push({ op: "i32.lt_u" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [
      { op: "local.get", index: vecLocal },
      { op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 },
      { op: "local.get", index: idxLocal },
      { op: "local.get", index: valLocal },
      { op: "array.set", typeIdx: arrTypeIdx },
    ],
    else: [],
  });
}
function isSafeBoundsEliminated(fctx, expr) {
  if (!fctx.safeIndexedArrays || fctx.safeIndexedArrays.size === 0) return false;
  if (!ts12.isIdentifier(expr.expression) || !ts12.isIdentifier(expr.argumentExpression)) return false;
  const arrayVar = expr.expression.text;
  const indexVar = expr.argumentExpression.text;
  return fctx.safeIndexedArrays.has(arrayVar + ":" + indexVar);
}
function compileElementAccess(ctx, fctx, expr) {
  if (expr.expression.kind === ts12.SyntaxKind.SuperKeyword) {
    return compileSuperElementAccess(ctx, fctx, expr);
  }
  if (ts12.isIdentifier(expr.expression)) {
    const objName = expr.expression.text;
    if (ctx.classSet.has(objName)) {
      const key = resolveComputedKeyExpression(ctx, expr.argumentExpression);
      if (key !== void 0) {
        const accessorKey = `${objName}_${key}`;
        if (ctx.classAccessorSet.has(accessorKey)) {
          const getterName = `${objName}_get_${key}`;
          const funcIdx = ctx.funcMap.get(getterName);
          if (funcIdx !== void 0) {
            const retType = emitGetterCallWithDummy(ctx, fctx, objName, getterName, funcIdx);
            return retType ?? { kind: "externref" };
          }
        }
        const fullName = `${objName}_${key}`;
        const globalIdx = ctx.staticProps.get(fullName);
        if (globalIdx !== void 0) {
          fctx.body.push({ op: "global.get", index: globalIdx });
          const globalDef = ctx.mod.globals[localGlobalIdx(ctx, globalIdx)];
          return globalDef?.type ?? { kind: "f64" };
        }
        if (ctx.staticMethodSet.has(fullName) || ctx.classMethodSet.has(fullName)) {
          const funcIdx = ctx.funcMap.get(fullName);
          if (funcIdx !== void 0) {
            fctx.body.push({ op: "ref.null.extern" });
            return { kind: "externref" };
          }
        }
      }
    }
  }
  if (
    ts12.isPropertyAccessExpression(expr.expression) &&
    ts12.isIdentifier(expr.expression.expression) &&
    expr.expression.name.text === "prototype"
  ) {
    const className = expr.expression.expression.text;
    if (ctx.classSet.has(className)) {
      const key = resolveComputedKeyExpression(ctx, expr.argumentExpression);
      if (key !== void 0) {
        const accessorKey = `${className}_${key}`;
        if (ctx.classAccessorSet.has(accessorKey) && !ctx.staticAccessorSet.has(accessorKey)) {
          const getterName = `${className}_get_${key}`;
          const funcIdx = ctx.funcMap.get(getterName);
          if (funcIdx !== void 0) {
            const retType = emitGetterCallWithDummy(ctx, fctx, className, getterName, funcIdx);
            return retType ?? { kind: "externref" };
          }
        }
      }
    }
  }
  const objType = compileExpression(ctx, fctx, expr.expression);
  if (!objType) return null;
  if (objType.kind === "ref_null") {
    if (!isProvablyNonNull(expr.expression, ctx.checker)) {
      emitNullCheckThrow2(ctx, fctx, objType, expr);
    }
    const nonNullObjType = { kind: "ref", typeIdx: objType.typeIdx };
    return compileElementAccessBody(ctx, fctx, expr, nonNullObjType);
  }
  if (objType.kind === "externref") {
    if (!isProvablyNonNull(expr.expression, ctx.checker)) {
      emitNullCheckThrow2(ctx, fctx, objType, expr);
    }
  }
  return compileElementAccessBody(ctx, fctx, expr, objType);
}
function compileElementAccessBody(ctx, fctx, expr, objType) {
  if (objType.kind === "externref") {
    compileExpression(ctx, fctx, expr.argumentExpression, { kind: "externref" });
    const funcIdx = ensureLateImport2(
      ctx,
      "__extern_get",
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "externref" };
    }
    return null;
  }
  if (objType.kind !== "ref" && objType.kind !== "ref_null") {
    if (objType.kind === "f64") {
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      } else {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "ref.null.extern" });
      }
    } else if (objType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      } else {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "ref.null.extern" });
      }
    } else {
      ctx.errors.push({
        message: "Element access on non-array value",
        line: getLine(expr),
        column: getCol(expr),
      });
      return null;
    }
    compileExpression(ctx, fctx, expr.argumentExpression, { kind: "externref" });
    const funcIdx = ensureLateImport2(
      ctx,
      "__extern_get",
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "externref" };
    }
    return null;
  }
  const typeIdx = objType.typeIdx;
  const typeDef = ctx.mod.types[typeIdx];
  if (typeDef?.kind === "struct") {
    const isVecStructAccess =
      typeDef.fields[0]?.name === "length" &&
      typeDef.fields[1]?.name === "data" &&
      (typeDef.fields.length === 2 || (typeDef.fields.length === 3 && typeDef.fields[2]?.name === "raw"));
    if (!isVecStructAccess) {
      const isTuple = Array.from(ctx.tupleTypeMap.values()).includes(typeIdx);
      if (isTuple) {
        if (!ts12.isNumericLiteral(expr.argumentExpression)) {
          ctx.errors.push({
            message: "Tuple element access requires a numeric literal index",
            line: getLine(expr),
            column: getCol(expr),
          });
          return null;
        }
        const fieldIdx = Number(expr.argumentExpression.text);
        if (fieldIdx < 0 || fieldIdx >= typeDef.fields.length) {
          ctx.errors.push({
            message: `Tuple index ${fieldIdx} out of bounds (tuple has ${typeDef.fields.length} elements)`,
            line: getLine(expr),
            column: getCol(expr),
          });
          return null;
        }
        fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
        return typeDef.fields[fieldIdx].type;
      }
      let fieldName;
      if (ts12.isStringLiteral(expr.argumentExpression)) {
        fieldName = expr.argumentExpression.text;
      } else if (ts12.isNumericLiteral(expr.argumentExpression)) {
        fieldName = expr.argumentExpression.text;
      } else if (ts12.isIdentifier(expr.argumentExpression)) {
        const sym = ctx.checker.getSymbolAtLocation(expr.argumentExpression);
        if (sym) {
          const decl = sym.valueDeclaration;
          if (decl && ts12.isVariableDeclaration(decl) && decl.initializer) {
            const declList = decl.parent;
            if (ts12.isVariableDeclarationList(declList) && (declList.flags & ts12.NodeFlags.Const) !== 0) {
              if (ts12.isStringLiteral(decl.initializer)) {
                fieldName = decl.initializer.text;
              } else if (ts12.isNumericLiteral(decl.initializer)) {
                fieldName = decl.initializer.text;
              }
            }
          }
        }
      }
      if (fieldName === void 0) {
        fieldName = resolveComputedKeyExpression(ctx, expr.argumentExpression);
      }
      if (fieldName !== void 0) {
        const objTsType = ctx.checker.getTypeAtLocation(expr.expression);
        const sName = resolveStructName(ctx, objTsType);
        if (sName) {
          const accessorKey = `${sName}_${fieldName}`;
          if (ctx.classAccessorSet.has(accessorKey)) {
            const getterName = `${sName}_get_${fieldName}`;
            const funcIdx = ctx.funcMap.get(getterName);
            if (funcIdx !== void 0) {
              fctx.body.push({ op: "call", funcIdx });
              const elGetterLocalIdx = funcIdx - ctx.numImportFuncs;
              const elGetterDef = elGetterLocalIdx >= 0 ? ctx.mod.functions[elGetterLocalIdx] : void 0;
              if (elGetterDef) {
                const elGetterType = ctx.mod.types[elGetterDef.typeIdx];
                if (elGetterType?.kind === "func" && elGetterType.results.length > 0) {
                  return elGetterType.results[0];
                }
              }
              const propType = ctx.checker.getTypeAtLocation(expr);
              return resolveWasmType(ctx, propType);
            }
          }
        }
        const fieldIdx = typeDef.fields.findIndex((f) => f.name === fieldName);
        if (fieldIdx >= 0) {
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
          return typeDef.fields[fieldIdx].type;
        }
      }
      fctx.body.push({ op: "extern.convert_any" });
      compileExpression(ctx, fctx, expr.argumentExpression, { kind: "externref" });
      {
        const funcIdx = ensureLateImport2(
          ctx,
          "__extern_get",
          [{ kind: "externref" }, { kind: "externref" }],
          [{ kind: "externref" }],
        );
        flushLateImportShifts2(ctx, fctx);
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          return { kind: "externref" };
        }
      }
      return null;
    }
    const arrTypeIdx = getArrTypeIdxFromVec(ctx, typeIdx);
    const arrDef = ctx.mod.types[arrTypeIdx];
    if (!arrDef || arrDef.kind !== "array") {
      ctx.errors.push({ message: "Element access: vec data is not array", line: 0, column: 0 });
      return null;
    }
    fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
    if (ctx.fast) {
      compileExpression(ctx, fctx, expr.argumentExpression, { kind: "i32" });
    } else {
      compileExpression(ctx, fctx, expr.argumentExpression, { kind: "f64" });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
    if (isSafeBoundsEliminated(fctx, expr)) {
      fctx.body.push({ op: "array.get", typeIdx: arrTypeIdx });
    } else {
      emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, arrDef.element);
    }
    return arrDef.element;
  }
  if (!typeDef || typeDef.kind !== "array") {
    ctx.errors.push({
      message: "Element access on non-array type",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  if (ctx.fast) {
    compileExpression(ctx, fctx, expr.argumentExpression, { kind: "i32" });
  } else {
    compileExpression(ctx, fctx, expr.argumentExpression, { kind: "f64" });
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  }
  if (isSafeBoundsEliminated(fctx, expr)) {
    fctx.body.push({ op: "array.get", typeIdx });
  } else {
    emitBoundsCheckedArrayGet2(fctx, typeIdx, typeDef.element);
  }
  return typeDef.element;
}

// src/codegen/expressions.ts
function emitThrowString2(ctx, fctx, message) {
  addStringConstantGlobal(ctx, message);
  const strIdx = ctx.stringGlobalMap.get(message);
  fctx.body.push({ op: "global.get", index: strIdx });
  const tagIdx = ensureExnTag(ctx);
  fctx.body.push({ op: "throw", tagIdx });
}
function isEffectivelyVoidReturn(ctx, retType, funcName) {
  if (isVoidType(retType)) return true;
  if (funcName && ctx.asyncFunctions.has(funcName)) {
    const unwrapped = unwrapPromiseType(retType, ctx.checker);
    if (isVoidType(unwrapped)) return true;
  }
  return false;
}
function wasmFuncReturnsVoid(ctx, funcIdx) {
  if (funcIdx < ctx.numImportFuncs) {
    let importFuncCount = 0;
    for (const imp of ctx.mod.imports) {
      if (imp.desc.kind === "func") {
        if (importFuncCount === funcIdx) {
          const typeDef = ctx.mod.types[imp.desc.typeIdx];
          return !typeDef || typeDef.kind !== "func" || typeDef.results.length === 0;
        }
        importFuncCount++;
      }
    }
    return true;
  }
  const localIdx = funcIdx - ctx.numImportFuncs;
  const func = ctx.mod.functions[localIdx];
  if (func) {
    const typeDef = ctx.mod.types[func.typeIdx];
    return !typeDef || typeDef.kind !== "func" || typeDef.results.length === 0;
  }
  return true;
}
function wasmFuncTypeReturnsVoid(ctx, typeIdx) {
  const typeDef = ctx.mod.types[typeIdx];
  return !typeDef || typeDef.kind !== "func" || typeDef.results.length === 0;
}
function _isLastInstrVoidCall(ctx, fctx, bodyLenBefore) {
  if (fctx.body.length <= bodyLenBefore) return true;
  const lastInstr = fctx.body[fctx.body.length - 1];
  if (!lastInstr) return false;
  const op = lastInstr.op;
  if (op === "call" && lastInstr.funcIdx !== void 0) {
    return wasmFuncReturnsVoid(ctx, lastInstr.funcIdx);
  }
  if (op === "call_ref" && lastInstr.typeIdx !== void 0) {
    return wasmFuncTypeReturnsVoid(ctx, lastInstr.typeIdx);
  }
  return false;
}
function getWasmFuncReturnType(ctx, funcIdx) {
  if (funcIdx < ctx.numImportFuncs) {
    let importFuncCount = 0;
    for (const imp of ctx.mod.imports) {
      if (imp.desc.kind === "func") {
        if (importFuncCount === funcIdx) {
          const typeDef = ctx.mod.types[imp.desc.typeIdx];
          if (typeDef?.kind === "func" && typeDef.results.length > 0) {
            return typeDef.results[0];
          }
          return void 0;
        }
        importFuncCount++;
      }
    }
    return void 0;
  }
  const localIdx = funcIdx - ctx.numImportFuncs;
  const func = ctx.mod.functions[localIdx];
  if (func) {
    const typeDef = ctx.mod.types[func.typeIdx];
    if (typeDef?.kind === "func" && typeDef.results.length > 0) {
      return typeDef.results[0];
    }
  }
  return void 0;
}
function shiftLateImportIndices(ctx, fctx, importsBefore, added) {
  if (added <= 0) return;
  function shiftInstrs(instrs) {
    walkInstructions(instrs, (instr) => {
      if ("funcIdx" in instr && typeof instr.funcIdx === "number") {
        if (instr.funcIdx >= importsBefore) {
          instr.funcIdx += added;
        }
      }
    });
  }
  const shifted = /* @__PURE__ */ new Set();
  for (const func of ctx.mod.functions) {
    if (!shifted.has(func.body)) {
      shiftInstrs(func.body);
      shifted.add(func.body);
    }
  }
  const curBody = fctx.body;
  if (!shifted.has(curBody)) {
    shiftInstrs(curBody);
    shifted.add(curBody);
  }
  for (const sb of fctx.savedBodies) {
    if (shifted.has(sb)) continue;
    shiftInstrs(sb);
    shifted.add(sb);
  }
  for (const parentFctx of ctx.funcStack) {
    if (!shifted.has(parentFctx.body)) {
      shiftInstrs(parentFctx.body);
      shifted.add(parentFctx.body);
    }
    for (const sb of parentFctx.savedBodies) {
      if (!shifted.has(sb)) {
        shiftInstrs(sb);
        shifted.add(sb);
      }
    }
  }
  for (const pb of ctx.parentBodiesStack) {
    if (!shifted.has(pb)) {
      shiftInstrs(pb);
      shifted.add(pb);
    }
  }
  if (ctx.pendingInitBody && !shifted.has(ctx.pendingInitBody)) {
    shiftInstrs(ctx.pendingInitBody);
    shifted.add(ctx.pendingInitBody);
  }
  const importNames = /* @__PURE__ */ new Set();
  for (const imp of ctx.mod.imports) {
    if (imp.desc.kind === "func") importNames.add(imp.name);
  }
  for (const [name, idx] of ctx.funcMap) {
    if (importNames.has(name)) continue;
    if (idx >= importsBefore) {
      ctx.funcMap.set(name, idx + added);
    }
  }
  for (const exp of ctx.mod.exports) {
    if (exp.desc.kind === "func" && exp.desc.index >= importsBefore) {
      exp.desc.index += added;
    }
  }
  for (const elem of ctx.mod.elements) {
    if (elem.funcIndices) {
      for (let i = 0; i < elem.funcIndices.length; i++) {
        if (elem.funcIndices[i] >= importsBefore) {
          elem.funcIndices[i] += added;
        }
      }
    }
  }
  if (ctx.mod.declaredFuncRefs.length > 0) {
    ctx.mod.declaredFuncRefs = ctx.mod.declaredFuncRefs.map((idx) => (idx >= importsBefore ? idx + added : idx));
  }
}
function ensureLateImport2(ctx, name, paramTypes, resultTypes) {
  const existing = ctx.funcMap.get(name);
  if (existing !== void 0) return existing;
  if (ctx.pendingLateImportShift === null) {
    ctx.pendingLateImportShift = { importsBefore: ctx.numImportFuncs };
  }
  const typeIdx = addFuncType(ctx, paramTypes, resultTypes);
  addImport(ctx, "env", name, { kind: "func", typeIdx });
  return ctx.funcMap.get(name);
}
function flushLateImportShifts2(ctx, fctx) {
  const pending = ctx.pendingLateImportShift;
  if (pending === null) return;
  const added = ctx.numImportFuncs - pending.importsBefore;
  ctx.pendingLateImportShift = null;
  if (added <= 0) return;
  shiftLateImportIndices(ctx, fctx, pending.importsBefore, added);
}
function ensureGetUndefined(ctx) {
  return ensureLateImport2(ctx, "__get_undefined", [], [{ kind: "externref" }]);
}
function emitUndefined(ctx, fctx) {
  const funcIdx = ensureGetUndefined(ctx);
  if (funcIdx !== void 0) {
    flushLateImportShifts2(ctx, fctx);
    fctx.body.push({ op: "call", funcIdx });
  } else {
    fctx.body.push({ op: "ref.null.extern" });
  }
}
function ensureExternIsUndefinedImport(ctx) {
  return ensureLateImport2(ctx, "__extern_is_undefined", [{ kind: "externref" }], [{ kind: "i32" }]);
}
function patchStructNewForAddedField(ctx, fctx, typeIdx, fieldType) {
  function defaultInstrFor(ft) {
    switch (ft.kind) {
      case "f64":
        return { op: "f64.const", value: 0 };
      case "i32":
        return { op: "i32.const", value: 0 };
      case "externref":
        return { op: "ref.null.extern" };
      case "ref":
      case "ref_null":
        return { op: "ref.null", typeIdx: ft.typeIdx };
      default:
        if (ft.kind === "i64") {
          return { op: "i64.const", value: 0n };
        }
        if (ft.kind === "eqref") {
          return { op: "ref.null.eq" };
        }
        return { op: "i32.const", value: 0 };
    }
  }
  function patchInstrs(instrs) {
    for (let i = instrs.length - 1; i >= 0; i--) {
      const instr = instrs[i];
      if (instr.op === "struct.new" && instr.typeIdx === typeIdx) {
        instrs.splice(i, 0, defaultInstrFor(fieldType));
      }
      if ("body" in instr && Array.isArray(instr.body)) {
        patchInstrs(instr.body);
      }
      if ("then" in instr && Array.isArray(instr.then)) {
        patchInstrs(instr.then);
      }
      if ("else" in instr && Array.isArray(instr.else)) {
        patchInstrs(instr.else);
      }
      if ("catches" in instr && Array.isArray(instr.catches)) {
        for (const c of instr.catches) {
          if (Array.isArray(c.body)) patchInstrs(c.body);
        }
      }
      if ("catchAll" in instr && Array.isArray(instr.catchAll)) {
        patchInstrs(instr.catchAll);
      }
    }
  }
  const patched = /* @__PURE__ */ new Set();
  for (const func of ctx.mod.functions) {
    patchInstrs(func.body);
    patched.add(func.body);
  }
  if (!patched.has(fctx.body)) {
    patchInstrs(fctx.body);
    patched.add(fctx.body);
  }
  for (const sb of fctx.savedBodies) {
    if (!patched.has(sb)) {
      patchInstrs(sb);
      patched.add(sb);
    }
  }
}
var __compileDepth = 0;
var MAX_COMPILE_DEPTH = 500;
function resetCompileDepth() {
  __compileDepth = 0;
}
function compileExpression2(ctx, fctx, expr, expectedType) {
  __compileDepth++;
  if (__compileDepth > MAX_COMPILE_DEPTH) {
    __compileDepth--;
    ctx.errors.push({
      message: `compilation depth exceeded (${MAX_COMPILE_DEPTH}) \u2014 possible infinite recursion`,
      line: 0,
      column: 0,
    });
    const fallbackType = expectedType ?? { kind: "externref" };
    if (fallbackType.kind === "f64") fctx.body.push({ op: "f64.const", value: 0 });
    else if (fallbackType.kind === "i32") fctx.body.push({ op: "i32.const", value: 0 });
    else fctx.body.push({ op: "ref.null.extern" });
    return fallbackType;
  }
  try {
    return compileExpressionBody(ctx, fctx, expr, expectedType);
  } finally {
    __compileDepth--;
  }
}
function compileExpressionBody(ctx, fctx, expr, expectedType) {
  if (!expr) {
    ctx.errors.push({
      message: "unexpected undefined AST node in compileExpression",
      line: 0,
      column: 0,
    });
    const fallbackType = expectedType ?? { kind: "f64" };
    pushDefaultValue(fctx, fallbackType, ctx);
    return fallbackType;
  }
  if (expectedType?.kind === "f64" || expectedType?.kind === "i32") {
    let inner = expr;
    while (
      ts13.isAsExpression(inner) ||
      ts13.isNonNullExpression(inner) ||
      ts13.isParenthesizedExpression(inner) ||
      ts13.isTypeAssertionExpression(inner)
    ) {
      inner = ts13.isParenthesizedExpression(inner)
        ? inner.expression
        : ts13.isAsExpression(inner)
          ? inner.expression
          : ts13.isNonNullExpression(inner)
            ? inner.expression
            : inner.expression;
    }
    const isNull = inner.kind === ts13.SyntaxKind.NullKeyword;
    const isUndefined =
      inner.kind === ts13.SyntaxKind.UndefinedKeyword ||
      (ts13.isIdentifier(inner) && inner.text === "undefined") ||
      ts13.isOmittedExpression(inner);
    if (isNull || isUndefined) {
      if (expectedType.kind === "f64") {
        fctx.body.push({ op: "f64.const", value: isNull ? 0 : NaN });
        return { kind: "f64" };
      }
      if (expectedType.kind === "i32") {
        fctx.body.push({ op: "i32.const", value: 0 });
        return { kind: "i32" };
      }
    }
    if (ts13.isVoidExpression(inner)) {
      const bodyLenBefore2 = fctx.body.length;
      const operandType = compileExpressionInner(ctx, fctx, inner.expression);
      if (operandType !== null && operandType !== VOID_RESULT) {
        if (!_isLastInstrVoidCall(ctx, fctx, bodyLenBefore2)) {
          fctx.body.push({ op: "drop" });
        }
      }
      if (expectedType.kind === "f64") {
        fctx.body.push({ op: "f64.const", value: NaN });
        return { kind: "f64" };
      }
      fctx.body.push({ op: "i32.const", value: 0 });
      return { kind: "i32" };
    }
  }
  if (expectedType && (expectedType.kind === "ref_null" || expectedType.kind === "ref")) {
    let inner = expr;
    while (
      ts13.isAsExpression(inner) ||
      ts13.isNonNullExpression(inner) ||
      ts13.isParenthesizedExpression(inner) ||
      ts13.isTypeAssertionExpression(inner)
    ) {
      inner = ts13.isParenthesizedExpression(inner)
        ? inner.expression
        : ts13.isAsExpression(inner)
          ? inner.expression
          : ts13.isNonNullExpression(inner)
            ? inner.expression
            : inner.expression;
    }
    const isNull = inner.kind === ts13.SyntaxKind.NullKeyword;
    const isUndefined =
      inner.kind === ts13.SyntaxKind.UndefinedKeyword ||
      (ts13.isIdentifier(inner) && inner.text === "undefined") ||
      ts13.isOmittedExpression(inner);
    if (isNull || isUndefined) {
      const typeIdx = expectedType.typeIdx;
      fctx.body.push({ op: "ref.null", typeIdx });
      return { kind: "ref_null", typeIdx };
    }
  }
  if (expectedType && isAnyValue(expectedType, ctx)) {
    let inner = expr;
    while (
      ts13.isAsExpression(inner) ||
      ts13.isNonNullExpression(inner) ||
      ts13.isParenthesizedExpression(inner) ||
      ts13.isTypeAssertionExpression(inner)
    ) {
      inner = ts13.isParenthesizedExpression(inner)
        ? inner.expression
        : ts13.isAsExpression(inner)
          ? inner.expression
          : ts13.isNonNullExpression(inner)
            ? inner.expression
            : inner.expression;
    }
    const isNull = inner.kind === ts13.SyntaxKind.NullKeyword;
    const isUndefined =
      inner.kind === ts13.SyntaxKind.UndefinedKeyword ||
      (ts13.isIdentifier(inner) && inner.text === "undefined") ||
      ts13.isOmittedExpression(inner);
    if (isNull || isUndefined) {
      ensureAnyHelpers(ctx);
      const helperName = isNull ? "__any_box_null" : "__any_box_undefined";
      const funcIdx = ctx.funcMap.get(helperName);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return expectedType;
      }
    }
    if (ts13.isVoidExpression(inner)) {
      const bodyLenBefore2 = fctx.body.length;
      const operandType = compileExpressionInner(ctx, fctx, inner.expression);
      if (operandType !== null && operandType !== VOID_RESULT) {
        if (!_isLastInstrVoidCall(ctx, fctx, bodyLenBefore2)) {
          fctx.body.push({ op: "drop" });
        }
      }
      ensureAnyHelpers(ctx);
      const funcIdx = ctx.funcMap.get("__any_box_undefined");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return expectedType;
      }
    }
    if (inner.kind === ts13.SyntaxKind.TrueKeyword || inner.kind === ts13.SyntaxKind.FalseKeyword) {
      ensureAnyHelpers(ctx);
      const funcIdx = ctx.funcMap.get("__any_box_bool");
      if (funcIdx !== void 0) {
        fctx.body.push({
          op: "i32.const",
          value: inner.kind === ts13.SyntaxKind.TrueKeyword ? 1 : 0,
        });
        fctx.body.push({ op: "call", funcIdx });
        return expectedType;
      }
    }
  }
  const bodyLenBefore = fctx.body.length;
  let result;
  try {
    result = compileExpressionInner(ctx, fctx, expr);
  } catch (e) {
    fctx.body.length = bodyLenBefore;
    const msg = e instanceof Error ? e.message : String(e);
    ctx.errors.push({
      message: `Internal error compiling expression: ${msg}`,
      line: getLine(expr),
      column: getCol(expr),
    });
    const fallbackType = expectedType ?? { kind: "f64" };
    pushDefaultValue(fctx, fallbackType, ctx);
    return fallbackType;
  }
  if (result === VOID_RESULT) {
    if (expectedType) {
      if (expectedType.kind === "externref" && ts13.isCallExpression(expr)) {
        const tsType = ctx.checker.getTypeAtLocation(expr);
        const symName = tsType.getSymbol()?.name;
        if (symName === "Promise") {
          let resolveIdx = ctx.funcMap.get("Promise_resolve");
          if (resolveIdx === void 0) {
            resolveIdx = ensureLateImport2(ctx, "Promise_resolve", [{ kind: "externref" }], [{ kind: "externref" }]);
            flushLateImportShifts2(ctx, fctx);
            resolveIdx = ctx.funcMap.get("Promise_resolve") ?? resolveIdx;
          }
          if (resolveIdx !== void 0) {
            fctx.body.push({ op: "ref.null.extern" });
            fctx.body.push({ op: "call", funcIdx: resolveIdx });
            return expectedType;
          }
        }
      }
      if (expectedType.kind === "f64") {
        fctx.body.push({ op: "f64.const", value: NaN });
      } else {
        pushDefaultValue(fctx, expectedType, ctx);
      }
      return expectedType;
    }
    return null;
  }
  if (result !== null && fctx.body.length > bodyLenBefore) {
    const lastInstr = fctx.body[fctx.body.length - 1];
    if (lastInstr) {
      const op = lastInstr.op;
      let isVoidCall = false;
      if (op === "call" && lastInstr.funcIdx !== void 0) {
        isVoidCall = wasmFuncReturnsVoid(ctx, lastInstr.funcIdx);
      } else if (op === "call_ref" && lastInstr.typeIdx !== void 0) {
        isVoidCall = wasmFuncTypeReturnsVoid(ctx, lastInstr.typeIdx);
      }
      if (isVoidCall) {
        if (expectedType) {
          pushDefaultValue(fctx, expectedType, ctx);
          return expectedType;
        }
        return null;
      }
    }
  }
  if (result !== null && (typeof result !== "object" || result === null || !("kind" in result))) {
    const fallbackType = expectedType ?? { kind: "f64" };
    pushDefaultValue(fctx, fallbackType, ctx);
    return fallbackType;
  }
  if (result !== null) {
    if (expectedType && result.kind !== expectedType.kind) {
      if (result.kind === "i32" && isAnyValue(expectedType, ctx)) {
        const tsType = ctx.checker.getTypeAtLocation(expr);
        if (tsType.flags & ts13.TypeFlags.BooleanLike) {
          ensureAnyHelpers(ctx);
          const funcIdx = ctx.funcMap.get("__any_box_bool");
          if (funcIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx });
            return expectedType;
          }
        }
      }
      if (result.kind === "i32" && expectedType.kind === "externref") {
        const tsType = ctx.checker.getTypeAtLocation(expr);
        if (tsType.flags & ts13.TypeFlags.ESSymbolLike) {
          const boxSymIdx = ensureLateImport2(ctx, "__box_symbol", [{ kind: "i32" }], [{ kind: "externref" }]);
          if (boxSymIdx !== void 0) {
            flushLateImportShifts2(ctx, fctx);
            fctx.body.push({ op: "call", funcIdx: boxSymIdx });
            return expectedType;
          }
        }
      }
      coerceType3(ctx, fctx, result, expectedType);
      return expectedType;
    }
    if (
      expectedType &&
      (result.kind === "ref" || result.kind === "ref_null") &&
      (expectedType.kind === "ref" || expectedType.kind === "ref_null")
    ) {
      const resultIdx = result.typeIdx;
      const expectedIdx = expectedType.typeIdx;
      if (resultIdx !== expectedIdx) {
        coerceType3(ctx, fctx, result, expectedType);
        return expectedType;
      }
    }
    return result;
  }
  fctx.body.length = bodyLenBefore;
  let wasmType;
  if (expectedType) {
    wasmType = expectedType;
  } else {
    try {
      wasmType = mapTsTypeToWasm(ctx.checker.getTypeAtLocation(expr), ctx.checker);
    } catch {
      wasmType = { kind: "f64" };
    }
  }
  pushDefaultValue(fctx, wasmType, ctx);
  return wasmType;
}
function emitCoercedLocalSet(ctx, fctx, localIdx, stackType) {
  const localType = getLocalType(fctx, localIdx);
  if (localType && !valTypesMatch(stackType, localType)) {
    const sameRefTypeIdx =
      (stackType.kind === "ref" || stackType.kind === "ref_null") &&
      (localType.kind === "ref" || localType.kind === "ref_null") &&
      stackType.typeIdx === localType.typeIdx;
    if (sameRefTypeIdx && stackType.kind === "ref_null" && localType.kind === "ref") {
      widenLocalToNullable(fctx, localIdx);
    } else if (sameRefTypeIdx) {
    } else if (
      (stackType.kind === "ref" || stackType.kind === "ref_null") &&
      (localType.kind === "ref" || localType.kind === "ref_null")
    ) {
      const bodyLenBefore = fctx.body.length;
      coerceType3(ctx, fctx, stackType, localType);
      if (fctx.body.length === bodyLenBefore) {
        updateLocalType(fctx, localIdx, stackType);
      }
    } else {
      coerceType3(ctx, fctx, stackType, localType);
    }
  }
  fctx.body.push({ op: "local.set", index: localIdx });
}
function updateLocalType(fctx, localIdx, newType) {
  if (localIdx < fctx.params.length) {
    const param = fctx.params[localIdx];
    if (param) param.type = newType;
  } else {
    const local = fctx.locals[localIdx - fctx.params.length];
    if (local) local.type = newType;
  }
}
function widenLocalToNullable(fctx, localIdx) {
  if (localIdx < fctx.params.length) {
    const param = fctx.params[localIdx];
    if (param && param.type.kind === "ref") {
      param.type = {
        kind: "ref_null",
        typeIdx: param.type.typeIdx,
      };
    }
  } else {
    const local = fctx.locals[localIdx - fctx.params.length];
    if (local && local.type.kind === "ref") {
      local.type = {
        kind: "ref_null",
        typeIdx: local.type.typeIdx,
      };
    }
  }
}
function coerceType3(ctx, fctx, from, to, toPrimitiveHint) {
  return coerceType2(ctx, fctx, from, to, toPrimitiveHint);
}
function compileExpressionInner(ctx, fctx, expr) {
  if (ts13.isNumericLiteral(expr)) {
    const value = Number(expr.text.replace(/_/g, ""));
    if (ctx.fast && Number.isInteger(value) && value >= -2147483648 && value <= 2147483647) {
      fctx.body.push({ op: "i32.const", value });
      return { kind: "i32" };
    }
    fctx.body.push({ op: "f64.const", value });
    return { kind: "f64" };
  }
  if (ts13.isBigIntLiteral(expr)) {
    const text = expr.text.replace(/_/g, "").replace(/n$/i, "");
    const value = BigInt(text);
    fctx.body.push({ op: "i64.const", value });
    return { kind: "i64" };
  }
  if (ts13.isStringLiteral(expr) || ts13.isNoSubstitutionTemplateLiteral(expr)) {
    return compileStringLiteral(ctx, fctx, expr.text, expr);
  }
  if (ts13.isTemplateExpression(expr)) {
    return compileTemplateExpression(ctx, fctx, expr);
  }
  if (expr.kind === ts13.SyntaxKind.TrueKeyword) {
    fctx.body.push({ op: "i32.const", value: 1 });
    return { kind: "i32" };
  }
  if (expr.kind === ts13.SyntaxKind.FalseKeyword) {
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  if (expr.kind === ts13.SyntaxKind.NullKeyword) {
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  if (expr.kind === ts13.SyntaxKind.UndefinedKeyword) {
    emitUndefined(ctx, fctx);
    return { kind: "externref" };
  }
  if (ts13.isIdentifier(expr) && expr.text === "undefined") {
    emitUndefined(ctx, fctx);
    return { kind: "externref" };
  }
  if (ts13.isOmittedExpression(expr)) {
    emitUndefined(ctx, fctx);
    return { kind: "externref" };
  }
  if (expr.kind === ts13.SyntaxKind.ThisKeyword) {
    const selfIdx = fctx.localMap.get("this");
    if (selfIdx !== void 0) {
      fctx.body.push({ op: "local.get", index: selfIdx });
      if (selfIdx < fctx.params.length) {
        return fctx.params[selfIdx].type;
      }
      const localDef = fctx.locals[selfIdx - fctx.params.length];
      return localDef?.type ?? { kind: "externref" };
    }
    emitUndefined(ctx, fctx);
    return { kind: "externref" };
  }
  if (ts13.isIdentifier(expr)) {
    return compileIdentifier(ctx, fctx, expr);
  }
  if (ts13.isBinaryExpression(expr)) {
    if (expr.operatorToken.kind === ts13.SyntaxKind.InstanceOfKeyword) {
      const rhsResult = resolveInstanceOfRHS(ctx, expr.right);
      if (!rhsResult) {
        return compileHostInstanceOf(ctx, fctx, expr);
      }
    }
    return compileBinaryExpression(ctx, fctx, expr);
  }
  if (ts13.isTypeOfExpression(expr)) {
    return compileTypeofExpression(ctx, fctx, expr);
  }
  if (ts13.isPrefixUnaryExpression(expr)) {
    return compilePrefixUnary(ctx, fctx, expr);
  }
  if (ts13.isPostfixUnaryExpression(expr)) {
    return compilePostfixUnary(ctx, fctx, expr);
  }
  if (ts13.isParenthesizedExpression(expr)) {
    return compileExpressionInner(ctx, fctx, expr.expression);
  }
  if (ts13.isCallExpression(expr)) {
    const callResult = compileCallExpression(ctx, fctx, expr);
    if (fctx.pendingCallbackWritebacks && fctx.pendingCallbackWritebacks.length > 0) {
      fctx.body.push(...fctx.pendingCallbackWritebacks);
      fctx.pendingCallbackWritebacks = void 0;
    }
    return callResult;
  }
  if (ts13.isNewExpression(expr)) {
    return compileNewExpression(ctx, fctx, expr);
  }
  if (ts13.isConditionalExpression(expr)) {
    return compileConditionalExpression(ctx, fctx, expr);
  }
  if (ts13.isPropertyAccessExpression(expr)) {
    return compilePropertyAccess(ctx, fctx, expr);
  }
  if (ts13.isElementAccessExpression(expr)) {
    return compileElementAccess(ctx, fctx, expr);
  }
  if (ts13.isObjectLiteralExpression(expr)) {
    return compileObjectLiteral(ctx, fctx, expr);
  }
  if (ts13.isArrayLiteralExpression(expr)) {
    return compileArrayLiteral(ctx, fctx, expr);
  }
  if (ts13.isAsExpression(expr)) {
    return compileExpressionInner(ctx, fctx, expr.expression);
  }
  if (ts13.isNonNullExpression(expr)) {
    return compileExpressionInner(ctx, fctx, expr.expression);
  }
  if (ts13.isAwaitExpression(expr)) {
    return compileExpressionInner(ctx, fctx, expr.expression);
  }
  if (ts13.isYieldExpression(expr)) {
    return compileYieldExpression(ctx, fctx, expr);
  }
  if (ts13.isVoidExpression(expr)) {
    const voidBodyLen = fctx.body.length;
    const operandType = compileExpressionInner(ctx, fctx, expr.expression);
    if (operandType !== null && operandType !== VOID_RESULT) {
      if (!_isLastInstrVoidCall(ctx, fctx, voidBodyLen)) {
        fctx.body.push({ op: "drop" });
      }
    }
    emitUndefined(ctx, fctx);
    return { kind: "externref" };
  }
  if (ts13.isDeleteExpression(expr)) {
    return compileDeleteExpression(ctx, fctx, expr);
  }
  if (ts13.isArrowFunction(expr) || ts13.isFunctionExpression(expr)) {
    return compileArrowFunction(ctx, fctx, expr);
  }
  if (ts13.isMetaProperty(expr) && expr.keywordToken === ts13.SyntaxKind.NewKeyword && expr.name.text === "target") {
    if (fctx.isConstructor) {
      fctx.body.push({ op: "i32.const", value: 1 });
      return { kind: "i32" };
    } else {
      emitUndefined(ctx, fctx);
      return { kind: "externref" };
    }
  }
  if (ts13.isMetaProperty(expr) && expr.keywordToken === ts13.SyntaxKind.ImportKeyword && expr.name.text === "meta") {
    return compileStringLiteral(ctx, fctx, "[object Object]");
  }
  if (ts13.isMetaProperty(expr) && expr.keywordToken === ts13.SyntaxKind.ImportKeyword) {
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  if (expr.kind === ts13.SyntaxKind.RegularExpressionLiteral) {
    return compileRegExpLiteral(ctx, fctx, expr);
  }
  if (ts13.isTaggedTemplateExpression(expr)) {
    return compileTaggedTemplateExpression(ctx, fctx, expr);
  }
  if (ts13.isClassExpression(expr)) {
    return compileClassExpression(ctx, fctx, expr);
  }
  if (ts13.isPrivateIdentifier(expr)) {
    fctx.body.push({ op: "i32.const", value: 1 });
    return { kind: "i32" };
  }
  if (expr.kind === ts13.SyntaxKind.SuperKeyword) {
    const selfIdx = fctx.localMap.get("this");
    if (selfIdx !== void 0) {
      fctx.body.push({ op: "local.get", index: selfIdx });
      const selfType = fctx.locals[selfIdx];
      if (selfType) return selfType.type;
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  if (ts13.isSpreadElement(expr)) {
    return compileExpressionInner(ctx, fctx, expr.expression);
  }
  ctx.errors.push({
    message: `Unsupported expression: ${ts13.SyntaxKind[expr.kind]}`,
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}
function emitLocalTdzCheck(ctx, fctx, _name, flagIdx) {
  const tagIdx = ensureExnTag(ctx);
  fctx.body.push({ op: "local.get", index: flagIdx });
  fctx.body.push({ op: "i32.eqz" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [{ op: "ref.null.extern" }, { op: "throw", tagIdx }],
    else: [],
  });
}
function analyzeTdzAccess(ctx, id) {
  const symbol = ctx.checker.getSymbolAtLocation(id);
  if (!symbol) return "check";
  const decl = symbol.valueDeclaration;
  if (!decl) return "check";
  const accessPos = id.getStart();
  const declEnd = decl.getEnd();
  const accessFunc = getContainingFunction(id);
  const declFunc = getContainingFunction(decl);
  if (accessFunc !== declFunc) {
    if (accessFunc && !ts13.isFunctionDeclaration(accessFunc) && !ts13.isSourceFile(accessFunc)) {
      const closureStart = accessFunc.getStart();
      if (closureStart >= declEnd && !isInsideLoopContaining(accessFunc, decl)) {
        return "skip";
      }
    }
    return "check";
  }
  if (isInsideLoopContaining(id, decl)) return "check";
  if (accessPos >= declEnd) {
    return "skip";
  } else {
    return "throw";
  }
}
function getContainingFunction(node) {
  let current = node.parent;
  while (current) {
    if (
      ts13.isFunctionDeclaration(current) ||
      ts13.isFunctionExpression(current) ||
      ts13.isArrowFunction(current) ||
      ts13.isMethodDeclaration(current) ||
      ts13.isConstructorDeclaration(current) ||
      ts13.isGetAccessorDeclaration(current) ||
      ts13.isSetAccessorDeclaration(current) ||
      ts13.isSourceFile(current)
    ) {
      return current;
    }
    current = current.parent;
  }
  return void 0;
}
function isInsideLoopContaining(access, decl) {
  let current = access.parent;
  while (current) {
    if (
      ts13.isFunctionDeclaration(current) ||
      ts13.isFunctionExpression(current) ||
      ts13.isArrowFunction(current) ||
      ts13.isMethodDeclaration(current) ||
      ts13.isSourceFile(current)
    ) {
      return false;
    }
    if (isLoopStatement(current)) {
      if (isDescendantOf(decl, current)) {
        const body = getLoopBody(current);
        if (body && isDescendantOf(decl, body) && access.getStart() >= decl.getEnd()) {
          return false;
        }
        return true;
      }
    }
    current = current.parent;
  }
  return false;
}
function getLoopBody(loop) {
  if (ts13.isForStatement(loop)) return loop.statement;
  if (ts13.isForInStatement(loop)) return loop.statement;
  if (ts13.isForOfStatement(loop)) return loop.statement;
  if (ts13.isWhileStatement(loop)) return loop.statement;
  if (ts13.isDoStatement(loop)) return loop.statement;
  return void 0;
}
function isLoopStatement(node) {
  return (
    ts13.isForStatement(node) ||
    ts13.isForInStatement(node) ||
    ts13.isForOfStatement(node) ||
    ts13.isWhileStatement(node) ||
    ts13.isDoStatement(node)
  );
}
function isDescendantOf(node, ancestor) {
  let current = node;
  while (current) {
    if (current === ancestor) return true;
    current = current.parent;
  }
  return false;
}
function analyzeTdzAccessByPos(ctx, varName, callNode) {
  const sourceFile = callNode.getSourceFile();
  if (!sourceFile) return "check";
  const sym = ctx.checker.getSymbolsInScope(callNode, ts13.SymbolFlags.Variable).find((s) => s.name === varName);
  if (!sym) return "check";
  const decl = sym.valueDeclaration;
  if (!decl) return "check";
  const callPos = callNode.getStart();
  const declEnd = decl.getEnd();
  const callFunc = getContainingFunction(callNode);
  const declFunc = getContainingFunction(decl);
  if (callFunc !== declFunc) return "check";
  if (isInsideLoopContaining(callNode, decl)) return "check";
  if (callPos >= declEnd) {
    return "skip";
  } else {
    return "throw";
  }
}
function emitStaticTdzThrow(ctx, fctx, name) {
  const tagIdx = ensureExnTag(ctx);
  fctx.body.push({ op: "ref.null.extern" });
  fctx.body.push({ op: "throw", tagIdx });
}
function compileIdentifier(ctx, fctx, id) {
  const name = id.text;
  const localIdx = fctx.localMap.get(name);
  if (localIdx !== void 0) {
    const tdzFlagIdx = fctx.tdzFlagLocals?.get(name);
    if (tdzFlagIdx !== void 0) {
      const tdzResult = analyzeTdzAccess(ctx, id);
      if (tdzResult === "check") {
        emitLocalTdzCheck(ctx, fctx, name, tdzFlagIdx);
      } else if (tdzResult === "throw") {
        emitStaticTdzThrow(ctx, fctx, id.text);
      }
    }
    const boxed = fctx.boxedCaptures?.get(name);
    if (boxed) {
      fctx.body.push({ op: "local.get", index: localIdx });
      emitNullGuardedStructGet(
        ctx,
        fctx,
        { kind: "ref_null", typeIdx: boxed.refCellTypeIdx },
        boxed.valType,
        boxed.refCellTypeIdx,
        0,
        void 0,
        false,
      );
      return boxed.valType;
    }
    fctx.body.push({ op: "local.get", index: localIdx });
    let declaredType;
    if (localIdx < fctx.params.length) {
      declaredType = fctx.params[localIdx].type;
    } else {
      const localDef = fctx.locals[localIdx - fctx.params.length];
      declaredType = localDef?.type ?? { kind: "f64" };
    }
    if (declaredType.kind === "externref") {
      const narrowedType = ctx.checker.getTypeAtLocation(id);
      const narrowed = narrowTypeToUnbox(ctx, fctx, narrowedType);
      if (narrowed) return narrowed;
    }
    if (declaredType.kind === "ref_null" && fctx.narrowedNonNull?.has(name)) {
      fctx.body.push({ op: "ref.as_non_null" });
      return { kind: "ref", typeIdx: declaredType.typeIdx };
    }
    return declaredType;
  }
  const capturedIdx = ctx.capturedGlobals.get(name);
  if (capturedIdx !== void 0) {
    const tdzResult = ctx.tdzGlobals.has(name) ? analyzeTdzAccess(ctx, id) : "skip";
    if (tdzResult === "check") {
      emitTdzCheck(ctx, fctx, name);
    } else if (tdzResult === "throw") {
      emitStaticTdzThrow(ctx, fctx, id.text);
    }
    fctx.body.push({ op: "global.get", index: capturedIdx });
    const globalDef = ctx.mod.globals[localGlobalIdx(ctx, capturedIdx)];
    const gType = globalDef?.type ?? { kind: "f64" };
    if (gType.kind === "ref_null" && (ctx.capturedGlobalsWidened.has(name) || fctx.narrowedNonNull?.has(name))) {
      fctx.body.push({ op: "ref.as_non_null" });
      return { kind: "ref", typeIdx: gType.typeIdx };
    }
    return gType;
  }
  const moduleIdx = ctx.moduleGlobals.get(name);
  if (moduleIdx !== void 0) {
    const tdzResult = ctx.tdzGlobals.has(name) ? analyzeTdzAccess(ctx, id) : "skip";
    if (tdzResult === "check") {
      emitTdzCheck(ctx, fctx, name);
    } else if (tdzResult === "throw") {
      emitStaticTdzThrow(ctx, fctx, id.text);
    }
    fctx.body.push({ op: "global.get", index: moduleIdx });
    const globalDef = ctx.mod.globals[localGlobalIdx(ctx, moduleIdx)];
    const mType = globalDef?.type ?? { kind: "f64" };
    if (mType.kind === "ref_null" && fctx.narrowedNonNull?.has(name)) {
      fctx.body.push({ op: "ref.as_non_null" });
      return { kind: "ref", typeIdx: mType.typeIdx };
    }
    return mType;
  }
  const globalInfo = ctx.declaredGlobals.get(name);
  if (globalInfo) {
    fctx.body.push({ op: "call", funcIdx: globalInfo.funcIdx });
    return globalInfo.type;
  }
  if (name === "globalThis") {
    let funcIdx = ctx.funcMap.get("__get_globalThis");
    if (funcIdx === void 0) {
      const typeIdx = addFuncType(ctx, [], [{ kind: "externref" }]);
      addImport(ctx, "env", "__get_globalThis", { kind: "func", typeIdx });
      funcIdx = ctx.funcMap.get("__get_globalThis");
    }
    fctx.body.push({ op: "call", funcIdx });
    return { kind: "externref" };
  }
  if (name === "NaN") {
    fctx.body.push({ op: "f64.const", value: NaN });
    return { kind: "f64" };
  }
  if (name === "Infinity") {
    fctx.body.push({ op: "f64.const", value: Infinity });
    return { kind: "f64" };
  }
  const funcRefIdx = ctx.funcMap.get(name);
  if (funcRefIdx !== void 0 && !name.startsWith("__") && !ctx.classSet.has(name)) {
    const existingClosure = ctx.closureMap.get(name);
    if (existingClosure) {
      const closureModGlobal = ctx.moduleGlobals.get(name);
      if (closureModGlobal !== void 0) {
        fctx.body.push({ op: "global.get", index: closureModGlobal });
        const globalDef = ctx.mod.globals[localGlobalIdx(ctx, closureModGlobal)];
        return (
          globalDef?.type ?? {
            kind: "ref",
            typeIdx: existingClosure.structTypeIdx,
          }
        );
      }
    }
    const refType = emitFuncRefAsClosure(ctx, fctx, name, funcRefIdx);
    if (refType) return refType;
  }
  const sym = ctx.checker.getSymbolAtLocation(id);
  if (!sym) {
    const tagIdx = ensureExnTag(ctx);
    fctx.body.push({ op: "ref.null.extern" });
    fctx.body.push({ op: "throw", tagIdx });
    return { kind: "externref" };
  }
  const tsType = ctx.checker.getTypeAtLocation(id);
  const wasmType = resolveWasmType(ctx, tsType);
  if (wasmType.kind === "f64") {
    fctx.body.push({ op: "f64.const", value: 0 });
    return { kind: "f64" };
  }
  if (wasmType.kind === "i32") {
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  if (wasmType.kind === "i64") {
    fctx.body.push({ op: "i64.const", value: 0n });
    return { kind: "i64" };
  }
  fctx.body.push({ op: "ref.null.extern" });
  return { kind: "externref" };
}
function narrowTypeToUnbox(ctx, fctx, narrowedType) {
  if (isHeterogeneousUnion(narrowedType, ctx.checker)) return null;
  if (narrowedType.isUnion()) return null;
  if (isNumberType(narrowedType)) {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__unbox_number");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "f64" };
    }
  }
  if (isBooleanType(narrowedType)) {
    addUnionImports(ctx);
    const funcIdx = ctx.funcMap.get("__unbox_boolean");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "i32" };
    }
  }
  if (isStringType(narrowedType)) return null;
  return null;
}
function resolveInstanceOfRHS(ctx, rightExpr) {
  if (ts13.isIdentifier(rightExpr)) {
    const name = rightExpr.text;
    if (ctx.classTagMap.has(name)) return name;
    const mapped = ctx.classExprNameMap.get(name);
    if (mapped && ctx.classTagMap.has(mapped)) return mapped;
  }
  const tsType = ctx.checker.getTypeAtLocation(rightExpr);
  const constructSigs = tsType.getConstructSignatures?.();
  if (constructSigs && constructSigs.length > 0) {
    const instanceType = constructSigs[0].getReturnType();
    const symbolName2 = instanceType.getSymbol()?.name;
    if (symbolName2) {
      if (ctx.classTagMap.has(symbolName2)) return symbolName2;
      const mapped = ctx.classExprNameMap.get(symbolName2);
      if (mapped && ctx.classTagMap.has(mapped)) return mapped;
    }
  }
  const symbolName = tsType.getSymbol()?.name;
  if (symbolName) {
    if (ctx.classTagMap.has(symbolName)) return symbolName;
    const mapped = ctx.classExprNameMap.get(symbolName);
    if (mapped && ctx.classTagMap.has(mapped)) return mapped;
  }
  return void 0;
}
function compileHostInstanceOf(ctx, fctx, expr) {
  let ctorName;
  if (ts13.isIdentifier(expr.right)) {
    ctorName = expr.right.text;
  }
  if (!ctorName) {
    const leftType2 = compileExpression2(ctx, fctx, expr.left);
    if (leftType2) fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  const instanceofIdx = ensureLateImport2(
    ctx,
    "__instanceof",
    [{ kind: "externref" }, { kind: "externref" }],
    [{ kind: "i32" }],
  );
  flushLateImportShifts2(ctx, fctx);
  if (instanceofIdx === void 0) {
    const leftType2 = compileExpression2(ctx, fctx, expr.left);
    if (leftType2) fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "i32.const", value: 0 });
    return { kind: "i32" };
  }
  const leftType = compileExpression2(ctx, fctx, expr.left);
  if (!leftType) {
    fctx.body.push({ op: "ref.null.extern" });
  } else if (leftType.kind !== "externref") {
    coerceType3(ctx, fctx, leftType, { kind: "externref" });
  }
  addStringConstantGlobal(ctx, ctorName);
  const strGlobalIdx = ctx.stringGlobalMap.get(ctorName);
  if (strGlobalIdx !== void 0) {
    fctx.body.push({ op: "global.get", index: strGlobalIdx });
  } else {
    fctx.body.push({ op: "ref.null.extern" });
  }
  fctx.body.push({ op: "call", funcIdx: instanceofIdx });
  return { kind: "i32" };
}
function compileLogicalAnd(ctx, fctx, expr) {
  const leftType = compileExpression2(ctx, fctx, expr.left);
  if (!leftType) {
    ensureI32Condition(fctx, leftType, ctx);
    return { kind: "i32" };
  }
  const tmp = allocTempLocal(fctx, leftType);
  fctx.body.push({ op: "local.tee", index: tmp });
  ensureI32Condition(fctx, leftType, ctx);
  const savedBody = pushBody(fctx);
  const rightType = compileExpression2(ctx, fctx, expr.right);
  let thenInstrs = fctx.body;
  fctx.body = savedBody;
  if (!rightType) {
    const resultType2 = leftType;
    thenInstrs.push(...defaultValueInstrs(resultType2));
    const elseInstrs2 = [{ op: "local.get", index: tmp }];
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: resultType2 },
      then: thenInstrs,
      else: elseInstrs2,
    });
    releaseTempLocal(fctx, tmp);
    return resultType2;
  }
  const rType = rightType;
  let resultType = leftType;
  if (!valTypesMatch(leftType, rType)) {
    if ((leftType.kind === "i32" || leftType.kind === "f64") && (rType.kind === "i32" || rType.kind === "f64")) {
      resultType = { kind: "f64" };
    } else {
      resultType = { kind: "externref" };
    }
  }
  if (!valTypesMatch(rType, resultType)) {
    const coerceBody = [];
    fctx.body = coerceBody;
    coerceType3(ctx, fctx, rType, resultType);
    fctx.body = savedBody;
    thenInstrs = [...thenInstrs, ...coerceBody];
  }
  let elseInstrs = [{ op: "local.get", index: tmp }];
  if (!valTypesMatch(leftType, resultType)) {
    const coerceBody = [];
    fctx.body = coerceBody;
    coerceType3(ctx, fctx, leftType, resultType);
    fctx.body = savedBody;
    elseInstrs = [...elseInstrs, ...coerceBody];
  }
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: resultType },
    then: thenInstrs,
    else: elseInstrs,
  });
  releaseTempLocal(fctx, tmp);
  return resultType;
}
function compileLogicalOr(ctx, fctx, expr) {
  const leftType = compileExpression2(ctx, fctx, expr.left);
  if (!leftType) {
    ensureI32Condition(fctx, leftType, ctx);
    return { kind: "i32" };
  }
  const tmp = allocTempLocal(fctx, leftType);
  fctx.body.push({ op: "local.tee", index: tmp });
  ensureI32Condition(fctx, leftType, ctx);
  const savedBody = pushBody(fctx);
  const rightType = compileExpression2(ctx, fctx, expr.right);
  let elseInstrs = fctx.body;
  fctx.body = savedBody;
  if (!rightType) {
    const resultType2 = leftType;
    elseInstrs.push(...defaultValueInstrs(resultType2));
    const thenInstrs2 = [{ op: "local.get", index: tmp }];
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: resultType2 },
      then: thenInstrs2,
      else: elseInstrs,
    });
    releaseTempLocal(fctx, tmp);
    return resultType2;
  }
  const rType = rightType;
  let resultType = leftType;
  if (!valTypesMatch(leftType, rType)) {
    if ((leftType.kind === "i32" || leftType.kind === "f64") && (rType.kind === "i32" || rType.kind === "f64")) {
      resultType = { kind: "f64" };
    } else {
      resultType = { kind: "externref" };
    }
  }
  let thenInstrs = [{ op: "local.get", index: tmp }];
  if (!valTypesMatch(leftType, resultType)) {
    const coerceBody = [];
    fctx.body = coerceBody;
    coerceType3(ctx, fctx, leftType, resultType);
    fctx.body = savedBody;
    thenInstrs = [...thenInstrs, ...coerceBody];
  }
  if (!valTypesMatch(rType, resultType)) {
    const coerceBody = [];
    fctx.body = coerceBody;
    coerceType3(ctx, fctx, rType, resultType);
    fctx.body = savedBody;
    elseInstrs = [...elseInstrs, ...coerceBody];
  }
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: resultType },
    then: thenInstrs,
    else: elseInstrs,
  });
  releaseTempLocal(fctx, tmp);
  return resultType;
}
function compileNullishCoalescing(ctx, fctx, expr) {
  const leftType = compileExpression2(ctx, fctx, expr.left);
  if (!leftType) {
    ctx.errors.push({
      message: "Failed to compile nullish coalescing LHS",
      line: getLine(expr),
      column: getCol(expr),
    });
    return { kind: "externref" };
  }
  const resultKind = leftType ?? { kind: "externref" };
  const tmp = allocTempLocal(fctx, resultKind);
  fctx.body.push({ op: "local.tee", index: tmp });
  if (resultKind.kind === "i32" || resultKind.kind === "f64") {
    releaseTempLocal(fctx, tmp);
    return resultKind;
  }
  fctx.body.push({ op: "ref.is_null" });
  const isUndefIdx = ensureLateImport2(ctx, "__extern_is_undefined", [{ kind: "externref" }], [{ kind: "i32" }]);
  flushLateImportShifts2(ctx, fctx);
  if (isUndefIdx !== void 0) {
    fctx.body.push({ op: "local.get", index: tmp });
    if (resultKind.kind !== "externref") {
      fctx.body.push({ op: "extern.convert_any" });
    }
    fctx.body.push({ op: "call", funcIdx: isUndefIdx });
    fctx.body.push({ op: "i32.or" });
  }
  const savedBody = pushBody(fctx);
  const rhsType = compileExpression2(ctx, fctx, expr.right);
  let thenInstrs = fctx.body;
  fctx.body = savedBody;
  if (!rhsType) {
    thenInstrs.push(...defaultValueInstrs(resultKind));
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: resultKind },
      then: thenInstrs,
      else: [{ op: "local.get", index: tmp }],
    });
    releaseTempLocal(fctx, tmp);
    return resultKind;
  }
  const rType = rhsType;
  if (valTypesMatch(resultKind, rType)) {
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: resultKind },
      then: thenInstrs,
      else: [{ op: "local.get", index: tmp }],
    });
    releaseTempLocal(fctx, tmp);
    return resultKind;
  }
  let unifiedType;
  if (
    rType.kind === "f64" &&
    (resultKind.kind === "externref" || resultKind.kind === "ref" || resultKind.kind === "ref_null")
  ) {
    unifiedType = { kind: "externref" };
  } else {
    unifiedType = rType;
  }
  if (!valTypesMatch(rType, unifiedType)) {
    const coerceRhsBody = [];
    fctx.body = coerceRhsBody;
    coerceType3(ctx, fctx, rType, unifiedType);
    fctx.body = savedBody;
    thenInstrs = [...thenInstrs, ...coerceRhsBody];
  }
  const elseInstrs = [{ op: "local.get", index: tmp }];
  const coerceLhsBody = [];
  fctx.body = coerceLhsBody;
  coerceType3(ctx, fctx, resultKind, unifiedType);
  fctx.body = savedBody;
  elseInstrs.push(...coerceLhsBody);
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: unifiedType },
    then: thenInstrs,
    else: elseInstrs,
  });
  releaseTempLocal(fctx, tmp);
  return unifiedType;
}
function emitMappedArgParamSync(ctx, fctx, paramIdx, resultType) {
  const info = fctx.mappedArgsInfo;
  if (!info) return;
  const argIndex = paramIdx - info.paramOffset;
  if (argIndex < 0 || argIndex >= info.paramCount) return;
  const tmp = allocLocal(fctx, `__arg_sync_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmp });
  const paramType = info.paramTypes[argIndex];
  const coerceInstrs = [];
  if (paramType.kind === "f64") {
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx !== void 0) {
      coerceInstrs.push({ op: "call", funcIdx: boxIdx });
    }
  } else if (paramType.kind === "i32") {
    coerceInstrs.push({ op: "f64.convert_i32_s" });
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx !== void 0) {
      coerceInstrs.push({ op: "call", funcIdx: boxIdx });
    }
  } else if (paramType.kind === "ref" || paramType.kind === "ref_null") {
    coerceInstrs.push({ op: "extern.convert_any" });
  }
  fctx.body.push({ op: "local.get", index: info.argsLocalIdx });
  fctx.body.push({ op: "ref.is_null" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: [],
    else: [
      { op: "local.get", index: info.argsLocalIdx },
      { op: "struct.get", typeIdx: info.vecTypeIdx, fieldIdx: 1 },
      { op: "i32.const", value: argIndex },
      { op: "local.get", index: paramIdx },
      ...coerceInstrs,
      { op: "array.set", typeIdx: info.arrTypeIdx },
    ],
  });
  fctx.body.push({ op: "local.get", index: tmp });
}
function emitMappedArgReverseSync(ctx, fctx, idxLocal, valLocal) {
  const info = fctx.mappedArgsInfo;
  if (!info) return;
  for (let i = 0; i < info.paramCount; i++) {
    const paramType = info.paramTypes[i];
    const localIdx = i + info.paramOffset;
    const convertInstrs = [];
    convertInstrs.push({ op: "local.get", index: valLocal });
    if (paramType.kind === "f64") {
      const unboxIdx = ctx.funcMap.get("__unbox_number");
      if (unboxIdx !== void 0) {
        convertInstrs.push({ op: "call", funcIdx: unboxIdx });
      }
    } else if (paramType.kind === "i32") {
      const unboxIdx = ctx.funcMap.get("__unbox_number");
      if (unboxIdx !== void 0) {
        convertInstrs.push({ op: "call", funcIdx: unboxIdx });
      }
      convertInstrs.push({ op: "i32.trunc_sat_f64_s" });
    } else if (paramType.kind === "ref" || paramType.kind === "ref_null") {
      convertInstrs.push({ op: "any.convert_extern" });
      if (paramType.kind === "ref") {
        convertInstrs.push({ op: "ref.cast", typeIdx: paramType.typeIdx });
      }
    }
    fctx.body.push({ op: "local.get", index: idxLocal });
    fctx.body.push({ op: "i32.const", value: i });
    fctx.body.push({ op: "i32.eq" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [...convertInstrs, { op: "local.set", index: localIdx }],
      else: [],
    });
  }
}
function compileAssignment(ctx, fctx, expr) {
  let lhs = expr.left;
  while (ts13.isParenthesizedExpression(lhs)) {
    lhs = lhs.expression;
  }
  if (lhs !== expr.left) {
    const synth = { ...expr, left: lhs };
    return compileAssignment(ctx, fctx, synth);
  }
  if (ts13.isIdentifier(expr.left)) {
    const name = expr.left.text;
    if (fctx.constBindings?.has(name)) {
      const rhsType = compileExpression2(ctx, fctx, expr.right);
      if (rhsType) fctx.body.push({ op: "drop" });
      emitThrowString2(ctx, fctx, "TypeError: Assignment to constant variable.");
      fctx.body.push({ op: "unreachable" });
      return { kind: "f64" };
    }
    if (fctx.readOnlyBindings?.has(name)) {
      const rhsType = compileExpression2(ctx, fctx, expr.right);
      return rhsType;
    }
    const localIdx = fctx.localMap.get(name);
    if (localIdx !== void 0) {
      const boxed = fctx.boxedCaptures?.get(name);
      if (boxed) {
        const resultType2 = compileExpression2(ctx, fctx, expr.right, boxed.valType);
        if (!resultType2) {
          ctx.errors.push({
            message: "Failed to compile assignment value",
            line: getLine(expr),
            column: getCol(expr),
          });
          return null;
        }
        const tmpVal = allocLocal(fctx, `__box_tmp_${fctx.locals.length}`, boxed.valType);
        fctx.body.push({ op: "local.set", index: tmpVal });
        fctx.body.push({ op: "local.get", index: localIdx });
        fctx.body.push({ op: "ref.is_null" });
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: [],
          else: [
            { op: "local.get", index: localIdx },
            { op: "local.get", index: tmpVal },
            {
              op: "struct.set",
              typeIdx: boxed.refCellTypeIdx,
              fieldIdx: 0,
            },
          ],
        });
        fctx.body.push({ op: "local.get", index: tmpVal });
        return resultType2;
      }
      const localType =
        localIdx < fctx.params.length ? fctx.params[localIdx].type : fctx.locals[localIdx - fctx.params.length]?.type;
      const isFuncExprRHS = ts13.isFunctionExpression(expr.right) || ts13.isArrowFunction(expr.right);
      const isFuncRefRHS = ts13.isIdentifier(expr.right) && ctx.funcMap.has(expr.right.text);
      const isCallableRHS = isFuncExprRHS || isFuncRefRHS;
      const localIsClosureRef =
        localType &&
        (localType.kind === "ref" || localType.kind === "ref_null") &&
        ctx.closureInfoByTypeIdx.has(localType.typeIdx);
      const typeHint =
        (isCallableRHS || localIsClosureRef) && localType?.kind === "externref"
          ? void 0
          : localIsClosureRef
            ? void 0
            : localType;
      const resultType = compileExpression2(ctx, fctx, expr.right, typeHint);
      if (!resultType) {
        ctx.errors.push({
          message: "Failed to compile assignment value",
          line: getLine(expr),
          column: getCol(expr),
        });
        return null;
      }
      if (
        (isCallableRHS || localIsClosureRef) &&
        resultType.kind === "ref" &&
        (localType?.kind === "externref" || localIsClosureRef)
      ) {
        if (localIdx < fctx.params.length) {
          fctx.params[localIdx].type = resultType;
        } else {
          const localEntry = fctx.locals[localIdx - fctx.params.length];
          if (localEntry) localEntry.type = resultType;
        }
      }
      const effectiveLocalType =
        localIdx < fctx.params.length ? fctx.params[localIdx].type : fctx.locals[localIdx - fctx.params.length]?.type;
      if (effectiveLocalType && !valTypesMatch(resultType, effectiveLocalType)) {
        const bodyLenBeforeCoerce = fctx.body.length;
        coerceType3(ctx, fctx, resultType, effectiveLocalType);
        if (
          fctx.body.length === bodyLenBeforeCoerce &&
          (resultType.kind === "ref" || resultType.kind === "ref_null") &&
          (effectiveLocalType.kind === "ref" || effectiveLocalType.kind === "ref_null")
        ) {
          updateLocalType(fctx, localIdx, resultType);
          fctx.body.push({ op: "local.tee", index: localIdx });
          emitMappedArgParamSync(ctx, fctx, localIdx, resultType);
          return resultType;
        }
        fctx.body.push({ op: "local.tee", index: localIdx });
        emitMappedArgParamSync(ctx, fctx, localIdx, effectiveLocalType);
        return effectiveLocalType;
      }
      fctx.body.push({ op: "local.tee", index: localIdx });
      emitMappedArgParamSync(ctx, fctx, localIdx, resultType);
      return resultType;
    }
    const capturedIdx = ctx.capturedGlobals.get(name);
    if (capturedIdx !== void 0) {
      const globalDef = ctx.mod.globals[localGlobalIdx(ctx, capturedIdx)];
      const resultType = compileExpression2(ctx, fctx, expr.right, globalDef?.type);
      if (!resultType) {
        ctx.errors.push({
          message: "Failed to compile assignment value",
          line: getLine(expr),
          column: getCol(expr),
        });
        return null;
      }
      const capturedIdxPost = ctx.capturedGlobals.get(name);
      fctx.body.push({ op: "global.set", index: capturedIdxPost });
      fctx.body.push({ op: "global.get", index: capturedIdxPost });
      return resultType;
    }
    const moduleIdx = ctx.moduleGlobals.get(name);
    if (moduleIdx !== void 0) {
      const globalDef = ctx.mod.globals[localGlobalIdx(ctx, moduleIdx)];
      const resultType = compileExpression2(ctx, fctx, expr.right, globalDef?.type);
      if (!resultType) {
        ctx.errors.push({
          message: "Failed to compile assignment value",
          line: getLine(expr),
          column: getCol(expr),
        });
        return null;
      }
      const moduleIdxPost = ctx.moduleGlobals.get(name);
      fctx.body.push({ op: "global.set", index: moduleIdxPost });
      fctx.body.push({ op: "global.get", index: moduleIdxPost });
      return resultType;
    }
    {
      const resultType = compileExpression2(ctx, fctx, expr.right);
      if (!resultType) return null;
      const newLocalIdx = allocLocal(fctx, name, resultType);
      fctx.body.push({ op: "local.tee", index: newLocalIdx });
      return resultType;
    }
  }
  if (ts13.isPropertyAccessExpression(expr.left)) {
    return compilePropertyAssignment(ctx, fctx, expr.left, expr.right);
  }
  if (ts13.isElementAccessExpression(expr.left)) {
    return compileElementAssignment(ctx, fctx, expr.left, expr.right);
  }
  if (ts13.isObjectLiteralExpression(expr.left)) {
    return compileDestructuringAssignment(ctx, fctx, expr.left, expr.right);
  }
  if (ts13.isArrayLiteralExpression(expr.left)) {
    return compileArrayDestructuringAssignment(ctx, fctx, expr.left, expr.right);
  }
  ctx.errors.push({
    message: "Unsupported assignment target",
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}
function compileDestructuringAssignment(ctx, fctx, target, value) {
  const resultType = compileExpression2(ctx, fctx, value);
  if (!resultType) return null;
  const rhsType = ctx.checker.getTypeAtLocation(value);
  const symName = rhsType.symbol?.name;
  let typeName =
    symName && symName !== "__type" && symName !== "__object" && ctx.structMap.has(symName)
      ? symName
      : (ctx.anonTypeMap.get(rhsType) ?? symName);
  if (
    typeName &&
    (typeName === "__type" || typeName === "__object") &&
    !ctx.anonTypeMap.has(rhsType) &&
    rhsType.getProperties().length > 0
  ) {
    ensureStructForType(ctx, rhsType);
    typeName = ctx.anonTypeMap.get(rhsType) ?? typeName;
  }
  if (!typeName || !ctx.structMap.has(typeName) || !ctx.structFields.get(typeName)) {
    if (resultType.kind === "externref" || resultType.kind === "ref_null") {
      const typeErrMsg = "TypeError: Cannot destructure 'null' or 'undefined'";
      addStringConstantGlobal(ctx, typeErrMsg);
      const strIdx = ctx.stringGlobalMap.get(typeErrMsg);
      const tagIdx = ensureExnTag(ctx);
      const tmpNullChk = allocLocal(fctx, `__destruct_null_chk_${fctx.locals.length}`, resultType);
      fctx.body.push({ op: "local.tee", index: tmpNullChk });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "global.get", index: strIdx },
          { op: "throw", tagIdx },
        ],
        else: [],
      });
      fctx.body.push({ op: "local.get", index: tmpNullChk });
    }
    for (const prop of target.properties) {
      if (ts13.isShorthandPropertyAssignment(prop)) {
        const name = prop.name.text;
        if (!fctx.localMap.has(name) && !ctx.moduleGlobals.has(name)) {
          allocLocal(fctx, name, { kind: "externref" });
        }
      } else if (ts13.isSpreadAssignment(prop) && ts13.isIdentifier(prop.expression)) {
        const name = prop.expression.text;
        if (!fctx.localMap.has(name) && !ctx.moduleGlobals.has(name)) {
          allocLocal(fctx, name, { kind: "externref" });
        }
      }
    }
    return resultType;
  }
  const structTypeIdx = ctx.structMap.get(typeName);
  const fields = ctx.structFields.get(typeName);
  const tmpLocal = allocLocal(fctx, `__destruct_assign_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  const isNullableDA = resultType.kind === "ref_null";
  const savedBodyDA = fctx.body;
  const destructInstrsDA = [];
  fctx.body = destructInstrsDA;
  for (const prop of target.properties) {
    if (ts13.isShorthandPropertyAssignment(prop)) {
      const propName = prop.name.text;
      let localIdx = fctx.localMap.get(propName);
      const fieldIdx = fields.findIndex((f) => f.name === propName);
      if (fieldIdx === -1) {
        ctx.errors.push({
          message: `Unknown field in destructuring: ${propName}`,
          line: getLine(prop),
          column: getCol(prop),
        });
        continue;
      }
      if (localIdx === void 0) {
        const fieldType2 = fields[fieldIdx].type;
        localIdx = allocLocal(fctx, propName, fieldType2);
      }
      const fieldType = fields[fieldIdx].type;
      const localType = getLocalType(fctx, localIdx);
      fctx.body.push({ op: "local.get", index: tmpLocal });
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
      if (prop.objectAssignmentInitializer) {
        if (fieldType.kind === "externref") {
          const tmpField = allocLocal(fctx, `__dflt_${fctx.locals.length}`, fieldType);
          fctx.body.push({ op: "local.tee", index: tmpField });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              ...(() => {
                const saved = fctx.body;
                fctx.body = [];
                compileExpression2(ctx, fctx, prop.objectAssignmentInitializer, localType ?? fieldType);
                fctx.body.push({ op: "local.set", index: localIdx });
                const instrs = fctx.body;
                fctx.body = saved;
                return instrs;
              })(),
            ],
            else: [
              { op: "local.get", index: tmpField },
              ...(() => {
                if (localType && !valTypesMatch(fieldType, localType)) {
                  const saved = fctx.body;
                  fctx.body = [];
                  coerceType3(ctx, fctx, fieldType, localType);
                  const instrs = fctx.body;
                  fctx.body = saved;
                  return instrs;
                }
                return [];
              })(),
              { op: "local.set", index: localIdx },
            ],
          });
        } else {
          if (localType && !valTypesMatch(fieldType, localType)) {
            coerceType3(ctx, fctx, fieldType, localType);
          }
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      } else {
        if (localType && !valTypesMatch(fieldType, localType)) {
          coerceType3(ctx, fctx, fieldType, localType);
        }
        fctx.body.push({ op: "local.set", index: localIdx });
      }
    } else if (ts13.isPropertyAssignment(prop)) {
      let propName = ts13.isIdentifier(prop.name)
        ? prop.name.text
        : ts13.isStringLiteral(prop.name)
          ? prop.name.text
          : ts13.isNumericLiteral(prop.name)
            ? prop.name.text
            : void 0;
      if (!propName && ts13.isComputedPropertyName(prop.name)) {
        propName = resolveComputedKeyExpression(ctx, prop.name.expression);
      }
      if (!propName) continue;
      const fieldIdx = fields.findIndex((f) => f.name === propName);
      if (fieldIdx === -1) continue;
      const fieldType = fields[fieldIdx].type;
      let targetExpr = prop.initializer;
      let defaultExpr;
      if (
        ts13.isBinaryExpression(targetExpr) &&
        targetExpr.operatorToken.kind === ts13.SyntaxKind.EqualsToken &&
        ts13.isIdentifier(targetExpr.left)
      ) {
        defaultExpr = targetExpr.right;
        targetExpr = targetExpr.left;
      }
      if (ts13.isIdentifier(targetExpr)) {
        const localName = targetExpr.text;
        let localIdx = fctx.localMap.get(localName);
        if (localIdx === void 0) {
          localIdx = allocLocal(fctx, localName, fieldType);
        }
        const localType = getLocalType(fctx, localIdx);
        fctx.body.push({ op: "local.get", index: tmpLocal });
        fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
        if (defaultExpr) {
          if (fieldType.kind === "externref" || fieldType.kind === "ref" || fieldType.kind === "ref_null") {
            const tmpField = allocLocal(fctx, `__dflt_${fctx.locals.length}`, fieldType);
            fctx.body.push({ op: "local.tee", index: tmpField });
            fctx.body.push({ op: "ref.is_null" });
            fctx.body.push({
              op: "if",
              blockType: { kind: "empty" },
              then: [
                ...(() => {
                  const saved = fctx.body;
                  fctx.body = [];
                  compileExpression2(ctx, fctx, defaultExpr, localType ?? fieldType);
                  fctx.body.push({
                    op: "local.set",
                    index: localIdx,
                  });
                  const instrs = fctx.body;
                  fctx.body = saved;
                  return instrs;
                })(),
              ],
              else: [
                { op: "local.get", index: tmpField },
                ...(() => {
                  if (localType && !valTypesMatch(fieldType, localType)) {
                    const saved = fctx.body;
                    fctx.body = [];
                    coerceType3(ctx, fctx, fieldType, localType);
                    const instrs = fctx.body;
                    fctx.body = saved;
                    return instrs;
                  }
                  return [];
                })(),
                { op: "local.set", index: localIdx },
              ],
            });
          } else {
            if (localType && !valTypesMatch(fieldType, localType)) {
              coerceType3(ctx, fctx, fieldType, localType);
            }
            fctx.body.push({ op: "local.set", index: localIdx });
          }
        } else {
          if (localType && !valTypesMatch(fieldType, localType)) {
            coerceType3(ctx, fctx, fieldType, localType);
          }
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      } else if (ts13.isObjectLiteralExpression(targetExpr)) {
        const tmpNested = allocLocal(fctx, `__nested_${fctx.locals.length}`, fieldType);
        fctx.body.push({ op: "local.get", index: tmpLocal });
        fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
        fctx.body.push({ op: "local.set", index: tmpNested });
        emitObjectDestructureFromLocal(ctx, fctx, targetExpr, tmpNested, fieldType);
      } else if (ts13.isArrayLiteralExpression(targetExpr)) {
        const tmpNested = allocLocal(fctx, `__nested_${fctx.locals.length}`, fieldType);
        fctx.body.push({ op: "local.get", index: tmpLocal });
        fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
        fctx.body.push({ op: "local.set", index: tmpNested });
        emitArrayDestructureFromLocal(ctx, fctx, targetExpr, tmpNested, fieldType);
      } else if (ts13.isPropertyAccessExpression(targetExpr) || ts13.isElementAccessExpression(targetExpr)) {
        const tmpElem = allocLocal(fctx, `__nested_elem_${fctx.locals.length}`, fieldType);
        fctx.body.push({ op: "local.get", index: tmpLocal });
        fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
        fctx.body.push({ op: "local.set", index: tmpElem });
        emitAssignToTarget(ctx, fctx, targetExpr, tmpElem, fieldType);
      }
    } else if (ts13.isSpreadAssignment(prop)) {
      if (ts13.isIdentifier(prop.expression)) {
        const restName = prop.expression.text;
        let restIdx = fctx.localMap.get(restName);
        if (restIdx === void 0) {
          restIdx = allocLocal(fctx, restName, { kind: "externref" });
        }
        const excludedKeys = [];
        for (const p of target.properties) {
          if (ts13.isSpreadAssignment(p)) continue;
          if (ts13.isPropertyAssignment(p) || ts13.isShorthandPropertyAssignment(p)) {
            const pn = ts13.isPropertyAssignment(p) ? p.name : p.name;
            if (ts13.isIdentifier(pn)) excludedKeys.push(pn.text);
            else if (ts13.isStringLiteral(pn)) excludedKeys.push(pn.text);
            else if (ts13.isNumericLiteral(pn)) excludedKeys.push(pn.text);
          }
        }
        let restObjIdx = ctx.funcMap.get("__extern_rest_object");
        if (restObjIdx === void 0) {
          const importsBefore = ctx.numImportFuncs;
          const restObjType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
          addImport(ctx, "env", "__extern_rest_object", { kind: "func", typeIdx: restObjType });
          shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
          restObjIdx = ctx.funcMap.get("__extern_rest_object");
        }
        if (restObjIdx !== void 0) {
          const excludedStr = excludedKeys.join(",");
          addStringConstantGlobal(ctx, excludedStr);
          const excludedStrIdx = ctx.stringGlobalMap.get(excludedStr);
          if (excludedStrIdx !== void 0) {
            fctx.body.push({ op: "local.get", index: tmpLocal });
            fctx.body.push({ op: "extern.convert_any" });
            fctx.body.push({ op: "global.get", index: excludedStrIdx });
            fctx.body.push({ op: "call", funcIdx: restObjIdx });
            fctx.body.push({ op: "local.set", index: restIdx });
          }
        }
      }
    }
  }
  fctx.body = savedBodyDA;
  if (isNullableDA) {
    const typeErrMsg = "TypeError: Cannot destructure 'null' or 'undefined'";
    addStringConstantGlobal(ctx, typeErrMsg);
    const strIdx = ctx.stringGlobalMap.get(typeErrMsg);
    const tagIdx = ensureExnTag(ctx);
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "global.get", index: strIdx },
        { op: "throw", tagIdx },
      ],
      else: destructInstrsDA,
    });
  } else {
    fctx.body.push(...destructInstrsDA);
  }
  fctx.body.push({ op: "local.get", index: tmpLocal });
  return resultType;
}
function compileArrayDestructuringAssignment(ctx, fctx, target, value) {
  const resultType = compileExpression2(ctx, fctx, value);
  if (!resultType) return null;
  if (resultType.kind !== "ref" && resultType.kind !== "ref_null") {
    if (resultType.kind === "externref") {
      return compileExternrefArrayDestructuringAssignment(ctx, fctx, target, resultType);
    }
    if (resultType.kind === "f64" || resultType.kind === "i32") {
      if (resultType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
      }
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
        return compileExternrefArrayDestructuringAssignment(ctx, fctx, target, {
          kind: "externref",
        });
      }
    }
    ctx.errors.push({
      message: "Cannot destructure: not an array type",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const typeIdx = resultType.typeIdx;
  const typeDef = ctx.mod.types[typeIdx];
  if (!typeDef || typeDef.kind !== "struct") {
    fctx.body.push({ op: "extern.convert_any" });
    return compileExternrefArrayDestructuringAssignment(ctx, fctx, target, {
      kind: "externref",
    });
  }
  const isVecStruct =
    typeDef.fields.length === 2 && typeDef.fields[0]?.name === "length" && typeDef.fields[1]?.name === "data";
  let arrTypeIdx = -1;
  let arrDef;
  if (isVecStruct) {
    arrTypeIdx = getArrTypeIdxFromVec(ctx, typeIdx);
    const ad = ctx.mod.types[arrTypeIdx];
    if (!ad || ad.kind !== "array") {
      ctx.errors.push({
        message: "Cannot destructure: vec data is not array",
        line: getLine(target),
        column: getCol(target),
      });
      return null;
    }
    arrDef = ad;
  }
  const tmpLocal = allocLocal(fctx, `__arr_destruct_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  const isNullableADA = resultType.kind === "ref_null";
  const savedBodyADA = fctx.body;
  const arrDestructInstrsADA = [];
  fctx.body = arrDestructInstrsADA;
  const getElemType = (i) => {
    if (isVecStruct) return arrDef.element;
    const field = typeDef.fields[i];
    return field ? field.type : { kind: "f64" };
  };
  const emitElementGet = (i) => {
    fctx.body.push({ op: "local.get", index: tmpLocal });
    if (isVecStruct) {
      fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
      fctx.body.push({ op: "i32.const", value: i });
      emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, arrDef.element);
    } else {
      fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: i });
    }
  };
  for (let i = 0; i < target.elements.length; i++) {
    const element = target.elements[i];
    if (ts13.isOmittedExpression(element)) continue;
    if (ts13.isSpreadElement(element)) {
      if (isVecStruct) {
        const restTarget = element.expression;
        if (ts13.isIdentifier(restTarget)) {
          const restName = restTarget.text;
          let restLocalIdx = fctx.localMap.get(restName);
          if (restLocalIdx === void 0) {
            restLocalIdx = allocLocal(fctx, restName, resultType);
          }
          const tmpLen = allocLocal(fctx, `__rest_len_${fctx.locals.length}`, {
            kind: "i32",
          });
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 0 });
          fctx.body.push({ op: "i32.const", value: i });
          fctx.body.push({ op: "i32.sub" });
          fctx.body.push({ op: "local.tee", index: tmpLen });
          fctx.body.push({
            op: "array.new_default",
            typeIdx: arrTypeIdx,
          });
          const tmpRestArr = allocLocal(fctx, `__rest_arr_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
          fctx.body.push({ op: "local.set", index: tmpRestArr });
          const tmpJ = allocLocal(fctx, `__rest_j_${fctx.locals.length}`, {
            kind: "i32",
          });
          fctx.body.push({ op: "i32.const", value: 0 });
          fctx.body.push({ op: "local.set", index: tmpJ });
          const loopBody = [
            { op: "local.get", index: tmpJ },
            { op: "local.get", index: tmpLen },
            { op: "i32.lt_s" },
            { op: "i32.eqz" },
            { op: "br_if", depth: 1 },
            { op: "local.get", index: tmpRestArr },
            { op: "local.get", index: tmpJ },
            { op: "local.get", index: tmpLocal },
            { op: "struct.get", typeIdx, fieldIdx: 1 },
            { op: "local.get", index: tmpJ },
            { op: "i32.const", value: i },
            { op: "i32.add" },
            { op: "array.get", typeIdx: arrTypeIdx },
            { op: "array.set", typeIdx: arrTypeIdx },
            { op: "local.get", index: tmpJ },
            { op: "i32.const", value: 1 },
            { op: "i32.add" },
            { op: "local.set", index: tmpJ },
            { op: "br", depth: 0 },
          ];
          fctx.body.push({
            op: "block",
            blockType: { kind: "empty" },
            body: [
              {
                op: "loop",
                blockType: { kind: "empty" },
                body: loopBody,
              },
            ],
          });
          fctx.body.push({ op: "local.get", index: tmpLen });
          fctx.body.push({ op: "local.get", index: tmpRestArr });
          fctx.body.push({ op: "struct.new", typeIdx });
          fctx.body.push({ op: "local.set", index: restLocalIdx });
        }
      }
      continue;
    }
    const elemType = getElemType(i);
    if (ts13.isIdentifier(element)) {
      const localName = element.text;
      let localIdx = fctx.localMap.get(localName);
      if (localIdx === void 0) {
        localIdx = allocLocal(fctx, localName, elemType);
      }
      emitElementGet(i);
      const localType = getLocalType(fctx, localIdx);
      if (localType && !valTypesMatch(elemType, localType)) {
        coerceType3(ctx, fctx, elemType, localType);
      }
      fctx.body.push({ op: "local.set", index: localIdx });
    } else if (ts13.isPropertyAccessExpression(element)) {
      emitElementGet(i);
      const tmpElem = allocLocal(fctx, `__arr_elem_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.set", index: tmpElem });
      emitAssignToTarget(ctx, fctx, element, tmpElem, elemType);
    } else if (ts13.isElementAccessExpression(element)) {
      emitElementGet(i);
      const tmpElem = allocLocal(fctx, `__arr_elem_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.set", index: tmpElem });
      emitAssignToTarget(ctx, fctx, element, tmpElem, elemType);
    } else if (ts13.isObjectLiteralExpression(element)) {
      emitElementGet(i);
      const tmpElem = allocLocal(fctx, `__arr_elem_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.set", index: tmpElem });
      emitObjectDestructureFromLocal(ctx, fctx, element, tmpElem, elemType);
    } else if (ts13.isArrayLiteralExpression(element)) {
      emitElementGet(i);
      const tmpElem = allocLocal(fctx, `__arr_elem_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.set", index: tmpElem });
      emitArrayDestructureFromLocal(ctx, fctx, element, tmpElem, elemType);
    } else if (ts13.isBinaryExpression(element) && element.operatorToken.kind === ts13.SyntaxKind.EqualsToken) {
      const assignTarget = element.left;
      const defaultExpr = element.right;
      if (ts13.isIdentifier(assignTarget)) {
        const localName = assignTarget.text;
        let localIdx = fctx.localMap.get(localName);
        if (localIdx === void 0) {
          localIdx = allocLocal(fctx, localName, elemType);
        }
        emitElementGet(i);
        if (elemType.kind === "externref" || elemType.kind === "ref" || elemType.kind === "ref_null") {
          const tmpElem = allocLocal(fctx, `__dflt_${fctx.locals.length}`, elemType);
          fctx.body.push({ op: "local.tee", index: tmpElem });
          fctx.body.push({ op: "ref.is_null" });
          const localType = getLocalType(fctx, localIdx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              ...(() => {
                const saved = fctx.body;
                fctx.body = [];
                compileExpression2(ctx, fctx, defaultExpr, localType ?? elemType);
                fctx.body.push({ op: "local.set", index: localIdx });
                const instrs = fctx.body;
                fctx.body = saved;
                return instrs;
              })(),
            ],
            else: [
              { op: "local.get", index: tmpElem },
              ...(() => {
                if (localType && !valTypesMatch(elemType, localType)) {
                  const saved = fctx.body;
                  fctx.body = [];
                  coerceType3(ctx, fctx, elemType, localType);
                  const instrs = fctx.body;
                  fctx.body = saved;
                  return instrs;
                }
                return [];
              })(),
              { op: "local.set", index: localIdx },
            ],
          });
        } else {
          const localType = getLocalType(fctx, localIdx);
          if (localType && !valTypesMatch(elemType, localType)) {
            coerceType3(ctx, fctx, elemType, localType);
          }
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      }
    }
  }
  fctx.body = savedBodyADA;
  if (isNullableADA) {
    const typeErrMsg = "TypeError: Cannot destructure 'null' or 'undefined'";
    addStringConstantGlobal(ctx, typeErrMsg);
    const strIdx = ctx.stringGlobalMap.get(typeErrMsg);
    const tagIdx = ensureExnTag(ctx);
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "global.get", index: strIdx },
        { op: "throw", tagIdx },
      ],
      else: arrDestructInstrsADA,
    });
  } else {
    fctx.body.push(...arrDestructInstrsADA);
  }
  fctx.body.push({ op: "local.get", index: tmpLocal });
  return resultType;
}
function compileExternrefArrayDestructuringAssignment(ctx, fctx, target, resultType) {
  const tmpLocal = allocLocal(fctx, `__ext_arr_destruct_${fctx.locals.length}`, resultType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  if (resultType.kind === "externref") {
    const typeErrMsg = "TypeError: Cannot destructure 'null' or 'undefined'";
    addStringConstantGlobal(ctx, typeErrMsg);
    const strIdx = ctx.stringGlobalMap.get(typeErrMsg);
    const tagIdx = ensureExnTag(ctx);
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "global.get", index: strIdx },
        { op: "throw", tagIdx },
      ],
      else: [],
    });
  }
  let getIdx = ctx.funcMap.get("__extern_get");
  if (getIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const getType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__extern_get", { kind: "func", typeIdx: getType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (getIdx === void 0) return null;
  let boxIdx = ctx.funcMap.get("__box_number");
  if (boxIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const boxType = addFuncType(ctx, [{ kind: "f64" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__box_number", { kind: "func", typeIdx: boxType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    boxIdx = ctx.funcMap.get("__box_number");
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (boxIdx === void 0 || getIdx === void 0) return null;
  for (let i = 0; i < target.elements.length; i++) {
    const element = target.elements[i];
    if (ts13.isOmittedExpression(element)) continue;
    if (ts13.isSpreadElement(element)) {
      const restTarget = element.expression;
      if (ts13.isIdentifier(restTarget)) {
        const restName = restTarget.text;
        let restLocalIdx = fctx.localMap.get(restName);
        if (restLocalIdx === void 0) {
          restLocalIdx = allocLocal(fctx, restName, { kind: "externref" });
        }
        let sliceIdx = ctx.funcMap.get("__extern_slice");
        if (sliceIdx === void 0) {
          const importsBefore = ctx.numImportFuncs;
          const sliceType = addFuncType(ctx, [{ kind: "externref" }, { kind: "f64" }], [{ kind: "externref" }]);
          addImport(ctx, "env", "__extern_slice", { kind: "func", typeIdx: sliceType });
          shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
          sliceIdx = ctx.funcMap.get("__extern_slice");
          boxIdx = ctx.funcMap.get("__box_number");
          getIdx = ctx.funcMap.get("__extern_get");
        }
        if (sliceIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: tmpLocal });
          fctx.body.push({ op: "f64.const", value: i });
          fctx.body.push({ op: "call", funcIdx: sliceIdx });
          fctx.body.push({ op: "local.set", index: restLocalIdx });
        }
      }
      continue;
    }
    fctx.body.push({ op: "local.get", index: tmpLocal });
    fctx.body.push({ op: "f64.const", value: i });
    fctx.body.push({ op: "call", funcIdx: boxIdx });
    fctx.body.push({ op: "call", funcIdx: getIdx });
    const elemType = { kind: "externref" };
    if (ts13.isIdentifier(element)) {
      const localName = element.text;
      let localIdx = fctx.localMap.get(localName);
      if (localIdx === void 0) {
        localIdx = allocLocal(fctx, localName, elemType);
      }
      const localType = getLocalType(fctx, localIdx);
      if (localType && !valTypesMatch(elemType, localType)) {
        coerceType3(ctx, fctx, elemType, localType);
      }
      fctx.body.push({ op: "local.set", index: localIdx });
    } else if (ts13.isPropertyAccessExpression(element) || ts13.isElementAccessExpression(element)) {
      const tmpElem = allocLocal(fctx, `__ext_arr_elem_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.set", index: tmpElem });
      emitAssignToTarget(ctx, fctx, element, tmpElem, elemType);
    } else if (ts13.isBinaryExpression(element) && element.operatorToken.kind === ts13.SyntaxKind.EqualsToken) {
      const assignTarget = element.left;
      const defaultExpr = element.right;
      if (ts13.isIdentifier(assignTarget)) {
        const localName = assignTarget.text;
        let localIdx = fctx.localMap.get(localName);
        if (localIdx === void 0) {
          localIdx = allocLocal(fctx, localName, elemType);
        }
        const tmpElem = allocLocal(fctx, `__ext_dflt_${fctx.locals.length}`, elemType);
        fctx.body.push({ op: "local.tee", index: tmpElem });
        fctx.body.push({ op: "ref.is_null" });
        const localType = getLocalType(fctx, localIdx);
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: [
            ...(() => {
              const saved = fctx.body;
              fctx.body = [];
              compileExpression2(ctx, fctx, defaultExpr, localType ?? elemType);
              fctx.body.push({ op: "local.set", index: localIdx });
              const instrs = fctx.body;
              fctx.body = saved;
              return instrs;
            })(),
          ],
          else: [
            { op: "local.get", index: tmpElem },
            ...(() => {
              if (localType && !valTypesMatch(elemType, localType)) {
                const saved = fctx.body;
                fctx.body = [];
                coerceType3(ctx, fctx, elemType, localType);
                const instrs = fctx.body;
                fctx.body = saved;
                return instrs;
              }
              return [];
            })(),
            { op: "local.set", index: localIdx },
          ],
        });
      }
    } else if (ts13.isArrayLiteralExpression(element) || ts13.isObjectLiteralExpression(element)) {
      const tmpNested = allocLocal(fctx, `__ext_nested_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.tee", index: tmpNested });
      fctx.body.push({ op: "ref.is_null" });
      const typeErrMsg = "TypeError: Cannot destructure 'null' or 'undefined'";
      addStringConstantGlobal(ctx, typeErrMsg);
      const strIdx = ctx.stringGlobalMap.get(typeErrMsg);
      const tagIdx = ensureExnTag(ctx);
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "global.get", index: strIdx },
          { op: "throw", tagIdx },
        ],
        else: [],
      });
      if (ts13.isArrayLiteralExpression(element)) {
        fctx.body.push({ op: "local.get", index: tmpNested });
        const nestedResult = compileExternrefArrayDestructuringAssignment(ctx, fctx, element, elemType);
        if (nestedResult) {
          fctx.body.push({ op: "drop" });
        }
      }
    }
  }
  fctx.body.push({ op: "local.get", index: tmpLocal });
  return resultType;
}
function emitAssignToTarget(ctx, fctx, target, valueLocal, valueType) {
  if (ts13.isPropertyAccessExpression(target)) {
    if (ts13.isIdentifier(target.expression) && ctx.frozenVars.has(target.expression.text)) {
      emitThrowString2(ctx, fctx, "TypeError: Cannot assign to read only property of frozen object");
      return;
    }
    const objType = ctx.checker.getTypeAtLocation(target.expression);
    let typeName = resolveStructName(ctx, objType);
    if (!typeName && ts13.isIdentifier(target.expression)) {
      typeName = ctx.widenedVarStructMap.get(target.expression.text);
    }
    if (!typeName && target.expression.kind === ts13.SyntaxKind.ThisKeyword) {
      typeName = resolveThisStructName(ctx, fctx);
    }
    if (!typeName) return;
    const structTypeIdx = ctx.structMap.get(typeName);
    const fields = ctx.structFields.get(typeName);
    if (structTypeIdx === void 0 || !fields) return;
    const fieldName = target.name.text;
    const fieldIdx = fields.findIndex((f) => f.name === fieldName);
    if (fieldIdx === -1) return;
    const fieldType = fields[fieldIdx].type;
    compileExpression2(ctx, fctx, target.expression);
    fctx.body.push({ op: "local.get", index: valueLocal });
    if (!valTypesMatch(valueType, fieldType)) {
      coerceType3(ctx, fctx, valueType, fieldType);
    }
    fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
  } else if (ts13.isElementAccessExpression(target)) {
    const arrType = compileExpression2(ctx, fctx, target.expression);
    if (!arrType || (arrType.kind !== "ref" && arrType.kind !== "ref_null")) return;
    const tIdx = arrType.typeIdx;
    const tDef = ctx.mod.types[tIdx];
    if (
      tDef?.kind === "struct" &&
      tDef.fields.length === 2 &&
      tDef.fields[0]?.name === "length" &&
      tDef.fields[1]?.name === "data"
    ) {
      const aIdx = getArrTypeIdxFromVec(ctx, tIdx);
      const vecTmp = allocLocal(fctx, `__dstr_vec_${fctx.locals.length}`, arrType);
      fctx.body.push({ op: "local.set", index: vecTmp });
      const idxResult = compileExpression2(ctx, fctx, target.argumentExpression);
      if (!idxResult) return;
      if (idxResult.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_f64_s" });
      }
      const idxTmp = allocLocal(fctx, `__dstr_idx_${fctx.locals.length}`, {
        kind: "i32",
      });
      fctx.body.push({ op: "local.set", index: idxTmp });
      fctx.body.push({ op: "local.get", index: idxTmp });
      fctx.body.push({ op: "local.get", index: vecTmp });
      fctx.body.push({ op: "struct.get", typeIdx: tIdx, fieldIdx: 1 });
      fctx.body.push({ op: "array.len" });
      fctx.body.push({ op: "i32.lt_u" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: vecTmp },
          { op: "struct.get", typeIdx: tIdx, fieldIdx: 1 },
          { op: "local.get", index: idxTmp },
          { op: "local.get", index: valueLocal },
          { op: "array.set", typeIdx: aIdx },
        ],
        else: [],
      });
    }
  }
}
function emitObjectDestructureFromLocal(ctx, fctx, pattern, srcLocal, srcType) {
  if (srcType.kind !== "ref" && srcType.kind !== "ref_null") return;
  const srcTypeIdx = srcType.typeIdx;
  const structName = ctx.typeIdxToStructName.get(srcTypeIdx);
  if (!structName) return;
  const fields = ctx.structFields.get(structName);
  if (!fields) return;
  const savedBodyODFL = fctx.body;
  const odflInstrs = [];
  fctx.body = odflInstrs;
  for (const prop of pattern.properties) {
    if (ts13.isShorthandPropertyAssignment(prop)) {
      const propName = prop.name.text;
      const fieldIdx = fields.findIndex((f) => f.name === propName);
      if (fieldIdx === -1) continue;
      let localIdx = fctx.localMap.get(propName);
      if (localIdx === void 0) {
        localIdx = allocLocal(fctx, propName, fields[fieldIdx].type);
      }
      fctx.body.push({ op: "local.get", index: srcLocal });
      fctx.body.push({ op: "struct.get", typeIdx: srcTypeIdx, fieldIdx });
      const fieldType = fields[fieldIdx].type;
      const localType = getLocalType(fctx, localIdx);
      if (localType && !valTypesMatch(fieldType, localType)) {
        coerceType3(ctx, fctx, fieldType, localType);
      }
      fctx.body.push({ op: "local.set", index: localIdx });
    } else if (ts13.isPropertyAssignment(prop)) {
      let propName = ts13.isIdentifier(prop.name)
        ? prop.name.text
        : ts13.isStringLiteral(prop.name)
          ? prop.name.text
          : ts13.isNumericLiteral(prop.name)
            ? prop.name.text
            : void 0;
      if (!propName && ts13.isComputedPropertyName(prop.name)) {
        propName = resolveComputedKeyExpression(ctx, prop.name.expression);
      }
      if (!propName) continue;
      const fieldIdx = fields.findIndex((f) => f.name === propName);
      if (fieldIdx === -1) continue;
      const fieldType = fields[fieldIdx].type;
      const targetExpr = prop.initializer;
      if (ts13.isIdentifier(targetExpr)) {
        let localIdx = fctx.localMap.get(targetExpr.text);
        if (localIdx === void 0) {
          localIdx = allocLocal(fctx, targetExpr.text, fieldType);
        }
        fctx.body.push({ op: "local.get", index: srcLocal });
        fctx.body.push({ op: "struct.get", typeIdx: srcTypeIdx, fieldIdx });
        const localType = getLocalType(fctx, localIdx);
        if (localType && !valTypesMatch(fieldType, localType)) {
          coerceType3(ctx, fctx, fieldType, localType);
        }
        emitCoercedLocalSet(ctx, fctx, localIdx, fieldType);
      } else if (ts13.isObjectLiteralExpression(targetExpr)) {
        const tmpNested = allocLocal(fctx, `__nested_${fctx.locals.length}`, fieldType);
        fctx.body.push({ op: "local.get", index: srcLocal });
        fctx.body.push({ op: "struct.get", typeIdx: srcTypeIdx, fieldIdx });
        fctx.body.push({ op: "local.set", index: tmpNested });
        emitObjectDestructureFromLocal(ctx, fctx, targetExpr, tmpNested, fieldType);
      } else if (ts13.isArrayLiteralExpression(targetExpr)) {
        const tmpNested = allocLocal(fctx, `__nested_${fctx.locals.length}`, fieldType);
        fctx.body.push({ op: "local.get", index: srcLocal });
        fctx.body.push({ op: "struct.get", typeIdx: srcTypeIdx, fieldIdx });
        fctx.body.push({ op: "local.set", index: tmpNested });
        emitArrayDestructureFromLocal(ctx, fctx, targetExpr, tmpNested, fieldType);
      } else if (ts13.isPropertyAccessExpression(targetExpr) || ts13.isElementAccessExpression(targetExpr)) {
        const tmpElem = allocLocal(fctx, `__nested_elem_${fctx.locals.length}`, fieldType);
        fctx.body.push({ op: "local.get", index: srcLocal });
        fctx.body.push({ op: "struct.get", typeIdx: srcTypeIdx, fieldIdx });
        fctx.body.push({ op: "local.set", index: tmpElem });
        emitAssignToTarget(ctx, fctx, targetExpr, tmpElem, fieldType);
      }
    }
  }
  fctx.body = savedBodyODFL;
  if (srcType.kind === "ref_null") {
    const typeErrMsg = "TypeError: Cannot destructure 'null' or 'undefined'";
    addStringConstantGlobal(ctx, typeErrMsg);
    const strIdx = ctx.stringGlobalMap.get(typeErrMsg);
    const tagIdx = ensureExnTag(ctx);
    fctx.body.push({ op: "local.get", index: srcLocal });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "global.get", index: strIdx },
        { op: "throw", tagIdx },
      ],
      else: odflInstrs,
    });
  } else {
    fctx.body.push(...odflInstrs);
  }
}
function emitArrayDestructureFromLocal(ctx, fctx, pattern, srcLocal, srcType) {
  if (srcType.kind !== "ref" && srcType.kind !== "ref_null") return;
  const srcTypeIdx = srcType.typeIdx;
  const srcDef = ctx.mod.types[srcTypeIdx];
  if (!srcDef || srcDef.kind !== "struct") return;
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, srcTypeIdx);
  const arrDef = ctx.mod.types[arrTypeIdx];
  if (!arrDef || arrDef.kind !== "array") return;
  const elemType = arrDef.element;
  const savedBodyADFL = fctx.body;
  const adflInstrs = [];
  fctx.body = adflInstrs;
  for (let i = 0; i < pattern.elements.length; i++) {
    const element = pattern.elements[i];
    if (ts13.isOmittedExpression(element)) continue;
    if (ts13.isIdentifier(element)) {
      let localIdx = fctx.localMap.get(element.text);
      if (localIdx === void 0) {
        localIdx = allocLocal(fctx, element.text, elemType);
      }
      fctx.body.push({ op: "local.get", index: srcLocal });
      fctx.body.push({ op: "struct.get", typeIdx: srcTypeIdx, fieldIdx: 1 });
      fctx.body.push({ op: "i32.const", value: i });
      emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elemType);
      const localType = getLocalType(fctx, localIdx);
      if (localType && !valTypesMatch(elemType, localType)) {
        coerceType3(ctx, fctx, elemType, localType);
      }
      emitCoercedLocalSet(ctx, fctx, localIdx, elemType);
    }
  }
  fctx.body = savedBodyADFL;
  if (srcType.kind === "ref_null") {
    const typeErrMsg = "TypeError: Cannot destructure 'null' or 'undefined'";
    addStringConstantGlobal(ctx, typeErrMsg);
    const strIdx = ctx.stringGlobalMap.get(typeErrMsg);
    const tagIdx = ensureExnTag(ctx);
    fctx.body.push({ op: "local.get", index: srcLocal });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "global.get", index: strIdx },
        { op: "throw", tagIdx },
      ],
      else: adflInstrs,
    });
  } else {
    fctx.body.push(...adflInstrs);
  }
}
function compilePropertyAssignment(ctx, fctx, target, value) {
  const objType = ctx.checker.getTypeAtLocation(target.expression);
  if (ts13.isIdentifier(target.expression) && ctx.frozenVars.has(target.expression.text)) {
    const rhsType = compileExpression2(ctx, fctx, value);
    if (rhsType) {
      fctx.body.push({ op: "drop" });
    }
    emitThrowString2(ctx, fctx, "TypeError: Cannot assign to read only property of frozen object");
    return { kind: "f64" };
  }
  if (ts13.isIdentifier(target.expression) && ctx.classSet.has(target.expression.text)) {
    const clsName = target.expression.text;
    const fullName = `${clsName}_${target.name.text}`;
    const globalIdx = ctx.staticProps.get(fullName);
    if (globalIdx !== void 0) {
      const globalDef = ctx.mod.globals[localGlobalIdx(ctx, globalIdx)];
      const valType2 = compileExpression2(ctx, fctx, value, globalDef?.type);
      if (!valType2) return null;
      const tmpVal2 = allocLocal(fctx, `__prop_assign_${fctx.locals.length}`, valType2);
      fctx.body.push({ op: "local.tee", index: tmpVal2 });
      fctx.body.push({ op: "global.set", index: globalIdx });
      fctx.body.push({ op: "local.get", index: tmpVal2 });
      return valType2;
    }
  }
  if (isExternalDeclaredClass(objType, ctx.checker)) {
    const externSetResult = compileExternPropertySet(ctx, fctx, target, value, objType);
    if (externSetResult !== null) return externSetResult;
  }
  if (ts13.isIdentifier(target.expression)) {
    const shapeInfo = ctx.shapeMap.get(target.expression.text);
    if (shapeInfo) {
      const fieldName2 = target.name.text;
      const vecDef = ctx.mod.types[shapeInfo.vecTypeIdx];
      if (vecDef && vecDef.kind === "struct") {
        const fieldIdx2 = vecDef.fields.findIndex((f) => f.name === fieldName2);
        if (fieldIdx2 >= 0) {
          const structObjResult2 = compileExpression2(ctx, fctx, target.expression);
          if (!structObjResult2) return null;
          const valType2 = compileExpression2(ctx, fctx, value, vecDef.fields[fieldIdx2].type);
          if (!valType2) return null;
          const tmpVal2 = allocLocal(fctx, `__prop_assign_${fctx.locals.length}`, valType2);
          fctx.body.push({ op: "local.tee", index: tmpVal2 });
          fctx.body.push({
            op: "struct.set",
            typeIdx: shapeInfo.vecTypeIdx,
            fieldIdx: fieldIdx2,
          });
          fctx.body.push({ op: "local.get", index: tmpVal2 });
          return valType2;
        }
      }
    }
  }
  if (target.name.text === "length") {
    const arrInfo = resolveArrayInfo(ctx, objType);
    if (arrInfo) {
      const { vecTypeIdx } = arrInfo;
      const structObjResult2 = compileExpression2(ctx, fctx, target.expression);
      if (!structObjResult2) return null;
      const vecTmp = allocLocal(fctx, `__arr_len_set_vec_${fctx.locals.length}`, {
        kind: "ref_null",
        typeIdx: vecTypeIdx,
      });
      fctx.body.push({ op: "local.set", index: vecTmp });
      const valType2 = compileExpression2(ctx, fctx, value);
      if (!valType2) return null;
      const newLenTmp = allocLocal(fctx, `__arr_len_set_nl_${fctx.locals.length}`, { kind: "i32" });
      if (valType2.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_f64_s" });
      }
      fctx.body.push({ op: "local.set", index: newLenTmp });
      fctx.body.push({ op: "local.get", index: vecTmp });
      fctx.body.push({ op: "local.get", index: newLenTmp });
      fctx.body.push({ op: "struct.set", typeIdx: vecTypeIdx, fieldIdx: 0 });
      fctx.body.push({ op: "local.get", index: newLenTmp });
      if (!ctx.fast) fctx.body.push({ op: "f64.convert_i32_s" });
      return ctx.fast ? { kind: "i32" } : { kind: "f64" };
    }
  }
  let typeName = resolveStructName(ctx, objType);
  if (!typeName && ts13.isIdentifier(target.expression)) {
    typeName = ctx.widenedVarStructMap.get(target.expression.text);
  }
  if (!typeName && target.expression.kind === ts13.SyntaxKind.ThisKeyword) {
    typeName = resolveThisStructName(ctx, fctx);
  }
  if (!typeName) return null;
  const fieldName = ts13.isPrivateIdentifier(target.name) ? "__priv_" + target.name.text.slice(1) : target.name.text;
  const accessorKey = `${typeName}_${fieldName}`;
  if (ctx.classAccessorSet.has(accessorKey)) {
    const setterName = `${typeName}_set_${fieldName}`;
    const funcIdx = ctx.funcMap.get(setterName);
    if (funcIdx !== void 0) {
      const setterParamTypes = getFuncParamTypes(ctx, funcIdx);
      const setterObjResult = compileExpression2(ctx, fctx, target.expression, setterParamTypes?.[0]);
      if (!setterObjResult) {
        ctx.errors.push({
          message: "Failed to compile setter receiver",
          line: getLine(target),
          column: getCol(target),
        });
        return null;
      }
      const setterValExpectedType = setterParamTypes?.[1];
      const setterValResult = compileExpression2(ctx, fctx, value, setterValExpectedType);
      if (!setterValResult) {
        ctx.errors.push({
          message: "Failed to compile setter value",
          line: getLine(target),
          column: getCol(target),
        });
        return null;
      }
      const setterTmpVal = allocLocal(fctx, `__setter_assign_${fctx.locals.length}`, setterValResult);
      fctx.body.push({ op: "local.tee", index: setterTmpVal });
      const setterHasValueParam = setterParamTypes && setterParamTypes.length > 1;
      if (!setterHasValueParam) {
        fctx.body.push({ op: "drop" });
      }
      const finalSetterIdx = ctx.funcMap.get(setterName) ?? funcIdx;
      fctx.body.push({ op: "call", funcIdx: finalSetterIdx });
      fctx.body.push({ op: "local.get", index: setterTmpVal });
      return setterValResult;
    }
  }
  const structTypeIdx = ctx.structMap.get(typeName);
  const fields = ctx.structFields.get(typeName);
  if (structTypeIdx === void 0 || !fields) return null;
  const fieldIdx = fields.findIndex((f) => f.name === fieldName);
  if (fieldIdx === -1) return null;
  const structSelfType = { kind: "ref_null", typeIdx: structTypeIdx };
  const structObjResult = compileExpression2(ctx, fctx, target.expression, structSelfType);
  if (!structObjResult) {
    ctx.errors.push({
      message: "Failed to compile struct field receiver",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const valType = compileExpression2(ctx, fctx, value, fields[fieldIdx].type);
  if (!valType) return null;
  const tmpVal = allocLocal(fctx, `__prop_assign_${fctx.locals.length}`, valType);
  fctx.body.push({ op: "local.tee", index: tmpVal });
  fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
  fctx.body.push({ op: "local.get", index: tmpVal });
  return valType;
}
function compileExternPropertySet(ctx, fctx, target, value, objType) {
  const className = objType.getSymbol()?.name;
  const propName = target.name.text;
  if (!className) return null;
  const resolvedInfo = findExternInfoForMember(ctx, className, propName, "property");
  const propOwner = resolvedInfo ?? ctx.externClasses.get(className);
  if (!propOwner) return null;
  const importName = `${propOwner.importPrefix}_set_${propName}`;
  const funcIdx = ctx.funcMap.get(importName);
  if (funcIdx === void 0) {
    return null;
  }
  const externObjResult = compileExpression2(ctx, fctx, target.expression);
  if (!externObjResult) {
    ctx.errors.push({
      message: "Failed to compile extern property receiver",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const propInfo = propOwner.properties.get(propName);
  const externValResult = compileExpression2(ctx, fctx, value, propInfo?.type);
  if (!externValResult) {
    ctx.errors.push({
      message: "Failed to compile extern property value",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const externTmpVal = allocLocal(fctx, `__extern_assign_${fctx.locals.length}`, externValResult);
  fctx.body.push({ op: "local.tee", index: externTmpVal });
  fctx.body.push({ op: "call", funcIdx });
  fctx.body.push({ op: "local.get", index: externTmpVal });
  return externValResult;
}
function emitSetterCallWithDummy(ctx, fctx, className, setterName, funcIdx, value) {
  const setterPTypes = getFuncParamTypes(ctx, funcIdx);
  const valTypeHint = setterPTypes?.[1];
  const valResult = compileExpression2(ctx, fctx, value, valTypeHint);
  if (!valResult) return null;
  const tmpLocal = allocLocal(fctx, `__setter_assign_${fctx.locals.length}`, valResult);
  fctx.body.push({ op: "local.tee", index: tmpLocal });
  const valLocal = allocLocal(fctx, `__setter_val_${fctx.locals.length}`, valResult);
  fctx.body.push({ op: "local.set", index: valLocal });
  const structTypeIdx = ctx.structMap.get(className);
  const fields = ctx.structFields.get(className);
  if (structTypeIdx === void 0 || !fields) return valResult;
  for (const field of fields) {
    if (field.name === "__tag") {
      const tag = ctx.classTagMap.get(className) ?? 0;
      fctx.body.push({ op: "i32.const", value: tag });
    } else {
      switch (field.type.kind) {
        case "f64":
          fctx.body.push({ op: "f64.const", value: 0 });
          break;
        case "i32":
          fctx.body.push({ op: "i32.const", value: 0 });
          break;
        case "externref":
          fctx.body.push({ op: "ref.null.extern" });
          break;
        case "ref_null":
          fctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
          break;
        case "ref":
          fctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
          break;
        default:
          fctx.body.push({ op: "i32.const", value: 0 });
          break;
      }
    }
  }
  fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
  fctx.body.push({ op: "local.get", index: valLocal });
  fctx.body.push({ op: "call", funcIdx });
  fctx.body.push({ op: "local.get", index: tmpLocal });
  return valResult;
}
function compileElementAssignment(ctx, fctx, target, value) {
  if (ts13.isIdentifier(target.expression)) {
    const objName = target.expression.text;
    if (ctx.classSet.has(objName)) {
      const key = resolveComputedKeyExpression(ctx, target.argumentExpression);
      if (key !== void 0) {
        const accessorKey = `${objName}_${key}`;
        if (ctx.classAccessorSet.has(accessorKey)) {
          const setterName = `${objName}_set_${key}`;
          const funcIdx = ctx.funcMap.get(setterName);
          if (funcIdx !== void 0) {
            return emitSetterCallWithDummy(ctx, fctx, objName, setterName, funcIdx, value);
          }
        }
        const fullName = `${objName}_${key}`;
        const globalIdx = ctx.staticProps.get(fullName);
        if (globalIdx !== void 0) {
          const globalDef = ctx.mod.globals[localGlobalIdx(ctx, globalIdx)];
          const globalType = globalDef?.type ?? { kind: "f64" };
          const valResult = compileExpression2(ctx, fctx, value, globalType);
          if (!valResult) return null;
          const tmpLocal = allocLocal(fctx, `__static_assign_${fctx.locals.length}`, valResult);
          fctx.body.push({ op: "local.tee", index: tmpLocal });
          fctx.body.push({ op: "global.set", index: globalIdx });
          fctx.body.push({ op: "local.get", index: tmpLocal });
          return valResult;
        }
      }
    }
  }
  if (
    ts13.isPropertyAccessExpression(target.expression) &&
    ts13.isIdentifier(target.expression.expression) &&
    target.expression.name.text === "prototype"
  ) {
    const className = target.expression.expression.text;
    if (ctx.classSet.has(className)) {
      const key = resolveComputedKeyExpression(ctx, target.argumentExpression);
      if (key !== void 0) {
        const accessorKey = `${className}_${key}`;
        if (ctx.classAccessorSet.has(accessorKey) && !ctx.staticAccessorSet.has(accessorKey)) {
          const setterName = `${className}_set_${key}`;
          const funcIdx = ctx.funcMap.get(setterName);
          if (funcIdx !== void 0) {
            return emitSetterCallWithDummy(ctx, fctx, className, setterName, funcIdx, value);
          }
        }
      }
    }
  }
  const arrType = compileExpression2(ctx, fctx, target.expression);
  if (!arrType) {
    ctx.errors.push({
      message: "Assignment to non-array",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  if (arrType.kind !== "ref" && arrType.kind !== "ref_null") {
    return compileExternSetFallback(ctx, fctx, target, value, arrType);
  }
  const typeIdx = arrType.typeIdx;
  const typeDef = ctx.mod.types[typeIdx];
  if (typeDef?.kind === "struct") {
    const isVecStructAssign =
      typeDef.fields.length === 2 && typeDef.fields[0]?.name === "length" && typeDef.fields[1]?.name === "data";
    if (!isVecStructAssign) {
      let fieldName;
      if (ts13.isStringLiteral(target.argumentExpression)) {
        fieldName = target.argumentExpression.text;
      } else if (ts13.isNumericLiteral(target.argumentExpression)) {
        fieldName = target.argumentExpression.text;
      } else if (ts13.isIdentifier(target.argumentExpression)) {
        const sym = ctx.checker.getSymbolAtLocation(target.argumentExpression);
        if (sym) {
          const decl = sym.valueDeclaration;
          if (decl && ts13.isVariableDeclaration(decl) && decl.initializer) {
            const declList = decl.parent;
            if (ts13.isVariableDeclarationList(declList) && (declList.flags & ts13.NodeFlags.Const) !== 0) {
              if (ts13.isStringLiteral(decl.initializer)) {
                fieldName = decl.initializer.text;
              } else if (ts13.isNumericLiteral(decl.initializer)) {
                fieldName = decl.initializer.text;
              }
            }
          }
        }
      }
      if (fieldName === void 0) {
        fieldName = resolveComputedKeyExpression(ctx, target.argumentExpression);
      }
      if (fieldName !== void 0) {
        const objTsType = ctx.checker.getTypeAtLocation(target.expression);
        const sName = resolveStructName(ctx, objTsType);
        if (sName) {
          const accessorKey = `${sName}_${fieldName}`;
          if (ctx.classAccessorSet.has(accessorKey)) {
            const setterName = `${sName}_set_${fieldName}`;
            const funcIdx = ctx.funcMap.get(setterName);
            if (funcIdx !== void 0) {
              const eaSetterParamTypes = getFuncParamTypes(ctx, funcIdx);
              const eaSetterValType = eaSetterParamTypes?.[1];
              const setValResult = compileExpression2(ctx, fctx, value, eaSetterValType);
              if (!setValResult) return null;
              const setValLocal = allocLocal(fctx, `__setter_assign_${fctx.locals.length}`, setValResult);
              fctx.body.push({ op: "local.tee", index: setValLocal });
              if (!eaSetterParamTypes || eaSetterParamTypes.length <= 1) {
                fctx.body.push({ op: "drop" });
              }
              const finalEaSetterIdx = ctx.funcMap.get(setterName) ?? funcIdx;
              fctx.body.push({ op: "call", funcIdx: finalEaSetterIdx });
              fctx.body.push({ op: "local.get", index: setValLocal });
              return setValResult;
            }
          }
        }
        const fieldIdx = typeDef.fields.findIndex((f) => f.name === fieldName);
        if (fieldIdx !== -1) {
          const valType = compileExpression2(ctx, fctx, value, typeDef.fields[fieldIdx].type);
          if (!valType) return null;
          const tmpVal = allocLocal(fctx, `__elem_assign_${fctx.locals.length}`, valType);
          fctx.body.push({ op: "local.tee", index: tmpVal });
          fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
          fctx.body.push({ op: "local.get", index: tmpVal });
          return valType;
        }
      }
    }
  }
  const isVecStruct =
    typeDef?.kind === "struct" &&
    typeDef.fields.length === 2 &&
    typeDef.fields[0]?.name === "length" &&
    typeDef.fields[1]?.name === "data";
  if (isVecStruct) {
    const arrTypeIdx = getArrTypeIdxFromVec(ctx, typeIdx);
    const arrDef = ctx.mod.types[arrTypeIdx];
    if (!arrDef || arrDef.kind !== "array") {
      ctx.errors.push({
        message: "Assignment: vec data is not array",
        line: 0,
        column: 0,
      });
      return null;
    }
    const vecLocal = allocLocal(fctx, `__vec_${fctx.locals.length}`, arrType);
    fctx.body.push({ op: "local.set", index: vecLocal });
    if (arrType.kind === "ref_null" && !isProvablyNonNull(target.expression, ctx.checker)) {
      const tagIdx = ensureExnTag(ctx);
      fctx.body.push({ op: "local.get", index: vecLocal });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: [{ op: "ref.null.extern" }, { op: "throw", tagIdx }],
        else: [],
      });
    }
    const idxResult = compileExpression2(ctx, fctx, target.argumentExpression, {
      kind: "f64",
    });
    if (!idxResult) {
      ctx.errors.push({
        message: "Failed to compile element index",
        line: getLine(target),
        column: getCol(target),
      });
      return null;
    }
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    const idxLocal = allocLocal(fctx, `__idx_${fctx.locals.length}`, {
      kind: "i32",
    });
    fctx.body.push({ op: "local.set", index: idxLocal });
    const elemValResult = compileExpression2(ctx, fctx, value, arrDef.element);
    if (!elemValResult) {
      ctx.errors.push({
        message: "Failed to compile element value",
        line: getLine(target),
        column: getCol(target),
      });
      return null;
    }
    const valLocal = allocLocal(fctx, `__val_${fctx.locals.length}`, arrDef.element);
    fctx.body.push({ op: "local.set", index: valLocal });
    const dataLocal = allocLocal(fctx, `__vec_data_${fctx.locals.length}`, {
      kind: "ref_null",
      typeIdx: arrTypeIdx,
    });
    fctx.body.push({ op: "local.get", index: vecLocal });
    fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
    fctx.body.push({ op: "local.set", index: dataLocal });
    const newCapLocal = allocLocal(fctx, `__vec_ncap_${fctx.locals.length}`, {
      kind: "i32",
    });
    const newDataLocal = allocLocal(fctx, `__vec_ndata_${fctx.locals.length}`, {
      kind: "ref_null",
      typeIdx: arrTypeIdx,
    });
    const oldCapLocal = allocLocal(fctx, `__vec_ocap_${fctx.locals.length}`, {
      kind: "i32",
    });
    fctx.body.push({ op: "local.get", index: idxLocal });
    fctx.body.push({ op: "local.get", index: dataLocal });
    fctx.body.push({ op: "array.len" });
    fctx.body.push({ op: "i32.ge_s" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        // oldCap = array.len(data)
        { op: "local.get", index: dataLocal },
        { op: "array.len" },
        { op: "local.set", index: oldCapLocal },
        // newCap = max(idx + 1, oldCap * 2): store idx+1 first, then compare
        { op: "local.get", index: idxLocal },
        { op: "i32.const", value: 1 },
        { op: "i32.add" },
        { op: "local.set", index: newCapLocal },
        // newCap = idx + 1
        // if oldCap * 2 > newCap, use oldCap * 2
        { op: "local.get", index: oldCapLocal },
        { op: "i32.const", value: 1 },
        { op: "i32.shl" },
        // oldCap * 2
        { op: "local.get", index: newCapLocal },
        { op: "i32.gt_s" },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "local.get", index: oldCapLocal },
            { op: "i32.const", value: 1 },
            { op: "i32.shl" },
            { op: "local.set", index: newCapLocal },
          ],
        },
        // Ensure at least 4
        { op: "i32.const", value: 4 },
        { op: "local.get", index: newCapLocal },
        { op: "i32.gt_s" },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "i32.const", value: 4 },
            { op: "local.set", index: newCapLocal },
          ],
        },
        // newData = array.new_default(newCap)
        { op: "local.get", index: newCapLocal },
        { op: "array.new_default", typeIdx: arrTypeIdx },
        { op: "local.set", index: newDataLocal },
        // array.copy newData[0..oldCap] = data[0..oldCap]
        { op: "local.get", index: newDataLocal },
        { op: "i32.const", value: 0 },
        { op: "local.get", index: dataLocal },
        { op: "i32.const", value: 0 },
        { op: "local.get", index: oldCapLocal },
        {
          op: "array.copy",
          dstTypeIdx: arrTypeIdx,
          srcTypeIdx: arrTypeIdx,
        },
        // Update vec.data = newData
        { op: "local.get", index: vecLocal },
        { op: "local.get", index: newDataLocal },
        { op: "ref.as_non_null" },
        { op: "struct.set", typeIdx, fieldIdx: 1 },
        // Update local data pointer
        { op: "local.get", index: newDataLocal },
        { op: "local.set", index: dataLocal },
      ],
    });
    fctx.body.push({ op: "local.get", index: dataLocal });
    fctx.body.push({ op: "local.get", index: idxLocal });
    fctx.body.push({ op: "local.get", index: valLocal });
    fctx.body.push({ op: "array.set", typeIdx: arrTypeIdx });
    fctx.body.push({ op: "local.get", index: idxLocal });
    fctx.body.push({ op: "i32.const", value: 1 });
    fctx.body.push({ op: "i32.add" });
    fctx.body.push({ op: "local.get", index: vecLocal });
    fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 0 });
    fctx.body.push({ op: "i32.gt_s" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: vecLocal },
        { op: "local.get", index: idxLocal },
        { op: "i32.const", value: 1 },
        { op: "i32.add" },
        { op: "struct.set", typeIdx, fieldIdx: 0 },
      ],
    });
    if (fctx.mappedArgsInfo && ts13.isIdentifier(target.expression) && target.expression.text === "arguments") {
      emitMappedArgReverseSync(ctx, fctx, idxLocal, valLocal);
    }
    fctx.body.push({ op: "local.get", index: valLocal });
    return elemValResult;
  }
  if (typeDef?.kind === "struct") {
    let fieldName;
    if (ts13.isStringLiteral(target.argumentExpression)) {
      fieldName = target.argumentExpression.text;
    } else if (ts13.isNumericLiteral(target.argumentExpression)) {
      fieldName = target.argumentExpression.text;
    } else if (ts13.isIdentifier(target.argumentExpression)) {
      const sym = ctx.checker.getSymbolAtLocation(target.argumentExpression);
      if (sym) {
        const decl = sym.valueDeclaration;
        if (decl && ts13.isVariableDeclaration(decl) && decl.initializer) {
          const declList = decl.parent;
          if (ts13.isVariableDeclarationList(declList) && (declList.flags & ts13.NodeFlags.Const) !== 0) {
            if (ts13.isStringLiteral(decl.initializer)) fieldName = decl.initializer.text;
            else if (ts13.isNumericLiteral(decl.initializer)) fieldName = decl.initializer.text;
          }
        }
      }
    }
    if (fieldName === void 0) {
      fieldName = resolveComputedKeyExpression(ctx, target.argumentExpression);
    }
    if (fieldName !== void 0) {
      const objTsType = ctx.checker.getTypeAtLocation(target.expression);
      const sName = resolveStructName(ctx, objTsType);
      if (sName) {
        const accessorKey = `${sName}_${fieldName}`;
        if (ctx.classAccessorSet.has(accessorKey)) {
          const setterName = `${sName}_set_${fieldName}`;
          const funcIdx = ctx.funcMap.get(setterName);
          if (funcIdx !== void 0) {
            const objLocal = allocLocal(fctx, `__struct_obj_${fctx.locals.length}`, arrType);
            fctx.body.push({ op: "local.set", index: objLocal });
            const valResult = compileExpression2(ctx, fctx, value);
            if (!valResult) return null;
            const valLocal = allocLocal(fctx, `__struct_val_${fctx.locals.length}`, valResult);
            fctx.body.push({ op: "local.set", index: valLocal });
            fctx.body.push({ op: "local.get", index: objLocal });
            const eaSetterPTypes = getFuncParamTypes(ctx, funcIdx);
            if (eaSetterPTypes && eaSetterPTypes.length > 1) {
              fctx.body.push({ op: "local.get", index: valLocal });
            }
            fctx.body.push({ op: "call", funcIdx });
            fctx.body.push({ op: "local.get", index: valLocal });
            return valResult;
          }
        }
      }
      const fieldIdx = typeDef.fields.findIndex((f) => f.name === fieldName);
      if (fieldIdx >= 0) {
        const objLocal = allocLocal(fctx, `__struct_obj_${fctx.locals.length}`, arrType);
        fctx.body.push({ op: "local.set", index: objLocal });
        const fieldType = typeDef.fields[fieldIdx].type;
        const valResult = compileExpression2(ctx, fctx, value, fieldType);
        if (!valResult) return null;
        const valLocal = allocLocal(fctx, `__struct_val_${fctx.locals.length}`, fieldType);
        fctx.body.push({ op: "local.set", index: valLocal });
        fctx.body.push({ op: "local.get", index: objLocal });
        fctx.body.push({ op: "local.get", index: valLocal });
        fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
        fctx.body.push({ op: "local.get", index: valLocal });
        return valResult;
      }
    }
  }
  if (!typeDef || typeDef.kind !== "array") {
    return compileExternSetFallback(ctx, fctx, target, value, arrType);
  }
  const plainIdxResult = compileExpression2(ctx, fctx, target.argumentExpression, { kind: "f64" });
  if (!plainIdxResult) {
    ctx.errors.push({
      message: "Failed to compile element index",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  fctx.body.push({ op: "i32.trunc_sat_f64_s" });
  const plainValResult = compileExpression2(ctx, fctx, value, typeDef.element);
  if (!plainValResult) {
    ctx.errors.push({
      message: "Failed to compile element value",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const plainValLocal = allocLocal(fctx, `__arr_assign_${fctx.locals.length}`, plainValResult);
  fctx.body.push({ op: "local.tee", index: plainValLocal });
  fctx.body.push({ op: "array.set", typeIdx });
  fctx.body.push({ op: "local.get", index: plainValLocal });
  return plainValResult;
}
function compileExternSetFallback(ctx, fctx, target, value, objType) {
  if (objType.kind === "externref") {
  } else if (objType.kind === "f64") {
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: boxIdx });
    } else {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "ref.null.extern" });
    }
  } else if (objType.kind === "i32") {
    fctx.body.push({ op: "f64.convert_i32_s" });
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: boxIdx });
    } else {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "ref.null.extern" });
    }
  } else if (objType.kind === "ref" || objType.kind === "ref_null") {
    fctx.body.push({ op: "extern.convert_any" });
  } else {
    ctx.errors.push({
      message: "Unsupported element assignment target type",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const objLocal = allocLocal(fctx, `__eset_obj_${fctx.locals.length}`, {
    kind: "externref",
  });
  fctx.body.push({ op: "local.set", index: objLocal });
  const valResult = compileExpression2(ctx, fctx, value, { kind: "externref" });
  if (!valResult) return null;
  const valLocal = allocLocal(fctx, `__eset_val_${fctx.locals.length}`, {
    kind: "externref",
  });
  fctx.body.push({ op: "local.set", index: valLocal });
  fctx.body.push({ op: "local.get", index: objLocal });
  compileExpression2(ctx, fctx, target.argumentExpression, {
    kind: "externref",
  });
  fctx.body.push({ op: "local.get", index: valLocal });
  const funcIdx = ensureLateImport2(
    ctx,
    "__extern_set",
    [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }],
    [],
  );
  flushLateImportShifts2(ctx, fctx);
  if (funcIdx !== void 0) {
    fctx.body.push({ op: "call", funcIdx });
  }
  fctx.body.push({ op: "local.get", index: valLocal });
  return { kind: "externref" };
}
function compileLogicalAssignment(ctx, fctx, expr, op) {
  if (ts13.isPropertyAccessExpression(expr.left)) {
    return compilePropertyLogicalAssignment(ctx, fctx, expr.left, expr.right, op);
  }
  if (ts13.isElementAccessExpression(expr.left)) {
    return compileElementLogicalAssignment(ctx, fctx, expr.left, expr.right, op);
  }
  if (!ts13.isIdentifier(expr.left)) {
    ctx.errors.push({
      message: "Logical assignment only supported for simple identifiers, property access, or element access",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const name = expr.left.text;
  let storage = null;
  const localIdx = fctx.localMap.get(name);
  if (localIdx !== void 0) {
    const localType =
      localIdx < fctx.params.length ? fctx.params[localIdx].type : fctx.locals[localIdx - fctx.params.length]?.type;
    storage = {
      kind: "local",
      index: localIdx,
      type: localType ?? { kind: "f64" },
    };
  }
  if (!storage) {
    const capturedIdx = ctx.capturedGlobals.get(name);
    if (capturedIdx !== void 0) {
      const globalDef = ctx.mod.globals[capturedIdx];
      storage = {
        kind: "captured",
        index: capturedIdx,
        type: globalDef?.type ?? { kind: "f64" },
      };
    }
  }
  if (!storage) {
    const moduleIdx = ctx.moduleGlobals.get(name);
    if (moduleIdx !== void 0) {
      const globalDef = ctx.mod.globals[moduleIdx];
      storage = {
        kind: "module",
        index: moduleIdx,
        type: globalDef?.type ?? { kind: "f64" },
      };
    }
  }
  if (!storage) {
    const rhsFallback = compileExpression2(ctx, fctx, expr.right);
    if (rhsFallback) {
      fctx.body.push({ op: "drop" });
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  const varType = storage.type;
  const getStorageIndex = () => {
    if (storage.kind === "local") return storage.index;
    if (storage.kind === "captured") return ctx.capturedGlobals.get(name);
    return ctx.moduleGlobals.get(name);
  };
  const emitGet = () => {
    if (storage.kind === "local") fctx.body.push({ op: "local.get", index: getStorageIndex() });
    else fctx.body.push({ op: "global.get", index: getStorageIndex() });
  };
  const emitSet = () => {
    if (storage.kind === "local") fctx.body.push({ op: "local.tee", index: getStorageIndex() });
    else {
      const idx = getStorageIndex();
      fctx.body.push({ op: "global.set", index: idx });
      fctx.body.push({ op: "global.get", index: idx });
    }
  };
  if (op === ts13.SyntaxKind.QuestionQuestionEqualsToken) {
    if (!isRefType(varType)) {
      emitGet();
      return varType;
    }
    emitGet();
    const qqeTmp = allocTempLocal(fctx, varType);
    fctx.body.push({ op: "local.tee", index: qqeTmp });
    fctx.body.push({ op: "ref.is_null" });
    const qqeUndefIdx = ensureLateImport2(ctx, "__extern_is_undefined", [{ kind: "externref" }], [{ kind: "i32" }]);
    flushLateImportShifts2(ctx, fctx);
    if (qqeUndefIdx !== void 0) {
      fctx.body.push({ op: "local.get", index: qqeTmp });
      if (varType.kind !== "externref") {
        fctx.body.push({ op: "extern.convert_any" });
      }
      fctx.body.push({ op: "call", funcIdx: qqeUndefIdx });
      fctx.body.push({ op: "i32.or" });
    }
    releaseTempLocal(fctx, qqeTmp);
    const savedBody = pushBody(fctx);
    const nullishRhsResult = compileExpression2(ctx, fctx, expr.right, varType);
    if (!nullishRhsResult) {
      fctx.body = savedBody;
      return null;
    }
    emitSet();
    const thenInstrs = fctx.body;
    fctx.body = [];
    emitGet();
    const elseInstrs = fctx.body;
    fctx.body = savedBody;
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: varType },
      then: thenInstrs,
      else: elseInstrs,
    });
  } else if (op === ts13.SyntaxKind.BarBarEqualsToken) {
    emitGet();
    ensureI32Condition(fctx, varType, ctx);
    const savedBody = pushBody(fctx);
    emitGet();
    const thenInstrs = fctx.body;
    fctx.body = [];
    const orRhsResult = compileExpression2(ctx, fctx, expr.right, varType);
    if (!orRhsResult) {
      fctx.body = savedBody;
      return null;
    }
    emitSet();
    const elseInstrs = fctx.body;
    fctx.body = savedBody;
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: varType },
      then: thenInstrs,
      else: elseInstrs,
    });
  } else {
    emitGet();
    ensureI32Condition(fctx, varType, ctx);
    const savedBody = pushBody(fctx);
    const andRhsResult = compileExpression2(ctx, fctx, expr.right, varType);
    if (!andRhsResult) {
      fctx.body = savedBody;
      return null;
    }
    emitSet();
    const thenInstrs = fctx.body;
    fctx.body = [];
    emitGet();
    const elseInstrs = fctx.body;
    fctx.body = savedBody;
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: varType },
      then: thenInstrs,
      else: elseInstrs,
    });
  }
  return varType;
}
function compilePropertyLogicalAssignment(ctx, fctx, target, rhs, op) {
  const objType = ctx.checker.getTypeAtLocation(target.expression);
  const propName = ts13.isPrivateIdentifier(target.name) ? "__priv_" + target.name.text.slice(1) : target.name.text;
  let typeName = resolveStructName(ctx, objType);
  if (!typeName && ts13.isIdentifier(target.expression)) {
    typeName = ctx.widenedVarStructMap.get(target.expression.text);
  }
  if (!typeName) {
    return compilePropertyLogicalAssignmentExternref(ctx, fctx, target, rhs, op, propName);
  }
  const accessorKey = `${typeName}_${propName}`;
  if (ctx.classAccessorSet.has(accessorKey)) {
    const getterName = `${typeName}_get_${propName}`;
    const setterName = `${typeName}_set_${propName}`;
    const getterIdx = ctx.funcMap.get(getterName);
    const setterIdx = ctx.funcMap.get(setterName);
    if (getterIdx !== void 0 && setterIdx !== void 0) {
      const getterPTypes = getFuncParamTypes(ctx, getterIdx);
      const objResult2 = compileExpression2(ctx, fctx, target.expression, getterPTypes?.[0]);
      if (!objResult2) return null;
      const objLocal2 = allocLocal(fctx, `__logprop_acc_obj_${fctx.locals.length}`, objResult2);
      fctx.body.push({ op: "local.set", index: objLocal2 });
      const propType = ctx.checker.getTypeAtLocation(target);
      const fieldType2 = resolveWasmType(ctx, propType);
      const emitFieldGet2 = () => {
        const gIdx = ctx.funcMap.get(getterName);
        fctx.body.push({ op: "local.get", index: objLocal2 });
        fctx.body.push({ op: "call", funcIdx: gIdx });
      };
      const emitFieldSet2 = () => {
        const sIdx = ctx.funcMap.get(setterName);
        const tmpVal = allocLocal(fctx, `__logprop_acc_val_${fctx.locals.length}`, fieldType2);
        fctx.body.push({ op: "local.set", index: tmpVal });
        fctx.body.push({ op: "local.get", index: objLocal2 });
        const logSetterPTypes = getFuncParamTypes(ctx, sIdx);
        if (logSetterPTypes && logSetterPTypes.length > 1) {
          fctx.body.push({ op: "local.get", index: tmpVal });
        }
        fctx.body.push({ op: "call", funcIdx: sIdx });
        fctx.body.push({ op: "local.get", index: tmpVal });
      };
      return emitLogicalAssignmentPattern(ctx, fctx, rhs, op, fieldType2, emitFieldGet2, emitFieldSet2);
    }
  }
  const structTypeIdx = ctx.structMap.get(typeName);
  const fields = ctx.structFields.get(typeName);
  if (structTypeIdx === void 0 || !fields) {
    return compilePropertyLogicalAssignmentExternref(ctx, fctx, target, rhs, op, propName);
  }
  const fieldIdx = fields.findIndex((f) => f.name === propName);
  if (fieldIdx === -1) {
    fctx.body.push({ op: "f64.const", value: NaN });
    return { kind: "f64" };
  }
  const fieldType = fields[fieldIdx].type;
  const objResult = compileExpression2(ctx, fctx, target.expression);
  if (!objResult) return null;
  const objLocal = allocLocal(fctx, `__logprop_obj_${fctx.locals.length}`, objResult);
  fctx.body.push({ op: "local.set", index: objLocal });
  const emitFieldGet = () => {
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
  };
  const emitFieldSet = () => {
    const tmpVal = allocLocal(fctx, `__logprop_val_${fctx.locals.length}`, fieldType);
    fctx.body.push({ op: "local.set", index: tmpVal });
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "local.get", index: tmpVal });
    fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
    fctx.body.push({ op: "local.get", index: tmpVal });
  };
  return emitLogicalAssignmentPattern(ctx, fctx, rhs, op, fieldType, emitFieldGet, emitFieldSet);
}
function compilePropertyLogicalAssignmentExternref(ctx, fctx, target, rhs, op, propName) {
  const objResult = compileExpression2(ctx, fctx, target.expression);
  if (!objResult) return null;
  if (objResult.kind === "ref" || objResult.kind === "ref_null") {
    const typeIdx = objResult.typeIdx;
    const resolvedTypeName = ctx.typeIdxToStructName.get(typeIdx);
    if (resolvedTypeName) {
      const fields = ctx.structFields.get(resolvedTypeName);
      if (fields) {
        let fieldIdx = fields.findIndex((f) => f.name === propName);
        if (fieldIdx === -1 && !ctx.classSet.has(resolvedTypeName)) {
          const objTsType = ctx.checker.getTypeAtLocation(target.expression);
          const tsProps = objTsType.getProperties?.();
          if (tsProps) {
            const tsProp = tsProps.find((p) => p.name === propName);
            if (tsProp) {
              const propTsType = ctx.checker.getTypeOfSymbolAtLocation(tsProp, target);
              const propWasmType = resolveWasmType(ctx, propTsType);
              const newField = {
                name: propName,
                type: propWasmType,
                mutable: true,
              };
              fields.push(newField);
              patchStructNewForAddedField(ctx, fctx, typeIdx, propWasmType);
              const typeDef = ctx.mod.types[typeIdx];
              if (typeDef?.kind === "struct" && typeDef.fields !== fields) {
                typeDef.fields.push(newField);
              }
              patchStructNewForDynamicField(ctx, typeIdx, propWasmType);
              fieldIdx = fields.length - 1;
            }
          }
        }
        if (fieldIdx !== -1) {
          const fieldType = fields[fieldIdx].type;
          const objTmp = allocLocal(fctx, `__logprop_ext_obj_${fctx.locals.length}`, objResult);
          fctx.body.push({ op: "local.set", index: objTmp });
          const emitGet2 = () => {
            fctx.body.push({ op: "local.get", index: objTmp });
            fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
          };
          const emitSet2 = () => {
            const tmpVal = allocLocal(fctx, `__logprop_ext_val_${fctx.locals.length}`, fieldType);
            fctx.body.push({ op: "local.set", index: tmpVal });
            fctx.body.push({ op: "local.get", index: objTmp });
            fctx.body.push({ op: "local.get", index: tmpVal });
            fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
            fctx.body.push({ op: "local.get", index: tmpVal });
          };
          return emitLogicalAssignmentPattern(ctx, fctx, rhs, op, fieldType, emitGet2, emitSet2);
        }
      }
    }
    fctx.body.push({ op: "extern.convert_any" });
  } else if (objResult.kind !== "externref") {
    if (objResult.kind === "f64") {
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      } else {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "f64.const", value: NaN });
        return { kind: "f64" };
      }
    } else if (objResult.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
      const boxIdx = ctx.funcMap.get("__box_number");
      if (boxIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx });
      } else {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "f64.const", value: NaN });
        return { kind: "f64" };
      }
    } else {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "f64.const", value: NaN });
      return { kind: "f64" };
    }
  }
  const objLocal = allocLocal(fctx, `__logprop_pobj_${fctx.locals.length}`, {
    kind: "externref",
  });
  fctx.body.push({ op: "local.set", index: objLocal });
  addStringConstantGlobal(ctx, propName);
  const keyResult = compileStringLiteral(ctx, fctx, propName);
  if (!keyResult) return null;
  if (keyResult.kind !== "externref") {
    coerceType3(ctx, fctx, keyResult, { kind: "externref" });
  }
  const keyLocal = allocLocal(fctx, `__logprop_pkey_${fctx.locals.length}`, {
    kind: "externref",
  });
  fctx.body.push({ op: "local.set", index: keyLocal });
  const getIdx = ensureLateImport2(
    ctx,
    "__extern_get",
    [{ kind: "externref" }, { kind: "externref" }],
    [{ kind: "externref" }],
  );
  if (getIdx === void 0) return null;
  const setIdx = ensureLateImport2(
    ctx,
    "__extern_set",
    [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }],
    [],
  );
  flushLateImportShifts2(ctx, fctx);
  if (setIdx === void 0) return null;
  addUnionImports(ctx);
  const varType = { kind: "externref" };
  const finalGetIdx = getIdx;
  const finalSetIdx = setIdx;
  const emitGet = () => {
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "local.get", index: keyLocal });
    fctx.body.push({ op: "call", funcIdx: finalGetIdx });
  };
  const emitSet = () => {
    const tmpVal = allocLocal(fctx, `__logprop_pval_${fctx.locals.length}`, {
      kind: "externref",
    });
    fctx.body.push({ op: "local.set", index: tmpVal });
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "local.get", index: keyLocal });
    fctx.body.push({ op: "local.get", index: tmpVal });
    fctx.body.push({ op: "call", funcIdx: finalSetIdx });
    fctx.body.push({ op: "local.get", index: tmpVal });
  };
  return emitLogicalAssignmentPattern(ctx, fctx, rhs, op, varType, emitGet, emitSet);
}
function compileElementLogicalAssignment(ctx, fctx, target, rhs, op) {
  const arrType = compileExpression2(ctx, fctx, target.expression);
  if (!arrType || (arrType.kind !== "ref" && arrType.kind !== "ref_null")) {
    ctx.errors.push({
      message: "Logical assignment on non-array element access",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const typeIdx = arrType.typeIdx;
  const typeDef = ctx.mod.types[typeIdx];
  if (typeDef?.kind === "struct") {
    const isVecStruct =
      typeDef.fields.length === 2 && typeDef.fields[0]?.name === "length" && typeDef.fields[1]?.name === "data";
    if (!isVecStruct) {
      let fieldName;
      if (ts13.isStringLiteral(target.argumentExpression)) {
        fieldName = target.argumentExpression.text;
      } else if (ts13.isNumericLiteral(target.argumentExpression)) {
        fieldName = target.argumentExpression.text;
      }
      if (fieldName !== void 0) {
        const fieldIdx = typeDef.fields.findIndex((f) => f.name === fieldName);
        if (fieldIdx !== -1) {
          const fieldType = typeDef.fields[fieldIdx].type;
          const objLocal = allocLocal(fctx, `__logelem_obj_${fctx.locals.length}`, arrType);
          fctx.body.push({ op: "local.set", index: objLocal });
          const emitFieldGet = () => {
            fctx.body.push({ op: "local.get", index: objLocal });
            fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
          };
          const emitFieldSet = () => {
            const tmpVal = allocLocal(fctx, `__logelem_val_${fctx.locals.length}`, fieldType);
            fctx.body.push({ op: "local.set", index: tmpVal });
            fctx.body.push({ op: "local.get", index: objLocal });
            fctx.body.push({ op: "local.get", index: tmpVal });
            fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
            fctx.body.push({ op: "local.get", index: tmpVal });
          };
          return emitLogicalAssignmentPattern(ctx, fctx, rhs, op, fieldType, emitFieldGet, emitFieldSet);
        }
      }
    }
    if (isVecStruct) {
      const arrLocal = allocLocal(fctx, `__logelem_arr_${fctx.locals.length}`, arrType);
      fctx.body.push({ op: "local.set", index: arrLocal });
      const idxResult = compileExpression2(ctx, fctx, target.argumentExpression, { kind: "f64" });
      if (!idxResult) return null;
      if (idxResult.kind !== "i32") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
      const idxLocal = allocLocal(fctx, `__logelem_idx_${fctx.locals.length}`, {
        kind: "i32",
      });
      fctx.body.push({ op: "local.set", index: idxLocal });
      const dataField = typeDef.fields[1];
      const dataTypeIdx = dataField.type.typeIdx;
      const dataDef = ctx.mod.types[dataTypeIdx];
      if (!dataDef || dataDef.kind !== "array") {
        ctx.errors.push({
          message: "Vec struct data field is not an array",
          line: getLine(target),
          column: getCol(target),
        });
        return null;
      }
      const elemType = dataDef.element;
      const emitElemGet = () => {
        fctx.body.push({ op: "local.get", index: arrLocal });
        fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
        fctx.body.push({ op: "local.get", index: idxLocal });
        emitBoundsCheckedArrayGet2(fctx, dataTypeIdx, elemType);
      };
      const emitElemSet = () => {
        const tmpVal = allocLocal(fctx, `__logelem_aval_${fctx.locals.length}`, elemType);
        fctx.body.push({ op: "local.set", index: tmpVal });
        fctx.body.push({ op: "local.get", index: idxLocal });
        fctx.body.push({ op: "local.get", index: arrLocal });
        fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
        fctx.body.push({ op: "array.len" });
        fctx.body.push({ op: "i32.lt_u" });
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "local.get", index: arrLocal },
            { op: "struct.get", typeIdx, fieldIdx: 1 },
            { op: "local.get", index: idxLocal },
            { op: "local.get", index: tmpVal },
            { op: "array.set", typeIdx: dataTypeIdx },
          ],
          else: [],
        });
        fctx.body.push({ op: "local.get", index: tmpVal });
      };
      return emitLogicalAssignmentPattern(ctx, fctx, rhs, op, elemType, emitElemGet, emitElemSet);
    }
  }
  ctx.errors.push({
    message: "Unsupported element access logical assignment target",
    line: getLine(target),
    column: getCol(target),
  });
  return null;
}
function isRefType(t) {
  return (
    t.kind === "ref" ||
    t.kind === "ref_null" ||
    t.kind === "funcref" ||
    t.kind === "externref" ||
    t.kind === "ref_extern" ||
    t.kind === "eqref"
  );
}
function emitLogicalAssignmentPattern(ctx, fctx, rhs, op, varType, emitGet, emitSet) {
  if (op === ts13.SyntaxKind.QuestionQuestionEqualsToken) {
    if (!isRefType(varType)) {
      emitGet();
      return varType;
    }
    emitGet();
    fctx.body.push({ op: "ref.is_null" });
    const savedBody = pushBody(fctx);
    const rhsResult = compileExpression2(ctx, fctx, rhs, varType);
    if (!rhsResult) {
      fctx.body = savedBody;
      return null;
    }
    emitSet();
    const thenInstrs = fctx.body;
    fctx.body = [];
    emitGet();
    const elseInstrs = fctx.body;
    fctx.body = savedBody;
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: varType },
      then: thenInstrs,
      else: elseInstrs,
    });
  } else if (op === ts13.SyntaxKind.BarBarEqualsToken) {
    emitGet();
    ensureI32Condition(fctx, varType, ctx);
    const savedBody = pushBody(fctx);
    emitGet();
    const thenInstrs = fctx.body;
    fctx.body = [];
    const rhsResult = compileExpression2(ctx, fctx, rhs, varType);
    if (!rhsResult) {
      fctx.body = savedBody;
      return null;
    }
    emitSet();
    const elseInstrs = fctx.body;
    fctx.body = savedBody;
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: varType },
      then: thenInstrs,
      else: elseInstrs,
    });
  } else {
    emitGet();
    ensureI32Condition(fctx, varType, ctx);
    const savedBody = pushBody(fctx);
    const rhsResult = compileExpression2(ctx, fctx, rhs, varType);
    if (!rhsResult) {
      fctx.body = savedBody;
      return null;
    }
    emitSet();
    const thenInstrs = fctx.body;
    fctx.body = [];
    emitGet();
    const elseInstrs = fctx.body;
    fctx.body = savedBody;
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: varType },
      then: thenInstrs,
      else: elseInstrs,
    });
  }
  return varType;
}
function isCompoundAssignment(op) {
  return (
    op === ts13.SyntaxKind.PlusEqualsToken ||
    op === ts13.SyntaxKind.MinusEqualsToken ||
    op === ts13.SyntaxKind.AsteriskEqualsToken ||
    op === ts13.SyntaxKind.AsteriskAsteriskEqualsToken ||
    op === ts13.SyntaxKind.SlashEqualsToken ||
    op === ts13.SyntaxKind.PercentEqualsToken ||
    op === ts13.SyntaxKind.AmpersandEqualsToken ||
    op === ts13.SyntaxKind.BarEqualsToken ||
    op === ts13.SyntaxKind.CaretEqualsToken ||
    op === ts13.SyntaxKind.LessThanLessThanEqualsToken ||
    op === ts13.SyntaxKind.GreaterThanGreaterThanEqualsToken ||
    op === ts13.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken
  );
}
function compileStringCompoundAssignment(ctx, fctx, expr, name) {
  addStringImports(ctx);
  const concatIdx = ctx.funcMap.get("concat");
  if (concatIdx === void 0) {
    ctx.errors.push({
      message: "String concat import not available",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const localIdx = fctx.localMap.get(name);
  const capturedIdx = ctx.capturedGlobals.get(name);
  const moduleIdx = ctx.moduleGlobals.get(name);
  if (localIdx !== void 0) {
    fctx.body.push({ op: "local.get", index: localIdx });
  } else if (capturedIdx !== void 0) {
    fctx.body.push({ op: "global.get", index: capturedIdx });
  } else if (moduleIdx !== void 0) {
    fctx.body.push({ op: "global.get", index: moduleIdx });
  } else {
    const rhsFallback = compileExpression2(ctx, fctx, expr.right);
    if (rhsFallback) {
      fctx.body.push({ op: "drop" });
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  const rhsType = compileExpression2(ctx, fctx, expr.right);
  if (!rhsType) {
    ctx.errors.push({
      message: "Failed to compile string += RHS",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  if (rhsType.kind === "f64" || rhsType.kind === "i32") {
    const rhsTsType = ctx.checker.getTypeAtLocation(expr.right);
    if (isBooleanType(rhsTsType) && rhsType.kind === "i32") {
      emitBoolToString(ctx, fctx);
    } else {
      if (rhsType.kind === "i32") fctx.body.push({ op: "f64.convert_i32_s" });
      const toStr = ctx.funcMap.get("number_toString");
      if (toStr !== void 0) fctx.body.push({ op: "call", funcIdx: toStr });
    }
  }
  fctx.body.push({ op: "call", funcIdx: concatIdx });
  if (localIdx !== void 0) {
    fctx.body.push({ op: "local.tee", index: localIdx });
  } else if (capturedIdx !== void 0) {
    const capturedIdxPost = ctx.capturedGlobals.get(name);
    fctx.body.push({ op: "global.set", index: capturedIdxPost });
    fctx.body.push({ op: "global.get", index: capturedIdxPost });
  } else if (moduleIdx !== void 0) {
    const moduleIdxPost = ctx.moduleGlobals.get(name);
    fctx.body.push({ op: "global.set", index: moduleIdxPost });
    fctx.body.push({ op: "global.get", index: moduleIdxPost });
  }
  return { kind: "externref" };
}
function hasStringAssignment(name, fromExpr) {
  let scope = fromExpr;
  while (
    scope &&
    !ts13.isFunctionDeclaration(scope) &&
    !ts13.isFunctionExpression(scope) &&
    !ts13.isArrowFunction(scope) &&
    !ts13.isMethodDeclaration(scope) &&
    !ts13.isSourceFile(scope)
  ) {
    scope = scope.parent;
  }
  if (!scope) return false;
  let found = false;
  function visit(node) {
    if (found) return;
    if (
      ts13.isBinaryExpression(node) &&
      node.operatorToken.kind === ts13.SyntaxKind.EqualsToken &&
      ts13.isIdentifier(node.left) &&
      node.left.text === name
    ) {
      if (
        ts13.isStringLiteral(node.right) ||
        ts13.isNoSubstitutionTemplateLiteral(node.right) ||
        ts13.isTemplateExpression(node.right)
      ) {
        found = true;
        return;
      }
    }
    if (
      ts13.isVariableDeclaration(node) &&
      ts13.isIdentifier(node.name) &&
      node.name.text === name &&
      node.initializer
    ) {
      if (
        ts13.isStringLiteral(node.initializer) ||
        ts13.isNoSubstitutionTemplateLiteral(node.initializer) ||
        ts13.isTemplateExpression(node.initializer)
      ) {
        found = true;
        return;
      }
    }
    ts13.forEachChild(node, visit);
  }
  ts13.forEachChild(scope, visit);
  return found;
}
function hasStringAssignmentInParentScopes(name, fromExpr) {
  let root = fromExpr;
  while (root.parent) root = root.parent;
  if (!ts13.isSourceFile(root)) return false;
  let found = false;
  function visit(node) {
    if (found) return;
    if (
      ts13.isBinaryExpression(node) &&
      node.operatorToken.kind === ts13.SyntaxKind.EqualsToken &&
      ts13.isIdentifier(node.left) &&
      node.left.text === name
    ) {
      if (
        ts13.isStringLiteral(node.right) ||
        ts13.isNoSubstitutionTemplateLiteral(node.right) ||
        ts13.isTemplateExpression(node.right)
      ) {
        found = true;
        return;
      }
    }
    if (
      ts13.isVariableDeclaration(node) &&
      ts13.isIdentifier(node.name) &&
      node.name.text === name &&
      node.initializer
    ) {
      if (
        ts13.isStringLiteral(node.initializer) ||
        ts13.isNoSubstitutionTemplateLiteral(node.initializer) ||
        ts13.isTemplateExpression(node.initializer)
      ) {
        found = true;
        return;
      }
    }
    ts13.forEachChild(node, visit);
  }
  ts13.forEachChild(root, visit);
  return found;
}
function compileCompoundAssignment(ctx, fctx, expr, op) {
  if (ts13.isPropertyAccessExpression(expr.left)) {
    return compilePropertyCompoundAssignment(ctx, fctx, expr.left, expr.right, op);
  }
  if (ts13.isElementAccessExpression(expr.left)) {
    return compileElementCompoundAssignment(ctx, fctx, expr.left, expr.right, op);
  }
  if (!ts13.isIdentifier(expr.left)) {
    ctx.errors.push({
      message: "Compound assignment only supported for simple identifiers",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const name = expr.left.text;
  if (fctx.constBindings?.has(name)) {
    const rhsType = compileExpression2(ctx, fctx, expr.right);
    if (rhsType) fctx.body.push({ op: "drop" });
    emitThrowString2(ctx, fctx, "TypeError: Assignment to constant variable.");
    fctx.body.push({ op: "unreachable" });
    return { kind: "f64" };
  }
  if (op === ts13.SyntaxKind.PlusEqualsToken) {
    const leftTsType = ctx.checker.getTypeAtLocation(expr.left);
    let isStr = isStringType(leftTsType);
    if (!isStr && (leftTsType.flags & ts13.TypeFlags.Any) !== 0) {
      isStr = hasStringAssignment(name, expr);
    }
    if (isStr) {
      return compileStringCompoundAssignment(ctx, fctx, expr, name);
    }
  }
  const capturedIdx = ctx.capturedGlobals.get(name);
  if (capturedIdx !== void 0 && fctx.localMap.get(name) === void 0) {
    const globalDef = ctx.mod.globals[localGlobalIdx(ctx, capturedIdx)];
    const globalType = globalDef?.type ?? { kind: "f64" };
    const needsCoerce = globalType.kind !== "f64";
    fctx.body.push({ op: "global.get", index: capturedIdx });
    if (needsCoerce) coerceType3(ctx, fctx, globalType, { kind: "f64" });
    const compoundRhsType1 = compileExpression2(ctx, fctx, expr.right, {
      kind: "f64",
    });
    if (!compoundRhsType1) {
      ctx.errors.push({
        message: "Failed to compile compound assignment RHS",
        line: getLine(expr),
        column: getCol(expr),
      });
      return null;
    }
    if (compoundRhsType1.kind !== "f64") coerceType3(ctx, fctx, compoundRhsType1, { kind: "f64" });
    emitCompoundOp(ctx, fctx, op);
    const capturedIdxPost = ctx.capturedGlobals.get(name);
    if (needsCoerce) coerceType3(ctx, fctx, { kind: "f64" }, globalType);
    fctx.body.push({ op: "global.set", index: capturedIdxPost });
    fctx.body.push({ op: "global.get", index: capturedIdxPost });
    return globalType;
  }
  const moduleIdx = ctx.moduleGlobals.get(name);
  if (moduleIdx !== void 0 && fctx.localMap.get(name) === void 0) {
    const globalDef = ctx.mod.globals[localGlobalIdx(ctx, moduleIdx)];
    const globalType = globalDef?.type ?? { kind: "f64" };
    const needsCoerce = globalType.kind !== "f64";
    fctx.body.push({ op: "global.get", index: moduleIdx });
    if (needsCoerce) coerceType3(ctx, fctx, globalType, { kind: "f64" });
    const compoundRhsType2 = compileExpression2(ctx, fctx, expr.right, {
      kind: "f64",
    });
    if (!compoundRhsType2) {
      ctx.errors.push({
        message: "Failed to compile compound assignment RHS",
        line: getLine(expr),
        column: getCol(expr),
      });
      return null;
    }
    if (compoundRhsType2.kind !== "f64") coerceType3(ctx, fctx, compoundRhsType2, { kind: "f64" });
    emitCompoundOp(ctx, fctx, op);
    const moduleIdxPost = ctx.moduleGlobals.get(name);
    if (needsCoerce) coerceType3(ctx, fctx, { kind: "f64" }, globalType);
    fctx.body.push({ op: "global.set", index: moduleIdxPost });
    fctx.body.push({ op: "global.get", index: moduleIdxPost });
    return globalType;
  }
  let localIdx = fctx.localMap.get(name);
  if (localIdx === void 0) {
    const tsType = ctx.checker.getTypeAtLocation(expr.left);
    const wasmType = resolveWasmType(ctx, tsType);
    localIdx = allocLocal(fctx, name, wasmType);
  }
  const boxed = fctx.boxedCaptures?.get(name);
  if (boxed) {
    fctx.body.push({ op: "local.get", index: localIdx });
    emitNullGuardedStructGet(
      ctx,
      fctx,
      { kind: "ref_null", typeIdx: boxed.refCellTypeIdx },
      boxed.valType,
      boxed.refCellTypeIdx,
      0,
      void 0,
      false,
    );
    if (boxed.valType.kind === "externref" && op === ts13.SyntaxKind.PlusEqualsToken) {
      const rightTsType = ctx.checker.getTypeAtLocation(expr.right);
      const rhsIsString = isStringType(rightTsType);
      const varHasStringAssign = hasStringAssignment(name, expr) || hasStringAssignmentInParentScopes(name, expr);
      if (rhsIsString || varHasStringAssign) {
        addStringImports(ctx);
        const concatIdx = ctx.funcMap.get("concat");
        if (concatIdx !== void 0) {
          const compoundRhsStr = compileExpression2(ctx, fctx, expr.right);
          if (!compoundRhsStr) {
            ctx.errors.push({
              message: "Failed to compile compound assignment RHS",
              line: getLine(expr),
              column: getCol(expr),
            });
            return null;
          }
          if (compoundRhsStr.kind === "f64" || compoundRhsStr.kind === "i32") {
            if (compoundRhsStr.kind === "i32") fctx.body.push({ op: "f64.convert_i32_s" });
            const toStr = ctx.funcMap.get("number_toString");
            if (toStr !== void 0) fctx.body.push({ op: "call", funcIdx: toStr });
          }
          fctx.body.push({ op: "call", funcIdx: concatIdx });
          const tmpStrResult = allocLocal(fctx, `__box_cmp_${fctx.locals.length}`, boxed.valType);
          fctx.body.push({ op: "local.set", index: tmpStrResult });
          fctx.body.push({ op: "local.get", index: localIdx });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [],
            else: [
              { op: "local.get", index: localIdx },
              { op: "local.get", index: tmpStrResult },
              { op: "struct.set", typeIdx: boxed.refCellTypeIdx, fieldIdx: 0 },
            ],
          });
          fctx.body.push({ op: "local.get", index: tmpStrResult });
          return boxed.valType;
        }
      }
    }
    const boxedNeedsCoerce = boxed.valType.kind !== "f64" && boxed.valType.kind !== "i32";
    if (boxedNeedsCoerce) {
      coerceType3(ctx, fctx, boxed.valType, { kind: "f64" });
    }
    const compoundRhsBoxed = compileExpression2(
      ctx,
      fctx,
      expr.right,
      boxedNeedsCoerce ? { kind: "f64" } : boxed.valType,
    );
    if (!compoundRhsBoxed) {
      ctx.errors.push({
        message: "Failed to compile compound assignment RHS",
        line: getLine(expr),
        column: getCol(expr),
      });
      return null;
    }
    if (boxedNeedsCoerce && compoundRhsBoxed.kind !== "f64") {
      coerceType3(ctx, fctx, compoundRhsBoxed, { kind: "f64" });
    }
    switch (op) {
      case ts13.SyntaxKind.PlusEqualsToken:
        fctx.body.push({ op: "f64.add" });
        break;
      case ts13.SyntaxKind.MinusEqualsToken:
        fctx.body.push({ op: "f64.sub" });
        break;
      case ts13.SyntaxKind.AsteriskEqualsToken:
        fctx.body.push({ op: "f64.mul" });
        break;
      case ts13.SyntaxKind.SlashEqualsToken:
        fctx.body.push({ op: "f64.div" });
        break;
      case ts13.SyntaxKind.PercentEqualsToken:
        emitModulo(fctx);
        break;
      case ts13.SyntaxKind.AsteriskAsteriskEqualsToken: {
        const fi = ctx.funcMap.get("Math_pow");
        if (fi !== void 0) fctx.body.push({ op: "call", funcIdx: fi });
        break;
      }
      case ts13.SyntaxKind.AmpersandEqualsToken:
      case ts13.SyntaxKind.BarEqualsToken:
      case ts13.SyntaxKind.CaretEqualsToken:
      case ts13.SyntaxKind.LessThanLessThanEqualsToken:
      case ts13.SyntaxKind.GreaterThanGreaterThanEqualsToken:
      case ts13.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken:
        emitBitwiseCompoundOp(fctx, op);
        break;
    }
    if (boxedNeedsCoerce) {
      coerceType3(ctx, fctx, { kind: "f64" }, boxed.valType);
    }
    const tmpResult = allocLocal(fctx, `__box_cmp_${fctx.locals.length}`, boxed.valType);
    fctx.body.push({ op: "local.set", index: tmpResult });
    fctx.body.push({ op: "local.get", index: localIdx });
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: [],
      else: [
        { op: "local.get", index: localIdx },
        { op: "local.get", index: tmpResult },
        {
          op: "struct.set",
          typeIdx: boxed.refCellTypeIdx,
          fieldIdx: 0,
        },
      ],
    });
    fctx.body.push({ op: "local.get", index: tmpResult });
    return boxed.valType;
  }
  const localType = getLocalType(fctx, localIdx) ?? { kind: "f64" };
  const needsLocalCoerce = localType.kind !== "f64";
  fctx.body.push({ op: "local.get", index: localIdx });
  if (needsLocalCoerce) coerceType3(ctx, fctx, localType, { kind: "f64" });
  const compoundRhsType3 = compileExpression2(ctx, fctx, expr.right, {
    kind: "f64",
  });
  if (!compoundRhsType3) {
    ctx.errors.push({
      message: "Failed to compile compound assignment RHS",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  if (compoundRhsType3.kind !== "f64") coerceType3(ctx, fctx, compoundRhsType3, { kind: "f64" });
  emitCompoundOp(ctx, fctx, op);
  if (needsLocalCoerce) {
    coerceType3(ctx, fctx, { kind: "f64" }, localType);
    fctx.body.push({ op: "local.tee", index: localIdx });
    emitMappedArgParamSync(ctx, fctx, localIdx, localType);
    return localType;
  }
  fctx.body.push({ op: "local.tee", index: localIdx });
  emitMappedArgParamSync(ctx, fctx, localIdx, { kind: "f64" });
  return { kind: "f64" };
}
function emitBitwiseCompoundOp(fctx, op) {
  const opMap = {
    [ts13.SyntaxKind.AmpersandEqualsToken]: { i32op: "i32.and", unsigned: false },
    [ts13.SyntaxKind.BarEqualsToken]: { i32op: "i32.or", unsigned: false },
    [ts13.SyntaxKind.CaretEqualsToken]: { i32op: "i32.xor", unsigned: false },
    [ts13.SyntaxKind.LessThanLessThanEqualsToken]: {
      i32op: "i32.shl",
      unsigned: false,
    },
    [ts13.SyntaxKind.GreaterThanGreaterThanEqualsToken]: {
      i32op: "i32.shr_s",
      unsigned: false,
    },
    [ts13.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken]: {
      i32op: "i32.shr_u",
      unsigned: true,
    },
  };
  const entry = opMap[op];
  const tmpR = allocLocal(fctx, `__bw_r_${fctx.locals.length}`, {
    kind: "f64",
  });
  fctx.body.push({ op: "local.set", index: tmpR });
  emitToInt32(fctx);
  fctx.body.push({ op: "local.get", index: tmpR });
  emitToInt32(fctx);
  fctx.body.push({ op: entry.i32op });
  fctx.body.push({
    op: entry.unsigned ? "f64.convert_i32_u" : "f64.convert_i32_s",
  });
}
function emitCompoundOp(ctx, fctx, op) {
  switch (op) {
    case ts13.SyntaxKind.PlusEqualsToken:
      fctx.body.push({ op: "f64.add" });
      break;
    case ts13.SyntaxKind.MinusEqualsToken:
      fctx.body.push({ op: "f64.sub" });
      break;
    case ts13.SyntaxKind.AsteriskEqualsToken:
      fctx.body.push({ op: "f64.mul" });
      break;
    case ts13.SyntaxKind.AsteriskAsteriskEqualsToken: {
      const funcIdx = ctx.funcMap.get("Math_pow");
      if (funcIdx !== void 0) fctx.body.push({ op: "call", funcIdx });
      break;
    }
    case ts13.SyntaxKind.SlashEqualsToken:
      fctx.body.push({ op: "f64.div" });
      break;
    case ts13.SyntaxKind.PercentEqualsToken:
      emitModulo(fctx);
      break;
    case ts13.SyntaxKind.AmpersandEqualsToken:
    case ts13.SyntaxKind.BarEqualsToken:
    case ts13.SyntaxKind.CaretEqualsToken:
    case ts13.SyntaxKind.LessThanLessThanEqualsToken:
    case ts13.SyntaxKind.GreaterThanGreaterThanEqualsToken:
    case ts13.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken:
      emitBitwiseCompoundOp(fctx, op);
      break;
  }
}
function compilePropertyCompoundAssignment(ctx, fctx, target, rhs, op) {
  const objType = ctx.checker.getTypeAtLocation(target.expression);
  const propName = ts13.isPrivateIdentifier(target.name) ? "__priv_" + target.name.text.slice(1) : target.name.text;
  if (ts13.isIdentifier(target.expression) && ctx.classSet.has(target.expression.text)) {
    const clsName = target.expression.text;
    const fullName = `${clsName}_${propName}`;
    const globalIdx = ctx.staticProps.get(fullName);
    if (globalIdx !== void 0) {
      fctx.body.push({ op: "global.get", index: globalIdx });
      const rhsType2 = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
      if (!rhsType2) return null;
      emitCompoundOp(ctx, fctx, op);
      fctx.body.push({ op: "global.set", index: globalIdx });
      fctx.body.push({ op: "global.get", index: globalIdx });
      return { kind: "f64" };
    }
  }
  let typeName = resolveStructName(ctx, objType);
  if (!typeName && ts13.isIdentifier(target.expression)) {
    typeName = ctx.widenedVarStructMap.get(target.expression.text);
  }
  if (!typeName) {
    return compilePropertyCompoundAssignmentExternref(ctx, fctx, target, rhs, op, propName);
  }
  const accessorKey = `${typeName}_${propName}`;
  if (ctx.classAccessorSet.has(accessorKey)) {
    const getterName = `${typeName}_get_${propName}`;
    const setterName = `${typeName}_set_${propName}`;
    const getterIdx = ctx.funcMap.get(getterName);
    const setterIdx = ctx.funcMap.get(setterName);
    if (getterIdx !== void 0 && setterIdx !== void 0) {
      const cmpGetterPTypes = getFuncParamTypes(ctx, getterIdx);
      const objResult2 = compileExpression2(ctx, fctx, target.expression, cmpGetterPTypes?.[0]);
      if (!objResult2) return null;
      const objTmp2 = allocLocal(fctx, `__cmpd_acc_obj_${fctx.locals.length}`, objResult2);
      fctx.body.push({ op: "local.set", index: objTmp2 });
      fctx.body.push({ op: "local.get", index: objTmp2 });
      fctx.body.push({ op: "call", funcIdx: getterIdx });
      const rhsType2 = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
      if (!rhsType2) return null;
      emitCompoundOp(ctx, fctx, op);
      const resultTmp2 = allocLocal(fctx, `__cmpd_acc_res_${fctx.locals.length}`, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: resultTmp2 });
      fctx.body.push({ op: "local.get", index: objTmp2 });
      const cmpSetterParamTypes = getFuncParamTypes(ctx, setterIdx);
      const cmpSetterValType = cmpSetterParamTypes?.[1];
      if (cmpSetterValType) {
        fctx.body.push({ op: "local.get", index: resultTmp2 });
        if (cmpSetterValType.kind === "externref") {
          addUnionImports(ctx);
          const boxIdx = ctx.funcMap.get("__box_number");
          if (boxIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: boxIdx });
          }
        }
      }
      const finalCmpSetterIdx = ctx.funcMap.get(setterName) ?? setterIdx;
      fctx.body.push({ op: "call", funcIdx: finalCmpSetterIdx });
      fctx.body.push({ op: "local.get", index: resultTmp2 });
      return { kind: "f64" };
    }
  }
  const structTypeIdx = ctx.structMap.get(typeName);
  const fields = ctx.structFields.get(typeName);
  if (structTypeIdx === void 0 || !fields) {
    return compilePropertyCompoundAssignmentExternref(ctx, fctx, target, rhs, op, propName);
  }
  const fieldIdx = fields.findIndex((f) => f.name === propName);
  if (fieldIdx === -1) {
    return compilePropertyCompoundAssignmentExternref(ctx, fctx, target, rhs, op, propName);
  }
  const fieldType = fields[fieldIdx].type;
  const objResult = compileExpression2(ctx, fctx, target.expression);
  if (!objResult) return null;
  const objTmp = allocLocal(fctx, `__cmpd_obj_${fctx.locals.length}`, objResult);
  fctx.body.push({ op: "local.set", index: objTmp });
  fctx.body.push({ op: "local.get", index: objTmp });
  fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
  if (fieldType.kind !== "f64") {
    coerceType3(ctx, fctx, fieldType, { kind: "f64" });
  }
  const rhsType = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
  if (!rhsType) return null;
  emitCompoundOp(ctx, fctx, op);
  const resultTmp = allocLocal(fctx, `__cmpd_res_${fctx.locals.length}`, {
    kind: "f64",
  });
  fctx.body.push({ op: "local.set", index: resultTmp });
  fctx.body.push({ op: "local.get", index: objTmp });
  fctx.body.push({ op: "local.get", index: resultTmp });
  if (fieldType.kind !== "f64") {
    coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
  }
  fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
  fctx.body.push({ op: "local.get", index: resultTmp });
  return { kind: "f64" };
}
function compilePropertyCompoundAssignmentExternref(ctx, fctx, target, rhs, op, propName) {
  const objResult = compileExpression2(ctx, fctx, target.expression);
  if (!objResult) return null;
  if (objResult.kind === "ref" || objResult.kind === "ref_null") {
    const typeIdx = objResult.typeIdx;
    const resolvedTypeName = ctx.typeIdxToStructName.get(typeIdx);
    if (resolvedTypeName) {
      const fields = ctx.structFields.get(resolvedTypeName);
      if (fields) {
        let fieldIdx = fields.findIndex((f) => f.name === propName);
        if (fieldIdx === -1 && !ctx.classSet.has(resolvedTypeName)) {
          const objTsType = ctx.checker.getTypeAtLocation(target.expression);
          const tsProps = objTsType.getProperties?.();
          if (tsProps) {
            const tsProp = tsProps.find((p) => p.name === propName);
            if (tsProp) {
              const propTsType = ctx.checker.getTypeOfSymbolAtLocation(tsProp, target);
              const propWasmType = resolveWasmType(ctx, propTsType);
              const newField = {
                name: propName,
                type: propWasmType,
                mutable: true,
              };
              fields.push(newField);
              patchStructNewForAddedField(ctx, fctx, typeIdx, propWasmType);
              const typeDef = ctx.mod.types[typeIdx];
              if (typeDef?.kind === "struct" && typeDef.fields !== fields) {
                typeDef.fields.push(newField);
              }
              patchStructNewForDynamicField(ctx, typeIdx, propWasmType);
              fieldIdx = fields.length - 1;
            }
          }
        }
        if (fieldIdx !== -1) {
          const fieldType = fields[fieldIdx].type;
          const objTmp = allocLocal(fctx, `__cmpd_obj_${fctx.locals.length}`, objResult);
          fctx.body.push({ op: "local.set", index: objTmp });
          fctx.body.push({ op: "local.get", index: objTmp });
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
          if (fieldType.kind !== "f64") {
            coerceType3(ctx, fctx, fieldType, { kind: "f64" });
          }
          const rhsType2 = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
          if (!rhsType2) return null;
          emitCompoundOp(ctx, fctx, op);
          const resultTmp = allocLocal(fctx, `__cmpd_res_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.set", index: resultTmp });
          fctx.body.push({ op: "local.get", index: objTmp });
          fctx.body.push({ op: "local.get", index: resultTmp });
          if (fieldType.kind !== "f64") {
            coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
          }
          fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
          fctx.body.push({ op: "local.get", index: resultTmp });
          return { kind: "f64" };
        }
      }
    }
    fctx.body.push({ op: "extern.convert_any" });
  } else if (objResult.kind !== "externref") {
    if (objResult.kind === "f64") {
      const boxIdx2 = ctx.funcMap.get("__box_number");
      if (boxIdx2 !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx2 });
      } else {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "f64.const", value: NaN });
        return { kind: "f64" };
      }
    } else if (objResult.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
      const boxIdx2 = ctx.funcMap.get("__box_number");
      if (boxIdx2 !== void 0) {
        fctx.body.push({ op: "call", funcIdx: boxIdx2 });
      } else {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "f64.const", value: NaN });
        return { kind: "f64" };
      }
    } else {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "f64.const", value: NaN });
      return { kind: "f64" };
    }
  }
  const objLocal = allocLocal(fctx, `__cmpd_pobj_${fctx.locals.length}`, {
    kind: "externref",
  });
  fctx.body.push({ op: "local.set", index: objLocal });
  addStringConstantGlobal(ctx, propName);
  const keyResult = compileStringLiteral(ctx, fctx, propName);
  if (!keyResult) return null;
  if (keyResult.kind !== "externref") {
    coerceType3(ctx, fctx, keyResult, { kind: "externref" });
  }
  const keyLocal = allocLocal(fctx, `__cmpd_pkey_${fctx.locals.length}`, {
    kind: "externref",
  });
  fctx.body.push({ op: "local.set", index: keyLocal });
  fctx.body.push({ op: "local.get", index: objLocal });
  fctx.body.push({ op: "local.get", index: keyLocal });
  const getIdx = ensureLateImport2(
    ctx,
    "__extern_get",
    [{ kind: "externref" }, { kind: "externref" }],
    [{ kind: "externref" }],
  );
  flushLateImportShifts2(ctx, fctx);
  if (getIdx === void 0) return null;
  fctx.body.push({ op: "call", funcIdx: getIdx });
  addUnionImports(ctx);
  const unboxIdx = ctx.funcMap.get("__unbox_number");
  if (unboxIdx === void 0) {
    ctx.errors.push({
      message: "Missing __unbox_number for compound externref property assignment",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  fctx.body.push({ op: "call", funcIdx: unboxIdx });
  const rhsType = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
  if (!rhsType) return null;
  emitCompoundOp(ctx, fctx, op);
  const resultLocal = allocLocal(fctx, `__cmpd_pres_${fctx.locals.length}`, {
    kind: "f64",
  });
  fctx.body.push({ op: "local.set", index: resultLocal });
  fctx.body.push({ op: "local.get", index: resultLocal });
  const boxIdx = ctx.funcMap.get("__box_number");
  if (boxIdx === void 0) {
    ctx.errors.push({
      message: "Missing __box_number for compound externref property assignment",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  fctx.body.push({ op: "call", funcIdx: boxIdx });
  const boxedLocal = allocLocal(fctx, `__cmpd_pboxed_${fctx.locals.length}`, {
    kind: "externref",
  });
  fctx.body.push({ op: "local.set", index: boxedLocal });
  fctx.body.push({ op: "local.get", index: objLocal });
  fctx.body.push({ op: "local.get", index: keyLocal });
  fctx.body.push({ op: "local.get", index: boxedLocal });
  const setIdx = ensureLateImport2(
    ctx,
    "__extern_set",
    [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }],
    [],
  );
  flushLateImportShifts2(ctx, fctx);
  if (setIdx !== void 0) {
    fctx.body.push({ op: "call", funcIdx: setIdx });
  }
  fctx.body.push({ op: "local.get", index: resultLocal });
  return { kind: "f64" };
}
function compileElementCompoundAssignment(ctx, fctx, target, rhs, op) {
  const objResult = compileExpression2(ctx, fctx, target.expression);
  if (!objResult) return null;
  if (objResult.kind === "externref") {
    const objLocal = allocLocal(fctx, `__cmpd_eobj_${fctx.locals.length}`, {
      kind: "externref",
    });
    fctx.body.push({ op: "local.set", index: objLocal });
    const keyResult = compileExpression2(ctx, fctx, target.argumentExpression, {
      kind: "externref",
    });
    if (!keyResult) return null;
    const keyLocal = allocLocal(fctx, `__cmpd_ekey_${fctx.locals.length}`, {
      kind: "externref",
    });
    fctx.body.push({ op: "local.set", index: keyLocal });
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "local.get", index: keyLocal });
    const getIdx = ensureLateImport2(
      ctx,
      "__extern_get",
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (getIdx === void 0) return null;
    fctx.body.push({ op: "call", funcIdx: getIdx });
    addUnionImports(ctx);
    const unboxIdx = ctx.funcMap.get("__unbox_number");
    if (unboxIdx === void 0) {
      ctx.errors.push({
        message: "Missing __unbox_number for compound externref assignment",
        line: getLine(target),
        column: getCol(target),
      });
      return null;
    }
    fctx.body.push({ op: "call", funcIdx: unboxIdx });
    const rhsType = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
    if (!rhsType) return null;
    emitCompoundOp(ctx, fctx, op);
    const resultLocal = allocLocal(fctx, `__cmpd_eres_${fctx.locals.length}`, {
      kind: "f64",
    });
    fctx.body.push({ op: "local.set", index: resultLocal });
    fctx.body.push({ op: "local.get", index: resultLocal });
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx === void 0) {
      ctx.errors.push({
        message: "Missing __box_number for compound externref assignment",
        line: getLine(target),
        column: getCol(target),
      });
      return null;
    }
    fctx.body.push({ op: "call", funcIdx: boxIdx });
    const boxedLocal = allocLocal(fctx, `__cmpd_eboxed_${fctx.locals.length}`, {
      kind: "externref",
    });
    fctx.body.push({ op: "local.set", index: boxedLocal });
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "local.get", index: keyLocal });
    fctx.body.push({ op: "local.get", index: boxedLocal });
    const setIdx = ensureLateImport2(
      ctx,
      "__extern_set",
      [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }],
      [],
    );
    flushLateImportShifts2(ctx, fctx);
    if (setIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: setIdx });
    }
    fctx.body.push({ op: "local.get", index: resultLocal });
    return { kind: "f64" };
  }
  if (objResult.kind === "f64" || objResult.kind === "i32" || objResult.kind === "i64") {
    coerceType3(ctx, fctx, objResult, { kind: "externref" });
    const objLocal = allocLocal(fctx, `__cmpd_eobj_${fctx.locals.length}`, {
      kind: "externref",
    });
    fctx.body.push({ op: "local.set", index: objLocal });
    const keyResult = compileExpression2(ctx, fctx, target.argumentExpression, {
      kind: "externref",
    });
    if (!keyResult) return null;
    const keyLocal = allocLocal(fctx, `__cmpd_ekey_${fctx.locals.length}`, {
      kind: "externref",
    });
    fctx.body.push({ op: "local.set", index: keyLocal });
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "local.get", index: keyLocal });
    const getIdx = ensureLateImport2(
      ctx,
      "__extern_get",
      [{ kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (getIdx === void 0) return null;
    fctx.body.push({ op: "call", funcIdx: getIdx });
    addUnionImports(ctx);
    const unboxIdx = ctx.funcMap.get("__unbox_number");
    if (unboxIdx === void 0) {
      ctx.errors.push({
        message: "Missing __unbox_number for compound element assignment",
        line: getLine(target),
        column: getCol(target),
      });
      return null;
    }
    fctx.body.push({ op: "call", funcIdx: unboxIdx });
    const rhsType = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
    if (!rhsType) return null;
    emitCompoundOp(ctx, fctx, op);
    const resultLocal = allocLocal(fctx, `__cmpd_eres_${fctx.locals.length}`, {
      kind: "f64",
    });
    fctx.body.push({ op: "local.set", index: resultLocal });
    fctx.body.push({ op: "local.get", index: resultLocal });
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx === void 0) {
      ctx.errors.push({
        message: "Missing __box_number for compound element assignment",
        line: getLine(target),
        column: getCol(target),
      });
      return null;
    }
    fctx.body.push({ op: "call", funcIdx: boxIdx });
    const boxedLocal = allocLocal(fctx, `__cmpd_eboxed_${fctx.locals.length}`, {
      kind: "externref",
    });
    fctx.body.push({ op: "local.set", index: boxedLocal });
    fctx.body.push({ op: "local.get", index: objLocal });
    fctx.body.push({ op: "local.get", index: keyLocal });
    fctx.body.push({ op: "local.get", index: boxedLocal });
    const setIdx = ensureLateImport2(
      ctx,
      "__extern_set",
      [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }],
      [],
    );
    flushLateImportShifts2(ctx, fctx);
    if (setIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: setIdx });
    }
    fctx.body.push({ op: "local.get", index: resultLocal });
    return { kind: "f64" };
  }
  if (objResult.kind !== "ref" && objResult.kind !== "ref_null") {
    ctx.errors.push({
      message: "Compound assignment on non-ref element access",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const typeIdx = objResult.typeIdx;
  const typeDef = ctx.mod.types[typeIdx];
  if (typeDef?.kind === "struct") {
    const isVec =
      typeDef.fields.length === 2 && typeDef.fields[0]?.name === "length" && typeDef.fields[1]?.name === "data";
    if (!isVec) {
      let fieldName;
      if (ts13.isStringLiteral(target.argumentExpression)) {
        fieldName = target.argumentExpression.text;
      } else if (ts13.isNumericLiteral(target.argumentExpression)) {
        fieldName = target.argumentExpression.text;
      } else if (ts13.isIdentifier(target.argumentExpression)) {
        const sym = ctx.checker.getSymbolAtLocation(target.argumentExpression);
        if (sym) {
          const decl = sym.valueDeclaration;
          if (decl && ts13.isVariableDeclaration(decl) && decl.initializer) {
            const declList = decl.parent;
            if (ts13.isVariableDeclarationList(declList) && (declList.flags & ts13.NodeFlags.Const) !== 0) {
              if (ts13.isStringLiteral(decl.initializer)) {
                fieldName = decl.initializer.text;
              } else if (ts13.isNumericLiteral(decl.initializer)) {
                fieldName = decl.initializer.text;
              }
            }
          }
        }
      }
      if (fieldName === void 0) {
        fieldName = resolveComputedKeyExpression(ctx, target.argumentExpression);
      }
      if (fieldName !== void 0) {
        const fieldIdx = typeDef.fields.findIndex((f) => f.name === fieldName);
        if (fieldIdx !== -1) {
          const fieldType = typeDef.fields[fieldIdx].type;
          const objTmp = allocLocal(fctx, `__cmpd_obj_${fctx.locals.length}`, objResult);
          fctx.body.push({ op: "local.set", index: objTmp });
          fctx.body.push({ op: "local.get", index: objTmp });
          fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
          if (fieldType.kind !== "f64") {
            coerceType3(ctx, fctx, fieldType, { kind: "f64" });
          }
          const rhsType = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
          if (!rhsType) return null;
          emitCompoundOp(ctx, fctx, op);
          const resultTmp = allocLocal(fctx, `__cmpd_res_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.set", index: resultTmp });
          fctx.body.push({ op: "local.get", index: objTmp });
          fctx.body.push({ op: "local.get", index: resultTmp });
          if (fieldType.kind !== "f64") {
            coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
          }
          fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
          fctx.body.push({ op: "local.get", index: resultTmp });
          return { kind: "f64" };
        }
      }
    }
    if (isVec) {
      const objTmp = allocLocal(fctx, `__cmpd_arr_${fctx.locals.length}`, objResult);
      fctx.body.push({ op: "local.set", index: objTmp });
      const idxResult = compileExpression2(ctx, fctx, target.argumentExpression);
      if (!idxResult) return null;
      if (idxResult.kind === "f64") {
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      }
      const idxTmp = allocLocal(fctx, `__cmpd_idx_${fctx.locals.length}`, {
        kind: "i32",
      });
      fctx.body.push({ op: "local.set", index: idxTmp });
      const dataFieldType = typeDef.fields[1].type;
      const arrayTypeIdx = dataFieldType.typeIdx;
      const arrayDef = ctx.mod.types[arrayTypeIdx];
      const elemType = arrayDef && arrayDef.kind === "array" ? arrayDef.element : { kind: "f64" };
      fctx.body.push({ op: "local.get", index: objTmp });
      fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
      fctx.body.push({ op: "local.get", index: idxTmp });
      emitBoundsCheckedArrayGet2(fctx, arrayTypeIdx, elemType);
      if (elemType.kind !== "f64") {
        coerceType3(ctx, fctx, elemType, { kind: "f64" });
      }
      const rhsType = compileExpression2(ctx, fctx, rhs, { kind: "f64" });
      if (!rhsType) return null;
      emitCompoundOp(ctx, fctx, op);
      const resultTmp = allocLocal(fctx, `__cmpd_res_${fctx.locals.length}`, {
        kind: "f64",
      });
      fctx.body.push({ op: "local.set", index: resultTmp });
      fctx.body.push({ op: "local.get", index: idxTmp });
      fctx.body.push({ op: "local.get", index: objTmp });
      fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
      fctx.body.push({ op: "array.len" });
      fctx.body.push({ op: "i32.lt_u" });
      {
        const setInstrs = [
          { op: "local.get", index: objTmp },
          { op: "struct.get", typeIdx, fieldIdx: 1 },
          { op: "local.get", index: idxTmp },
          { op: "local.get", index: resultTmp },
        ];
        if (elemType.kind !== "f64") {
          const savedBody = fctx.body;
          fctx.body = setInstrs;
          coerceType3(ctx, fctx, { kind: "f64" }, elemType);
          fctx.body = savedBody;
        }
        setInstrs.push({ op: "array.set", typeIdx: arrayTypeIdx });
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: setInstrs,
          else: [],
        });
      }
      fctx.body.push({ op: "local.get", index: resultTmp });
      return { kind: "f64" };
    }
  }
  ctx.errors.push({
    message: `Unsupported compound assignment on element access`,
    line: getLine(target),
    column: getCol(target),
  });
  return null;
}
function unwrapParens(node) {
  while (ts13.isParenthesizedExpression(node)) {
    node = node.expression;
  }
  return node;
}
function compileMemberIncDec(ctx, fctx, operand, arithOp, mode) {
  const f64Op = arithOp === "add" ? "f64.add" : "f64.sub";
  const i32Op = arithOp === "add" ? "i32.add" : "i32.sub";
  operand = unwrapParens(operand);
  if (ts13.isPropertyAccessExpression(operand)) {
    const objType = ctx.checker.getTypeAtLocation(operand.expression);
    const propName = ts13.isPrivateIdentifier(operand.name)
      ? "__priv_" + operand.name.text.slice(1)
      : operand.name.text;
    ensureStructForType(ctx, objType);
    let typeName = resolveStructName(ctx, objType);
    if (!typeName && ts13.isIdentifier(operand.expression)) {
      typeName = ctx.widenedVarStructMap.get(operand.expression.text);
    }
    if (!typeName) {
      fctx.body.push({ op: "f64.const", value: NaN });
      return { kind: "f64" };
    }
    const accessorKey = `${typeName}_${propName}`;
    if (ctx.classAccessorSet.has(accessorKey)) {
      const getterName = `${typeName}_get_${propName}`;
      const setterName = `${typeName}_set_${propName}`;
      const getterIdx = ctx.funcMap.get(getterName);
      const setterIdx = ctx.funcMap.get(setterName);
      if (getterIdx !== void 0 && setterIdx !== void 0) {
        const incGetterPTypes = getFuncParamTypes(ctx, getterIdx);
        const objResult2 = compileExpression2(ctx, fctx, operand.expression, incGetterPTypes?.[0]);
        if (!objResult2) return null;
        const objTmp2 = allocLocal(fctx, `__incdec_acc_obj_${fctx.locals.length}`, objResult2);
        fctx.body.push({ op: "local.set", index: objTmp2 });
        fctx.body.push({ op: "local.get", index: objTmp2 });
        fctx.body.push({ op: "call", funcIdx: getterIdx });
        if (mode === "postfix") {
          const oldTmp = allocLocal(fctx, `__incdec_acc_old_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: oldTmp });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: f64Op });
          const newTmp = allocLocal(fctx, `__incdec_acc_new_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.set", index: newTmp });
          fctx.body.push({ op: "local.get", index: objTmp2 });
          {
            const idParamTypes = getFuncParamTypes(ctx, setterIdx);
            const idValType = idParamTypes?.[1];
            if (idValType) {
              fctx.body.push({ op: "local.get", index: newTmp });
              if (idValType.kind === "externref") {
                addUnionImports(ctx);
                const bIdx = ctx.funcMap.get("__box_number");
                if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
              }
            }
          }
          {
            const fs = ctx.funcMap.get(setterName) ?? setterIdx;
            fctx.body.push({ op: "call", funcIdx: fs });
          }
          fctx.body.push({ op: "local.get", index: oldTmp });
        } else {
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: f64Op });
          const newTmp = allocLocal(fctx, `__incdec_acc_new_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: newTmp });
          const valTmp = allocLocal(fctx, `__incdec_acc_val_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.set", index: valTmp });
          fctx.body.push({ op: "local.get", index: objTmp2 });
          {
            const idParamTypes = getFuncParamTypes(ctx, setterIdx);
            const idValType = idParamTypes?.[1];
            if (idValType) {
              fctx.body.push({ op: "local.get", index: valTmp });
              if (idValType.kind === "externref") {
                addUnionImports(ctx);
                const bIdx = ctx.funcMap.get("__box_number");
                if (bIdx !== void 0) fctx.body.push({ op: "call", funcIdx: bIdx });
              }
            }
          }
          {
            const fs = ctx.funcMap.get(setterName) ?? setterIdx;
            fctx.body.push({ op: "call", funcIdx: fs });
          }
          fctx.body.push({ op: "local.get", index: newTmp });
        }
        return { kind: "f64" };
      }
    }
    const structTypeIdx = ctx.structMap.get(typeName);
    const fields = ctx.structFields.get(typeName);
    if (structTypeIdx === void 0 || !fields) {
      fctx.body.push({ op: "f64.const", value: NaN });
      return { kind: "f64" };
    }
    const fieldIdx = fields.findIndex((f) => f.name === propName);
    if (fieldIdx === -1) {
      fctx.body.push({ op: "f64.const", value: NaN });
      return { kind: "f64" };
    }
    const fieldType = fields[fieldIdx].type;
    const objResult = compileExpression2(ctx, fctx, operand.expression);
    if (!objResult) return null;
    const objTmp = allocLocal(fctx, `__incdec_obj_${fctx.locals.length}`, objResult);
    fctx.body.push({ op: "local.set", index: objTmp });
    fctx.body.push({ op: "local.get", index: objTmp });
    fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
    if (ctx.fast && fieldType.kind === "i32") {
      if (mode === "postfix") {
        const oldTmp = allocLocal(fctx, `__incdec_old_${fctx.locals.length}`, {
          kind: "i32",
        });
        fctx.body.push({ op: "local.tee", index: oldTmp });
        fctx.body.push({ op: "i32.const", value: 1 });
        fctx.body.push({ op: i32Op });
        const newTmp = allocLocal(fctx, `__incdec_new_${fctx.locals.length}`, {
          kind: "i32",
        });
        fctx.body.push({ op: "local.set", index: newTmp });
        fctx.body.push({ op: "local.get", index: objTmp });
        fctx.body.push({ op: "local.get", index: newTmp });
        fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
        fctx.body.push({ op: "local.get", index: oldTmp });
        return { kind: "i32" };
      } else {
        fctx.body.push({ op: "i32.const", value: 1 });
        fctx.body.push({ op: i32Op });
        const newTmp = allocLocal(fctx, `__incdec_new_${fctx.locals.length}`, {
          kind: "i32",
        });
        fctx.body.push({ op: "local.set", index: newTmp });
        fctx.body.push({ op: "local.get", index: objTmp });
        fctx.body.push({ op: "local.get", index: newTmp });
        fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
        fctx.body.push({ op: "local.get", index: newTmp });
        return { kind: "i32" };
      }
    }
    if (fieldType.kind !== "f64") {
      coerceType3(ctx, fctx, fieldType, { kind: "f64" });
    }
    if (mode === "postfix") {
      const oldTmp = allocLocal(fctx, `__incdec_old_${fctx.locals.length}`, {
        kind: "f64",
      });
      fctx.body.push({ op: "local.tee", index: oldTmp });
      fctx.body.push({ op: "f64.const", value: 1 });
      fctx.body.push({ op: f64Op });
      if (fieldType.kind !== "f64") {
        coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
      }
      const newTmp = allocLocal(fctx, `__incdec_new_${fctx.locals.length}`, fieldType);
      fctx.body.push({ op: "local.set", index: newTmp });
      fctx.body.push({ op: "local.get", index: objTmp });
      fctx.body.push({ op: "local.get", index: newTmp });
      fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
      fctx.body.push({ op: "local.get", index: oldTmp });
      return { kind: "f64" };
    } else {
      fctx.body.push({ op: "f64.const", value: 1 });
      fctx.body.push({ op: f64Op });
      const newF64Tmp = allocLocal(fctx, `__incdec_new_${fctx.locals.length}`, {
        kind: "f64",
      });
      fctx.body.push({ op: "local.set", index: newF64Tmp });
      fctx.body.push({ op: "local.get", index: objTmp });
      fctx.body.push({ op: "local.get", index: newF64Tmp });
      if (fieldType.kind !== "f64") {
        coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
      }
      fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
      fctx.body.push({ op: "local.get", index: newF64Tmp });
      return { kind: "f64" };
    }
  }
  if (ts13.isElementAccessExpression(operand)) {
    const objTsType = ctx.checker.getTypeAtLocation(operand.expression);
    const objResult = compileExpression2(ctx, fctx, operand.expression);
    if (!objResult) return null;
    if (objResult.kind === "externref") {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "f64.const", value: NaN });
      return { kind: "f64" };
    }
    if (objResult.kind !== "ref" && objResult.kind !== "ref_null") {
      fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "f64.const", value: NaN });
      return { kind: "f64" };
    }
    const elemObjTmp = allocLocal(fctx, `__incdec_eobj_${fctx.locals.length}`, objResult);
    fctx.body.push({ op: "local.set", index: elemObjTmp });
    const typeIdx = objResult.typeIdx;
    const typeDef = ctx.mod.types[typeIdx];
    if (typeDef?.kind === "struct") {
      const isVec =
        typeDef.fields.length === 2 && typeDef.fields[0]?.name === "length" && typeDef.fields[1]?.name === "data";
      if (!isVec) {
        let fieldName;
        if (ts13.isStringLiteral(operand.argumentExpression)) {
          fieldName = operand.argumentExpression.text;
        } else if (ts13.isNumericLiteral(operand.argumentExpression)) {
          fieldName = operand.argumentExpression.text;
        }
        if (fieldName) {
          const fieldIdx = typeDef.fields.findIndex((f) => f.name === fieldName);
          if (fieldIdx !== -1) {
            const fieldType = typeDef.fields[fieldIdx].type;
            fctx.body.push({ op: "local.get", index: elemObjTmp });
            fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
            if (fieldType.kind !== "f64") {
              coerceType3(ctx, fctx, fieldType, { kind: "f64" });
            }
            if (mode === "postfix") {
              const oldTmp = allocLocal(fctx, `__incdec_old_${fctx.locals.length}`, { kind: "f64" });
              fctx.body.push({ op: "local.tee", index: oldTmp });
              fctx.body.push({ op: "f64.const", value: 1 });
              fctx.body.push({ op: f64Op });
              if (fieldType.kind !== "f64") coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
              const newTmp = allocLocal(fctx, `__incdec_new_${fctx.locals.length}`, fieldType);
              fctx.body.push({ op: "local.set", index: newTmp });
              fctx.body.push({ op: "local.get", index: elemObjTmp });
              fctx.body.push({ op: "local.get", index: newTmp });
              fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
              fctx.body.push({ op: "local.get", index: oldTmp });
              return { kind: "f64" };
            } else {
              fctx.body.push({ op: "f64.const", value: 1 });
              fctx.body.push({ op: f64Op });
              const newTmp = allocLocal(fctx, `__incdec_new_${fctx.locals.length}`, { kind: "f64" });
              fctx.body.push({ op: "local.set", index: newTmp });
              fctx.body.push({ op: "local.get", index: elemObjTmp });
              fctx.body.push({ op: "local.get", index: newTmp });
              if (fieldType.kind !== "f64") coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
              fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
              fctx.body.push({ op: "local.get", index: newTmp });
              return { kind: "f64" };
            }
          }
        }
      }
      if (isVec) {
        const objTmp = elemObjTmp;
        const idxResult = compileExpression2(ctx, fctx, operand.argumentExpression);
        if (!idxResult) return null;
        if (idxResult.kind === "f64") {
          fctx.body.push({ op: "i32.trunc_sat_f64_s" });
        }
        const idxTmp = allocLocal(fctx, `__incdec_idx_${fctx.locals.length}`, {
          kind: "i32",
        });
        fctx.body.push({ op: "local.set", index: idxTmp });
        const dataFieldType = typeDef.fields[1].type;
        const arrayTypeIdx = dataFieldType.typeIdx;
        const arrayDef = ctx.mod.types[arrayTypeIdx];
        const elemType = arrayDef && arrayDef.kind === "array" ? arrayDef.element : { kind: "f64" };
        fctx.body.push({ op: "local.get", index: objTmp });
        fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
        fctx.body.push({ op: "local.get", index: idxTmp });
        emitBoundsCheckedArrayGet2(fctx, arrayTypeIdx, elemType);
        if (elemType.kind !== "f64" && elemType.kind !== "i32") {
          coerceType3(ctx, fctx, elemType, { kind: "f64" });
        }
        const numType = ctx.fast && elemType.kind === "i32" ? "i32" : "f64";
        const op = numType === "i32" ? i32Op : f64Op;
        if (mode === "postfix") {
          const oldTmp = allocLocal(fctx, `__incdec_old_${fctx.locals.length}`, { kind: numType });
          fctx.body.push({ op: "local.tee", index: oldTmp });
          if (numType === "i32") {
            fctx.body.push({ op: "i32.const", value: 1 });
          } else {
            fctx.body.push({ op: "f64.const", value: 1 });
          }
          fctx.body.push({ op });
          const newTmp = allocLocal(fctx, `__incdec_new_${fctx.locals.length}`, { kind: numType });
          fctx.body.push({ op: "local.set", index: newTmp });
          emitBoundsGuardedArraySet(fctx, objTmp, typeIdx, idxTmp, newTmp, arrayTypeIdx);
          fctx.body.push({ op: "local.get", index: oldTmp });
          return { kind: numType };
        } else {
          if (numType === "i32") {
            fctx.body.push({ op: "i32.const", value: 1 });
          } else {
            fctx.body.push({ op: "f64.const", value: 1 });
          }
          fctx.body.push({ op });
          const newTmp = allocLocal(fctx, `__incdec_new_${fctx.locals.length}`, { kind: numType });
          fctx.body.push({ op: "local.set", index: newTmp });
          emitBoundsGuardedArraySet(fctx, objTmp, typeIdx, idxTmp, newTmp, arrayTypeIdx);
          fctx.body.push({ op: "local.get", index: newTmp });
          return { kind: numType };
        }
      }
    }
  }
  fctx.body.push({ op: "f64.const", value: NaN });
  return { kind: "f64" };
}
function compilePrefixUnary(ctx, fctx, expr) {
  switch (expr.operator) {
    case ts13.SyntaxKind.PlusToken: {
      const staticVal = tryStaticToNumber(ctx, expr.operand);
      if (staticVal !== void 0) {
        fctx.body.push({ op: "f64.const", value: staticVal });
        return { kind: "f64" };
      }
      const operandType = compileExpression2(ctx, fctx, expr.operand);
      if (operandType?.kind === "externref") {
        const unboxIdx = ctx.funcMap.get("__unbox_number");
        if (unboxIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: unboxIdx });
          return { kind: "f64" };
        }
        const pfIdx = ctx.funcMap.get("parseFloat");
        if (pfIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: pfIdx });
          return { kind: "f64" };
        }
      }
      if (operandType && (operandType.kind === "ref" || operandType.kind === "ref_null")) {
        coerceType3(ctx, fctx, operandType, { kind: "f64" });
        return { kind: "f64" };
      }
      if (operandType?.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
        return { kind: "f64" };
      }
      return operandType;
    }
    case ts13.SyntaxKind.MinusToken: {
      const staticVal = tryStaticToNumber(ctx, expr.operand);
      if (staticVal !== void 0) {
        fctx.body.push({ op: "f64.const", value: -staticVal });
        return { kind: "f64" };
      }
      const operandType = compileExpression2(ctx, fctx, expr.operand);
      if (!operandType) return null;
      if (isAnyValue(operandType, ctx)) {
        ensureAnyHelpers(ctx);
        const negIdx = ctx.funcMap.get("__any_neg");
        if (negIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: negIdx });
          return { kind: "ref", typeIdx: ctx.anyValueTypeIdx };
        }
      }
      if (ctx.fast && operandType?.kind === "i32") {
        let innerOperand = expr.operand;
        while (ts13.isParenthesizedExpression(innerOperand)) {
          innerOperand = innerOperand.expression;
        }
        if (ts13.isNumericLiteral(innerOperand) && Number(innerOperand.text) === 0) {
          fctx.body.pop();
          fctx.body.push({ op: "f64.const", value: -0 });
          return { kind: "f64" };
        }
        const tmp = allocLocal(fctx, `__neg_${fctx.locals.length}`, {
          kind: "i32",
        });
        fctx.body.push({ op: "local.set", index: tmp });
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "i32.sub" });
        return { kind: "i32" };
      }
      if (operandType?.kind === "i64") {
        const tmp = allocLocal(fctx, `__neg_${fctx.locals.length}`, {
          kind: "i64",
        });
        fctx.body.push({ op: "local.set", index: tmp });
        fctx.body.push({ op: "i64.const", value: 0n });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "i64.sub" });
        return { kind: "i64" };
      }
      if (operandType?.kind !== "f64") {
        coerceType3(ctx, fctx, operandType, { kind: "f64" });
      }
      fctx.body.push({ op: "f64.neg" });
      return { kind: "f64" };
    }
    case ts13.SyntaxKind.ExclamationToken: {
      const operandType = compileExpression2(ctx, fctx, expr.operand);
      ensureI32Condition(fctx, operandType, ctx);
      fctx.body.push({ op: "i32.eqz" });
      return { kind: "i32" };
    }
    case ts13.SyntaxKind.TildeToken: {
      const operandType = compileExpression2(ctx, fctx, expr.operand);
      if (operandType?.kind === "i64") {
        fctx.body.push({ op: "i64.const", value: -1n });
        fctx.body.push({ op: "i64.xor" });
        return { kind: "i64" };
      }
      if (ctx.fast) {
        if (operandType?.kind !== "i32") coerceType3(ctx, fctx, operandType, { kind: "i32" });
        fctx.body.push({ op: "i32.const", value: -1 });
        fctx.body.push({ op: "i32.xor" });
        return { kind: "i32" };
      }
      if (operandType?.kind !== "f64") coerceType3(ctx, fctx, operandType, { kind: "f64" });
      emitToInt32(fctx);
      fctx.body.push({ op: "i32.const", value: -1 });
      fctx.body.push({ op: "i32.xor" });
      fctx.body.push({ op: "f64.convert_i32_s" });
      return { kind: "f64" };
    }
    case ts13.SyntaxKind.PlusPlusToken: {
      const ppOperand = unwrapParens(expr.operand);
      if (ts13.isIdentifier(ppOperand) && fctx.constBindings?.has(ppOperand.text)) {
        emitThrowString2(ctx, fctx, "TypeError: Assignment to constant variable.");
        fctx.body.push({ op: "unreachable" });
        return { kind: "f64" };
      }
      if (ts13.isIdentifier(ppOperand)) {
        const idx = fctx.localMap.get(ppOperand.text);
        if (idx !== void 0) {
          const boxedPP = fctx.boxedCaptures?.get(ppOperand.text);
          if (boxedPP) {
            const needsCoerce = boxedPP.valType.kind !== "f64" && boxedPP.valType.kind !== "i32";
            if (needsCoerce) {
              const ppF64Tmp = allocLocal(fctx, `__pp_f64_${fctx.locals.length}`, { kind: "f64" });
              const ppNewTmp = allocLocal(fctx, `__pp_new_${fctx.locals.length}`, boxedPP.valType);
              const savedBody = fctx.body;
              const elseBranch = [];
              fctx.body = elseBranch;
              fctx.body.push({ op: "local.get", index: idx });
              fctx.body.push({
                op: "struct.get",
                typeIdx: boxedPP.refCellTypeIdx,
                fieldIdx: 0,
              });
              coerceType3(ctx, fctx, boxedPP.valType, { kind: "f64" });
              fctx.body.push({ op: "f64.const", value: 1 });
              fctx.body.push({ op: "f64.add" });
              fctx.body.push({ op: "local.tee", index: ppF64Tmp });
              coerceType3(ctx, fctx, { kind: "f64" }, boxedPP.valType);
              fctx.body.push({ op: "local.set", index: ppNewTmp });
              fctx.body.push({ op: "local.get", index: idx });
              fctx.body.push({ op: "local.get", index: ppNewTmp });
              fctx.body.push({
                op: "struct.set",
                typeIdx: boxedPP.refCellTypeIdx,
                fieldIdx: 0,
              });
              fctx.body.push({ op: "local.get", index: ppF64Tmp });
              fctx.body = savedBody;
              fctx.body.push({ op: "local.get", index: idx });
              fctx.body.push({ op: "ref.is_null" });
              fctx.body.push({
                op: "if",
                blockType: { kind: "val", type: { kind: "f64" } },
                then: [{ op: "f64.const", value: NaN }],
                else: elseBranch,
              });
              return { kind: "f64" };
            }
            const ppTmp = allocLocal(fctx, `__pp_${fctx.locals.length}`, boxedPP.valType);
            fctx.body.push({ op: "local.get", index: idx });
            fctx.body.push({ op: "ref.is_null" });
            fctx.body.push({
              op: "if",
              blockType: { kind: "val", type: boxedPP.valType },
              then: defaultValueInstrs(boxedPP.valType),
              else: [
                { op: "local.get", index: idx },
                { op: "local.get", index: idx },
                {
                  op: "struct.get",
                  typeIdx: boxedPP.refCellTypeIdx,
                  fieldIdx: 0,
                },
                ...(boxedPP.valType.kind === "i32"
                  ? [{ op: "i32.const", value: 1 }, { op: "i32.add" }]
                  : [{ op: "f64.const", value: 1 }, { op: "f64.add" }]),
                { op: "local.tee", index: ppTmp },
                {
                  op: "struct.set",
                  typeIdx: boxedPP.refCellTypeIdx,
                  fieldIdx: 0,
                },
                { op: "local.get", index: ppTmp },
              ],
            });
            return boxedPP.valType;
          }
          const localType = getLocalType(fctx, idx);
          if (localType?.kind === "i32") {
            fctx.body.push({ op: "local.get", index: idx });
            fctx.body.push({ op: "i32.const", value: 1 });
            fctx.body.push({ op: "i32.add" });
            fctx.body.push({ op: "local.tee", index: idx });
            emitMappedArgParamSync(ctx, fctx, idx, { kind: "i32" });
            return { kind: "i32" };
          }
          if (localType?.kind === "externref") {
            fctx.body.push({ op: "local.get", index: idx });
            emitSafeExternrefToF64(ctx, fctx);
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: "f64.add" });
            addUnionImports(ctx);
            fctx.body.push({
              op: "call",
              funcIdx: ctx.funcMap.get("__box_number"),
            });
            fctx.body.push({ op: "local.tee", index: idx });
            emitMappedArgParamSync(ctx, fctx, idx, { kind: "externref" });
            return { kind: "externref" };
          }
          if (localType?.kind === "ref" || localType?.kind === "ref_null") {
            fctx.body.push({ op: "local.get", index: idx });
            coerceType3(ctx, fctx, localType, { kind: "f64" });
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: "f64.add" });
            return { kind: "f64" };
          }
          if (localType?.kind === "i64") {
            fctx.body.push({ op: "local.get", index: idx });
            fctx.body.push({ op: "i64.const", value: 1n });
            fctx.body.push({ op: "i64.add" });
            fctx.body.push({ op: "local.tee", index: idx });
            emitMappedArgParamSync(ctx, fctx, idx, { kind: "i64" });
            return { kind: "i64" };
          }
          fctx.body.push({ op: "local.get", index: idx });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: "f64.add" });
          fctx.body.push({ op: "local.tee", index: idx });
          emitMappedArgParamSync(ctx, fctx, idx, { kind: "f64" });
          return { kind: "f64" };
        }
        const ppModIdx = ctx.moduleGlobals.get(ppOperand.text);
        if (ppModIdx !== void 0) {
          const ppModGlobalDef = ctx.mod.globals[localGlobalIdx(ctx, ppModIdx)];
          if (ppModGlobalDef?.type.kind === "externref") {
            fctx.body.push({ op: "global.get", index: ppModIdx });
            emitSafeExternrefToF64(ctx, fctx);
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: "f64.add" });
            addUnionImports(ctx);
            fctx.body.push({
              op: "call",
              funcIdx: ctx.funcMap.get("__box_number"),
            });
            fctx.body.push({ op: "global.set", index: ppModIdx });
            fctx.body.push({ op: "global.get", index: ppModIdx });
            return { kind: "externref" };
          }
          if (ppModGlobalDef && (ppModGlobalDef.type.kind === "ref" || ppModGlobalDef.type.kind === "ref_null")) {
            fctx.body.push({ op: "global.get", index: ppModIdx });
            coerceType3(ctx, fctx, ppModGlobalDef.type, { kind: "f64" });
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: "f64.add" });
            return { kind: "f64" };
          }
          fctx.body.push({ op: "global.get", index: ppModIdx });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: "f64.add" });
          const ppTmp = allocLocal(fctx, `__pp_mod_${fctx.locals.length}`, {
            kind: "f64",
          });
          fctx.body.push({ op: "local.tee", index: ppTmp });
          fctx.body.push({ op: "global.set", index: ppModIdx });
          fctx.body.push({ op: "local.get", index: ppTmp });
          return { kind: "f64" };
        }
        const ppCapIdx = ctx.capturedGlobals.get(ppOperand.text);
        if (ppCapIdx !== void 0) {
          const ppCapGlobalDef = ctx.mod.globals[localGlobalIdx(ctx, ppCapIdx)];
          if (ppCapGlobalDef?.type.kind === "externref") {
            fctx.body.push({ op: "global.get", index: ppCapIdx });
            emitSafeExternrefToF64(ctx, fctx);
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: "f64.add" });
            addUnionImports(ctx);
            fctx.body.push({
              op: "call",
              funcIdx: ctx.funcMap.get("__box_number"),
            });
            fctx.body.push({ op: "global.set", index: ppCapIdx });
            fctx.body.push({ op: "global.get", index: ppCapIdx });
            return { kind: "externref" };
          }
          if (ppCapGlobalDef && (ppCapGlobalDef.type.kind === "ref" || ppCapGlobalDef.type.kind === "ref_null")) {
            fctx.body.push({ op: "global.get", index: ppCapIdx });
            coerceType3(ctx, fctx, ppCapGlobalDef.type, { kind: "f64" });
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: "f64.add" });
            return { kind: "f64" };
          }
          fctx.body.push({ op: "global.get", index: ppCapIdx });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: "f64.add" });
          const ppTmp = allocLocal(fctx, `__pp_cap_${fctx.locals.length}`, {
            kind: "f64",
          });
          fctx.body.push({ op: "local.tee", index: ppTmp });
          fctx.body.push({ op: "global.set", index: ppCapIdx });
          fctx.body.push({ op: "local.get", index: ppTmp });
          return { kind: "f64" };
        }
      }
      return compileMemberIncDec(ctx, fctx, expr.operand, "add", "prefix");
    }
    case ts13.SyntaxKind.MinusMinusToken: {
      const isIncrement = false;
      const arithOp = isIncrement ? "f64.add" : "f64.sub";
      const arithOpI32 = isIncrement ? "i32.add" : "i32.sub";
      const mmOperand = unwrapParens(expr.operand);
      if (ts13.isIdentifier(mmOperand) && fctx.constBindings?.has(mmOperand.text)) {
        emitThrowString2(ctx, fctx, "TypeError: Assignment to constant variable.");
        fctx.body.push({ op: "unreachable" });
        return { kind: "f64" };
      }
      if (ts13.isIdentifier(mmOperand)) {
        const idx = fctx.localMap.get(mmOperand.text);
        if (idx !== void 0) {
          const boxed = fctx.boxedCaptures?.get(mmOperand.text);
          if (boxed) {
            const needsCoerce = boxed.valType.kind !== "f64" && boxed.valType.kind !== "i32";
            if (needsCoerce) {
              const mmF64Tmp = allocLocal(fctx, `__mm_f64_${fctx.locals.length}`, { kind: "f64" });
              const mmNewTmp = allocLocal(fctx, `__mm_new_${fctx.locals.length}`, boxed.valType);
              const savedBody = fctx.body;
              const elseBranch = [];
              fctx.body = elseBranch;
              fctx.body.push({ op: "local.get", index: idx });
              fctx.body.push({
                op: "struct.get",
                typeIdx: boxed.refCellTypeIdx,
                fieldIdx: 0,
              });
              coerceType3(ctx, fctx, boxed.valType, { kind: "f64" });
              fctx.body.push({ op: "f64.const", value: 1 });
              fctx.body.push({ op: arithOp });
              fctx.body.push({ op: "local.tee", index: mmF64Tmp });
              coerceType3(ctx, fctx, { kind: "f64" }, boxed.valType);
              fctx.body.push({ op: "local.set", index: mmNewTmp });
              fctx.body.push({ op: "local.get", index: idx });
              fctx.body.push({ op: "local.get", index: mmNewTmp });
              fctx.body.push({
                op: "struct.set",
                typeIdx: boxed.refCellTypeIdx,
                fieldIdx: 0,
              });
              fctx.body.push({ op: "local.get", index: mmF64Tmp });
              fctx.body = savedBody;
              fctx.body.push({ op: "local.get", index: idx });
              fctx.body.push({ op: "ref.is_null" });
              fctx.body.push({
                op: "if",
                blockType: { kind: "val", type: { kind: "f64" } },
                then: [{ op: "f64.const", value: NaN }],
                else: elseBranch,
              });
              return { kind: "f64" };
            }
            const tmp = allocLocal(fctx, `__pp_${fctx.locals.length}`, boxed.valType);
            fctx.body.push({ op: "local.get", index: idx });
            fctx.body.push({ op: "ref.is_null" });
            fctx.body.push({
              op: "if",
              blockType: { kind: "val", type: boxed.valType },
              then: defaultValueInstrs(boxed.valType),
              else: [
                { op: "local.get", index: idx },
                { op: "local.get", index: idx },
                {
                  op: "struct.get",
                  typeIdx: boxed.refCellTypeIdx,
                  fieldIdx: 0,
                },
                ...(boxed.valType.kind === "i32"
                  ? [{ op: "i32.const", value: 1 }, { op: arithOpI32 }]
                  : [{ op: "f64.const", value: 1 }, { op: arithOp }]),
                { op: "local.tee", index: tmp },
                {
                  op: "struct.set",
                  typeIdx: boxed.refCellTypeIdx,
                  fieldIdx: 0,
                },
                { op: "local.get", index: tmp },
              ],
            });
            return boxed.valType;
          }
          const localType = getLocalType(fctx, idx);
          if (localType?.kind === "i32") {
            fctx.body.push({ op: "local.get", index: idx });
            fctx.body.push({ op: "i32.const", value: 1 });
            fctx.body.push({ op: arithOpI32 });
            fctx.body.push({ op: "local.tee", index: idx });
            emitMappedArgParamSync(ctx, fctx, idx, { kind: "i32" });
            return { kind: "i32" };
          }
          if (localType?.kind === "externref") {
            fctx.body.push({ op: "local.get", index: idx });
            emitSafeExternrefToF64(ctx, fctx);
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: arithOp });
            addUnionImports(ctx);
            fctx.body.push({
              op: "call",
              funcIdx: ctx.funcMap.get("__box_number"),
            });
            fctx.body.push({ op: "local.tee", index: idx });
            emitMappedArgParamSync(ctx, fctx, idx, { kind: "externref" });
            return { kind: "externref" };
          }
          if (localType?.kind === "ref" || localType?.kind === "ref_null") {
            fctx.body.push({ op: "local.get", index: idx });
            coerceType3(ctx, fctx, localType, { kind: "f64" });
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: arithOp });
            return { kind: "f64" };
          }
          if (localType?.kind === "i64") {
            fctx.body.push({ op: "local.get", index: idx });
            fctx.body.push({ op: "i64.const", value: 1n });
            fctx.body.push({ op: isIncrement ? "i64.add" : "i64.sub" });
            fctx.body.push({ op: "local.tee", index: idx });
            emitMappedArgParamSync(ctx, fctx, idx, { kind: "i64" });
            return { kind: "i64" };
          }
          fctx.body.push({ op: "local.get", index: idx });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: arithOp });
          fctx.body.push({ op: "local.tee", index: idx });
          emitMappedArgParamSync(ctx, fctx, idx, { kind: "f64" });
          return { kind: "f64" };
        }
        const mmModIdx = ctx.moduleGlobals.get(mmOperand.text);
        if (mmModIdx !== void 0) {
          const mmModGlobalDef = ctx.mod.globals[localGlobalIdx(ctx, mmModIdx)];
          if (mmModGlobalDef?.type.kind === "externref") {
            fctx.body.push({ op: "global.get", index: mmModIdx });
            emitSafeExternrefToF64(ctx, fctx);
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: arithOp });
            addUnionImports(ctx);
            fctx.body.push({
              op: "call",
              funcIdx: ctx.funcMap.get("__box_number"),
            });
            fctx.body.push({ op: "global.set", index: mmModIdx });
            fctx.body.push({ op: "global.get", index: mmModIdx });
            return { kind: "externref" };
          }
          if (mmModGlobalDef && (mmModGlobalDef.type.kind === "ref" || mmModGlobalDef.type.kind === "ref_null")) {
            fctx.body.push({ op: "global.get", index: mmModIdx });
            coerceType3(ctx, fctx, mmModGlobalDef.type, { kind: "f64" });
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: arithOp });
            return { kind: "f64" };
          }
          fctx.body.push({ op: "global.get", index: mmModIdx });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: arithOp });
          const mmTmp = allocLocal(fctx, `__mm_mod_${fctx.locals.length}`, {
            kind: "f64",
          });
          fctx.body.push({ op: "local.tee", index: mmTmp });
          fctx.body.push({ op: "global.set", index: mmModIdx });
          fctx.body.push({ op: "local.get", index: mmTmp });
          return { kind: "f64" };
        }
        const mmCapIdx = ctx.capturedGlobals.get(mmOperand.text);
        if (mmCapIdx !== void 0) {
          const mmCapGlobalDef = ctx.mod.globals[localGlobalIdx(ctx, mmCapIdx)];
          if (mmCapGlobalDef?.type.kind === "externref") {
            fctx.body.push({ op: "global.get", index: mmCapIdx });
            emitSafeExternrefToF64(ctx, fctx);
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: arithOp });
            addUnionImports(ctx);
            fctx.body.push({
              op: "call",
              funcIdx: ctx.funcMap.get("__box_number"),
            });
            fctx.body.push({ op: "global.set", index: mmCapIdx });
            fctx.body.push({ op: "global.get", index: mmCapIdx });
            return { kind: "externref" };
          }
          if (mmCapGlobalDef && (mmCapGlobalDef.type.kind === "ref" || mmCapGlobalDef.type.kind === "ref_null")) {
            fctx.body.push({ op: "global.get", index: mmCapIdx });
            coerceType3(ctx, fctx, mmCapGlobalDef.type, { kind: "f64" });
            fctx.body.push({ op: "f64.const", value: 1 });
            fctx.body.push({ op: arithOp });
            return { kind: "f64" };
          }
          fctx.body.push({ op: "global.get", index: mmCapIdx });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: arithOp });
          const mmTmp = allocLocal(fctx, `__mm_cap_${fctx.locals.length}`, {
            kind: "f64",
          });
          fctx.body.push({ op: "local.tee", index: mmTmp });
          fctx.body.push({ op: "global.set", index: mmCapIdx });
          fctx.body.push({ op: "local.get", index: mmTmp });
          return { kind: "f64" };
        }
      }
      return compileMemberIncDec(ctx, fctx, expr.operand, "sub", "prefix");
    }
  }
  ctx.errors.push({
    message: `Unsupported prefix unary operator: ${ts13.SyntaxKind[expr.operator]}`,
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}
function compilePostfixUnary(ctx, fctx, expr) {
  const isIncrement = expr.operator === ts13.SyntaxKind.PlusPlusToken;
  const arithOp = isIncrement ? "f64.add" : "f64.sub";
  const arithOpI32 = isIncrement ? "i32.add" : "i32.sub";
  const postOperand = unwrapParens(expr.operand);
  if (!ts13.isIdentifier(postOperand)) {
    const memberOp = isIncrement ? "add" : "sub";
    return compileMemberIncDec(ctx, fctx, expr.operand, memberOp, "postfix");
  }
  if (ts13.isIdentifier(postOperand)) {
    if (fctx.constBindings?.has(postOperand.text)) {
      emitThrowString2(ctx, fctx, "TypeError: Assignment to constant variable.");
      fctx.body.push({ op: "unreachable" });
      return { kind: "f64" };
    }
    const idx = fctx.localMap.get(postOperand.text);
    if (idx === void 0) {
      const postModIdx = ctx.moduleGlobals.get(postOperand.text);
      if (postModIdx !== void 0) {
        const postModGlobalDef = ctx.mod.globals[localGlobalIdx(ctx, postModIdx)];
        if (postModGlobalDef?.type.kind === "externref") {
          fctx.body.push({ op: "global.get", index: postModIdx });
          emitSafeExternrefToF64(ctx, fctx);
          const postOldTmp = allocLocal(fctx, `__post_old_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: postOldTmp });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: arithOp });
          addUnionImports(ctx);
          fctx.body.push({
            op: "call",
            funcIdx: ctx.funcMap.get("__box_number"),
          });
          fctx.body.push({ op: "global.set", index: postModIdx });
          fctx.body.push({ op: "local.get", index: postOldTmp });
          return { kind: "f64" };
        }
        if (postModGlobalDef && (postModGlobalDef.type.kind === "ref" || postModGlobalDef.type.kind === "ref_null")) {
          fctx.body.push({ op: "global.get", index: postModIdx });
          coerceType3(ctx, fctx, postModGlobalDef.type, { kind: "f64" });
          return { kind: "f64" };
        }
        fctx.body.push({ op: "global.get", index: postModIdx });
        fctx.body.push({ op: "global.get", index: postModIdx });
        fctx.body.push({ op: "f64.const", value: 1 });
        fctx.body.push({ op: arithOp });
        fctx.body.push({ op: "global.set", index: postModIdx });
        return { kind: "f64" };
      }
      const postCapIdx = ctx.capturedGlobals.get(postOperand.text);
      if (postCapIdx !== void 0) {
        const postCapGlobalDef = ctx.mod.globals[localGlobalIdx(ctx, postCapIdx)];
        if (postCapGlobalDef?.type.kind === "externref") {
          fctx.body.push({ op: "global.get", index: postCapIdx });
          emitSafeExternrefToF64(ctx, fctx);
          const postCapOldTmp = allocLocal(fctx, `__post_cap_old_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: postCapOldTmp });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: arithOp });
          addUnionImports(ctx);
          fctx.body.push({
            op: "call",
            funcIdx: ctx.funcMap.get("__box_number"),
          });
          fctx.body.push({ op: "global.set", index: postCapIdx });
          fctx.body.push({ op: "local.get", index: postCapOldTmp });
          return { kind: "f64" };
        }
        if (postCapGlobalDef && (postCapGlobalDef.type.kind === "ref" || postCapGlobalDef.type.kind === "ref_null")) {
          fctx.body.push({ op: "global.get", index: postCapIdx });
          coerceType3(ctx, fctx, postCapGlobalDef.type, { kind: "f64" });
          return { kind: "f64" };
        }
        fctx.body.push({ op: "global.get", index: postCapIdx });
        fctx.body.push({ op: "global.get", index: postCapIdx });
        fctx.body.push({ op: "f64.const", value: 1 });
        fctx.body.push({ op: arithOp });
        fctx.body.push({ op: "global.set", index: postCapIdx });
        return { kind: "f64" };
      }
      fctx.body.push({ op: "f64.const", value: 0 });
      return { kind: "f64" };
    }
    const boxedPost = fctx.boxedCaptures?.get(postOperand.text);
    if (boxedPost) {
      const needsCoerce = boxedPost.valType.kind !== "f64" && boxedPost.valType.kind !== "i32";
      if (needsCoerce) {
        const postOldF64 = allocLocal(fctx, `__postbox_f64_${fctx.locals.length}`, { kind: "f64" });
        const postNewTmp = allocLocal(fctx, `__postnew_${fctx.locals.length}`, boxedPost.valType);
        const savedBody = fctx.body;
        const elseBranch = [];
        fctx.body = elseBranch;
        fctx.body.push({ op: "local.get", index: idx });
        fctx.body.push({
          op: "struct.get",
          typeIdx: boxedPost.refCellTypeIdx,
          fieldIdx: 0,
        });
        coerceType3(ctx, fctx, boxedPost.valType, { kind: "f64" });
        fctx.body.push({ op: "local.tee", index: postOldF64 });
        fctx.body.push({ op: "f64.const", value: 1 });
        fctx.body.push({ op: arithOp });
        coerceType3(ctx, fctx, { kind: "f64" }, boxedPost.valType);
        fctx.body.push({ op: "local.set", index: postNewTmp });
        fctx.body.push({ op: "local.get", index: idx });
        fctx.body.push({ op: "local.get", index: postNewTmp });
        fctx.body.push({
          op: "struct.set",
          typeIdx: boxedPost.refCellTypeIdx,
          fieldIdx: 0,
        });
        fctx.body.push({ op: "local.get", index: postOldF64 });
        fctx.body = savedBody;
        fctx.body.push({ op: "local.get", index: idx });
        fctx.body.push({ op: "ref.is_null" });
        fctx.body.push({
          op: "if",
          blockType: { kind: "val", type: { kind: "f64" } },
          then: [{ op: "f64.const", value: NaN }],
          else: elseBranch,
        });
        return { kind: "f64" };
      }
      const oldTmp = allocLocal(fctx, `__postbox_${fctx.locals.length}`, boxedPost.valType);
      fctx.body.push({ op: "local.get", index: idx });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "val", type: boxedPost.valType },
        then: defaultValueInstrs(boxedPost.valType),
        else: [
          { op: "local.get", index: idx },
          {
            op: "struct.get",
            typeIdx: boxedPost.refCellTypeIdx,
            fieldIdx: 0,
          },
          { op: "local.tee", index: oldTmp },
          ...(boxedPost.valType.kind === "i32"
            ? [{ op: "i32.const", value: 1 }, { op: arithOpI32 }]
            : [{ op: "f64.const", value: 1 }, { op: arithOp }]),
          ...(() => {
            const newTmp = allocLocal(fctx, `__postnew_${fctx.locals.length}`, boxedPost.valType);
            return [
              { op: "local.set", index: newTmp },
              { op: "local.get", index: idx },
              { op: "local.get", index: newTmp },
              {
                op: "struct.set",
                typeIdx: boxedPost.refCellTypeIdx,
                fieldIdx: 0,
              },
              { op: "local.get", index: oldTmp },
            ];
          })(),
        ],
      });
      return boxedPost.valType;
    }
    const localType = getLocalType(fctx, idx);
    if (localType?.kind === "i32") {
      fctx.body.push({ op: "local.get", index: idx });
      fctx.body.push({ op: "local.get", index: idx });
      fctx.body.push({ op: "i32.const", value: 1 });
      fctx.body.push({ op: arithOpI32 });
      fctx.body.push({ op: "local.set", index: idx });
      emitMappedArgParamSync(ctx, fctx, idx, { kind: "i32" });
      return { kind: "i32" };
    }
    if (localType?.kind === "externref") {
      fctx.body.push({ op: "local.get", index: idx });
      emitSafeExternrefToF64(ctx, fctx);
      const tmpOld = allocLocal(fctx, `__postfix_old_${fctx.locals.length}`, {
        kind: "f64",
      });
      fctx.body.push({ op: "local.tee", index: tmpOld });
      fctx.body.push({ op: "f64.const", value: 1 });
      fctx.body.push({ op: arithOp });
      addUnionImports(ctx);
      fctx.body.push({ op: "call", funcIdx: ctx.funcMap.get("__box_number") });
      fctx.body.push({ op: "local.set", index: idx });
      fctx.body.push({ op: "local.get", index: tmpOld });
      emitMappedArgParamSync(ctx, fctx, idx, { kind: "f64" });
      return { kind: "f64" };
    }
    if (localType?.kind === "ref" || localType?.kind === "ref_null") {
      fctx.body.push({ op: "local.get", index: idx });
      coerceType3(ctx, fctx, localType, { kind: "f64" });
      return { kind: "f64" };
    }
    if (localType?.kind === "i64") {
      fctx.body.push({ op: "local.get", index: idx });
      fctx.body.push({ op: "local.get", index: idx });
      fctx.body.push({ op: "i64.const", value: 1n });
      fctx.body.push({ op: isIncrement ? "i64.add" : "i64.sub" });
      fctx.body.push({ op: "local.set", index: idx });
      emitMappedArgParamSync(ctx, fctx, idx, { kind: "i64" });
      return { kind: "i64" };
    }
    fctx.body.push({ op: "local.get", index: idx });
    fctx.body.push({ op: "local.get", index: idx });
    fctx.body.push({ op: "f64.const", value: 1 });
    fctx.body.push({ op: arithOp });
    fctx.body.push({ op: "local.set", index: idx });
    emitMappedArgParamSync(ctx, fctx, idx, { kind: "f64" });
    return { kind: "f64" };
  }
  if (ts13.isPropertyAccessExpression(expr.operand)) {
    return compilePostfixIncrementProperty(ctx, fctx, expr.operand, isIncrement);
  }
  if (ts13.isElementAccessExpression(expr.operand)) {
    return compilePostfixIncrementElement(ctx, fctx, expr.operand, isIncrement);
  }
  ctx.errors.push({
    message: "Unsupported postfix unary target",
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}
function compilePostfixIncrementProperty(ctx, fctx, target, isIncrement) {
  const objType = ctx.checker.getTypeAtLocation(target.expression);
  const propName = ts13.isPrivateIdentifier(target.name) ? "__priv_" + target.name.text.slice(1) : target.name.text;
  const typeName = resolveStructName(ctx, objType);
  if (!typeName) {
    ctx.errors.push({
      message: `Cannot resolve struct for postfix increment on property: ${propName}`,
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const structTypeIdx = ctx.structMap.get(typeName);
  const fields = ctx.structFields.get(typeName);
  if (structTypeIdx === void 0 || !fields) {
    ctx.errors.push({
      message: `Unknown struct type for postfix increment: ${typeName}`,
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const fieldIdx = fields.findIndex((f) => f.name === propName);
  if (fieldIdx === -1) {
    fctx.body.push({ op: "f64.const", value: NaN });
    return { kind: "f64" };
  }
  const objResult = compileExpression2(ctx, fctx, target.expression);
  if (!objResult) return null;
  const objLocal = allocLocal(fctx, `__postinc_obj_${fctx.locals.length}`, objResult);
  fctx.body.push({ op: "local.set", index: objLocal });
  fctx.body.push({ op: "local.get", index: objLocal });
  fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
  const fieldType = fields[fieldIdx].type;
  if (fieldType.kind !== "f64") {
    coerceType3(ctx, fctx, fieldType, { kind: "f64" });
  }
  const oldVal = allocLocal(fctx, `__postinc_old_${fctx.locals.length}`, {
    kind: "f64",
  });
  fctx.body.push({ op: "local.set", index: oldVal });
  fctx.body.push({ op: "local.get", index: oldVal });
  fctx.body.push({ op: "f64.const", value: 1 });
  fctx.body.push({ op: isIncrement ? "f64.add" : "f64.sub" });
  const newVal = allocLocal(fctx, `__postinc_new_${fctx.locals.length}`, {
    kind: "f64",
  });
  fctx.body.push({ op: "local.set", index: newVal });
  fctx.body.push({ op: "local.get", index: objLocal });
  fctx.body.push({ op: "local.get", index: newVal });
  if (fieldType.kind !== "f64") {
    coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
  }
  fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
  fctx.body.push({ op: "local.get", index: oldVal });
  return { kind: "f64" };
}
function compilePostfixIncrementElement(ctx, fctx, target, isIncrement) {
  const arrType = compileExpression2(ctx, fctx, target.expression);
  if (!arrType || (arrType.kind !== "ref" && arrType.kind !== "ref_null")) {
    ctx.errors.push({
      message: "Postfix increment on non-array element access",
      line: getLine(target),
      column: getCol(target),
    });
    return null;
  }
  const typeIdx = arrType.typeIdx;
  const typeDef = ctx.mod.types[typeIdx];
  if (typeDef?.kind === "struct" && ts13.isStringLiteral(target.argumentExpression)) {
    const propName = target.argumentExpression.text;
    const fieldIdx = typeDef.fields.findIndex((f) => f.name === propName);
    if (fieldIdx !== -1) {
      const objLocal = allocLocal(fctx, `__postinc_obj_${fctx.locals.length}`, arrType);
      fctx.body.push({ op: "local.set", index: objLocal });
      fctx.body.push({ op: "local.get", index: objLocal });
      fctx.body.push({ op: "struct.get", typeIdx, fieldIdx });
      const fieldType = typeDef.fields[fieldIdx].type;
      if (fieldType.kind !== "f64") coerceType3(ctx, fctx, fieldType, { kind: "f64" });
      const oldVal = allocLocal(fctx, `__postinc_old_${fctx.locals.length}`, {
        kind: "f64",
      });
      fctx.body.push({ op: "local.set", index: oldVal });
      fctx.body.push({ op: "local.get", index: oldVal });
      fctx.body.push({ op: "f64.const", value: 1 });
      fctx.body.push({ op: isIncrement ? "f64.add" : "f64.sub" });
      const newVal = allocLocal(fctx, `__postinc_new_${fctx.locals.length}`, {
        kind: "f64",
      });
      fctx.body.push({ op: "local.set", index: newVal });
      fctx.body.push({ op: "local.get", index: objLocal });
      fctx.body.push({ op: "local.get", index: newVal });
      if (fieldType.kind !== "f64") coerceType3(ctx, fctx, { kind: "f64" }, fieldType);
      fctx.body.push({ op: "struct.set", typeIdx, fieldIdx });
      fctx.body.push({ op: "local.get", index: oldVal });
      return { kind: "f64" };
    }
  }
  const isVecStruct =
    typeDef?.kind === "struct" &&
    typeDef.fields.length === 2 &&
    typeDef.fields[0]?.name === "length" &&
    typeDef.fields[1]?.name === "data";
  if (isVecStruct) {
    const arrTypeIdx = getArrTypeIdxFromVec(ctx, typeIdx);
    const arrDef = ctx.mod.types[arrTypeIdx];
    if (!arrDef || arrDef.kind !== "array") {
      ctx.errors.push({
        message: "Postfix increment: vec data is not array",
        line: getLine(target),
        column: getCol(target),
      });
      return null;
    }
    const vecLocal = allocLocal(fctx, `__postinc_vec_${fctx.locals.length}`, arrType);
    fctx.body.push({ op: "local.set", index: vecLocal });
    const idxResult = compileExpression2(ctx, fctx, target.argumentExpression, {
      kind: "f64",
    });
    if (!idxResult) return null;
    fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    const idxLocal = allocLocal(fctx, `__postinc_idx_${fctx.locals.length}`, {
      kind: "i32",
    });
    fctx.body.push({ op: "local.set", index: idxLocal });
    const elemType = arrDef.element;
    fctx.body.push({ op: "local.get", index: idxLocal });
    fctx.body.push({ op: "local.get", index: vecLocal });
    fctx.body.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
    fctx.body.push({ op: "array.len" });
    fctx.body.push({ op: "i32.lt_u" });
    const thenInstrs = [];
    thenInstrs.push({ op: "local.get", index: vecLocal });
    thenInstrs.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
    thenInstrs.push({ op: "local.get", index: idxLocal });
    thenInstrs.push({ op: "array.get", typeIdx: arrTypeIdx });
    if (elemType.kind !== "f64") {
      const savedBody = fctx.body;
      fctx.body = thenInstrs;
      coerceType3(ctx, fctx, elemType, { kind: "f64" });
      fctx.body = savedBody;
    }
    const oldVal = allocLocal(fctx, `__postinc_old_${fctx.locals.length}`, {
      kind: "f64",
    });
    thenInstrs.push({ op: "local.set", index: oldVal });
    thenInstrs.push({ op: "local.get", index: oldVal });
    thenInstrs.push({ op: "f64.const", value: 1 });
    thenInstrs.push({ op: isIncrement ? "f64.add" : "f64.sub" });
    const newVal = allocLocal(fctx, `__postinc_new_${fctx.locals.length}`, {
      kind: "f64",
    });
    thenInstrs.push({ op: "local.set", index: newVal });
    thenInstrs.push({ op: "local.get", index: vecLocal });
    thenInstrs.push({ op: "struct.get", typeIdx, fieldIdx: 1 });
    thenInstrs.push({ op: "local.get", index: idxLocal });
    thenInstrs.push({ op: "local.get", index: newVal });
    if (elemType.kind !== "f64") {
      const savedBody = fctx.body;
      fctx.body = thenInstrs;
      coerceType3(ctx, fctx, { kind: "f64" }, elemType);
      fctx.body = savedBody;
    }
    thenInstrs.push({ op: "array.set", typeIdx: arrTypeIdx });
    thenInstrs.push({ op: "local.get", index: oldVal });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "f64" } },
      then: thenInstrs,
      else: [{ op: "f64.const", value: NaN }],
    });
    return { kind: "f64" };
  }
  ctx.errors.push({
    message: "Unsupported postfix increment element access target",
    line: getLine(target),
    column: getCol(target),
  });
  return null;
}
function getFuncParamTypes(ctx, funcIdx) {
  if (funcIdx < ctx.numImportFuncs) {
    let importFuncCount = 0;
    for (const imp of ctx.mod.imports) {
      if (imp.desc.kind === "func") {
        if (importFuncCount === funcIdx) {
          const typeDef = ctx.mod.types[imp.desc.typeIdx];
          if (typeDef?.kind === "func") return typeDef.params;
          return void 0;
        }
        importFuncCount++;
      }
    }
  } else {
    const localIdx = funcIdx - ctx.numImportFuncs;
    const func = ctx.mod.functions[localIdx];
    if (func) {
      const typeDef = ctx.mod.types[func.typeIdx];
      if (typeDef?.kind === "func") return typeDef.params;
    }
  }
  return void 0;
}
function compileOptionalCallExpression(ctx, fctx, expr) {
  const propAccess = expr.expression;
  const objType = compileExpression2(ctx, fctx, propAccess.expression);
  if (!objType) return null;
  const tmp = allocLocal(fctx, `__optcall_${fctx.locals.length}`, objType);
  fctx.body.push({ op: "local.tee", index: tmp });
  fctx.body.push({ op: "ref.is_null" });
  let callReturnType = VOID_RESULT;
  const sig = ctx.checker.getResolvedSignature(expr);
  if (sig) {
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    if (!isVoidType(retType)) callReturnType = resolveWasmType(ctx, retType);
  }
  let resultType = callReturnType === VOID_RESULT ? { kind: "externref" } : callReturnType;
  const savedBody = pushBody(fctx);
  const tsReceiverType = ctx.checker.getTypeAtLocation(propAccess.expression);
  const methodName = ts13.isPrivateIdentifier(propAccess.name) ? propAccess.name.text.slice(1) : propAccess.name.text;
  let methodResolved = false;
  if (!methodResolved && isExternalDeclaredClass(tsReceiverType, ctx.checker)) {
    const className = tsReceiverType.getSymbol()?.name;
    if (className) {
      let current = className;
      while (current) {
        const info = ctx.externClasses.get(current);
        if (info?.methods.has(methodName)) {
          const importName = `${info.importPrefix}_${methodName}`;
          const funcIdx = ctx.funcMap.get(importName);
          if (funcIdx !== void 0) {
            fctx.body.push({ op: "local.get", index: tmp });
            for (const arg of expr.arguments) compileExpression2(ctx, fctx, arg);
            fctx.body.push({ op: "call", funcIdx });
            methodResolved = true;
          }
          break;
        }
        current = ctx.externClassParent.get(current);
      }
    }
  }
  if (!methodResolved) {
    let receiverClassName = tsReceiverType.getSymbol()?.name;
    if (receiverClassName && !ctx.classSet.has(receiverClassName)) {
      receiverClassName = ctx.classExprNameMap.get(receiverClassName) ?? receiverClassName;
    }
    if (receiverClassName && ctx.classSet.has(receiverClassName)) {
      let fullName = `${receiverClassName}_${methodName}`;
      let funcIdx = ctx.funcMap.get(fullName);
      if (funcIdx === void 0) {
        let ancestor = ctx.classParentMap.get(receiverClassName);
        while (ancestor && funcIdx === void 0) {
          fullName = `${ancestor}_${methodName}`;
          funcIdx = ctx.funcMap.get(fullName);
          ancestor = ctx.classParentMap.get(ancestor);
        }
      }
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "local.get", index: tmp });
        if (objType.kind === "ref_null") fctx.body.push({ op: "ref.as_non_null" });
        const paramTypes = getFuncParamTypes(ctx, funcIdx);
        for (let i = 0; i < expr.arguments.length; i++) {
          compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
        }
        if (paramTypes) {
          for (let i = expr.arguments.length + 1; i < paramTypes.length; i++) {
            pushDefaultValue(fctx, paramTypes[i], ctx);
          }
        }
        fctx.body.push({ op: "call", funcIdx });
        methodResolved = true;
      }
    }
  }
  if (!methodResolved) {
    const structTypeName = resolveStructName(ctx, tsReceiverType);
    if (structTypeName) {
      const fullName = `${structTypeName}_${methodName}`;
      const funcIdx = ctx.funcMap.get(fullName);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "local.get", index: tmp });
        if (objType.kind === "ref_null") fctx.body.push({ op: "ref.as_non_null" });
        const paramTypes = getFuncParamTypes(ctx, funcIdx);
        for (let i = 0; i < expr.arguments.length; i++) {
          compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
        }
        if (paramTypes) {
          for (let i = expr.arguments.length + 1; i < paramTypes.length; i++) {
            pushDefaultValue(fctx, paramTypes[i], ctx);
          }
        }
        fctx.body.push({ op: "call", funcIdx });
        methodResolved = true;
      }
    }
  }
  if (!methodResolved && isStringType(tsReceiverType)) {
    if (ctx.fast && ctx.nativeStrTypeIdx >= 0) {
      const nativeResult = compileNativeStringMethodCall(ctx, fctx, expr, propAccess, methodName);
      if (nativeResult !== null) {
        resultType = nativeResult;
        methodResolved = true;
      }
    } else {
      const importName = `string_${methodName}`;
      const funcIdx = ctx.funcMap.get(importName);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "local.get", index: tmp });
        for (const arg of expr.arguments) compileExpression2(ctx, fctx, arg);
        fctx.body.push({ op: "call", funcIdx });
        methodResolved = true;
      }
    }
  }
  if (!methodResolved) fctx.body.push(...defaultValueInstrs(resultType));
  const elseInstrs = fctx.body;
  popBody(fctx, savedBody);
  if (resultType.kind === "ref") resultType = { kind: "ref_null", typeIdx: resultType.typeIdx };
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: resultType },
    then: defaultValueInstrs(resultType),
    else: elseInstrs,
  });
  return resultType;
}
function compileOptionalDirectCall(ctx, fctx, expr) {
  const callee = expr.expression;
  const calleeType = compileExpression2(ctx, fctx, callee);
  if (!calleeType) return null;
  if (calleeType.kind !== "ref" && calleeType.kind !== "ref_null" && calleeType.kind !== "externref") {
    fctx.body.push({ op: "drop" });
    const syntheticCall = ts13.factory.createCallExpression(callee, expr.typeArguments, expr.arguments);
    ts13.setTextRange(syntheticCall, expr);
    return compileCallExpression(ctx, fctx, syntheticCall);
  }
  const tmp = allocLocal(fctx, `__optdcall_${fctx.locals.length}`, calleeType);
  fctx.body.push({ op: "local.tee", index: tmp });
  fctx.body.push({ op: "ref.is_null" });
  let resultType = { kind: "externref" };
  const sig = ctx.checker.getResolvedSignature(expr);
  if (sig) {
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    if (!isVoidType(retType)) {
      const resolved2 = resolveWasmType(ctx, retType);
      resultType = resolved2.kind === "ref" ? { kind: "ref_null", typeIdx: resolved2.typeIdx } : resolved2;
    }
  }
  const savedBody = pushBody(fctx);
  const funcName = callee.text;
  const closureInfo = ctx.closureMap.get(funcName);
  const funcIdx = ctx.funcMap.get(funcName);
  let resolved = false;
  if (closureInfo && (calleeType.kind === "ref" || calleeType.kind === "ref_null")) {
    fctx.body.push({ op: "local.get", index: tmp });
    if (calleeType.kind === "ref_null") fctx.body.push({ op: "ref.as_non_null" });
    const closureTmp = allocLocal(fctx, `__optdcall_cls_${fctx.locals.length}`, {
      kind: "ref",
      typeIdx: calleeType.typeIdx,
    });
    fctx.body.push({ op: "local.tee", index: closureTmp });
    fctx.body.push({ op: "local.get", index: closureTmp });
    for (const arg of expr.arguments) compileExpression2(ctx, fctx, arg);
    fctx.body.push({ op: "call_ref", typeIdx: closureInfo.funcTypeIdx });
    resolved = true;
  } else if (funcIdx !== void 0) {
    const paramTypes = getFuncParamTypes(ctx, funcIdx);
    for (let i = 0; i < expr.arguments.length; i++) {
      compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i]);
    }
    if (paramTypes) {
      for (let i = expr.arguments.length; i < paramTypes.length; i++) {
        pushDefaultValue(fctx, paramTypes[i], ctx);
      }
    }
    fctx.body.push({ op: "call", funcIdx });
    resolved = true;
  }
  if (!resolved) fctx.body.push(...defaultValueInstrs(resultType));
  const elseInstrs = fctx.body;
  popBody(fctx, savedBody);
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: resultType },
    then: defaultValueInstrs(resultType),
    else: elseInstrs,
  });
  return resultType;
}
function compileClosureCall(ctx, fctx, expr, varName, info) {
  const localIdx = fctx.localMap.get(varName);
  const moduleIdx = localIdx === void 0 ? ctx.moduleGlobals.get(varName) : void 0;
  if (localIdx === void 0 && moduleIdx === void 0) return null;
  let effectiveLocalIdx = localIdx;
  if (localIdx !== void 0) {
    const localType =
      localIdx < fctx.params.length ? fctx.params[localIdx]?.type : fctx.locals[localIdx - fctx.params.length]?.type;
    if (localType?.kind === "externref") {
      const castType = { kind: "ref_null", typeIdx: info.structTypeIdx };
      const castLocal = allocLocal(fctx, `__closure_cast_${fctx.locals.length}`, castType);
      fctx.body.push({ op: "local.get", index: localIdx });
      fctx.body.push({ op: "any.convert_extern" });
      emitGuardedRefCast(fctx, info.structTypeIdx);
      fctx.body.push({ op: "local.set", index: castLocal });
      effectiveLocalIdx = castLocal;
    }
  }
  const pushClosureRef = () => {
    if (effectiveLocalIdx !== void 0) {
      fctx.body.push({ op: "local.get", index: effectiveLocalIdx });
    } else {
      fctx.body.push({ op: "global.get", index: moduleIdx });
    }
    emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: info.structTypeIdx });
  };
  pushClosureRef();
  const paramCount = info.paramTypes.length;
  for (let i = 0; i < Math.min(expr.arguments.length, paramCount); i++) {
    compileExpression2(ctx, fctx, expr.arguments[i], info.paramTypes[i]);
  }
  for (let i = paramCount; i < expr.arguments.length; i++) {
    const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
    if (extraType !== null) {
      fctx.body.push({ op: "drop" });
    }
  }
  for (let i = expr.arguments.length; i < info.paramTypes.length; i++) {
    pushDefaultValue(fctx, info.paramTypes[i], ctx);
  }
  pushClosureRef();
  fctx.body.push({
    op: "struct.get",
    typeIdx: info.structTypeIdx,
    fieldIdx: 0,
  });
  emitGuardedFuncRefCast(fctx, info.funcTypeIdx);
  emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: info.funcTypeIdx });
  fctx.body.push({ op: "call_ref", typeIdx: info.funcTypeIdx });
  return info.returnType ?? VOID_RESULT;
}
function compileGetterCallable(ctx, fctx, expr, propAccess, receiverClassName, getterIdx) {
  const propTsType = ctx.checker.getTypeAtLocation(propAccess);
  const callSigs = propTsType.getCallSignatures?.();
  if (!callSigs || callSigs.length === 0) return void 0;
  const methodName = ts13.isPrivateIdentifier(propAccess.name)
    ? "__priv_" + propAccess.name.text.slice(1)
    : propAccess.name.text;
  const candidateNames = [
    `${receiverClassName}___priv_${methodName}`,
    // get method -> this.#method
    `${receiverClassName}_${methodName}`,
    // get method -> this.method (self-reference unlikely but check)
  ];
  let ancestor = ctx.classParentMap.get(receiverClassName);
  while (ancestor) {
    candidateNames.push(`${ancestor}___priv_${methodName}`);
    candidateNames.push(`${ancestor}_${methodName}`);
    ancestor = ctx.classParentMap.get(ancestor);
  }
  for (const candidateName of candidateNames) {
    const candidateIdx = ctx.funcMap.get(candidateName);
    if (candidateIdx === void 0) continue;
    const structTypeIdx = ctx.structMap.get(receiverClassName);
    const paramTypes = getFuncParamTypes(ctx, candidateIdx);
    const recvTypeHint = paramTypes?.[0];
    const recvType = compileExpression2(ctx, fctx, propAccess.expression, recvTypeHint);
    if (recvType && recvTypeHint) {
      if (
        recvType.kind === "externref" &&
        (recvTypeHint.kind === "ref" || recvTypeHint.kind === "ref_null") &&
        structTypeIdx !== void 0
      ) {
        fctx.body.push({ op: "any.convert_extern" });
        emitGuardedRefCast(fctx, structTypeIdx);
      } else if ((recvType.kind === "ref" || recvType.kind === "ref_null") && recvTypeHint.kind === "externref") {
        fctx.body.push({ op: "extern.convert_any" });
      } else if (recvType.kind !== recvTypeHint.kind) {
        coerceType3(ctx, fctx, recvType, recvTypeHint);
      }
    } else if (recvType && recvType.kind === "externref" && structTypeIdx !== void 0 && recvTypeHint === void 0) {
      fctx.body.push({ op: "any.convert_extern" });
      emitGuardedRefCast(fctx, structTypeIdx);
    }
    const methodParamCount = paramTypes ? paramTypes.length - 1 : expr.arguments.length;
    for (let i = 0; i < Math.min(expr.arguments.length, methodParamCount); i++) {
      compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
    }
    for (let i = methodParamCount; i < expr.arguments.length; i++) {
      const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
      if (extraType !== null) {
        fctx.body.push({ op: "drop" });
      }
    }
    if (paramTypes) {
      for (let i = Math.min(expr.arguments.length, methodParamCount) + 1; i < paramTypes.length; i++) {
        pushDefaultValue(fctx, paramTypes[i], ctx);
      }
    }
    fctx.body.push({ op: "call", funcIdx: candidateIdx });
    const sig = ctx.checker.getResolvedSignature(expr);
    if (sig) {
      const retType = ctx.checker.getReturnTypeOfSignature(sig);
      if (isEffectivelyVoidReturn(ctx, retType, candidateName)) return VOID_RESULT;
      if (wasmFuncReturnsVoid(ctx, candidateIdx)) return VOID_RESULT;
      return getWasmFuncReturnType(ctx, candidateIdx) ?? resolveWasmType(ctx, retType);
    }
    return getWasmFuncReturnType(ctx, candidateIdx) ?? VOID_RESULT;
  }
  return void 0;
}
function compileCallablePropertyCall(ctx, fctx, expr, propAccess, className) {
  const methodName = ts13.isPrivateIdentifier(propAccess.name)
    ? "__priv_" + propAccess.name.text.slice(1)
    : propAccess.name.text;
  const structTypeIdx = ctx.structMap.get(className);
  const fields = ctx.structFields.get(className);
  if (structTypeIdx === void 0 || !fields) return void 0;
  const fieldIdx = fields.findIndex((f) => f.name === methodName);
  if (fieldIdx === -1) return void 0;
  const fieldType = fields[fieldIdx].type;
  const propTsType = ctx.checker.getTypeAtLocation(propAccess);
  const callSigs = propTsType.getCallSignatures?.();
  if (!callSigs || callSigs.length === 0) return void 0;
  const sig = callSigs[0];
  const sigParamCount = sig.parameters.length;
  const sigRetType = ctx.checker.getReturnTypeOfSignature(sig);
  const sigRetWasm = isVoidType(sigRetType) ? null : resolveWasmType(ctx, sigRetType);
  const sigParamWasmTypes = [];
  for (let i = 0; i < sigParamCount; i++) {
    const paramType = ctx.checker.getTypeOfSymbol(sig.parameters[i]);
    sigParamWasmTypes.push(resolveWasmType(ctx, paramType));
  }
  if (fieldType.kind === "ref" || fieldType.kind === "ref_null") {
    const closureInfo = ctx.closureInfoByTypeIdx.get(fieldType.typeIdx);
    if (closureInfo) {
      compileExpression2(ctx, fctx, propAccess.expression);
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
      const closureLocal = allocLocal(fctx, `__cprop_${fctx.locals.length}`, fieldType);
      fctx.body.push({ op: "local.set", index: closureLocal });
      fctx.body.push({ op: "local.get", index: closureLocal });
      if (fieldType.kind === "ref_null") {
        emitNullCheckThrow2(ctx, fctx, fieldType);
      }
      {
        const cpParamCount = closureInfo.paramTypes.length;
        for (let i = 0; i < Math.min(expr.arguments.length, cpParamCount); i++) {
          compileExpression2(ctx, fctx, expr.arguments[i], closureInfo.paramTypes[i]);
        }
        for (let i = cpParamCount; i < expr.arguments.length; i++) {
          const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
          if (extraType !== null) {
            fctx.body.push({ op: "drop" });
          }
        }
      }
      for (let i = expr.arguments.length; i < closureInfo.paramTypes.length; i++) {
        pushDefaultValue(fctx, closureInfo.paramTypes[i], ctx);
      }
      fctx.body.push({ op: "local.get", index: closureLocal });
      if (fieldType.kind === "ref_null") {
        emitNullCheckThrow2(ctx, fctx, fieldType);
      }
      fctx.body.push({
        op: "struct.get",
        typeIdx: fieldType.typeIdx,
        fieldIdx: 0,
      });
      emitGuardedFuncRefCast(fctx, closureInfo.funcTypeIdx);
      emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: closureInfo.funcTypeIdx });
      fctx.body.push({ op: "call_ref", typeIdx: closureInfo.funcTypeIdx });
      return closureInfo.returnType ?? VOID_RESULT;
    }
  }
  if (fieldType.kind === "externref") {
    const resultTypes = sigRetWasm ? [sigRetWasm] : [];
    const wrapperTypes = getOrCreateFuncRefWrapperTypes(ctx, sigParamWasmTypes, resultTypes);
    if (wrapperTypes) {
      const { structTypeIdx: wrapperStructIdx, closureInfo: matchedClosureInfo } = wrapperTypes;
      compileExpression2(ctx, fctx, propAccess.expression);
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
      const closureRefType = {
        kind: "ref_null",
        typeIdx: wrapperStructIdx,
      };
      const closureLocal = allocLocal(fctx, `__cprop_ext_${fctx.locals.length}`, closureRefType);
      fctx.body.push({ op: "any.convert_extern" });
      emitGuardedRefCast(fctx, wrapperStructIdx);
      fctx.body.push({ op: "local.set", index: closureLocal });
      fctx.body.push({ op: "local.get", index: closureLocal });
      emitNullCheckThrow2(ctx, fctx, closureRefType);
      {
        const wpParamCount = matchedClosureInfo.paramTypes.length;
        for (let i = 0; i < Math.min(expr.arguments.length, wpParamCount); i++) {
          compileExpression2(ctx, fctx, expr.arguments[i], matchedClosureInfo.paramTypes[i]);
        }
        for (let i = wpParamCount; i < expr.arguments.length; i++) {
          const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
          if (extraType !== null) {
            fctx.body.push({ op: "drop" });
          }
        }
      }
      for (let i = expr.arguments.length; i < matchedClosureInfo.paramTypes.length; i++) {
        pushDefaultValue(fctx, matchedClosureInfo.paramTypes[i], ctx);
      }
      fctx.body.push({ op: "local.get", index: closureLocal });
      emitNullCheckThrow2(ctx, fctx, closureRefType);
      fctx.body.push({
        op: "struct.get",
        typeIdx: wrapperStructIdx,
        fieldIdx: 0,
      });
      emitGuardedFuncRefCast(fctx, matchedClosureInfo.funcTypeIdx);
      emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedClosureInfo.funcTypeIdx });
      fctx.body.push({
        op: "call_ref",
        typeIdx: matchedClosureInfo.funcTypeIdx,
      });
      return matchedClosureInfo.returnType ?? VOID_RESULT;
    }
  }
  if (fieldType.kind === "ref" || fieldType.kind === "ref_null") {
    let matchedClosureInfo;
    let matchedStructTypeIdx;
    for (const [typeIdx, info] of ctx.closureInfoByTypeIdx) {
      if (info.paramTypes.length !== sigParamCount) continue;
      if (sigRetWasm === null && info.returnType !== null) continue;
      if (sigRetWasm !== null && info.returnType === null) continue;
      if (sigRetWasm !== null && info.returnType !== null && sigRetWasm.kind !== info.returnType.kind) continue;
      let paramsMatch = true;
      for (let i = 0; i < sigParamCount; i++) {
        if (sigParamWasmTypes[i].kind !== info.paramTypes[i].kind) {
          paramsMatch = false;
          break;
        }
      }
      if (paramsMatch) {
        matchedClosureInfo = info;
        matchedStructTypeIdx = typeIdx;
        break;
      }
    }
    if (matchedClosureInfo && matchedStructTypeIdx !== void 0) {
      compileExpression2(ctx, fctx, propAccess.expression);
      fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
      const closureLocal = allocLocal(fctx, `__cprop_ref_${fctx.locals.length}`, fieldType);
      fctx.body.push({ op: "local.set", index: closureLocal });
      fctx.body.push({ op: "local.get", index: closureLocal });
      if (fieldType.kind === "ref_null") {
        emitNullCheckThrow2(ctx, fctx, fieldType);
      }
      if (fieldType.typeIdx !== matchedStructTypeIdx) {
        emitGuardedRefCast(fctx, matchedStructTypeIdx);
      }
      {
        const cpRefParamCount = matchedClosureInfo.paramTypes.length;
        for (let i = 0; i < Math.min(expr.arguments.length, cpRefParamCount); i++) {
          compileExpression2(ctx, fctx, expr.arguments[i], matchedClosureInfo.paramTypes[i]);
        }
        for (let i = cpRefParamCount; i < expr.arguments.length; i++) {
          const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
          if (extraType !== null) {
            fctx.body.push({ op: "drop" });
          }
        }
      }
      for (let i = expr.arguments.length; i < matchedClosureInfo.paramTypes.length; i++) {
        pushDefaultValue(fctx, matchedClosureInfo.paramTypes[i], ctx);
      }
      fctx.body.push({ op: "local.get", index: closureLocal });
      if (fieldType.kind === "ref_null") {
        emitNullCheckThrow2(ctx, fctx, fieldType);
      }
      if (fieldType.typeIdx !== matchedStructTypeIdx) {
        emitGuardedRefCast(fctx, matchedStructTypeIdx);
      }
      fctx.body.push({
        op: "struct.get",
        typeIdx: matchedStructTypeIdx,
        fieldIdx: 0,
      });
      emitGuardedFuncRefCast(fctx, matchedClosureInfo.funcTypeIdx);
      emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedClosureInfo.funcTypeIdx });
      fctx.body.push({
        op: "call_ref",
        typeIdx: matchedClosureInfo.funcTypeIdx,
      });
      return matchedClosureInfo.returnType ?? VOID_RESULT;
    }
  }
  return void 0;
}
function tryExternClassMethodOnAny(ctx, fctx, expr, propAccess, methodName) {
  for (const [, info] of ctx.externClasses) {
    const sig = info.methods.get(methodName);
    if (!sig) continue;
    const importName = `${info.importPrefix}_${methodName}`;
    let funcIdx = ctx.funcMap.get(importName);
    if (funcIdx === void 0) {
      const importsBefore = ctx.numImportFuncs;
      const typeIdx = addFuncType(ctx, sig.params, sig.results);
      addImport(ctx, "env", importName, { kind: "func", typeIdx });
      shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
      funcIdx = ctx.funcMap.get(importName);
    }
    if (funcIdx === void 0) continue;
    compileExpression2(ctx, fctx, propAccess.expression, { kind: "externref" });
    const argCount = sig.params.length - 1;
    for (let i = 0; i < expr.arguments.length && i < argCount; i++) {
      compileExpression2(ctx, fctx, expr.arguments[i], { kind: "externref" });
    }
    for (let i = expr.arguments.length; i < argCount; i++) {
      fctx.body.push({ op: "ref.null.extern" });
    }
    for (let i = argCount; i < expr.arguments.length; i++) {
      const argType = compileExpression2(ctx, fctx, expr.arguments[i]);
      if (argType) fctx.body.push({ op: "drop" });
    }
    fctx.body.push({ op: "call", funcIdx });
    if (sig.results.length === 0) return VOID_RESULT;
    return sig.results[0];
  }
  return null;
}
function compileCallExpression(ctx, fctx, expr) {
  if (expr.questionDotToken && ts13.isPropertyAccessExpression(expr.expression)) {
    return compileOptionalCallExpression(ctx, fctx, expr);
  }
  if (expr.questionDotToken && ts13.isIdentifier(expr.expression)) {
    return compileOptionalDirectCall(ctx, fctx, expr);
  }
  if (expr.expression.kind === ts13.SyntaxKind.ImportKeyword) {
    let dynIdx = ctx.funcMap.get("__dynamic_import");
    if (dynIdx === void 0) {
      const importsBefore = ctx.numImportFuncs;
      const dynType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
      addImport(ctx, "env", "__dynamic_import", { kind: "func", typeIdx: dynType });
      shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
      dynIdx = ctx.funcMap.get("__dynamic_import");
    }
    if (dynIdx === void 0) {
      fctx.body.push({ op: "unreachable" });
      return null;
    }
    const specArg = expr.arguments[0];
    if (specArg) {
      const specResult = compileExpression2(ctx, fctx, specArg);
      if (specResult && specResult.kind !== "externref") {
        coerceType3(ctx, fctx, specResult, { kind: "externref" });
      }
    } else {
      fctx.body.push({ op: "ref.null", refType: "extern" });
    }
    for (let ai = 1; ai < expr.arguments.length; ai++) {
      const extraArg = expr.arguments[ai];
      const extraResult = compileExpression2(ctx, fctx, extraArg);
      if (extraResult) {
        fctx.body.push({ op: "drop" });
      }
    }
    fctx.body.push({ op: "call", funcIdx: dynIdx });
    return { kind: "externref" };
  }
  if (ts13.isParenthesizedExpression(expr.expression)) {
    let unwrapped = expr.expression;
    while (ts13.isParenthesizedExpression(unwrapped)) {
      unwrapped = unwrapped.expression;
    }
    if (
      !ts13.isFunctionExpression(unwrapped) &&
      !ts13.isArrowFunction(unwrapped) &&
      !(ts13.isBinaryExpression(unwrapped) && unwrapped.operatorToken.kind === ts13.SyntaxKind.CommaToken)
    ) {
      if (ts13.isConditionalExpression(unwrapped)) {
        return compileConditionalCallee(ctx, fctx, expr, unwrapped);
      }
      if (ts13.isBinaryExpression(unwrapped)) {
        return compileExpressionCallee(ctx, fctx, expr, unwrapped);
      }
      if (ts13.isPrefixUnaryExpression(unwrapped) || ts13.isPostfixUnaryExpression(unwrapped)) {
        return compileExpressionCallee(ctx, fctx, expr, unwrapped);
      }
      const syntheticCall = ts13.factory.createCallExpression(unwrapped, expr.typeArguments, expr.arguments);
      ts13.setTextRange(syntheticCall, expr);
      syntheticCall.parent = expr.parent;
      return compileCallExpression(ctx, fctx, syntheticCall);
    }
  }
  if (
    ts13.isPropertyAccessExpression(expr.expression) &&
    expr.expression.expression.kind === ts13.SyntaxKind.SuperKeyword
  ) {
    return compileSuperMethodCall(ctx, fctx, expr);
  }
  if (ts13.isPropertyAccessExpression(expr.expression)) {
    const propAccess = expr.expression;
    {
      const callResult = compileArrayPrototypeCall(ctx, fctx, expr, propAccess);
      if (callResult !== void 0) return callResult;
    }
    if (propAccess.name.text === "call" || propAccess.name.text === "apply") {
      const isCall = propAccess.name.text === "call";
      const innerExpr = propAccess.expression;
      if (ts13.isIdentifier(innerExpr)) {
        const funcName = innerExpr.text;
        let closureInfo = ctx.closureMap.get(funcName);
        const funcIdx = ctx.funcMap.get(funcName);
        if (!closureInfo && funcIdx === void 0) {
          const localIdx = fctx.localMap.get(funcName);
          if (localIdx !== void 0) {
            const localType =
              localIdx < fctx.params.length
                ? fctx.params[localIdx]?.type
                : fctx.locals[localIdx - fctx.params.length]?.type;
            if (localType && (localType.kind === "ref" || localType.kind === "ref_null")) {
              closureInfo = ctx.closureInfoByTypeIdx.get(localType.typeIdx);
            }
          }
        }
        if (closureInfo || funcIdx !== void 0) {
          if (expr.arguments.length > 0) {
            const thisType = compileExpression2(ctx, fctx, expr.arguments[0]);
            if (thisType) {
              fctx.body.push({ op: "drop" });
            }
          }
          if (isCall) {
            const remainingArgs = expr.arguments.slice(1);
            if (closureInfo) {
              const syntheticCall = ts13.factory.createCallExpression(innerExpr, void 0, remainingArgs);
              syntheticCall.parent = expr.parent;
              return compileClosureCall(ctx, fctx, syntheticCall, funcName, closureInfo);
            }
            const callRestInfo = ctx.funcRestParams.get(funcName);
            if (callRestInfo) {
              const paramTypes = getFuncParamTypes(ctx, funcIdx);
              for (let i = 0; i < callRestInfo.restIndex; i++) {
                if (i < remainingArgs.length) {
                  compileExpression2(ctx, fctx, remainingArgs[i], paramTypes?.[i]);
                } else {
                  pushDefaultValue(fctx, paramTypes?.[i] ?? { kind: "f64" }, ctx);
                }
              }
              const restArgCount = Math.max(0, remainingArgs.length - callRestInfo.restIndex);
              fctx.body.push({ op: "i32.const", value: restArgCount });
              for (let i = callRestInfo.restIndex; i < remainingArgs.length; i++) {
                compileExpression2(ctx, fctx, remainingArgs[i], callRestInfo.elemType);
              }
              fctx.body.push({
                op: "array.new_fixed",
                typeIdx: callRestInfo.arrayTypeIdx,
                length: restArgCount,
              });
              fctx.body.push({
                op: "struct.new",
                typeIdx: callRestInfo.vecTypeIdx,
              });
            } else {
              const paramTypes = getFuncParamTypes(ctx, funcIdx);
              for (let i = 0; i < remainingArgs.length; i++) {
                compileExpression2(ctx, fctx, remainingArgs[i], paramTypes?.[i]);
              }
              const optInfo = ctx.funcOptionalParams.get(funcName);
              if (optInfo) {
                const numProvided = remainingArgs.length;
                for (const opt of optInfo) {
                  if (opt.index >= numProvided) {
                    pushParamSentinel(fctx, opt.type, ctx, opt);
                  }
                }
              }
              if (paramTypes) {
                const providedCount = Math.min(remainingArgs.length, paramTypes.length);
                const optFilledCount = ctx.funcOptionalParams.get(funcName)
                  ? ctx.funcOptionalParams.get(funcName).filter((o) => o.index >= remainingArgs.length).length
                  : 0;
                const totalPushed = providedCount + optFilledCount;
                for (let i = totalPushed; i < paramTypes.length; i++) {
                  pushDefaultValue(fctx, paramTypes[i], ctx);
                }
              }
            }
            const finalFuncIdx = ctx.funcMap.get(funcName) ?? funcIdx;
            fctx.body.push({ op: "call", funcIdx: finalFuncIdx });
            if (wasmFuncReturnsVoid(ctx, finalFuncIdx)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, finalFuncIdx) ?? VOID_RESULT;
          }
          if (!isCall && expr.arguments.length >= 2) {
            const argsExpr = expr.arguments[1];
            if (ts13.isArrayLiteralExpression(argsExpr)) {
              const elements = argsExpr.elements;
              if (closureInfo) {
                const syntheticCall = ts13.factory.createCallExpression(innerExpr, void 0, elements);
                syntheticCall.parent = expr.parent;
                return compileClosureCall(ctx, fctx, syntheticCall, funcName, closureInfo);
              }
              const applyRestInfo = ctx.funcRestParams.get(funcName);
              if (applyRestInfo) {
                const paramTypes = getFuncParamTypes(ctx, funcIdx);
                for (let i = 0; i < applyRestInfo.restIndex; i++) {
                  if (i < elements.length) {
                    compileExpression2(ctx, fctx, elements[i], paramTypes?.[i]);
                  } else {
                    pushDefaultValue(fctx, paramTypes?.[i] ?? { kind: "f64" }, ctx);
                  }
                }
                const restArgCount = Math.max(0, elements.length - applyRestInfo.restIndex);
                fctx.body.push({ op: "i32.const", value: restArgCount });
                for (let i = applyRestInfo.restIndex; i < elements.length; i++) {
                  compileExpression2(ctx, fctx, elements[i], applyRestInfo.elemType);
                }
                fctx.body.push({
                  op: "array.new_fixed",
                  typeIdx: applyRestInfo.arrayTypeIdx,
                  length: restArgCount,
                });
                fctx.body.push({
                  op: "struct.new",
                  typeIdx: applyRestInfo.vecTypeIdx,
                });
              } else {
                const paramTypes = getFuncParamTypes(ctx, funcIdx);
                for (let i = 0; i < elements.length; i++) {
                  compileExpression2(ctx, fctx, elements[i], paramTypes?.[i]);
                }
                const optInfo = ctx.funcOptionalParams.get(funcName);
                if (optInfo) {
                  for (const opt of optInfo) {
                    if (opt.index >= elements.length) pushParamSentinel(fctx, opt.type, ctx, opt);
                  }
                }
                if (paramTypes) {
                  const providedCount = Math.min(elements.length, paramTypes.length);
                  const optFilledCount = ctx.funcOptionalParams.get(funcName)
                    ? ctx.funcOptionalParams.get(funcName).filter((o) => o.index >= elements.length).length
                    : 0;
                  const totalPushed = providedCount + optFilledCount;
                  for (let i = totalPushed; i < paramTypes.length; i++) {
                    pushDefaultValue(fctx, paramTypes[i], ctx);
                  }
                }
              }
              const finalFuncIdx = ctx.funcMap.get(funcName) ?? funcIdx;
              fctx.body.push({ op: "call", funcIdx: finalFuncIdx });
              if (wasmFuncReturnsVoid(ctx, finalFuncIdx)) return VOID_RESULT;
              return getWasmFuncReturnType(ctx, finalFuncIdx) ?? VOID_RESULT;
            }
          }
          if (!isCall) {
            if (closureInfo) {
              const syntheticCall = ts13.factory.createCallExpression(innerExpr, void 0, []);
              syntheticCall.parent = expr.parent;
              return compileClosureCall(ctx, fctx, syntheticCall, funcName, closureInfo);
            }
            const applyNoArgsRestInfo = ctx.funcRestParams.get(funcName);
            if (applyNoArgsRestInfo) {
              const paramTypes = getFuncParamTypes(ctx, funcIdx);
              for (let i = 0; i < applyNoArgsRestInfo.restIndex; i++) {
                pushDefaultValue(fctx, paramTypes?.[i] ?? { kind: "f64" }, ctx);
              }
              fctx.body.push({ op: "i32.const", value: 0 });
              fctx.body.push({
                op: "array.new_fixed",
                typeIdx: applyNoArgsRestInfo.arrayTypeIdx,
                length: 0,
              });
              fctx.body.push({
                op: "struct.new",
                typeIdx: applyNoArgsRestInfo.vecTypeIdx,
              });
            } else {
              const optInfo = ctx.funcOptionalParams.get(funcName);
              if (optInfo) {
                for (const opt of optInfo) pushParamSentinel(fctx, opt.type, ctx, opt);
              }
              const paramTypes = getFuncParamTypes(ctx, funcIdx);
              if (paramTypes) {
                const optFilledCount = ctx.funcOptionalParams.get(funcName)
                  ? ctx.funcOptionalParams.get(funcName).length
                  : 0;
                for (let i = optFilledCount; i < paramTypes.length; i++) {
                  pushDefaultValue(fctx, paramTypes[i], ctx);
                }
              }
            }
            const finalFuncIdx = ctx.funcMap.get(funcName) ?? funcIdx;
            fctx.body.push({ op: "call", funcIdx: finalFuncIdx });
            if (wasmFuncReturnsVoid(ctx, finalFuncIdx)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, finalFuncIdx) ?? VOID_RESULT;
          }
        }
      }
      if (ts13.isPropertyAccessExpression(innerExpr)) {
        const methodName = innerExpr.name.text;
        const objExpr = innerExpr.expression;
        const objType = ctx.checker.getTypeAtLocation(objExpr);
        if (
          ts13.isPropertyAccessExpression(objExpr) &&
          objExpr.name.text === "prototype" &&
          ts13.isIdentifier(objExpr.expression) &&
          isCall &&
          expr.arguments.length >= 1
        ) {
          const typeName = objExpr.expression.text;
          if (
            (typeName === "String" ||
              typeName === "Number" ||
              typeName === "Array" ||
              typeName === "Boolean" ||
              typeName === "Object") &&
            expr.arguments.length >= 1
          ) {
            const receiverArg = expr.arguments[0];
            const remainingArgs = Array.from(expr.arguments).slice(1);
            const syntheticPropAccess = ts13.factory.createPropertyAccessExpression(receiverArg, methodName);
            const syntheticCall = ts13.factory.createCallExpression(
              syntheticPropAccess,
              expr.typeArguments,
              remainingArgs,
            );
            ts13.setTextRange(syntheticCall, expr);
            syntheticCall.parent = expr.parent;
            return compileCallExpression(ctx, fctx, syntheticCall);
          }
        }
        let className = objType.getSymbol()?.name;
        if (className && !ctx.classSet.has(className)) {
          className = ctx.classExprNameMap.get(className) ?? className;
        }
        if (!className || !ctx.classSet.has(className)) {
          className = resolveStructName(ctx, objType) ?? void 0;
        }
        if (className && (ctx.classSet.has(className) || ctx.funcMap.has(`${className}_${methodName}`))) {
          const fullName = `${className}_${methodName}`;
          const funcIdx = ctx.funcMap.get(fullName);
          if (funcIdx !== void 0 && expr.arguments.length > 0) {
            compileExpression2(ctx, fctx, expr.arguments[0]);
            if (isCall) {
              const paramTypes = getFuncParamTypes(ctx, funcIdx);
              const callParamCount = paramTypes ? paramTypes.length - 1 : expr.arguments.length - 1;
              for (let i = 1; i < expr.arguments.length; i++) {
                if (i - 1 < callParamCount) {
                  compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i]);
                } else {
                  const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
                  if (extraType !== null) {
                    fctx.body.push({ op: "drop" });
                  }
                }
              }
              if (paramTypes) {
                for (let i = expr.arguments.length; i < paramTypes.length; i++) {
                  pushDefaultValue(fctx, paramTypes[i], ctx);
                }
              }
            } else if (expr.arguments.length >= 2 && ts13.isArrayLiteralExpression(expr.arguments[1])) {
              const elements = expr.arguments[1].elements;
              const paramTypes = getFuncParamTypes(ctx, funcIdx);
              const applyParamCount = paramTypes ? paramTypes.length - 1 : elements.length;
              for (let i = 0; i < elements.length; i++) {
                if (i < applyParamCount) {
                  compileExpression2(ctx, fctx, elements[i], paramTypes?.[i + 1]);
                } else {
                  const extraType = compileExpression2(ctx, fctx, elements[i]);
                  if (extraType !== null) {
                    fctx.body.push({ op: "drop" });
                  }
                }
              }
              if (paramTypes) {
                for (let i = elements.length + 1; i < paramTypes.length; i++) {
                  pushDefaultValue(fctx, paramTypes[i], ctx);
                }
              }
            }
            const finalCallIdx = ctx.funcMap.get(fullName) ?? funcIdx;
            fctx.body.push({ op: "call", funcIdx: finalCallIdx });
            if (wasmFuncReturnsVoid(ctx, finalCallIdx)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, finalCallIdx) ?? VOID_RESULT;
          }
        }
      }
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "console" &&
      (propAccess.name.text === "log" || propAccess.name.text === "warn" || propAccess.name.text === "error")
    ) {
      return compileConsoleCall(ctx, fctx, expr, propAccess.name.text);
    }
    if (
      ctx.wasi &&
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "process" &&
      propAccess.name.text === "exit" &&
      ctx.wasiProcExitIdx >= 0
    ) {
      if (expr.arguments.length >= 1) {
        compileExpression2(ctx, fctx, expr.arguments[0], { kind: "i32" });
        const argType = ctx.checker.getTypeAtLocation(expr.arguments[0]);
        if (isNumberType(argType)) {
          fctx.body.push({ op: "i32.trunc_sat_f64_s" });
        }
      } else {
        fctx.body.push({ op: "i32.const", value: 0 });
      }
      fctx.body.push({ op: "call", funcIdx: ctx.wasiProcExitIdx });
      return VOID_RESULT;
    }
    if (ts13.isIdentifier(propAccess.expression) && propAccess.expression.text === "Math") {
      const mathResult = compileMathCall(ctx, fctx, propAccess.name.text, expr);
      if (mathResult !== void 0) return mathResult;
    }
    if (ts13.isIdentifier(propAccess.expression) && propAccess.expression.text === "Number") {
      const method = propAccess.name.text;
      if (method === "isNaN" && expr.arguments.length >= 1) {
        compileExpression2(ctx, fctx, expr.arguments[0], { kind: "f64" });
        const tmp = allocLocal(fctx, `__isnan_${fctx.locals.length}`, {
          kind: "f64",
        });
        fctx.body.push({ op: "local.tee", index: tmp });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "f64.ne" });
        return { kind: "i32" };
      }
      if (method === "isInteger" && expr.arguments.length >= 1) {
        compileExpression2(ctx, fctx, expr.arguments[0], { kind: "f64" });
        const tmp = allocLocal(fctx, `__isint_${fctx.locals.length}`, {
          kind: "f64",
        });
        fctx.body.push({ op: "local.tee", index: tmp });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "f64.trunc" });
        fctx.body.push({ op: "f64.eq" });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "f64.sub" });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.eq" });
        fctx.body.push({ op: "i32.and" });
        return { kind: "i32" };
      }
      if (method === "isFinite" && expr.arguments.length >= 1) {
        compileExpression2(ctx, fctx, expr.arguments[0], { kind: "f64" });
        const tmp = allocLocal(fctx, `__isfin_${fctx.locals.length}`, {
          kind: "f64",
        });
        fctx.body.push({ op: "local.tee", index: tmp });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "f64.sub" });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.eq" });
        return { kind: "i32" };
      }
      if (method === "isSafeInteger" && expr.arguments.length >= 1) {
        compileExpression2(ctx, fctx, expr.arguments[0], { kind: "f64" });
        const tmp = allocLocal(fctx, `__issafe_${fctx.locals.length}`, {
          kind: "f64",
        });
        fctx.body.push({ op: "local.tee", index: tmp });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "f64.trunc" });
        fctx.body.push({ op: "f64.eq" });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "f64.sub" });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.eq" });
        fctx.body.push({ op: "i32.and" });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "f64.abs" });
        fctx.body.push({ op: "f64.const", value: Number.MAX_SAFE_INTEGER });
        fctx.body.push({ op: "f64.le" });
        fctx.body.push({ op: "i32.and" });
        return { kind: "i32" };
      }
      if ((method === "parseFloat" || method === "parseInt") && expr.arguments.length >= 1) {
        const funcIdx = ctx.funcMap.get(method === "parseFloat" ? "parseFloat" : "parseInt");
        if (funcIdx !== void 0) {
          compileExpression2(ctx, fctx, expr.arguments[0], {
            kind: "externref",
          });
          if (method === "parseInt") {
            if (expr.arguments.length >= 2) {
              compileExpression2(ctx, fctx, expr.arguments[1], { kind: "f64" });
            } else {
              fctx.body.push({ op: "f64.const", value: NaN });
            }
          }
          fctx.body.push({ op: "call", funcIdx });
          return { kind: "f64" };
        }
      }
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Array" &&
      propAccess.name.text === "isArray" &&
      expr.arguments.length >= 1
    ) {
      const argTsType = ctx.checker.getTypeAtLocation(expr.arguments[0]);
      const argWasmType = resolveWasmType(ctx, argTsType);
      const isArr = argWasmType.kind === "ref" || argWasmType.kind === "ref_null";
      const argSideType = compileExpression2(ctx, fctx, expr.arguments[0]);
      if (argSideType) fctx.body.push({ op: "drop" });
      fctx.body.push({ op: "i32.const", value: isArr ? 1 : 0 });
      return { kind: "i32" };
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "String" &&
      propAccess.name.text === "fromCharCode" &&
      expr.arguments.length >= 1
    ) {
      const funcIdx = ctx.funcMap.get("String_fromCharCode");
      if (funcIdx !== void 0) {
        const argType = compileExpression2(ctx, fctx, expr.arguments[0], {
          kind: "f64",
        });
        if (argType && argType.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
        }
        fctx.body.push({ op: "call", funcIdx });
        if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
          const fromExternIdx = ctx.nativeStrHelpers.get("__str_from_extern");
          if (fromExternIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: fromExternIdx });
          }
          return nativeStringType(ctx);
        }
        return { kind: "externref" };
      }
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Array" &&
      propAccess.name.text === "from" &&
      expr.arguments.length >= 1
    ) {
      const argTsType = ctx.checker.getTypeAtLocation(expr.arguments[0]);
      const argWasmType = resolveWasmType(ctx, argTsType);
      if (argWasmType.kind === "ref" || argWasmType.kind === "ref_null") {
        const arrInfo = resolveArrayInfo(ctx, argTsType);
        if (arrInfo) {
          const { vecTypeIdx, arrTypeIdx, elemType } = arrInfo;
          compileExpression2(ctx, fctx, expr.arguments[0]);
          const srcVec = allocLocal(fctx, `__arrfrom_src_${fctx.locals.length}`, {
            kind: "ref_null",
            typeIdx: vecTypeIdx,
          });
          const srcData = allocLocal(fctx, `__arrfrom_sdata_${fctx.locals.length}`, {
            kind: "ref_null",
            typeIdx: arrTypeIdx,
          });
          const lenTmp = allocLocal(fctx, `__arrfrom_len_${fctx.locals.length}`, { kind: "i32" });
          const dstData = allocLocal(fctx, `__arrfrom_ddata_${fctx.locals.length}`, {
            kind: "ref_null",
            typeIdx: arrTypeIdx,
          });
          fctx.body.push({ op: "local.set", index: srcVec });
          fctx.body.push({ op: "local.get", index: srcVec });
          fctx.body.push({
            op: "struct.get",
            typeIdx: vecTypeIdx,
            fieldIdx: 0,
          });
          fctx.body.push({ op: "local.set", index: lenTmp });
          fctx.body.push({ op: "local.get", index: srcVec });
          fctx.body.push({
            op: "struct.get",
            typeIdx: vecTypeIdx,
            fieldIdx: 1,
          });
          fctx.body.push({ op: "local.set", index: srcData });
          const defaultVal =
            elemType.kind === "f64"
              ? { op: "f64.const", value: 0 }
              : elemType.kind === "i32"
                ? { op: "i32.const", value: 0 }
                : { op: "ref.null", typeIdx: elemType.typeIdx ?? -1 };
          fctx.body.push(defaultVal);
          fctx.body.push({ op: "local.get", index: lenTmp });
          fctx.body.push({ op: "array.new", typeIdx: arrTypeIdx });
          fctx.body.push({ op: "local.set", index: dstData });
          fctx.body.push({ op: "local.get", index: dstData });
          fctx.body.push({ op: "i32.const", value: 0 });
          fctx.body.push({ op: "local.get", index: srcData });
          fctx.body.push({ op: "i32.const", value: 0 });
          fctx.body.push({ op: "local.get", index: lenTmp });
          fctx.body.push({
            op: "array.copy",
            dstTypeIdx: arrTypeIdx,
            srcTypeIdx: arrTypeIdx,
          });
          fctx.body.push({ op: "local.get", index: lenTmp });
          fctx.body.push({ op: "local.get", index: dstData });
          fctx.body.push({ op: "ref.as_non_null" });
          fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
          return { kind: "ref", typeIdx: vecTypeIdx };
        }
      }
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      (propAccess.name.text === "keys" || propAccess.name.text === "values" || propAccess.name.text === "entries") &&
      expr.arguments.length === 1
    ) {
      return compileObjectKeysOrValues(ctx, fctx, propAccess.name.text, expr);
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      (propAccess.name.text === "freeze" ||
        propAccess.name.text === "seal" ||
        propAccess.name.text === "preventExtensions") &&
      expr.arguments.length >= 1
    ) {
      const method = propAccess.name.text;
      const arg0 = expr.arguments[0];
      if (ts13.isIdentifier(arg0)) {
        ctx.nonExtensibleVars.add(arg0.text);
        if (method === "freeze") {
          ctx.frozenVars.add(arg0.text);
          ctx.sealedVars.add(arg0.text);
        } else if (method === "seal") {
          ctx.sealedVars.add(arg0.text);
        }
      }
      const argType = compileExpression2(ctx, fctx, expr.arguments[0]);
      if (!argType) return null;
      if (argType.kind === "externref") {
        const objLocal = allocLocal(fctx, `__freeze_obj_${fctx.locals.length}`, { kind: "externref" });
        fctx.body.push({ op: "local.set", index: objLocal });
        const importName =
          method === "freeze" ? "__object_freeze" : method === "seal" ? "__object_seal" : "__object_preventExtensions";
        const hostIdx = ensureLateImport2(ctx, importName, [{ kind: "externref" }], [{ kind: "externref" }]);
        flushLateImportShifts2(ctx, fctx);
        if (hostIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: objLocal });
          fctx.body.push({ op: "call", funcIdx: hostIdx });
          return { kind: "externref" };
        }
        fctx.body.push({ op: "local.get", index: objLocal });
        return { kind: "externref" };
      }
      return argType;
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      (propAccess.name.text === "isFrozen" || propAccess.name.text === "isSealed") &&
      expr.arguments.length >= 1
    ) {
      const arg0 = expr.arguments[0];
      const argType = compileExpression2(ctx, fctx, arg0);
      if (argType) {
        fctx.body.push({ op: "drop" });
      }
      let result = 0;
      if (ts13.isIdentifier(arg0)) {
        if (propAccess.name.text === "isFrozen" && ctx.frozenVars.has(arg0.text)) {
          result = 1;
        } else if (propAccess.name.text === "isSealed" && ctx.sealedVars.has(arg0.text)) {
          result = 1;
        }
      }
      fctx.body.push({ op: "i32.const", value: result });
      return { kind: "i32" };
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      propAccess.name.text === "isExtensible" &&
      expr.arguments.length >= 1
    ) {
      const arg0 = expr.arguments[0];
      const argType = compileExpression2(ctx, fctx, arg0);
      if (argType) {
        fctx.body.push({ op: "drop" });
      }
      let result = 1;
      if (ts13.isIdentifier(arg0) && ctx.nonExtensibleVars.has(arg0.text)) {
        result = 0;
      }
      fctx.body.push({ op: "i32.const", value: result });
      return { kind: "i32" };
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      propAccess.name.text === "setPrototypeOf" &&
      expr.arguments.length >= 2
    ) {
      const objType = compileExpression2(ctx, fctx, expr.arguments[0]);
      const protoType = compileExpression2(ctx, fctx, expr.arguments[1]);
      if (protoType) {
        fctx.body.push({ op: "drop" });
      }
      return objType;
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      propAccess.name.text === "getPrototypeOf" &&
      expr.arguments.length >= 1
    ) {
      const arg0 = expr.arguments[0];
      if (
        ts13.isPropertyAccessExpression(arg0) &&
        ts13.isIdentifier(arg0.expression) &&
        arg0.name.text === "prototype" &&
        ctx.classSet.has(arg0.expression.text)
      ) {
        const childClassName = arg0.expression.text;
        const parentClassName = ctx.classParentMap.get(childClassName);
        if (parentClassName && emitLazyProtoGet(ctx, fctx, parentClassName)) {
          return { kind: "externref" };
        }
        fctx.body.push({ op: "ref.null.extern" });
        return { kind: "externref" };
      }
      const argTsType = ctx.checker.getTypeAtLocation(arg0);
      const className = resolveStructName(ctx, argTsType);
      if (className && ctx.classSet.has(className)) {
        const argType2 = compileExpression2(ctx, fctx, arg0);
        if (argType2) {
          fctx.body.push({ op: "drop" });
        }
        if (emitLazyProtoGet(ctx, fctx, className)) {
          return { kind: "externref" };
        }
      }
      const argType = compileExpression2(ctx, fctx, arg0);
      if (argType) {
        fctx.body.push({ op: "drop" });
      }
      fctx.body.push({ op: "ref.null.extern" });
      return { kind: "externref" };
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      propAccess.name.text === "create" &&
      expr.arguments.length >= 1
    ) {
      const arg0 = expr.arguments[0];
      if (
        ts13.isPropertyAccessExpression(arg0) &&
        ts13.isIdentifier(arg0.expression) &&
        arg0.name.text === "prototype"
      ) {
        const protoClassName = arg0.expression.text;
        if (ctx.classSet.has(protoClassName)) {
          const structTypeIdx = ctx.structMap.get(protoClassName);
          const fields = ctx.structFields.get(protoClassName);
          if (structTypeIdx !== void 0 && fields) {
            for (const field of fields) {
              pushDefaultValue(fctx, field.type, ctx);
            }
            fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
            return { kind: "ref", typeIdx: structTypeIdx };
          }
        }
      }
      const hostIdx = ensureLateImport2(ctx, "__object_create", [{ kind: "externref" }], [{ kind: "externref" }]);
      flushLateImportShifts2(ctx, fctx);
      if (hostIdx !== void 0) {
        if (arg0.kind === ts13.SyntaxKind.NullKeyword) {
          fctx.body.push({ op: "ref.null.extern" });
        } else {
          const argType = compileExpression2(ctx, fctx, arg0);
          if (!argType) {
            fctx.body.push({ op: "ref.null.extern" });
          } else if (argType.kind !== "externref") {
            fctx.body.push({ op: "extern.convert_any" });
          }
        }
        fctx.body.push({ op: "call", funcIdx: hostIdx });
        if (expr.arguments.length >= 2 && ts13.isObjectLiteralExpression(expr.arguments[1])) {
          const descsLiteral = expr.arguments[1];
          const objLocal = allocLocal(fctx, `__ocreate_obj_${fctx.locals.length}`, { kind: "externref" });
          fctx.body.push({ op: "local.set", index: objLocal });
          for (const prop of descsLiteral.properties) {
            if (!ts13.isPropertyAssignment(prop)) continue;
            const propName = ts13.isIdentifier(prop.name)
              ? prop.name.text
              : ts13.isStringLiteral(prop.name)
                ? prop.name.text
                : ts13.isNumericLiteral(prop.name)
                  ? prop.name.text
                  : void 0;
            if (propName === void 0) continue;
            let valueExpr;
            let getExpr;
            let setExpr;
            let descWritable;
            let descEnumerable;
            let descConfigurable;
            if (ts13.isObjectLiteralExpression(prop.initializer)) {
              for (const dp of prop.initializer.properties) {
                if (ts13.isPropertyAssignment(dp) && ts13.isIdentifier(dp.name)) {
                  if (dp.name.text === "value") valueExpr = dp.initializer;
                  if (dp.name.text === "get") getExpr = dp.initializer;
                  if (dp.name.text === "set") setExpr = dp.initializer;
                  if (dp.name.text === "writable") {
                    descWritable = dp.initializer.kind === ts13.SyntaxKind.TrueKeyword;
                  }
                  if (dp.name.text === "enumerable") {
                    descEnumerable = dp.initializer.kind === ts13.SyntaxKind.TrueKeyword;
                  }
                  if (dp.name.text === "configurable") {
                    descConfigurable = dp.initializer.kind === ts13.SyntaxKind.TrueKeyword;
                  }
                }
              }
            }
            const dpIdx = ensureLateImport2(
              ctx,
              "__defineProperty_value",
              [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }, { kind: "i32" }],
              [{ kind: "externref" }],
            );
            flushLateImportShifts2(ctx, fctx);
            if (dpIdx !== void 0) {
              fctx.body.push({ op: "local.get", index: objLocal });
              addStringConstantGlobal(ctx, propName);
              const strGlobalIdx = ctx.stringGlobalMap.get(propName);
              if (strGlobalIdx !== void 0) {
                fctx.body.push({ op: "global.get", index: strGlobalIdx });
              } else {
                fctx.body.push({ op: "ref.null.extern" });
              }
              if (valueExpr) {
                const vt = compileExpression2(ctx, fctx, valueExpr);
                if (!vt) {
                  fctx.body.push({ op: "ref.null.extern" });
                } else if (vt.kind !== "externref") {
                  coerceType3(ctx, fctx, vt, { kind: "externref" });
                }
              } else {
                fctx.body.push({ op: "ref.null.extern" });
              }
              let flags = 0;
              if (descWritable) flags |= 1;
              if (descEnumerable) flags |= 2;
              if (descConfigurable) flags |= 4;
              if (descWritable !== void 0) flags |= 8;
              if (descEnumerable !== void 0) flags |= 16;
              if (descConfigurable !== void 0) flags |= 32;
              if (valueExpr) flags |= 128;
              if (getExpr || setExpr) flags |= 64;
              fctx.body.push({ op: "i32.const", value: flags });
              fctx.body.push({ op: "call", funcIdx: dpIdx });
              fctx.body.push({ op: "drop" });
            }
          }
          fctx.body.push({ op: "local.get", index: objLocal });
        } else if (expr.arguments.length >= 2) {
          const objLocal = allocLocal(fctx, `__ocreate_obj_${fctx.locals.length}`, { kind: "externref" });
          fctx.body.push({ op: "local.set", index: objLocal });
          const dpIdx = ensureLateImport2(
            ctx,
            "__defineProperties",
            [{ kind: "externref" }, { kind: "externref" }],
            [{ kind: "externref" }],
          );
          flushLateImportShifts2(ctx, fctx);
          if (dpIdx !== void 0) {
            fctx.body.push({ op: "local.get", index: objLocal });
            const descType = compileExpression2(ctx, fctx, expr.arguments[1]);
            if (!descType) {
              fctx.body.push({ op: "ref.null.extern" });
            } else if (descType.kind !== "externref") {
              fctx.body.push({ op: "extern.convert_any" });
            }
            fctx.body.push({ op: "call", funcIdx: dpIdx });
          } else {
            fctx.body.push({ op: "local.get", index: objLocal });
          }
        }
        return { kind: "externref" };
      }
      if (arg0.kind === ts13.SyntaxKind.NullKeyword) {
        fctx.body.push({ op: "ref.null.extern" });
      } else {
        const argType = compileExpression2(ctx, fctx, arg0);
        if (argType) {
          fctx.body.push({ op: "drop" });
        }
        fctx.body.push({ op: "ref.null.extern" });
      }
      return { kind: "externref" };
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      propAccess.name.text === "defineProperty" &&
      expr.arguments.length >= 3
    ) {
      return compileObjectDefineProperty(ctx, fctx, expr);
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      propAccess.name.text === "defineProperties" &&
      expr.arguments.length >= 2
    ) {
      return compileObjectDefineProperties(ctx, fctx, expr);
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Object" &&
      propAccess.name.text === "getOwnPropertyDescriptor" &&
      expr.arguments.length >= 2
    ) {
      const arg0 = expr.arguments[0];
      const arg1 = expr.arguments[1];
      const arg0TsType = ctx.checker.getTypeAtLocation(arg0);
      const structName = resolveStructName(ctx, arg0TsType);
      const propLiteral = ts13.isStringLiteral(arg1) ? arg1.text : void 0;
      if (structName && propLiteral !== void 0) {
        const structTypeIdx = ctx.structMap.get(structName);
        const fields = ctx.structFields.get(structName);
        if (structTypeIdx !== void 0 && fields) {
          const userFields = fields
            .map((f, idx) => ({ field: f, fieldIdx: idx }))
            .filter((e) => !e.field.name.startsWith("__"));
          const entry = userFields.find((e) => e.field.name === propLiteral);
          if (entry) {
            const flagsArr = ctx.shapePropFlags.get(structTypeIdx);
            const userFieldIdx = userFields.indexOf(entry);
            const flags = flagsArr && userFieldIdx >= 0 ? flagsArr[userFieldIdx] : 7;
            const objType2 = compileExpression2(ctx, fctx, arg0);
            if (!objType2) {
              fctx.body.push({ op: "ref.null.extern" });
              return { kind: "externref" };
            }
            {
              let needsCast = false;
              if (objType2.kind === "externref") {
                fctx.body.push({ op: "any.convert_extern" });
                needsCast = true;
              } else if (objType2.kind === "ref_null" && objType2.typeIdx !== structTypeIdx) {
                needsCast = true;
              }
              if (needsCast) {
                const gopdTmp = allocLocal(fctx, `__gopd_tmp_${fctx.locals.length}`, { kind: "anyref" });
                fctx.body.push({ op: "local.set", index: gopdTmp });
                fctx.body.push({ op: "local.get", index: gopdTmp });
                fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx });
                fctx.body.push({
                  op: "if",
                  blockType: { kind: "val", type: { kind: "externref" } },
                  then: (() => {
                    const thenInstrs = [
                      { op: "local.get", index: gopdTmp },
                      { op: "ref.cast", typeIdx: structTypeIdx },
                      { op: "struct.get", typeIdx: structTypeIdx, fieldIdx: entry.fieldIdx },
                    ];
                    const ft = entry.field.type;
                    if (ft.kind === "f64") {
                      const boxIdx2 = ensureLateImport2(
                        ctx,
                        "__box_number",
                        [{ kind: "f64" }],
                        [{ kind: "externref" }],
                      );
                      flushLateImportShifts2(ctx, fctx);
                      if (boxIdx2 !== void 0) thenInstrs.push({ op: "call", funcIdx: boxIdx2 });
                    } else if (ft.kind === "i32") {
                      thenInstrs.push({ op: "f64.convert_i32_s" });
                      const boxIdx2 = ensureLateImport2(
                        ctx,
                        "__box_number",
                        [{ kind: "f64" }],
                        [{ kind: "externref" }],
                      );
                      flushLateImportShifts2(ctx, fctx);
                      if (boxIdx2 !== void 0) thenInstrs.push({ op: "call", funcIdx: boxIdx2 });
                    } else if (ft.kind === "ref" || ft.kind === "ref_null") {
                      thenInstrs.push({ op: "extern.convert_any" });
                    } else if (ft.kind !== "externref") {
                      thenInstrs.push({ op: "extern.convert_any" });
                    }
                    thenInstrs.push({ op: "i32.const", value: flags });
                    const createIdx2 = ensureLateImport2(
                      ctx,
                      "__create_descriptor",
                      [{ kind: "externref" }, { kind: "i32" }],
                      [{ kind: "externref" }],
                    );
                    flushLateImportShifts2(ctx, fctx);
                    if (createIdx2 !== void 0) thenInstrs.push({ op: "call", funcIdx: createIdx2 });
                    return thenInstrs;
                  })(),
                  else: [
                    // Cast would fail — return undefined (property not own)
                    { op: "ref.null.extern" },
                  ],
                });
                return { kind: "externref" };
              }
            }
            const objLocal = allocLocal(fctx, `__gopd_obj_${fctx.locals.length}`, {
              kind: "ref",
              typeIdx: structTypeIdx,
            });
            fctx.body.push({ op: "local.set", index: objLocal });
            fctx.body.push({ op: "local.get", index: objLocal });
            fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: entry.fieldIdx });
            const fieldType = entry.field.type;
            if (fieldType.kind === "f64") {
              const boxIdx = ensureLateImport2(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
              flushLateImportShifts2(ctx, fctx);
              if (boxIdx !== void 0) {
                fctx.body.push({ op: "call", funcIdx: boxIdx });
              }
            } else if (fieldType.kind === "i32") {
              fctx.body.push({ op: "f64.convert_i32_s" });
              const boxIdx = ensureLateImport2(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
              flushLateImportShifts2(ctx, fctx);
              if (boxIdx !== void 0) {
                fctx.body.push({ op: "call", funcIdx: boxIdx });
              }
            } else if (fieldType.kind === "i64") {
              fctx.body.push({ op: "f64.convert_i64_s" });
              const boxIdx = ensureLateImport2(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
              flushLateImportShifts2(ctx, fctx);
              if (boxIdx !== void 0) {
                fctx.body.push({ op: "call", funcIdx: boxIdx });
              }
            } else if (fieldType.kind === "ref" || fieldType.kind === "ref_null") {
              fctx.body.push({ op: "extern.convert_any" });
            } else if (fieldType.kind !== "externref") {
              fctx.body.push({ op: "extern.convert_any" });
            }
            fctx.body.push({ op: "i32.const", value: flags });
            const createIdx = ensureLateImport2(
              ctx,
              "__create_descriptor",
              [{ kind: "externref" }, { kind: "i32" }],
              [{ kind: "externref" }],
            );
            flushLateImportShifts2(ctx, fctx);
            if (createIdx !== void 0) {
              fctx.body.push({ op: "call", funcIdx: createIdx });
            }
            return { kind: "externref" };
          }
          const argResult = compileExpression2(ctx, fctx, arg0);
          if (argResult) fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "ref.null.extern" });
          return { kind: "externref" };
        }
      }
      const objType = compileExpression2(ctx, fctx, arg0, { kind: "externref" });
      if (!objType) {
        fctx.body.push({ op: "ref.null.extern" });
        return { kind: "externref" };
      }
      if (objType.kind !== "externref") {
        coerceType3(ctx, fctx, objType, { kind: "externref" });
      }
      const propType = compileExpression2(ctx, fctx, arg1, { kind: "externref" });
      if (!propType) {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "ref.null.extern" });
        return { kind: "externref" };
      }
      if (propType.kind !== "externref") {
        coerceType3(ctx, fctx, propType, { kind: "externref" });
      }
      const funcIdx = ensureLateImport2(
        ctx,
        "__getOwnPropertyDescriptor",
        [{ kind: "externref" }, { kind: "externref" }],
        [{ kind: "externref" }],
      );
      flushLateImportShifts2(ctx, fctx);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
      }
      return { kind: "externref" };
    }
    if (ts13.isIdentifier(propAccess.expression) && propAccess.expression.text === "Reflect") {
      const reflectMethod = propAccess.name.text;
      if (reflectMethod === "get" && expr.arguments.length >= 2) {
        const syntheticElemAccess = ts13.factory.createElementAccessExpression(expr.arguments[0], expr.arguments[1]);
        ts13.setTextRange(syntheticElemAccess, expr);
        syntheticElemAccess.parent = expr.parent;
        return compileExpression2(ctx, fctx, syntheticElemAccess);
      }
      if (reflectMethod === "set" && expr.arguments.length >= 3) {
        const syntheticElemAccess = ts13.factory.createElementAccessExpression(expr.arguments[0], expr.arguments[1]);
        const syntheticAssign = ts13.factory.createBinaryExpression(
          syntheticElemAccess,
          ts13.factory.createToken(ts13.SyntaxKind.EqualsToken),
          expr.arguments[2],
        );
        ts13.setTextRange(syntheticAssign, expr);
        syntheticAssign.parent = expr.parent;
        const assignType = compileExpression2(ctx, fctx, syntheticAssign);
        if (assignType) {
          fctx.body.push({ op: "drop" });
        }
        fctx.body.push({ op: "i32.const", value: 1 });
        return { kind: "i32" };
      }
      if (reflectMethod === "has" && expr.arguments.length >= 2) {
        const syntheticIn = ts13.factory.createBinaryExpression(
          expr.arguments[1],
          ts13.factory.createToken(ts13.SyntaxKind.InKeyword),
          expr.arguments[0],
        );
        ts13.setTextRange(syntheticIn, expr);
        syntheticIn.parent = expr.parent;
        return compileExpression2(ctx, fctx, syntheticIn);
      }
      if (reflectMethod === "apply" && expr.arguments.length >= 3) {
        const syntheticPropAccess = ts13.factory.createPropertyAccessExpression(expr.arguments[0], "apply");
        const syntheticCall = ts13.factory.createCallExpression(syntheticPropAccess, void 0, [
          expr.arguments[1],
          expr.arguments[2],
        ]);
        ts13.setTextRange(syntheticCall, expr);
        syntheticCall.parent = expr.parent;
        return compileCallExpression(ctx, fctx, syntheticCall);
      }
      if (reflectMethod === "construct" && expr.arguments.length >= 2) {
        const ctorExpr = expr.arguments[0];
        const argsExpr = expr.arguments[1];
        let newArgs;
        if (ts13.isArrayLiteralExpression(argsExpr)) {
          newArgs = argsExpr.elements;
        } else {
          newArgs = [argsExpr];
        }
        const syntheticNew = ts13.factory.createNewExpression(ctorExpr, void 0, newArgs);
        ts13.setTextRange(syntheticNew, expr);
        syntheticNew.parent = expr.parent;
        return compileExpression2(ctx, fctx, syntheticNew);
      }
      if (reflectMethod === "ownKeys" && expr.arguments.length >= 1) {
        const syntheticPropAccess = ts13.factory.createPropertyAccessExpression(
          ts13.factory.createIdentifier("Object"),
          "keys",
        );
        const syntheticCall = ts13.factory.createCallExpression(syntheticPropAccess, void 0, [expr.arguments[0]]);
        ts13.setTextRange(syntheticCall, expr);
        syntheticCall.parent = expr.parent;
        return compileCallExpression(ctx, fctx, syntheticCall);
      }
      if (reflectMethod === "defineProperty" && expr.arguments.length >= 3) {
        const syntheticPropAccess = ts13.factory.createPropertyAccessExpression(
          ts13.factory.createIdentifier("Object"),
          "defineProperty",
        );
        const syntheticCall = ts13.factory.createCallExpression(
          syntheticPropAccess,
          void 0,
          Array.from(expr.arguments),
        );
        ts13.setTextRange(syntheticCall, expr);
        syntheticCall.parent = expr.parent;
        const resultType = compileCallExpression(ctx, fctx, syntheticCall);
        if (resultType) {
          fctx.body.push({ op: "drop" });
        }
        fctx.body.push({ op: "i32.const", value: 1 });
        return { kind: "i32" };
      }
      if (reflectMethod === "getPrototypeOf" && expr.arguments.length >= 1) {
        const syntheticPropAccess = ts13.factory.createPropertyAccessExpression(
          ts13.factory.createIdentifier("Object"),
          "getPrototypeOf",
        );
        const syntheticCall = ts13.factory.createCallExpression(syntheticPropAccess, void 0, [expr.arguments[0]]);
        ts13.setTextRange(syntheticCall, expr);
        syntheticCall.parent = expr.parent;
        return compileCallExpression(ctx, fctx, syntheticCall);
      }
      if (reflectMethod === "setPrototypeOf" && expr.arguments.length >= 2) {
        const syntheticPropAccess = ts13.factory.createPropertyAccessExpression(
          ts13.factory.createIdentifier("Object"),
          "setPrototypeOf",
        );
        const syntheticCall = ts13.factory.createCallExpression(syntheticPropAccess, void 0, [
          expr.arguments[0],
          expr.arguments[1],
        ]);
        ts13.setTextRange(syntheticCall, expr);
        syntheticCall.parent = expr.parent;
        const resultType = compileCallExpression(ctx, fctx, syntheticCall);
        if (resultType) {
          fctx.body.push({ op: "drop" });
        }
        fctx.body.push({ op: "i32.const", value: 1 });
        return { kind: "i32" };
      }
      if (reflectMethod === "deleteProperty" && expr.arguments.length >= 2) {
        const syntheticElemAccess = ts13.factory.createElementAccessExpression(expr.arguments[0], expr.arguments[1]);
        const syntheticDelete = ts13.factory.createDeleteExpression(syntheticElemAccess);
        ts13.setTextRange(syntheticDelete, expr);
        syntheticDelete.parent = expr.parent;
        return compileExpression2(ctx, fctx, syntheticDelete);
      }
      if (reflectMethod === "isExtensible" && expr.arguments.length >= 1) {
        const arg0 = expr.arguments[0];
        const argType = compileExpression2(ctx, fctx, arg0);
        if (argType) fctx.body.push({ op: "drop" });
        let result = 1;
        if (ts13.isIdentifier(arg0) && ctx.nonExtensibleVars.has(arg0.text)) {
          result = 0;
        }
        fctx.body.push({ op: "i32.const", value: result });
        return { kind: "i32" };
      }
      if (reflectMethod === "preventExtensions" && expr.arguments.length >= 1) {
        const arg0 = expr.arguments[0];
        if (ts13.isIdentifier(arg0)) {
          ctx.nonExtensibleVars.add(arg0.text);
        }
        const argType = compileExpression2(ctx, fctx, arg0);
        if (argType) fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "i32.const", value: 1 });
        return { kind: "i32" };
      }
      if (reflectMethod === "getOwnPropertyDescriptor" && expr.arguments.length >= 2) {
        const syntheticPropAccess = ts13.factory.createPropertyAccessExpression(
          ts13.factory.createIdentifier("Object"),
          "getOwnPropertyDescriptor",
        );
        const syntheticCall = ts13.factory.createCallExpression(syntheticPropAccess, void 0, [
          expr.arguments[0],
          expr.arguments[1],
        ]);
        ts13.setTextRange(syntheticCall, expr);
        syntheticCall.parent = expr.parent;
        return compileCallExpression(ctx, fctx, syntheticCall);
      }
    }
    if (
      ts13.isIdentifier(propAccess.expression) &&
      propAccess.expression.text === "Promise" &&
      (propAccess.name.text === "all" ||
        propAccess.name.text === "race" ||
        propAccess.name.text === "allSettled" ||
        propAccess.name.text === "any" ||
        propAccess.name.text === "resolve" ||
        propAccess.name.text === "reject")
    ) {
      const methodName = propAccess.name.text;
      const importName = `Promise_${methodName}`;
      const funcIdx =
        ctx.funcMap.get(importName) ??
        ensureLateImport2(ctx, importName, [{ kind: "externref" }], [{ kind: "externref" }]);
      flushLateImportShifts2(ctx, fctx);
      if (funcIdx !== void 0) {
        if (expr.arguments.length >= 1) {
          compileExpression2(ctx, fctx, expr.arguments[0], {
            kind: "externref",
          });
        } else {
          fctx.body.push({ op: "ref.null.extern" });
        }
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "externref" };
      }
    }
    if (ts13.isIdentifier(propAccess.expression) && propAccess.expression.text === "JSON") {
      const method = propAccess.name.text;
      if ((method === "stringify" || method === "parse") && expr.arguments.length >= 1) {
        const importName = `JSON_${method}`;
        const funcIdx = ctx.funcMap.get(importName);
        if (funcIdx !== void 0) {
          const argType = compileExpression2(ctx, fctx, expr.arguments[0]);
          if (argType && argType.kind !== "externref") {
            coerceType3(ctx, fctx, argType, { kind: "externref" });
          }
          if (method === "stringify") {
            if (expr.arguments.length >= 2) {
              const repType = compileExpression2(ctx, fctx, expr.arguments[1]);
              if (repType && repType.kind !== "externref") {
                coerceType3(ctx, fctx, repType, { kind: "externref" });
              }
            } else {
              fctx.body.push({ op: "ref.null.extern" });
            }
            if (expr.arguments.length >= 3) {
              const spType = compileExpression2(ctx, fctx, expr.arguments[2]);
              if (spType && spType.kind !== "externref") {
                coerceType3(ctx, fctx, spType, { kind: "externref" });
              }
            } else {
              fctx.body.push({ op: "ref.null.extern" });
            }
          }
          fctx.body.push({ op: "call", funcIdx });
          return { kind: "externref" };
        }
      }
    }
    if (ts13.isIdentifier(propAccess.expression) && propAccess.expression.text === "Date") {
      const method = propAccess.name.text;
      if (method === "now") {
        const dateNowIdx = ensureLateImport2(ctx, "__date_now", [], [{ kind: "f64" }]);
        if (dateNowIdx !== void 0) {
          flushLateImportShifts2(ctx, fctx);
          fctx.body.push({ op: "call", funcIdx: dateNowIdx });
        } else {
          fctx.body.push({ op: "f64.const", value: 0 });
        }
        return { kind: "f64" };
      }
      if (method === "UTC") {
        const daysFromCivilIdx = ensureDateDaysFromCivilHelper(ctx);
        const args = expr.arguments;
        if (args.length >= 1) {
          compileExpression2(ctx, fctx, args[0], { kind: "f64" });
        } else {
          fctx.body.push({ op: "f64.const", value: 1970 });
        }
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
        const yearL = allocTempLocal(fctx, { kind: "i64" });
        fctx.body.push({ op: "local.set", index: yearL });
        if (args.length >= 2) {
          compileExpression2(ctx, fctx, args[1], { kind: "f64" });
          fctx.body.push({ op: "i64.trunc_sat_f64_s" });
          fctx.body.push({ op: "i64.const", value: 1n });
          fctx.body.push({ op: "i64.add" });
        } else {
          fctx.body.push({ op: "i64.const", value: 1n });
        }
        const monthL = allocTempLocal(fctx, { kind: "i64" });
        fctx.body.push({ op: "local.set", index: monthL });
        if (args.length >= 3) {
          compileExpression2(ctx, fctx, args[2], { kind: "f64" });
          fctx.body.push({ op: "i64.trunc_sat_f64_s" });
        } else {
          fctx.body.push({ op: "i64.const", value: 1n });
        }
        const dayL = allocTempLocal(fctx, { kind: "i64" });
        fctx.body.push({ op: "local.set", index: dayL });
        if (args.length >= 4) {
          compileExpression2(ctx, fctx, args[3], { kind: "f64" });
          fctx.body.push({ op: "i64.trunc_sat_f64_s" });
        } else {
          fctx.body.push({ op: "i64.const", value: 0n });
        }
        const hoursL = allocTempLocal(fctx, { kind: "i64" });
        fctx.body.push({ op: "local.set", index: hoursL });
        if (args.length >= 5) {
          compileExpression2(ctx, fctx, args[4], { kind: "f64" });
          fctx.body.push({ op: "i64.trunc_sat_f64_s" });
        } else {
          fctx.body.push({ op: "i64.const", value: 0n });
        }
        const minutesL = allocTempLocal(fctx, { kind: "i64" });
        fctx.body.push({ op: "local.set", index: minutesL });
        if (args.length >= 6) {
          compileExpression2(ctx, fctx, args[5], { kind: "f64" });
          fctx.body.push({ op: "i64.trunc_sat_f64_s" });
        } else {
          fctx.body.push({ op: "i64.const", value: 0n });
        }
        const secondsL = allocTempLocal(fctx, { kind: "i64" });
        fctx.body.push({ op: "local.set", index: secondsL });
        if (args.length >= 7) {
          compileExpression2(ctx, fctx, args[6], { kind: "f64" });
          fctx.body.push({ op: "i64.trunc_sat_f64_s" });
        } else {
          fctx.body.push({ op: "i64.const", value: 0n });
        }
        const msL = allocTempLocal(fctx, { kind: "i64" });
        fctx.body.push({ op: "local.set", index: msL });
        fctx.body.push(
          { op: "local.get", index: yearL },
          { op: "local.get", index: monthL },
          { op: "local.get", index: dayL },
          { op: "call", funcIdx: daysFromCivilIdx },
          { op: "i64.const", value: 86400000n },
          { op: "i64.mul" },
          { op: "local.get", index: hoursL },
          { op: "i64.const", value: 3600000n },
          { op: "i64.mul" },
          { op: "i64.add" },
          { op: "local.get", index: minutesL },
          { op: "i64.const", value: 60000n },
          { op: "i64.mul" },
          { op: "i64.add" },
          { op: "local.get", index: secondsL },
          { op: "i64.const", value: 1000n },
          { op: "i64.mul" },
          { op: "i64.add" },
          { op: "local.get", index: msL },
          { op: "i64.add" },
          { op: "f64.convert_i64_s" },
        );
        releaseTempLocal(fctx, msL);
        releaseTempLocal(fctx, secondsL);
        releaseTempLocal(fctx, minutesL);
        releaseTempLocal(fctx, hoursL);
        releaseTempLocal(fctx, dayL);
        releaseTempLocal(fctx, monthL);
        releaseTempLocal(fctx, yearL);
        return { kind: "f64" };
      }
      if (method === "parse") {
        for (const arg of expr.arguments) {
          const t = compileExpression2(ctx, fctx, arg);
          if (t) fctx.body.push({ op: "drop" });
        }
        fctx.body.push({ op: "f64.const", value: NaN });
        return { kind: "f64" };
      }
    }
    if (ts13.isIdentifier(propAccess.expression) && ctx.classSet.has(propAccess.expression.text)) {
      const clsName = propAccess.expression.text;
      const methodName = propAccess.name.text;
      const fullName = `${clsName}_${methodName}`;
      if (ctx.staticMethodSet.has(fullName)) {
        const funcIdx = ctx.funcMap.get(fullName);
        if (funcIdx !== void 0) {
          const paramTypes = getFuncParamTypes(ctx, funcIdx);
          const staticParamCount = paramTypes ? paramTypes.length : expr.arguments.length;
          for (let i = 0; i < expr.arguments.length; i++) {
            if (i < staticParamCount) {
              compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i]);
            } else {
              const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
              if (extraType !== null) {
                fctx.body.push({ op: "drop" });
              }
            }
          }
          if (paramTypes) {
            for (let i = expr.arguments.length; i < paramTypes.length; i++) {
              pushDefaultValue(fctx, paramTypes[i], ctx);
            }
          }
          const finalStaticIdx = ctx.funcMap.get(fullName) ?? funcIdx;
          fctx.body.push({ op: "call", funcIdx: finalStaticIdx });
          const sig = ctx.checker.getResolvedSignature(expr);
          if (sig) {
            const retType = ctx.checker.getReturnTypeOfSignature(sig);
            if (isEffectivelyVoidReturn(ctx, retType, fullName)) return VOID_RESULT;
            if (wasmFuncReturnsVoid(ctx, finalStaticIdx)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, finalStaticIdx) ?? resolveWasmType(ctx, retType);
          }
          return VOID_RESULT;
        }
      }
    }
    const receiverType = ctx.checker.getTypeAtLocation(propAccess.expression);
    {
      const dateResult = compileDateMethodCall(ctx, fctx, propAccess, expr, receiverType);
      if (dateResult !== void 0) return dateResult;
    }
    if (isExternalDeclaredClass(receiverType, ctx.checker)) {
      return compileExternMethodCall(ctx, fctx, propAccess, expr);
    }
    if (propAccess.name.text === "hasOwnProperty" || propAccess.name.text === "propertyIsEnumerable") {
      return compilePropertyIntrospection(ctx, fctx, propAccess, expr);
    }
    if (isGeneratorType(receiverType)) {
      const methodName = propAccess.name.text;
      if (methodName === "next") {
        compileExpression2(ctx, fctx, propAccess.expression);
        const funcIdx = ctx.funcMap.get("__gen_next");
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          return { kind: "externref" };
        }
      } else if (methodName === "return") {
        compileExpression2(ctx, fctx, propAccess.expression);
        if (expr.arguments.length > 0) {
          compileExpression2(ctx, fctx, expr.arguments[0], {
            kind: "externref",
          });
        } else {
          fctx.body.push({ op: "ref.null.extern" });
        }
        const funcIdx = ctx.funcMap.get("__gen_return");
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          return { kind: "externref" };
        }
      } else if (methodName === "throw") {
        compileExpression2(ctx, fctx, propAccess.expression);
        if (expr.arguments.length > 0) {
          compileExpression2(ctx, fctx, expr.arguments[0], {
            kind: "externref",
          });
        } else {
          fctx.body.push({ op: "ref.null.extern" });
        }
        const funcIdx = ctx.funcMap.get("__gen_throw");
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          return { kind: "externref" };
        }
      }
    }
    {
      const method = propAccess.name.text;
      if ((method === "then" || method === "catch" || method === "finally") && expr.arguments.length >= 1) {
        const useThen2 = method === "then" && expr.arguments.length >= 2;
        const importName = useThen2 ? "Promise_then2" : `Promise_${method}`;
        const funcIdx =
          ctx.funcMap.get(importName) ??
          ensureLateImport2(
            ctx,
            importName,
            useThen2
              ? [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }]
              : [{ kind: "externref" }, { kind: "externref" }],
            [{ kind: "externref" }],
          );
        flushLateImportShifts2(ctx, fctx);
        if (funcIdx !== void 0) {
          compileExpression2(ctx, fctx, propAccess.expression, {
            kind: "externref",
          });
          const cbType = compileExpression2(ctx, fctx, expr.arguments[0], {
            kind: "externref",
          });
          if (cbType && cbType.kind !== "externref") {
            coerceType3(ctx, fctx, cbType, { kind: "externref" });
          }
          if (useThen2) {
            const cb2Type = compileExpression2(ctx, fctx, expr.arguments[1], {
              kind: "externref",
            });
            if (cb2Type && cb2Type.kind !== "externref") {
              coerceType3(ctx, fctx, cb2Type, { kind: "externref" });
            }
          }
          const finalIdx = ctx.funcMap.get(importName) ?? funcIdx;
          fctx.body.push({ op: "call", funcIdx: finalIdx });
          return { kind: "externref" };
        }
      }
    }
    {
      const wrapperMethodName = propAccess.name.text;
      const recvSymName = receiverType.getSymbol()?.name;
      if (recvSymName === "Number" && wrapperMethodName === "valueOf") {
        compileExpression2(ctx, fctx, propAccess.expression, { kind: "f64" });
        return { kind: "f64" };
      }
      if (recvSymName === "String" && wrapperMethodName === "valueOf") {
        const strType = ctx.nativeStrings ? nativeStringType(ctx) : { kind: "externref" };
        compileExpression2(ctx, fctx, propAccess.expression, strType);
        return strType;
      }
      if (recvSymName === "Boolean" && wrapperMethodName === "valueOf") {
        compileExpression2(ctx, fctx, propAccess.expression, { kind: "i32" });
        return { kind: "i32" };
      }
    }
    let receiverClassName = receiverType.getSymbol()?.name;
    if (receiverClassName && !ctx.classSet.has(receiverClassName)) {
      receiverClassName = ctx.classExprNameMap.get(receiverClassName) ?? receiverClassName;
    }
    if (!receiverClassName || !ctx.classSet.has(receiverClassName)) {
      const methodName = ts13.isPrivateIdentifier(propAccess.name)
        ? "__priv_" + propAccess.name.text.slice(1)
        : propAccess.name.text;
      if (receiverType.isUnion()) {
        for (const memberType of receiverType.types) {
          let memberName = memberType.getSymbol()?.name;
          if (memberName && !ctx.classSet.has(memberName)) {
            memberName = ctx.classExprNameMap.get(memberName) ?? memberName;
          }
          if (memberName && ctx.classSet.has(memberName)) {
            const fullName = `${memberName}_${methodName}`;
            if (ctx.funcMap.has(fullName)) {
              receiverClassName = memberName;
              break;
            }
            let ancestor = ctx.classParentMap.get(memberName);
            while (ancestor) {
              if (ctx.funcMap.has(`${ancestor}_${methodName}`)) {
                receiverClassName = memberName;
                break;
              }
              ancestor = ctx.classParentMap.get(ancestor);
            }
            if (receiverClassName && ctx.classSet.has(receiverClassName)) break;
          }
        }
      }
      if (!receiverClassName || !ctx.classSet.has(receiverClassName)) {
        const apparentType = ctx.checker.getApparentType(receiverType);
        if (apparentType !== receiverType) {
          let apparentName = apparentType.getSymbol()?.name;
          if (apparentName && !ctx.classSet.has(apparentName)) {
            apparentName = ctx.classExprNameMap.get(apparentName) ?? apparentName;
          }
          if (apparentName && ctx.classSet.has(apparentName) && ctx.funcMap.has(`${apparentName}_${methodName}`)) {
            receiverClassName = apparentName;
          }
        }
      }
      if (!receiverClassName || !ctx.classSet.has(receiverClassName)) {
        const baseTypes = receiverType.getBaseTypes?.();
        if (baseTypes) {
          for (const baseType of baseTypes) {
            let baseName = baseType.getSymbol()?.name;
            if (baseName && !ctx.classSet.has(baseName)) {
              baseName = ctx.classExprNameMap.get(baseName) ?? baseName;
            }
            if (baseName && ctx.classSet.has(baseName) && ctx.funcMap.has(`${baseName}_${methodName}`)) {
              receiverClassName = baseName;
              break;
            }
          }
        }
      }
      if (!receiverClassName || !ctx.classSet.has(receiverClassName)) {
        const structName = resolveStructName(ctx, receiverType);
        if (structName && ctx.classSet.has(structName) && ctx.funcMap.has(`${structName}_${methodName}`)) {
          receiverClassName = structName;
        }
      }
      if (!receiverClassName || !ctx.classSet.has(receiverClassName)) {
        const recvProps = receiverType.getProperties?.() ?? [];
        const recvPropNames = new Set(recvProps.map((p) => p.name));
        for (const className of ctx.classSet) {
          if (!ctx.funcMap.has(`${className}_${methodName}`)) continue;
          const classFields = ctx.structFields.get(className);
          if (classFields && recvPropNames.size > 0) {
            const classFieldNames = new Set(classFields.map((f) => f.name));
            let compatible = true;
            for (const prop of recvPropNames) {
              const propSymbol = recvProps.find((p) => p.name === prop);
              const propType = propSymbol ? ctx.checker.getTypeOfSymbol(propSymbol) : void 0;
              const isMethod = propType && (propType.getCallSignatures?.()?.length ?? 0) > 0;
              if (!isMethod && !classFieldNames.has(prop)) {
                compatible = false;
                break;
              }
            }
            if (!compatible) continue;
          }
          receiverClassName = className;
          break;
        }
      }
    }
    if (receiverClassName && ctx.classSet.has(receiverClassName)) {
      const methodName = ts13.isPrivateIdentifier(propAccess.name)
        ? "__priv_" + propAccess.name.text.slice(1)
        : propAccess.name.text;
      let fullName = `${receiverClassName}_${methodName}`;
      let funcIdx = ctx.funcMap.get(fullName);
      if (funcIdx === void 0) {
        let ancestor = ctx.classParentMap.get(receiverClassName);
        while (ancestor && funcIdx === void 0) {
          fullName = `${ancestor}_${methodName}`;
          funcIdx = ctx.funcMap.get(fullName);
          ancestor = ctx.classParentMap.get(ancestor);
        }
      }
      if (funcIdx === void 0) {
        for (const [childClass, parentClass] of ctx.classParentMap) {
          if (parentClass === receiverClassName || parentClass === fullName.split("_")[0]) {
            const childFullName = `${childClass}_${methodName}`;
            const childFuncIdx = ctx.funcMap.get(childFullName);
            if (childFuncIdx !== void 0) {
              fullName = childFullName;
              funcIdx = childFuncIdx;
              break;
            }
          }
        }
      }
      if (funcIdx === void 0) {
        const callablePropResult = compileCallablePropertyCall(ctx, fctx, expr, propAccess, receiverClassName);
        if (callablePropResult !== void 0) return callablePropResult;
      }
      if (funcIdx === void 0) {
        const getterName = `${receiverClassName}_get_${methodName}`;
        const getterIdx = ctx.funcMap.get(getterName);
        if (getterIdx !== void 0) {
          const getterCallResult = compileGetterCallable(ctx, fctx, expr, propAccess, receiverClassName, getterIdx);
          if (getterCallResult !== void 0) return getterCallResult;
        }
      }
      if (funcIdx !== void 0) {
        const isStaticMethod = ctx.staticMethodSet.has(fullName);
        if (isStaticMethod) {
          const recvType2 = compileExpression2(ctx, fctx, propAccess.expression);
          if (recvType2 !== null) {
            fctx.body.push({ op: "drop" });
          }
          const paramTypes2 = getFuncParamTypes(ctx, funcIdx);
          const paramCount = paramTypes2 ? paramTypes2.length : expr.arguments.length;
          for (let i = 0; i < expr.arguments.length; i++) {
            if (i < paramCount) {
              compileExpression2(ctx, fctx, expr.arguments[i], paramTypes2?.[i]);
            } else {
              const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
              if (extraType !== null) {
                fctx.body.push({ op: "drop" });
              }
            }
          }
          if (paramTypes2) {
            for (let i = expr.arguments.length; i < paramTypes2.length; i++) {
              pushDefaultValue(fctx, paramTypes2[i], ctx);
            }
          }
          const finalMethodIdx2 = ctx.funcMap.get(fullName) ?? funcIdx;
          fctx.body.push({ op: "call", funcIdx: finalMethodIdx2 });
          const sig2 = ctx.checker.getResolvedSignature(expr);
          if (sig2) {
            const retType = ctx.checker.getReturnTypeOfSignature(sig2);
            if (isEffectivelyVoidReturn(ctx, retType, fullName)) return VOID_RESULT;
            if (wasmFuncReturnsVoid(ctx, finalMethodIdx2)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, finalMethodIdx2) ?? resolveWasmType(ctx, retType);
          }
          return VOID_RESULT;
        }
        const methodParamTypes0 = getFuncParamTypes(ctx, funcIdx);
        let recvType = compileExpression2(ctx, fctx, propAccess.expression, methodParamTypes0?.[0]);
        let receiverWasCast = false;
        if (recvType && recvType.kind === "externref") {
          const structTypeIdx = ctx.structMap.get(receiverClassName);
          if (structTypeIdx !== void 0) {
            emitNullCheckThrow2(ctx, fctx, { kind: "externref" });
            fctx.body.push({ op: "any.convert_extern" });
            emitGuardedRefCast(fctx, structTypeIdx);
            recvType = { kind: "ref_null", typeIdx: structTypeIdx };
            receiverWasCast = true;
          }
        }
        if (recvType && recvType.kind === "ref_null") {
          const sig2 = ctx.checker.getResolvedSignature(expr);
          let callReturnType = VOID_RESULT;
          if (sig2) {
            const retType = ctx.checker.getReturnTypeOfSignature(sig2);
            if (!isEffectivelyVoidReturn(ctx, retType, fullName))
              callReturnType = getWasmFuncReturnType(ctx, funcIdx) ?? resolveWasmType(ctx, retType);
          }
          const tmp = allocLocal(fctx, `__ng_recv_${fctx.locals.length}`, recvType);
          fctx.body.push({ op: "local.tee", index: tmp });
          fctx.body.push({ op: "ref.is_null" });
          const savedBody = pushBody(fctx);
          fctx.body.push({ op: "local.get", index: tmp });
          fctx.body.push({ op: "ref.as_non_null" });
          const paramTypes2 = getFuncParamTypes(ctx, funcIdx);
          if (paramTypes2?.[0]) {
            const recvRefType = { kind: "ref", typeIdx: recvType.typeIdx };
            if (!valTypesMatch(recvRefType, paramTypes2[0])) {
              coerceType3(ctx, fctx, recvRefType, paramTypes2[0]);
            }
          }
          const ngParamCount = paramTypes2 ? paramTypes2.length - 1 : expr.arguments.length;
          for (let i = 0; i < expr.arguments.length; i++) {
            if (i < ngParamCount) {
              compileExpression2(ctx, fctx, expr.arguments[i], paramTypes2?.[i + 1]);
            } else {
              const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
              if (extraType !== null) {
                fctx.body.push({ op: "drop" });
              }
            }
          }
          if (paramTypes2) {
            for (let i = expr.arguments.length + 1; i < paramTypes2.length; i++) {
              pushDefaultValue(fctx, paramTypes2[i], ctx);
            }
          }
          const finalMethodIdx2 = ctx.funcMap.get(fullName) ?? funcIdx;
          fctx.body.push({ op: "call", funcIdx: finalMethodIdx2 });
          const elseInstrs = fctx.body;
          fctx.body = savedBody;
          if (callReturnType === VOID_RESULT) {
            fctx.body.push({
              op: "if",
              blockType: { kind: "empty" },
              then: receiverWasCast ? [] : typeErrorThrowInstrs(ctx),
              else: elseInstrs,
            });
            return VOID_RESULT;
          } else {
            const resultType =
              callReturnType.kind === "ref" ? { kind: "ref_null", typeIdx: callReturnType.typeIdx } : callReturnType;
            fctx.body.push({
              op: "if",
              blockType: { kind: "val", type: resultType },
              then: receiverWasCast ? defaultValueInstrs(resultType) : typeErrorThrowInstrs(ctx),
              else: elseInstrs,
            });
            return resultType;
          }
        }
        const paramTypes = getFuncParamTypes(ctx, funcIdx);
        const methodParamCount = paramTypes ? paramTypes.length - 1 : expr.arguments.length;
        for (let i = 0; i < expr.arguments.length; i++) {
          if (i < methodParamCount) {
            compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
          } else {
            const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
            if (extraType !== null) {
              fctx.body.push({ op: "drop" });
            }
          }
        }
        if (paramTypes) {
          for (let i = expr.arguments.length + 1; i < paramTypes.length; i++) {
            pushDefaultValue(fctx, paramTypes[i], ctx);
          }
        }
        const finalMethodIdx = ctx.funcMap.get(fullName) ?? funcIdx;
        fctx.body.push({ op: "call", funcIdx: finalMethodIdx });
        const sig = ctx.checker.getResolvedSignature(expr);
        if (sig) {
          const retType = ctx.checker.getReturnTypeOfSignature(sig);
          if (isEffectivelyVoidReturn(ctx, retType, fullName)) return VOID_RESULT;
          if (wasmFuncReturnsVoid(ctx, finalMethodIdx)) return VOID_RESULT;
          return getWasmFuncReturnType(ctx, finalMethodIdx) ?? resolveWasmType(ctx, retType);
        }
        return VOID_RESULT;
      }
    }
    {
      const structTypeName = resolveStructName(ctx, receiverType);
      if (structTypeName) {
        const methodName = propAccess.name.text;
        const fullName = `${structTypeName}_${methodName}`;
        const funcIdx = ctx.funcMap.get(fullName);
        if (funcIdx === void 0) {
          const callablePropResult = compileCallablePropertyCall(ctx, fctx, expr, propAccess, structTypeName);
          if (callablePropResult !== void 0) return callablePropResult;
        }
        if (funcIdx !== void 0) {
          const structMethodPTypes = getFuncParamTypes(ctx, funcIdx);
          const recvType = compileExpression2(ctx, fctx, propAccess.expression, structMethodPTypes?.[0]);
          const smReceiverWasCast = fctx.__lastGuardedCastBackup !== void 0;
          if (recvType && recvType.kind === "ref_null") {
            const sig2 = ctx.checker.getResolvedSignature(expr);
            let callReturnType = VOID_RESULT;
            if (sig2) {
              const retType = ctx.checker.getReturnTypeOfSignature(sig2);
              if (!isEffectivelyVoidReturn(ctx, retType, fullName))
                callReturnType = getWasmFuncReturnType(ctx, funcIdx) ?? resolveWasmType(ctx, retType);
            }
            const tmp = allocLocal(fctx, `__ng_srecv_${fctx.locals.length}`, recvType);
            fctx.body.push({ op: "local.tee", index: tmp });
            fctx.body.push({ op: "ref.is_null" });
            const savedBody = pushBody(fctx);
            fctx.body.push({ op: "local.get", index: tmp });
            fctx.body.push({ op: "ref.as_non_null" });
            const paramTypes2 = getFuncParamTypes(ctx, funcIdx);
            if (paramTypes2?.[0]) {
              const recvRefType = { kind: "ref", typeIdx: recvType.typeIdx };
              if (!valTypesMatch(recvRefType, paramTypes2[0])) {
                coerceType3(ctx, fctx, recvRefType, paramTypes2[0]);
              }
            }
            const smMethodParamCount = paramTypes2 ? paramTypes2.length - 1 : expr.arguments.length;
            for (let i = 0; i < expr.arguments.length; i++) {
              if (i < smMethodParamCount) {
                compileExpression2(ctx, fctx, expr.arguments[i], paramTypes2?.[i + 1]);
              } else {
                const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
                if (extraType !== null) {
                  fctx.body.push({ op: "drop" });
                }
              }
            }
            if (paramTypes2) {
              for (let i = Math.min(expr.arguments.length, smMethodParamCount) + 1; i < paramTypes2.length; i++) {
                pushDefaultValue(fctx, paramTypes2[i], ctx);
              }
            }
            const finalStructMethodIdx2 = ctx.funcMap.get(fullName) ?? funcIdx;
            fctx.body.push({ op: "call", funcIdx: finalStructMethodIdx2 });
            const elseInstrs = fctx.body;
            fctx.body = savedBody;
            if (callReturnType === VOID_RESULT) {
              fctx.body.push({
                op: "if",
                blockType: { kind: "empty" },
                then: smReceiverWasCast ? [] : typeErrorThrowInstrs(ctx),
                else: elseInstrs,
              });
              return VOID_RESULT;
            } else {
              const resultType =
                callReturnType.kind === "ref"
                  ? {
                      kind: "ref_null",
                      typeIdx: callReturnType.typeIdx,
                    }
                  : callReturnType;
              fctx.body.push({
                op: "if",
                blockType: { kind: "val", type: resultType },
                then: smReceiverWasCast ? defaultValueInstrs(resultType) : typeErrorThrowInstrs(ctx),
                else: elseInstrs,
              });
              return resultType;
            }
          }
          const paramTypes = getFuncParamTypes(ctx, funcIdx);
          const nnMethodParamCount = paramTypes ? paramTypes.length - 1 : expr.arguments.length;
          for (let i = 0; i < expr.arguments.length; i++) {
            if (i < nnMethodParamCount) {
              compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
            } else {
              const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
              if (extraType !== null) {
                fctx.body.push({ op: "drop" });
              }
            }
          }
          if (paramTypes) {
            for (let i = Math.min(expr.arguments.length, nnMethodParamCount) + 1; i < paramTypes.length; i++) {
              pushDefaultValue(fctx, paramTypes[i], ctx);
            }
          }
          const finalStructMethodIdx = ctx.funcMap.get(fullName) ?? funcIdx;
          fctx.body.push({ op: "call", funcIdx: finalStructMethodIdx });
          const sig = ctx.checker.getResolvedSignature(expr);
          if (sig) {
            const retType = ctx.checker.getReturnTypeOfSignature(sig);
            if (isEffectivelyVoidReturn(ctx, retType, fullName)) return VOID_RESULT;
            if (wasmFuncReturnsVoid(ctx, finalStructMethodIdx)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, finalStructMethodIdx) ?? resolveWasmType(ctx, retType);
          }
          return VOID_RESULT;
        }
      }
    }
    {
      const arrMethodResult = compileArrayMethodCall(ctx, fctx, propAccess, expr, receiverType);
      if (arrMethodResult !== void 0) return arrMethodResult;
    }
    if (isNumberType(receiverType) && propAccess.name.text === "toString") {
      if (expr.arguments.length > 0) {
        compileExpression2(ctx, fctx, expr.arguments[0], { kind: "f64" });
        fctx.body.push({ op: "f64.floor" });
        const radixLocal = allocLocal(fctx, `__radix_${fctx.locals.length}`, { kind: "f64" });
        fctx.body.push({ op: "local.tee", index: radixLocal });
        fctx.body.push({ op: "f64.const", value: 2 });
        fctx.body.push({ op: "f64.lt" });
        fctx.body.push({ op: "local.get", index: radixLocal });
        fctx.body.push({ op: "f64.const", value: 36 });
        fctx.body.push({ op: "f64.gt" });
        fctx.body.push({ op: "i32.or" });
        fctx.body.push({ op: "local.get", index: radixLocal });
        fctx.body.push({ op: "local.get", index: radixLocal });
        fctx.body.push({ op: "f64.ne" });
        fctx.body.push({ op: "i32.or" });
        {
          const rangeErrMsg = "RangeError: toString() radix must be between 2 and 36";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "global.get", index: strIdx },
              { op: "throw", tagIdx },
            ],
            else: [],
          });
        }
      }
      const exprType = compileExpression2(ctx, fctx, propAccess.expression);
      if (exprType && exprType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
      }
      const funcIdx = ctx.funcMap.get("number_toString");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "externref" };
      }
    }
    if (isNumberType(receiverType) && propAccess.name.text === "toFixed") {
      const exprType = compileExpression2(ctx, fctx, propAccess.expression);
      if (exprType && exprType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
      }
      if (expr.arguments.length > 0) {
        compileExpression2(ctx, fctx, expr.arguments[0]);
        const digitsLocal = allocLocal(fctx, `__toFixed_digits_${fctx.locals.length}`, { kind: "f64" });
        fctx.body.push({ op: "local.tee", index: digitsLocal });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.lt" });
        fctx.body.push({ op: "local.get", index: digitsLocal });
        fctx.body.push({ op: "f64.const", value: 100 });
        fctx.body.push({ op: "f64.gt" });
        fctx.body.push({ op: "i32.or" });
        {
          const rangeErrMsg = "RangeError: toFixed() digits argument must be between 0 and 100";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "global.get", index: strIdx },
              { op: "throw", tagIdx },
            ],
            else: [],
          });
        }
        fctx.body.push({ op: "local.get", index: digitsLocal });
      } else {
        fctx.body.push({ op: "f64.const", value: 0 });
      }
      const funcIdx = ctx.funcMap.get("number_toFixed");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "externref" };
      }
    }
    if (isNumberType(receiverType) && propAccess.name.text === "toPrecision") {
      const exprType = compileExpression2(ctx, fctx, propAccess.expression);
      if (exprType && exprType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
      }
      if (expr.arguments.length > 0) {
        compileExpression2(ctx, fctx, expr.arguments[0]);
        const precLocal = allocLocal(fctx, `__toPrecision_prec_${fctx.locals.length}`, { kind: "f64" });
        fctx.body.push({ op: "local.tee", index: precLocal });
        fctx.body.push({ op: "f64.const", value: 1 });
        fctx.body.push({ op: "f64.lt" });
        fctx.body.push({ op: "local.get", index: precLocal });
        fctx.body.push({ op: "f64.const", value: 100 });
        fctx.body.push({ op: "f64.gt" });
        fctx.body.push({ op: "i32.or" });
        fctx.body.push({ op: "local.get", index: precLocal });
        fctx.body.push({ op: "local.get", index: precLocal });
        fctx.body.push({ op: "f64.ne" });
        fctx.body.push({ op: "i32.or" });
        {
          const rangeErrMsg = "RangeError: toPrecision() argument must be between 1 and 100";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "global.get", index: strIdx },
              { op: "throw", tagIdx },
            ],
            else: [],
          });
        }
        fctx.body.push({ op: "local.get", index: precLocal });
      } else {
        const funcIdx2 = ctx.funcMap.get("number_toString");
        if (funcIdx2 !== void 0) {
          fctx.body.push({ op: "call", funcIdx: funcIdx2 });
          return { kind: "externref" };
        }
      }
      const funcIdx = ctx.funcMap.get("number_toPrecision");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "externref" };
      }
    }
    if (isNumberType(receiverType) && propAccess.name.text === "toExponential") {
      const exprType = compileExpression2(ctx, fctx, propAccess.expression);
      if (exprType && exprType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
      }
      if (expr.arguments.length > 0) {
        compileExpression2(ctx, fctx, expr.arguments[0]);
        const digitsLocal = allocLocal(fctx, `__toExponential_digits_${fctx.locals.length}`, { kind: "f64" });
        fctx.body.push({ op: "local.tee", index: digitsLocal });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.lt" });
        fctx.body.push({ op: "local.get", index: digitsLocal });
        fctx.body.push({ op: "f64.const", value: 100 });
        fctx.body.push({ op: "f64.gt" });
        fctx.body.push({ op: "i32.or" });
        {
          const rangeErrMsg = "RangeError: toExponential() argument must be between 0 and 100";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "global.get", index: strIdx },
              { op: "throw", tagIdx },
            ],
            else: [],
          });
        }
        fctx.body.push({ op: "local.get", index: digitsLocal });
      } else {
        fctx.body.push({ op: "f64.const", value: NaN });
      }
      const funcIdx = ctx.funcMap.get("number_toExponential");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return { kind: "externref" };
      }
    }
    if (isStringType(receiverType)) {
      const method = propAccess.name.text;
      if (method === "toString" || method === "valueOf") {
        return compileExpression2(ctx, fctx, propAccess.expression);
      }
      if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
        return compileNativeStringMethodCall(ctx, fctx, expr, propAccess, method);
      }
      if (method === "charCodeAt") {
        const charCodeAtIdx = ctx.funcMap.get("charCodeAt");
        if (charCodeAtIdx !== void 0) {
          compileExpression2(ctx, fctx, propAccess.expression);
          if (expr.arguments.length > 0) {
            const argType = compileExpression2(ctx, fctx, expr.arguments[0], {
              kind: "f64",
            });
            if (!argType) {
              fctx.body.push({ op: "i32.const", value: 0 });
            } else if (argType.kind === "f64") {
              fctx.body.push({ op: "i32.trunc_sat_f64_s" });
            }
          } else {
            fctx.body.push({ op: "i32.const", value: 0 });
          }
          fctx.body.push({ op: "call", funcIdx: charCodeAtIdx });
          return { kind: "i32" };
        }
      }
      const importName = `string_${method}`;
      const funcIdx = ctx.funcMap.get(importName);
      if (funcIdx !== void 0) {
        compileExpression2(ctx, fctx, propAccess.expression);
        const paramTypes = getFuncParamTypes(ctx, funcIdx);
        const args = expr.arguments;
        const userParamCount = paramTypes ? paramTypes.length - 1 : args.length;
        for (let ai = 0; ai < args.length; ai++) {
          if (ai < userParamCount) {
            const expectedArgType = paramTypes?.[ai + 1];
            const argResult = compileExpression2(ctx, fctx, args[ai], expectedArgType);
            if (!argResult) {
              pushDefaultValue(fctx, expectedArgType ?? { kind: "f64" }, ctx);
            } else if (expectedArgType && argResult.kind !== expectedArgType.kind) {
              coerceType3(ctx, fctx, argResult, expectedArgType);
            }
          } else {
            const extraType = compileExpression2(ctx, fctx, args[ai]);
            if (extraType !== null) {
              fctx.body.push({ op: "drop" });
            }
          }
        }
        if (paramTypes && args.length + 1 < paramTypes.length) {
          for (let pi = args.length + 1; pi < paramTypes.length; pi++) {
            const pt = paramTypes[pi];
            if (pt.kind === "externref") fctx.body.push({ op: "ref.null.extern" });
            else if (pt.kind === "f64") fctx.body.push({ op: "f64.const", value: 0 });
            else if (pt.kind === "i32") fctx.body.push({ op: "i32.const", value: 0 });
          }
        }
        fctx.body.push({ op: "call", funcIdx });
        const returnsBool = method === "includes" || method === "startsWith" || method === "endsWith";
        const returnsNum =
          method === "indexOf" || method === "lastIndexOf" || method === "codePointAt" || method === "search";
        return returnsBool ? { kind: "i32" } : returnsNum ? { kind: "f64" } : { kind: "externref" };
      }
    }
    if (isBooleanType(receiverType)) {
      const method = propAccess.name.text;
      if (method === "toString") {
        compileExpression2(ctx, fctx, propAccess.expression);
        emitBoolToString(ctx, fctx);
        return { kind: "externref" };
      }
      if (method === "valueOf") {
        return compileExpression2(ctx, fctx, propAccess.expression);
      }
    }
    if (isNumberType(receiverType) && propAccess.name.text === "valueOf") {
      return compileExpression2(ctx, fctx, propAccess.expression);
    }
    if (propAccess.name.text === "toString" && expr.arguments.length === 0) {
      const tsType = ctx.checker.getTypeAtLocation(propAccess.expression);
      const wasm = resolveWasmType(ctx, tsType);
      if (wasm.kind === "externref") {
        const toStrIdx = ensureLateImport2(ctx, "__extern_toString", [{ kind: "externref" }], [{ kind: "externref" }]);
        flushLateImportShifts2(ctx, fctx);
        if (toStrIdx !== void 0) {
          compileExpression2(ctx, fctx, propAccess.expression);
          fctx.body.push({ op: "call", funcIdx: toStrIdx });
          return { kind: "externref" };
        }
      }
      const exprType = compileExpression2(ctx, fctx, propAccess.expression);
      if (exprType) {
        if (exprType.kind === "externref") {
          const toStrIdx = ensureLateImport2(
            ctx,
            "__extern_toString",
            [{ kind: "externref" }],
            [{ kind: "externref" }],
          );
          flushLateImportShifts2(ctx, fctx);
          if (toStrIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: toStrIdx });
            return { kind: "externref" };
          }
        }
        fctx.body.push({ op: "drop" });
      }
      let isArray = false;
      if (wasm.kind === "ref" || wasm.kind === "ref_null") {
        const arrInfo = resolveArrayInfo(ctx, tsType);
        if (arrInfo) isArray = true;
      }
      const callSigs = tsType.getCallSignatures?.();
      const isFunc = callSigs && callSigs.length > 0 && !tsType.getProperties?.()?.length;
      if (isFunc) {
        addStringConstantGlobal(ctx, "function () { [native code] }");
        const idx = ctx.stringGlobalMap.get("function () { [native code] }");
        fctx.body.push({ op: "global.get", index: idx });
      } else {
        const str = isArray ? "[object Array]" : "[object Object]";
        addStringConstantGlobal(ctx, str);
        const idx = ctx.stringGlobalMap.get(str);
        fctx.body.push({ op: "global.get", index: idx });
      }
      return { kind: "externref" };
    }
    if (propAccess.name.text === "valueOf" && expr.arguments.length === 0) {
      return compileExpression2(ctx, fctx, propAccess.expression);
    }
    {
      const recvTsType = ctx.checker.getTypeAtLocation(propAccess.expression);
      const recvWasm = resolveWasmType(ctx, recvTsType);
      const isAnyOrExternref = (recvTsType.flags & ts13.TypeFlags.Any) !== 0 || recvWasm.kind === "externref";
      if (isAnyOrExternref) {
        const methodName = propAccess.name.text;
        if (methodName === "next") {
          const genNextIdx = ctx.funcMap.get("__gen_next");
          if (genNextIdx !== void 0) {
            compileExpression2(ctx, fctx, propAccess.expression, {
              kind: "externref",
            });
            for (const arg of expr.arguments) {
              const argType = compileExpression2(ctx, fctx, arg);
              if (argType) {
                fctx.body.push({ op: "drop" });
              }
            }
            fctx.body.push({ op: "call", funcIdx: genNextIdx });
            return { kind: "externref" };
          }
        }
        if (methodName === "return") {
          const genReturnIdx = ctx.funcMap.get("__gen_return");
          if (genReturnIdx !== void 0) {
            compileExpression2(ctx, fctx, propAccess.expression, {
              kind: "externref",
            });
            if (expr.arguments.length > 0) {
              compileExpression2(ctx, fctx, expr.arguments[0], {
                kind: "externref",
              });
            } else {
              fctx.body.push({ op: "ref.null.extern" });
            }
            fctx.body.push({ op: "call", funcIdx: genReturnIdx });
            return { kind: "externref" };
          }
        }
        if (methodName === "throw") {
          const genThrowIdx = ctx.funcMap.get("__gen_throw");
          if (genThrowIdx !== void 0) {
            compileExpression2(ctx, fctx, propAccess.expression, {
              kind: "externref",
            });
            if (expr.arguments.length > 0) {
              compileExpression2(ctx, fctx, expr.arguments[0], {
                kind: "externref",
              });
            } else {
              fctx.body.push({ op: "ref.null.extern" });
            }
            fctx.body.push({ op: "call", funcIdx: genThrowIdx });
            return { kind: "externref" };
          }
        }
        {
          const externResult = tryExternClassMethodOnAny(ctx, fctx, expr, propAccess, methodName);
          if (externResult !== null) return externResult;
        }
        {
          const recvType = compileExpression2(ctx, fctx, propAccess.expression);
          if (recvType) {
            fctx.body.push({ op: "drop" });
          }
          for (const arg of expr.arguments) {
            const argType = compileExpression2(ctx, fctx, arg);
            if (argType) {
              fctx.body.push({ op: "drop" });
            }
          }
          fctx.body.push({ op: "ref.null.extern" });
          return { kind: "externref" };
        }
      }
    }
  }
  if (ts13.isIdentifier(expr.expression)) {
    const funcName = expr.expression.text;
    if (funcName === "isNaN" && expr.arguments.length >= 1) {
      compileExpression2(ctx, fctx, expr.arguments[0], { kind: "f64" });
      const tmp = allocLocal(fctx, `__isnan_${fctx.locals.length}`, {
        kind: "f64",
      });
      fctx.body.push({ op: "local.tee", index: tmp });
      fctx.body.push({ op: "local.get", index: tmp });
      fctx.body.push({ op: "f64.ne" });
      return { kind: "i32" };
    }
    if (funcName === "isFinite" && expr.arguments.length >= 1) {
      compileExpression2(ctx, fctx, expr.arguments[0], { kind: "f64" });
      const tmp = allocLocal(fctx, `__isfin_${fctx.locals.length}`, {
        kind: "f64",
      });
      fctx.body.push({ op: "local.tee", index: tmp });
      fctx.body.push({ op: "local.get", index: tmp });
      fctx.body.push({ op: "f64.sub" });
      fctx.body.push({ op: "f64.const", value: 0 });
      fctx.body.push({ op: "f64.eq" });
      return { kind: "i32" };
    }
    if ((funcName === "parseInt" || funcName === "parseFloat") && expr.arguments.length >= 1) {
      const importFuncIdx = ctx.funcMap.get(funcName);
      if (importFuncIdx !== void 0) {
        const arg0 = expr.arguments[0];
        const arg0Type = compileExpression2(ctx, fctx, arg0);
        if (arg0Type && arg0Type.kind !== "externref") {
          if (
            arg0Type.kind === "i32" &&
            (arg0.kind === ts13.SyntaxKind.TrueKeyword || arg0.kind === ts13.SyntaxKind.FalseKeyword)
          ) {
            addUnionImports(ctx);
            const boxIdx = ctx.funcMap.get("__box_boolean");
            if (boxIdx !== void 0) fctx.body.push({ op: "call", funcIdx: boxIdx });
          } else {
            coerceType3(ctx, fctx, arg0Type, { kind: "externref" });
          }
        }
        if (funcName === "parseInt") {
          if (expr.arguments.length >= 2) {
            compileExpression2(ctx, fctx, expr.arguments[1], { kind: "f64" });
          } else {
            fctx.body.push({ op: "f64.const", value: NaN });
          }
        }
        fctx.body.push({ op: "call", funcIdx: importFuncIdx });
        return { kind: "f64" };
      }
    }
    if (
      (funcName === "decodeURI" ||
        funcName === "decodeURIComponent" ||
        funcName === "encodeURI" ||
        funcName === "encodeURIComponent") &&
      expr.arguments.length >= 1
    ) {
      const importFuncIdx = ctx.funcMap.get(funcName);
      if (importFuncIdx !== void 0) {
        const arg0Type = compileExpression2(ctx, fctx, expr.arguments[0]);
        if (arg0Type && arg0Type.kind !== "externref") {
          coerceType3(ctx, fctx, arg0Type, { kind: "externref" });
        }
        fctx.body.push({ op: "call", funcIdx: importFuncIdx });
        return { kind: "externref" };
      }
    }
    if (funcName === "Number" && expr.arguments.length >= 1) {
      const argType = compileExpression2(ctx, fctx, expr.arguments[0]);
      if (argType?.kind === "i64") {
        fctx.body.push({ op: "f64.convert_i64_s" });
        return { kind: "f64" };
      }
      if (argType?.kind === "externref") {
        const pfIdx = ctx.funcMap.get("parseFloat");
        if (pfIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: pfIdx });
          return { kind: "f64" };
        }
      }
      if (argType?.kind === "ref" || argType?.kind === "ref_null") {
        coerceType3(ctx, fctx, argType, { kind: "f64" }, "number");
        return { kind: "f64" };
      }
      return argType;
    }
    if (funcName === "BigInt" && expr.arguments.length >= 1) {
      const argType = compileExpression2(ctx, fctx, expr.arguments[0]);
      if (argType?.kind === "f64") {
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
        return { kind: "i64" };
      }
      if (argType?.kind === "i32") {
        fctx.body.push({ op: "i64.extend_i32_s" });
        return { kind: "i64" };
      }
      return argType;
    }
    if (funcName === "Number" && expr.arguments.length === 0) {
      fctx.body.push({
        op: ctx.fast ? "i32.const" : "f64.const",
        value: 0,
      });
      return ctx.fast ? { kind: "i32" } : { kind: "f64" };
    }
    if (funcName === "Symbol") {
      return compileSymbolCall(ctx, fctx, expr.arguments);
    }
    if (funcName === "String") {
      if (expr.arguments.length === 0) {
        addStringConstantGlobal(ctx, "");
        const emptyIdx = ctx.stringGlobalMap.get("");
        fctx.body.push({ op: "global.get", index: emptyIdx });
        return { kind: "externref" };
      }
      const strArg0 = expr.arguments[0];
      const strArg0IsNull = strArg0.kind === ts13.SyntaxKind.NullKeyword;
      const strArg0IsUndefined =
        strArg0.kind === ts13.SyntaxKind.UndefinedKeyword ||
        (ts13.isIdentifier(strArg0) && strArg0.text === "undefined") ||
        ts13.isVoidExpression(strArg0);
      if (strArg0IsNull) {
        addStringConstantGlobal(ctx, "null");
        const nullGIdx = ctx.stringGlobalMap.get("null");
        fctx.body.push({ op: "global.get", index: nullGIdx });
        return { kind: "externref" };
      }
      if (strArg0IsUndefined) {
        addStringConstantGlobal(ctx, "undefined");
        const undefGIdx = ctx.stringGlobalMap.get("undefined");
        fctx.body.push({ op: "global.get", index: undefGIdx });
        return { kind: "externref" };
      }
      const argType = compileExpression2(ctx, fctx, strArg0);
      if (argType === null) {
        addStringConstantGlobal(ctx, "undefined");
        const undefGIdx = ctx.stringGlobalMap.get("undefined");
        fctx.body.push({ op: "global.get", index: undefGIdx });
        return { kind: "externref" };
      }
      if (argType?.kind === "i32") {
        const argTsType = ctx.checker.getTypeAtLocation(strArg0);
        if (isBooleanType(argTsType)) {
          emitBoolToString(ctx, fctx);
          return { kind: "externref" };
        }
        const toStrIdx = ctx.funcMap.get("number_toString");
        if (toStrIdx !== void 0) {
          fctx.body.push({ op: "f64.convert_i32_s" });
          fctx.body.push({ op: "call", funcIdx: toStrIdx });
          return { kind: "externref" };
        }
      }
      if (argType?.kind === "f64") {
        const toStrIdx = ctx.funcMap.get("number_toString");
        if (toStrIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: toStrIdx });
          return { kind: "externref" };
        }
      }
      if (argType?.kind === "externref") {
        const argTsType = ctx.checker.getTypeAtLocation(strArg0);
        if (argTsType.flags & ts13.TypeFlags.Null) {
          fctx.body.push({ op: "drop" });
          addStringConstantGlobal(ctx, "null");
          const nullGIdx = ctx.stringGlobalMap.get("null");
          fctx.body.push({ op: "global.get", index: nullGIdx });
          return { kind: "externref" };
        }
        if (argTsType.flags & (ts13.TypeFlags.Undefined | ts13.TypeFlags.Void)) {
          fctx.body.push({ op: "drop" });
          addStringConstantGlobal(ctx, "undefined");
          const undefGIdx = ctx.stringGlobalMap.get("undefined");
          fctx.body.push({ op: "global.get", index: undefGIdx });
          return { kind: "externref" };
        }
        if (isStringType(argTsType)) {
          return { kind: "externref" };
        }
        const toStrIdx = ctx.funcMap.get("extern_toString");
        if (toStrIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: toStrIdx });
        }
        return { kind: "externref" };
      }
      if (argType?.kind === "ref" || argType?.kind === "ref_null") {
        const argTsType = ctx.checker.getTypeAtLocation(strArg0);
        if (isStringType(argTsType)) {
          return argType;
        }
        coerceType3(ctx, fctx, argType, { kind: "externref" }, "string");
        return { kind: "externref" };
      }
      return argType ?? { kind: "externref" };
    }
    if (funcName === "Boolean") {
      if (expr.arguments.length === 0) {
        fctx.body.push({ op: "i32.const", value: 0 });
        return { kind: "i32" };
      }
      const argType = compileExpression2(ctx, fctx, expr.arguments[0]);
      if (argType === null) {
        fctx.body.push({ op: "i32.const", value: 0 });
        return { kind: "i32" };
      }
      if (argType?.kind === "f64") {
        const tmp = allocLocal(fctx, `__bool_${fctx.locals.length}`, {
          kind: "f64",
        });
        fctx.body.push({ op: "local.tee", index: tmp });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.ne" });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "local.get", index: tmp });
        fctx.body.push({ op: "f64.eq" });
        fctx.body.push({ op: "i32.and" });
        return { kind: "i32" };
      }
      if (argType?.kind === "i32") {
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "i32.ne" });
        return { kind: "i32" };
      }
      if (
        (argType?.kind === "ref" || argType?.kind === "ref_null") &&
        ctx.nativeStrings &&
        ctx.anyStrTypeIdx >= 0 &&
        isStringType(ctx.checker.getTypeAtLocation(expr.arguments[0]))
      ) {
        fctx.body.push({
          op: "struct.get",
          typeIdx: ctx.anyStrTypeIdx,
          fieldIdx: 0,
        });
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "i32.ne" });
        return { kind: "i32" };
      }
      if (argType?.kind === "externref") {
        const argTsType = ctx.checker.getTypeAtLocation(expr.arguments[0]);
        if (isStringType(argTsType)) {
          addStringImports(ctx);
          const lenIdx = ctx.funcMap.get("length");
          if (lenIdx !== void 0) {
            fctx.body.push({ op: "call", funcIdx: lenIdx });
            fctx.body.push({ op: "i32.const", value: 0 });
            fctx.body.push({ op: "i32.ne" });
            return { kind: "i32" };
          }
        }
        fctx.body.push({ op: "ref.is_null" });
        fctx.body.push({ op: "i32.const", value: 1 });
        fctx.body.push({ op: "i32.xor" });
        return { kind: "i32" };
      }
      if (argType?.kind === "ref" || argType?.kind === "ref_null") {
        fctx.body.push({ op: "drop" });
        fctx.body.push({ op: "i32.const", value: 1 });
        return { kind: "i32" };
      }
      return { kind: "i32" };
    }
    if (funcName === "Array") {
      return compileArrayConstructorCall(ctx, fctx, expr);
    }
  }
  if (ts13.isIdentifier(expr.expression)) {
    const funcName = expr.expression.text;
    let closureInfo = ctx.closureMap.get(funcName);
    if (!closureInfo) {
      const localIdx = fctx.localMap.get(funcName);
      if (localIdx !== void 0) {
        const localType =
          localIdx < fctx.params.length
            ? fctx.params[localIdx]?.type
            : fctx.locals[localIdx - fctx.params.length]?.type;
        if (localType && (localType.kind === "ref" || localType.kind === "ref_null")) {
          closureInfo = ctx.closureInfoByTypeIdx.get(localType.typeIdx);
        }
      }
    }
    if (closureInfo) {
      return compileClosureCall(ctx, fctx, expr, funcName, closureInfo);
    }
    const funcIdx = ctx.funcMap.get(funcName);
    if (funcIdx === void 0) {
      const calleeLocalIdx = fctx.localMap.get(funcName);
      const calleeModGlobal = calleeLocalIdx === void 0 ? ctx.moduleGlobals.get(funcName) : void 0;
      const calleeCapturedGlobal =
        calleeLocalIdx === void 0 && calleeModGlobal === void 0 ? ctx.capturedGlobals.get(funcName) : void 0;
      const isKnownVariable =
        calleeLocalIdx !== void 0 || calleeModGlobal !== void 0 || calleeCapturedGlobal !== void 0;
      const calleeTsType = ctx.checker.getTypeAtLocation(expr.expression);
      const callSigs = isKnownVariable ? calleeTsType.getCallSignatures?.() : void 0;
      if (callSigs && callSigs.length > 0) {
        const sig2 = callSigs[0];
        const sigParamCount = sig2.parameters.length;
        const sigRetType = ctx.checker.getReturnTypeOfSignature(sig2);
        const sigRetWasm = isVoidType(sigRetType) ? null : resolveWasmType(ctx, sigRetType);
        const sigParamWasmTypes = [];
        for (let i = 0; i < sigParamCount; i++) {
          const paramType = ctx.checker.getTypeOfSymbol(sig2.parameters[i]);
          sigParamWasmTypes.push(resolveWasmType(ctx, paramType));
        }
        const resultTypes = sigRetWasm ? [sigRetWasm] : [];
        const wrapperTypes = getOrCreateFuncRefWrapperTypes(ctx, sigParamWasmTypes, resultTypes);
        if (wrapperTypes) {
          const matchedClosureInfo = wrapperTypes.closureInfo;
          const matchedStructTypeIdx = wrapperTypes.structTypeIdx;
          const innerResultType = compileExpression2(ctx, fctx, expr.expression);
          let closureLocal;
          if (innerResultType?.kind === "externref") {
            const closureRefType = {
              kind: "ref_null",
              typeIdx: matchedStructTypeIdx,
            };
            closureLocal = allocLocal(fctx, `__callable_param_${fctx.locals.length}`, closureRefType);
            fctx.body.push({ op: "any.convert_extern" });
            emitGuardedRefCast(fctx, matchedStructTypeIdx);
            fctx.body.push({ op: "local.set", index: closureLocal });
          } else {
            const closureRefType = innerResultType ?? {
              kind: "ref",
              typeIdx: matchedStructTypeIdx,
            };
            closureLocal = allocLocal(fctx, `__callable_param_${fctx.locals.length}`, closureRefType);
            fctx.body.push({ op: "local.set", index: closureLocal });
          }
          fctx.body.push({ op: "local.get", index: closureLocal });
          emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedStructTypeIdx });
          {
            const cpParamCnt = matchedClosureInfo.paramTypes.length;
            for (let i = 0; i < Math.min(expr.arguments.length, cpParamCnt); i++) {
              compileExpression2(ctx, fctx, expr.arguments[i], matchedClosureInfo.paramTypes[i]);
            }
            for (let i = cpParamCnt; i < expr.arguments.length; i++) {
              const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
              if (extraType !== null) {
                fctx.body.push({ op: "drop" });
              }
            }
          }
          for (let i = expr.arguments.length; i < matchedClosureInfo.paramTypes.length; i++) {
            pushDefaultValue(fctx, matchedClosureInfo.paramTypes[i], ctx);
          }
          fctx.body.push({ op: "local.get", index: closureLocal });
          emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedStructTypeIdx });
          fctx.body.push({
            op: "struct.get",
            typeIdx: matchedStructTypeIdx,
            fieldIdx: 0,
          });
          emitGuardedFuncRefCast(fctx, matchedClosureInfo.funcTypeIdx);
          emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedClosureInfo.funcTypeIdx });
          fctx.body.push({
            op: "call_ref",
            typeIdx: matchedClosureInfo.funcTypeIdx,
          });
          return matchedClosureInfo.returnType ?? VOID_RESULT;
        }
      }
      for (const arg of expr.arguments) {
        const argType = compileExpression2(ctx, fctx, arg);
        if (argType) {
          fctx.body.push({ op: "drop" });
        }
      }
      fctx.body.push({ op: "ref.null.extern" });
      return { kind: "externref" };
    }
    const inlineInfo = ctx.inlinableFunctions.get(funcName);
    if (inlineInfo && !expr.arguments.some((a) => ts13.isSpreadElement(a))) {
      const argLocals = [];
      for (let i = 0; i < inlineInfo.paramCount; i++) {
        if (i < expr.arguments.length) {
          compileExpression2(ctx, fctx, expr.arguments[i], inlineInfo.paramTypes[i]);
        } else {
          pushDefaultValue(fctx, inlineInfo.paramTypes[i], ctx);
        }
        const tmpLocal = allocLocal(fctx, `__inline_${funcName}_p${i}_${fctx.locals.length}`, inlineInfo.paramTypes[i]);
        fctx.body.push({ op: "local.set", index: tmpLocal });
        argLocals.push(tmpLocal);
      }
      for (let i = inlineInfo.paramCount; i < expr.arguments.length; i++) {
        const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
        if (extraType !== null) {
          fctx.body.push({ op: "drop" });
        }
      }
      for (const instr of inlineInfo.body) {
        if (instr.op === "local.get") {
          const mapped = argLocals[instr.index];
          if (mapped !== void 0) {
            fctx.body.push({ op: "local.get", index: mapped });
          } else {
            fctx.body.push(instr);
          }
        } else {
          fctx.body.push(instr);
        }
      }
      return inlineInfo.returnType ?? VOID_RESULT;
    }
    const nestedCaptures = ctx.nestedFuncCaptures.get(funcName);
    if (nestedCaptures) {
      const captureParamTypes = getFuncParamTypes(ctx, funcIdx);
      for (let capIdx = 0; capIdx < nestedCaptures.length; capIdx++) {
        const cap = nestedCaptures[capIdx];
        if (cap.mutable && cap.valType) {
          const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.valType);
          if (fctx.boxedCaptures?.has(cap.name)) {
            const currentLocalIdx = fctx.localMap.get(cap.name);
            fctx.body.push({ op: "local.get", index: currentLocalIdx });
          } else {
            fctx.body.push({ op: "local.get", index: cap.outerLocalIdx });
            fctx.body.push({ op: "struct.new", typeIdx: refCellTypeIdx });
            const boxedLocalIdx = allocLocal(fctx, `__boxed_${cap.name}`, {
              kind: "ref",
              typeIdx: refCellTypeIdx,
            });
            fctx.body.push({ op: "local.tee", index: boxedLocalIdx });
            fctx.localMap.set(cap.name, boxedLocalIdx);
            if (!fctx.boxedCaptures) fctx.boxedCaptures = /* @__PURE__ */ new Map();
            fctx.boxedCaptures.set(cap.name, {
              refCellTypeIdx,
              valType: cap.valType,
            });
          }
          const expectedMutCapType = captureParamTypes?.[capIdx];
          if (expectedMutCapType) {
            const refCellType = { kind: "ref", typeIdx: refCellTypeIdx };
            if (!valTypesMatch(refCellType, expectedMutCapType)) {
              coerceType3(ctx, fctx, refCellType, expectedMutCapType);
            }
          }
        } else {
          const capTdzIdx = fctx.tdzFlagLocals?.get(cap.name);
          if (capTdzIdx !== void 0) {
            const capTdzResult = analyzeTdzAccessByPos(ctx, cap.name, expr);
            if (capTdzResult === "check") {
              emitLocalTdzCheck(ctx, fctx, cap.name, capTdzIdx);
            } else if (capTdzResult === "throw") {
              emitStaticTdzThrow(ctx, fctx, cap.name);
            }
          }
          fctx.body.push({ op: "local.get", index: cap.outerLocalIdx });
          const expectedCapType = captureParamTypes?.[capIdx];
          if (expectedCapType) {
            const actualType = getLocalType(fctx, cap.outerLocalIdx);
            if (actualType && !valTypesMatch(actualType, expectedCapType)) {
              coerceType3(ctx, fctx, actualType, expectedCapType);
            }
          }
        }
      }
    }
    const restInfo = ctx.funcRestParams.get(funcName);
    const hasSpreadArg = expr.arguments.some((a) => ts13.isSpreadElement(a));
    if (restInfo && !hasSpreadArg) {
      const paramTypes = getFuncParamTypes(ctx, funcIdx);
      for (let i = 0; i < restInfo.restIndex; i++) {
        if (i < expr.arguments.length) {
          compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i]);
        } else {
          pushDefaultValue(fctx, paramTypes?.[i] ?? { kind: "f64" }, ctx);
        }
      }
      const restArgCount = Math.max(0, expr.arguments.length - restInfo.restIndex);
      fctx.body.push({ op: "i32.const", value: restArgCount });
      for (let i = restInfo.restIndex; i < expr.arguments.length; i++) {
        compileExpression2(ctx, fctx, expr.arguments[i], restInfo.elemType);
      }
      fctx.body.push({
        op: "array.new_fixed",
        typeIdx: restInfo.arrayTypeIdx,
        length: restArgCount,
      });
      fctx.body.push({ op: "struct.new", typeIdx: restInfo.vecTypeIdx });
    } else if (hasSpreadArg) {
      compileSpreadCallArgs(ctx, fctx, expr, funcIdx, restInfo);
    } else {
      const paramTypes = getFuncParamTypes(ctx, funcIdx);
      const captureCount = nestedCaptures ? nestedCaptures.length : 0;
      const paramCount = paramTypes ? paramTypes.length - captureCount : expr.arguments.length;
      for (let i = 0; i < expr.arguments.length; i++) {
        if (i < paramCount) {
          compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + captureCount]);
        } else {
          const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
          if (extraType !== null) {
            fctx.body.push({ op: "drop" });
          }
        }
      }
      const optInfo = ctx.funcOptionalParams.get(funcName);
      if (optInfo) {
        const numProvided = expr.arguments.length;
        for (const opt of optInfo) {
          if (opt.index >= numProvided) {
            pushParamSentinel(fctx, opt.type, ctx, opt);
          }
        }
      }
      if (paramTypes) {
        const providedCount = Math.min(expr.arguments.length, paramCount) + captureCount;
        const optFilledCount = optInfo ? optInfo.filter((o) => o.index >= expr.arguments.length).length : 0;
        const totalPushed = providedCount + optFilledCount;
        for (let i = totalPushed; i < paramTypes.length; i++) {
          pushDefaultValue(fctx, paramTypes[i], ctx);
        }
      }
    }
    const finalFuncIdx = ctx.funcMap.get(funcName) ?? funcIdx;
    fctx.body.push({ op: "call", funcIdx: finalFuncIdx });
    const sig = ctx.checker.getResolvedSignature(expr);
    if (sig) {
      const retType = ctx.checker.getReturnTypeOfSignature(sig);
      if (isEffectivelyVoidReturn(ctx, retType, funcName)) {
        if (funcName && ctx.asyncFunctions.has(funcName)) {
          let resolveIdx = ctx.funcMap.get("Promise_resolve");
          if (resolveIdx === void 0) {
            resolveIdx = ensureLateImport2(ctx, "Promise_resolve", [{ kind: "externref" }], [{ kind: "externref" }]);
            flushLateImportShifts2(ctx, fctx);
            resolveIdx = ctx.funcMap.get("Promise_resolve") ?? resolveIdx;
          }
          if (resolveIdx !== void 0) {
            fctx.body.push({ op: "ref.null.extern" });
            fctx.body.push({ op: "call", funcIdx: resolveIdx });
            return { kind: "externref" };
          }
        }
        return VOID_RESULT;
      }
      if (wasmFuncReturnsVoid(ctx, finalFuncIdx)) return VOID_RESULT;
      return getWasmFuncReturnType(ctx, finalFuncIdx) ?? resolveWasmType(ctx, retType);
    }
    return getWasmFuncReturnType(ctx, finalFuncIdx) ?? { kind: "f64" };
  }
  {
    let callee = expr.expression;
    while (ts13.isParenthesizedExpression(callee)) {
      callee = callee.expression;
    }
    if (ts13.isFunctionExpression(callee) || ts13.isArrowFunction(callee)) {
      const isGeneratorIIFE = ts13.isFunctionExpression(callee) && callee.asteriskToken !== void 0;
      if (isGeneratorIIFE) {
        const closureType = compileArrowFunction(ctx, fctx, callee);
        if (closureType && (closureType.kind === "ref" || closureType.kind === "ref_null")) {
          const typeIdx = closureType.typeIdx;
          const closureInfo = ctx.closureInfoByTypeIdx.get(typeIdx);
          if (closureInfo) {
            const tmpName = `__gen_iife_${fctx.locals.length}`;
            const tmpLocal = allocLocal(fctx, tmpName, closureType);
            fctx.body.push({ op: "local.set", index: tmpLocal });
            fctx.localMap.set(tmpName, tmpLocal);
            return compileClosureCall(ctx, fctx, expr, tmpName, closureInfo);
          }
        }
        if (closureType) {
          fctx.body.push({ op: "drop" });
        }
      } else {
        const params = callee.parameters;
        const args = expr.arguments;
        const iifeNeedsArguments = ts13.isFunctionExpression(callee) && callee.body && usesArguments(callee.body);
        if (params.length <= args.length) {
          const paramLocals = [];
          const allArgLocals = [];
          for (let i = 0; i < params.length; i++) {
            const param = params[i];
            const paramName = ts13.isIdentifier(param.name) ? param.name.text : `__iife_p${i}`;
            const argType = compileExpression2(ctx, fctx, args[i]);
            const localType = argType ?? { kind: "f64" };
            const idx = allocLocal(fctx, paramName, localType);
            fctx.body.push({ op: "local.set", index: idx });
            paramLocals.push(idx);
            if (iifeNeedsArguments) {
              allArgLocals.push({ idx, type: localType });
            }
          }
          if (iifeNeedsArguments) {
            for (let i = params.length; i < args.length; i++) {
              const t = compileExpression2(ctx, fctx, args[i]);
              const localType = t ?? { kind: "f64" };
              if (t === null) {
                fctx.body.push({ op: "f64.const", value: 0 });
              }
              const idx = allocLocal(fctx, `__iife_extra_${i}`, localType);
              fctx.body.push({ op: "local.set", index: idx });
              allArgLocals.push({ idx, type: localType });
            }
          } else {
            for (let i = params.length; i < args.length; i++) {
              const t = compileExpression2(ctx, fctx, args[i]);
              if (t) {
                fctx.body.push({ op: "drop" });
              }
            }
          }
          if (iifeNeedsArguments && allArgLocals.length > 0) {
            const hasNumeric = allArgLocals.some((a) => a.type.kind === "f64" || a.type.kind === "i32");
            if (hasNumeric) {
              ensureLateImport2(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
              flushLateImportShifts2(ctx, fctx);
            }
            const vti = getOrRegisterVecType(ctx, "externref", { kind: "externref" });
            const ati = getArrTypeIdxFromVec(ctx, vti);
            const vecRef = { kind: "ref", typeIdx: vti };
            const argsLocal = allocLocal(fctx, "arguments", vecRef);
            const arrTmp = allocLocal(fctx, "__iife_args_arr", { kind: "ref", typeIdx: ati });
            for (const { idx, type } of allArgLocals) {
              fctx.body.push({ op: "local.get", index: idx });
              if (type.kind === "f64") {
                const boxIdx = ctx.funcMap.get("__box_number");
                if (boxIdx !== void 0) {
                  fctx.body.push({ op: "call", funcIdx: boxIdx });
                } else {
                  fctx.body.push({ op: "drop" });
                  fctx.body.push({ op: "ref.null.extern" });
                }
              } else if (type.kind === "i32") {
                fctx.body.push({ op: "f64.convert_i32_s" });
                const boxIdx = ctx.funcMap.get("__box_number");
                if (boxIdx !== void 0) {
                  fctx.body.push({ op: "call", funcIdx: boxIdx });
                } else {
                  fctx.body.push({ op: "drop" });
                  fctx.body.push({ op: "ref.null.extern" });
                }
              } else if (type.kind === "ref" || type.kind === "ref_null") {
                fctx.body.push({ op: "extern.convert_any" });
              }
            }
            fctx.body.push({ op: "array.new_fixed", typeIdx: ati, length: allArgLocals.length });
            fctx.body.push({ op: "local.set", index: arrTmp });
            fctx.body.push({ op: "i32.const", value: allArgLocals.length });
            fctx.body.push({ op: "local.get", index: arrTmp });
            fctx.body.push({ op: "struct.new", typeIdx: vti });
            fctx.body.push({ op: "local.set", index: argsLocal });
          } else if (iifeNeedsArguments) {
            const vti = getOrRegisterVecType(ctx, "externref", { kind: "externref" });
            const ati = getArrTypeIdxFromVec(ctx, vti);
            const vecRef = { kind: "ref", typeIdx: vti };
            const argsLocal = allocLocal(fctx, "arguments", vecRef);
            const arrTmp = allocLocal(fctx, "__iife_args_arr", { kind: "ref", typeIdx: ati });
            fctx.body.push({ op: "array.new_fixed", typeIdx: ati, length: 0 });
            fctx.body.push({ op: "local.set", index: arrTmp });
            fctx.body.push({ op: "i32.const", value: 0 });
            fctx.body.push({ op: "local.get", index: arrTmp });
            fctx.body.push({ op: "struct.new", typeIdx: vti });
            fctx.body.push({ op: "local.set", index: argsLocal });
          }
          if (ts13.isArrowFunction(callee) && !ts13.isBlock(callee.body)) {
            return compileExpression2(ctx, fctx, callee.body);
          }
          const bodyStmts = ts13.isArrowFunction(callee) ? callee.body.statements : callee.body.statements;
          if (bodyStmts.length === 0) {
            return VOID_RESULT;
          }
          const iifeRetType = ctx.checker.getTypeAtLocation(expr);
          const iifeWasmRetType = isVoidType(iifeRetType) ? null : resolveWasmType(ctx, iifeRetType);
          if (iifeWasmRetType) {
            let patchReturns2 = function (instrs, depth) {
              for (let i = 0; i < instrs.length; i++) {
                const op = instrs[i].op;
                if (op === "return") {
                  instrs[i] = { op: "local.set", index: retLocal };
                  instrs.splice(i + 1, 0, { op: "br", depth });
                  i++;
                } else if (op === "return_call" || op === "return_call_ref") {
                  const instr2 = instrs[i];
                  instr2.op = op === "return_call" ? "call" : "call_ref";
                  instrs.splice(i + 1, 0, { op: "local.set", index: retLocal }, { op: "br", depth });
                  i += 2;
                }
                const instr = instrs[i];
                if (instr.then) patchReturns2(instr.then, depth + 1);
                if (instr.else) patchReturns2(instr.else, depth + 1);
                if (instr.body && Array.isArray(instr.body)) patchReturns2(instr.body, depth + 1);
              }
            };
            var patchReturns = patchReturns2;
            const retLocal = allocLocal(fctx, `__iife_ret_${fctx.locals.length}`, iifeWasmRetType);
            const savedBody = fctx.body;
            fctx.savedBodies.push(savedBody);
            const blockBody = [];
            fctx.body = blockBody;
            const savedReturnType = fctx.returnType;
            fctx.returnType = iifeWasmRetType;
            hoistLetConstWithTdz(ctx, fctx, bodyStmts);
            hoistFunctionDeclarations(ctx, fctx, bodyStmts);
            fctx.blockDepth++;
            for (const stmt of bodyStmts) {
              compileStatement(ctx, fctx, stmt);
            }
            fctx.blockDepth--;
            fctx.returnType = savedReturnType;
            fctx.savedBodies.pop();
            fctx.body = savedBody;
            patchReturns2(blockBody, 0);
            fctx.body.push({
              op: "block",
              blockType: { kind: "empty" },
              body: blockBody,
            });
            fctx.body.push({ op: "local.get", index: retLocal });
            return iifeWasmRetType;
          } else {
            hoistLetConstWithTdz(ctx, fctx, bodyStmts);
            hoistFunctionDeclarations(ctx, fctx, bodyStmts);
            for (const stmt of bodyStmts) {
              compileStatement(ctx, fctx, stmt);
            }
            return VOID_RESULT;
          }
        }
      }
    }
  }
  if (expr.expression.kind === ts13.SyntaxKind.SuperKeyword) {
    return null;
  }
  {
    const iifeResult = compileIIFE(ctx, fctx, expr);
    if (iifeResult !== void 0) return iifeResult;
  }
  {
    let callee = expr.expression;
    while (ts13.isParenthesizedExpression(callee)) {
      callee = callee.expression;
    }
    if (ts13.isBinaryExpression(callee) && callee.operatorToken.kind === ts13.SyntaxKind.CommaToken) {
      const leftType = compileExpression2(ctx, fctx, callee.left);
      if (leftType) {
        fctx.body.push({ op: "drop" });
      }
      const syntheticCall = ts13.factory.createCallExpression(callee.right, expr.typeArguments, expr.arguments);
      ts13.setTextRange(syntheticCall, expr);
      syntheticCall.parent = expr.parent;
      return compileCallExpression(ctx, fctx, syntheticCall);
    }
  }
  if (ts13.isElementAccessExpression(expr.expression)) {
    const elemAccess = expr.expression;
    const argExpr = elemAccess.argumentExpression;
    let resolvedMethodName;
    if (argExpr) {
      if (ts13.isStringLiteral(argExpr)) {
        resolvedMethodName = argExpr.text;
      } else if (ts13.isNumericLiteral(argExpr)) {
        resolvedMethodName = String(Number(argExpr.text));
      } else {
        resolvedMethodName = resolveComputedKeyExpression(ctx, argExpr);
      }
    }
    if (elemAccess.expression.kind === ts13.SyntaxKind.SuperKeyword && resolvedMethodName !== void 0) {
      return compileSuperElementMethodCall(ctx, fctx, expr, resolvedMethodName);
    }
    if (resolvedMethodName !== void 0) {
      const methodName = resolvedMethodName;
      const receiverType = ctx.checker.getTypeAtLocation(elemAccess.expression);
      let receiverClassName = receiverType.getSymbol()?.name;
      if (receiverClassName && !ctx.classSet.has(receiverClassName)) {
        receiverClassName = ctx.classExprNameMap.get(receiverClassName) ?? receiverClassName;
      }
      if (receiverClassName && ctx.classSet.has(receiverClassName)) {
        const fullName = `${receiverClassName}_${methodName}`;
        const funcIdx = ctx.funcMap.get(fullName);
        if (funcIdx !== void 0) {
          compileExpression2(ctx, fctx, elemAccess.expression);
          const paramTypes = getFuncParamTypes(ctx, funcIdx);
          const eaMethodParamCount = paramTypes ? paramTypes.length - 1 : expr.arguments.length;
          for (let i = 0; i < expr.arguments.length; i++) {
            if (i < eaMethodParamCount) {
              compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
            } else {
              const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
              if (extraType !== null) {
                fctx.body.push({ op: "drop" });
              }
            }
          }
          if (paramTypes) {
            for (let i = Math.min(expr.arguments.length, eaMethodParamCount) + 1; i < paramTypes.length; i++) {
              pushDefaultValue(fctx, paramTypes[i], ctx);
            }
          }
          fctx.body.push({ op: "call", funcIdx });
          const sig = ctx.checker.getResolvedSignature(expr);
          if (sig) {
            const retType = ctx.checker.getReturnTypeOfSignature(sig);
            if (isEffectivelyVoidReturn(ctx, retType, fullName)) return VOID_RESULT;
            if (wasmFuncReturnsVoid(ctx, funcIdx)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, funcIdx) ?? resolveWasmType(ctx, retType);
          }
          return VOID_RESULT;
        }
      }
      const structTypeName = resolveStructName(ctx, receiverType);
      if (structTypeName) {
        const fullName = `${structTypeName}_${methodName}`;
        const funcIdx = ctx.funcMap.get(fullName);
        if (funcIdx !== void 0) {
          const recvType = compileExpression2(ctx, fctx, elemAccess.expression);
          const eaReceiverWasCast = fctx.__lastGuardedCastBackup !== void 0;
          if (recvType && recvType.kind === "ref_null") {
            const sig2 = ctx.checker.getResolvedSignature(expr);
            let callReturnType = VOID_RESULT;
            if (sig2) {
              const retType = ctx.checker.getReturnTypeOfSignature(sig2);
              if (!isEffectivelyVoidReturn(ctx, retType, fullName))
                callReturnType = getWasmFuncReturnType(ctx, funcIdx) ?? resolveWasmType(ctx, retType);
            }
            const tmp = allocLocal(fctx, `__ng_ea_recv_${fctx.locals.length}`, recvType);
            fctx.body.push({ op: "local.tee", index: tmp });
            fctx.body.push({ op: "ref.is_null" });
            const savedBody = pushBody(fctx);
            fctx.body.push({ op: "local.get", index: tmp });
            fctx.body.push({ op: "ref.as_non_null" });
            const paramTypes2 = getFuncParamTypes(ctx, funcIdx);
            const eaNgParamCount = paramTypes2 ? paramTypes2.length - 1 : expr.arguments.length;
            for (let i = 0; i < expr.arguments.length; i++) {
              if (i < eaNgParamCount) {
                compileExpression2(ctx, fctx, expr.arguments[i], paramTypes2?.[i + 1]);
              } else {
                const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
                if (extraType !== null) {
                  fctx.body.push({ op: "drop" });
                }
              }
            }
            if (paramTypes2) {
              for (let i = Math.min(expr.arguments.length, eaNgParamCount) + 1; i < paramTypes2.length; i++) {
                pushDefaultValue(fctx, paramTypes2[i], ctx);
              }
            }
            fctx.body.push({ op: "call", funcIdx });
            const elseInstrs = fctx.body;
            fctx.body = savedBody;
            if (callReturnType === VOID_RESULT) {
              fctx.body.push({
                op: "if",
                blockType: { kind: "empty" },
                then: eaReceiverWasCast ? [] : typeErrorThrowInstrs(ctx),
                else: elseInstrs,
              });
              return VOID_RESULT;
            } else {
              const resultType =
                callReturnType.kind === "ref"
                  ? {
                      kind: "ref_null",
                      typeIdx: callReturnType.typeIdx,
                    }
                  : callReturnType;
              fctx.body.push({
                op: "if",
                blockType: { kind: "val", type: resultType },
                then: eaReceiverWasCast ? defaultValueInstrs(resultType) : typeErrorThrowInstrs(ctx),
                else: elseInstrs,
              });
              return resultType;
            }
          }
          const paramTypes = getFuncParamTypes(ctx, funcIdx);
          const eaNnParamCount = paramTypes ? paramTypes.length - 1 : expr.arguments.length;
          for (let i = 0; i < expr.arguments.length; i++) {
            if (i < eaNnParamCount) {
              compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
            } else {
              const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
              if (extraType !== null) {
                fctx.body.push({ op: "drop" });
              }
            }
          }
          if (paramTypes) {
            for (let i = Math.min(expr.arguments.length, eaNnParamCount) + 1; i < paramTypes.length; i++) {
              pushDefaultValue(fctx, paramTypes[i], ctx);
            }
          }
          fctx.body.push({ op: "call", funcIdx });
          const sig = ctx.checker.getResolvedSignature(expr);
          if (sig) {
            const retType = ctx.checker.getReturnTypeOfSignature(sig);
            if (isEffectivelyVoidReturn(ctx, retType, fullName)) return VOID_RESULT;
            if (wasmFuncReturnsVoid(ctx, funcIdx)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, funcIdx) ?? resolveWasmType(ctx, retType);
          }
          return VOID_RESULT;
        }
      }
      if (ts13.isIdentifier(elemAccess.expression) && ctx.classSet.has(elemAccess.expression.text)) {
        const clsName = elemAccess.expression.text;
        const fullName = `${clsName}_${methodName}`;
        if (ctx.staticMethodSet.has(fullName)) {
          const funcIdx = ctx.funcMap.get(fullName);
          if (funcIdx !== void 0) {
            const paramTypes = getFuncParamTypes(ctx, funcIdx);
            const eaStaticParamCount = paramTypes ? paramTypes.length : expr.arguments.length;
            for (let i = 0; i < expr.arguments.length; i++) {
              if (i < eaStaticParamCount) {
                compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i]);
              } else {
                const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
                if (extraType !== null) {
                  fctx.body.push({ op: "drop" });
                }
              }
            }
            if (paramTypes) {
              for (let i = expr.arguments.length; i < paramTypes.length; i++) {
                pushDefaultValue(fctx, paramTypes[i], ctx);
              }
            }
            fctx.body.push({ op: "call", funcIdx });
            const sig = ctx.checker.getResolvedSignature(expr);
            if (sig) {
              const retType = ctx.checker.getReturnTypeOfSignature(sig);
              if (isEffectivelyVoidReturn(ctx, retType, fullName)) return VOID_RESULT;
              if (wasmFuncReturnsVoid(ctx, funcIdx)) return VOID_RESULT;
              return getWasmFuncReturnType(ctx, funcIdx) ?? resolveWasmType(ctx, retType);
            }
            return VOID_RESULT;
          }
        }
      }
      if (isStringType(receiverType)) {
        const importName = `string_${methodName}`;
        const funcIdx = ctx.funcMap.get(importName);
        if (funcIdx !== void 0) {
          compileExpression2(ctx, fctx, elemAccess.expression);
          const paramTypes = getFuncParamTypes(ctx, funcIdx);
          const args = expr.arguments;
          for (let ai = 0; ai < args.length; ai++) {
            const argResult = compileExpression2(ctx, fctx, args[ai]);
            const expectedType = paramTypes?.[ai + 1];
            if (argResult && expectedType && argResult.kind !== expectedType.kind) {
              coerceType3(ctx, fctx, argResult, expectedType);
            }
          }
          if (paramTypes && args.length + 1 < paramTypes.length) {
            for (let pi = args.length + 1; pi < paramTypes.length; pi++) {
              const pt = paramTypes[pi];
              if (pt.kind === "externref") fctx.body.push({ op: "ref.null.extern" });
              else if (pt.kind === "f64") fctx.body.push({ op: "f64.const", value: 0 });
              else if (pt.kind === "i32") fctx.body.push({ op: "i32.const", value: 0 });
            }
          }
          fctx.body.push({ op: "call", funcIdx });
          const returnsBool = methodName === "includes" || methodName === "startsWith" || methodName === "endsWith";
          return returnsBool
            ? { kind: "i32" }
            : methodName === "indexOf" || methodName === "lastIndexOf" || methodName === "search"
              ? { kind: "f64" }
              : { kind: "externref" };
        }
      }
      if (
        isNumberType(receiverType) &&
        (methodName === "toString" ||
          methodName === "toFixed" ||
          methodName === "toPrecision" ||
          methodName === "toExponential")
      ) {
        if (methodName === "toString" && expr.arguments.length > 0) {
          compileExpression2(ctx, fctx, expr.arguments[0], { kind: "f64" });
          fctx.body.push({ op: "f64.floor" });
          const radixLocal = allocLocal(fctx, `__radix_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: radixLocal });
          fctx.body.push({ op: "f64.const", value: 2 });
          fctx.body.push({ op: "f64.lt" });
          fctx.body.push({ op: "local.get", index: radixLocal });
          fctx.body.push({ op: "f64.const", value: 36 });
          fctx.body.push({ op: "f64.gt" });
          fctx.body.push({ op: "i32.or" });
          fctx.body.push({ op: "local.get", index: radixLocal });
          fctx.body.push({ op: "local.get", index: radixLocal });
          fctx.body.push({ op: "f64.ne" });
          fctx.body.push({ op: "i32.or" });
          {
            const rangeErrMsg = "RangeError: toString() radix must be between 2 and 36";
            addStringConstantGlobal(ctx, rangeErrMsg);
            const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
            const tagIdx = ensureExnTag(ctx);
            fctx.body.push({
              op: "if",
              blockType: { kind: "empty" },
              then: [
                { op: "global.get", index: strIdx },
                { op: "throw", tagIdx },
              ],
              else: [],
            });
          }
        }
        const exprType = compileExpression2(ctx, fctx, elemAccess.expression);
        if (exprType && exprType.kind === "i32") {
          fctx.body.push({ op: "f64.convert_i32_s" });
        }
        if (methodName === "toFixed" && expr.arguments.length > 0) {
          compileExpression2(ctx, fctx, expr.arguments[0]);
          const digitsLocal = allocLocal(fctx, `__toFixed_digits_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: digitsLocal });
          fctx.body.push({ op: "f64.const", value: 0 });
          fctx.body.push({ op: "f64.lt" });
          fctx.body.push({ op: "local.get", index: digitsLocal });
          fctx.body.push({ op: "f64.const", value: 100 });
          fctx.body.push({ op: "f64.gt" });
          fctx.body.push({ op: "i32.or" });
          {
            const rangeErrMsg = "RangeError: toFixed() digits argument must be between 0 and 100";
            addStringConstantGlobal(ctx, rangeErrMsg);
            const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
            const tagIdx = ensureExnTag(ctx);
            fctx.body.push({
              op: "if",
              blockType: { kind: "empty" },
              then: [
                { op: "global.get", index: strIdx },
                { op: "throw", tagIdx },
              ],
              else: [],
            });
          }
          fctx.body.push({ op: "local.get", index: digitsLocal });
        } else if (methodName === "toFixed") {
          fctx.body.push({ op: "f64.const", value: 0 });
        }
        if (methodName === "toPrecision" && expr.arguments.length > 0) {
          compileExpression2(ctx, fctx, expr.arguments[0]);
          const precLocal = allocLocal(fctx, `__toPrecision_prec_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: precLocal });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: "f64.lt" });
          fctx.body.push({ op: "local.get", index: precLocal });
          fctx.body.push({ op: "f64.const", value: 100 });
          fctx.body.push({ op: "f64.gt" });
          fctx.body.push({ op: "i32.or" });
          fctx.body.push({ op: "local.get", index: precLocal });
          fctx.body.push({ op: "local.get", index: precLocal });
          fctx.body.push({ op: "f64.ne" });
          fctx.body.push({ op: "i32.or" });
          {
            const rangeErrMsg = "RangeError: toPrecision() argument must be between 1 and 100";
            addStringConstantGlobal(ctx, rangeErrMsg);
            const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
            const tagIdx = ensureExnTag(ctx);
            fctx.body.push({
              op: "if",
              blockType: { kind: "empty" },
              then: [
                { op: "global.get", index: strIdx },
                { op: "throw", tagIdx },
              ],
              else: [],
            });
          }
          fctx.body.push({ op: "local.get", index: precLocal });
        } else if (methodName === "toPrecision") {
          const funcIdx2 = ctx.funcMap.get("number_toString");
          if (funcIdx2 !== void 0) {
            fctx.body.push({ op: "call", funcIdx: funcIdx2 });
            return { kind: "externref" };
          }
        }
        if (methodName === "toExponential" && expr.arguments.length > 0) {
          compileExpression2(ctx, fctx, expr.arguments[0]);
          const digitsLocal2 = allocLocal(fctx, `__toExponential_digits_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: digitsLocal2 });
          fctx.body.push({ op: "f64.const", value: 0 });
          fctx.body.push({ op: "f64.lt" });
          fctx.body.push({ op: "local.get", index: digitsLocal2 });
          fctx.body.push({ op: "f64.const", value: 100 });
          fctx.body.push({ op: "f64.gt" });
          fctx.body.push({ op: "i32.or" });
          {
            const rangeErrMsg = "RangeError: toExponential() argument must be between 0 and 100";
            addStringConstantGlobal(ctx, rangeErrMsg);
            const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
            const tagIdx = ensureExnTag(ctx);
            fctx.body.push({
              op: "if",
              blockType: { kind: "empty" },
              then: [
                { op: "global.get", index: strIdx },
                { op: "throw", tagIdx },
              ],
              else: [],
            });
          }
          fctx.body.push({ op: "local.get", index: digitsLocal2 });
        } else if (methodName === "toExponential") {
          fctx.body.push({ op: "f64.const", value: NaN });
        }
        const funcName =
          methodName === "toFixed"
            ? "number_toFixed"
            : methodName === "toPrecision"
              ? "number_toPrecision"
              : methodName === "toExponential"
                ? "number_toExponential"
                : "number_toString";
        const funcIdx = ctx.funcMap.get(funcName);
        if (funcIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx });
          return { kind: "externref" };
        }
      }
      {
        const arrMethodResult = compileArrayMethodCall(ctx, fctx, elemAccess, expr, receiverType, methodName);
        if (arrMethodResult !== void 0) return arrMethodResult;
      }
      {
        const recvType = compileExpression2(ctx, fctx, elemAccess.expression);
        if (recvType) {
          fctx.body.push({ op: "drop" });
        }
        for (const arg of expr.arguments) {
          const argType = compileExpression2(ctx, fctx, arg);
          if (argType) {
            fctx.body.push({ op: "drop" });
          }
        }
        fctx.body.push({ op: "ref.null.extern" });
        return { kind: "externref" };
      }
    }
    {
      const recvType = compileExpression2(ctx, fctx, elemAccess.expression);
      if (recvType) {
        fctx.body.push({ op: "drop" });
      }
      if (argExpr) {
        const keyType = compileExpression2(ctx, fctx, argExpr);
        if (keyType) {
          fctx.body.push({ op: "drop" });
        }
      }
      for (const arg of expr.arguments) {
        const argType = compileExpression2(ctx, fctx, arg);
        if (argType) {
          fctx.body.push({ op: "drop" });
        }
      }
      fctx.body.push({ op: "ref.null.extern" });
      return { kind: "externref" };
    }
  }
  if (ts13.isCallExpression(expr.expression)) {
    const bindCall = expr.expression;
    if (ts13.isPropertyAccessExpression(bindCall.expression) && bindCall.expression.name.text === "bind") {
      const bindTarget = bindCall.expression.expression;
      if (ts13.isIdentifier(bindTarget)) {
        const funcName = bindTarget.text;
        const closureInfo = ctx.closureMap.get(funcName);
        const funcIdx = ctx.funcMap.get(funcName);
        if (closureInfo || funcIdx !== void 0) {
          if (bindCall.arguments.length > 0) {
            const thisType = compileExpression2(ctx, fctx, bindCall.arguments[0]);
            if (thisType) {
              fctx.body.push({ op: "drop" });
            }
          }
          const partialArgs = bindCall.arguments.length > 1 ? Array.from(bindCall.arguments).slice(1) : [];
          const allArgs = [...partialArgs, ...Array.from(expr.arguments)];
          if (closureInfo) {
            const syntheticCall = ts13.factory.createCallExpression(bindTarget, void 0, allArgs);
            syntheticCall.parent = expr.parent;
            return compileClosureCall(ctx, fctx, syntheticCall, funcName, closureInfo);
          }
          const paramTypes = getFuncParamTypes(ctx, funcIdx);
          for (let i = 0; i < allArgs.length; i++) {
            compileExpression2(ctx, fctx, allArgs[i], paramTypes?.[i]);
          }
          const optInfo = ctx.funcOptionalParams.get(funcName);
          if (optInfo) {
            for (const opt of optInfo) {
              if (opt.index >= allArgs.length) {
                pushParamSentinel(fctx, opt.type, ctx, opt);
              }
            }
          }
          if (paramTypes) {
            const optFilledCount = optInfo ? optInfo.filter((o) => o.index >= allArgs.length).length : 0;
            const totalPushed = allArgs.length + optFilledCount;
            for (let i = totalPushed; i < paramTypes.length; i++) {
              pushDefaultValue(fctx, paramTypes[i], ctx);
            }
          }
          const finalFuncIdx = ctx.funcMap.get(funcName) ?? funcIdx;
          fctx.body.push({ op: "call", funcIdx: finalFuncIdx });
          const sig = ctx.checker.getResolvedSignature(expr);
          if (sig) {
            const retType = ctx.checker.getReturnTypeOfSignature(sig);
            if (isEffectivelyVoidReturn(ctx, retType, funcName)) return VOID_RESULT;
            if (wasmFuncReturnsVoid(ctx, finalFuncIdx)) return VOID_RESULT;
            return getWasmFuncReturnType(ctx, finalFuncIdx) ?? resolveWasmType(ctx, retType);
          }
          return getWasmFuncReturnType(ctx, finalFuncIdx) ?? { kind: "f64" };
        }
      }
      if (ts13.isPropertyAccessExpression(bindTarget)) {
        const methodName = bindTarget.name.text;
        const objExpr = bindTarget.expression;
        const objType = ctx.checker.getTypeAtLocation(objExpr);
        let className = objType.getSymbol()?.name;
        if (className && !ctx.classSet.has(className)) {
          className = ctx.classExprNameMap.get(className) ?? className;
        }
        if (!className || !ctx.classSet.has(className)) {
          className = resolveStructName(ctx, objType) ?? void 0;
        }
        if (className && (ctx.classSet.has(className) || ctx.funcMap.has(`${className}_${methodName}`))) {
          const fullName = `${className}_${methodName}`;
          const funcIdx = ctx.funcMap.get(fullName);
          if (funcIdx !== void 0 && bindCall.arguments.length > 0) {
            compileExpression2(ctx, fctx, bindCall.arguments[0]);
            const partialArgs = bindCall.arguments.length > 1 ? Array.from(bindCall.arguments).slice(1) : [];
            const allArgs = [...partialArgs, ...Array.from(expr.arguments)];
            const paramTypes = getFuncParamTypes(ctx, funcIdx);
            const bindParamCount = paramTypes ? paramTypes.length - 1 : allArgs.length;
            for (let i = 0; i < allArgs.length; i++) {
              if (i < bindParamCount) {
                compileExpression2(ctx, fctx, allArgs[i], paramTypes?.[i + 1]);
              } else {
                const extraType = compileExpression2(ctx, fctx, allArgs[i]);
                if (extraType !== null) {
                  fctx.body.push({ op: "drop" });
                }
              }
            }
            if (paramTypes) {
              for (let i = allArgs.length + 1; i < paramTypes.length; i++) {
                pushDefaultValue(fctx, paramTypes[i], ctx);
              }
            }
            const finalCallIdx = ctx.funcMap.get(fullName) ?? funcIdx;
            fctx.body.push({ op: "call", funcIdx: finalCallIdx });
            const sig = ctx.checker.getResolvedSignature(expr);
            if (sig) {
              const retType = ctx.checker.getReturnTypeOfSignature(sig);
              if (isEffectivelyVoidReturn(ctx, retType, fullName)) return VOID_RESULT;
              if (wasmFuncReturnsVoid(ctx, finalCallIdx)) return VOID_RESULT;
              return getWasmFuncReturnType(ctx, finalCallIdx) ?? resolveWasmType(ctx, retType);
            }
            return VOID_RESULT;
          }
        }
      }
    }
  }
  if (ts13.isCallExpression(expr.expression)) {
    const innerResultTsType = ctx.checker.getTypeAtLocation(expr.expression);
    const callSigs = innerResultTsType.getCallSignatures?.();
    if (callSigs && callSigs.length > 0) {
      const sig = callSigs[0];
      let matchedClosureInfo;
      let matchedStructTypeIdx;
      const sigParamCount = sig.parameters.length;
      const sigRetType = ctx.checker.getReturnTypeOfSignature(sig);
      const sigRetWasm = isVoidType(sigRetType) ? null : resolveWasmType(ctx, sigRetType);
      const sigParamWasmTypes = [];
      for (let i = 0; i < sigParamCount; i++) {
        const paramType = ctx.checker.getTypeOfSymbol(sig.parameters[i]);
        sigParamWasmTypes.push(resolveWasmType(ctx, paramType));
      }
      for (const [typeIdx, info] of ctx.closureInfoByTypeIdx) {
        if (info.paramTypes.length !== sigParamCount) continue;
        if (sigRetWasm === null && info.returnType !== null) continue;
        if (sigRetWasm !== null && info.returnType === null) continue;
        if (sigRetWasm !== null && info.returnType !== null && sigRetWasm.kind !== info.returnType.kind) continue;
        let paramsMatch = true;
        for (let i = 0; i < sigParamCount; i++) {
          if (sigParamWasmTypes[i].kind !== info.paramTypes[i].kind) {
            paramsMatch = false;
            break;
          }
        }
        if (paramsMatch) {
          matchedClosureInfo = info;
          matchedStructTypeIdx = typeIdx;
          break;
        }
      }
      if (matchedClosureInfo && matchedStructTypeIdx !== void 0) {
        const innerResultType = compileExpression2(ctx, fctx, expr.expression);
        let closureLocal;
        if (innerResultType?.kind === "externref") {
          const closureRefType = {
            kind: "ref_null",
            typeIdx: matchedStructTypeIdx,
          };
          closureLocal = allocLocal(fctx, `__call_ret_${fctx.locals.length}`, closureRefType);
          fctx.body.push({ op: "any.convert_extern" });
          emitGuardedRefCast(fctx, matchedStructTypeIdx);
          fctx.body.push({ op: "local.set", index: closureLocal });
        } else {
          const closureRefType = innerResultType ?? {
            kind: "ref",
            typeIdx: matchedStructTypeIdx,
          };
          closureLocal = allocLocal(fctx, `__call_ret_${fctx.locals.length}`, closureRefType);
          fctx.body.push({ op: "local.set", index: closureLocal });
        }
        fctx.body.push({ op: "local.get", index: closureLocal });
        emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedStructTypeIdx });
        {
          const crParamCnt = matchedClosureInfo.paramTypes.length;
          for (let i = 0; i < Math.min(expr.arguments.length, crParamCnt); i++) {
            compileExpression2(ctx, fctx, expr.arguments[i], matchedClosureInfo.paramTypes[i]);
          }
          for (let i = crParamCnt; i < expr.arguments.length; i++) {
            const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
            if (extraType !== null) {
              fctx.body.push({ op: "drop" });
            }
          }
        }
        for (let i = expr.arguments.length; i < matchedClosureInfo.paramTypes.length; i++) {
          pushDefaultValue(fctx, matchedClosureInfo.paramTypes[i], ctx);
        }
        fctx.body.push({ op: "local.get", index: closureLocal });
        emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedStructTypeIdx });
        fctx.body.push({
          op: "struct.get",
          typeIdx: matchedStructTypeIdx,
          fieldIdx: 0,
        });
        emitGuardedFuncRefCast(fctx, matchedClosureInfo.funcTypeIdx);
        emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedClosureInfo.funcTypeIdx });
        fctx.body.push({
          op: "call_ref",
          typeIdx: matchedClosureInfo.funcTypeIdx,
        });
        return matchedClosureInfo.returnType ?? VOID_RESULT;
      }
    }
  }
  if (ts13.isConditionalExpression(expr.expression)) {
    return compileConditionalCallee(ctx, fctx, expr, expr.expression);
  }
  {
    const calleeTsType = ctx.checker.getTypeAtLocation(expr.expression);
    const callSigs = calleeTsType.getCallSignatures?.();
    if (callSigs && callSigs.length > 0) {
      const sig = callSigs[0];
      const sigParamCount = sig.parameters.length;
      const sigRetType = ctx.checker.getReturnTypeOfSignature(sig);
      const sigRetWasm = isVoidType(sigRetType) ? null : resolveWasmType(ctx, sigRetType);
      const sigParamWasmTypes = [];
      for (let i = 0; i < sigParamCount; i++) {
        const paramType = ctx.checker.getTypeOfSymbol(sig.parameters[i]);
        sigParamWasmTypes.push(resolveWasmType(ctx, paramType));
      }
      let matchedClosureInfo;
      let matchedStructTypeIdx;
      for (const [typeIdx, info] of ctx.closureInfoByTypeIdx) {
        if (info.paramTypes.length !== sigParamCount) continue;
        if (sigRetWasm === null && info.returnType !== null) continue;
        if (sigRetWasm !== null && info.returnType === null) continue;
        if (sigRetWasm !== null && info.returnType !== null && sigRetWasm.kind !== info.returnType.kind) continue;
        let paramsMatch = true;
        for (let i = 0; i < sigParamCount; i++) {
          if (sigParamWasmTypes[i].kind !== info.paramTypes[i].kind) {
            paramsMatch = false;
            break;
          }
        }
        if (paramsMatch) {
          matchedClosureInfo = info;
          matchedStructTypeIdx = typeIdx;
          break;
        }
      }
      if (matchedClosureInfo && matchedStructTypeIdx !== void 0) {
        const innerResultType = compileExpression2(ctx, fctx, expr.expression);
        let closureLocal;
        if (innerResultType?.kind === "externref") {
          const closureRefType = {
            kind: "ref_null",
            typeIdx: matchedStructTypeIdx,
          };
          closureLocal = allocLocal(fctx, `__cond_call_${fctx.locals.length}`, closureRefType);
          fctx.body.push({ op: "any.convert_extern" });
          emitGuardedRefCast(fctx, matchedStructTypeIdx);
          fctx.body.push({ op: "local.set", index: closureLocal });
        } else {
          const closureRefType = innerResultType ?? {
            kind: "ref",
            typeIdx: matchedStructTypeIdx,
          };
          closureLocal = allocLocal(fctx, `__cond_call_${fctx.locals.length}`, closureRefType);
          fctx.body.push({ op: "local.set", index: closureLocal });
        }
        fctx.body.push({ op: "local.get", index: closureLocal });
        emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedStructTypeIdx });
        {
          const ccParamCnt = matchedClosureInfo.paramTypes.length;
          for (let i = 0; i < Math.min(expr.arguments.length, ccParamCnt); i++) {
            compileExpression2(ctx, fctx, expr.arguments[i], matchedClosureInfo.paramTypes[i]);
          }
          for (let i = ccParamCnt; i < expr.arguments.length; i++) {
            const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
            if (extraType !== null) {
              fctx.body.push({ op: "drop" });
            }
          }
        }
        for (let i = expr.arguments.length; i < matchedClosureInfo.paramTypes.length; i++) {
          pushDefaultValue(fctx, matchedClosureInfo.paramTypes[i], ctx);
        }
        fctx.body.push({ op: "local.get", index: closureLocal });
        emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedStructTypeIdx });
        fctx.body.push({
          op: "struct.get",
          typeIdx: matchedStructTypeIdx,
          fieldIdx: 0,
        });
        emitGuardedFuncRefCast(fctx, matchedClosureInfo.funcTypeIdx);
        emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedClosureInfo.funcTypeIdx });
        fctx.body.push({
          op: "call_ref",
          typeIdx: matchedClosureInfo.funcTypeIdx,
        });
        return matchedClosureInfo.returnType ?? VOID_RESULT;
      }
    }
  }
  {
    const calleeType = compileExpression2(ctx, fctx, expr.expression);
    if (calleeType) {
      fctx.body.push({ op: "drop" });
    }
    for (const arg of expr.arguments) {
      const argType = compileExpression2(ctx, fctx, arg);
      if (argType) {
        fctx.body.push({ op: "drop" });
      }
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
}
function compileConditionalCallee(ctx, fctx, expr, condExpr) {
  const condType = compileExpression2(ctx, fctx, condExpr.condition);
  if (!condType) {
    fctx.body.push({ op: "i32.const", value: 0 });
  } else {
    ensureI32Condition(fctx, condType, ctx);
  }
  const callSig = ctx.checker.getResolvedSignature(expr);
  let callRetType = null;
  if (callSig) {
    const retTsType = ctx.checker.getReturnTypeOfSignature(callSig);
    if (!isVoidType(retTsType)) {
      callRetType = resolveWasmType(ctx, retTsType);
    }
  }
  function compileBranchCall(branchExpr) {
    if (ts13.isIdentifier(branchExpr)) {
      const funcName = branchExpr.text;
      let closureInfo = ctx.closureMap.get(funcName);
      if (!closureInfo) {
        const localIdx = fctx.localMap.get(funcName);
        if (localIdx !== void 0) {
          const localType =
            localIdx < fctx.params.length
              ? fctx.params[localIdx]?.type
              : fctx.locals[localIdx - fctx.params.length]?.type;
          if (localType && (localType.kind === "ref" || localType.kind === "ref_null")) {
            closureInfo = ctx.closureInfoByTypeIdx.get(localType.typeIdx);
          }
        }
      }
      if (closureInfo) {
        const syntheticCall = Object.create(expr);
        syntheticCall.expression = branchExpr;
        return compileClosureCall(ctx, fctx, syntheticCall, funcName, closureInfo);
      }
      const funcIdx = ctx.funcMap.get(funcName);
      if (funcIdx !== void 0) {
        const paramTypes = getFuncParamTypes(ctx, funcIdx);
        const ccParamCount = paramTypes ? paramTypes.length : expr.arguments.length;
        for (let i = 0; i < expr.arguments.length; i++) {
          if (i < ccParamCount) {
            compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i]);
          } else {
            const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
            if (extraType !== null) {
              fctx.body.push({ op: "drop" });
            }
          }
        }
        if (paramTypes) {
          for (let i = expr.arguments.length; i < paramTypes.length; i++) {
            pushDefaultValue(fctx, paramTypes[i], ctx);
          }
        }
        const finalFuncIdx = ctx.funcMap.get(funcName) ?? funcIdx;
        fctx.body.push({ op: "call", funcIdx: finalFuncIdx });
        if (callRetType) return callRetType;
        const branchType = ctx.checker.getTypeAtLocation(branchExpr);
        const branchSigs = branchType.getCallSignatures?.();
        if (branchSigs && branchSigs.length > 0) {
          const retType = ctx.checker.getReturnTypeOfSignature(branchSigs[0]);
          if (isEffectivelyVoidReturn(ctx, retType, funcName)) return VOID_RESULT;
          if (wasmFuncReturnsVoid(ctx, finalFuncIdx)) return VOID_RESULT;
          return getWasmFuncReturnType(ctx, finalFuncIdx) ?? resolveWasmType(ctx, retType);
        }
        return callRetType ?? getWasmFuncReturnType(ctx, finalFuncIdx) ?? { kind: "f64" };
      }
    }
    if (ts13.isConditionalExpression(branchExpr)) {
      return compileConditionalCallee(ctx, fctx, expr, branchExpr);
    }
    if (ts13.isParenthesizedExpression(branchExpr)) {
      let inner = branchExpr;
      while (ts13.isParenthesizedExpression(inner)) {
        inner = inner.expression;
      }
      return compileBranchCall(inner);
    }
    if (ts13.isPropertyAccessExpression(branchExpr)) {
      const syntheticCall = ts13.factory.createCallExpression(branchExpr, expr.typeArguments, expr.arguments);
      ts13.setTextRange(syntheticCall, expr);
      syntheticCall.parent = expr.parent;
      return compileCallExpression(ctx, fctx, syntheticCall);
    }
    const calleeType = compileExpression2(ctx, fctx, branchExpr);
    if (calleeType) {
      fctx.body.push({ op: "drop" });
    }
    for (const arg of expr.arguments) {
      const argType = compileExpression2(ctx, fctx, arg);
      if (argType) {
        fctx.body.push({ op: "drop" });
      }
    }
    if (callRetType) {
      pushDefaultValue(fctx, callRetType, ctx);
      return callRetType;
    }
    fctx.body.push({ op: "f64.const", value: 0 });
    return { kind: "f64" };
  }
  const savedBody = fctx.body;
  fctx.body = [];
  const thenType = compileBranchCall(condExpr.whenTrue);
  let thenInstrs = fctx.body;
  fctx.body = [];
  const elseType = compileBranchCall(condExpr.whenFalse);
  let elseInstrs = fctx.body;
  fctx.body = savedBody;
  if (thenType === VOID_RESULT && elseType === VOID_RESULT) {
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: thenInstrs,
      else: elseInstrs,
    });
    return VOID_RESULT;
  }
  const thenVal = thenType && thenType !== VOID_RESULT ? thenType : (callRetType ?? { kind: "f64" });
  const elseVal = elseType && elseType !== VOID_RESULT ? elseType : (callRetType ?? { kind: "f64" });
  let resultType = callRetType ?? thenVal;
  if (thenVal.kind !== resultType.kind) {
    const coerceBody = [];
    fctx.body = coerceBody;
    coerceType3(ctx, fctx, thenVal, resultType);
    fctx.body = savedBody;
    thenInstrs = [...thenInstrs, ...coerceBody];
  }
  if (elseVal.kind !== resultType.kind) {
    const coerceBody = [];
    fctx.body = coerceBody;
    coerceType3(ctx, fctx, elseVal, resultType);
    fctx.body = savedBody;
    elseInstrs = [...elseInstrs, ...coerceBody];
  }
  if (thenType === VOID_RESULT || thenType === null) {
    thenInstrs = [...thenInstrs, ...defaultValueInstrs(resultType)];
  }
  if (elseType === VOID_RESULT || elseType === null) {
    elseInstrs = [...elseInstrs, ...defaultValueInstrs(resultType)];
  }
  if (
    resultType.kind === "ref" &&
    (thenType === VOID_RESULT || thenType === null || elseType === VOID_RESULT || elseType === null)
  ) {
    resultType = { kind: "ref_null", typeIdx: resultType.typeIdx };
  }
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: resultType },
    then: thenInstrs,
    else: elseInstrs,
  });
  return resultType;
}
function compileExpressionCallee(ctx, fctx, expr, calleeExpr) {
  if (
    ts13.isBinaryExpression(calleeExpr) &&
    calleeExpr.operatorToken.kind >= ts13.SyntaxKind.FirstAssignment &&
    calleeExpr.operatorToken.kind <= ts13.SyntaxKind.LastAssignment &&
    calleeExpr.operatorToken.kind === ts13.SyntaxKind.EqualsToken
  ) {
    const rhs = calleeExpr.right;
    if (ts13.isIdentifier(rhs)) {
      const funcIdx = ctx.funcMap.get(rhs.text);
      const closureInfo = ctx.closureMap.get(rhs.text);
      if (funcIdx !== void 0 || closureInfo) {
        const assignResult = compileExpression2(ctx, fctx, calleeExpr);
        if (assignResult) {
          fctx.body.push({ op: "drop" });
        }
        const syntheticCall = ts13.factory.createCallExpression(rhs, expr.typeArguments, expr.arguments);
        ts13.setTextRange(syntheticCall, expr);
        syntheticCall.parent = expr.parent;
        return compileCallExpression(ctx, fctx, syntheticCall);
      }
    }
  }
  const calleeTsType = ctx.checker.getTypeAtLocation(calleeExpr);
  const callSigs = calleeTsType.getCallSignatures?.();
  if (callSigs && callSigs.length > 0) {
    const sig = callSigs[0];
    const sigParamCount = sig.parameters.length;
    const sigRetType = ctx.checker.getReturnTypeOfSignature(sig);
    const sigRetWasm = isVoidType(sigRetType) ? null : resolveWasmType(ctx, sigRetType);
    const sigParamWasmTypes = [];
    for (let i = 0; i < sigParamCount; i++) {
      const paramType = ctx.checker.getTypeOfSymbol(sig.parameters[i]);
      sigParamWasmTypes.push(resolveWasmType(ctx, paramType));
    }
    let matchedClosureInfo;
    let matchedStructTypeIdx;
    for (const [typeIdx, info] of ctx.closureInfoByTypeIdx) {
      if (info.paramTypes.length !== sigParamCount) continue;
      if (sigRetWasm === null && info.returnType !== null) continue;
      if (sigRetWasm !== null && info.returnType === null) continue;
      if (sigRetWasm !== null && info.returnType !== null && sigRetWasm.kind !== info.returnType.kind) continue;
      let paramsMatch = true;
      for (let i = 0; i < sigParamCount; i++) {
        if (sigParamWasmTypes[i].kind !== info.paramTypes[i].kind) {
          paramsMatch = false;
          break;
        }
      }
      if (paramsMatch) {
        matchedClosureInfo = info;
        matchedStructTypeIdx = typeIdx;
        break;
      }
    }
    if (matchedClosureInfo && matchedStructTypeIdx !== void 0) {
      const innerResultType = compileExpression2(ctx, fctx, calleeExpr);
      let closureLocal;
      if (innerResultType?.kind === "externref") {
        const closureRefType = {
          kind: "ref_null",
          typeIdx: matchedStructTypeIdx,
        };
        closureLocal = allocLocal(fctx, `__expr_call_${fctx.locals.length}`, closureRefType);
        fctx.body.push({ op: "any.convert_extern" });
        emitGuardedRefCast(fctx, matchedStructTypeIdx);
        fctx.body.push({ op: "local.set", index: closureLocal });
      } else {
        const closureRefType = innerResultType ?? {
          kind: "ref",
          typeIdx: matchedStructTypeIdx,
        };
        closureLocal = allocLocal(fctx, `__expr_call_${fctx.locals.length}`, closureRefType);
        fctx.body.push({ op: "local.set", index: closureLocal });
      }
      fctx.body.push({ op: "local.get", index: closureLocal });
      emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedStructTypeIdx });
      {
        const ecParamCnt = matchedClosureInfo.paramTypes.length;
        for (let i = 0; i < Math.min(expr.arguments.length, ecParamCnt); i++) {
          compileExpression2(ctx, fctx, expr.arguments[i], matchedClosureInfo.paramTypes[i]);
        }
        for (let i = ecParamCnt; i < expr.arguments.length; i++) {
          const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
          if (extraType !== null) {
            fctx.body.push({ op: "drop" });
          }
        }
      }
      for (let i = expr.arguments.length; i < matchedClosureInfo.paramTypes.length; i++) {
        pushDefaultValue(fctx, matchedClosureInfo.paramTypes[i], ctx);
      }
      fctx.body.push({ op: "local.get", index: closureLocal });
      emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedStructTypeIdx });
      fctx.body.push({
        op: "struct.get",
        typeIdx: matchedStructTypeIdx,
        fieldIdx: 0,
      });
      emitGuardedFuncRefCast(fctx, matchedClosureInfo.funcTypeIdx);
      emitNullCheckThrow2(ctx, fctx, { kind: "ref_null", typeIdx: matchedClosureInfo.funcTypeIdx });
      fctx.body.push({
        op: "call_ref",
        typeIdx: matchedClosureInfo.funcTypeIdx,
      });
      return matchedClosureInfo.returnType ?? VOID_RESULT;
    }
  }
  if (ts13.isBinaryExpression(calleeExpr)) {
    const assignResult = compileExpression2(ctx, fctx, calleeExpr);
    if (assignResult) {
      fctx.body.push({ op: "drop" });
    }
    const rhs = calleeExpr.right;
    if (ts13.isIdentifier(rhs) || ts13.isPropertyAccessExpression(rhs)) {
      const syntheticCall = ts13.factory.createCallExpression(rhs, expr.typeArguments, expr.arguments);
      ts13.setTextRange(syntheticCall, expr);
      syntheticCall.parent = expr.parent;
      return compileCallExpression(ctx, fctx, syntheticCall);
    }
  }
  {
    const calleeType = compileExpression2(ctx, fctx, calleeExpr);
    if (calleeType) {
      fctx.body.push({ op: "drop" });
    }
    for (const arg of expr.arguments) {
      const argType = compileExpression2(ctx, fctx, arg);
      if (argType) {
        fctx.body.push({ op: "drop" });
      }
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
}
function compileIIFE(ctx, fctx, expr) {
  let callee = expr.expression;
  while (ts13.isParenthesizedExpression(callee)) {
    callee = callee.expression;
  }
  if (!ts13.isFunctionExpression(callee) && !ts13.isArrowFunction(callee)) {
    return void 0;
  }
  if (ts13.isFunctionExpression(callee) && callee.asteriskToken !== void 0) {
    return void 0;
  }
  const funcExpr = callee;
  const paramTypes = [];
  for (const p of funcExpr.parameters) {
    const paramType = ctx.checker.getTypeAtLocation(p);
    paramTypes.push(resolveWasmType(ctx, paramType));
  }
  const sig = ctx.checker.getSignatureFromDeclaration(funcExpr);
  let returnType = null;
  if (sig) {
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    if (!isVoidType(retType)) {
      returnType = resolveWasmType(ctx, retType);
    }
  }
  const body = funcExpr.body;
  const referencedNames = /* @__PURE__ */ new Set();
  if (ts13.isBlock(body)) {
    for (const stmt of body.statements) {
      collectReferencedIdentifiers(stmt, referencedNames);
    }
  } else {
    collectReferencedIdentifiers(body, referencedNames);
  }
  const writtenInIIFE = /* @__PURE__ */ new Set();
  if (ts13.isBlock(body)) {
    for (const stmt of body.statements) {
      collectWrittenIdentifiers(stmt, writtenInIIFE);
    }
  } else {
    collectWrittenIdentifiers(body, writtenInIIFE);
  }
  const ownParamNames = new Set(funcExpr.parameters.filter((p) => ts13.isIdentifier(p.name)).map((p) => p.name.text));
  const captures = [];
  for (const name of referencedNames) {
    if (ownParamNames.has(name)) continue;
    const localIdx = fctx.localMap.get(name);
    if (localIdx === void 0) continue;
    if (ctx.funcMap.has(name)) continue;
    const type =
      localIdx < fctx.params.length
        ? fctx.params[localIdx].type
        : (fctx.locals[localIdx - fctx.params.length]?.type ?? { kind: "f64" });
    const isMutable = writtenInIIFE.has(name);
    captures.push({ name, type, localIdx, mutable: isMutable });
  }
  const iifeName = `__iife_${ctx.closureCounter++}`;
  const results = returnType ? [returnType] : [];
  const captureParamTypes = captures.map((c) => {
    if (c.mutable) {
      const refCellTypeIdx = getOrRegisterRefCellType(ctx, c.type);
      return { kind: "ref_null", typeIdx: refCellTypeIdx };
    }
    if (c.type.kind === "ref") {
      return {
        kind: "ref_null",
        typeIdx: c.type.typeIdx,
      };
    }
    return c.type;
  });
  const allParamTypes = [...captureParamTypes, ...paramTypes];
  const funcTypeIdx = addFuncType(ctx, allParamTypes, results, `${iifeName}_type`);
  const liftedFctx = {
    name: iifeName,
    params: [
      ...captures.map((c, i) => ({
        name: c.name,
        type: captureParamTypes[i],
      })),
      ...funcExpr.parameters.map((p, i) => ({
        name: ts13.isIdentifier(p.name) ? p.name.text : `__param${i}`,
        type: paramTypes[i] ?? { kind: "f64" },
      })),
    ],
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    labelMap: /* @__PURE__ */ new Map(),
    savedBodies: [],
  };
  for (let i = 0; i < liftedFctx.params.length; i++) {
    liftedFctx.localMap.set(liftedFctx.params[i].name, i);
  }
  for (let i = 0; i < captures.length; i++) {
    const cap = captures[i];
    if (cap.mutable) {
      const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
      if (!liftedFctx.boxedCaptures) liftedFctx.boxedCaptures = /* @__PURE__ */ new Map();
      liftedFctx.boxedCaptures.set(cap.name, {
        refCellTypeIdx,
        valType: cap.type,
      });
    } else {
      const outerBoxed = fctx.boxedCaptures?.get(cap.name);
      if (outerBoxed && (cap.type.kind === "ref" || cap.type.kind === "ref_null")) {
        if (!liftedFctx.boxedCaptures) liftedFctx.boxedCaptures = /* @__PURE__ */ new Map();
        liftedFctx.boxedCaptures.set(cap.name, {
          refCellTypeIdx: outerBoxed.refCellTypeIdx,
          valType: outerBoxed.valType,
        });
      }
    }
  }
  const savedFunc = ctx.currentFunc;
  if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
  if (savedFunc) ctx.funcStack.push(savedFunc);
  ctx.currentFunc = liftedFctx;
  if (ts13.isBlock(body)) {
    hoistVarDeclarations(ctx, liftedFctx, body.statements);
    hoistLetConstWithTdz(ctx, liftedFctx, body.statements);
    hoistFunctionDeclarations(ctx, liftedFctx, body.statements);
    for (const stmt of body.statements) {
      compileStatement(ctx, liftedFctx, stmt);
    }
  } else {
    const exprType = compileExpression2(ctx, liftedFctx, body);
    if (exprType === null && returnType) {
      if (returnType.kind === "f64") liftedFctx.body.push({ op: "f64.const", value: 0 });
      else if (returnType.kind === "i32") liftedFctx.body.push({ op: "i32.const", value: 0 });
      else if (returnType.kind === "externref") liftedFctx.body.push({ op: "ref.null.extern" });
    }
  }
  if (returnType) {
    const lastInstr = liftedFctx.body[liftedFctx.body.length - 1];
    if (!lastInstr || lastInstr.op !== "return") {
      if (returnType.kind === "f64") liftedFctx.body.push({ op: "f64.const", value: 0 });
      else if (returnType.kind === "i32") liftedFctx.body.push({ op: "i32.const", value: 0 });
      else if (returnType.kind === "externref") liftedFctx.body.push({ op: "ref.null.extern" });
    }
  }
  if (savedFunc) ctx.funcStack.pop();
  if (savedFunc) ctx.parentBodiesStack.pop();
  ctx.currentFunc = savedFunc;
  const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.mod.functions.push({
    name: iifeName,
    typeIdx: funcTypeIdx,
    locals: liftedFctx.locals,
    body: liftedFctx.body,
    exported: false,
  });
  ctx.funcMap.set(iifeName, funcIdx);
  for (const cap of captures) {
    if (cap.mutable) {
      const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
      if (fctx.boxedCaptures?.has(cap.name)) {
        fctx.body.push({ op: "local.get", index: cap.localIdx });
      } else {
        fctx.body.push({ op: "local.get", index: cap.localIdx });
        fctx.body.push({ op: "struct.new", typeIdx: refCellTypeIdx });
        const boxedLocalIdx = allocLocal(fctx, `__boxed_${cap.name}`, {
          kind: "ref",
          typeIdx: refCellTypeIdx,
        });
        fctx.body.push({ op: "local.tee", index: boxedLocalIdx });
        fctx.localMap.set(cap.name, boxedLocalIdx);
        if (!fctx.boxedCaptures) fctx.boxedCaptures = /* @__PURE__ */ new Map();
        fctx.boxedCaptures.set(cap.name, { refCellTypeIdx, valType: cap.type });
      }
    } else {
      fctx.body.push({ op: "local.get", index: cap.localIdx });
    }
  }
  const flatIIFEArgs = flattenCallArgs(expr.arguments) ?? expr.arguments;
  const paramCount = paramTypes.length;
  for (let i = 0; i < flatIIFEArgs.length; i++) {
    const arg = flatIIFEArgs[i];
    if (ts13.isSpreadElement(arg)) continue;
    if (i < paramCount) {
      compileExpression2(ctx, fctx, arg, paramTypes[i]);
    } else {
      const extraType = compileExpression2(ctx, fctx, arg);
      if (extraType !== null) {
        fctx.body.push({ op: "drop" });
      }
    }
  }
  for (let i = flatIIFEArgs.length; i < paramCount; i++) {
    const pt = paramTypes[i] ?? { kind: "f64" };
    if (pt.kind === "f64") fctx.body.push({ op: "f64.const", value: NaN });
    else if (pt.kind === "i32") fctx.body.push({ op: "i32.const", value: 0 });
    else if (pt.kind === "externref") fctx.body.push({ op: "ref.null.extern" });
    else if (pt.kind === "ref" || pt.kind === "ref_null") fctx.body.push({ op: "ref.null", typeIdx: pt.typeIdx });
  }
  const finalFuncIdx = ctx.funcMap.get(iifeName) ?? funcIdx;
  fctx.body.push({ op: "call", funcIdx: finalFuncIdx });
  if (returnType) return returnType;
  return VOID_RESULT;
}
function resolveEnclosingClassName2(fctx) {
  if (fctx.enclosingClassName) return fctx.enclosingClassName;
  const underscoreIdx = fctx.name.indexOf("_");
  if (underscoreIdx > 0) return fctx.name.substring(0, underscoreIdx);
  return void 0;
}
function compileSuperMethodCall(ctx, fctx, expr) {
  const propAccess = expr.expression;
  const methodName = propAccess.name.text;
  const currentClassName = resolveEnclosingClassName2(fctx);
  if (!currentClassName) {
    for (const arg of expr.arguments) {
      const argType = compileExpression2(ctx, fctx, arg);
      if (argType !== null) fctx.body.push({ op: "drop" });
    }
    const sig2 = ctx.checker.getResolvedSignature(expr);
    if (sig2) {
      const retType = ctx.checker.getReturnTypeOfSignature(sig2);
      const wasmType = resolveWasmType(ctx, retType);
      if (wasmType.kind === "f64") {
        fctx.body.push({ op: "f64.const", value: 0 });
      } else if (wasmType.kind === "i32") {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else {
        fctx.body.push({ op: "ref.null.extern" });
      }
      return wasmType;
    }
    return null;
  }
  const parentClassName = ctx.classParentMap.get(currentClassName);
  if (!parentClassName) {
    for (const arg of expr.arguments) {
      const argType = compileExpression2(ctx, fctx, arg);
      if (argType !== null) fctx.body.push({ op: "drop" });
    }
    const sig2 = ctx.checker.getResolvedSignature(expr);
    if (sig2) {
      const retType = ctx.checker.getReturnTypeOfSignature(sig2);
      const wasmType = resolveWasmType(ctx, retType);
      if (wasmType.kind === "f64") {
        fctx.body.push({ op: "f64.const", value: 0 });
      } else if (wasmType.kind === "i32") {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else {
        fctx.body.push({ op: "ref.null.extern" });
      }
      return wasmType;
    }
    return null;
  }
  let ancestor = parentClassName;
  let funcIdx;
  while (ancestor) {
    funcIdx = ctx.funcMap.get(`${ancestor}_${methodName}`);
    if (funcIdx !== void 0) break;
    ancestor = ctx.classParentMap.get(ancestor);
  }
  if (funcIdx === void 0) {
    ctx.errors.push({
      message: `Cannot find method '${methodName}' on parent class '${parentClassName}'`,
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const selfIdx = fctx.localMap.get("this");
  if (selfIdx !== void 0) {
    fctx.body.push({ op: "local.get", index: selfIdx });
  }
  const paramTypes = getFuncParamTypes(ctx, funcIdx);
  const superParamCount = paramTypes ? paramTypes.length - 1 : expr.arguments.length;
  for (let i = 0; i < expr.arguments.length; i++) {
    if (i < superParamCount) {
      compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
    } else {
      const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
      if (extraType !== null) {
        fctx.body.push({ op: "drop" });
      }
    }
  }
  if (paramTypes) {
    for (let i = expr.arguments.length + 1; i < paramTypes.length; i++) {
      pushDefaultValue(fctx, paramTypes[i], ctx);
    }
  }
  const resolvedName = `${ancestor}_${methodName}`;
  const finalSuperIdx = ctx.funcMap.get(resolvedName) ?? funcIdx;
  fctx.body.push({ op: "call", funcIdx: finalSuperIdx });
  const sig = ctx.checker.getResolvedSignature(expr);
  if (sig) {
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    if (isEffectivelyVoidReturn(ctx, retType, resolvedName)) return null;
    if (wasmFuncReturnsVoid(ctx, finalSuperIdx)) return null;
    return getWasmFuncReturnType(ctx, finalSuperIdx) ?? resolveWasmType(ctx, retType);
  }
  return null;
}
function compileSuperElementMethodCall(ctx, fctx, expr, methodName) {
  const currentClassName = resolveEnclosingClassName2(fctx);
  if (!currentClassName) {
    for (const arg of expr.arguments) {
      const argType = compileExpression2(ctx, fctx, arg);
      if (argType !== null) fctx.body.push({ op: "drop" });
    }
    return null;
  }
  const parentClassName = ctx.classParentMap.get(currentClassName);
  if (!parentClassName) {
    for (const arg of expr.arguments) {
      const argType = compileExpression2(ctx, fctx, arg);
      if (argType !== null) fctx.body.push({ op: "drop" });
    }
    return null;
  }
  let ancestor = parentClassName;
  let funcIdx;
  while (ancestor) {
    funcIdx = ctx.funcMap.get(`${ancestor}_${methodName}`);
    if (funcIdx !== void 0) break;
    ancestor = ctx.classParentMap.get(ancestor);
  }
  if (funcIdx === void 0) {
    ctx.errors.push({
      message: `Cannot find method '${methodName}' on parent class '${parentClassName}'`,
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const selfIdx = fctx.localMap.get("this");
  if (selfIdx !== void 0) {
    fctx.body.push({ op: "local.get", index: selfIdx });
  }
  const paramTypes = getFuncParamTypes(ctx, funcIdx);
  const superElemParamCount = paramTypes ? paramTypes.length - 1 : expr.arguments.length;
  for (let i = 0; i < expr.arguments.length; i++) {
    if (i < superElemParamCount) {
      compileExpression2(ctx, fctx, expr.arguments[i], paramTypes?.[i + 1]);
    } else {
      const extraType = compileExpression2(ctx, fctx, expr.arguments[i]);
      if (extraType !== null) {
        fctx.body.push({ op: "drop" });
      }
    }
  }
  if (paramTypes) {
    for (let i = expr.arguments.length + 1; i < paramTypes.length; i++) {
      pushDefaultValue(fctx, paramTypes[i], ctx);
    }
  }
  const resolvedName = `${ancestor}_${methodName}`;
  const finalSuperIdx = ctx.funcMap.get(resolvedName) ?? funcIdx;
  fctx.body.push({ op: "call", funcIdx: finalSuperIdx });
  const sig = ctx.checker.getResolvedSignature(expr);
  if (sig) {
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    if (isEffectivelyVoidReturn(ctx, retType, resolvedName)) return null;
    if (wasmFuncReturnsVoid(ctx, finalSuperIdx)) return null;
    return getWasmFuncReturnType(ctx, finalSuperIdx) ?? resolveWasmType(ctx, retType);
  }
  return null;
}
function compileSuperPropertyAccess(ctx, fctx, expr, propName) {
  const currentClassName = resolveEnclosingClassName2(fctx);
  if (!currentClassName) {
    const accessType2 = ctx.checker.getTypeAtLocation(expr);
    const wasmType2 = resolveWasmType(ctx, accessType2);
    if (wasmType2.kind === "f64") {
      fctx.body.push({ op: "f64.const", value: 0 });
    } else if (wasmType2.kind === "i32") {
      fctx.body.push({ op: "i32.const", value: 0 });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    return wasmType2;
  }
  const parentClassName = ctx.classParentMap.get(currentClassName);
  if (!parentClassName) {
    const accessType2 = ctx.checker.getTypeAtLocation(expr);
    const wasmType2 = resolveWasmType(ctx, accessType2);
    if (wasmType2.kind === "f64") {
      fctx.body.push({ op: "f64.const", value: 0 });
    } else if (wasmType2.kind === "i32") {
      fctx.body.push({ op: "i32.const", value: 0 });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    return wasmType2;
  }
  let ancestor = parentClassName;
  while (ancestor) {
    const accessorKey = `${ancestor}_${propName}`;
    if (ctx.classAccessorSet.has(accessorKey)) {
      const getterName = `${ancestor}_get_${propName}`;
      const funcIdx = ctx.funcMap.get(getterName);
      if (funcIdx !== void 0) {
        const selfIdx = fctx.localMap.get("this");
        if (selfIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: selfIdx });
        }
        fctx.body.push({ op: "call", funcIdx });
        const propType = ctx.checker.getTypeAtLocation(expr);
        return resolveWasmType(ctx, propType);
      }
    }
    ancestor = ctx.classParentMap.get(ancestor);
  }
  ancestor = parentClassName;
  while (ancestor) {
    const structTypeIdx = ctx.structMap.get(ancestor);
    const fields = ctx.structFields.get(ancestor);
    if (structTypeIdx !== void 0 && fields) {
      const fieldIdx = fields.findIndex((f) => f.name === propName);
      if (fieldIdx !== -1) {
        const currentStructTypeIdx = ctx.structMap.get(currentClassName);
        const currentFields = ctx.structFields.get(currentClassName);
        if (currentStructTypeIdx !== void 0 && currentFields) {
          const currentFieldIdx = currentFields.findIndex((f) => f.name === propName);
          if (currentFieldIdx !== -1) {
            const selfIdx2 = fctx.localMap.get("this");
            if (selfIdx2 !== void 0) {
              fctx.body.push({ op: "local.get", index: selfIdx2 });
            }
            fctx.body.push({
              op: "struct.get",
              typeIdx: currentStructTypeIdx,
              fieldIdx: currentFieldIdx,
            });
            return currentFields[currentFieldIdx].type;
          }
        }
        const selfIdx = fctx.localMap.get("this");
        if (selfIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: selfIdx });
        }
        fctx.body.push({
          op: "struct.get",
          typeIdx: structTypeIdx,
          fieldIdx,
        });
        return fields[fieldIdx].type;
      }
    }
    ancestor = ctx.classParentMap.get(ancestor);
  }
  const accessType = ctx.checker.getTypeAtLocation(expr);
  const wasmType = resolveWasmType(ctx, accessType);
  if (wasmType.kind === "f64") {
    fctx.body.push({ op: "f64.const", value: 0 });
  } else if (wasmType.kind === "i32") {
    fctx.body.push({ op: "i32.const", value: 0 });
  } else {
    fctx.body.push({ op: "ref.null.extern" });
  }
  return wasmType;
}
function compileSuperElementAccess(ctx, fctx, expr) {
  const argExpr = expr.argumentExpression;
  let propName;
  if (argExpr) {
    if (ts13.isStringLiteral(argExpr)) {
      propName = argExpr.text;
    } else if (ts13.isNumericLiteral(argExpr)) {
      propName = String(Number(argExpr.text));
    } else {
      propName = resolveComputedKeyExpression(ctx, argExpr);
    }
  }
  if (propName === void 0) {
    const accessType2 = ctx.checker.getTypeAtLocation(expr);
    const wasmType2 = resolveWasmType(ctx, accessType2);
    if (wasmType2.kind === "f64") {
      fctx.body.push({ op: "f64.const", value: 0 });
    } else if (wasmType2.kind === "i32") {
      fctx.body.push({ op: "i32.const", value: 0 });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    return wasmType2;
  }
  const currentClassName = resolveEnclosingClassName2(fctx);
  if (!currentClassName) {
    const accessType2 = ctx.checker.getTypeAtLocation(expr);
    const wasmType2 = resolveWasmType(ctx, accessType2);
    if (wasmType2.kind === "f64") {
      fctx.body.push({ op: "f64.const", value: 0 });
    } else if (wasmType2.kind === "i32") {
      fctx.body.push({ op: "i32.const", value: 0 });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    return wasmType2;
  }
  const parentClassName = ctx.classParentMap.get(currentClassName);
  if (!parentClassName) {
    const accessType2 = ctx.checker.getTypeAtLocation(expr);
    const wasmType2 = resolveWasmType(ctx, accessType2);
    if (wasmType2.kind === "f64") {
      fctx.body.push({ op: "f64.const", value: 0 });
    } else if (wasmType2.kind === "i32") {
      fctx.body.push({ op: "i32.const", value: 0 });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    return wasmType2;
  }
  let ancestor = parentClassName;
  while (ancestor) {
    const accessorKey = `${ancestor}_${propName}`;
    if (ctx.classAccessorSet.has(accessorKey)) {
      const getterName = `${ancestor}_get_${propName}`;
      const funcIdx = ctx.funcMap.get(getterName);
      if (funcIdx !== void 0) {
        const selfIdx = fctx.localMap.get("this");
        if (selfIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: selfIdx });
        }
        fctx.body.push({ op: "call", funcIdx });
        const propType = ctx.checker.getTypeAtLocation(expr);
        return resolveWasmType(ctx, propType);
      }
    }
    ancestor = ctx.classParentMap.get(ancestor);
  }
  ancestor = parentClassName;
  while (ancestor) {
    const structTypeIdx = ctx.structMap.get(ancestor);
    const fields = ctx.structFields.get(ancestor);
    if (structTypeIdx !== void 0 && fields) {
      const fieldIdx = fields.findIndex((f) => f.name === propName);
      if (fieldIdx !== -1) {
        const currentStructTypeIdx = ctx.structMap.get(currentClassName);
        const currentFields = ctx.structFields.get(currentClassName);
        if (currentStructTypeIdx !== void 0 && currentFields) {
          const currentFieldIdx = currentFields.findIndex((f) => f.name === propName);
          if (currentFieldIdx !== -1) {
            const selfIdx2 = fctx.localMap.get("this");
            if (selfIdx2 !== void 0) {
              fctx.body.push({ op: "local.get", index: selfIdx2 });
            }
            fctx.body.push({
              op: "struct.get",
              typeIdx: currentStructTypeIdx,
              fieldIdx: currentFieldIdx,
            });
            return currentFields[currentFieldIdx].type;
          }
        }
        const selfIdx = fctx.localMap.get("this");
        if (selfIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: selfIdx });
        }
        fctx.body.push({
          op: "struct.get",
          typeIdx: structTypeIdx,
          fieldIdx,
        });
        return fields[fieldIdx].type;
      }
    }
    ancestor = ctx.classParentMap.get(ancestor);
  }
  const accessType = ctx.checker.getTypeAtLocation(expr);
  const wasmType = resolveWasmType(ctx, accessType);
  if (wasmType.kind === "f64") {
    fctx.body.push({ op: "f64.const", value: 0 });
  } else if (wasmType.kind === "i32") {
    fctx.body.push({ op: "i32.const", value: 0 });
  } else {
    fctx.body.push({ op: "ref.null.extern" });
  }
  return wasmType;
}
function inferArrayElementType(ctx, expr) {
  const parent = expr.parent;
  let varName = null;
  if (ts13.isVariableDeclaration(parent) && ts13.isIdentifier(parent.name)) {
    varName = parent.name.text;
  } else if (
    ts13.isBinaryExpression(parent) &&
    parent.operatorToken.kind === ts13.SyntaxKind.EqualsToken &&
    ts13.isIdentifier(parent.left)
  ) {
    varName = parent.left.text;
  }
  if (!varName) return null;
  let scope = expr;
  while (
    scope &&
    !ts13.isFunctionDeclaration(scope) &&
    !ts13.isFunctionExpression(scope) &&
    !ts13.isArrowFunction(scope) &&
    !ts13.isMethodDeclaration(scope) &&
    !ts13.isSourceFile(scope)
  ) {
    scope = scope.parent;
  }
  if (!scope) return null;
  let inferredElemType = null;
  function visit(node) {
    if (inferredElemType) return;
    if (
      ts13.isBinaryExpression(node) &&
      node.operatorToken.kind === ts13.SyntaxKind.EqualsToken &&
      ts13.isElementAccessExpression(node.left) &&
      ts13.isIdentifier(node.left.expression) &&
      node.left.expression.text === varName
    ) {
      const valType = ctx.checker.getTypeAtLocation(node.right);
      if (!(valType.flags & ts13.TypeFlags.Any)) {
        inferredElemType = valType;
        return;
      }
    }
    if (
      ts13.isCallExpression(node) &&
      ts13.isPropertyAccessExpression(node.expression) &&
      node.expression.name.text === "push" &&
      ts13.isIdentifier(node.expression.expression) &&
      node.expression.expression.text === varName &&
      node.arguments.length >= 1
    ) {
      const valType = ctx.checker.getTypeAtLocation(node.arguments[0]);
      if (!(valType.flags & ts13.TypeFlags.Any)) {
        inferredElemType = valType;
        return;
      }
    }
    ts13.forEachChild(node, visit);
  }
  visit(scope);
  return inferredElemType;
}
function usesArguments(node) {
  if (ts13.isIdentifier(node) && node.text === "arguments") return true;
  if (ts13.isFunctionDeclaration(node) || ts13.isFunctionExpression(node)) {
    return false;
  }
  return ts13.forEachChild(node, usesArguments) ?? false;
}
function flattenCallArgs(args) {
  const result = [];
  for (const arg of args) {
    if (ts13.isSpreadElement(arg)) {
      if (ts13.isArrayLiteralExpression(arg.expression)) {
        for (const el of arg.expression.elements) {
          result.push(el);
        }
      } else {
        return null;
      }
    } else {
      result.push(arg);
    }
  }
  return result;
}
function compileNewFunctionDeclaration(ctx, fctx, expr, funcName, funcDecl) {
  const body = funcDecl.body;
  if (!body) return null;
  const fields = [];
  function collectThisAssignments(stmts) {
    for (const stmt of stmts) {
      if (
        ts13.isExpressionStatement(stmt) &&
        ts13.isBinaryExpression(stmt.expression) &&
        stmt.expression.operatorToken.kind === ts13.SyntaxKind.EqualsToken &&
        ts13.isPropertyAccessExpression(stmt.expression.left) &&
        stmt.expression.left.expression.kind === ts13.SyntaxKind.ThisKeyword
      ) {
        const fieldName = stmt.expression.left.name.text;
        if (!fields.some((f) => f.name === fieldName)) {
          const lhsType = ctx.checker.getTypeAtLocation(stmt.expression.left);
          const rhsType = ctx.checker.getTypeAtLocation(stmt.expression.right);
          const lhsWasm = resolveWasmType(ctx, lhsType);
          const rhsWasm = resolveWasmType(ctx, rhsType);
          const fieldType = lhsWasm.kind === "externref" ? rhsWasm : lhsWasm;
          fields.push({ name: fieldName, type: fieldType, mutable: true });
        }
      }
      if (ts13.isIfStatement(stmt)) {
        if (ts13.isBlock(stmt.thenStatement)) {
          collectThisAssignments(stmt.thenStatement.statements);
        }
        if (stmt.elseStatement && ts13.isBlock(stmt.elseStatement)) {
          collectThisAssignments(stmt.elseStatement.statements);
        }
      }
      if (
        (ts13.isForStatement(stmt) ||
          ts13.isForInStatement(stmt) ||
          ts13.isForOfStatement(stmt) ||
          ts13.isWhileStatement(stmt) ||
          ts13.isDoStatement(stmt)) &&
        ts13.isBlock(stmt.statement)
      ) {
        collectThisAssignments(stmt.statement.statements);
      }
    }
  }
  collectThisAssignments(body.statements);
  for (const field of fields) {
    if (field.type.kind === "ref") {
      field.type = { kind: "ref_null", typeIdx: field.type.typeIdx };
    }
  }
  const structName = `__fnctor_${funcName}`;
  const structTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: structName,
    fields,
  });
  ctx.structMap.set(structName, structTypeIdx);
  ctx.typeIdxToStructName.set(structTypeIdx, structName);
  ctx.structFields.set(structName, fields);
  const ctorParams = [];
  for (let i = 0; i < funcDecl.parameters.length; i++) {
    const param = funcDecl.parameters[i];
    const paramType = ctx.checker.getTypeAtLocation(param);
    ctorParams.push(resolveWasmType(ctx, paramType));
  }
  const ctorName = `${structName}_new`;
  const ctorResults = [{ kind: "ref", typeIdx: structTypeIdx }];
  const ctorTypeIdx = addFuncType(ctx, ctorParams, ctorResults, `${ctorName}_type`);
  const ctorFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set(ctorName, ctorFuncIdx);
  const ctorFunc = {
    name: ctorName,
    typeIdx: ctorTypeIdx,
    locals: [],
    body: [],
    exported: false,
  };
  ctx.mod.functions.push(ctorFunc);
  ctx.funcConstructorMap.set(funcName, { structTypeIdx, ctorFuncName: ctorName });
  const paramDefs = [];
  for (let i = 0; i < funcDecl.parameters.length; i++) {
    const p = funcDecl.parameters[i];
    paramDefs.push({
      name: ts13.isIdentifier(p.name) ? p.name.text : `__param${i}`,
      type: ctorParams[i] ?? { kind: "f64" },
    });
  }
  const ctorFctx = {
    name: ctorName,
    params: paramDefs,
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: { kind: "ref", typeIdx: structTypeIdx },
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    labelMap: /* @__PURE__ */ new Map(),
    savedBodies: [],
  };
  for (let i = 0; i < ctorFctx.params.length; i++) {
    ctorFctx.localMap.set(ctorFctx.params[i].name, i);
  }
  for (const field of fields) {
    if (field.type.kind === "f64") {
      ctorFctx.body.push({ op: "f64.const", value: 0 });
    } else if (field.type.kind === "i32") {
      ctorFctx.body.push({ op: "i32.const", value: 0 });
    } else if (field.type.kind === "i64") {
      ctorFctx.body.push({ op: "i64.const", value: 0n });
    } else if (field.type.kind === "externref") {
      ctorFctx.body.push({ op: "ref.null.extern" });
    } else if (field.type.kind === "ref_null") {
      ctorFctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
    } else if (field.type.kind === "ref") {
      ctorFctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
    } else {
      ctorFctx.body.push({ op: "i32.const", value: 0 });
    }
  }
  ctorFctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
  const selfLocal = allocLocal(ctorFctx, "__self", { kind: "ref", typeIdx: structTypeIdx });
  ctorFctx.body.push({ op: "local.set", index: selfLocal });
  ctorFctx.localMap.set("this", selfLocal);
  const savedFunc = ctx.currentFunc;
  if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
  if (savedFunc) ctx.funcStack.push(savedFunc);
  ctx.currentFunc = ctorFctx;
  for (const stmt of body.statements) {
    compileStatement(ctx, ctorFctx, stmt);
  }
  if (savedFunc) ctx.funcStack.pop();
  if (savedFunc) ctx.parentBodiesStack.pop();
  ctx.currentFunc = savedFunc;
  ctorFctx.body.push({ op: "local.get", index: selfLocal });
  ctorFunc.locals = ctorFctx.locals;
  ctorFunc.body = ctorFctx.body;
  const args = expr.arguments ?? [];
  const paramTypes = getFuncParamTypes(ctx, ctorFuncIdx);
  for (let i = 0; i < args.length; i++) {
    compileExpression2(ctx, fctx, args[i], paramTypes?.[i]);
  }
  if (paramTypes) {
    for (let i = args.length; i < paramTypes.length; i++) {
      pushDefaultValue(fctx, paramTypes[i], ctx);
    }
  }
  const finalCtorIdx = ctx.funcMap.get(ctorName) ?? ctorFuncIdx;
  fctx.body.push({ op: "call", funcIdx: finalCtorIdx });
  return { kind: "ref", typeIdx: structTypeIdx };
}
function compileNewFunctionExpression(ctx, fctx, expr, funcExpr) {
  const closureId = ctx.closureCounter++;
  const closureName = `__new_ctor_${closureId}`;
  const body = funcExpr.body;
  if (!body || !ts13.isBlock(body)) return null;
  const rawArgs = expr.arguments ?? [];
  const flatArgs = flattenCallArgs(rawArgs);
  if (!flatArgs) {
    ctx.errors.push({
      message: "new FunctionExpression with non-literal spread not supported",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const needsArguments = usesArguments(body);
  const formalParams = [];
  if (funcExpr.parameters.length > 0) {
    for (const p of funcExpr.parameters) {
      const paramType = ctx.checker.getTypeAtLocation(p);
      formalParams.push(resolveWasmType(ctx, paramType));
    }
  } else {
    for (let i = 0; i < flatArgs.length; i++) {
      formalParams.push({ kind: "f64" });
    }
  }
  const referencedNames = /* @__PURE__ */ new Set();
  for (const stmt of body.statements) {
    collectReferencedIdentifiers(stmt, referencedNames);
  }
  const writtenInClosure = /* @__PURE__ */ new Set();
  for (const stmt of body.statements) {
    collectWrittenIdentifiers(stmt, writtenInClosure);
  }
  const captures = [];
  for (const name of referencedNames) {
    const localIdx = fctx.localMap.get(name);
    if (localIdx === void 0) continue;
    if (ctx.funcMap.has(name)) continue;
    const isOwnParam = funcExpr.parameters.some((p) => ts13.isIdentifier(p.name) && p.name.text === name);
    if (isOwnParam) continue;
    if (name === "arguments") continue;
    const type =
      localIdx < fctx.params.length
        ? fctx.params[localIdx].type
        : (fctx.locals[localIdx - fctx.params.length]?.type ?? { kind: "f64" });
    const isMutable = writtenInClosure.has(name);
    captures.push({ name, type, localIdx, mutable: isMutable });
  }
  const structFields = [
    { name: "func", type: { kind: "funcref" }, mutable: false },
    ...captures.map((c) => {
      if (c.mutable) {
        const refCellTypeIdx = getOrRegisterRefCellType(ctx, c.type);
        return {
          name: c.name,
          type: { kind: "ref_null", typeIdx: refCellTypeIdx },
          mutable: false,
        };
      }
      return { name: c.name, type: c.type, mutable: false };
    }),
  ];
  const structTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: `${closureName}_struct`,
    fields: structFields,
  });
  const liftedParams = [{ kind: "ref", typeIdx: structTypeIdx }, ...formalParams];
  const liftedFuncTypeIdx = addFuncType(ctx, liftedParams, [], `${closureName}_type`);
  const paramDefs = [{ name: "__self", type: { kind: "ref", typeIdx: structTypeIdx } }];
  if (funcExpr.parameters.length > 0) {
    for (let i = 0; i < funcExpr.parameters.length; i++) {
      const p = funcExpr.parameters[i];
      paramDefs.push({
        name: ts13.isIdentifier(p.name) ? p.name.text : `__param${i}`,
        type: formalParams[i] ?? { kind: "f64" },
      });
    }
  } else {
    for (let i = 0; i < flatArgs.length; i++) {
      paramDefs.push({ name: `__arg${i}`, type: { kind: "f64" } });
    }
  }
  const liftedFctx = {
    name: closureName,
    params: paramDefs,
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: null,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    labelMap: /* @__PURE__ */ new Map(),
    savedBodies: [],
  };
  for (let i = 0; i < liftedFctx.params.length; i++) {
    liftedFctx.localMap.set(liftedFctx.params[i].name, i);
  }
  for (let i = 0; i < captures.length; i++) {
    const cap = captures[i];
    if (cap.mutable) {
      const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
      const refCellType = {
        kind: "ref_null",
        typeIdx: refCellTypeIdx,
      };
      const localIdx = allocLocal(liftedFctx, cap.name, refCellType);
      liftedFctx.body.push({ op: "local.get", index: 0 });
      liftedFctx.body.push({
        op: "struct.get",
        typeIdx: structTypeIdx,
        fieldIdx: i + 1,
      });
      liftedFctx.body.push({ op: "local.set", index: localIdx });
      if (!liftedFctx.boxedCaptures) liftedFctx.boxedCaptures = /* @__PURE__ */ new Map();
      liftedFctx.boxedCaptures.set(cap.name, {
        refCellTypeIdx,
        valType: cap.type,
      });
    } else {
      const outerBoxed = fctx.boxedCaptures?.get(cap.name);
      if (outerBoxed && (cap.type.kind === "ref" || cap.type.kind === "ref_null")) {
        const refCellType = { kind: "ref_null", typeIdx: outerBoxed.refCellTypeIdx };
        const localIdx = allocLocal(liftedFctx, cap.name, refCellType);
        liftedFctx.body.push({ op: "local.get", index: 0 });
        liftedFctx.body.push({
          op: "struct.get",
          typeIdx: structTypeIdx,
          fieldIdx: i + 1,
        });
        liftedFctx.body.push({ op: "local.set", index: localIdx });
        if (!liftedFctx.boxedCaptures) liftedFctx.boxedCaptures = /* @__PURE__ */ new Map();
        liftedFctx.boxedCaptures.set(cap.name, {
          refCellTypeIdx: outerBoxed.refCellTypeIdx,
          valType: outerBoxed.valType,
        });
      } else {
        const localIdx = allocLocal(liftedFctx, cap.name, cap.type);
        liftedFctx.body.push({ op: "local.get", index: 0 });
        liftedFctx.body.push({
          op: "struct.get",
          typeIdx: structTypeIdx,
          fieldIdx: i + 1,
        });
        liftedFctx.body.push({ op: "local.set", index: localIdx });
      }
    }
  }
  if (needsArguments) {
    const hasNumericFormal = formalParams.some((pt) => pt.kind === "f64" || pt.kind === "i32");
    if (hasNumericFormal) {
      ensureLateImport2(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
      flushLateImportShifts2(ctx, fctx);
    }
    const numArgs = formalParams.length;
    const elemType = { kind: "externref" };
    const vti = getOrRegisterVecType(ctx, "externref", elemType);
    const ati = getArrTypeIdxFromVec(ctx, vti);
    const vecRef = { kind: "ref", typeIdx: vti };
    const argsLocal = allocLocal(liftedFctx, "arguments", vecRef);
    const arrTmp = allocLocal(liftedFctx, "__args_arr_tmp", {
      kind: "ref",
      typeIdx: ati,
    });
    if (hasNumericFormal) {
      ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
      flushLateImportShifts2(ctx, liftedFctx);
    }
    liftedFctx.mappedArgsInfo = {
      argsLocalIdx: argsLocal,
      arrTypeIdx: ati,
      vecTypeIdx: vti,
      paramCount: numArgs,
      paramOffset: 1,
      // skip __self capture param
      paramTypes: formalParams.slice(),
    };
    for (let i = 0; i < numArgs; i++) {
      liftedFctx.body.push({ op: "local.get", index: i + 1 });
      const pt = formalParams[i];
      if (pt.kind === "f64") {
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          liftedFctx.body.push({ op: "call", funcIdx: boxIdx });
        } else {
          liftedFctx.body.push({ op: "drop" });
          liftedFctx.body.push({ op: "ref.null.extern" });
        }
      } else if (pt.kind === "i32") {
        liftedFctx.body.push({ op: "f64.convert_i32_s" });
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          liftedFctx.body.push({ op: "call", funcIdx: boxIdx });
        } else {
          liftedFctx.body.push({ op: "drop" });
          liftedFctx.body.push({ op: "ref.null.extern" });
        }
      } else if (pt.kind === "ref" || pt.kind === "ref_null") {
        liftedFctx.body.push({ op: "extern.convert_any" });
      }
    }
    liftedFctx.body.push({
      op: "array.new_fixed",
      typeIdx: ati,
      length: numArgs,
    });
    liftedFctx.body.push({ op: "local.set", index: arrTmp });
    liftedFctx.body.push({ op: "i32.const", value: numArgs });
    liftedFctx.body.push({ op: "local.get", index: arrTmp });
    liftedFctx.body.push({ op: "struct.new", typeIdx: vti });
    liftedFctx.body.push({ op: "local.set", index: argsLocal });
  }
  const savedFunc = ctx.currentFunc;
  if (savedFunc) ctx.parentBodiesStack.push(savedFunc.body);
  if (savedFunc) ctx.funcStack.push(savedFunc);
  ctx.currentFunc = liftedFctx;
  for (const stmt of body.statements) {
    compileStatement(ctx, liftedFctx, stmt);
  }
  if (savedFunc) ctx.funcStack.pop();
  if (savedFunc) ctx.parentBodiesStack.pop();
  ctx.currentFunc = savedFunc;
  const liftedFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.mod.functions.push({
    name: closureName,
    typeIdx: liftedFuncTypeIdx,
    locals: liftedFctx.locals,
    body: liftedFctx.body,
    exported: false,
  });
  ctx.funcMap.set(closureName, liftedFuncIdx);
  fctx.body.push({ op: "ref.func", funcIdx: liftedFuncIdx });
  for (const cap of captures) {
    if (cap.mutable) {
      if (fctx.boxedCaptures?.has(cap.name)) {
        fctx.body.push({ op: "local.get", index: cap.localIdx });
      } else {
        const refCellTypeIdx = getOrRegisterRefCellType(ctx, cap.type);
        fctx.body.push({ op: "local.get", index: cap.localIdx });
        fctx.body.push({ op: "struct.new", typeIdx: refCellTypeIdx });
        const boxedLocalIdx = allocLocal(fctx, `__boxed_${cap.name}`, {
          kind: "ref_null",
          typeIdx: refCellTypeIdx,
        });
        fctx.body.push({ op: "local.tee", index: boxedLocalIdx });
        fctx.localMap.set(cap.name, boxedLocalIdx);
        if (!fctx.boxedCaptures) fctx.boxedCaptures = /* @__PURE__ */ new Map();
        fctx.boxedCaptures.set(cap.name, { refCellTypeIdx, valType: cap.type });
      }
    } else {
      fctx.body.push({ op: "local.get", index: cap.localIdx });
    }
  }
  fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
  const closureLocal = allocLocal(fctx, `__ctor_closure_${closureId}`, {
    kind: "ref",
    typeIdx: structTypeIdx,
  });
  fctx.body.push({ op: "local.set", index: closureLocal });
  fctx.body.push({ op: "local.get", index: closureLocal });
  for (let i = 0; i < flatArgs.length; i++) {
    compileExpression2(ctx, fctx, flatArgs[i], formalParams[i]);
  }
  fctx.body.push({ op: "call", funcIdx: liftedFuncIdx });
  fctx.body.push({ op: "ref.null.extern" });
  return { kind: "externref" };
}
function compileClassExpression(ctx, fctx, expr) {
  const syntheticName = ctx.anonClassExprNames.get(expr);
  const classNameForCheck = syntheticName ?? expr.name?.text;
  if (classNameForCheck && ctx.classThrowsOnEval.has(classNameForCheck)) {
    emitThrowString2(ctx, fctx, "TypeError: Classes may not have a static property named 'prototype'");
    fctx.body.push({ op: "unreachable" });
    return { kind: "externref" };
  }
  if (syntheticName) {
    const ctorName = `${syntheticName}_new`;
    const funcIdx = ctx.funcMap.get(ctorName);
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "ref.func", funcIdx });
      return { kind: "funcref" };
    }
  }
  if (expr.name) {
    const className = expr.name.text;
    if (ctx.classSet.has(className)) {
      const ctorName = `${className}_new`;
      const funcIdx = ctx.funcMap.get(ctorName);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "ref.func", funcIdx });
        return { kind: "funcref" };
      }
    }
  }
  fctx.body.push({ op: "ref.null.extern" });
  return { kind: "externref" };
}
function compileNewExpression(ctx, fctx, expr) {
  if (ts13.isFunctionExpression(expr.expression)) {
    return compileNewFunctionExpression(ctx, fctx, expr, expr.expression);
  }
  {
    let unwrappedNew = expr.expression;
    while (ts13.isParenthesizedExpression(unwrappedNew)) {
      unwrappedNew = unwrappedNew.expression;
    }
    if (ts13.isArrowFunction(unwrappedNew)) {
      emitThrowString2(ctx, fctx, "TypeError: is not a constructor");
      fctx.body.push({ op: "ref.null.extern" });
      return { kind: "externref" };
    }
  }
  {
    let unwrappedExpr = expr.expression;
    while (ts13.isParenthesizedExpression(unwrappedExpr)) {
      unwrappedExpr = unwrappedExpr.expression;
    }
    if (ts13.isClassExpression(unwrappedExpr)) {
      const syntheticName = ctx.anonClassExprNames.get(unwrappedExpr);
      if (syntheticName) {
        const ctorName = `${syntheticName}_new`;
        const funcIdx = ctx.funcMap.get(ctorName);
        if (funcIdx === void 0) {
          ctx.errors.push({
            message: `Missing constructor for anonymous class`,
            line: getLine(expr),
            column: getCol(expr),
          });
          return null;
        }
        const paramTypes = getFuncParamTypes(ctx, funcIdx);
        const args = expr.arguments ?? [];
        for (let i = 0; i < args.length; i++) {
          compileExpression2(ctx, fctx, args[i], paramTypes?.[i]);
        }
        if (paramTypes) {
          for (let i = args.length; i < paramTypes.length; i++) {
            pushDefaultValue(fctx, paramTypes[i], ctx);
          }
        }
        fctx.body.push({ op: "call", funcIdx });
        const structTypeIdx = ctx.structMap.get(syntheticName);
        return { kind: "ref", typeIdx: structTypeIdx };
      }
    }
  }
  if (ts13.isIdentifier(expr.expression) && expr.expression.text === "Promise") {
    const funcIdx = ctx.funcMap.get("Promise_new");
    if (funcIdx !== void 0) {
      const args = expr.arguments ?? [];
      if (args.length >= 1) {
        compileExpression2(ctx, fctx, args[0], { kind: "externref" });
      } else {
        fctx.body.push({ op: "ref.null.extern" });
      }
      fctx.body.push({ op: "call", funcIdx });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    return { kind: "externref" };
  }
  if (ts13.isIdentifier(expr.expression)) {
    const ctorName = expr.expression.text;
    if (ctorName === "Number" || ctorName === "String" || ctorName === "Boolean") {
      const args = expr.arguments ?? [];
      if (ctorName === "Number") {
        if (args.length >= 1) {
          compileExpression2(ctx, fctx, args[0], { kind: "f64" });
        } else {
          fctx.body.push({ op: "f64.const", value: 0 });
        }
        addUnionImports(ctx);
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: boxIdx });
        }
        return { kind: "externref" };
      }
      if (ctorName === "String") {
        if (args.length >= 1) {
          compileExpression2(ctx, fctx, args[0], { kind: "externref" });
        } else {
          const emptyStrResult = compileStringLiteral(ctx, fctx, "");
          if (!emptyStrResult) {
            fctx.body.push({ op: "ref.null.extern" });
          }
        }
        return { kind: "externref" };
      }
      if (ctorName === "Boolean") {
        if (args.length >= 1) {
          compileExpression2(ctx, fctx, args[0], { kind: "f64" });
        } else {
          fctx.body.push({ op: "f64.const", value: 0 });
        }
        addUnionImports(ctx);
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: boxIdx });
        }
        return { kind: "externref" };
      }
    }
  }
  if (ts13.isIdentifier(expr.expression)) {
    const ctorName = expr.expression.text;
    if (
      ctorName === "Error" ||
      ctorName === "TypeError" ||
      ctorName === "RangeError" ||
      ctorName === "SyntaxError" ||
      ctorName === "URIError" ||
      ctorName === "EvalError" ||
      ctorName === "ReferenceError" ||
      ctorName === "Test262Error"
    ) {
      const args = expr.arguments ?? [];
      if (args.length >= 1) {
        const resultType = compileExpression2(ctx, fctx, args[0], {
          kind: "externref",
        });
        if (resultType && resultType.kind !== "externref") {
          coerceType3(ctx, fctx, resultType, { kind: "externref" });
        }
      } else {
        fctx.body.push({ op: "ref.null.extern" });
      }
      const importName = `__new_${ctorName}`;
      const funcIdx = ensureLateImport2(
        ctx,
        importName,
        [{ kind: "externref" }],
        // message param
        [{ kind: "externref" }],
        // returns Error object
      );
      flushLateImportShifts2(ctx, fctx);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
      }
      return { kind: "externref" };
    }
  }
  if (ts13.isIdentifier(expr.expression) && expr.expression.text === "AggregateError") {
    const args = expr.arguments ?? [];
    if (args.length >= 1) {
      const errorsType = compileExpression2(ctx, fctx, args[0], { kind: "externref" });
      if (errorsType && errorsType.kind !== "externref") {
        coerceType3(ctx, fctx, errorsType, { kind: "externref" });
      }
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    if (args.length >= 2) {
      const msgType = compileExpression2(ctx, fctx, args[1], { kind: "externref" });
      if (msgType && msgType.kind !== "externref") {
        coerceType3(ctx, fctx, msgType, { kind: "externref" });
      }
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    if (args.length >= 3) {
      const optsType = compileExpression2(ctx, fctx, args[2], { kind: "externref" });
      if (optsType && optsType.kind !== "externref") {
        coerceType3(ctx, fctx, optsType, { kind: "externref" });
      }
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    const funcIdx = ensureLateImport2(
      ctx,
      "__new_AggregateError",
      [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    flushLateImportShifts2(ctx, fctx);
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
    }
    return { kind: "externref" };
  }
  if (ts13.isIdentifier(expr.expression) && expr.expression.text === "Object") {
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  if (ts13.isIdentifier(expr.expression) && expr.expression.text === "Proxy") {
    const args = expr.arguments ?? [];
    if (args.length >= 1) {
      const bodyBefore = fctx.body.length;
      const targetResult = compileExpression2(ctx, fctx, args[0]);
      if (targetResult && targetResult.kind !== "externref") {
        if (targetResult.kind === "ref" || targetResult.kind === "ref_null") {
          fctx.body.push({ op: "extern.convert_any" });
        } else {
          coerceType2(ctx, fctx, targetResult, { kind: "externref" });
        }
      }
      if (args.length >= 2) {
        const handlerResult = compileExpression2(ctx, fctx, args[1]);
        if (handlerResult && handlerResult.kind !== "externref") {
          if (handlerResult.kind === "ref" || handlerResult.kind === "ref_null") {
            fctx.body.push({ op: "extern.convert_any" });
          } else {
            coerceType2(ctx, fctx, handlerResult, { kind: "externref" });
          }
        }
      } else {
        fctx.body.push({ op: "ref.null.extern" });
      }
      const proxyIdx = ensureLateImport2(
        ctx,
        "__proxy_create",
        [{ kind: "externref" }, { kind: "externref" }],
        [{ kind: "externref" }],
      );
      flushLateImportShifts2(ctx, fctx);
      if (proxyIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: proxyIdx });
      }
      return { kind: "externref" };
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  if (ts13.isIdentifier(expr.expression) && expr.expression.text === "Function") {
    const args = expr.arguments ?? [];
    for (const arg of args) {
      const argResult = compileExpression2(ctx, fctx, arg);
      if (argResult) {
        fctx.body.push({ op: "drop" });
      }
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  if (ts13.isIdentifier(expr.expression) && expr.expression.text === "Date") {
    const dateTypeIdx = ensureDateStruct(ctx);
    const args = expr.arguments ?? [];
    if (args.length === 0) {
      const dateNowIdx = ensureLateImport2(ctx, "__date_now", [], [{ kind: "f64" }]);
      if (dateNowIdx !== void 0) {
        flushLateImportShifts2(ctx, fctx);
        fctx.body.push({ op: "call", funcIdx: dateNowIdx });
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      } else {
        fctx.body.push({ op: "i64.const", value: 0n });
      }
      fctx.body.push({ op: "struct.new", typeIdx: dateTypeIdx });
      return { kind: "ref", typeIdx: dateTypeIdx };
    }
    if (args.length === 1) {
      compileExpression2(ctx, fctx, args[0], { kind: "f64" });
      fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      fctx.body.push({ op: "struct.new", typeIdx: dateTypeIdx });
      return { kind: "ref", typeIdx: dateTypeIdx };
    }
    {
      const daysFromCivilIdx = ensureDateDaysFromCivilHelper(ctx);
      compileExpression2(ctx, fctx, args[0], { kind: "f64" });
      fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      const yearLocal = allocTempLocal(fctx, { kind: "i64" });
      fctx.body.push({ op: "local.set", index: yearLocal });
      compileExpression2(ctx, fctx, args[1], { kind: "f64" });
      fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      fctx.body.push({ op: "i64.const", value: 1n });
      fctx.body.push({ op: "i64.add" });
      const monthLocal = allocTempLocal(fctx, { kind: "i64" });
      fctx.body.push({ op: "local.set", index: monthLocal });
      if (args.length >= 3) {
        compileExpression2(ctx, fctx, args[2], { kind: "f64" });
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      } else {
        fctx.body.push({ op: "i64.const", value: 1n });
      }
      const dayLocal = allocTempLocal(fctx, { kind: "i64" });
      fctx.body.push({ op: "local.set", index: dayLocal });
      if (args.length >= 4) {
        compileExpression2(ctx, fctx, args[3], { kind: "f64" });
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      } else {
        fctx.body.push({ op: "i64.const", value: 0n });
      }
      const hoursLocal = allocTempLocal(fctx, { kind: "i64" });
      fctx.body.push({ op: "local.set", index: hoursLocal });
      if (args.length >= 5) {
        compileExpression2(ctx, fctx, args[4], { kind: "f64" });
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      } else {
        fctx.body.push({ op: "i64.const", value: 0n });
      }
      const minutesLocal = allocTempLocal(fctx, { kind: "i64" });
      fctx.body.push({ op: "local.set", index: minutesLocal });
      if (args.length >= 6) {
        compileExpression2(ctx, fctx, args[5], { kind: "f64" });
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      } else {
        fctx.body.push({ op: "i64.const", value: 0n });
      }
      const secondsLocal = allocTempLocal(fctx, { kind: "i64" });
      fctx.body.push({ op: "local.set", index: secondsLocal });
      if (args.length >= 7) {
        compileExpression2(ctx, fctx, args[6], { kind: "f64" });
        fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      } else {
        fctx.body.push({ op: "i64.const", value: 0n });
      }
      const msLocal = allocTempLocal(fctx, { kind: "i64" });
      fctx.body.push({ op: "local.set", index: msLocal });
      fctx.body.push(
        { op: "local.get", index: yearLocal },
        { op: "i64.const", value: 0n },
        { op: "i64.ge_s" },
        { op: "local.get", index: yearLocal },
        { op: "i64.const", value: 99n },
        { op: "i64.le_s" },
        { op: "i32.and" },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "local.get", index: yearLocal },
            { op: "i64.const", value: 1900n },
            { op: "i64.add" },
            { op: "local.set", index: yearLocal },
          ],
        },
      );
      fctx.body.push(
        { op: "local.get", index: yearLocal },
        { op: "local.get", index: monthLocal },
        { op: "local.get", index: dayLocal },
        { op: "call", funcIdx: daysFromCivilIdx },
      );
      fctx.body.push(
        { op: "i64.const", value: 86400000n },
        { op: "i64.mul" },
        { op: "local.get", index: hoursLocal },
        { op: "i64.const", value: 3600000n },
        { op: "i64.mul" },
        { op: "i64.add" },
        { op: "local.get", index: minutesLocal },
        { op: "i64.const", value: 60000n },
        { op: "i64.mul" },
        { op: "i64.add" },
        { op: "local.get", index: secondsLocal },
        { op: "i64.const", value: 1000n },
        { op: "i64.mul" },
        { op: "i64.add" },
        { op: "local.get", index: msLocal },
        { op: "i64.add" },
      );
      fctx.body.push({ op: "struct.new", typeIdx: dateTypeIdx });
      releaseTempLocal(fctx, msLocal);
      releaseTempLocal(fctx, secondsLocal);
      releaseTempLocal(fctx, minutesLocal);
      releaseTempLocal(fctx, hoursLocal);
      releaseTempLocal(fctx, dayLocal);
      releaseTempLocal(fctx, monthLocal);
      releaseTempLocal(fctx, yearLocal);
      return { kind: "ref", typeIdx: dateTypeIdx };
    }
  }
  if (ts13.isIdentifier(expr.expression)) {
    const TYPED_ARRAY_NAMES = /* @__PURE__ */ new Set([
      "Int8Array",
      "Uint8Array",
      "Uint8ClampedArray",
      "Int16Array",
      "Uint16Array",
      "Int32Array",
      "Uint32Array",
      "Float32Array",
      "Float64Array",
    ]);
    if (TYPED_ARRAY_NAMES.has(expr.expression.text)) {
      const elemWasm = { kind: "f64" };
      const vecTypeIdx = getOrRegisterVecType(ctx, "f64", elemWasm);
      const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
      const args = expr.arguments ?? [];
      if (args.length === 0) {
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
        fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
        return { kind: "ref_null", typeIdx: vecTypeIdx };
      }
      if (args.length === 1) {
        const argType = ctx.checker.getTypeAtLocation(args[0]);
        const argSym = argType.getSymbol?.();
        const isArrayLike =
          argSym?.name === "Array" ||
          ((argType.flags & ts13.TypeFlags.Object) !== 0 &&
            argSym?.name !== void 0 &&
            TYPED_ARRAY_NAMES.has(argSym.name));
        if (!isArrayLike || ts13.isNumericLiteral(args[0])) {
          compileExpression2(ctx, fctx, args[0], { kind: "f64" });
          fctx.body.push({ op: "i32.trunc_sat_f64_s" });
          const sizeLocal = allocLocal(fctx, `__ta_size_${fctx.locals.length}`, { kind: "i32" });
          fctx.body.push({ op: "local.tee", index: sizeLocal });
          fctx.body.push({ op: "local.get", index: sizeLocal });
          fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
          fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
          return { kind: "ref_null", typeIdx: vecTypeIdx };
        }
        const srcResult = compileExpression2(ctx, fctx, args[0]);
        if (srcResult && (srcResult.kind === "ref" || srcResult.kind === "ref_null")) {
          const srcTypeIdx = srcResult.typeIdx;
          const srcTypeDef = ctx.mod.types[srcTypeIdx];
          if (
            srcTypeDef?.kind === "struct" &&
            srcTypeDef.fields[0]?.name === "length" &&
            srcTypeDef.fields[1]?.name === "data"
          ) {
            const srcVecLocal = allocLocal(fctx, `__ta_src_${fctx.locals.length}`, srcResult);
            fctx.body.push({ op: "local.set", index: srcVecLocal });
            fctx.body.push({ op: "local.get", index: srcVecLocal });
            fctx.body.push({
              op: "struct.get",
              typeIdx: srcTypeIdx,
              fieldIdx: 0,
            });
            const lenLocal = allocLocal(fctx, `__ta_len_${fctx.locals.length}`, { kind: "i32" });
            fctx.body.push({ op: "local.tee", index: lenLocal });
            fctx.body.push({ op: "local.get", index: lenLocal });
            fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
            const dstDataLocal = allocLocal(fctx, `__ta_dst_${fctx.locals.length}`, {
              kind: "ref",
              typeIdx: arrTypeIdx,
            });
            fctx.body.push({ op: "local.set", index: dstDataLocal });
            const srcArrTypeIdx = getArrTypeIdxFromVec(ctx, srcTypeIdx);
            if (srcArrTypeIdx === arrTypeIdx) {
              fctx.body.push({ op: "local.get", index: dstDataLocal });
              fctx.body.push({ op: "i32.const", value: 0 });
              fctx.body.push({ op: "local.get", index: srcVecLocal });
              fctx.body.push({
                op: "struct.get",
                typeIdx: srcTypeIdx,
                fieldIdx: 1,
              });
              fctx.body.push({ op: "i32.const", value: 0 });
              fctx.body.push({ op: "local.get", index: lenLocal });
              fctx.body.push({
                op: "array.copy",
                dstTypeIdx: arrTypeIdx,
                srcTypeIdx: arrTypeIdx,
              });
            }
            fctx.body.push({ op: "local.get", index: lenLocal });
            fctx.body.push({ op: "local.get", index: dstDataLocal });
            fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
            return { kind: "ref_null", typeIdx: vecTypeIdx };
          }
        }
        if (srcResult) fctx.body.push({ op: "drop" });
        compileExpression2(ctx, fctx, args[0], { kind: "f64" });
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
        const fallbackSize = allocLocal(fctx, `__ta_fsz_${fctx.locals.length}`, { kind: "i32" });
        fctx.body.push({ op: "local.tee", index: fallbackSize });
        fctx.body.push({ op: "local.get", index: fallbackSize });
        fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
        fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
        return { kind: "ref_null", typeIdx: vecTypeIdx };
      }
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
      fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
      return { kind: "ref_null", typeIdx: vecTypeIdx };
    }
  }
  const type = ctx.checker.getTypeAtLocation(expr);
  const symbol = type.getSymbol();
  let className = symbol?.name;
  if (className && !ctx.classSet.has(className)) {
    const mapped = ctx.classExprNameMap.get(className);
    if (mapped) {
      className = mapped;
    }
  }
  if ((!className || !ctx.classSet.has(className)) && ts13.isIdentifier(expr.expression)) {
    const idName = expr.expression.text;
    if (ctx.classSet.has(idName)) {
      className = idName;
    } else {
      const mapped = ctx.classExprNameMap.get(idName);
      if (mapped && ctx.classSet.has(mapped)) {
        className = mapped;
      }
    }
  }
  if ((!className || !ctx.classSet.has(className)) && ts13.isIdentifier(expr.expression)) {
    const fnName = expr.expression.text;
    const cachedFnCtor = ctx.funcConstructorMap.get(fnName);
    if (cachedFnCtor) {
      const ctorFuncIdx = ctx.funcMap.get(cachedFnCtor.ctorFuncName);
      if (ctorFuncIdx !== void 0) {
        const paramTypes = getFuncParamTypes(ctx, ctorFuncIdx);
        const args = expr.arguments ?? [];
        for (let i = 0; i < args.length; i++) {
          compileExpression2(ctx, fctx, args[i], paramTypes?.[i]);
        }
        if (paramTypes) {
          for (let i = args.length; i < paramTypes.length; i++) {
            pushDefaultValue(fctx, paramTypes[i], ctx);
          }
        }
        const finalIdx = ctx.funcMap.get(cachedFnCtor.ctorFuncName) ?? ctorFuncIdx;
        fctx.body.push({ op: "call", funcIdx: finalIdx });
        return { kind: "ref", typeIdx: cachedFnCtor.structTypeIdx };
      }
    }
    if (!cachedFnCtor) {
      const exprSymbol = ctx.checker.getSymbolAtLocation(expr.expression);
      const decls = exprSymbol?.getDeclarations();
      if (decls) {
        for (const decl of decls) {
          if (ts13.isFunctionDeclaration(decl) && decl.body) {
            const result = compileNewFunctionDeclaration(ctx, fctx, expr, fnName, decl);
            if (result) return result;
            break;
          }
          if (ts13.isVariableDeclaration(decl) && decl.initializer) {
            let init = decl.initializer;
            while (ts13.isParenthesizedExpression(init)) init = init.expression;
            if (ts13.isFunctionExpression(init) && init.body) {
              const result = compileNewFunctionDeclaration(ctx, fctx, expr, fnName, init);
              if (result) return result;
              break;
            }
          }
        }
      }
    }
  }
  if (!className) {
    const ctorName = ts13.isIdentifier(expr.expression) ? expr.expression.text : "__unknown";
    const args = expr.arguments ?? [];
    if (ctorName === "ArrayBuffer" && args.length >= 1) {
      compileExpression2(ctx, fctx, args[0], { kind: "f64" });
      const lenF64 = allocLocal(fctx, `__ab_len_f64_${fctx.locals.length}`, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: lenF64 });
      fctx.body.push({ op: "local.get", index: lenF64 });
      fctx.body.push({ op: "local.get", index: lenF64 });
      fctx.body.push({ op: "f64.floor" });
      fctx.body.push({ op: "f64.ne" });
      fctx.body.push({ op: "local.get", index: lenF64 });
      fctx.body.push({ op: "f64.const", value: 0 });
      fctx.body.push({ op: "f64.lt" });
      fctx.body.push({ op: "i32.or" });
      {
        const rangeErrMsg = "RangeError: Invalid array buffer length";
        addStringConstantGlobal(ctx, rangeErrMsg);
        const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
        const tagIdx = ensureExnTag(ctx);
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "global.get", index: strIdx },
            { op: "throw", tagIdx },
          ],
          else: [],
        });
      }
    }
    if (ctorName === "DataView") {
      if (args.length >= 2) {
        compileExpression2(ctx, fctx, args[1], { kind: "f64" });
        const offsetF64 = allocLocal(fctx, `__dv_offset_f64_${fctx.locals.length}`, { kind: "f64" });
        fctx.body.push({ op: "local.set", index: offsetF64 });
        fctx.body.push({ op: "local.get", index: offsetF64 });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.lt" });
        fctx.body.push({ op: "local.get", index: offsetF64 });
        fctx.body.push({ op: "local.get", index: offsetF64 });
        fctx.body.push({ op: "f64.floor" });
        fctx.body.push({ op: "f64.ne" });
        fctx.body.push({ op: "i32.or" });
        {
          const rangeErrMsg = "RangeError: Start offset is outside the bounds of the buffer";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "global.get", index: strIdx },
              { op: "throw", tagIdx },
            ],
            else: [],
          });
        }
      }
      if (args.length >= 3) {
        compileExpression2(ctx, fctx, args[2], { kind: "f64" });
        const lenF64 = allocLocal(fctx, `__dv_len_f64_${fctx.locals.length}`, { kind: "f64" });
        fctx.body.push({ op: "local.set", index: lenF64 });
        fctx.body.push({ op: "local.get", index: lenF64 });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.lt" });
        fctx.body.push({ op: "local.get", index: lenF64 });
        fctx.body.push({ op: "local.get", index: lenF64 });
        fctx.body.push({ op: "f64.floor" });
        fctx.body.push({ op: "f64.ne" });
        fctx.body.push({ op: "i32.or" });
        {
          const rangeErrMsg = "RangeError: Invalid DataView length";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "global.get", index: strIdx },
              { op: "throw", tagIdx },
            ],
            else: [],
          });
        }
      }
    }
    if (ctorName === "Array" && args.length === 1) {
      compileExpression2(ctx, fctx, args[0], { kind: "f64" });
      const nF64 = allocLocal(fctx, `__arr_n_f64_${fctx.locals.length}`, { kind: "f64" });
      fctx.body.push({ op: "local.set", index: nF64 });
      fctx.body.push({ op: "local.get", index: nF64 });
      fctx.body.push({ op: "local.get", index: nF64 });
      fctx.body.push({ op: "f64.floor" });
      fctx.body.push({ op: "f64.ne" });
      fctx.body.push({ op: "local.get", index: nF64 });
      fctx.body.push({ op: "f64.const", value: 0 });
      fctx.body.push({ op: "f64.lt" });
      fctx.body.push({ op: "i32.or" });
      fctx.body.push({ op: "local.get", index: nF64 });
      fctx.body.push({ op: "f64.const", value: 4294967296 });
      fctx.body.push({ op: "f64.ge" });
      fctx.body.push({ op: "i32.or" });
      {
        const rangeErrMsg = "RangeError: Invalid array length";
        addStringConstantGlobal(ctx, rangeErrMsg);
        const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
        const tagIdx = ensureExnTag(ctx);
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "global.get", index: strIdx },
            { op: "throw", tagIdx },
          ],
          else: [],
        });
      }
    }
    const importName = `__new_${ctorName}`;
    const funcIdx = ctx.funcMap.get(importName);
    if (funcIdx !== void 0) {
      for (const arg of args) {
        const resultType = compileExpression2(ctx, fctx, arg, {
          kind: "externref",
        });
        if (resultType && resultType.kind !== "externref") {
          fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "ref.null.extern" });
        }
      }
      const importParamTypes = getFuncParamTypes(ctx, funcIdx);
      if (importParamTypes) {
        for (let i = args.length; i < importParamTypes.length; i++) {
          pushDefaultValue(fctx, importParamTypes[i], ctx);
        }
      }
      const finalNewIdx = ctx.funcMap.get(importName) ?? funcIdx;
      fctx.body.push({ op: "call", funcIdx: finalNewIdx });
    } else {
      fctx.body.push({ op: "ref.null.extern" });
    }
    return { kind: "externref" };
  }
  if (ctx.classSet.has(className)) {
    const ctorName = `${className}_new`;
    const funcIdx = ctx.funcMap.get(ctorName);
    if (funcIdx === void 0) {
      ctx.errors.push({
        message: `Missing constructor for class: ${className}`,
        line: getLine(expr),
        column: getCol(expr),
      });
      return null;
    }
    const paramTypes = getFuncParamTypes(ctx, funcIdx);
    const args = expr.arguments ?? [];
    const ctorRestInfo = ctx.funcRestParams.get(ctorName);
    const hasSpreadCtorArg = args.some((a) => ts13.isSpreadElement(a));
    if (hasSpreadCtorArg && paramTypes) {
      const flatCtorArgs = flattenCallArgs(args);
      if (flatCtorArgs) {
        for (let i = 0; i < flatCtorArgs.length && i < paramTypes.length; i++) {
          compileExpression2(ctx, fctx, flatCtorArgs[i], paramTypes[i]);
        }
        for (let i = flatCtorArgs.length; i < paramTypes.length; i++) {
          pushDefaultValue(fctx, paramTypes[i], ctx);
        }
      } else {
        compileSpreadCallArgs(ctx, fctx, expr, funcIdx, ctorRestInfo);
      }
    } else if (ctorRestInfo && !hasSpreadCtorArg) {
      for (let i = 0; i < ctorRestInfo.restIndex; i++) {
        if (i < args.length) {
          compileExpression2(ctx, fctx, args[i], paramTypes?.[i]);
        } else {
          pushDefaultValue(fctx, paramTypes?.[i] ?? { kind: "f64" }, ctx);
        }
      }
      const restArgCount = Math.max(0, args.length - ctorRestInfo.restIndex);
      fctx.body.push({ op: "i32.const", value: restArgCount });
      for (let i = ctorRestInfo.restIndex; i < args.length; i++) {
        compileExpression2(ctx, fctx, args[i], ctorRestInfo.elemType);
      }
      fctx.body.push({
        op: "array.new_fixed",
        typeIdx: ctorRestInfo.arrayTypeIdx,
        length: restArgCount,
      });
      fctx.body.push({ op: "struct.new", typeIdx: ctorRestInfo.vecTypeIdx });
    } else {
      for (let i = 0; i < args.length; i++) {
        compileExpression2(ctx, fctx, args[i], paramTypes?.[i]);
      }
      if (paramTypes) {
        for (let i = args.length; i < paramTypes.length; i++) {
          pushDefaultValue(fctx, paramTypes[i], ctx);
        }
      }
    }
    const finalCtorIdx = ctx.funcMap.get(ctorName) ?? funcIdx;
    fctx.body.push({ op: "call", funcIdx: finalCtorIdx });
    const structTypeIdx = ctx.structMap.get(className);
    return { kind: "ref", typeIdx: structTypeIdx };
  }
  const externInfo = ctx.externClasses.get(className);
  if (externInfo) {
    const args = expr.arguments ?? [];
    for (let i = 0; i < args.length; i++) {
      compileExpression2(ctx, fctx, args[i], externInfo.constructorParams[i]);
    }
    for (let i = args.length; i < externInfo.constructorParams.length; i++) {
      pushDefaultValue(fctx, externInfo.constructorParams[i], ctx);
    }
    const importName = `${externInfo.importPrefix}_new`;
    const funcIdx = ctx.funcMap.get(importName);
    if (funcIdx === void 0) {
      ctx.errors.push({
        message: `Missing import for constructor: ${importName}`,
        line: getLine(expr),
        column: getCol(expr),
      });
      return null;
    }
    fctx.body.push({ op: "call", funcIdx });
    return { kind: "externref" };
  }
  {
    const TYPED_ARRAY_CTORS = /* @__PURE__ */ new Set([
      "Int8Array",
      "Uint8Array",
      "Int16Array",
      "Uint16Array",
      "Int32Array",
      "Uint32Array",
      "Float32Array",
      "Float64Array",
    ]);
    if (className && TYPED_ARRAY_CTORS.has(className)) {
      const elemType = { kind: "f64" };
      const vecTypeIdx = getOrRegisterVecType(ctx, "f64", elemType);
      const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
      const args = expr.arguments ?? [];
      if (args.length === 0) {
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
        fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
      } else {
        compileExpression2(ctx, fctx, args[0], { kind: "f64" });
        fctx.body.push({ op: "i32.trunc_sat_f64_s" });
        const sizeLocal = allocLocal(fctx, `__ta_size_${fctx.locals.length}`, {
          kind: "i32",
        });
        fctx.body.push({ op: "local.tee", index: sizeLocal });
        fctx.body.push({ op: "local.get", index: sizeLocal });
        fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
        fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
      }
      return { kind: "ref_null", typeIdx: vecTypeIdx };
    }
  }
  if (className === "ArrayBuffer") {
    const elemType = { kind: "i32" };
    const vecTypeIdx = getOrRegisterVecType(ctx, "i32_byte", elemType);
    const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
    const args = expr.arguments ?? [];
    if (args.length >= 1) {
      compileExpression2(ctx, fctx, args[0], { kind: "f64" });
      const lenF64Local = allocLocal(fctx, `__ab_len_f64_${fctx.locals.length}`, { kind: "f64" });
      fctx.body.push({ op: "local.tee", index: lenF64Local });
      fctx.body.push({ op: "local.get", index: lenF64Local });
      fctx.body.push({ op: "f64.floor" });
      fctx.body.push({ op: "f64.ne" });
      fctx.body.push({ op: "local.get", index: lenF64Local });
      fctx.body.push({ op: "f64.const", value: 0 });
      fctx.body.push({ op: "f64.lt" });
      fctx.body.push({ op: "i32.or" });
      {
        const rangeErrMsg = "RangeError: Invalid array buffer length";
        addStringConstantGlobal(ctx, rangeErrMsg);
        const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
        const tagIdx = ensureExnTag(ctx);
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "global.get", index: strIdx },
            { op: "throw", tagIdx },
          ],
          else: [],
        });
      }
      fctx.body.push({ op: "local.get", index: lenF64Local });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    const sizeLocal = allocLocal(fctx, `__ab_size_${fctx.locals.length}`, {
      kind: "i32",
    });
    fctx.body.push({ op: "local.tee", index: sizeLocal });
    fctx.body.push({ op: "local.get", index: sizeLocal });
    fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  if (className === "DataView") {
    const elemType = { kind: "i32" };
    const vecTypeIdx = getOrRegisterVecType(ctx, "i32_byte", elemType);
    const args = expr.arguments ?? [];
    if (args.length >= 1) {
      const resultType = compileExpression2(ctx, fctx, args[0]);
      if (args.length >= 2) {
        const bufLocal = allocLocal(
          fctx,
          `__dv_buf_${fctx.locals.length}`,
          resultType && (resultType.kind === "ref" || resultType.kind === "ref_null")
            ? resultType
            : { kind: "externref" },
        );
        fctx.body.push({ op: "local.set", index: bufLocal });
        compileExpression2(ctx, fctx, args[1], { kind: "f64" });
        const offsetF64 = allocLocal(fctx, `__dv_offset_f64_${fctx.locals.length}`, { kind: "f64" });
        fctx.body.push({ op: "local.tee", index: offsetF64 });
        fctx.body.push({ op: "f64.const", value: 0 });
        fctx.body.push({ op: "f64.lt" });
        fctx.body.push({ op: "local.get", index: offsetF64 });
        fctx.body.push({ op: "local.get", index: offsetF64 });
        fctx.body.push({ op: "f64.floor" });
        fctx.body.push({ op: "f64.ne" });
        fctx.body.push({ op: "i32.or" });
        if (resultType && (resultType.kind === "ref" || resultType.kind === "ref_null")) {
          fctx.body.push({ op: "local.get", index: offsetF64 });
          fctx.body.push({ op: "local.get", index: bufLocal });
          fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
          fctx.body.push({ op: "f64.convert_i32_s" });
          fctx.body.push({ op: "f64.gt" });
          fctx.body.push({ op: "i32.or" });
        }
        {
          const rangeErrMsg = "RangeError: Start offset is outside the bounds of the buffer";
          addStringConstantGlobal(ctx, rangeErrMsg);
          const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
          const tagIdx = ensureExnTag(ctx);
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: [
              { op: "global.get", index: strIdx },
              { op: "throw", tagIdx },
            ],
            else: [],
          });
        }
        if (args.length >= 3) {
          compileExpression2(ctx, fctx, args[2], { kind: "f64" });
          const lenF64 = allocLocal(fctx, `__dv_len_f64_${fctx.locals.length}`, { kind: "f64" });
          fctx.body.push({ op: "local.tee", index: lenF64 });
          fctx.body.push({ op: "f64.const", value: 0 });
          fctx.body.push({ op: "f64.lt" });
          fctx.body.push({ op: "local.get", index: lenF64 });
          fctx.body.push({ op: "local.get", index: lenF64 });
          fctx.body.push({ op: "f64.floor" });
          fctx.body.push({ op: "f64.ne" });
          fctx.body.push({ op: "i32.or" });
          if (resultType && (resultType.kind === "ref" || resultType.kind === "ref_null")) {
            fctx.body.push({ op: "local.get", index: offsetF64 });
            fctx.body.push({ op: "local.get", index: lenF64 });
            fctx.body.push({ op: "f64.add" });
            fctx.body.push({ op: "local.get", index: bufLocal });
            fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
            fctx.body.push({ op: "f64.convert_i32_s" });
            fctx.body.push({ op: "f64.gt" });
            fctx.body.push({ op: "i32.or" });
          }
          {
            const rangeErrMsg = "RangeError: Invalid DataView length";
            addStringConstantGlobal(ctx, rangeErrMsg);
            const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
            const tagIdx = ensureExnTag(ctx);
            fctx.body.push({
              op: "if",
              blockType: { kind: "empty" },
              then: [
                { op: "global.get", index: strIdx },
                { op: "throw", tagIdx },
              ],
              else: [],
            });
          }
        }
        fctx.body.push({ op: "local.get", index: bufLocal });
        if (resultType && (resultType.kind === "ref" || resultType.kind === "ref_null")) {
          return resultType;
        }
        if (resultType) return resultType;
        return { kind: "ref_null", typeIdx: vecTypeIdx };
      }
      if (resultType && (resultType.kind === "ref" || resultType.kind === "ref_null")) {
        return resultType;
      }
      if (resultType) return resultType;
      return { kind: "ref_null", typeIdx: vecTypeIdx };
    } else {
      const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
      fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
      return { kind: "ref_null", typeIdx: vecTypeIdx };
    }
  }
  if (className === "Array") {
    const ctxType = ctx.checker.getContextualType(expr);
    const exprType = ctxType ?? ctx.checker.getTypeAtLocation(expr);
    let inferredElemWasm = null;
    const rawTypeArgs = ctx.checker.getTypeArguments(exprType);
    if (rawTypeArgs?.[0] && rawTypeArgs[0].flags & ts13.TypeFlags.Any) {
      const inferredElemTsType = inferArrayElementType(ctx, expr);
      if (inferredElemTsType) {
        inferredElemWasm = resolveWasmType(ctx, inferredElemTsType);
      }
    }
    let vecTypeIdx;
    let arrTypeIdx;
    let elemWasm;
    if (inferredElemWasm) {
      const elemKey =
        inferredElemWasm.kind === "ref" || inferredElemWasm.kind === "ref_null"
          ? `ref_${inferredElemWasm.typeIdx}`
          : inferredElemWasm.kind;
      vecTypeIdx = getOrRegisterVecType(ctx, elemKey, inferredElemWasm);
      arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
      elemWasm = inferredElemWasm;
    } else {
      const resolved = resolveWasmType(ctx, exprType);
      vecTypeIdx = resolved.typeIdx;
      arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
      const typeArgs = ctx.checker.getTypeArguments(exprType);
      const elemTsType = typeArgs?.[0];
      elemWasm = elemTsType ? resolveWasmType(ctx, elemTsType) : { kind: "f64" };
    }
    if (arrTypeIdx < 0) {
      vecTypeIdx = getOrRegisterVecType(ctx, "externref", { kind: "externref" });
      arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
      elemWasm = { kind: "externref" };
    }
    const args = expr.arguments ?? [];
    if (args.length === 0) {
      const DEFAULT_CAPACITY = 64;
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.const", value: DEFAULT_CAPACITY });
      fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
      fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
      return { kind: "ref_null", typeIdx: vecTypeIdx };
    }
    if (args.length === 1) {
      compileExpression2(ctx, fctx, args[0], { kind: "f64" });
      const nF64Local = allocLocal(fctx, `__arr_n_f64_${fctx.locals.length}`, { kind: "f64" });
      fctx.body.push({ op: "local.tee", index: nF64Local });
      fctx.body.push({ op: "local.get", index: nF64Local });
      fctx.body.push({ op: "f64.floor" });
      fctx.body.push({ op: "f64.ne" });
      fctx.body.push({ op: "local.get", index: nF64Local });
      fctx.body.push({ op: "f64.const", value: 0 });
      fctx.body.push({ op: "f64.lt" });
      fctx.body.push({ op: "i32.or" });
      fctx.body.push({ op: "local.get", index: nF64Local });
      fctx.body.push({ op: "f64.const", value: 4294967296 });
      fctx.body.push({ op: "f64.ge" });
      fctx.body.push({ op: "i32.or" });
      {
        const rangeErrMsg = "RangeError: Invalid array length";
        addStringConstantGlobal(ctx, rangeErrMsg);
        const strIdx = ctx.stringGlobalMap.get(rangeErrMsg);
        const tagIdx = ensureExnTag(ctx);
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "global.get", index: strIdx },
            { op: "throw", tagIdx },
          ],
          else: [],
        });
      }
      fctx.body.push({ op: "local.get", index: nF64Local });
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
      const sizeLocal = allocLocal(fctx, `__arr_size_${fctx.locals.length}`, {
        kind: "i32",
      });
      fctx.body.push({ op: "local.tee", index: sizeLocal });
      fctx.body.push({ op: "local.get", index: sizeLocal });
      fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
      fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
      return { kind: "ref_null", typeIdx: vecTypeIdx };
    }
    for (const arg of args) {
      compileExpression2(ctx, fctx, arg, elemWasm);
    }
    fctx.body.push({
      op: "array.new_fixed",
      typeIdx: arrTypeIdx,
      length: args.length,
    });
    const tmpData = allocLocal(fctx, `__arr_data_${fctx.locals.length}`, {
      kind: "ref",
      typeIdx: arrTypeIdx,
    });
    fctx.body.push({ op: "local.set", index: tmpData });
    fctx.body.push({ op: "i32.const", value: args.length });
    fctx.body.push({ op: "local.get", index: tmpData });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
    return { kind: "ref_null", typeIdx: vecTypeIdx };
  }
  ctx.errors.push({
    message: `Unsupported new expression for class: ${className}`,
    line: getLine(expr),
    column: getCol(expr),
  });
  return null;
}
function findExternInfoForMember(ctx, className, memberName, kind) {
  let current = className;
  while (current) {
    const info = ctx.externClasses.get(current);
    if (info) {
      if (kind === "method" && info.methods.has(memberName)) return info;
      if (kind === "property" && info.properties.has(memberName)) return info;
    }
    current = ctx.externClassParent.get(current);
  }
  return null;
}
function compileExternMethodCall(ctx, fctx, propAccess, callExpr) {
  const receiverType = ctx.checker.getTypeAtLocation(propAccess.expression);
  const className = receiverType.getSymbol()?.name;
  const methodName = propAccess.name.text;
  if (!className) return null;
  const resolvedInfo = findExternInfoForMember(ctx, className, methodName, "method");
  const externInfo = resolvedInfo ?? ctx.externClasses.get(className);
  if (!externInfo) {
    ctx.errors.push({
      message: `Unknown extern class: ${className}`,
      line: getLine(callExpr),
      column: getCol(callExpr),
    });
    return null;
  }
  compileExpression2(ctx, fctx, propAccess.expression);
  const methodOwner = resolvedInfo ?? externInfo;
  const methodInfo = methodOwner.methods.get(methodName);
  const extMethodParamCount = methodInfo ? methodInfo.params.length - 1 : callExpr.arguments.length;
  for (let i = 0; i < callExpr.arguments.length; i++) {
    if (i < extMethodParamCount) {
      const hint = methodInfo?.params[i + 1];
      compileExpression2(ctx, fctx, callExpr.arguments[i], hint);
    } else {
      const extraType = compileExpression2(ctx, fctx, callExpr.arguments[i]);
      if (extraType !== null) {
        fctx.body.push({ op: "drop" });
      }
    }
  }
  if (methodInfo) {
    const actualArgs = Math.min(callExpr.arguments.length, extMethodParamCount) + 1;
    for (let i = actualArgs; i < methodInfo.params.length; i++) {
      pushDefaultValue(fctx, methodInfo.params[i], ctx);
    }
  }
  const importName = `${methodOwner.importPrefix}_${methodName}`;
  const funcIdx = ctx.funcMap.get(importName);
  if (funcIdx === void 0) {
    ctx.errors.push({
      message: `Missing import for method: ${importName}`,
      line: getLine(callExpr),
      column: getCol(callExpr),
    });
    return null;
  }
  fctx.body.push({ op: "call", funcIdx });
  if (!methodInfo || methodInfo.results.length === 0) return VOID_RESULT;
  return methodInfo.results[0];
}
function emitLazyProtoGet(ctx, fctx, className) {
  const protoGlobalIdx = ctx.protoGlobals?.get(className);
  if (protoGlobalIdx === void 0) return false;
  const structTypeIdx = ctx.structMap.get(className);
  const fields = ctx.structFields.get(className);
  if (structTypeIdx === void 0 || !fields) return false;
  const initBody = [];
  for (const field of fields) {
    if (field.name === "__tag") {
      const tag = ctx.classTagMap.get(className) ?? 0;
      initBody.push({ op: "i32.const", value: tag });
    } else {
      switch (field.type.kind) {
        case "f64":
          initBody.push({ op: "f64.const", value: 0 });
          break;
        case "i32":
          initBody.push({ op: "i32.const", value: 0 });
          break;
        case "i64":
          initBody.push({ op: "i64.const", value: 0n });
          break;
        case "externref":
          initBody.push({ op: "ref.null.extern" });
          break;
        case "ref_null":
          initBody.push({ op: "ref.null", typeIdx: field.type.typeIdx });
          break;
        case "ref":
          initBody.push({ op: "ref.null", typeIdx: field.type.typeIdx });
          break;
        default:
          initBody.push({ op: "i32.const", value: 0 });
          break;
      }
    }
  }
  initBody.push({ op: "struct.new", typeIdx: structTypeIdx });
  initBody.push({ op: "extern.convert_any" });
  initBody.push({ op: "global.set", index: protoGlobalIdx });
  fctx.body.push({ op: "global.get", index: protoGlobalIdx });
  fctx.body.push({ op: "ref.is_null" });
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: initBody,
    else: [],
  });
  fctx.body.push({ op: "global.get", index: protoGlobalIdx });
  return true;
}
function patchStructNewForDynamicField(ctx, structTypeIdx, newFieldType) {
  for (const func of ctx.mod.functions) {
    if (!func.body || func.body.length === 0) continue;
    patchStructNewInBody(func.body, structTypeIdx, newFieldType);
  }
  if (ctx.currentFunc) {
    patchStructNewInBody(ctx.currentFunc.body, structTypeIdx, newFieldType);
    if (ctx.currentFunc.savedBodies) {
      for (const savedBody of ctx.currentFunc.savedBodies) {
        patchStructNewInBody(savedBody, structTypeIdx, newFieldType);
      }
    }
  }
}
function patchStructNewInBody(body, structTypeIdx, newFieldType) {
  for (let i = 0; i < body.length; i++) {
    const instr = body[i];
    if (instr.op === "struct.new" && instr.typeIdx === structTypeIdx) {
      const defaultInstr = defaultValueInstrForType(newFieldType);
      body.splice(i, 0, ...defaultInstr);
      i += defaultInstr.length;
    }
    if (instr.then) patchStructNewInBody(instr.then, structTypeIdx, newFieldType);
    if (instr.else) patchStructNewInBody(instr.else, structTypeIdx, newFieldType);
    if (instr.body) {
      const nestedBody = instr.body;
      if (Array.isArray(nestedBody)) patchStructNewInBody(nestedBody, structTypeIdx, newFieldType);
    }
    if (instr.instrs) {
      const nestedInstrs = instr.instrs;
      if (Array.isArray(nestedInstrs)) patchStructNewInBody(nestedInstrs, structTypeIdx, newFieldType);
    }
    if (instr.catches) {
      for (const c of instr.catches) {
        if (Array.isArray(c.body)) patchStructNewInBody(c.body, structTypeIdx, newFieldType);
      }
    }
    if (instr.catchAll) {
      if (Array.isArray(instr.catchAll)) patchStructNewInBody(instr.catchAll, structTypeIdx, newFieldType);
    }
  }
}
function defaultValueInstrForType(type) {
  switch (type.kind) {
    case "f64":
      return [{ op: "f64.const", value: 0 }];
    case "i32":
      return [{ op: "i32.const", value: 0 }];
    case "externref":
      return [{ op: "ref.null.extern" }];
    case "ref_null":
      return [{ op: "ref.null", typeIdx: type.typeIdx }];
    case "ref":
      return [{ op: "ref.null", typeIdx: type.typeIdx }, { op: "ref.as_non_null" }];
    case "eqref":
      return [{ op: "ref.null.eq" }];
    default:
      return [{ op: "i32.const", value: 0 }];
  }
}
function compileSpreadCallArgs(ctx, fctx, expr, funcIdx, restInfo) {
  const paramTypes = getFuncParamTypes(ctx, funcIdx);
  if (restInfo) {
    let argIdx = 0;
    for (let i = 0; i < restInfo.restIndex; i++) {
      if (argIdx < expr.arguments.length) {
        compileExpression2(ctx, fctx, expr.arguments[argIdx], paramTypes?.[i]);
        argIdx++;
      }
    }
    if (argIdx < expr.arguments.length) {
      const restArg = expr.arguments[argIdx];
      if (ts13.isSpreadElement(restArg)) {
        compileExpression2(ctx, fctx, restArg.expression);
      } else {
        fctx.body.push({ op: "i32.const", value: 1 });
        compileExpression2(ctx, fctx, restArg, restInfo.elemType);
        fctx.body.push({
          op: "array.new_fixed",
          typeIdx: restInfo.arrayTypeIdx,
          length: 1,
        });
        fctx.body.push({ op: "struct.new", typeIdx: restInfo.vecTypeIdx });
      }
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({
        op: "array.new_fixed",
        typeIdx: restInfo.arrayTypeIdx,
        length: 0,
      });
      fctx.body.push({ op: "struct.new", typeIdx: restInfo.vecTypeIdx });
    }
    return;
  }
  if (!paramTypes) return;
  let paramIdx = 0;
  for (const arg of expr.arguments) {
    if (ts13.isSpreadElement(arg)) {
      const vecType = compileExpression2(ctx, fctx, arg.expression);
      if (!vecType || (vecType.kind !== "ref" && vecType.kind !== "ref_null")) continue;
      const vecTypeDef = ctx.mod.types[vecType.typeIdx];
      if (!vecTypeDef || vecTypeDef.kind !== "struct") continue;
      const vecLocal = allocLocal(fctx, `__spread_vec_${fctx.locals.length}`, vecType);
      fctx.body.push({ op: "local.set", index: vecLocal });
      const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecType.typeIdx);
      if (arrTypeIdx < 0) continue;
      const dataLocal = allocLocal(fctx, `__spread_data_${fctx.locals.length}`, {
        kind: "ref_null",
        typeIdx: arrTypeIdx,
      });
      fctx.body.push({ op: "local.get", index: vecLocal });
      fctx.body.push({
        op: "struct.get",
        typeIdx: vecType.typeIdx,
        fieldIdx: 1,
      });
      fctx.body.push({ op: "local.set", index: dataLocal });
      const arrDefSpread = ctx.mod.types[arrTypeIdx];
      const spreadElemType = arrDefSpread && arrDefSpread.kind === "array" ? arrDefSpread.element : { kind: "f64" };
      const remainingParams = paramTypes.length - paramIdx;
      for (let i = 0; i < remainingParams; i++) {
        fctx.body.push({ op: "local.get", index: dataLocal });
        fctx.body.push({ op: "i32.const", value: i });
        emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, spreadElemType);
        const expectedParamType = paramTypes[paramIdx];
        if (expectedParamType && !valTypesMatch(spreadElemType, expectedParamType)) {
          coerceType3(ctx, fctx, spreadElemType, expectedParamType);
        }
        paramIdx++;
      }
    } else {
      compileExpression2(ctx, fctx, arg, paramTypes[paramIdx]);
      paramIdx++;
    }
  }
}
function compileConsoleCall(ctx, fctx, expr, method) {
  if (ctx.wasi) {
    return compileConsoleCallWasi(ctx, fctx, expr, method);
  }
  for (const arg of expr.arguments) {
    const argType = ctx.checker.getTypeAtLocation(arg);
    compileExpression2(ctx, fctx, arg);
    if (isStringType(argType)) {
      if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
        const strFlattenIdx = ctx.nativeStrHelpers.get("__str_flatten");
        if (strFlattenIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: strFlattenIdx });
        }
        const toExternIdx = ctx.nativeStrHelpers.get("__str_to_extern");
        if (toExternIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: toExternIdx });
        }
      }
      const funcIdx = ctx.funcMap.get(`console_${method}_string`);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
      }
    } else if (isBooleanType(argType)) {
      const funcIdx = ctx.funcMap.get(`console_${method}_bool`);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
      }
    } else if (isNumberType(argType)) {
      const funcIdx = ctx.funcMap.get(`console_${method}_number`);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
      }
    } else {
      const funcIdx = ctx.funcMap.get(`console_${method}_externref`);
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
      }
    }
  }
  return VOID_RESULT;
}
function ensureDateStruct(ctx) {
  const existing = ctx.structMap.get("__Date");
  if (existing !== void 0) return existing;
  const typeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "__Date",
    fields: [{ name: "timestamp", type: { kind: "i64" }, mutable: true }],
  });
  ctx.structMap.set("__Date", typeIdx);
  ctx.typeIdxToStructName.set(typeIdx, "__Date");
  ctx.structFields.set("__Date", [{ name: "timestamp", type: { kind: "i64" }, mutable: true }]);
  return typeIdx;
}
function ensureDateCivilHelper(ctx) {
  const existing = ctx.funcMap.get("__date_civil_from_days");
  if (existing !== void 0) return existing;
  const funcTypeIdx = addFuncType(ctx, [{ kind: "i64" }], [{ kind: "i64" }]);
  const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set("__date_civil_from_days", funcIdx);
  const body = [];
  body.push(
    { op: "local.get", index: 0 },
    { op: "i64.const", value: 719468n },
    { op: "i64.add" },
    { op: "local.set", index: 0 },
  );
  body.push(
    { op: "local.get", index: 0 },
    { op: "i64.const", value: 0n },
    { op: "i64.ge_s" },
    {
      op: "if",
      blockType: { kind: "val", type: { kind: "i64" } },
      then: [{ op: "local.get", index: 0 }],
      else: [{ op: "local.get", index: 0 }, { op: "i64.const", value: 146096n }, { op: "i64.sub" }],
    },
    { op: "i64.const", value: 146097n },
    { op: "i64.div_s" },
    { op: "local.set", index: 1 },
    // era
  );
  body.push(
    { op: "local.get", index: 0 },
    { op: "local.get", index: 1 },
    { op: "i64.const", value: 146097n },
    { op: "i64.mul" },
    { op: "i64.sub" },
    { op: "local.set", index: 2 },
    // doe
  );
  body.push(
    { op: "local.get", index: 2 },
    // doe
    { op: "local.get", index: 2 },
    { op: "i64.const", value: 1460n },
    { op: "i64.div_s" },
    { op: "i64.sub" },
    { op: "local.get", index: 2 },
    { op: "i64.const", value: 36524n },
    { op: "i64.div_s" },
    { op: "i64.add" },
    { op: "local.get", index: 2 },
    { op: "i64.const", value: 146096n },
    { op: "i64.div_s" },
    { op: "i64.sub" },
    { op: "i64.const", value: 365n },
    { op: "i64.div_s" },
    { op: "local.set", index: 3 },
    // yoe
  );
  body.push(
    { op: "local.get", index: 3 },
    { op: "local.get", index: 1 },
    { op: "i64.const", value: 400n },
    { op: "i64.mul" },
    { op: "i64.add" },
    { op: "local.set", index: 6 },
    // y (still March-based)
  );
  body.push(
    { op: "local.get", index: 2 },
    // doe
    { op: "i64.const", value: 365n },
    { op: "local.get", index: 3 },
    { op: "i64.mul" },
    { op: "local.get", index: 3 },
    { op: "i64.const", value: 4n },
    { op: "i64.div_s" },
    { op: "i64.add" },
    { op: "local.get", index: 3 },
    { op: "i64.const", value: 100n },
    { op: "i64.div_s" },
    { op: "i64.sub" },
    { op: "i64.sub" },
    { op: "local.set", index: 4 },
    // doy
  );
  body.push(
    { op: "i64.const", value: 5n },
    { op: "local.get", index: 4 },
    { op: "i64.mul" },
    { op: "i64.const", value: 2n },
    { op: "i64.add" },
    { op: "i64.const", value: 153n },
    { op: "i64.div_s" },
    { op: "local.set", index: 5 },
    // mp
  );
  body.push(
    { op: "local.get", index: 4 },
    { op: "i64.const", value: 153n },
    { op: "local.get", index: 5 },
    { op: "i64.mul" },
    { op: "i64.const", value: 2n },
    { op: "i64.add" },
    { op: "i64.const", value: 5n },
    { op: "i64.div_s" },
    { op: "i64.sub" },
    { op: "i64.const", value: 1n },
    { op: "i64.add" },
    { op: "local.set", index: 8 },
    // d
  );
  body.push(
    { op: "local.get", index: 5 },
    { op: "i64.const", value: 10n },
    { op: "i64.lt_s" },
    {
      op: "if",
      blockType: { kind: "val", type: { kind: "i64" } },
      then: [{ op: "local.get", index: 5 }, { op: "i64.const", value: 3n }, { op: "i64.add" }],
      else: [{ op: "local.get", index: 5 }, { op: "i64.const", value: 9n }, { op: "i64.sub" }],
    },
    { op: "local.set", index: 7 },
    // m (1-12)
  );
  body.push(
    { op: "local.get", index: 6 },
    { op: "local.get", index: 7 },
    { op: "i64.const", value: 2n },
    { op: "i64.le_s" },
    { op: "i64.extend_i32_s" },
    { op: "i64.add" },
    { op: "local.set", index: 6 },
    // y (adjusted)
  );
  body.push(
    { op: "local.get", index: 6 },
    { op: "i64.const", value: 10000n },
    { op: "i64.mul" },
    { op: "local.get", index: 7 },
    { op: "i64.const", value: 100n },
    { op: "i64.mul" },
    { op: "i64.add" },
    { op: "local.get", index: 8 },
    { op: "i64.add" },
  );
  ctx.mod.functions.push({
    name: "__date_civil_from_days",
    typeIdx: funcTypeIdx,
    locals: [
      // 0: z (param), 1: era, 2: doe, 3: yoe, 4: doy, 5: mp, 6: y, 7: m, 8: d
      { name: "$era", type: { kind: "i64" } },
      { name: "$doe", type: { kind: "i64" } },
      { name: "$yoe", type: { kind: "i64" } },
      { name: "$doy", type: { kind: "i64" } },
      { name: "$mp", type: { kind: "i64" } },
      { name: "$y", type: { kind: "i64" } },
      { name: "$m", type: { kind: "i64" } },
      { name: "$d", type: { kind: "i64" } },
    ],
    body,
    exported: false,
  });
  return funcIdx;
}
function ensureDateDaysFromCivilHelper(ctx) {
  const existing = ctx.funcMap.get("__date_days_from_civil");
  if (existing !== void 0) return existing;
  const funcTypeIdx = addFuncType(ctx, [{ kind: "i64" }, { kind: "i64" }, { kind: "i64" }], [{ kind: "i64" }]);
  const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set("__date_days_from_civil", funcIdx);
  const body = [];
  body.push(
    { op: "local.get", index: 0 },
    // y
    { op: "local.get", index: 1 },
    // m
    { op: "i64.const", value: 2n },
    { op: "i64.le_s" },
    { op: "i64.extend_i32_s" },
    { op: "i64.sub" },
    { op: "local.set", index: 0 },
    // y adjusted
  );
  body.push(
    { op: "local.get", index: 0 },
    { op: "i64.const", value: 0n },
    { op: "i64.ge_s" },
    {
      op: "if",
      blockType: { kind: "val", type: { kind: "i64" } },
      then: [{ op: "local.get", index: 0 }],
      else: [{ op: "local.get", index: 0 }, { op: "i64.const", value: 399n }, { op: "i64.sub" }],
    },
    { op: "i64.const", value: 400n },
    { op: "i64.div_s" },
    { op: "local.set", index: 3 },
    // era
  );
  body.push(
    { op: "local.get", index: 0 },
    { op: "local.get", index: 3 },
    { op: "i64.const", value: 400n },
    { op: "i64.mul" },
    { op: "i64.sub" },
    { op: "local.set", index: 4 },
    // yoe
  );
  body.push(
    { op: "i64.const", value: 153n },
    { op: "local.get", index: 1 },
    // m
    { op: "i64.const", value: 2n },
    { op: "i64.gt_s" },
    {
      op: "if",
      blockType: { kind: "val", type: { kind: "i64" } },
      then: [{ op: "local.get", index: 1 }, { op: "i64.const", value: 3n }, { op: "i64.sub" }],
      else: [{ op: "local.get", index: 1 }, { op: "i64.const", value: 9n }, { op: "i64.add" }],
    },
    { op: "i64.mul" },
    { op: "i64.const", value: 2n },
    { op: "i64.add" },
    { op: "i64.const", value: 5n },
    { op: "i64.div_s" },
    { op: "local.get", index: 2 },
    // d
    { op: "i64.add" },
    { op: "i64.const", value: 1n },
    { op: "i64.sub" },
    { op: "local.set", index: 5 },
    // doy
  );
  body.push(
    { op: "local.get", index: 4 },
    // yoe
    { op: "i64.const", value: 365n },
    { op: "i64.mul" },
    { op: "local.get", index: 4 },
    { op: "i64.const", value: 4n },
    { op: "i64.div_s" },
    { op: "i64.add" },
    { op: "local.get", index: 4 },
    { op: "i64.const", value: 100n },
    { op: "i64.div_s" },
    { op: "i64.sub" },
    { op: "local.get", index: 5 },
    { op: "i64.add" },
    { op: "local.set", index: 6 },
    // doe
  );
  body.push(
    { op: "local.get", index: 3 },
    // era
    { op: "i64.const", value: 146097n },
    { op: "i64.mul" },
    { op: "local.get", index: 6 },
    // doe
    { op: "i64.add" },
    { op: "i64.const", value: 719468n },
    { op: "i64.sub" },
  );
  ctx.mod.functions.push({
    name: "__date_days_from_civil",
    typeIdx: funcTypeIdx,
    locals: [
      // 3: era, 4: yoe, 5: doy, 6: doe
      { name: "$era", type: { kind: "i64" } },
      { name: "$yoe", type: { kind: "i64" } },
      { name: "$doy", type: { kind: "i64" } },
      { name: "$doe", type: { kind: "i64" } },
    ],
    body,
    exported: false,
  });
  return funcIdx;
}
function compileDateMethodCall(ctx, fctx, propAccess, callExpr, receiverType) {
  const methodName = propAccess.name.text;
  const symName = receiverType.getSymbol()?.name;
  if (symName !== "Date") return void 0;
  const DATE_METHODS = /* @__PURE__ */ new Set([
    "getTime",
    "valueOf",
    "getFullYear",
    "getMonth",
    "getDate",
    "getHours",
    "getMinutes",
    "getSeconds",
    "getMilliseconds",
    "getDay",
    "setTime",
    "getTimezoneOffset",
    "getUTCFullYear",
    "getUTCMonth",
    "getUTCDate",
    "getUTCHours",
    "getUTCMinutes",
    "getUTCSeconds",
    "getUTCMilliseconds",
    "getUTCDay",
    "toISOString",
    "toJSON",
    "toString",
    "toDateString",
    "toTimeString",
    "toLocaleDateString",
    "toLocaleTimeString",
    "toLocaleString",
    "toUTCString",
    "toGMTString",
  ]);
  if (!DATE_METHODS.has(methodName)) return void 0;
  const dateTypeIdx = ensureDateStruct(ctx);
  const dateRefType = { kind: "ref", typeIdx: dateTypeIdx };
  const recvResult = compileExpression2(ctx, fctx, propAccess.expression, dateRefType);
  if (!recvResult) return null;
  if (methodName === "getTime" || methodName === "valueOf") {
    fctx.body.push({
      op: "struct.get",
      typeIdx: dateTypeIdx,
      fieldIdx: 0,
    });
    fctx.body.push({ op: "f64.convert_i64_s" });
    return { kind: "f64" };
  }
  if (methodName === "getTimezoneOffset") {
    fctx.body.push({ op: "drop" });
    fctx.body.push({ op: "f64.const", value: 0 });
    return { kind: "f64" };
  }
  if (methodName === "setTime") {
    const tempLocal = allocTempLocal(fctx, dateRefType);
    fctx.body.push({ op: "local.set", index: tempLocal });
    if (callExpr.arguments.length >= 1) {
      fctx.body.push({ op: "local.get", index: tempLocal });
      compileExpression2(ctx, fctx, callExpr.arguments[0], { kind: "f64" });
      fctx.body.push({ op: "i64.trunc_sat_f64_s" });
      fctx.body.push({
        op: "struct.set",
        typeIdx: dateTypeIdx,
        fieldIdx: 0,
      });
      fctx.body.push({ op: "local.get", index: tempLocal });
      fctx.body.push({
        op: "struct.get",
        typeIdx: dateTypeIdx,
        fieldIdx: 0,
      });
      fctx.body.push({ op: "f64.convert_i64_s" });
    } else {
      fctx.body.push({ op: "f64.const", value: NaN });
    }
    releaseTempLocal(fctx, tempLocal);
    return { kind: "f64" };
  }
  fctx.body.push({
    op: "struct.get",
    typeIdx: dateTypeIdx,
    fieldIdx: 0,
  });
  const MS_PER_DAY = 86400000n;
  const MS_PER_HOUR = 3600000n;
  const MS_PER_MINUTE = 60000n;
  const MS_PER_SECOND = 1000n;
  if (methodName === "getHours" || methodName === "getUTCHours") {
    fctx.body.push(
      { op: "i64.const", value: MS_PER_DAY },
      { op: "i64.rem_s" },
      { op: "i64.const", value: MS_PER_DAY },
      { op: "i64.add" },
      { op: "i64.const", value: MS_PER_DAY },
      { op: "i64.rem_s" },
      { op: "i64.const", value: MS_PER_HOUR },
      { op: "i64.div_s" },
      { op: "f64.convert_i64_s" },
    );
    return { kind: "f64" };
  }
  if (methodName === "getMinutes" || methodName === "getUTCMinutes") {
    fctx.body.push(
      { op: "i64.const", value: MS_PER_HOUR },
      { op: "i64.rem_s" },
      { op: "i64.const", value: MS_PER_HOUR },
      { op: "i64.add" },
      { op: "i64.const", value: MS_PER_HOUR },
      { op: "i64.rem_s" },
      { op: "i64.const", value: MS_PER_MINUTE },
      { op: "i64.div_s" },
      { op: "f64.convert_i64_s" },
    );
    return { kind: "f64" };
  }
  if (methodName === "getSeconds" || methodName === "getUTCSeconds") {
    fctx.body.push(
      { op: "i64.const", value: MS_PER_MINUTE },
      { op: "i64.rem_s" },
      { op: "i64.const", value: MS_PER_MINUTE },
      { op: "i64.add" },
      { op: "i64.const", value: MS_PER_MINUTE },
      { op: "i64.rem_s" },
      { op: "i64.const", value: MS_PER_SECOND },
      { op: "i64.div_s" },
      { op: "f64.convert_i64_s" },
    );
    return { kind: "f64" };
  }
  if (methodName === "getMilliseconds" || methodName === "getUTCMilliseconds") {
    fctx.body.push(
      { op: "i64.const", value: MS_PER_SECOND },
      { op: "i64.rem_s" },
      { op: "i64.const", value: MS_PER_SECOND },
      { op: "i64.add" },
      { op: "i64.const", value: MS_PER_SECOND },
      { op: "i64.rem_s" },
      { op: "f64.convert_i64_s" },
    );
    return { kind: "f64" };
  }
  if (methodName === "getDay" || methodName === "getUTCDay") {
    fctx.body.push(
      { op: "i64.const", value: MS_PER_DAY },
      { op: "i64.div_s" },
      // For negative timestamps, i64.div_s truncates toward zero, but we want floor division
      // This is fine because we handle the modular arithmetic with the +7 % 7 below
      { op: "i64.const", value: 4n },
      { op: "i64.add" },
      { op: "i64.const", value: 7n },
      { op: "i64.rem_s" },
      // Handle negative remainder: ((result % 7) + 7) % 7
      { op: "i64.const", value: 7n },
      { op: "i64.add" },
      { op: "i64.const", value: 7n },
      { op: "i64.rem_s" },
      { op: "f64.convert_i64_s" },
    );
    return { kind: "f64" };
  }
  const civilIdx = ensureDateCivilHelper(ctx);
  {
    const tempTs = allocTempLocal(fctx, { kind: "i64" });
    fctx.body.push({ op: "local.set", index: tempTs });
    fctx.body.push(
      { op: "local.get", index: tempTs },
      { op: "i64.const", value: 0n },
      { op: "i64.ge_s" },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "i64" } },
        then: [{ op: "local.get", index: tempTs }, { op: "i64.const", value: MS_PER_DAY }, { op: "i64.div_s" }],
        else: [
          { op: "local.get", index: tempTs },
          { op: "i64.const", value: MS_PER_DAY - 1n },
          { op: "i64.sub" },
          { op: "i64.const", value: MS_PER_DAY },
          { op: "i64.div_s" },
        ],
      },
    );
    releaseTempLocal(fctx, tempTs);
  }
  fctx.body.push({ op: "call", funcIdx: civilIdx });
  if (methodName === "getFullYear" || methodName === "getUTCFullYear") {
    fctx.body.push({ op: "i64.const", value: 10000n }, { op: "i64.div_s" }, { op: "f64.convert_i64_s" });
    return { kind: "f64" };
  }
  if (methodName === "getMonth" || methodName === "getUTCMonth") {
    fctx.body.push(
      { op: "i64.const", value: 100n },
      { op: "i64.div_s" },
      { op: "i64.const", value: 100n },
      { op: "i64.rem_s" },
      { op: "i64.const", value: 1n },
      { op: "i64.sub" },
      { op: "f64.convert_i64_s" },
    );
    return { kind: "f64" };
  }
  if (methodName === "getDate" || methodName === "getUTCDate") {
    fctx.body.push({ op: "i64.const", value: 100n }, { op: "i64.rem_s" }, { op: "f64.convert_i64_s" });
    return { kind: "f64" };
  }
  if (methodName === "toISOString" || methodName === "toJSON") {
    fctx.body.push({ op: "drop" });
    return compileStringLiteral(ctx, fctx, "1970-01-01T00:00:00.000Z");
  }
  const STRING_DATE_METHODS = /* @__PURE__ */ new Set([
    "toString",
    "toDateString",
    "toTimeString",
    "toLocaleDateString",
    "toLocaleTimeString",
    "toLocaleString",
    "toUTCString",
    "toGMTString",
  ]);
  if (STRING_DATE_METHODS.has(methodName)) {
    fctx.body.push({ op: "drop" });
    return compileStringLiteral(ctx, fctx, "Thu Jan 01 1970 00:00:00 GMT+0000");
  }
  fctx.body.push({ op: "drop" });
  fctx.body.push({ op: "f64.const", value: 0 });
  return { kind: "f64" };
}
function compileConsoleCallWasi(ctx, fctx, expr, _method) {
  const writeStringIdx = ctx.funcMap.get("__wasi_write_string");
  if (writeStringIdx === void 0) return VOID_RESULT;
  let first = true;
  for (const arg of expr.arguments) {
    if (!first) {
      const spaceData = wasiAllocStringData(ctx, " ");
      fctx.body.push({ op: "i32.const", value: spaceData.offset });
      fctx.body.push({ op: "i32.const", value: spaceData.length });
      fctx.body.push({ op: "call", funcIdx: writeStringIdx });
    }
    first = false;
    if (ts13.isStringLiteral(arg) || ts13.isNoSubstitutionTemplateLiteral(arg)) {
      const strValue = arg.text;
      const data = wasiAllocStringData(ctx, strValue);
      fctx.body.push({ op: "i32.const", value: data.offset });
      fctx.body.push({ op: "i32.const", value: data.length });
      fctx.body.push({ op: "call", funcIdx: writeStringIdx });
    } else if (ts13.isTemplateExpression(arg)) {
      if (arg.head.text) {
        const headData = wasiAllocStringData(ctx, arg.head.text);
        fctx.body.push({ op: "i32.const", value: headData.offset });
        fctx.body.push({ op: "i32.const", value: headData.length });
        fctx.body.push({ op: "call", funcIdx: writeStringIdx });
      }
      for (const span of arg.templateSpans) {
        const exprType = compileExpression2(ctx, fctx, span.expression);
        emitWasiValueToStdout(ctx, fctx, exprType, span.expression);
        if (span.literal.text) {
          const litData = wasiAllocStringData(ctx, span.literal.text);
          fctx.body.push({ op: "i32.const", value: litData.offset });
          fctx.body.push({ op: "i32.const", value: litData.length });
          fctx.body.push({ op: "call", funcIdx: writeStringIdx });
        }
      }
    } else {
      const argType = ctx.checker.getTypeAtLocation(arg);
      const exprType = compileExpression2(ctx, fctx, arg);
      emitWasiValueToStdout(ctx, fctx, exprType, arg);
    }
  }
  const newlineData = wasiAllocStringData(ctx, "\n");
  fctx.body.push({ op: "i32.const", value: newlineData.offset });
  fctx.body.push({ op: "i32.const", value: newlineData.length });
  fctx.body.push({ op: "call", funcIdx: writeStringIdx });
  return VOID_RESULT;
}
function wasiAllocStringData(ctx, str) {
  const encoder = new TextEncoder();
  const bytes = encoder.encode(str);
  let offset = 1024;
  for (const seg of ctx.mod.dataSegments) {
    const segEnd = seg.offset + seg.bytes.length;
    if (segEnd > offset) offset = segEnd;
  }
  ctx.mod.dataSegments.push({ offset, bytes });
  return { offset, length: bytes.length };
}
function emitWasiValueToStdout(ctx, fctx, exprType, _node) {
  const writeStringIdx = ctx.funcMap.get("__wasi_write_string");
  if (writeStringIdx === void 0) return;
  if (exprType === VOID_RESULT || exprType === null) {
    return;
  }
  if (exprType.kind === "f64") {
    const writeF64Idx = ensureWasiWriteF64Helper(ctx);
    if (writeF64Idx >= 0) {
      fctx.body.push({ op: "call", funcIdx: writeF64Idx });
    } else {
      fctx.body.push({ op: "drop" });
    }
  } else if (exprType.kind === "i32") {
    const writeI32Idx = ensureWasiWriteI32Helper(ctx);
    if (writeI32Idx >= 0) {
      fctx.body.push({ op: "call", funcIdx: writeI32Idx });
    } else {
      fctx.body.push({ op: "drop" });
    }
  } else {
    fctx.body.push({ op: "drop" });
    const placeholder = wasiAllocStringData(ctx, "[object]");
    fctx.body.push({ op: "i32.const", value: placeholder.offset });
    fctx.body.push({ op: "i32.const", value: placeholder.length });
    fctx.body.push({ op: "call", funcIdx: writeStringIdx });
  }
}
function ensureWasiWriteI32Helper(ctx) {
  const existing = ctx.funcMap.get("__wasi_write_i32");
  if (existing !== void 0) return existing;
  const writeStringIdx = ctx.funcMap.get("__wasi_write_string");
  if (writeStringIdx === void 0) return -1;
  const funcTypeIdx = addFuncType(ctx, [{ kind: "i32" }], []);
  const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set("__wasi_write_i32", funcIdx);
  const body = [];
  const bufStartLocal = 1;
  const bufPosLocal = 2;
  const isNegLocal = 3;
  const absValLocal = 4;
  const tmpLocal = 5;
  body.push(
    // buf_start = bump_ptr
    { op: "global.get", index: ctx.wasiBumpPtrGlobalIdx },
    { op: "local.set", index: bufStartLocal },
    // buf_pos = buf_start + 11 (write digits right-to-left, max 11 digits + sign)
    { op: "local.get", index: bufStartLocal },
    { op: "i32.const", value: 11 },
    { op: "i32.add" },
    { op: "local.set", index: bufPosLocal },
    // Check if value == 0
    { op: "local.get", index: 0 },
    { op: "i32.eqz" },
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        // Write "0" directly
        { op: "local.get", index: bufPosLocal },
        { op: "i32.const", value: 48 },
        // '0'
        { op: "i32.store8", align: 0, offset: 0 },
        { op: "local.get", index: bufPosLocal },
        { op: "i32.const", value: 1 },
        { op: "call", funcIdx: writeStringIdx },
        { op: "return" },
      ],
    },
    // Check if negative
    { op: "local.get", index: 0 },
    { op: "i32.const", value: 0 },
    { op: "i32.lt_s" },
    { op: "local.set", index: isNegLocal },
    // absVal = is_neg ? -value : value
    { op: "local.get", index: isNegLocal },
    {
      op: "if",
      blockType: { kind: "val", type: { kind: "i32" } },
      then: [{ op: "i32.const", value: 0 }, { op: "local.get", index: 0 }, { op: "i32.sub" }],
      else: [{ op: "local.get", index: 0 }],
    },
    { op: "local.set", index: absValLocal },
    // Loop: extract digits right to left
    {
      op: "block",
      blockType: { kind: "empty" },
      body: [
        {
          op: "loop",
          blockType: { kind: "empty" },
          body: [
            // if absVal == 0, break
            { op: "local.get", index: absValLocal },
            { op: "i32.eqz" },
            { op: "br_if", depth: 1 },
            // digit = absVal % 10
            { op: "local.get", index: absValLocal },
            { op: "i32.const", value: 10 },
            { op: "i32.rem_u" },
            { op: "local.set", index: tmpLocal },
            // absVal = absVal / 10
            { op: "local.get", index: absValLocal },
            { op: "i32.const", value: 10 },
            { op: "i32.div_u" },
            { op: "local.set", index: absValLocal },
            // buf_pos--
            { op: "local.get", index: bufPosLocal },
            { op: "i32.const", value: 1 },
            { op: "i32.sub" },
            { op: "local.set", index: bufPosLocal },
            // memory[buf_pos] = digit + '0'
            { op: "local.get", index: bufPosLocal },
            { op: "local.get", index: tmpLocal },
            { op: "i32.const", value: 48 },
            { op: "i32.add" },
            { op: "i32.store8", align: 0, offset: 0 },
            // continue loop
            { op: "br", depth: 0 },
          ],
        },
      ],
    },
    // If negative, prepend '-'
    { op: "local.get", index: isNegLocal },
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "local.get", index: bufPosLocal },
        { op: "i32.const", value: 1 },
        { op: "i32.sub" },
        { op: "local.set", index: bufPosLocal },
        { op: "local.get", index: bufPosLocal },
        { op: "i32.const", value: 45 },
        // '-'
        { op: "i32.store8", align: 0, offset: 0 },
      ],
    },
    // Call __wasi_write_string(buf_pos, buf_start + 12 - buf_pos)
    { op: "local.get", index: bufPosLocal },
    { op: "local.get", index: bufStartLocal },
    { op: "i32.const", value: 12 },
    { op: "i32.add" },
    { op: "local.get", index: bufPosLocal },
    { op: "i32.sub" },
    { op: "call", funcIdx: writeStringIdx },
  );
  ctx.mod.functions.push({
    name: "__wasi_write_i32",
    typeIdx: funcTypeIdx,
    locals: [
      { name: "buf_start", type: { kind: "i32" } },
      { name: "buf_pos", type: { kind: "i32" } },
      { name: "is_neg", type: { kind: "i32" } },
      { name: "abs_val", type: { kind: "i32" } },
      { name: "tmp", type: { kind: "i32" } },
    ],
    body,
    exported: false,
  });
  return funcIdx;
}
function ensureWasiWriteF64Helper(ctx) {
  const existing = ctx.funcMap.get("__wasi_write_f64");
  if (existing !== void 0) return existing;
  const writeI32Idx = ensureWasiWriteI32Helper(ctx);
  const writeStringIdx = ctx.funcMap.get("__wasi_write_string");
  if (writeStringIdx === void 0 || writeI32Idx < 0) return -1;
  const funcTypeIdx = addFuncType(ctx, [{ kind: "f64" }], []);
  const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set("__wasi_write_f64", funcIdx);
  const nanData = wasiAllocStringData(ctx, "NaN");
  const infData = wasiAllocStringData(ctx, "Infinity");
  const negInfData = wasiAllocStringData(ctx, "-Infinity");
  const body = [
    // Check NaN: value != value
    { op: "local.get", index: 0 },
    { op: "local.get", index: 0 },
    { op: "f64.ne" },
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: nanData.offset },
        { op: "i32.const", value: nanData.length },
        { op: "call", funcIdx: writeStringIdx },
        { op: "return" },
      ],
    },
    // Check positive infinity
    { op: "local.get", index: 0 },
    { op: "f64.const", value: Infinity },
    { op: "f64.eq" },
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: infData.offset },
        { op: "i32.const", value: infData.length },
        { op: "call", funcIdx: writeStringIdx },
        { op: "return" },
      ],
    },
    // Check negative infinity
    { op: "local.get", index: 0 },
    { op: "f64.const", value: -Infinity },
    { op: "f64.eq" },
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        { op: "i32.const", value: negInfData.offset },
        { op: "i32.const", value: negInfData.length },
        { op: "call", funcIdx: writeStringIdx },
        { op: "return" },
      ],
    },
    // Normal number: truncate to i32 and print
    { op: "local.get", index: 0 },
    { op: "i32.trunc_sat_f64_s" },
    { op: "call", funcIdx: writeI32Idx },
  ];
  ctx.mod.functions.push({
    name: "__wasi_write_f64",
    typeIdx: funcTypeIdx,
    locals: [],
    body,
    exported: false,
  });
  return funcIdx;
}
function compileMathCall(ctx, fctx, method, expr) {
  const nativeUnary = {
    sqrt: "f64.sqrt",
    abs: "f64.abs",
    floor: "f64.floor",
    ceil: "f64.ceil",
    trunc: "f64.trunc",
    nearest: "f64.nearest",
  };
  const f64Hint = { kind: "f64" };
  if (method === "round" && expr.arguments.length >= 1) {
    const xLocal = allocLocal(fctx, `__round_x_${fctx.locals.length}`, {
      kind: "f64",
    });
    const floorLocal = allocLocal(fctx, `__round_fl_${fctx.locals.length}`, {
      kind: "f64",
    });
    const rLocal = allocLocal(fctx, `__round_r_${fctx.locals.length}`, {
      kind: "f64",
    });
    compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
    fctx.body.push({ op: "local.tee", index: xLocal });
    fctx.body.push({ op: "f64.floor" });
    fctx.body.push({ op: "local.set", index: floorLocal });
    fctx.body.push({ op: "local.get", index: xLocal });
    fctx.body.push({ op: "local.get", index: floorLocal });
    fctx.body.push({ op: "f64.sub" });
    fctx.body.push({ op: "f64.const", value: 0.5 });
    fctx.body.push({ op: "f64.ge" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "f64" } },
      then: [{ op: "local.get", index: xLocal }, { op: "f64.ceil" }],
      else: [{ op: "local.get", index: floorLocal }],
    });
    fctx.body.push({ op: "local.tee", index: rLocal });
    fctx.body.push({ op: "f64.const", value: 0 });
    fctx.body.push({ op: "f64.eq" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "f64" } },
      then: [{ op: "f64.const", value: 0 }, { op: "local.get", index: xLocal }, { op: "f64.copysign" }],
      else: [{ op: "local.get", index: rLocal }],
    });
    return { kind: "f64" };
  }
  if (method in nativeUnary && expr.arguments.length >= 1) {
    compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
    fctx.body.push({ op: nativeUnary[method] });
    return { kind: "f64" };
  }
  if (method === "clz32" && expr.arguments.length >= 1) {
    const toU32Idx = ctx.funcMap.get("__toUint32");
    compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
    if (toU32Idx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: toU32Idx });
    } else {
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
    fctx.body.push({ op: "i32.clz" });
    fctx.body.push({ op: "f64.convert_i32_s" });
    return { kind: "f64" };
  }
  if (method === "imul" && expr.arguments.length >= 2) {
    const toU32Idx = ctx.funcMap.get("__toUint32");
    compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
    if (toU32Idx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: toU32Idx });
    } else {
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
    compileExpression2(ctx, fctx, expr.arguments[1], f64Hint);
    if (toU32Idx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: toU32Idx });
    } else {
      fctx.body.push({ op: "i32.trunc_sat_f64_s" });
    }
    fctx.body.push({ op: "i32.mul" });
    fctx.body.push({ op: "f64.convert_i32_s" });
    return { kind: "f64" };
  }
  if (method === "sign" && expr.arguments.length >= 1) {
    compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
    const tmp = allocLocal(fctx, `__sign_${fctx.locals.length}`, {
      kind: "f64",
    });
    fctx.body.push({ op: "local.tee", index: tmp });
    fctx.body.push({ op: "local.get", index: tmp });
    fctx.body.push({ op: "f64.ne" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "f64" } },
      then: [
        // return NaN
        { op: "f64.const", value: NaN },
      ],
      else: [
        // x == 0 check (true for both +0 and -0)
        { op: "local.get", index: tmp },
        { op: "f64.abs" },
        { op: "f64.const", value: 0 },
        { op: "f64.eq" },
        {
          op: "if",
          blockType: { kind: "val", type: { kind: "f64" } },
          then: [
            // return x (preserves -0)
            { op: "local.get", index: tmp },
          ],
          else: [
            // return copysign(1.0, x) — gives 1 or -1 based on sign of x
            { op: "f64.const", value: 1 },
            { op: "local.get", index: tmp },
            { op: "f64.copysign" },
          ],
        },
      ],
    });
    return { kind: "f64" };
  }
  if (method === "fround" && expr.arguments.length >= 1) {
    compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
    fctx.body.push({ op: "f32.demote_f64" });
    fctx.body.push({ op: "f64.promote_f32" });
    return { kind: "f64" };
  }
  if (method === "hypot") {
    if (expr.arguments.length === 0) {
      fctx.body.push({ op: "f64.const", value: 0 });
      return { kind: "f64" };
    }
    if (expr.arguments.length === 1) {
      compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
      fctx.body.push({ op: "f64.abs" });
      return { kind: "f64" };
    }
    const hypotLocals = [];
    for (let ai = 0; ai < expr.arguments.length; ai++) {
      const loc = allocLocal(fctx, `__hypot_${fctx.locals.length}`, {
        kind: "f64",
      });
      compileExpression2(ctx, fctx, expr.arguments[ai], f64Hint);
      fctx.body.push({ op: "local.set", index: loc });
      hypotLocals.push(loc);
    }
    for (let i = 0; i < hypotLocals.length; i++) {
      fctx.body.push({ op: "local.get", index: hypotLocals[i] });
      fctx.body.push({ op: "f64.abs" });
      fctx.body.push({ op: "f64.const", value: Infinity });
      fctx.body.push({ op: "f64.eq" });
      if (i > 0) {
        fctx.body.push({ op: "i32.or" });
      }
    }
    const thenBlock = [{ op: "f64.const", value: Infinity }];
    const elseBlock = [];
    for (let i = 0; i < hypotLocals.length; i++) {
      elseBlock.push({ op: "local.get", index: hypotLocals[i] });
      elseBlock.push({ op: "local.get", index: hypotLocals[i] });
      elseBlock.push({ op: "f64.mul" });
    }
    for (let i = 1; i < hypotLocals.length; i++) {
      elseBlock.push({ op: "f64.add" });
    }
    elseBlock.push({ op: "f64.sqrt" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "f64" } },
      then: thenBlock,
      else: elseBlock,
    });
    return { kind: "f64" };
  }
  const hostUnary = /* @__PURE__ */ new Set([
    "exp",
    "log",
    "log2",
    "log10",
    "sin",
    "cos",
    "tan",
    "asin",
    "acos",
    "atan",
    "acosh",
    "asinh",
    "atanh",
    "cosh",
    "sinh",
    "tanh",
    "cbrt",
    "expm1",
    "log1p",
  ]);
  if (hostUnary.has(method) && expr.arguments.length >= 1) {
    const funcIdx = ctx.funcMap.get(`Math_${method}`);
    if (funcIdx !== void 0) {
      compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "f64" };
    }
  }
  if ((method === "pow" || method === "atan2") && expr.arguments.length >= 2) {
    const funcIdx = ctx.funcMap.get(`Math_${method}`);
    if (funcIdx !== void 0) {
      compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
      compileExpression2(ctx, fctx, expr.arguments[1], f64Hint);
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "f64" };
    }
  }
  if (method === "random") {
    const funcIdx = ctx.funcMap.get("Math_random");
    if (funcIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx });
      return { kind: "f64" };
    }
  }
  if ((method === "min" || method === "max") && expr.arguments) {
    const wasmOp = method === "min" ? "f64.min" : "f64.max";
    if (expr.arguments.length === 0) {
      fctx.body.push({
        op: "f64.const",
        value: method === "min" ? Infinity : -Infinity,
      });
      return { kind: "f64" };
    }
    if (expr.arguments.some((a) => isStaticNaN(ctx, a))) {
      for (const arg of expr.arguments) {
        if (!isStaticNaN(ctx, arg)) {
          compileExpression2(ctx, fctx, arg, f64Hint);
          fctx.body.push({ op: "drop" });
        }
      }
      fctx.body.push({ op: "f64.const", value: NaN });
      return { kind: "f64" };
    }
    const staticValues = expr.arguments.map((a) => {
      const tsType = ctx.checker.getTypeAtLocation(a);
      if (tsType.flags & ts13.TypeFlags.Object) {
        return tryStaticToNumber(ctx, a);
      }
      return void 0;
    });
    if (staticValues.every((v) => v !== void 0)) {
      const nums = staticValues;
      const result = method === "min" ? nums.reduce((a, b) => Math.min(a, b)) : nums.reduce((a, b) => Math.max(a, b));
      fctx.body.push({ op: "f64.const", value: result });
      return { kind: "f64" };
    }
    if (expr.arguments.length === 1) {
      if (staticValues[0] !== void 0) {
        fctx.body.push({ op: "f64.const", value: staticValues[0] });
      } else {
        compileExpression2(ctx, fctx, expr.arguments[0], f64Hint);
      }
      return { kind: "f64" };
    }
    const argLocals = [];
    for (let ai = 0; ai < expr.arguments.length; ai++) {
      const local = allocLocal(fctx, `__minmax_${fctx.locals.length}`, {
        kind: "f64",
      });
      if (staticValues[ai] !== void 0) {
        fctx.body.push({ op: "f64.const", value: staticValues[ai] });
      } else {
        compileExpression2(ctx, fctx, expr.arguments[ai], f64Hint);
      }
      fctx.body.push({ op: "local.set", index: local });
      argLocals.push(local);
    }
    const f64Block = { kind: "val", type: { kind: "f64" } };
    let innerBody = [{ op: "local.get", index: argLocals[0] }];
    for (let i = 1; i < argLocals.length; i++) {
      innerBody.push({ op: "local.get", index: argLocals[i] });
      innerBody.push({ op: wasmOp });
    }
    for (let i = argLocals.length - 1; i >= 0; i--) {
      innerBody = [
        // isNaN check: local.get, local.get, f64.ne (x !== x)
        { op: "local.get", index: argLocals[i] },
        { op: "local.get", index: argLocals[i] },
        { op: "f64.ne" },
        {
          op: "if",
          blockType: f64Block,
          then: [{ op: "local.get", index: argLocals[i] }],
          else: innerBody,
        },
      ];
    }
    for (const instr of innerBody) {
      fctx.body.push(instr);
    }
    return { kind: "f64" };
  }
  return void 0;
}
function compileConditionalExpression(ctx, fctx, expr) {
  const condType = compileExpression2(ctx, fctx, expr.condition);
  if (!condType) {
    fctx.body.push({ op: "i32.const", value: 0 });
  } else {
    ensureI32Condition(fctx, condType, ctx);
  }
  const savedBody = pushBody(fctx);
  const thenResultType = compileExpression2(ctx, fctx, expr.whenTrue);
  if (!thenResultType) {
    fctx.body.push({ op: "f64.const", value: NaN });
  }
  let thenInstrs = fctx.body;
  fctx.body = [];
  const elseResultType = compileExpression2(ctx, fctx, expr.whenFalse);
  if (!elseResultType) {
    fctx.body.push({ op: "f64.const", value: NaN });
  }
  let elseInstrs = fctx.body;
  fctx.body = savedBody;
  const thenType = thenResultType ?? { kind: "f64" };
  const elseType = elseResultType ?? { kind: "f64" };
  let resultValType = thenType;
  const sameKind = thenType.kind === elseType.kind;
  const sameRefIdx =
    sameKind && (thenType.kind === "ref" || thenType.kind === "ref_null") && thenType.typeIdx === elseType.typeIdx;
  if (!sameKind || ((thenType.kind === "ref" || thenType.kind === "ref_null") && !sameRefIdx)) {
    if ((thenType.kind === "i32" || thenType.kind === "f64") && (elseType.kind === "i32" || elseType.kind === "f64")) {
      resultValType = { kind: "f64" };
    } else if (
      (thenType.kind === "ref" || thenType.kind === "ref_null") &&
      (elseType.kind === "ref" || elseType.kind === "ref_null") &&
      isAnyValue(thenType, ctx) === isAnyValue(elseType, ctx)
    ) {
      resultValType =
        thenType.kind === "ref"
          ? {
              kind: "ref_null",
              typeIdx: thenType.typeIdx,
            }
          : thenType;
    } else {
      resultValType = { kind: "externref" };
    }
    if (!valTypesMatch(thenType, resultValType)) {
      const coerceBody = [];
      fctx.body = coerceBody;
      coerceType3(ctx, fctx, thenType, resultValType);
      fctx.body = savedBody;
      thenInstrs = [...thenInstrs, ...coerceBody];
    }
    if (!valTypesMatch(elseType, resultValType)) {
      const coerceBody = [];
      fctx.body = coerceBody;
      coerceType3(ctx, fctx, elseType, resultValType);
      fctx.body = savedBody;
      elseInstrs = [...elseInstrs, ...coerceBody];
    }
  } else {
    resultValType = thenType;
  }
  if (resultValType.kind === "ref") {
    resultValType = {
      kind: "ref_null",
      typeIdx: resultValType.typeIdx,
    };
  }
  fctx.body.push({
    op: "if",
    blockType: { kind: "val", type: resultValType },
    then: thenInstrs,
    else: elseInstrs,
  });
  return resultValType;
}
function resolveStructName(ctx, tsType) {
  const name = tsType.symbol?.name;
  if (name && name !== "__type" && name !== "__object" && ctx.structMap.has(name)) {
    return name;
  }
  if (name) {
    const mapped = ctx.classExprNameMap.get(name);
    if (mapped && ctx.structMap.has(mapped)) {
      return mapped;
    }
  }
  return ctx.anonTypeMap.get(tsType);
}
function isGeneratorIteratorResultLike(ctx, type, propName) {
  if (propName !== "value" && propName !== "done") return false;
  const props = type.getProperties();
  const hasValue = props.some((p) => p.name === "value");
  const hasDone = props.some((p) => p.name === "done");
  if (hasValue && hasDone) return true;
  if (type.isUnion()) {
    for (const t of type.types) {
      if (isIteratorResultType(t)) return true;
    }
  }
  return false;
}
function getIteratorResultValueType(ctx, type) {
  const typeArgs = ctx.checker.getTypeArguments(type);
  if (typeArgs.length > 0) {
    return resolveWasmType(ctx, typeArgs[0]);
  }
  if (type.isUnion()) {
    for (const t of type.types) {
      const args = ctx.checker.getTypeArguments(t);
      if (args.length > 0) {
        return resolveWasmType(ctx, args[0]);
      }
    }
  }
  return null;
}
function compileYieldExpression(ctx, fctx, expr) {
  if (!fctx.isGenerator) {
    ctx.errors.push({
      message: "yield expression outside of generator function",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  const bufferIdx = fctx.localMap.get("__gen_buffer");
  if (bufferIdx === void 0) {
    ctx.errors.push({
      message: "Internal error: __gen_buffer not found in generator function",
      line: getLine(expr),
      column: getCol(expr),
    });
    return null;
  }
  if (!expr.expression) {
    const pushRefIdx = ctx.funcMap.get("__gen_push_ref");
    if (pushRefIdx !== void 0) {
      fctx.body.push({ op: "local.get", index: bufferIdx });
      fctx.body.push({ op: "ref.null.extern" });
      fctx.body.push({ op: "call", funcIdx: pushRefIdx });
    }
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  const yieldedType = compileExpressionInner(ctx, fctx, expr.expression);
  if (yieldedType === null || yieldedType === VOID_RESULT) {
    fctx.body.push({ op: "ref.null.extern" });
    return { kind: "externref" };
  }
  const tmpLocal = allocLocal(fctx, `__yield_tmp_${fctx.locals.length}`, yieldedType);
  fctx.body.push({ op: "local.set", index: tmpLocal });
  fctx.body.push({ op: "local.get", index: bufferIdx });
  fctx.body.push({ op: "local.get", index: tmpLocal });
  if (yieldedType.kind === "f64") {
    const pushIdx = ctx.funcMap.get("__gen_push_f64");
    if (pushIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: pushIdx });
    }
  } else if (yieldedType.kind === "i32") {
    const pushIdx = ctx.funcMap.get("__gen_push_i32");
    if (pushIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: pushIdx });
    }
  } else {
    const pushIdx = ctx.funcMap.get("__gen_push_ref");
    if (pushIdx !== void 0) {
      fctx.body.push({ op: "call", funcIdx: pushIdx });
    }
  }
  fctx.body.push({ op: "ref.null.extern" });
  return { kind: "externref" };
}
function tryStaticToNumber(ctx, expr) {
  if (ts13.isNumericLiteral(expr)) return Number(expr.text);
  if (ts13.isStringLiteral(expr) || ts13.isNoSubstitutionTemplateLiteral(expr)) return Number(expr.text);
  if (expr.kind === ts13.SyntaxKind.NullKeyword) return 0;
  if (ts13.isIdentifier(expr) && expr.text === "undefined") return NaN;
  if (expr.kind === ts13.SyntaxKind.TrueKeyword) return 1;
  if (expr.kind === ts13.SyntaxKind.FalseKeyword) return 0;
  if (ts13.isIdentifier(expr) && expr.text === "NaN") return NaN;
  if (ts13.isIdentifier(expr) && expr.text === "Infinity") return Infinity;
  if (ts13.isPrefixUnaryExpression(expr) && expr.operator === ts13.SyntaxKind.MinusToken) {
    const inner = tryStaticToNumber(ctx, expr.operand);
    if (inner !== void 0) return -inner;
  }
  if (ts13.isBinaryExpression(expr)) {
    if (
      expr.operatorToken.kind === ts13.SyntaxKind.PlusToken &&
      (ts13.isStringLiteral(expr.left) ||
        ts13.isNoSubstitutionTemplateLiteral(expr.left) ||
        ts13.isStringLiteral(expr.right) ||
        ts13.isNoSubstitutionTemplateLiteral(expr.right))
    ) {
      return void 0;
    }
    const left = tryStaticToNumber(ctx, expr.left);
    const right = tryStaticToNumber(ctx, expr.right);
    if (left !== void 0 && right !== void 0) {
      switch (expr.operatorToken.kind) {
        case ts13.SyntaxKind.PlusToken: {
          const leftTsType = ctx.checker.getTypeAtLocation(expr.left);
          const rightTsType = ctx.checker.getTypeAtLocation(expr.right);
          if (isStringType(leftTsType) || isStringType(rightTsType)) return void 0;
          return left + right;
        }
        case ts13.SyntaxKind.MinusToken:
          return left - right;
        case ts13.SyntaxKind.AsteriskToken:
          return left * right;
        case ts13.SyntaxKind.SlashToken:
          return right !== 0 ? left / right : void 0;
        case ts13.SyntaxKind.PercentToken:
          return right !== 0 ? left % right : void 0;
        case ts13.SyntaxKind.AsteriskAsteriskToken:
          return left ** right;
        case ts13.SyntaxKind.AmpersandToken:
          return left & right;
        case ts13.SyntaxKind.BarToken:
          return left | right;
        case ts13.SyntaxKind.CaretToken:
          return left ^ right;
        case ts13.SyntaxKind.LessThanLessThanToken:
          return left << right;
        case ts13.SyntaxKind.GreaterThanGreaterThanToken:
          return left >> right;
        case ts13.SyntaxKind.GreaterThanGreaterThanGreaterThanToken:
          return left >>> right;
        default:
          break;
      }
    }
  }
  if (ts13.isPropertyAccessExpression(expr) && expr.name.text === "length") {
    const obj = expr.expression;
    if (ts13.isStringLiteral(obj) || ts13.isNoSubstitutionTemplateLiteral(obj)) {
      return obj.text.length;
    }
    if (ts13.isIdentifier(obj)) {
      const sym = ctx.checker.getSymbolAtLocation(obj);
      const decl = sym?.valueDeclaration;
      if (decl && ts13.isVariableDeclaration(decl) && decl.initializer) {
        const init = decl.initializer;
        if (ts13.isStringLiteral(init) || ts13.isNoSubstitutionTemplateLiteral(init)) {
          return init.text.length;
        }
      }
    }
  }
  if (ts13.isObjectLiteralExpression(expr)) {
    if (expr.properties.length === 0) return NaN;
    const valueOfProp = expr.properties.find(
      (p) => ts13.isPropertyAssignment(p) && ts13.isIdentifier(p.name) && p.name.text === "valueOf",
    );
    if (valueOfProp && ts13.isPropertyAssignment(valueOfProp)) {
      const init = valueOfProp.initializer;
      if (ts13.isFunctionExpression(init) || ts13.isArrowFunction(init)) {
        const returnExpr = getReturnExpression(init);
        if (returnExpr && (ts13.isObjectLiteralExpression(returnExpr) || ts13.isArrayLiteralExpression(returnExpr))) {
        } else {
          const retVal = getStaticReturnValue(ctx, init);
          if (retVal !== void 0) return retVal;
          return void 0;
        }
      } else {
        return void 0;
      }
    }
    const toStringProp = expr.properties.find(
      (p) => ts13.isPropertyAssignment(p) && ts13.isIdentifier(p.name) && p.name.text === "toString",
    );
    if (toStringProp && ts13.isPropertyAssignment(toStringProp)) {
      const init = toStringProp.initializer;
      if (ts13.isFunctionExpression(init) || ts13.isArrowFunction(init)) {
        const retVal = getStaticReturnValue(ctx, init);
        if (retVal !== void 0) return retVal;
        return void 0;
      }
      return void 0;
    }
    return NaN;
  }
  if (ts13.isParenthesizedExpression(expr)) {
    return tryStaticToNumber(ctx, expr.expression);
  }
  if (ts13.isPrefixUnaryExpression(expr) && expr.operator === ts13.SyntaxKind.PlusToken) {
    return tryStaticToNumber(ctx, expr.operand);
  }
  if (ts13.isIdentifier(expr)) {
    const sym = ctx.checker.getSymbolAtLocation(expr);
    const decl = sym?.valueDeclaration;
    if (decl && ts13.isVariableDeclaration(decl) && decl.initializer) {
      const declList = decl.parent;
      if (ts13.isVariableDeclarationList(declList) && (declList.flags & ts13.NodeFlags.Const) !== 0) {
        return tryStaticToNumber(ctx, decl.initializer);
      }
    }
  }
  return void 0;
}
function getStaticReturnValue(ctx, fn) {
  const body = fn.body;
  if (!ts13.isBlock(body)) {
    return tryStaticToNumber(ctx, body);
  }
  for (const stmt of body.statements) {
    if (ts13.isReturnStatement(stmt) && stmt.expression) {
      return tryStaticToNumber(ctx, stmt.expression);
    }
  }
  return void 0;
}
function getReturnExpression(fn) {
  const body = fn.body;
  if (!ts13.isBlock(body)) return body;
  for (const stmt of body.statements) {
    if (ts13.isReturnStatement(stmt) && stmt.expression) return stmt.expression;
  }
  return void 0;
}
function isStaticNaN(ctx, expr) {
  if (ts13.isIdentifier(expr) && expr.text === "NaN") return true;
  if (
    ts13.isBinaryExpression(expr) &&
    expr.operatorToken.kind === ts13.SyntaxKind.SlashToken &&
    ts13.isNumericLiteral(expr.left) &&
    Number(expr.left.text) === 0 &&
    ts13.isNumericLiteral(expr.right) &&
    Number(expr.right.text) === 0
  )
    return true;
  if (ts13.isIdentifier(expr)) {
    const sym = ctx.checker.getSymbolAtLocation(expr);
    const decl = sym?.valueDeclaration;
    if (decl && ts13.isVariableDeclaration(decl) && decl.initializer) {
      return isStaticNaN(ctx, decl.initializer);
    }
  }
  return false;
}
registerCompileExpression(compileExpression2);
registerEnsureLateImport(ensureLateImport2);
registerFlushLateImportShifts(flushLateImportShifts2);

// src/shape-inference.ts
import ts14 from "typescript";
function collectShapes(checker, sourceFile) {
  const shapes = /* @__PURE__ */ new Map();
  function getOrCreate(name) {
    let shape = shapes.get(name);
    if (!shape) {
      shape = {
        fields: /* @__PURE__ */ new Map(),
        hasNumericIndexing: false,
        numericValueType: "unknown",
        maxNumericIndex: -1,
        usedInCallApply: false,
      };
      shapes.set(name, shape);
    }
    return shape;
  }
  function inferTypeCategory(tsType) {
    if (tsType.flags & ts14.TypeFlags.NumberLike) return "number";
    if (tsType.flags & ts14.TypeFlags.StringLike) return "string";
    if (tsType.flags & ts14.TypeFlags.BooleanLike) return "boolean";
    return "unknown";
  }
  function visit(node) {
    if (
      ts14.isBinaryExpression(node) &&
      node.operatorToken.kind === ts14.SyntaxKind.EqualsToken &&
      ts14.isPropertyAccessExpression(node.left) &&
      ts14.isIdentifier(node.left.expression)
    ) {
      const varName = node.left.expression.text;
      const propName = node.left.name.text;
      const valType = checker.getTypeAtLocation(node.right);
      const shape = getOrCreate(varName);
      shape.fields.set(propName, { type: inferTypeCategory(valType) });
    }
    if (
      ts14.isBinaryExpression(node) &&
      node.operatorToken.kind === ts14.SyntaxKind.EqualsToken &&
      ts14.isElementAccessExpression(node.left) &&
      ts14.isIdentifier(node.left.expression) &&
      ts14.isNumericLiteral(node.left.argumentExpression)
    ) {
      const varName = node.left.expression.text;
      const idx = parseInt(node.left.argumentExpression.text, 10);
      const valType = checker.getTypeAtLocation(node.right);
      const shape = getOrCreate(varName);
      shape.hasNumericIndexing = true;
      if (idx > shape.maxNumericIndex) shape.maxNumericIndex = idx;
      const valCategory = inferTypeCategory(valType);
      if (valCategory === "number" || valCategory === "string") {
        shape.numericValueType = valCategory;
      }
    }
    if (
      ts14.isCallExpression(node) &&
      ts14.isPropertyAccessExpression(node.expression) &&
      node.expression.name.text === "call" &&
      ts14.isPropertyAccessExpression(node.expression.expression)
    ) {
      const methodAccess = node.expression.expression;
      if (
        ts14.isPropertyAccessExpression(methodAccess.expression) &&
        ts14.isIdentifier(methodAccess.expression.expression) &&
        methodAccess.expression.expression.text === "Array" &&
        methodAccess.expression.name.text === "prototype"
      ) {
        if (node.arguments.length >= 1 && ts14.isIdentifier(node.arguments[0])) {
          const varName = node.arguments[0].text;
          const shape = getOrCreate(varName);
          shape.usedInCallApply = true;
        }
      }
    }
    ts14.forEachChild(node, visit);
  }
  visit(sourceFile);
  const result = /* @__PURE__ */ new Map();
  for (const [name, shape] of shapes) {
    const isArrayLike = shape.hasNumericIndexing && shape.fields.has("length");
    if (isArrayLike || (shape.usedInCallApply && shape.hasNumericIndexing)) {
      result.set(name, shape);
    }
  }
  return result;
}

// src/codegen/math-helpers.ts
var f64c = (v) => ({ op: "f64.const", value: v });
var localGet = (i) => ({ op: "local.get", index: i });
var localSet = (i) => ({ op: "local.set", index: i });
var localTee = (i) => ({ op: "local.tee", index: i });
var add = { op: "f64.add" };
var sub = { op: "f64.sub" };
var mul = { op: "f64.mul" };
var div = { op: "f64.div" };
var neg = { op: "f64.neg" };
var fabs = { op: "f64.abs" };
var fsqrt = { op: "f64.sqrt" };
var ffloor = { op: "f64.floor" };
var ftrunc = { op: "f64.trunc" };
var feq = { op: "f64.eq" };
var fne = { op: "f64.ne" };
var flt = { op: "f64.lt" };
var fgt = { op: "f64.gt" };
var fge = { op: "f64.ge" };
var ret = { op: "return" };
var copysign = { op: "f64.copysign" };
var i32const = (v) => ({ op: "i32.const", value: v });
var i32eqz = { op: "i32.eqz" };
var truncSatI32 = { op: "i32.trunc_sat_f64_s" };
var i32sub = { op: "i32.sub" };
function ifThenRet(cond, result) {
  return [...cond, { op: "if", blockType: { kind: "empty" }, then: [...result, ret] }];
}
function ifElse(type, thenBody, elseBody) {
  return {
    op: "if",
    blockType: { kind: "val", type },
    then: thenBody,
    else: elseBody,
  };
}
function call(funcIdx) {
  return { op: "call", funcIdx };
}
function blockLoop(body) {
  return {
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body,
      },
    ],
  };
}
var PI = Math.PI;
var TWO_PI = 2 * PI;
var HALF_PI = PI / 2;
var INV_TWO_PI = 1 / TWO_PI;
var LN2 = Math.LN2;
var LOG2E = Math.LOG2E;
var LOG10E = Math.LOG10E;
var f64Type = { kind: "f64" };
var i32Type = { kind: "i32" };
var f64Param = [f64Type];
var f64Result = [f64Type];
function emitInlineMathFunctions(ctx, needed) {
  const addedFuncs = /* @__PURE__ */ new Map();
  function addMathFunc(def) {
    const typeIdx = addFuncType(ctx, def.params, def.results, def.name + "_type");
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name: def.name,
      typeIdx,
      locals: def.locals,
      body: def.body,
      exported: false,
    });
    ctx.funcMap.set(def.name, funcIdx);
    addedFuncs.set(def.name, funcIdx);
    return funcIdx;
  }
  function getFuncIdx(name) {
    const idx = addedFuncs.get(name);
    if (idx === void 0) {
      throw new Error(`Math helper ${name} not yet added but referenced`);
    }
    return idx;
  }
  const needSinCos = needed.has("sin") || needed.has("cos") || needed.has("tan");
  const needExp =
    needed.has("exp") ||
    needed.has("sinh") ||
    needed.has("cosh") ||
    needed.has("tanh") ||
    needed.has("pow") ||
    needed.has("expm1");
  const needLog =
    needed.has("log") ||
    needed.has("log2") ||
    needed.has("log10") ||
    needed.has("pow") ||
    needed.has("asinh") ||
    needed.has("acosh") ||
    needed.has("atanh") ||
    needed.has("log1p");
  const needAtan = needed.has("atan") || needed.has("asin") || needed.has("acos") || needed.has("atan2");
  if (needSinCos) {
    addMathFunc({
      name: "__math_reduce_trig",
      params: f64Param,
      results: f64Result,
      locals: [{ name: "n", type: f64Type }],
      body: [
        // n = round(x / (2*pi))
        localGet(0),
        f64c(INV_TWO_PI),
        mul,
        f64c(0.5),
        add,
        ffloor,
        localSet(1),
        // r = x - n * 2*pi (two-step Cody-Waite for precision)
        localGet(0),
        localGet(1),
        f64c(6.283185307179586),
        mul,
        sub,
        localGet(1),
        f64c(12246467991473532e-32),
        mul,
        sub,
      ],
    });
  }
  if (needed.has("sin") || needed.has("tan")) {
    const reduceIdx = getFuncIdx("__math_reduce_trig");
    addMathFunc({
      name: "Math_sin",
      params: f64Param,
      results: f64Result,
      locals: [
        { name: "r", type: f64Type },
        { name: "r2", type: f64Type },
      ],
      body: [
        // NaN → NaN
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        // |x| == Infinity → NaN
        ...ifThenRet([localGet(0), fabs, f64c(Infinity), feq], [f64c(NaN)]),
        // x == 0 → x (preserves -0)
        ...ifThenRet([localGet(0), f64c(0), feq], [localGet(0)]),
        // Range reduce to [-pi, pi]
        localGet(0),
        call(reduceIdx),
        localSet(1),
        // r2 = r*r
        localGet(1),
        localGet(1),
        mul,
        localSet(2),
        // sin(r) via Horner form of Taylor series:
        // r * (1 + r2*(-1/6 + r2*(1/120 + r2*(-1/5040 + r2*(1/362880 + r2*(-1/39916800 + r2/6227020800))))))
        localGet(2),
        f64c(1 / 6227020800),
        mul,
        f64c(-1 / 39916800),
        add,
        localGet(2),
        mul,
        f64c(1 / 362880),
        add,
        localGet(2),
        mul,
        f64c(-1 / 5040),
        add,
        localGet(2),
        mul,
        f64c(1 / 120),
        add,
        localGet(2),
        mul,
        f64c(-1 / 6),
        add,
        localGet(2),
        mul,
        f64c(1),
        add,
        localGet(1),
        mul,
      ],
    });
  }
  if (needed.has("cos") || needed.has("tan")) {
    const reduceIdx = getFuncIdx("__math_reduce_trig");
    addMathFunc({
      name: "Math_cos",
      params: f64Param,
      results: f64Result,
      locals: [
        { name: "r", type: f64Type },
        { name: "r2", type: f64Type },
      ],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), fabs, f64c(Infinity), feq], [f64c(NaN)]),
        // Range reduce
        localGet(0),
        call(reduceIdx),
        localSet(1),
        localGet(1),
        localGet(1),
        mul,
        localSet(2),
        // cos(r) via Horner:
        // 1 + r2*(-1/2 + r2*(1/24 + r2*(-1/720 + r2*(1/40320 + r2*(-1/3628800 + r2/479001600)))))
        localGet(2),
        f64c(1 / 479001600),
        mul,
        f64c(-1 / 3628800),
        add,
        localGet(2),
        mul,
        f64c(1 / 40320),
        add,
        localGet(2),
        mul,
        f64c(-1 / 720),
        add,
        localGet(2),
        mul,
        f64c(1 / 24),
        add,
        localGet(2),
        mul,
        f64c(-1 / 2),
        add,
        localGet(2),
        mul,
        f64c(1),
        add,
      ],
    });
  }
  if (needExp) {
    addMathFunc({
      name: "Math_exp",
      params: f64Param,
      results: f64Result,
      locals: [
        { name: "n", type: f64Type },
        { name: "r", type: f64Type },
        { name: "expR", type: f64Type },
        { name: "ni", type: i32Type },
        { name: "pow2", type: f64Type },
        { name: "base", type: f64Type },
      ],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(Infinity), feq], [f64c(Infinity)]),
        ...ifThenRet([localGet(0), f64c(-Infinity), feq], [f64c(0)]),
        ...ifThenRet([localGet(0), f64c(709.7), fgt], [f64c(Infinity)]),
        ...ifThenRet([localGet(0), f64c(-745), flt], [f64c(0)]),
        // n = round(x / ln2)
        localGet(0),
        f64c(LOG2E),
        mul,
        f64c(0.5),
        add,
        ffloor,
        localSet(1),
        // r = x - n * ln2
        localGet(0),
        localGet(1),
        f64c(LN2),
        mul,
        sub,
        localSet(2),
        // exp(r) via Horner (Taylor order 7):
        // 1 + r*(1 + r*(1/2 + r*(1/6 + r*(1/24 + r*(1/120 + r*(1/720 + r/5040))))))
        localGet(2),
        f64c(1 / 5040),
        mul,
        f64c(1 / 720),
        add,
        localGet(2),
        mul,
        f64c(1 / 120),
        add,
        localGet(2),
        mul,
        f64c(1 / 24),
        add,
        localGet(2),
        mul,
        f64c(1 / 6),
        add,
        localGet(2),
        mul,
        f64c(1 / 2),
        add,
        localGet(2),
        mul,
        f64c(1),
        add,
        localGet(2),
        mul,
        f64c(1),
        add,
        localSet(3),
        // Compute 2^n via repeated squaring
        f64c(1),
        localSet(5),
        localGet(1),
        fabs,
        truncSatI32,
        localSet(4),
        f64c(2),
        localSet(6),
        blockLoop([
          localGet(4),
          i32eqz,
          { op: "br_if", depth: 1 },
          // if ni & 1, pow2 *= base
          localGet(4),
          i32const(1),
          { op: "i32.and" },
          { op: "if", blockType: { kind: "empty" }, then: [localGet(5), localGet(6), mul, localSet(5)] },
          // base *= base
          localGet(6),
          localGet(6),
          mul,
          localSet(6),
          // ni >>= 1
          localGet(4),
          i32const(1),
          { op: "i32.shr_u" },
          localSet(4),
          { op: "br", depth: 0 },
        ]),
        // If n < 0, pow2 = 1/pow2
        localGet(1),
        f64c(0),
        flt,
        { op: "if", blockType: { kind: "empty" }, then: [f64c(1), localGet(5), div, localSet(5)] },
        // result = expR * pow2
        localGet(3),
        localGet(5),
        mul,
      ],
    });
  }
  if (needLog) {
    addMathFunc({
      name: "Math_log",
      params: f64Param,
      results: f64Result,
      locals: [
        { name: "e", type: f64Type },
        { name: "f", type: f64Type },
        { name: "t", type: f64Type },
        { name: "t2", type: f64Type },
      ],
      body: [
        ...ifThenRet([localGet(0), f64c(0), flt], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(0), feq], [f64c(-Infinity)]),
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(Infinity), feq], [f64c(Infinity)]),
        ...ifThenRet([localGet(0), f64c(1), feq], [f64c(0)]),
        // e = 0, f = x
        f64c(0),
        localSet(1),
        localGet(0),
        localSet(2),
        // While f >= 2, halve f and increment e
        blockLoop([
          localGet(2),
          f64c(2),
          flt,
          { op: "br_if", depth: 1 },
          localGet(2),
          f64c(0.5),
          mul,
          localSet(2),
          localGet(1),
          f64c(1),
          add,
          localSet(1),
          { op: "br", depth: 0 },
        ]),
        // While f < 0.5, double f and decrement e
        blockLoop([
          localGet(2),
          f64c(0.5),
          fge,
          { op: "br_if", depth: 1 },
          localGet(2),
          f64c(2),
          mul,
          localSet(2),
          localGet(1),
          f64c(1),
          sub,
          localSet(1),
          { op: "br", depth: 0 },
        ]),
        // Adjust to [sqrt(0.5), sqrt(2)]
        localGet(2),
        f64c(1.4142135623730951),
        fgt,
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [localGet(2), f64c(0.5), mul, localSet(2), localGet(1), f64c(1), add, localSet(1)],
        },
        // t = (f - 1) / (f + 1)
        localGet(2),
        f64c(1),
        sub,
        localGet(2),
        f64c(1),
        add,
        div,
        localSet(3),
        // t2 = t * t
        localGet(3),
        localGet(3),
        mul,
        localSet(4),
        // log(f) = 2*t*(1 + t2*(1/3 + t2*(1/5 + t2*(1/7 + t2*(1/9 + t2*(1/11 + t2/13))))))
        localGet(4),
        f64c(1 / 13),
        mul,
        f64c(1 / 11),
        add,
        localGet(4),
        mul,
        f64c(1 / 9),
        add,
        localGet(4),
        mul,
        f64c(1 / 7),
        add,
        localGet(4),
        mul,
        f64c(1 / 5),
        add,
        localGet(4),
        mul,
        f64c(1 / 3),
        add,
        localGet(4),
        mul,
        f64c(1),
        add,
        localGet(3),
        mul,
        f64c(2),
        mul,
        // result = e * ln2 + log(f)
        localGet(1),
        f64c(LN2),
        mul,
        add,
      ],
    });
  }
  if (needAtan) {
    addMathFunc({
      name: "Math_atan",
      params: f64Param,
      results: f64Result,
      locals: [
        { name: "ax", type: f64Type },
        { name: "t", type: f64Type },
        { name: "t2", type: f64Type },
        { name: "offset", type: f64Type },
      ],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(Infinity), feq], [f64c(HALF_PI)]),
        ...ifThenRet([localGet(0), f64c(-Infinity), feq], [f64c(-HALF_PI)]),
        ...ifThenRet([localGet(0), f64c(0), feq], [localGet(0)]),
        // ax = |x|
        localGet(0),
        fabs,
        localSet(1),
        f64c(0),
        localSet(4),
        // Range reduction:
        // if ax > tan(67.5) = 2.414..., use atan(x) = pi/2 - atan(1/x)
        // if ax > tan(22.5) = 0.414..., use atan(x) = pi/4 + atan((ax-1)/(ax+1))
        localGet(1),
        f64c(2.414213562373095),
        fgt,
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [f64c(HALF_PI), localSet(4), f64c(1), localGet(1), div, neg, localSet(1)],
          else: [
            localGet(1),
            f64c(0.414213562373095),
            fgt,
            {
              op: "if",
              blockType: { kind: "empty" },
              then: [f64c(PI / 4), localSet(4), localGet(1), f64c(1), sub, localGet(1), f64c(1), add, div, localSet(1)],
            },
          ],
        },
        // t = reduced argument (stored back in local 1)
        localGet(1),
        localTee(2),
        localGet(2),
        mul,
        localSet(3),
        // atan(t) polynomial (odd, Horner form):
        // t*(1 + t2*(-1/3 + t2*(1/5 + t2*(-1/7 + t2*(1/9 + t2*(-1/11 + t2*(1/13 - t2/15)))))))
        localGet(3),
        f64c(-1 / 15),
        mul,
        f64c(1 / 13),
        add,
        localGet(3),
        mul,
        f64c(-1 / 11),
        add,
        localGet(3),
        mul,
        f64c(1 / 9),
        add,
        localGet(3),
        mul,
        f64c(-1 / 7),
        add,
        localGet(3),
        mul,
        f64c(1 / 5),
        add,
        localGet(3),
        mul,
        f64c(-1 / 3),
        add,
        localGet(3),
        mul,
        f64c(1),
        add,
        localGet(2),
        mul,
        // result = offset + poly
        localGet(4),
        add,
        // Apply original sign
        localGet(0),
        copysign,
      ],
    });
  }
  if (needed.has("tan")) {
    const sinIdx = getFuncIdx("Math_sin");
    const cosIdx = getFuncIdx("Math_cos");
    addMathFunc({
      name: "Math_tan",
      params: f64Param,
      results: f64Result,
      locals: [],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), fabs, f64c(Infinity), feq], [f64c(NaN)]),
        localGet(0),
        call(sinIdx),
        localGet(0),
        call(cosIdx),
        div,
      ],
    });
  }
  if (needed.has("asin")) {
    const atanIdx = getFuncIdx("Math_atan");
    addMathFunc({
      name: "Math_asin",
      params: f64Param,
      results: f64Result,
      locals: [],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), fabs, f64c(1), fgt], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(1), feq], [f64c(HALF_PI)]),
        ...ifThenRet([localGet(0), f64c(-1), feq], [f64c(-HALF_PI)]),
        // atan(x / sqrt(1 - x*x))
        localGet(0),
        f64c(1),
        localGet(0),
        localGet(0),
        mul,
        sub,
        fsqrt,
        div,
        call(atanIdx),
      ],
    });
  }
  if (needed.has("acos")) {
    const atanIdx = getFuncIdx("Math_atan");
    addMathFunc({
      name: "Math_acos",
      params: f64Param,
      results: f64Result,
      locals: [],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), fabs, f64c(1), fgt], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(1), feq], [f64c(0)]),
        ...ifThenRet([localGet(0), f64c(-1), feq], [f64c(PI)]),
        f64c(HALF_PI),
        localGet(0),
        f64c(1),
        localGet(0),
        localGet(0),
        mul,
        sub,
        fsqrt,
        div,
        call(atanIdx),
        sub,
      ],
    });
  }
  if (needed.has("atan2")) {
    const atanIdx = getFuncIdx("Math_atan");
    addMathFunc({
      name: "Math_atan2",
      params: [f64Type, f64Type],
      results: f64Result,
      locals: [{ name: "atmp", type: f64Type }],
      // local 2 = atan_result temp
      body: buildAtan2Body(atanIdx),
    });
  }
  if (needed.has("log2")) {
    addMathFunc({
      name: "Math_log2",
      params: f64Param,
      results: f64Result,
      locals: [
        { name: "e", type: f64Type },
        { name: "f", type: f64Type },
        { name: "t", type: f64Type },
        { name: "t2", type: f64Type },
      ],
      body: [
        ...ifThenRet([localGet(0), f64c(0), flt], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(0), feq], [f64c(-Infinity)]),
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(Infinity), feq], [f64c(Infinity)]),
        ...ifThenRet([localGet(0), f64c(1), feq], [f64c(0)]),
        // Range reduction: x = f * 2^e, f in [0.5, 2)
        f64c(0),
        localSet(1),
        localGet(0),
        localSet(2),
        // While f >= 2, halve f and increment e
        blockLoop([
          localGet(2),
          f64c(2),
          flt,
          { op: "br_if", depth: 1 },
          localGet(2),
          f64c(0.5),
          mul,
          localSet(2),
          localGet(1),
          f64c(1),
          add,
          localSet(1),
          { op: "br", depth: 0 },
        ]),
        // While f < 0.5, double f and decrement e
        blockLoop([
          localGet(2),
          f64c(0.5),
          fge,
          { op: "br_if", depth: 1 },
          localGet(2),
          f64c(2),
          mul,
          localSet(2),
          localGet(1),
          f64c(1),
          sub,
          localSet(1),
          { op: "br", depth: 0 },
        ]),
        // Adjust to [sqrt(0.5), sqrt(2)]
        localGet(2),
        f64c(1.4142135623730951),
        fgt,
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [localGet(2), f64c(0.5), mul, localSet(2), localGet(1), f64c(1), add, localSet(1)],
        },
        // If f == 1, result is exactly e (avoids precision loss)
        localGet(2),
        f64c(1),
        feq,
        { op: "if", blockType: { kind: "empty" }, then: [localGet(1), ret] },
        // t = (f - 1) / (f + 1)
        localGet(2),
        f64c(1),
        sub,
        localGet(2),
        f64c(1),
        add,
        div,
        localSet(3),
        // t2 = t * t
        localGet(3),
        localGet(3),
        mul,
        localSet(4),
        // log(f) via atanh series = 2*t*(1 + t2*(1/3 + t2*(1/5 + t2*(1/7 + t2*(1/9 + t2*(1/11 + t2/13))))))
        localGet(4),
        f64c(1 / 13),
        mul,
        f64c(1 / 11),
        add,
        localGet(4),
        mul,
        f64c(1 / 9),
        add,
        localGet(4),
        mul,
        f64c(1 / 7),
        add,
        localGet(4),
        mul,
        f64c(1 / 5),
        add,
        localGet(4),
        mul,
        f64c(1 / 3),
        add,
        localGet(4),
        mul,
        f64c(1),
        add,
        localGet(3),
        mul,
        f64c(2),
        mul,
        // log2(f) = log(f) * LOG2E
        f64c(LOG2E),
        mul,
        // result = e + log2(f)
        localGet(1),
        add,
      ],
    });
  }
  if (needed.has("log10")) {
    const logIdx = getFuncIdx("Math_log");
    addMathFunc({
      name: "Math_log10",
      params: f64Param,
      results: f64Result,
      locals: [
        { name: "result", type: f64Type },
        { name: "rounded", type: f64Type },
      ],
      body: [
        // Compute log(x) * LOG10E
        localGet(0),
        call(logIdx),
        f64c(LOG10E),
        mul,
        localSet(1),
        // Round-to-nearest integer when very close (within 1e-12)
        // This corrects precision loss for exact powers of 10
        localGet(1),
        { op: "f64.nearest" },
        localSet(2),
        localGet(1),
        localGet(2),
        sub,
        fabs,
        f64c(1e-12),
        flt,
        ifElse(f64Type, [localGet(2)], [localGet(1)]),
      ],
    });
  }
  if (needed.has("pow")) {
    const expIdx = getFuncIdx("Math_exp");
    const logIdx = getFuncIdx("Math_log");
    addMathFunc({
      name: "Math_pow",
      params: [f64Type, f64Type],
      results: f64Result,
      locals: [{ name: "absBase", type: f64Type }],
      body: buildPowBody(expIdx, logIdx),
    });
  }
  if (needed.has("sinh")) {
    const expIdx = getFuncIdx("Math_exp");
    addMathFunc({
      name: "Math_sinh",
      params: f64Param,
      results: f64Result,
      locals: [{ name: "ep", type: f64Type }],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(Infinity), feq], [f64c(Infinity)]),
        ...ifThenRet([localGet(0), f64c(-Infinity), feq], [f64c(-Infinity)]),
        // (exp(x) - 1/exp(x)) / 2
        localGet(0),
        call(expIdx),
        localTee(1),
        f64c(1),
        localGet(1),
        div,
        sub,
        f64c(2),
        div,
      ],
    });
  }
  if (needed.has("cosh")) {
    const expIdx = getFuncIdx("Math_exp");
    addMathFunc({
      name: "Math_cosh",
      params: f64Param,
      results: f64Result,
      locals: [{ name: "ep", type: f64Type }],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), fabs, f64c(Infinity), feq], [f64c(Infinity)]),
        localGet(0),
        call(expIdx),
        localTee(1),
        f64c(1),
        localGet(1),
        div,
        add,
        f64c(2),
        div,
      ],
    });
  }
  if (needed.has("tanh")) {
    const expIdx = getFuncIdx("Math_exp");
    addMathFunc({
      name: "Math_tanh",
      params: f64Param,
      results: f64Result,
      locals: [{ name: "e2x", type: f64Type }],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(20), fgt], [f64c(1)]),
        ...ifThenRet([localGet(0), f64c(-20), flt], [f64c(-1)]),
        // (exp(2x) - 1) / (exp(2x) + 1)
        localGet(0),
        f64c(2),
        mul,
        call(expIdx),
        localTee(1),
        f64c(1),
        sub,
        localGet(1),
        f64c(1),
        add,
        div,
      ],
    });
  }
  if (needed.has("asinh")) {
    const logIdx = getFuncIdx("Math_log");
    addMathFunc({
      name: "Math_asinh",
      params: f64Param,
      results: f64Result,
      locals: [],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(Infinity), feq], [f64c(Infinity)]),
        ...ifThenRet([localGet(0), f64c(-Infinity), feq], [f64c(-Infinity)]),
        // sign(x) * log(|x| + sqrt(x*x + 1))
        localGet(0),
        fabs,
        localGet(0),
        localGet(0),
        mul,
        f64c(1),
        add,
        fsqrt,
        add,
        call(logIdx),
        localGet(0),
        copysign,
      ],
    });
  }
  if (needed.has("acosh")) {
    const logIdx = getFuncIdx("Math_log");
    addMathFunc({
      name: "Math_acosh",
      params: f64Param,
      results: f64Result,
      locals: [],
      body: [
        ...ifThenRet([localGet(0), f64c(1), flt], [f64c(NaN)]),
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(1), feq], [f64c(0)]),
        localGet(0),
        localGet(0),
        localGet(0),
        mul,
        f64c(1),
        sub,
        fsqrt,
        add,
        call(logIdx),
      ],
    });
  }
  if (needed.has("atanh")) {
    const logIdx = getFuncIdx("Math_log");
    addMathFunc({
      name: "Math_atanh",
      params: f64Param,
      results: f64Result,
      locals: [],
      body: [
        ...ifThenRet([localGet(0), fabs, f64c(1), fgt], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(1), feq], [f64c(Infinity)]),
        ...ifThenRet([localGet(0), f64c(-1), feq], [f64c(-Infinity)]),
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        // atanh(+/-0) = +/-0: return x to preserve sign of zero
        ...ifThenRet([localGet(0), f64c(0), feq], [localGet(0)]),
        f64c(1),
        localGet(0),
        add,
        f64c(1),
        localGet(0),
        sub,
        div,
        call(logIdx),
        f64c(0.5),
        mul,
      ],
    });
  }
  if (needed.has("cbrt")) {
    addMathFunc({
      name: "Math_cbrt",
      params: f64Param,
      results: f64Result,
      locals: [
        { name: "guess", type: f64Type },
        { name: "i", type: i32Type },
      ],
      body: [
        ...ifThenRet([localGet(0), f64c(0), feq], [localGet(0)]),
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), fabs, f64c(Infinity), feq], [localGet(0)]),
        // Seed: copysign(sqrt(sqrt(|x|)), x)
        localGet(0),
        fabs,
        fsqrt,
        fsqrt,
        localGet(0),
        copysign,
        localSet(1),
        // 8 Newton iterations: guess = (2*guess + x/(guess*guess)) / 3
        i32const(8),
        localSet(2),
        blockLoop([
          localGet(2),
          i32eqz,
          { op: "br_if", depth: 1 },
          localGet(1),
          f64c(2),
          mul,
          localGet(0),
          localGet(1),
          localGet(1),
          mul,
          div,
          add,
          f64c(3),
          div,
          localSet(1),
          localGet(2),
          i32const(1),
          i32sub,
          localSet(2),
          { op: "br", depth: 0 },
        ]),
        localGet(1),
      ],
    });
  }
  if (needed.has("expm1")) {
    const expIdx = getFuncIdx("Math_exp");
    addMathFunc({
      name: "Math_expm1",
      params: f64Param,
      results: f64Result,
      locals: [],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(Infinity), feq], [f64c(Infinity)]),
        ...ifThenRet([localGet(0), f64c(-Infinity), feq], [f64c(-1)]),
        // expm1(+/-0) = +/-0: return x to preserve sign of zero
        ...ifThenRet([localGet(0), f64c(0), feq], [localGet(0)]),
        // For small |x|, use Taylor series for precision
        localGet(0),
        fabs,
        f64c(1e-5),
        flt,
        ifElse(
          f64Type,
          [
            // x + x^2/2 + x^3/6 + x^4/24
            localGet(0),
            localGet(0),
            localGet(0),
            mul,
            f64c(0.5),
            mul,
            add,
            localGet(0),
            localGet(0),
            mul,
            localGet(0),
            mul,
            f64c(1 / 6),
            mul,
            add,
            localGet(0),
            localGet(0),
            mul,
            localGet(0),
            mul,
            localGet(0),
            mul,
            f64c(1 / 24),
            mul,
            add,
          ],
          [localGet(0), call(expIdx), f64c(1), sub],
        ),
      ],
    });
  }
  if (needed.has("log1p")) {
    const logIdx = getFuncIdx("Math_log");
    addMathFunc({
      name: "Math_log1p",
      params: f64Param,
      results: f64Result,
      locals: [],
      body: [
        ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
        ...ifThenRet([localGet(0), f64c(-1), feq], [f64c(-Infinity)]),
        ...ifThenRet([localGet(0), f64c(-1), flt], [f64c(NaN)]),
        // For small |x|, use Taylor series
        localGet(0),
        fabs,
        f64c(1e-4),
        flt,
        ifElse(
          f64Type,
          [
            // x - x^2/2 + x^3/3
            localGet(0),
            localGet(0),
            localGet(0),
            mul,
            f64c(0.5),
            mul,
            sub,
            localGet(0),
            localGet(0),
            mul,
            localGet(0),
            mul,
            f64c(1 / 3),
            mul,
            add,
          ],
          [f64c(1), localGet(0), add, call(logIdx)],
        ),
      ],
    });
  }
}
function buildAtan2Body(atanIdx) {
  return [
    // NaN checks
    ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
    ...ifThenRet([localGet(1), localGet(1), fne], [f64c(NaN)]),
    // y == 0 cases
    localGet(0),
    f64c(0),
    feq,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        // y == 0, x > 0 → +0 (preserving sign of y)
        localGet(1),
        f64c(0),
        fgt,
        { op: "if", blockType: { kind: "empty" }, then: [localGet(0), ret] },
        // y == 0, x < 0 → copysign(pi, y)
        localGet(1),
        f64c(0),
        flt,
        { op: "if", blockType: { kind: "empty" }, then: [f64c(PI), localGet(0), copysign, ret] },
        // y == 0, x == 0 → copysign(0 or pi based on sign of x)
        // atan2(+0,+0) = +0, atan2(+0,-0) = pi, atan2(-0,+0) = -0, atan2(-0,-0) = -pi
        // Check sign of x via 1/x: +0 → +Inf, -0 → -Inf
        f64c(1),
        localGet(1),
        div,
        f64c(0),
        fgt,
        ifElse(f64Type, [f64c(0), localGet(0), copysign], [f64c(PI), localGet(0), copysign]),
        ret,
      ],
    },
    // x == +Inf
    localGet(1),
    f64c(Infinity),
    feq,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        localGet(0),
        fabs,
        f64c(Infinity),
        feq,
        ifElse(f64Type, [f64c(PI / 4), localGet(0), copysign], [f64c(0), localGet(0), copysign]),
        ret,
      ],
    },
    // x == -Inf
    localGet(1),
    f64c(-Infinity),
    feq,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        localGet(0),
        fabs,
        f64c(Infinity),
        feq,
        ifElse(f64Type, [f64c((3 * PI) / 4), localGet(0), copysign], [f64c(PI), localGet(0), copysign]),
        ret,
      ],
    },
    // y == ±Inf, x finite
    ...ifThenRet([localGet(0), fabs, f64c(Infinity), feq], [f64c(HALF_PI), localGet(0), copysign]),
    // General case: atan(y/x) with quadrant adjustment
    localGet(1),
    f64c(0),
    fgt,
    ifElse(
      f64Type,
      [localGet(0), localGet(1), div, call(atanIdx)],
      [
        localGet(1),
        f64c(0),
        flt,
        ifElse(
          f64Type,
          [
            localGet(0),
            localGet(1),
            div,
            call(atanIdx),
            localSet(2),
            // Add or subtract pi based on sign of y
            localGet(0),
            f64c(0),
            fge,
            ifElse(f64Type, [localGet(2), f64c(PI), add], [localGet(2), f64c(PI), sub]),
          ],
          [
            // x == 0, y != 0 → copysign(pi/2, y)
            f64c(HALF_PI),
            localGet(0),
            copysign,
          ],
        ),
      ],
    ),
  ];
}
function buildPowBody(expIdx, logIdx) {
  return [
    // exp == 0 → 1 (for any base, including NaN)
    ...ifThenRet([localGet(1), f64c(0), feq], [f64c(1)]),
    // NaN checks (must come before base==1, per spec: pow(1, NaN) → NaN)
    ...ifThenRet([localGet(0), localGet(0), fne], [f64c(NaN)]),
    ...ifThenRet([localGet(1), localGet(1), fne], [f64c(NaN)]),
    // base == 1 → 1
    ...ifThenRet([localGet(0), f64c(1), feq], [f64c(1)]),
    // exp == 1 → base
    ...ifThenRet([localGet(1), f64c(1), feq], [localGet(0)]),
    // exp == -1 → 1/base
    ...ifThenRet([localGet(1), f64c(-1), feq], [f64c(1), localGet(0), div]),
    // exp == 0.5 → sqrt(base)
    ...ifThenRet([localGet(1), f64c(0.5), feq], [localGet(0), fsqrt]),
    // exp == 2 → base*base
    ...ifThenRet([localGet(1), f64c(2), feq], [localGet(0), localGet(0), mul]),
    // base == 0: check sign of base and parity of exponent
    localGet(0),
    f64c(0),
    feq,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        // isNegZero AND isOddInt? → result has negative sign
        // isNegZero: 1/base == -Infinity
        // isOddInt: trunc(exp) == exp AND floor(exp/2)*2 != trunc(exp)
        f64c(1),
        localGet(0),
        div,
        f64c(-Infinity),
        feq,
        // isNegZero?
        localGet(1),
        localGet(1),
        ftrunc,
        feq,
        // isInteger?
        { op: "i32.and" },
        localGet(1),
        ftrunc,
        f64c(2),
        div,
        ffloor,
        // isOdd?
        f64c(2),
        mul,
        localGet(1),
        ftrunc,
        fne,
        { op: "i32.and" },
        // Stack: i32 (isNegZeroAndOddInt)
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            // Negative zero base + odd integer exp
            localGet(1),
            f64c(0),
            fgt,
            ifElse(f64Type, [f64c(-0)], [f64c(-Infinity)]),
            ret,
          ],
        },
        // Positive zero base (or even/non-integer exp)
        localGet(1),
        f64c(0),
        fgt,
        ifElse(f64Type, [f64c(0)], [f64c(Infinity)]),
        ret,
      ],
    },
    // base == +Inf
    ...ifThenRet(
      [localGet(0), f64c(Infinity), feq],
      [localGet(1), f64c(0), fgt, ifElse(f64Type, [f64c(Infinity)], [f64c(0)])],
    ),
    // base == -Inf: sign depends on whether exponent is odd integer
    localGet(0),
    f64c(-Infinity),
    feq,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        // isOddInt: trunc(exp) == exp AND floor(exp/2)*2 != trunc(exp)
        localGet(1),
        localGet(1),
        ftrunc,
        feq,
        // isInteger?
        localGet(1),
        ftrunc,
        f64c(2),
        div,
        ffloor,
        // isOdd?
        f64c(2),
        mul,
        localGet(1),
        ftrunc,
        fne,
        { op: "i32.and" },
        // Now i32 isOddInt is on stack
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            // Odd integer exponent: -Inf or -0
            localGet(1),
            f64c(0),
            fgt,
            ifElse(f64Type, [f64c(-Infinity)], [f64c(-0)]),
            ret,
          ],
        },
        // Even/non-integer exponent: +Inf or +0
        localGet(1),
        f64c(0),
        fgt,
        ifElse(f64Type, [f64c(Infinity)], [f64c(0)]),
        ret,
      ],
    },
    // base < 0: non-integer exp → NaN; integer exp → handle sign
    localGet(0),
    f64c(0),
    flt,
    {
      op: "if",
      blockType: { kind: "empty" },
      then: [
        // Non-integer exponent → NaN
        localGet(1),
        localGet(1),
        ftrunc,
        fne,
        { op: "if", blockType: { kind: "empty" }, then: [f64c(NaN), ret] },
        // Integer exponent: result = exp(exp * log(|base|))
        localGet(0),
        fabs,
        localSet(2),
        localGet(1),
        localGet(2),
        call(logIdx),
        mul,
        call(expIdx),
        localSet(2),
        // reuse absBase local to store result
        // If exponent is odd, negate the result
        // odd check: floor(exp/2)*2 != exp (for integer exp)
        localGet(1),
        ftrunc,
        f64c(2),
        div,
        ffloor,
        f64c(2),
        mul,
        localGet(1),
        ftrunc,
        fne,
        ifElse(
          f64Type,
          [localGet(2), neg],
          // odd → negate
          [localGet(2)],
          // even → keep
        ),
        ret,
      ],
    },
    // General case: exp(exponent * log(base))
    localGet(1),
    localGet(0),
    call(logIdx),
    mul,
    call(expIdx),
  ];
}

// src/codegen/index.ts
function markLeafStructsFinal(mod) {
  const hasSubtypes = /* @__PURE__ */ new Set();
  for (let i = 0; i < mod.types.length; i++) {
    const td = mod.types[i];
    if (td.kind === "struct" && td.superTypeIdx !== void 0 && td.superTypeIdx >= 0) {
      hasSubtypes.add(td.superTypeIdx);
    } else if (td.kind === "rec") {
      for (const inner of td.types) {
        if (inner.kind === "struct" && inner.superTypeIdx !== void 0 && inner.superTypeIdx >= 0) {
          hasSubtypes.add(inner.superTypeIdx);
        } else if (inner.kind === "sub" && inner.superType !== null && inner.superType >= 0) {
          hasSubtypes.add(inner.superType);
        }
      }
    } else if (td.kind === "sub" && td.superType !== null && td.superType >= 0) {
      hasSubtypes.add(td.superType);
    }
  }
  for (let i = 0; i < mod.types.length; i++) {
    const td = mod.types[i];
    if (td.kind === "struct" && td.superTypeIdx !== void 0 && !hasSubtypes.has(i)) {
      td.final = true;
    } else if (td.kind === "rec") {
      let innerIdx = i;
      for (const inner of td.types) {
        if (inner.kind === "struct" && inner.superTypeIdx !== void 0 && !hasSubtypes.has(innerIdx)) {
          inner.final = true;
        }
        innerIdx++;
      }
    }
  }
}
function repairStructTypeMismatches(mod) {
  let totalFixed = 0;
  for (const func of mod.functions) {
    const funcType = mod.types[func.typeIdx];
    const paramTypes = funcType && funcType.kind === "func" ? funcType.params : [];
    const localTypes = [...paramTypes, ...func.locals.map((l) => l.type)];
    totalFixed += repairBody(func.body, localTypes, mod);
  }
  return totalFixed;
}
function repairBody(body, localTypes, mod) {
  let fixed = 0;
  for (const instr of body) {
    switch (instr.op) {
      case "block":
      case "loop":
        if (instr.body) fixed += repairBody(instr.body, localTypes, mod);
        break;
      case "if":
        if (instr.then) fixed += repairBody(instr.then, localTypes, mod);
        if (instr.else) fixed += repairBody(instr.else, localTypes, mod);
        break;
      case "try":
        if (instr.body) fixed += repairBody(instr.body, localTypes, mod);
        if (instr.catches) {
          for (const c of instr.catches) {
            if (c.body) fixed += repairBody(c.body, localTypes, mod);
          }
        }
        break;
    }
  }
  let i = 0;
  while (i < body.length - 1) {
    const next = body[i + 1];
    if (next.op !== "struct.get") {
      i++;
      continue;
    }
    const structTypeIdx = next.typeIdx;
    const curr = body[i];
    if (curr.op === "ref.null.extern") {
      body[i] = { op: "ref.null", typeIdx: structTypeIdx };
      fixed++;
      i += 2;
      continue;
    }
    if (curr.op === "local.get") {
      const idx = curr.index;
      const localType = localTypes[idx];
      if (localType && localType.kind === "externref") {
        body.splice(i + 1, 0, { op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx: structTypeIdx });
        fixed++;
        i += 4;
        continue;
      }
    }
    if (curr.op === "local.tee") {
      const idx = curr.index;
      const localType = localTypes[idx];
      if (localType && localType.kind === "externref") {
        body.splice(i + 1, 0, { op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx: structTypeIdx });
        fixed++;
        i += 4;
        continue;
      }
    }
    if (curr.op === "call") {
      const funcIdx = curr.funcIdx;
      const numImports = mod.imports.filter((imp) => imp.desc.kind === "func").length;
      let retType;
      if (funcIdx < numImports) {
        const imp = mod.imports.filter((imp2) => imp2.desc.kind === "func")[funcIdx];
        const ft = imp ? mod.types[imp.desc.typeIdx] : void 0;
        if (ft?.kind === "func" && ft.results.length > 0) retType = ft.results[0];
      } else {
        const fn = mod.functions[funcIdx - numImports];
        const ft = fn ? mod.types[fn.typeIdx] : void 0;
        if (ft?.kind === "func" && ft.results.length > 0) retType = ft.results[0];
      }
      if (retType && retType.kind === "externref") {
        body.splice(i + 1, 0, { op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx: structTypeIdx });
        fixed++;
        i += 4;
        continue;
      }
    }
    i++;
  }
  i = 0;
  while (i < body.length) {
    const instr = body[i];
    if (instr.op !== "struct.set") {
      i++;
      continue;
    }
    const structTypeIdx = instr.typeIdx;
    let depth = 0;
    let refIdx = -1;
    for (let j = i - 1; j >= 0; j--) {
      depth += instrStackDelta(body[j], mod);
      if (depth >= 2) {
        refIdx = j;
        break;
      }
    }
    if (refIdx >= 0) {
      const refProducer = body[refIdx];
      if (refProducer.op === "ref.null.extern") {
        body[refIdx] = { op: "ref.null", typeIdx: structTypeIdx };
        fixed++;
        i++;
        continue;
      }
      if (refProducer.op === "local.get" || refProducer.op === "local.tee") {
        const idx = refProducer.index;
        const localType = localTypes[idx];
        if (localType && localType.kind === "externref") {
          body.splice(refIdx + 1, 0, { op: "any.convert_extern" }, { op: "ref.cast_null", typeIdx: structTypeIdx });
          fixed++;
          i += 3;
          continue;
        }
      }
    }
    i++;
  }
  return fixed;
}
function instrStackDelta(instr, mod) {
  switch (instr.op) {
    // Push 1 value, consume 0
    case "local.get":
    case "global.get":
    case "i32.const":
    case "i64.const":
    case "f64.const":
    case "f32.const":
    case "ref.null":
    case "ref.null.extern":
    case "ref.null.eq":
    case "ref.null.func":
    case "ref.func":
    case "memory.size":
      return 1;
    // Push 1, consume 1 (net 0)
    case "local.tee":
    case "ref.as_non_null":
    case "ref.cast":
    case "ref.cast_null":
    case "ref.test":
    case "ref.is_null":
    case "any.convert_extern":
    case "extern.convert_any":
    case "i32.eqz":
    case "i64.eqz":
    case "i32.clz":
    case "f64.abs":
    case "f64.neg":
    case "f64.floor":
    case "f64.ceil":
    case "f64.trunc":
    case "f64.nearest":
    case "f64.sqrt":
    case "f64.convert_i32_s":
    case "f64.convert_i32_u":
    case "f64.convert_i64_s":
    case "i32.trunc_f64_s":
    case "i32.trunc_f64_u":
    case "i32.trunc_sat_f64_s":
    case "i32.trunc_sat_f64_u":
    case "i64.trunc_sat_f64_s":
    case "i64.extend_i32_s":
    case "i64.extend_i32_u":
    case "i64.trunc_f64_s":
    case "array.len":
    case "f64.promote_f32":
    case "f64.reinterpret_i64":
    case "i64.reinterpret_f64":
      return 0;
    // Push 0, consume 1
    case "drop":
    case "local.set":
    case "global.set":
    case "br_if":
      return -1;
    // Push 1, consume 2 (net -1)
    case "i32.add":
    case "i32.sub":
    case "i32.mul":
    case "i32.eq":
    case "i32.ne":
    case "i32.lt_s":
    case "i32.le_s":
    case "i32.gt_s":
    case "i32.ge_s":
    case "i32.ge_u":
    case "i32.and":
    case "i32.or":
    case "i32.xor":
    case "i32.shl":
    case "i32.shr_s":
    case "i32.shr_u":
    case "i64.add":
    case "i64.sub":
    case "i64.mul":
    case "i64.div_s":
    case "i64.rem_s":
    case "i64.eq":
    case "i64.ne":
    case "i64.lt_s":
    case "i64.le_s":
    case "i64.gt_s":
    case "i64.ge_s":
    case "i64.and":
    case "i64.or":
    case "i64.xor":
    case "i64.shl":
    case "i64.shr_s":
    case "i64.shr_u":
    case "f64.add":
    case "f64.sub":
    case "f64.mul":
    case "f64.div":
    case "f64.eq":
    case "f64.ne":
    case "f64.lt":
    case "f64.le":
    case "f64.gt":
    case "f64.ge":
    case "f64.copysign":
    case "f64.min":
    case "f64.max":
    case "ref.eq":
      return -1;
    // Push 1, consume 3 (net -2)
    case "select":
      return -2;
    // struct.new: consumes N fields, pushes 1 struct ref
    case "struct.new": {
      const typeIdx = instr.typeIdx;
      const typeDef = mod.types[typeIdx];
      const fieldCount = typeDef?.kind === "struct" ? typeDef.fields.length : 0;
      return 1 - fieldCount;
    }
    // struct.get: consume 1 (struct ref), push 1 (field value) — net 0
    case "struct.get":
      return 0;
    // struct.set: consume 2 (struct ref + value), push 0 — net -2
    case "struct.set":
      return -2;
    // array operations
    case "array.get":
    case "array.get_s":
    case "array.get_u":
      return -1;
    // consume arr + idx, push value
    case "array.set":
      return -3;
    // consume arr + idx + value
    case "array.new":
    case "array.new_default":
      return 0;
    // consume length, push array (net 0)
    case "array.new_fixed": {
      const len = instr.length;
      return 1 - len;
    }
    // call: consumes params, pushes results
    case "call": {
      const funcIdx = instr.funcIdx;
      const numImports = mod.imports.filter((imp) => imp.desc.kind === "func").length;
      let typeIdx;
      if (funcIdx < numImports) {
        const imp = mod.imports.filter((imp2) => imp2.desc.kind === "func")[funcIdx];
        typeIdx = imp ? imp.desc.typeIdx : -1;
      } else {
        const fn = mod.functions[funcIdx - numImports];
        typeIdx = fn?.typeIdx ?? -1;
      }
      const ft = typeIdx >= 0 ? mod.types[typeIdx] : void 0;
      if (ft?.kind === "func") {
        return ft.results.length - ft.params.length;
      }
      return 0;
    }
    case "call_ref":
    case "return_call":
    case "return_call_ref":
    case "call_indirect":
      return 0;
    // conservative: unknown effect
    // Control flow — opaque
    case "block":
    case "loop":
    case "if":
    case "try":
      return 0;
    // Terminal
    case "return":
    case "br":
    case "unreachable":
    case "throw":
      return 0;
    case "nop":
      return 0;
    default:
      return 0;
  }
}
function extractConstantDefault(initializer, paramType) {
  if (paramType.kind === "f64") {
    if (ts15.isNumericLiteral(initializer)) {
      return { kind: "f64", value: Number(initializer.text) };
    }
    if (initializer.kind === ts15.SyntaxKind.TrueKeyword) {
      return { kind: "f64", value: 1 };
    }
    if (initializer.kind === ts15.SyntaxKind.FalseKeyword) {
      return { kind: "f64", value: 0 };
    }
    if (
      initializer.kind === ts15.SyntaxKind.UndefinedKeyword ||
      (ts15.isIdentifier(initializer) && initializer.text === "undefined")
    ) {
      return { kind: "f64", value: NaN };
    }
    if (initializer.kind === ts15.SyntaxKind.NullKeyword) {
      return { kind: "f64", value: 0 };
    }
    if (
      ts15.isPrefixUnaryExpression(initializer) &&
      initializer.operator === ts15.SyntaxKind.MinusToken &&
      ts15.isNumericLiteral(initializer.operand)
    ) {
      return { kind: "f64", value: -Number(initializer.operand.text) };
    }
    if (
      ts15.isPrefixUnaryExpression(initializer) &&
      initializer.operator === ts15.SyntaxKind.PlusToken &&
      ts15.isNumericLiteral(initializer.operand)
    ) {
      return { kind: "f64", value: Number(initializer.operand.text) };
    }
    return void 0;
  }
  if (paramType.kind === "i32") {
    if (ts15.isNumericLiteral(initializer)) {
      return { kind: "i32", value: Number(initializer.text) | 0 };
    }
    if (initializer.kind === ts15.SyntaxKind.TrueKeyword) {
      return { kind: "i32", value: 1 };
    }
    if (initializer.kind === ts15.SyntaxKind.FalseKeyword) {
      return { kind: "i32", value: 0 };
    }
    if (
      initializer.kind === ts15.SyntaxKind.NullKeyword ||
      initializer.kind === ts15.SyntaxKind.UndefinedKeyword ||
      (ts15.isIdentifier(initializer) && initializer.text === "undefined")
    ) {
      return { kind: "i32", value: 0 };
    }
    if (
      ts15.isPrefixUnaryExpression(initializer) &&
      initializer.operator === ts15.SyntaxKind.MinusToken &&
      ts15.isNumericLiteral(initializer.operand)
    ) {
      return { kind: "i32", value: -Number(initializer.operand.text) | 0 };
    }
    return void 0;
  }
  return void 0;
}
function generateModule(ast, options) {
  const mod = createEmptyModule();
  const ctx = createCodegenContext(mod, ast.checker, options);
  if (ctx.wasi) {
    registerWasiImports(ctx, ast.sourceFile);
  }
  collectExternDeclarations(ctx, ast.sourceFile);
  if (sourceUsesLibGlobals(ast.sourceFile)) {
    for (const sf of ast.program.getSourceFiles()) {
      const baseName = sf.fileName.split("/").pop() ?? sf.fileName;
      if (baseName.startsWith("lib.") && baseName.endsWith(".d.ts")) {
        collectExternDeclarations(ctx, sf);
        collectDeclaredGlobals(ctx, sf, ast.sourceFile);
      }
    }
  }
  registerBuiltinExternClasses(ctx);
  collectEmptyObjectWidening(ctx, ast.checker, ast.sourceFile);
  collectUsedExternImports(ctx, ast.sourceFile);
  collectAllSourceImports(ctx, ast.sourceFile);
  if (ctx.pendingMathMethods.size > 0) {
    emitInlineMathFunctions(ctx, ctx.pendingMathMethods);
  }
  emitWrapperValueOfFunctions(ctx);
  collectDeclarations(ctx, ast.sourceFile);
  applyShapeInference(ctx, ast.checker, ast.sourceFile);
  compileDeclarations(ctx, ast.sourceFile);
  fixupStructNewArgCounts(ctx);
  fixupStructNewResultCoercion(ctx);
  buildShapePropFlagsTable(ctx);
  collectDeclaredFuncRefs(ctx);
  const importNames = mod.imports.map((imp) => imp.name);
  for (const [key, info] of ctx.externClasses) {
    const prefix = `${info.importPrefix}_`;
    const isUsed = importNames.some((n) => n.startsWith(prefix));
    if (key === info.className && isUsed) {
      mod.externClasses.push({
        importPrefix: info.importPrefix,
        namespacePath: info.namespacePath,
        className: info.className,
        constructorParams: info.constructorParams,
        methods: info.methods,
        properties: info.properties,
      });
    }
  }
  mod.stringLiteralValues = ctx.stringLiteralValues;
  mod.asyncFunctions = ctx.asyncFunctions;
  emitStructFieldGetters(ctx);
  emitVecAccessExports(ctx);
  emitIteratorMethodExport(ctx);
  emitToPrimitiveMethodExports(ctx);
  if (ctx.wasi) {
    addWasiStartExport(ctx);
  }
  if (ctx.exnTagIdx >= 0) {
    const numImportTags = mod.imports.filter((i) => i.desc.kind === "tag").length;
    mod.exports.push({
      name: "__exn_tag",
      desc: { kind: "tag", index: numImportTags + ctx.exnTagIdx },
    });
  }
  markLeafStructsFinal(mod);
  eliminateDeadImports(mod);
  repairStructTypeMismatches(mod);
  peepholeOptimize(mod);
  stackBalance(mod);
  fixupExternConvertAny(ctx);
  return { module: mod, errors: ctx.errors };
}
function addWasiStartExport(ctx) {
  let targetIdx = ctx.funcMap.get("main");
  if (targetIdx === void 0) {
    for (let i = 0; i < ctx.mod.functions.length; i++) {
      if (ctx.mod.functions[i].name === "__module_init") {
        targetIdx = ctx.numImportFuncs + i;
        break;
      }
    }
  }
  if (targetIdx !== void 0) {
    const startTypeIdx = addFuncType(ctx, [], [], "$wasi_start_type");
    const startFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name: "_start",
      typeIdx: startTypeIdx,
      locals: [],
      body: [{ op: "call", funcIdx: targetIdx }],
      exported: true,
    });
    ctx.mod.exports.push({
      name: "_start",
      desc: { kind: "func", index: startFuncIdx },
    });
  }
}
function emitStructFieldGetters(ctx) {
  try {
    _emitStructFieldGettersInner(ctx);
  } catch (e) {}
}
function _emitStructFieldGettersInner(ctx) {
  const mod = ctx.mod;
  const fieldMap = /* @__PURE__ */ new Map();
  for (const [structName, fields] of ctx.structFields) {
    const typeIdx = ctx.structMap.get(structName);
    if (typeIdx === void 0) continue;
    if (
      structName.startsWith("Wrapper") ||
      structName === "$AnyValue" ||
      structName.startsWith("__vec_") ||
      structName.startsWith("__arr_")
    )
      continue;
    for (let i = 0; i < fields.length; i++) {
      const field = fields[i];
      if (!field || !field.type) continue;
      if (!field.name || field.name.startsWith("$")) continue;
      let entries = fieldMap.get(field.name);
      if (!entries) {
        entries = [];
        fieldMap.set(field.name, entries);
      }
      entries.push({ typeIdx, fieldIdx: i, fieldType: field.type });
    }
  }
  if (fieldMap.size === 0) return;
  const boxNumIdx = ctx.funcMap.get("__box_number");
  const getterExternTypeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }], "$sget_extern_type");
  const getterF64TypeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "f64" }], "$sget_f64_type");
  const getterI32TypeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }], "$sget_i32_type");
  for (const [fieldName, entries] of fieldMap) {
    const hasF64 = entries.some((e) => e.fieldType.kind === "f64");
    const hasI32 = entries.some((e) => e.fieldType.kind === "i32");
    const hasRef = entries.some((e) => e.fieldType.kind !== "f64" && e.fieldType.kind !== "i32");
    const allF64 = hasF64 && !hasI32 && !hasRef;
    const allI32 = hasI32 && !hasF64 && !hasRef;
    let getterTypeIdx;
    let returnMode;
    if (allF64) {
      getterTypeIdx = getterF64TypeIdx;
      returnMode = "f64";
    } else if (allI32) {
      getterTypeIdx = getterI32TypeIdx;
      returnMode = "i32";
    } else {
      getterTypeIdx = getterExternTypeIdx;
      returnMode = "extern";
    }
    const funcName = `__sget_${fieldName}`;
    const funcIdx = ctx.numImportFuncs + mod.functions.length;
    const anyLocal = 1;
    const funcBody = buildNestedIfElse(entries, anyLocal, boxNumIdx, returnMode);
    mod.functions.push({
      name: funcName,
      typeIdx: getterTypeIdx,
      locals: [{ name: "__any", type: { kind: "anyref" } }],
      body: funcBody,
      exported: true,
    });
    mod.exports.push({
      name: funcName,
      desc: { kind: "func", index: funcIdx },
    });
  }
  emitStructFieldNamesExport(ctx, fieldMap);
}
function emitStructFieldNamesExport(ctx, fieldMap) {
  const mod = ctx.mod;
  const structFieldNameMap = /* @__PURE__ */ new Map();
  for (const [structName, fields] of ctx.structFields) {
    const typeIdx = ctx.structMap.get(structName);
    if (typeIdx === void 0) continue;
    if (
      structName.startsWith("Wrapper") ||
      structName === "$AnyValue" ||
      structName.startsWith("__vec_") ||
      structName.startsWith("__arr_")
    )
      continue;
    const names = [];
    for (const field of fields) {
      if (!field || !field.type || !field.name) continue;
      if (field.name.startsWith("$") || field.name.startsWith("__")) continue;
      names.push(field.name);
    }
    if (names.length > 0) {
      structFieldNameMap.set(typeIdx, names);
    }
  }
  if (structFieldNameMap.size === 0) return;
  const typeIdxToGlobalIdx = /* @__PURE__ */ new Map();
  for (const [typeIdx, names] of structFieldNameMap) {
    const csv = names.join(",");
    addStringConstantGlobal(ctx, csv);
    const globalIdx = ctx.stringGlobalMap.get(csv);
    if (globalIdx !== void 0) {
      typeIdxToGlobalIdx.set(typeIdx, globalIdx);
    }
  }
  const getterExternTypeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }], "$sfnames_type");
  const anyLocal = 1;
  const body = [];
  body.push({ op: "local.get", index: 0 });
  body.push({ op: "any.convert_extern" });
  body.push({ op: "local.set", index: anyLocal });
  let fallback = [{ op: "ref.null.extern" }];
  const typeEntries = [...typeIdxToGlobalIdx.entries()];
  for (let i = typeEntries.length - 1; i >= 0; i--) {
    const [typeIdx, globalIdx] = typeEntries[i];
    const thenBranch = [{ op: "global.get", index: globalIdx }];
    const ifInstr = {
      op: "if",
      blockType: { kind: "val", type: { kind: "externref" } },
      then: thenBranch,
      else: fallback,
    };
    fallback = [{ op: "local.get", index: anyLocal }, { op: "ref.test", typeIdx }, ifInstr];
  }
  body.push(...fallback);
  const funcIdx = ctx.numImportFuncs + mod.functions.length;
  mod.functions.push({
    name: "__struct_field_names",
    typeIdx: getterExternTypeIdx,
    locals: [{ name: "__any", type: { kind: "anyref" } }],
    body,
    exported: true,
  });
  mod.exports.push({
    name: "__struct_field_names",
    desc: { kind: "func", index: funcIdx },
  });
}
function emitIteratorMethodExport(ctx) {
  if (!ctx.funcMap.has("__iterator") && !ctx.funcMap.has("__iterator_next")) return;
  const mod = ctx.mod;
  const dispatchTypeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }], "$call_method_type");
  const emitMethodDispatch = (methodSuffix, exportName) => {
    const entries = [];
    for (const [structName] of ctx.structFields) {
      const typeIdx = ctx.structMap.get(structName);
      if (typeIdx === void 0) continue;
      if (
        structName.startsWith("Wrapper") ||
        structName === "$AnyValue" ||
        structName.startsWith("__vec_") ||
        structName.startsWith("__arr_")
      )
        continue;
      const methodFullName = `${structName}_${methodSuffix}`;
      const funcIdx2 = ctx.funcMap.get(methodFullName);
      if (funcIdx2 === void 0) continue;
      const funcDef = mod.functions[funcIdx2 - ctx.numImportFuncs];
      const funcType = funcDef ? mod.types[funcDef.typeIdx] : void 0;
      const resultType =
        funcType && funcType.kind === "func" && funcType.results.length > 0
          ? funcType.results[0]
          : { kind: "externref" };
      entries.push({ structName, typeIdx, funcIdx: funcIdx2, resultType });
    }
    if (entries.length === 0) return;
    const funcIdx = ctx.numImportFuncs + mod.functions.length;
    const body = [];
    body.push({ op: "local.get", index: 0 });
    body.push({ op: "any.convert_extern" });
    body.push({ op: "local.set", index: 1 });
    let current = [{ op: "ref.null.extern" }];
    for (const entry of entries) {
      const testAndCall = [
        { op: "local.get", index: 1 },
        { op: "ref.cast", typeIdx: entry.typeIdx },
        { op: "call", funcIdx: entry.funcIdx },
      ];
      if (entry.resultType.kind === "ref" || entry.resultType.kind === "ref_null") {
        testAndCall.push({ op: "extern.convert_any" });
      } else if (entry.resultType.kind === "f64") {
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          testAndCall.push({ op: "call", funcIdx: boxIdx });
        }
      } else if (entry.resultType.kind === "i32") {
        testAndCall.push({ op: "f64.convert_i32_s" });
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          testAndCall.push({ op: "call", funcIdx: boxIdx });
        }
      }
      current = [
        { op: "local.get", index: 1 },
        { op: "ref.test", typeIdx: entry.typeIdx },
        {
          op: "if",
          blockType: { kind: "val", type: { kind: "externref" } },
          then: testAndCall,
          else: current,
        },
      ];
    }
    body.push(...current);
    mod.functions.push({
      name: exportName,
      typeIdx: dispatchTypeIdx,
      locals: [{ name: "__any", type: { kind: "anyref" } }],
      body,
      exported: true,
    });
    mod.exports.push({
      name: exportName,
      desc: { kind: "func", index: funcIdx },
    });
  };
  emitMethodDispatch("@@iterator", "__call_@@iterator");
  emitMethodDispatch("next", "__call_next");
}
function emitToPrimitiveMethodExports(ctx) {
  const mod = ctx.mod;
  const dispatchTypeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }], "$call_toPrim_type");
  const emitDispatchForMethod = (methodName, exportName) => {
    const entries = [];
    for (const [structName, fields] of ctx.structFields) {
      const typeIdx = ctx.structMap.get(structName);
      if (typeIdx === void 0) continue;
      if (
        structName.startsWith("Wrapper") ||
        structName === "$AnyValue" ||
        structName.startsWith("__vec_") ||
        structName.startsWith("__arr_")
      )
        continue;
      const methodFullName = `${structName}_${methodName}`;
      const funcIdx2 = ctx.funcMap.get(methodFullName);
      if (funcIdx2 !== void 0) {
        const funcDef = mod.functions[funcIdx2 - ctx.numImportFuncs];
        const funcType = funcDef ? mod.types[funcDef.typeIdx] : void 0;
        const resultType =
          funcType && funcType.kind === "func" && funcType.results.length > 0
            ? funcType.results[0]
            : { kind: "externref" };
        entries.push({ structName, typeIdx, mode: "standalone", funcIdx: funcIdx2, resultType });
        continue;
      }
      const fieldIdx = fields.findIndex((f) => f.name === methodName);
      if (fieldIdx < 0) continue;
      const field = fields[fieldIdx];
      if (field.type.kind === "ref" || field.type.kind === "ref_null") {
        const closureTypeIdx = field.type.typeIdx;
        const closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
        if (closureInfo && closureInfo.paramTypes.length === 0) {
          entries.push({ structName, typeIdx, mode: "closure", fieldIdx, closureTypeIdx, closureInfo });
          continue;
        }
      }
      if (field.type.kind === "eqref") {
        const trackedTypes = ctx.valueOfClosureTypes.get(structName) ?? [];
        for (const closureTypeIdx of trackedTypes) {
          const closureInfo = ctx.closureInfoByTypeIdx.get(closureTypeIdx);
          if (closureInfo && closureInfo.paramTypes.length === 0) {
            entries.push({ structName, typeIdx, mode: "closure", fieldIdx, closureTypeIdx, closureInfo });
            break;
          }
        }
      }
    }
    if (entries.length === 0) return;
    const funcIdx = ctx.numImportFuncs + mod.functions.length;
    const anyLocal = 1;
    const boxResult = (resultType, instrs) => {
      if (resultType.kind === "f64") {
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) instrs.push({ op: "call", funcIdx: boxIdx });
        else {
          instrs.push({ op: "drop" });
          instrs.push({ op: "ref.null.extern" });
        }
      } else if (resultType.kind === "i32") {
        instrs.push({ op: "f64.convert_i32_s" });
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) instrs.push({ op: "call", funcIdx: boxIdx });
        else {
          instrs.push({ op: "drop" });
          instrs.push({ op: "ref.null.extern" });
        }
      } else if (resultType.kind === "ref" || resultType.kind === "ref_null") {
        instrs.push({ op: "extern.convert_any" });
      }
    };
    const buildDispatch = (idx) => {
      if (idx >= entries.length) return [{ op: "ref.null.extern" }];
      const entry = entries[idx];
      const thenInstrs = [];
      if (entry.mode === "standalone") {
        thenInstrs.push(
          { op: "local.get", index: anyLocal },
          { op: "ref.cast", typeIdx: entry.typeIdx },
          { op: "call", funcIdx: entry.funcIdx },
        );
        boxResult(entry.resultType, thenInstrs);
      } else {
        const ci = entry.closureInfo;
        thenInstrs.push(
          { op: "local.get", index: anyLocal },
          { op: "ref.cast", typeIdx: entry.typeIdx },
          { op: "struct.get", typeIdx: entry.typeIdx, fieldIdx: entry.fieldIdx },
        );
        const closureLocal = 2;
        thenInstrs.push(
          { op: "local.set", index: closureLocal },
          // Cast eqref to closure struct type for the self-param
          { op: "local.get", index: closureLocal },
          { op: "ref.cast", typeIdx: entry.closureTypeIdx },
          // Get funcref from closure field 0
          { op: "local.get", index: closureLocal },
          { op: "ref.cast", typeIdx: entry.closureTypeIdx },
          { op: "struct.get", typeIdx: entry.closureTypeIdx, fieldIdx: 0 },
          { op: "ref.cast", typeIdx: ci.funcTypeIdx },
          { op: "call_ref", typeIdx: ci.funcTypeIdx },
        );
        const retType = ci.returnType ?? { kind: "externref" };
        if (!ci.returnType) {
          thenInstrs.push({ op: "ref.null.extern" });
        } else {
          boxResult(retType, thenInstrs);
        }
      }
      return [
        { op: "local.get", index: anyLocal },
        { op: "ref.test", typeIdx: entry.typeIdx },
        {
          op: "if",
          blockType: { kind: "val", type: { kind: "externref" } },
          then: thenInstrs,
          else: buildDispatch(idx + 1),
        },
      ];
    };
    const hasClosureEntry = entries.some((e) => e.mode === "closure");
    const locals = [{ name: "__any", type: { kind: "anyref" } }];
    if (hasClosureEntry) {
      locals.push({ name: "__closure", type: { kind: "eqref" } });
    }
    const body = [
      { op: "local.get", index: 0 },
      { op: "any.convert_extern" },
      { op: "local.set", index: anyLocal },
      ...buildDispatch(0),
    ];
    mod.functions.push({
      name: exportName,
      typeIdx: dispatchTypeIdx,
      locals,
      body,
      exported: true,
    });
    mod.exports.push({
      name: exportName,
      desc: { kind: "func", index: funcIdx },
    });
  };
  emitDispatchForMethod("toString", "__call_toString");
  emitDispatchForMethod("valueOf", "__call_valueOf");
}
function emitVecAccessExports(ctx) {
  if (!ctx.funcMap.has("__iterator") && !ctx.funcMap.has("JSON_stringify") && !ctx.funcMap.has("__make_iterable"))
    return;
  try {
    _emitVecAccessExportsInner(ctx);
  } catch {}
}
function _emitVecAccessExportsInner(ctx) {
  const mod = ctx.mod;
  const vecEntries = Array.from(ctx.vecTypeMap.entries());
  if (vecEntries.length === 0) return;
  addUnionImports(ctx);
  const lenTypeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }], "$__vec_len_type");
  const lenFuncIdx = ctx.numImportFuncs + mod.functions.length;
  {
    const body = [];
    body.push({ op: "local.get", index: 0 });
    body.push({ op: "any.convert_extern" });
    body.push({ op: "local.set", index: 1 });
    let current = [
      // Default: return 0 if no vec type matches
      { op: "i32.const", value: 0 },
      { op: "return" },
    ];
    for (let i = vecEntries.length - 1; i >= 0; i--) {
      const [, vecTypeIdx] = vecEntries[i];
      const thenBranch = [
        { op: "local.get", index: 1 },
        { op: "ref.cast", typeIdx: vecTypeIdx },
        { op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 },
        { op: "return" },
      ];
      current = [
        { op: "local.get", index: 1 },
        { op: "ref.test", typeIdx: vecTypeIdx },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: thenBranch,
          else: current,
        },
      ];
    }
    body.push(...current);
    mod.functions.push({
      name: "__vec_len",
      typeIdx: lenTypeIdx,
      locals: [{ name: "__any", type: { kind: "anyref" } }],
      body,
      exported: true,
    });
    mod.exports.push({
      name: "__vec_len",
      desc: { kind: "func", index: lenFuncIdx },
    });
  }
  const getTypeIdx = addFuncType(
    ctx,
    [{ kind: "externref" }, { kind: "i32" }],
    [{ kind: "externref" }],
    "$__vec_get_type",
  );
  const getFuncIdx = ctx.numImportFuncs + mod.functions.length;
  {
    const body = [];
    body.push({ op: "local.get", index: 0 });
    body.push({ op: "any.convert_extern" });
    body.push({ op: "local.set", index: 2 });
    let current = [
      // Default: return null if no vec type matches
      { op: "ref.null.extern" },
      { op: "return" },
    ];
    const boxNumIdx = ctx.funcMap.get("__box_number");
    for (let i = vecEntries.length - 1; i >= 0; i--) {
      const [elemKey, vecTypeIdx] = vecEntries[i];
      const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
      if (arrTypeIdx < 0) continue;
      if ((elemKey === "f64" || elemKey === "i32") && boxNumIdx === void 0) continue;
      let boxInstrs;
      if (elemKey === "externref") {
        boxInstrs = [];
      } else if (elemKey === "f64" && boxNumIdx !== void 0) {
        boxInstrs = [{ op: "call", funcIdx: boxNumIdx }];
      } else if (elemKey === "i32" && boxNumIdx !== void 0) {
        boxInstrs = [{ op: "f64.convert_i32_s" }, { op: "call", funcIdx: boxNumIdx }];
      } else if (elemKey === "i64") {
        if (boxNumIdx !== void 0) {
          boxInstrs = [{ op: "f64.convert_i64_s" }, { op: "call", funcIdx: boxNumIdx }];
        } else {
          boxInstrs = [{ op: "drop" }, { op: "ref.null.extern" }];
        }
      } else {
        boxInstrs = [{ op: "extern.convert_any" }];
      }
      const thenBranch = [
        // ref.cast to vec type, struct.get data array, then array.get with index
        { op: "local.get", index: 2 },
        { op: "ref.cast", typeIdx: vecTypeIdx },
        { op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 },
        { op: "local.get", index: 1 },
        // index
        { op: "array.get", typeIdx: arrTypeIdx },
        ...boxInstrs,
        { op: "return" },
      ];
      current = [
        { op: "local.get", index: 2 },
        { op: "ref.test", typeIdx: vecTypeIdx },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: thenBranch,
          else: current,
        },
      ];
    }
    body.push(...current);
    mod.functions.push({
      name: "__vec_get",
      typeIdx: getTypeIdx,
      locals: [{ name: "__any", type: { kind: "anyref" } }],
      body,
      exported: true,
    });
    mod.exports.push({
      name: "__vec_get",
      desc: { kind: "func", index: getFuncIdx },
    });
  }
}
function buildNestedIfElse(entries, anyLocal, boxNumIdx, returnMode = "extern") {
  const body = [];
  body.push({ op: "local.get", index: 0 });
  body.push({ op: "any.convert_extern" });
  body.push({ op: "local.set", index: anyLocal });
  let defaultVal;
  let blockRetType;
  if (returnMode === "f64") {
    defaultVal = { op: "f64.const", value: 0 };
    blockRetType = { kind: "f64" };
  } else if (returnMode === "i32") {
    defaultVal = { op: "i32.const", value: 0 };
    blockRetType = { kind: "i32" };
  } else {
    defaultVal = { op: "ref.null.extern" };
    blockRetType = { kind: "externref" };
  }
  let current = [defaultVal];
  for (let i = entries.length - 1; i >= 0; i--) {
    const entry = entries[i];
    const thenBranch = buildGetterExtract(entry, anyLocal, boxNumIdx, returnMode);
    const ifInstr = {
      op: "if",
      blockType: { kind: "val", type: blockRetType },
      then: thenBranch,
      else: current,
    };
    current = [{ op: "local.get", index: anyLocal }, { op: "ref.test", typeIdx: entry.typeIdx }, ifInstr];
  }
  body.push(...current);
  return body;
}
function buildGetterExtract(entry, anyLocal, boxNumIdx, returnMode = "extern") {
  const then = [];
  then.push({ op: "local.get", index: anyLocal });
  then.push({ op: "ref.cast", typeIdx: entry.typeIdx });
  then.push({ op: "struct.get", typeIdx: entry.typeIdx, fieldIdx: entry.fieldIdx });
  const ft = entry.fieldType;
  if (returnMode === "f64") {
    if (ft.kind === "f64") {
    } else if (ft.kind === "i32") {
      then.push({ op: "f64.convert_i32_s" });
    } else {
      then.push({ op: "drop" });
      then.push({ op: "f64.const", value: 0 });
    }
  } else if (returnMode === "i32") {
    if (ft.kind === "i32") {
    } else if (ft.kind === "f64") {
      then.push({ op: "i32.trunc_sat_f64_s" });
    } else {
      then.push({ op: "drop" });
      then.push({ op: "i32.const", value: 0 });
    }
  } else {
    if (ft.kind === "f64") {
      if (boxNumIdx !== void 0) {
        then.push({ op: "call", funcIdx: boxNumIdx });
      } else {
        then.push({ op: "drop" });
        then.push({ op: "ref.null.extern" });
      }
    } else if (ft.kind === "i32") {
      then.push({ op: "f64.convert_i32_s" });
      if (boxNumIdx !== void 0) {
        then.push({ op: "call", funcIdx: boxNumIdx });
      } else {
        then.push({ op: "drop" });
        then.push({ op: "ref.null.extern" });
      }
    } else if (ft.kind === "i64") {
      then.push({ op: "drop" });
      then.push({ op: "ref.null.extern" });
    } else if (ft.kind === "externref" || ft.kind === "ref_extern") {
    } else if (ft.kind === "ref" || ft.kind === "ref_null" || ft.kind === "anyref" || ft.kind === "eqref") {
      then.push({ op: "extern.convert_any" });
    } else {
      then.push({ op: "drop" });
      then.push({ op: "ref.null.extern" });
    }
  }
  return then;
}
var CONSOLE_METHODS_SET = /* @__PURE__ */ new Set(["log", "warn", "error"]);
function createUnifiedCollectorState(sourceFile) {
  return {
    consoleNeededByMethod: /* @__PURE__ */ new Map(),
    primitiveNeeded: /* @__PURE__ */ new Set(),
    stringLiterals: /* @__PURE__ */ new Set(),
    hasTypeofExprForStrings: false,
    hasTaggedTemplate: false,
    insideComputedPropertyName: 0,
    stringMethodNeeded: /* @__PURE__ */ new Set(),
    stringRegexpMethodNeeded: /* @__PURE__ */ new Set(),
    mathNeeded: /* @__PURE__ */ new Set(),
    mathNeedsToUint32: false,
    parseNeeded: /* @__PURE__ */ new Set(),
    uriNeeded: /* @__PURE__ */ new Set(),
    needsFromCharCode: false,
    promiseNeeded: /* @__PURE__ */ new Set(),
    promiseNeedConstructor: false,
    jsonNeedStringify: false,
    jsonNeedParse: false,
    callbackFound: false,
    funcArrayNeed1: false,
    funcArrayNeed2: false,
    unionFound: false,
    generatorFound: false,
    iteratorFound: false,
    arrayIteratorFound: false,
    forInFound: false,
    forInLiterals: /* @__PURE__ */ new Set(),
    inExprLiterals: /* @__PURE__ */ new Set(),
    objectMethodLiterals: /* @__PURE__ */ new Set(),
    objectMethodHasValues: false,
    wrapperFound: false,
    unknownCtorNeeded: /* @__PURE__ */ new Map(),
    sourceFile,
  };
}
function unifiedVisitNode(ctx, state, node) {
  if (state.insideComputedPropertyName === 0) {
    if (ts15.isStringLiteral(node)) {
      state.stringLiterals.add(node.text);
    }
    if (ts15.isNoSubstitutionTemplateLiteral(node)) {
      state.stringLiterals.add(node.text);
    }
    if (ts15.isTemplateExpression(node)) {
      state.stringLiterals.add(node.head.text);
      for (const span of node.templateSpans) {
        state.stringLiterals.add(span.literal.text);
      }
    }
    if (ts15.isTaggedTemplateExpression(node)) {
      state.hasTaggedTemplate = true;
      if (ts15.isNoSubstitutionTemplateLiteral(node.template)) {
        state.stringLiterals.add(node.template.text);
        const rawText = node.template.rawText;
        if (rawText !== void 0) state.stringLiterals.add(rawText);
      } else if (ts15.isTemplateExpression(node.template)) {
        state.stringLiterals.add(node.template.head.text);
        const headRaw = node.template.head.rawText;
        if (headRaw !== void 0) state.stringLiterals.add(headRaw);
        for (const span of node.template.templateSpans) {
          state.stringLiterals.add(span.literal.text);
          const spanRaw = span.literal.rawText;
          if (spanRaw !== void 0) state.stringLiterals.add(spanRaw);
        }
      }
    }
    if (node.kind === ts15.SyntaxKind.RegularExpressionLiteral) {
      const { pattern, flags } = parseRegExpLiteral(node.getText());
      state.stringLiterals.add(pattern);
      if (flags) state.stringLiterals.add(flags);
    }
    if (ts15.isTypeOfExpression(node)) {
      state.hasTypeofExprForStrings = true;
    }
    if (ts15.isMetaProperty(node) && node.keywordToken === ts15.SyntaxKind.ImportKeyword && node.name.text === "meta") {
      state.stringLiterals.add("module.wasm");
      state.stringLiterals.add("[object Object]");
    }
  }
  if (
    ts15.isCallExpression(node) &&
    ts15.isPropertyAccessExpression(node.expression) &&
    ts15.isIdentifier(node.expression.expression) &&
    node.expression.expression.text === "console"
  ) {
    const method = node.expression.name.text;
    if (CONSOLE_METHODS_SET.has(method)) {
      if (!state.consoleNeededByMethod.has(method)) state.consoleNeededByMethod.set(method, /* @__PURE__ */ new Set());
      const needed = state.consoleNeededByMethod.get(method);
      for (const arg of node.arguments) {
        const argType = ctx.checker.getTypeAtLocation(arg);
        if (isStringType(argType)) {
          needed.add("string");
        } else if (isBooleanType(argType)) {
          needed.add("bool");
        } else if (isNumberType(argType)) {
          needed.add("number");
        } else {
          needed.add("externref");
        }
      }
    }
  }
  if (ts15.isCallExpression(node) && ts15.isPropertyAccessExpression(node.expression)) {
    const prop = node.expression;
    const receiverType = ctx.checker.getTypeAtLocation(prop.expression);
    const methodName = prop.name.text;
    if (isNumberType(receiverType) && methodName === "toString") {
      state.primitiveNeeded.add("number_toString");
    }
    if (isNumberType(receiverType) && methodName === "toFixed") {
      state.primitiveNeeded.add("number_toFixed");
    }
    if (isNumberType(receiverType) && methodName === "toPrecision") {
      state.primitiveNeeded.add("number_toPrecision");
    }
    if (isNumberType(receiverType) && methodName === "toExponential") {
      state.primitiveNeeded.add("number_toExponential");
    }
    if (isStringType(receiverType) && Object.prototype.hasOwnProperty.call(STRING_METHODS, methodName)) {
      state.stringMethodNeeded.add(methodName);
      if (
        (methodName === "replace" ||
          methodName === "replaceAll" ||
          methodName === "split" ||
          methodName === "match" ||
          methodName === "search") &&
        ts15.isCallExpression(node) &&
        node.arguments.length > 0
      ) {
        const argType = ctx.checker.getTypeAtLocation(node.arguments[0]);
        const symName = argType.getSymbol()?.getName();
        if (symName === "RegExp") {
          state.stringRegexpMethodNeeded.add(methodName);
        }
      }
    }
  }
  if (ts15.isTemplateExpression(node)) {
    for (const span of node.templateSpans) {
      const spanType = ctx.checker.getTypeAtLocation(span.expression);
      if (isNumberType(spanType) || isBooleanType(spanType) || isBigIntType(spanType)) {
        state.primitiveNeeded.add("number_toString");
      }
    }
  }
  if (
    ts15.isCallExpression(node) &&
    ts15.isIdentifier(node.expression) &&
    node.expression.text === "String" &&
    node.arguments.length >= 1
  ) {
    const argType = ctx.checker.getTypeAtLocation(node.arguments[0]);
    if (isNumberType(argType) || !isStringType(argType)) {
      state.primitiveNeeded.add("number_toString");
    }
  }
  if (
    ts15.isBinaryExpression(node) &&
    (node.operatorToken.kind === ts15.SyntaxKind.PlusToken ||
      node.operatorToken.kind === ts15.SyntaxKind.PlusEqualsToken)
  ) {
    const leftType = ctx.checker.getTypeAtLocation(node.left);
    const rightType = ctx.checker.getTypeAtLocation(node.right);
    if (isStringType(leftType) && !isStringType(rightType)) {
      state.primitiveNeeded.add("number_toString");
    }
    if (!isStringType(leftType) && isStringType(rightType)) {
      state.primitiveNeeded.add("number_toString");
    }
    if (
      node.operatorToken.kind === ts15.SyntaxKind.PlusEqualsToken &&
      (leftType.flags & ts15.TypeFlags.Any) !== 0 &&
      !isStringType(rightType)
    ) {
      state.primitiveNeeded.add("number_toString");
    }
  }
  if (
    ts15.isBinaryExpression(node) &&
    (node.operatorToken.kind === ts15.SyntaxKind.LessThanToken ||
      node.operatorToken.kind === ts15.SyntaxKind.LessThanEqualsToken ||
      node.operatorToken.kind === ts15.SyntaxKind.GreaterThanToken ||
      node.operatorToken.kind === ts15.SyntaxKind.GreaterThanEqualsToken)
  ) {
    const leftType = ctx.checker.getTypeAtLocation(node.left);
    if (isStringType(leftType)) {
      state.primitiveNeeded.add("string_compare");
    }
  }
  if (
    ts15.isCallExpression(node) &&
    ts15.isPropertyAccessExpression(node.expression) &&
    ts15.isIdentifier(node.expression.expression) &&
    node.expression.expression.text === "Math"
  ) {
    const method = node.expression.name.text;
    if (MATH_HOST_METHODS_1ARG.has(method) || MATH_HOST_METHODS_2ARG.has(method) || method === "random") {
      state.mathNeeded.add(method);
    }
    if (method === "clz32" || method === "imul") {
      state.mathNeedsToUint32 = true;
    }
  }
  if (
    ts15.isBinaryExpression(node) &&
    (node.operatorToken.kind === ts15.SyntaxKind.AsteriskAsteriskToken ||
      node.operatorToken.kind === ts15.SyntaxKind.AsteriskAsteriskEqualsToken)
  ) {
    state.mathNeeded.add("pow");
  }
  if (ts15.isCallExpression(node) && ts15.isIdentifier(node.expression)) {
    const name = node.expression.text;
    if (name === "parseInt" || name === "parseFloat") {
      state.parseNeeded.add(name);
    }
    if (
      name === "decodeURI" ||
      name === "decodeURIComponent" ||
      name === "encodeURI" ||
      name === "encodeURIComponent"
    ) {
      state.uriNeeded.add(name);
    }
    if (name === "Number") {
      state.parseNeeded.add("parseFloat");
    }
  }
  if (
    ts15.isPrefixUnaryExpression(node) &&
    node.operator === ts15.SyntaxKind.PlusToken &&
    !ts15.isStringLiteral(node.operand) &&
    !ts15.isNoSubstitutionTemplateLiteral(node.operand)
  ) {
    const operandType = ctx.checker.getTypeAtLocation(node.operand);
    if (operandType.flags & ts15.TypeFlags.StringLike) {
      state.parseNeeded.add("parseFloat");
    }
  }
  if (
    ts15.isBinaryExpression(node) &&
    (node.operatorToken.kind === ts15.SyntaxKind.EqualsEqualsToken ||
      node.operatorToken.kind === ts15.SyntaxKind.ExclamationEqualsToken)
  ) {
    try {
      const leftType = ctx.checker.getTypeAtLocation(node.left);
      const rightType = ctx.checker.getTypeAtLocation(node.right);
      const leftIsStr = isStringType(leftType);
      const rightIsStr = isStringType(rightType);
      const leftIsNumOrBool = isNumberType(leftType) || isBooleanType(leftType);
      const rightIsNumOrBool = isNumberType(rightType) || isBooleanType(rightType);
      if ((leftIsStr && rightIsNumOrBool) || (rightIsStr && leftIsNumOrBool)) {
        state.parseNeeded.add("parseFloat");
      }
    } catch {}
  }
  if (ts15.isBinaryExpression(node)) {
    const opKind = node.operatorToken.kind;
    const isArithOrBitwise =
      opKind === ts15.SyntaxKind.MinusToken ||
      opKind === ts15.SyntaxKind.AsteriskToken ||
      opKind === ts15.SyntaxKind.AsteriskAsteriskToken ||
      opKind === ts15.SyntaxKind.SlashToken ||
      opKind === ts15.SyntaxKind.PercentToken ||
      opKind === ts15.SyntaxKind.AmpersandToken ||
      opKind === ts15.SyntaxKind.BarToken ||
      opKind === ts15.SyntaxKind.CaretToken ||
      opKind === ts15.SyntaxKind.LessThanLessThanToken ||
      opKind === ts15.SyntaxKind.GreaterThanGreaterThanToken ||
      opKind === ts15.SyntaxKind.GreaterThanGreaterThanGreaterThanToken;
    if (isArithOrBitwise) {
      try {
        const leftType = ctx.checker.getTypeAtLocation(node.left);
        const rightType = ctx.checker.getTypeAtLocation(node.right);
        if (isStringType(leftType) || isStringType(rightType)) {
          state.parseNeeded.add("parseFloat");
        }
      } catch {}
    }
  }
  if (
    ts15.isCallExpression(node) &&
    ts15.isPropertyAccessExpression(node.expression) &&
    ts15.isIdentifier(node.expression.expression) &&
    node.expression.expression.text === "String" &&
    node.expression.name.text === "fromCharCode"
  ) {
    state.needsFromCharCode = true;
  }
  if (
    ts15.isCallExpression(node) &&
    ts15.isPropertyAccessExpression(node.expression) &&
    ts15.isIdentifier(node.expression.expression) &&
    node.expression.expression.text === "Promise"
  ) {
    const method = node.expression.name.text;
    if (method === "all" || method === "race" || method === "resolve" || method === "reject") {
      state.promiseNeeded.add(method);
    }
  }
  if (ts15.isNewExpression(node) && ts15.isIdentifier(node.expression) && node.expression.text === "Promise") {
    state.promiseNeedConstructor = true;
  }
  if (
    ts15.isCallExpression(node) &&
    ts15.isPropertyAccessExpression(node.expression) &&
    ts15.isIdentifier(node.expression.expression) &&
    node.expression.expression.text === "JSON"
  ) {
    const method = node.expression.name.text;
    if (method === "stringify") state.jsonNeedStringify = true;
    if (method === "parse") state.jsonNeedParse = true;
  }
  if (!state.callbackFound) {
    if (ts15.isArrowFunction(node) || ts15.isFunctionExpression(node)) {
      state.callbackFound = true;
    }
  }
  if (ts15.isCallExpression(node) && ts15.isPropertyAccessExpression(node.expression)) {
    const method = node.expression.name.text;
    if (FUNCTIONAL_ARRAY_METHODS.has(method)) {
      if (method === "reduce") {
        state.funcArrayNeed2 = true;
      } else {
        state.funcArrayNeed1 = true;
      }
    }
    if (method === "call" && ts15.isPropertyAccessExpression(node.expression.expression)) {
      const innerMethod = node.expression.expression.name.text;
      if (FUNCTIONAL_ARRAY_METHODS.has(innerMethod)) {
        if (innerMethod === "reduce") {
          state.funcArrayNeed2 = true;
        } else {
          state.funcArrayNeed1 = true;
        }
      }
    }
  }
  if (!state.unionFound) {
    if (ts15.isFunctionDeclaration(node) && node.parameters) {
      for (const param of node.parameters) {
        const paramType = ctx.checker.getTypeAtLocation(param);
        if (isHeterogeneousUnion(paramType, ctx.checker)) {
          state.unionFound = true;
          break;
        }
      }
    }
    if (!state.unionFound && ts15.isVariableDeclaration(node) && node.type) {
      const varType = ctx.checker.getTypeAtLocation(node);
      if (isHeterogeneousUnion(varType, ctx.checker)) {
        state.unionFound = true;
      }
    }
    if (!state.unionFound && ts15.isTypeOfExpression(node)) {
      state.unionFound = true;
    }
    if (
      !state.unionFound &&
      ts15.isFunctionDeclaration(node) &&
      node.asteriskToken &&
      node.body &&
      !hasDeclareModifier(node)
    ) {
      state.unionFound = true;
    }
    if (!state.unionFound && ts15.isFunctionExpression(node) && node.asteriskToken) {
      state.unionFound = true;
    }
    if (!state.unionFound && ts15.isMethodDeclaration(node) && node.asteriskToken && node.body) {
      state.unionFound = true;
    }
    if (!state.unionFound && ts15.isForOfStatement(node)) {
      const exprType = ctx.checker.getTypeAtLocation(node.expression);
      const sym = exprType.symbol ?? exprType.symbol;
      if (sym?.name !== "Array") {
        state.unionFound = true;
      }
    }
  }
  if (!state.generatorFound) {
    if (ts15.isFunctionDeclaration(node) && node.asteriskToken && node.body && !hasDeclareModifier(node)) {
      state.generatorFound = true;
    }
    if (!state.generatorFound && ts15.isFunctionExpression(node) && node.asteriskToken) {
      state.generatorFound = true;
    }
    if (!state.generatorFound && ts15.isMethodDeclaration(node) && node.asteriskToken && node.body) {
      state.generatorFound = true;
    }
  }
  if (!state.arrayIteratorFound && ts15.isCallExpression(node) && ts15.isPropertyAccessExpression(node.expression)) {
    const methodName = node.expression.name.text;
    if (methodName === "entries" || methodName === "keys" || methodName === "values") {
      const recvType = ctx.checker.getTypeAtLocation(node.expression.expression);
      const sym = recvType.symbol ?? recvType.symbol;
      if (sym?.name === "Array") {
        state.arrayIteratorFound = true;
      }
    }
  }
  if (!state.iteratorFound && ts15.isForOfStatement(node)) {
    const exprType = ctx.checker.getTypeAtLocation(node.expression);
    const sym = exprType.symbol ?? exprType.symbol;
    if (sym?.name !== "Array") {
      if (ctx.nativeStrings && ctx.anyStrTypeIdx >= 0 && isStringType(exprType)) {
      } else {
        state.iteratorFound = true;
      }
    }
  }
  if (ts15.isForInStatement(node)) {
    state.forInFound = true;
    const exprType = ctx.checker.getTypeAtLocation(node.expression);
    const props = exprType.getProperties();
    for (const prop of props) {
      if (!ctx.stringGlobalMap.has(prop.name)) state.forInLiterals.add(prop.name);
    }
  }
  if (ts15.isBinaryExpression(node) && node.operatorToken.kind === ts15.SyntaxKind.InKeyword) {
    if (!ts15.isStringLiteral(node.left) && !ts15.isNumericLiteral(node.left)) {
      const rightType = ctx.checker.getTypeAtLocation(node.right);
      const props = rightType.getProperties();
      for (const prop of props) {
        if (!ctx.stringGlobalMap.has(prop.name)) state.inExprLiterals.add(prop.name);
      }
    }
  }
  if (
    ts15.isCallExpression(node) &&
    ts15.isPropertyAccessExpression(node.expression) &&
    ts15.isIdentifier(node.expression.expression) &&
    node.expression.expression.text === "Object" &&
    (node.expression.name.text === "keys" ||
      node.expression.name.text === "values" ||
      node.expression.name.text === "entries") &&
    node.arguments.length === 1
  ) {
    if (node.expression.name.text === "values" || node.expression.name.text === "entries")
      state.objectMethodHasValues = true;
    const argType = ctx.checker.getTypeAtLocation(node.arguments[0]);
    const props = argType.getProperties();
    for (const prop of props) {
      if (!ctx.stringLiteralMap.has(prop.name)) state.objectMethodLiterals.add(prop.name);
    }
  }
  if (!state.wrapperFound && ts15.isNewExpression(node) && ts15.isIdentifier(node.expression)) {
    const name = node.expression.text;
    if (name === "Number" || name === "String" || name === "Boolean") {
      state.wrapperFound = true;
    }
  }
  if (ts15.isNewExpression(node) && ts15.isIdentifier(node.expression)) {
    const name = node.expression.text;
    if (!KNOWN_CONSTRUCTORS.has(name)) {
      const sym = ctx.checker.getSymbolAtLocation(node.expression);
      const decls = sym?.getDeclarations() ?? [];
      const isLocalClass = decls.some((d) => {
        if (ts15.isClassDeclaration(d) || ts15.isClassExpression(d)) return d.getSourceFile() === state.sourceFile;
        if (ts15.isVariableDeclaration(d) && d.initializer && ts15.isClassExpression(d.initializer))
          return d.getSourceFile() === state.sourceFile;
        return false;
      });
      const isExtern = ctx.externClasses.has(name);
      if (!isLocalClass && !isExtern) {
        const argCount = node.arguments?.length ?? 0;
        const prev = state.unknownCtorNeeded.get(name) ?? 0;
        state.unknownCtorNeeded.set(name, Math.max(prev, argCount));
      }
    }
  }
  if (ts15.isFunctionDeclaration(node) && node.name) {
    state.stringLiterals.add(node.name.text);
  }
  if (ts15.isFunctionExpression(node) && node.name) {
    state.stringLiterals.add(node.name.text);
  }
  if (ts15.isClassDeclaration(node) && node.name) {
    state.stringLiterals.add(node.name.text);
  }
  if (ts15.isClassExpression(node) && node.name) {
    state.stringLiterals.add(node.name.text);
  }
  if (ts15.isVariableDeclaration(node) && ts15.isIdentifier(node.name) && node.initializer) {
    if (ts15.isFunctionExpression(node.initializer) && !node.initializer.name) {
      state.stringLiterals.add(node.name.text);
    }
    if (ts15.isArrowFunction(node.initializer)) {
      state.stringLiterals.add(node.name.text);
    }
    if (ts15.isClassExpression(node.initializer) && !node.initializer.name) {
      state.stringLiterals.add(node.name.text);
    }
  }
  if (ts15.isMethodDeclaration(node) && node.name && ts15.isIdentifier(node.name)) {
    state.stringLiterals.add(node.name.text);
  }
  if (ts15.isGetAccessorDeclaration(node) && node.name && ts15.isIdentifier(node.name)) {
    state.stringLiterals.add(`get ${node.name.text}`);
  }
  if (ts15.isSetAccessorDeclaration(node) && node.name && ts15.isIdentifier(node.name)) {
    state.stringLiterals.add(`set ${node.name.text}`);
  }
  if (ts15.isComputedPropertyName(node)) {
    state.insideComputedPropertyName++;
    ts15.forEachChild(node, (child) => unifiedVisitNode(ctx, state, child));
    state.insideComputedPropertyName--;
    return;
  }
  ts15.forEachChild(node, (child) => unifiedVisitNode(ctx, state, child));
}
function finalizeUnifiedCollector(ctx, state) {
  const CONSOLE_METHODS = ["log", "warn", "error"];
  for (const method of CONSOLE_METHODS) {
    const needed = state.consoleNeededByMethod.get(method);
    if (!needed) continue;
    if (needed.has("number")) {
      const t = addFuncType(ctx, [{ kind: "f64" }], []);
      addImport(ctx, "env", `console_${method}_number`, { kind: "func", typeIdx: t });
    }
    if (needed.has("bool")) {
      const t = addFuncType(ctx, [{ kind: "i32" }], []);
      addImport(ctx, "env", `console_${method}_bool`, { kind: "func", typeIdx: t });
    }
    if (needed.has("string")) {
      const t = addFuncType(ctx, [{ kind: "externref" }], []);
      addImport(ctx, "env", `console_${method}_string`, { kind: "func", typeIdx: t });
    }
    if (needed.has("externref")) {
      const t = addFuncType(ctx, [{ kind: "externref" }], []);
      addImport(ctx, "env", `console_${method}_externref`, { kind: "func", typeIdx: t });
    }
  }
  if (state.primitiveNeeded.has("number_toString")) {
    const t = addFuncType(ctx, [{ kind: "f64" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "number_toString", { kind: "func", typeIdx: t });
  }
  if (state.primitiveNeeded.has("number_toFixed")) {
    const t = addFuncType(ctx, [{ kind: "f64" }, { kind: "f64" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "number_toFixed", { kind: "func", typeIdx: t });
  }
  if (state.primitiveNeeded.has("number_toPrecision")) {
    const t = addFuncType(ctx, [{ kind: "f64" }, { kind: "f64" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "number_toPrecision", { kind: "func", typeIdx: t });
  }
  if (state.primitiveNeeded.has("number_toExponential")) {
    const t = addFuncType(ctx, [{ kind: "f64" }, { kind: "f64" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "number_toExponential", { kind: "func", typeIdx: t });
  }
  if (state.primitiveNeeded.has("string_compare")) {
    const t = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "i32" }]);
    addImport(ctx, "env", "string_compare", { kind: "func", typeIdx: t });
  }
  if (state.stringLiterals.size > 0) {
    state.stringLiterals.add("");
  }
  if (state.hasTypeofExprForStrings) {
    for (const s of ["number", "string", "boolean", "object", "undefined", "function", "symbol"]) {
      state.stringLiterals.add(s);
    }
  }
  if (state.hasTaggedTemplate) {
    getOrRegisterTemplateVecType(ctx);
  }
  if (state.stringLiterals.size > 0) {
    if (ctx.nativeStrings) {
      ensureNativeStringHelpers(ctx);
      for (const value of state.stringLiterals) {
        if (!ctx.stringGlobalMap.has(value)) {
          ctx.stringGlobalMap.set(value, -1);
        }
      }
    } else {
      addStringImports(ctx);
      for (const value of state.stringLiterals) {
        addStringConstantGlobal(ctx, value);
      }
    }
  }
  {
    const NATIVE_STR_METHODS = /* @__PURE__ */ new Set([
      "charAt",
      "substring",
      "slice",
      "at",
      "indexOf",
      "lastIndexOf",
      "includes",
      "startsWith",
      "endsWith",
      "trim",
      "trimStart",
      "trimEnd",
      "repeat",
      "padStart",
      "padEnd",
      "toLowerCase",
      "toUpperCase",
      "replace",
      "replaceAll",
      "split",
    ]);
    for (const method of state.stringMethodNeeded) {
      if (ctx.nativeStrings && NATIVE_STR_METHODS.has(method) && !state.stringRegexpMethodNeeded.has(method)) {
        ensureNativeStringHelpers(ctx);
        continue;
      }
      if (ctx.nativeStrings && NATIVE_STR_METHODS.has(method) && state.stringRegexpMethodNeeded.has(method)) {
        ensureNativeStringHelpers(ctx);
      }
      const sig = STRING_METHODS[method];
      const params = [{ kind: "externref" }, ...sig.params];
      const t = addFuncType(ctx, params, [sig.result]);
      addImport(ctx, "env", `string_${method}`, { kind: "func", typeIdx: t });
    }
    if ((state.stringMethodNeeded.has("split") || state.stringMethodNeeded.has("match")) && !ctx.nativeStrings) {
      if (!ctx.funcMap.has("__extern_get")) {
        const getType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
        addImport(ctx, "env", "__extern_get", { kind: "func", typeIdx: getType });
      }
      if (!ctx.funcMap.has("__extern_length")) {
        const lenType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "f64" }]);
        addImport(ctx, "env", "__extern_length", { kind: "func", typeIdx: lenType });
      }
    }
  }
  for (const method of state.mathNeeded) {
    if (method === "random") {
      const typeIdx = addFuncType(ctx, [], [{ kind: "f64" }]);
      addImport(ctx, "env", `Math_${method}`, { kind: "func", typeIdx });
    } else {
      ctx.pendingMathMethods.add(method);
    }
  }
  if (state.mathNeedsToUint32 && !ctx.funcMap.has("__toUint32")) {
    const typeIdx = addFuncType(ctx, [{ kind: "f64" }], [{ kind: "i32" }]);
    addImport(ctx, "env", "__toUint32", { kind: "func", typeIdx });
  }
  for (const name of state.parseNeeded) {
    if (name === "parseInt") {
      const typeIdx = addFuncType(ctx, [{ kind: "externref" }, { kind: "f64" }], [{ kind: "f64" }]);
      addImport(ctx, "env", name, { kind: "func", typeIdx });
    } else {
      const typeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "f64" }]);
      addImport(ctx, "env", name, { kind: "func", typeIdx });
    }
  }
  for (const name of state.uriNeeded) {
    const typeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", name, { kind: "func", typeIdx });
  }
  if (state.needsFromCharCode) {
    const typeIdx = addFuncType(ctx, [{ kind: "f64" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "String_fromCharCode", { kind: "func", typeIdx });
    if (ctx.nativeStrings) {
      ensureNativeStringHelpers(ctx);
    }
  }
  for (const method of state.promiseNeeded) {
    const importName = `Promise_${method}`;
    if (!ctx.funcMap.has(importName)) {
      const typeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
      addImport(ctx, "env", importName, { kind: "func", typeIdx });
    }
  }
  if (state.promiseNeedConstructor && !ctx.funcMap.has("Promise_new")) {
    const typeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "Promise_new", { kind: "func", typeIdx });
  }
  if (state.jsonNeedStringify || state.jsonNeedParse) {
    addUnionImports(ctx);
  }
  if (state.jsonNeedStringify) {
    const typeIdx = addFuncType(
      ctx,
      [{ kind: "externref" }, { kind: "externref" }, { kind: "externref" }],
      [{ kind: "externref" }],
    );
    addImport(ctx, "env", "JSON_stringify", { kind: "func", typeIdx });
  }
  if (state.jsonNeedParse) {
    const typeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "JSON_parse", { kind: "func", typeIdx });
  }
  if (state.callbackFound) {
    const typeIdx = addFuncType(ctx, [{ kind: "i32" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__make_callback", { kind: "func", typeIdx });
  }
  if (state.funcArrayNeed1) {
    if (ctx.fast) {
      const typeIdx = addFuncType(ctx, [{ kind: "externref" }, { kind: "i32" }], [{ kind: "i32" }]);
      addImport(ctx, "env", "__call_1_i32", { kind: "func", typeIdx });
    } else {
      const typeIdx = addFuncType(ctx, [{ kind: "externref" }, { kind: "f64" }], [{ kind: "f64" }]);
      addImport(ctx, "env", "__call_1_f64", { kind: "func", typeIdx });
    }
  }
  if (state.funcArrayNeed2) {
    if (ctx.fast) {
      const typeIdx = addFuncType(ctx, [{ kind: "externref" }, { kind: "i32" }, { kind: "i32" }], [{ kind: "i32" }]);
      addImport(ctx, "env", "__call_2_i32", { kind: "func", typeIdx });
    } else {
      const typeIdx = addFuncType(ctx, [{ kind: "externref" }, { kind: "f64" }, { kind: "f64" }], [{ kind: "f64" }]);
      addImport(ctx, "env", "__call_2_f64", { kind: "func", typeIdx });
    }
  }
  if (state.unionFound) {
    addUnionImports(ctx);
  }
  if (state.generatorFound && !ctx.funcMap.has("__gen_create_buffer")) {
    const bufType = addFuncType(ctx, [], [{ kind: "externref" }]);
    addImport(ctx, "env", "__gen_create_buffer", { kind: "func", typeIdx: bufType });
    const pushF64Type = addFuncType(ctx, [{ kind: "externref" }, { kind: "f64" }], []);
    addImport(ctx, "env", "__gen_push_f64", { kind: "func", typeIdx: pushF64Type });
    const pushI32Type = addFuncType(ctx, [{ kind: "externref" }, { kind: "i32" }], []);
    addImport(ctx, "env", "__gen_push_i32", { kind: "func", typeIdx: pushI32Type });
    const pushRefType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], []);
    addImport(ctx, "env", "__gen_push_ref", { kind: "func", typeIdx: pushRefType });
    const genType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__create_generator", { kind: "func", typeIdx: genType });
    addImport(ctx, "env", "__create_async_generator", { kind: "func", typeIdx: genType });
    addImport(ctx, "env", "__gen_next", { kind: "func", typeIdx: genType });
    const genReturnType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__gen_return", { kind: "func", typeIdx: genReturnType });
    addImport(ctx, "env", "__gen_throw", { kind: "func", typeIdx: genReturnType });
    addImport(ctx, "env", "__gen_result_value", { kind: "func", typeIdx: genType });
    const resultValF64Type = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "f64" }]);
    addImport(ctx, "env", "__gen_result_value_f64", { kind: "func", typeIdx: resultValF64Type });
    const resultDoneType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }]);
    addImport(ctx, "env", "__gen_result_done", { kind: "func", typeIdx: resultDoneType });
  }
  if (state.iteratorFound) {
    addIteratorImports(ctx);
  }
  if (state.arrayIteratorFound) {
    addArrayIteratorImports(ctx);
    if (!state.iteratorFound) {
      addIteratorImports(ctx);
    }
  }
  if (state.forInFound) {
    addForInImports(ctx);
  }
  if (state.forInLiterals.size > 0) {
    if (ctx.nativeStrings) {
      ensureNativeStringHelpers(ctx);
      for (const value of state.forInLiterals) {
        if (!ctx.stringGlobalMap.has(value)) ctx.stringGlobalMap.set(value, -1);
      }
    } else {
      addStringImports(ctx);
      for (const value of state.forInLiterals) {
        addStringConstantGlobal(ctx, value);
      }
    }
  }
  if (state.inExprLiterals.size > 0) {
    if (ctx.nativeStrings) {
      ensureNativeStringHelpers(ctx);
      for (const value of state.inExprLiterals) {
        if (!ctx.stringGlobalMap.has(value)) ctx.stringGlobalMap.set(value, -1);
      }
    } else {
      addStringImports(ctx);
      for (const value of state.inExprLiterals) {
        addStringConstantGlobal(ctx, value);
      }
    }
  }
  if (state.objectMethodHasValues) {
    addUnionImports(ctx);
  }
  if (state.objectMethodLiterals.size > 0) {
    if (ctx.nativeStrings) {
      ensureNativeStringHelpers(ctx);
      for (const value of state.objectMethodLiterals) {
        if (!ctx.stringGlobalMap.has(value)) ctx.stringGlobalMap.set(value, -1);
      }
    } else {
      addStringImports(ctx);
      for (const value of state.objectMethodLiterals) {
        addStringConstantGlobal(ctx, value);
      }
    }
  }
  if (state.wrapperFound) {
    ensureWrapperTypes(ctx);
  }
  for (const [name, argCount] of state.unknownCtorNeeded) {
    const importName = `__new_${name}`;
    if (ctx.funcMap.has(importName)) continue;
    const params = Array.from({ length: argCount }, () => ({ kind: "externref" }));
    const typeIdx = addFuncType(ctx, params, [{ kind: "externref" }]);
    addImport(ctx, "env", importName, { kind: "func", typeIdx });
  }
}
function collectAllSourceImports(ctx, sourceFile) {
  const state = createUnifiedCollectorState(sourceFile);
  ts15.forEachChild(sourceFile, (node) => unifiedVisitNode(ctx, state, node));
  finalizeUnifiedCollector(ctx, state);
}
function registerWasiImports(ctx, sourceFile) {
  ctx.mod.memories.push({ min: 1 });
  ctx.mod.exports.push({ name: "memory", desc: { kind: "memory", index: 0 } });
  const bumpGlobalIdx = ctx.numImportGlobals + ctx.mod.globals.length;
  ctx.mod.globals.push({
    name: "__wasi_bump_ptr",
    type: { kind: "i32" },
    mutable: true,
    init: [{ op: "i32.const", value: 1024 }],
  });
  ctx.wasiBumpPtrGlobalIdx = bumpGlobalIdx;
  let needsFdWrite = false;
  let needsProcExit = false;
  function visit(node) {
    if (ts15.isCallExpression(node) && ts15.isPropertyAccessExpression(node.expression)) {
      const propAccess = node.expression;
      if (
        ts15.isIdentifier(propAccess.expression) &&
        propAccess.expression.text === "console" &&
        ["log", "warn", "error"].includes(propAccess.name.text)
      ) {
        needsFdWrite = true;
      }
      if (
        ts15.isIdentifier(propAccess.expression) &&
        propAccess.expression.text === "process" &&
        propAccess.name.text === "exit"
      ) {
        needsProcExit = true;
      }
    }
    ts15.forEachChild(node, visit);
  }
  ts15.forEachChild(sourceFile, visit);
  if (needsFdWrite) {
    const fdWriteType = addFuncType(
      ctx,
      [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }, { kind: "i32" }],
      [{ kind: "i32" }],
      "$wasi_fd_write",
    );
    addImport(ctx, "wasi_snapshot_preview1", "fd_write", { kind: "func", typeIdx: fdWriteType });
    ctx.wasiFdWriteIdx = ctx.funcMap.get("fd_write");
  }
  if (needsProcExit) {
    const procExitType = addFuncType(ctx, [{ kind: "i32" }], [], "$wasi_proc_exit");
    addImport(ctx, "wasi_snapshot_preview1", "proc_exit", { kind: "func", typeIdx: procExitType });
    ctx.wasiProcExitIdx = ctx.funcMap.get("proc_exit");
  }
  if (needsFdWrite) {
    emitWasiWriteStringHelper(ctx);
  }
}
function emitWasiWriteStringHelper(ctx) {
  const funcTypeIdx = addFuncType(ctx, [{ kind: "i32" }, { kind: "i32" }], []);
  const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set("__wasi_write_string", funcIdx);
  const body = [
    // Store ptr at memory[0] (iovec.buf)
    { op: "i32.const", value: 0 },
    { op: "local.get", index: 0 },
    { op: "i32.store", align: 2, offset: 0 },
    // Store len at memory[4] (iovec.buf_len)
    { op: "i32.const", value: 4 },
    { op: "local.get", index: 1 },
    { op: "i32.store", align: 2, offset: 0 },
    // Call fd_write(fd=1, iovs=0, iovs_len=1, nwritten=8)
    { op: "i32.const", value: 1 },
    // fd = stdout
    { op: "i32.const", value: 0 },
    // iovs pointer
    { op: "i32.const", value: 1 },
    // iovs_len = 1
    { op: "i32.const", value: 8 },
    // nwritten pointer
    { op: "call", funcIdx: ctx.wasiFdWriteIdx },
    { op: "drop" },
    // drop the return value (errno)
  ];
  ctx.mod.functions.push({
    name: "__wasi_write_string",
    typeIdx: funcTypeIdx,
    locals: [],
    body,
    exported: false,
  });
}
var STRING_METHODS = {
  toUpperCase: { params: [], result: { kind: "externref" } },
  toLowerCase: { params: [], result: { kind: "externref" } },
  trim: { params: [], result: { kind: "externref" } },
  trimStart: { params: [], result: { kind: "externref" } },
  trimEnd: { params: [], result: { kind: "externref" } },
  charAt: { params: [{ kind: "f64" }], result: { kind: "externref" } },
  slice: {
    params: [{ kind: "f64" }, { kind: "f64" }],
    result: { kind: "externref" },
  },
  substring: {
    params: [{ kind: "f64" }, { kind: "f64" }],
    result: { kind: "externref" },
  },
  indexOf: { params: [{ kind: "externref" }, { kind: "externref" }], result: { kind: "f64" } },
  lastIndexOf: { params: [{ kind: "externref" }, { kind: "externref" }], result: { kind: "f64" } },
  includes: { params: [{ kind: "externref" }], result: { kind: "i32" } },
  startsWith: { params: [{ kind: "externref" }], result: { kind: "i32" } },
  endsWith: { params: [{ kind: "externref" }], result: { kind: "i32" } },
  replace: {
    params: [{ kind: "externref" }, { kind: "externref" }],
    result: { kind: "externref" },
  },
  replaceAll: {
    params: [{ kind: "externref" }, { kind: "externref" }],
    result: { kind: "externref" },
  },
  repeat: { params: [{ kind: "f64" }], result: { kind: "externref" } },
  padStart: {
    params: [{ kind: "f64" }, { kind: "externref" }],
    result: { kind: "externref" },
  },
  padEnd: {
    params: [{ kind: "f64" }, { kind: "externref" }],
    result: { kind: "externref" },
  },
  split: { params: [{ kind: "externref" }], result: { kind: "externref" } },
  match: { params: [{ kind: "externref" }], result: { kind: "externref" } },
  search: { params: [{ kind: "externref" }], result: { kind: "f64" } },
  at: { params: [{ kind: "f64" }], result: { kind: "externref" } },
  codePointAt: { params: [{ kind: "f64" }], result: { kind: "f64" } },
  normalize: { params: [{ kind: "externref" }], result: { kind: "externref" } },
};
function addStringImports(ctx) {
  if (ctx.hasStringImports) return;
  ctx.hasStringImports = true;
  const importsBefore = ctx.numImportFuncs;
  const concatType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "ref_extern" }]);
  addImport(ctx, "wasm:js-string", "concat", {
    kind: "func",
    typeIdx: concatType,
  });
  const lengthType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }]);
  addImport(ctx, "wasm:js-string", "length", {
    kind: "func",
    typeIdx: lengthType,
  });
  const equalsType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "i32" }]);
  addImport(ctx, "wasm:js-string", "equals", {
    kind: "func",
    typeIdx: equalsType,
  });
  const substringType = addFuncType(
    ctx,
    [{ kind: "externref" }, { kind: "i32" }, { kind: "i32" }],
    [{ kind: "ref_extern" }],
  );
  addImport(ctx, "wasm:js-string", "substring", {
    kind: "func",
    typeIdx: substringType,
  });
  const charCodeAtType = addFuncType(ctx, [{ kind: "externref" }, { kind: "i32" }], [{ kind: "i32" }]);
  addImport(ctx, "wasm:js-string", "charCodeAt", {
    kind: "func",
    typeIdx: charCodeAtType,
  });
  const delta = ctx.numImportFuncs - importsBefore;
  if (delta > 0 && ctx.mod.functions.length > 0) {
    let shiftFuncIndices2 = function (instrs) {
      for (const instr of instrs) {
        if ((instr.op === "call" || instr.op === "return_call") && instr.funcIdx >= importsBefore) {
          instr.funcIdx += delta;
        }
        if (instr.op === "ref.func" && instr.funcIdx >= importsBefore) {
          instr.funcIdx += delta;
        }
        if ("body" in instr && Array.isArray(instr.body)) {
          shiftFuncIndices2(instr.body);
        }
        if ("then" in instr && Array.isArray(instr.then)) {
          shiftFuncIndices2(instr.then);
        }
        if ("else" in instr && Array.isArray(instr.else)) {
          shiftFuncIndices2(instr.else);
        }
        if ("catches" in instr && Array.isArray(instr.catches)) {
          for (const c of instr.catches) {
            if (Array.isArray(c.body)) shiftFuncIndices2(c.body);
          }
        }
        if ("catchAll" in instr && Array.isArray(instr.catchAll)) {
          shiftFuncIndices2(instr.catchAll);
        }
      }
    };
    var shiftFuncIndices = shiftFuncIndices2;
    const newImportNames = /* @__PURE__ */ new Set(["concat", "length", "equals", "substring", "charCodeAt"]);
    for (const [name, idx] of ctx.funcMap) {
      if (!newImportNames.has(name) && idx >= importsBefore) {
        ctx.funcMap.set(name, idx + delta);
      }
    }
    for (const exp of ctx.mod.exports) {
      if (exp.desc.kind === "func" && exp.desc.index >= importsBefore) {
        exp.desc.index += delta;
      }
    }
    const shifted = /* @__PURE__ */ new Set();
    for (const func of ctx.mod.functions) {
      if (!shifted.has(func.body)) {
        shiftFuncIndices2(func.body);
        shifted.add(func.body);
      }
    }
    if (ctx.currentFunc) {
      if (!shifted.has(ctx.currentFunc.body)) {
        shiftFuncIndices2(ctx.currentFunc.body);
        shifted.add(ctx.currentFunc.body);
      }
      for (const sb of ctx.currentFunc.savedBodies) {
        if (!shifted.has(sb)) {
          shiftFuncIndices2(sb);
          shifted.add(sb);
        }
      }
    }
    for (const parentFctx of ctx.funcStack) {
      if (!shifted.has(parentFctx.body)) {
        shiftFuncIndices2(parentFctx.body);
        shifted.add(parentFctx.body);
      }
      for (const sb of parentFctx.savedBodies) {
        if (!shifted.has(sb)) {
          shiftFuncIndices2(sb);
          shifted.add(sb);
        }
      }
    }
    for (const pb of ctx.parentBodiesStack) {
      if (!shifted.has(pb)) {
        shiftFuncIndices2(pb);
        shifted.add(pb);
      }
    }
    for (const elem of ctx.mod.elements) {
      if (elem.funcIndices) {
        for (let i = 0; i < elem.funcIndices.length; i++) {
          if (elem.funcIndices[i] >= importsBefore) {
            elem.funcIndices[i] += delta;
          }
        }
      }
    }
    if (ctx.mod.declaredFuncRefs.length > 0) {
      ctx.mod.declaredFuncRefs = ctx.mod.declaredFuncRefs.map((idx) => (idx >= importsBefore ? idx + delta : idx));
    }
  }
}
function ensureAnyValueType(ctx) {
  if (ctx.anyValueTypeIdx >= 0) return;
  ctx.anyValueTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "AnyValue",
    fields: [
      { name: "tag", type: { kind: "i32" }, mutable: false },
      { name: "i32val", type: { kind: "i32" }, mutable: false },
      { name: "f64val", type: { kind: "f64" }, mutable: false },
      { name: "refval", type: { kind: "eqref" }, mutable: false },
      { name: "externval", type: { kind: "externref" }, mutable: false },
    ],
  });
}
function ensureWrapperTypes(ctx) {
  if (ctx.wrapperNumberTypeIdx >= 0) return;
  ctx.wrapperNumberTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "WrapperNumber",
    fields: [{ name: "value", type: { kind: "f64" }, mutable: false }],
  });
  ctx.structMap.set("WrapperNumber", ctx.wrapperNumberTypeIdx);
  ctx.typeIdxToStructName.set(ctx.wrapperNumberTypeIdx, "WrapperNumber");
  ctx.structFields.set("WrapperNumber", [{ name: "value", type: { kind: "f64" }, mutable: false }]);
  ctx.wrapperStringTypeIdx = ctx.mod.types.length;
  const strValType = ctx.nativeStrings ? nativeStringType(ctx) : { kind: "externref" };
  ctx.mod.types.push({
    kind: "struct",
    name: "WrapperString",
    fields: [{ name: "value", type: strValType, mutable: false }],
  });
  ctx.structMap.set("WrapperString", ctx.wrapperStringTypeIdx);
  ctx.typeIdxToStructName.set(ctx.wrapperStringTypeIdx, "WrapperString");
  ctx.structFields.set("WrapperString", [{ name: "value", type: strValType, mutable: false }]);
  ctx.wrapperBooleanTypeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "WrapperBoolean",
    fields: [{ name: "value", type: { kind: "i32" }, mutable: false }],
  });
  ctx.structMap.set("WrapperBoolean", ctx.wrapperBooleanTypeIdx);
  ctx.typeIdxToStructName.set(ctx.wrapperBooleanTypeIdx, "WrapperBoolean");
  ctx.structFields.set("WrapperBoolean", [{ name: "value", type: { kind: "i32" }, mutable: false }]);
}
function emitWrapperValueOfFunctions(ctx) {
  if (ctx.wrapperNumberTypeIdx < 0) return;
  if (ctx.funcMap.has("WrapperNumber_valueOf")) return;
  const strValType = ctx.nativeStrings ? nativeStringType(ctx) : { kind: "externref" };
  {
    const funcTypeIdx = ctx.mod.types.length;
    ctx.mod.types.push({
      kind: "func",
      params: [{ kind: "ref", typeIdx: ctx.wrapperNumberTypeIdx }],
      results: [{ kind: "f64" }],
    });
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name: "WrapperNumber_valueOf",
      typeIdx: funcTypeIdx,
      locals: [],
      body: [
        { op: "local.get", index: 0 },
        { op: "struct.get", typeIdx: ctx.wrapperNumberTypeIdx, fieldIdx: 0 },
      ],
      exported: false,
    });
    ctx.funcMap.set("WrapperNumber_valueOf", funcIdx);
  }
  {
    const funcTypeIdx = ctx.mod.types.length;
    ctx.mod.types.push({
      kind: "func",
      params: [{ kind: "ref", typeIdx: ctx.wrapperStringTypeIdx }],
      results: [strValType],
    });
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name: "WrapperString_valueOf",
      typeIdx: funcTypeIdx,
      locals: [],
      body: [
        { op: "local.get", index: 0 },
        { op: "struct.get", typeIdx: ctx.wrapperStringTypeIdx, fieldIdx: 0 },
      ],
      exported: false,
    });
    ctx.funcMap.set("WrapperString_valueOf", funcIdx);
  }
  {
    const funcTypeIdx = ctx.mod.types.length;
    ctx.mod.types.push({
      kind: "func",
      params: [{ kind: "ref", typeIdx: ctx.wrapperBooleanTypeIdx }],
      results: [{ kind: "i32" }],
    });
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name: "WrapperBoolean_valueOf",
      typeIdx: funcTypeIdx,
      locals: [],
      body: [
        { op: "local.get", index: 0 },
        { op: "struct.get", typeIdx: ctx.wrapperBooleanTypeIdx, fieldIdx: 0 },
      ],
      exported: false,
    });
    ctx.funcMap.set("WrapperBoolean_valueOf", funcIdx);
  }
}
function isAnyValue(type, ctx) {
  return (
    (type.kind === "ref" || type.kind === "ref_null") &&
    type.typeIdx === ctx.anyValueTypeIdx &&
    ctx.anyValueTypeIdx >= 0
  );
}
function ensureAnyHelpers(ctx) {
  if (ctx.anyHelpersEmitted) return;
  ctx.anyHelpersEmitted = true;
  ensureAnyValueType(ctx);
  const anyTypeIdx = ctx.anyValueTypeIdx;
  const anyRef = { kind: "ref", typeIdx: anyTypeIdx };
  const anyRefNull = { kind: "ref_null", typeIdx: anyTypeIdx };
  function addHelper(name, params, results, body, locals) {
    const typeIdx = addFuncType(ctx, params, results, name);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.mod.functions.push({
      name,
      typeIdx,
      locals: locals ?? [],
      body,
      exported: false,
    });
    ctx.funcMap.set(name, funcIdx);
    ctx.anyHelpers.set(name, funcIdx);
  }
  const EQ_HEAP_TYPE = -19;
  addHelper(
    "__any_box_null",
    [],
    [anyRef],
    [
      { op: "i32.const", value: 0 },
      { op: "i32.const", value: 0 },
      { op: "f64.const", value: 0 },
      { op: "ref.null", typeIdx: EQ_HEAP_TYPE },
      { op: "ref.null.extern" },
      { op: "struct.new", typeIdx: anyTypeIdx },
    ],
  );
  addHelper(
    "__any_box_undefined",
    [],
    [anyRef],
    [
      { op: "i32.const", value: 1 },
      { op: "i32.const", value: 0 },
      { op: "f64.const", value: 0 },
      { op: "ref.null", typeIdx: EQ_HEAP_TYPE },
      { op: "ref.null.extern" },
      { op: "struct.new", typeIdx: anyTypeIdx },
    ],
  );
  addHelper(
    "__any_box_i32",
    [{ kind: "i32" }],
    [anyRef],
    [
      { op: "i32.const", value: 2 },
      { op: "local.get", index: 0 },
      { op: "f64.const", value: 0 },
      { op: "ref.null", typeIdx: EQ_HEAP_TYPE },
      { op: "ref.null.extern" },
      { op: "struct.new", typeIdx: anyTypeIdx },
    ],
  );
  addHelper(
    "__any_box_f64",
    [{ kind: "f64" }],
    [anyRef],
    [
      { op: "i32.const", value: 3 },
      { op: "i32.const", value: 0 },
      { op: "local.get", index: 0 },
      { op: "ref.null", typeIdx: EQ_HEAP_TYPE },
      { op: "ref.null.extern" },
      { op: "struct.new", typeIdx: anyTypeIdx },
    ],
  );
  addHelper(
    "__any_box_bool",
    [{ kind: "i32" }],
    [anyRef],
    [
      { op: "i32.const", value: 4 },
      { op: "local.get", index: 0 },
      { op: "f64.const", value: 0 },
      { op: "ref.null", typeIdx: EQ_HEAP_TYPE },
      { op: "ref.null.extern" },
      { op: "struct.new", typeIdx: anyTypeIdx },
    ],
  );
  addHelper(
    "__any_box_string",
    [{ kind: "externref" }],
    [anyRef],
    [
      { op: "i32.const", value: 5 },
      { op: "i32.const", value: 0 },
      { op: "f64.const", value: 0 },
      { op: "ref.null", typeIdx: EQ_HEAP_TYPE },
      { op: "local.get", index: 0 },
      { op: "struct.new", typeIdx: anyTypeIdx },
    ],
  );
  addHelper(
    "__any_box_ref",
    [{ kind: "eqref" }],
    [anyRef],
    [
      { op: "i32.const", value: 6 },
      { op: "i32.const", value: 0 },
      { op: "f64.const", value: 0 },
      { op: "local.get", index: 0 },
      { op: "ref.null.extern" },
      { op: "struct.new", typeIdx: anyTypeIdx },
    ],
  );
  addHelper(
    "__any_unbox_i32",
    [anyRefNull],
    [{ kind: "i32" }],
    [
      // Check if tag == 3 (f64 number)
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "i32.const", value: 3 },
      { op: "i32.eq" },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
          { op: "i32.trunc_sat_f64_s" },
        ],
        else: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
        ],
      },
    ],
  );
  addHelper(
    "__any_unbox_f64",
    [anyRefNull],
    [{ kind: "f64" }],
    [
      // Check if tag == 2 (i32 number)
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "i32.const", value: 2 },
      { op: "i32.eq" },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "f64" } },
        then: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
          { op: "f64.convert_i32_s" },
        ],
        else: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
        ],
      },
    ],
  );
  addHelper(
    "__any_unbox_bool",
    [anyRefNull],
    [{ kind: "i32" }],
    [
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "i32.const", value: 4 },
      { op: "i32.eq" },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
        ],
        else: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
          { op: "i32.const", value: 2 },
          { op: "i32.eq" },
          {
            op: "if",
            blockType: { kind: "val", type: { kind: "i32" } },
            then: [
              { op: "local.get", index: 0 },
              { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
              { op: "i32.const", value: 0 },
              { op: "i32.ne" },
            ],
            else: [
              { op: "local.get", index: 0 },
              { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
              { op: "i32.const", value: 3 },
              { op: "i32.eq" },
              {
                op: "if",
                blockType: { kind: "val", type: { kind: "i32" } },
                then: [
                  { op: "local.get", index: 0 },
                  { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
                  { op: "f64.const", value: 0 },
                  { op: "f64.ne" },
                ],
                else: [
                  { op: "local.get", index: 0 },
                  { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
                  { op: "i32.const", value: 5 },
                  { op: "i32.ge_s" },
                ],
              },
            ],
          },
        ],
      },
    ],
  );
  addHelper(
    "__any_unbox_extern",
    [anyRefNull],
    [{ kind: "externref" }],
    [
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 4 },
    ],
  );
  addHelper(
    "__any_to_f64",
    [anyRefNull],
    [{ kind: "f64" }],
    [
      // tag = a.tag
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 1 },
      // if tag == 1 (undefined) → NaN
      { op: "local.get", index: 1 },
      { op: "i32.const", value: 1 },
      { op: "i32.eq" },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "f64" } },
        then: [{ op: "f64.const", value: NaN }],
        else: [
          // if tag == 2 (i32) or tag == 4 (bool) → convert i32val to f64
          { op: "local.get", index: 1 },
          { op: "i32.const", value: 2 },
          { op: "i32.eq" },
          { op: "local.get", index: 1 },
          { op: "i32.const", value: 4 },
          { op: "i32.eq" },
          { op: "i32.or" },
          {
            op: "if",
            blockType: { kind: "val", type: { kind: "f64" } },
            then: [
              { op: "local.get", index: 0 },
              { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
              { op: "f64.convert_i32_s" },
            ],
            else: [
              // tag 0 (null) → f64val (0.0), tag 3 (f64) → f64val
              { op: "local.get", index: 0 },
              { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
            ],
          },
        ],
      },
    ],
    [{ name: "tag", type: { kind: "i32" } }],
  );
  const toF64Idx = ctx.funcMap.get("__any_to_f64");
  const boxI32Idx = ctx.funcMap.get("__any_box_i32");
  const boxF64Idx = ctx.funcMap.get("__any_box_f64");
  addHelper(
    "__any_add",
    [anyRefNull, anyRefNull],
    [anyRef],
    [
      // tagA = a.tag
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 2 },
      // tagB = b.tag
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // if tagA == 2 && tagB == 2 → i32 add
      { op: "local.get", index: 2 },
      { op: "i32.const", value: 2 },
      { op: "i32.eq" },
      { op: "local.get", index: 3 },
      { op: "i32.const", value: 2 },
      { op: "i32.eq" },
      { op: "i32.and" },
      {
        op: "if",
        blockType: { kind: "val", type: anyRef },
        then: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
          { op: "local.get", index: 1 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
          { op: "i32.add" },
          { op: "call", funcIdx: boxI32Idx },
        ],
        else: [
          // f64 path: convert both to f64, add, box as f64
          { op: "local.get", index: 0 },
          { op: "call", funcIdx: toF64Idx },
          { op: "local.get", index: 1 },
          { op: "call", funcIdx: toF64Idx },
          { op: "f64.add" },
          { op: "call", funcIdx: boxF64Idx },
        ],
      },
    ],
    [
      { name: "tagA", type: { kind: "i32" } },
      { name: "tagB", type: { kind: "i32" } },
    ],
  );
  function addNumericBinaryHelper(name, i32op, f64op) {
    addHelper(
      name,
      [anyRefNull, anyRefNull],
      [anyRef],
      [
        // tagA = a.tag
        { op: "local.get", index: 0 },
        { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
        { op: "local.set", index: 2 },
        // tagB = b.tag
        { op: "local.get", index: 1 },
        { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
        { op: "local.set", index: 3 },
        // if tagA == 2 && tagB == 2 → i32 op
        { op: "local.get", index: 2 },
        { op: "i32.const", value: 2 },
        { op: "i32.eq" },
        { op: "local.get", index: 3 },
        { op: "i32.const", value: 2 },
        { op: "i32.eq" },
        { op: "i32.and" },
        {
          op: "if",
          blockType: { kind: "val", type: anyRef },
          then: [
            { op: "local.get", index: 0 },
            { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
            { op: "local.get", index: 1 },
            { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
            { op: i32op },
            { op: "call", funcIdx: boxI32Idx },
          ],
          else: [
            // f64 path
            { op: "local.get", index: 0 },
            { op: "call", funcIdx: toF64Idx },
            { op: "local.get", index: 1 },
            { op: "call", funcIdx: toF64Idx },
            { op: f64op },
            { op: "call", funcIdx: boxF64Idx },
          ],
        },
      ],
      [
        { name: "tagA", type: { kind: "i32" } },
        { name: "tagB", type: { kind: "i32" } },
      ],
    );
  }
  addNumericBinaryHelper("__any_sub", "i32.sub", "f64.sub");
  addNumericBinaryHelper("__any_mul", "i32.mul", "f64.mul");
  addHelper(
    "__any_div",
    [anyRefNull, anyRefNull],
    [anyRef],
    [
      { op: "local.get", index: 0 },
      { op: "call", funcIdx: toF64Idx },
      { op: "local.get", index: 1 },
      { op: "call", funcIdx: toF64Idx },
      { op: "f64.div" },
      { op: "call", funcIdx: boxF64Idx },
    ],
  );
  addHelper(
    "__any_mod",
    [anyRefNull, anyRefNull],
    [anyRef],
    [
      // tagA = a.tag
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 2 },
      // tagB = b.tag
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // if tagA == 2 && tagB == 2 → i32 rem_s
      { op: "local.get", index: 2 },
      { op: "i32.const", value: 2 },
      { op: "i32.eq" },
      { op: "local.get", index: 3 },
      { op: "i32.const", value: 2 },
      { op: "i32.eq" },
      { op: "i32.and" },
      {
        op: "if",
        blockType: { kind: "val", type: anyRef },
        then: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
          { op: "local.get", index: 1 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
          { op: "i32.rem_s" },
          { op: "call", funcIdx: boxI32Idx },
        ],
        else: [
          // f64 path: a - floor(a/b) * b
          { op: "local.get", index: 0 },
          { op: "call", funcIdx: toF64Idx },
          { op: "local.set", index: 4 },
          // fA
          { op: "local.get", index: 1 },
          { op: "call", funcIdx: toF64Idx },
          { op: "local.set", index: 5 },
          // fB
          // result = fA - floor(fA / fB) * fB
          { op: "local.get", index: 4 },
          { op: "local.get", index: 4 },
          { op: "local.get", index: 5 },
          { op: "f64.div" },
          { op: "f64.floor" },
          { op: "local.get", index: 5 },
          { op: "f64.mul" },
          { op: "f64.sub" },
          { op: "call", funcIdx: boxF64Idx },
        ],
      },
    ],
    [
      { name: "tagA", type: { kind: "i32" } },
      { name: "tagB", type: { kind: "i32" } },
      { name: "fA", type: { kind: "f64" } },
      { name: "fB", type: { kind: "f64" } },
    ],
  );
  addHelper(
    "__any_eq",
    [anyRefNull, anyRefNull],
    [{ kind: "i32" }],
    [
      // Fast path: if both refs point to the same AnyValue struct, they are equal.
      { op: "local.get", index: 0 },
      { op: "local.get", index: 1 },
      { op: "ref.eq" },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [{ op: "i32.const", value: 1 }],
        else: [
          // tagA = a.tag
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 2 },
          // tagB = b.tag
          { op: "local.get", index: 1 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 3 },
          // if tagA != tagB → 0
          { op: "local.get", index: 2 },
          { op: "local.get", index: 3 },
          { op: "i32.ne" },
          {
            op: "if",
            blockType: { kind: "val", type: { kind: "i32" } },
            then: [
              // Cross-tag numeric equality: if one is i32(2) and other is f64(3), compare as f64
              { op: "local.get", index: 2 },
              { op: "local.get", index: 3 },
              { op: "i32.add" },
              { op: "i32.const", value: 5 },
              // 2+3 = 5
              { op: "i32.eq" },
              {
                op: "if",
                blockType: { kind: "val", type: { kind: "i32" } },
                then: [
                  { op: "local.get", index: 0 },
                  { op: "call", funcIdx: toF64Idx },
                  { op: "local.get", index: 1 },
                  { op: "call", funcIdx: toF64Idx },
                  { op: "f64.eq" },
                ],
                else: [{ op: "i32.const", value: 0 }],
              },
            ],
            else: [
              // Same tag — compare by tag type
              { op: "local.get", index: 2 },
              { op: "i32.const", value: 2 },
              { op: "i32.eq" },
              {
                op: "if",
                blockType: { kind: "val", type: { kind: "i32" } },
                then: [
                  // i32 eq
                  { op: "local.get", index: 0 },
                  { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
                  { op: "local.get", index: 1 },
                  { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
                  { op: "i32.eq" },
                ],
                else: [
                  { op: "local.get", index: 2 },
                  { op: "i32.const", value: 3 },
                  { op: "i32.eq" },
                  {
                    op: "if",
                    blockType: { kind: "val", type: { kind: "i32" } },
                    then: [
                      // f64 eq
                      { op: "local.get", index: 0 },
                      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
                      { op: "local.get", index: 1 },
                      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
                      { op: "f64.eq" },
                    ],
                    else: [
                      { op: "local.get", index: 2 },
                      { op: "i32.const", value: 4 },
                      { op: "i32.eq" },
                      {
                        op: "if",
                        blockType: { kind: "val", type: { kind: "i32" } },
                        then: [
                          // bool eq (compare i32val)
                          { op: "local.get", index: 0 },
                          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
                          { op: "local.get", index: 1 },
                          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
                          { op: "i32.eq" },
                        ],
                        else: [
                          // tag 6 (ref): compare refval (eqref) with ref.eq
                          { op: "local.get", index: 2 },
                          { op: "i32.const", value: 6 },
                          { op: "i32.eq" },
                          {
                            op: "if",
                            blockType: { kind: "val", type: { kind: "i32" } },
                            then: [
                              { op: "local.get", index: 0 },
                              { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 3 },
                              { op: "local.get", index: 1 },
                              { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 3 },
                              { op: "ref.eq" },
                            ],
                            else: [
                              // null/undefined (tag < 2): both same tag → equal
                              { op: "local.get", index: 2 },
                              { op: "i32.const", value: 2 },
                              { op: "i32.lt_s" },
                            ],
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
    [
      { name: "tagA", type: { kind: "i32" } },
      { name: "tagB", type: { kind: "i32" } },
    ],
  );
  addHelper(
    "__any_strict_eq",
    [anyRefNull, anyRefNull],
    [{ kind: "i32" }],
    [
      // Fast path: if both refs point to the same AnyValue struct, they are equal.
      // This handles object identity (var b = a) for all tag types.
      { op: "local.get", index: 0 },
      { op: "local.get", index: 1 },
      { op: "ref.eq" },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [{ op: "i32.const", value: 1 }],
        else: [
          // tagA = a.tag
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 2 },
          // tagB = b.tag
          { op: "local.get", index: 1 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 3 },
          // if tagA != tagB → 0 (strict: no cross-type coercion)
          { op: "local.get", index: 2 },
          { op: "local.get", index: 3 },
          { op: "i32.ne" },
          {
            op: "if",
            blockType: { kind: "val", type: { kind: "i32" } },
            then: [{ op: "i32.const", value: 0 }],
            else: [
              // Same tag — compare by tag type
              { op: "local.get", index: 2 },
              { op: "i32.const", value: 2 },
              { op: "i32.eq" },
              {
                op: "if",
                blockType: { kind: "val", type: { kind: "i32" } },
                then: [
                  // i32 eq
                  { op: "local.get", index: 0 },
                  { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
                  { op: "local.get", index: 1 },
                  { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
                  { op: "i32.eq" },
                ],
                else: [
                  { op: "local.get", index: 2 },
                  { op: "i32.const", value: 3 },
                  { op: "i32.eq" },
                  {
                    op: "if",
                    blockType: { kind: "val", type: { kind: "i32" } },
                    then: [
                      // f64 eq
                      { op: "local.get", index: 0 },
                      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
                      { op: "local.get", index: 1 },
                      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
                      { op: "f64.eq" },
                    ],
                    else: [
                      { op: "local.get", index: 2 },
                      { op: "i32.const", value: 4 },
                      { op: "i32.eq" },
                      {
                        op: "if",
                        blockType: { kind: "val", type: { kind: "i32" } },
                        then: [
                          // bool eq (compare i32val)
                          { op: "local.get", index: 0 },
                          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
                          { op: "local.get", index: 1 },
                          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
                          { op: "i32.eq" },
                        ],
                        else: [
                          // tag 6 (ref): compare refval (eqref) with ref.eq
                          { op: "local.get", index: 2 },
                          { op: "i32.const", value: 6 },
                          { op: "i32.eq" },
                          {
                            op: "if",
                            blockType: { kind: "val", type: { kind: "i32" } },
                            then: [
                              { op: "local.get", index: 0 },
                              { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 3 },
                              { op: "local.get", index: 1 },
                              { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 3 },
                              { op: "ref.eq" },
                            ],
                            else: [
                              // null/undefined (tag < 2): both same tag → equal
                              // string (tag 5): different AnyValue boxes → not same ref
                              // (string content equality is handled by string-specific codepaths)
                              { op: "local.get", index: 2 },
                              { op: "i32.const", value: 2 },
                              { op: "i32.lt_s" },
                            ],
                          },
                        ],
                      },
                    ],
                  },
                ],
              },
            ],
          },
        ],
      },
    ],
    [
      { name: "tagA", type: { kind: "i32" } },
      { name: "tagB", type: { kind: "i32" } },
    ],
  );
  function addComparisonHelper(name, f64op) {
    addHelper(
      name,
      [anyRefNull, anyRefNull],
      [{ kind: "i32" }],
      [
        { op: "local.get", index: 0 },
        { op: "call", funcIdx: toF64Idx },
        { op: "local.get", index: 1 },
        { op: "call", funcIdx: toF64Idx },
        { op: f64op },
      ],
    );
  }
  addComparisonHelper("__any_lt", "f64.lt");
  addComparisonHelper("__any_gt", "f64.gt");
  addComparisonHelper("__any_le", "f64.le");
  addComparisonHelper("__any_ge", "f64.ge");
  addHelper(
    "__any_neg",
    [anyRefNull],
    [anyRef],
    [
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
      { op: "i32.const", value: 2 },
      { op: "i32.eq" },
      {
        op: "if",
        blockType: { kind: "val", type: anyRef },
        then: [
          { op: "i32.const", value: 0 },
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 1 },
          { op: "i32.sub" },
          { op: "call", funcIdx: boxI32Idx },
        ],
        else: [
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 2 },
          { op: "f64.neg" },
          { op: "call", funcIdx: boxF64Idx },
        ],
      },
    ],
  );
  if (ctx.nativeStrings && ctx.nativeStrTypeIdx >= 0) {
    let nativeStrConstInstrs2 = function (value) {
      const instrs = [];
      instrs.push({ op: "i32.const", value: value.length });
      instrs.push({ op: "i32.const", value: 0 });
      for (let i = 0; i < value.length; i++) {
        instrs.push({ op: "i32.const", value: value.charCodeAt(i) });
      }
      instrs.push({ op: "array.new_fixed", typeIdx: strDataTypeIdx, length: value.length });
      instrs.push({ op: "struct.new", typeIdx: strTypeIdx });
      return instrs;
    };
    var nativeStrConstInstrs = nativeStrConstInstrs2;
    const strDataTypeIdx = ctx.nativeStrDataTypeIdx;
    const strTypeIdx = ctx.nativeStrTypeIdx;
    const anyStrRef = { kind: "ref", typeIdx: ctx.anyStrTypeIdx };
    addHelper(
      "__any_typeof",
      [anyRefNull],
      [anyStrRef],
      [
        // Check tag and return corresponding string
        { op: "local.get", index: 0 },
        { op: "struct.get", typeIdx: anyTypeIdx, fieldIdx: 0 },
        { op: "local.set", index: 1 },
        // tag in local 1
        // tag == 0 (null) → "object"
        { op: "local.get", index: 1 },
        { op: "i32.const", value: 0 },
        { op: "i32.eq" },
        {
          op: "if",
          blockType: { kind: "val", type: anyStrRef },
          then: nativeStrConstInstrs2("object"),
          else: [
            // tag == 1 (undefined) → "undefined"
            { op: "local.get", index: 1 },
            { op: "i32.const", value: 1 },
            { op: "i32.eq" },
            {
              op: "if",
              blockType: { kind: "val", type: anyStrRef },
              then: nativeStrConstInstrs2("undefined"),
              else: [
                // tag == 2 or tag == 3 (i32/f64) → "number"
                { op: "local.get", index: 1 },
                { op: "i32.const", value: 2 },
                { op: "i32.eq" },
                { op: "local.get", index: 1 },
                { op: "i32.const", value: 3 },
                { op: "i32.eq" },
                { op: "i32.or" },
                {
                  op: "if",
                  blockType: { kind: "val", type: anyStrRef },
                  then: nativeStrConstInstrs2("number"),
                  else: [
                    // tag == 4 (bool) → "boolean"
                    { op: "local.get", index: 1 },
                    { op: "i32.const", value: 4 },
                    { op: "i32.eq" },
                    {
                      op: "if",
                      blockType: { kind: "val", type: anyStrRef },
                      then: nativeStrConstInstrs2("boolean"),
                      else: [
                        // tag == 5 (string/externref) or tag == 6 (gcref) — default to "object"
                        // (In practice tag 5 would be "string" but we don't use it in fast mode)
                        ...nativeStrConstInstrs2("object"),
                      ],
                    },
                  ],
                },
              ],
            },
          ],
        },
      ],
      [{ name: "tag", type: { kind: "i32" } }],
    );
  }
}
function nativeStringType(ctx) {
  return { kind: "ref", typeIdx: ctx.anyStrTypeIdx };
}
function flatStringType(ctx) {
  return { kind: "ref", typeIdx: ctx.nativeStrTypeIdx };
}
function ensureNativeStringHelpers(ctx) {
  if (ctx.nativeStrHelpersEmitted) return;
  ctx.nativeStrHelpersEmitted = true;
  const strDataTypeIdx = ctx.nativeStrDataTypeIdx;
  const strTypeIdx = ctx.nativeStrTypeIdx;
  const anyStrTypeIdx = ctx.anyStrTypeIdx;
  const consStrTypeIdx = ctx.consStrTypeIdx;
  const strRef = { kind: "ref", typeIdx: anyStrTypeIdx };
  const flatStrRef = { kind: "ref", typeIdx: strTypeIdx };
  const strDataRef = { kind: "ref", typeIdx: strDataTypeIdx };
  if (ctx.mod.memories.length === 0) {
    ctx.mod.memories.push({ min: 1 });
    ctx.mod.exports.push({
      name: "__str_mem",
      desc: { kind: "memory", index: 0 },
    });
  }
  {
    const typeIdx = addFuncType(ctx, [{ kind: "i32" }, { kind: "i32" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__str_from_mem", { kind: "func", typeIdx });
  }
  {
    const typeIdx = addFuncType(ctx, [{ kind: "externref" }, { kind: "i32" }], []);
    addImport(ctx, "env", "__str_to_mem", { kind: "func", typeIdx });
  }
  {
    const lenTypeIdx = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }]);
    addImport(ctx, "env", "__str_extern_len", { kind: "func", typeIdx: lenTypeIdx });
  }
  const getFlattenIdx = () => ctx.nativeStrHelpers.get("__str_flatten");
  function wrapBodyWithFlatten(body, strParamIndices) {
    const preamble = [];
    for (const idx of strParamIndices) {
      preamble.push(
        { op: "local.get", index: idx },
        { op: "call", funcIdx: getFlattenIdx() },
        // flatten returns ref $NativeString which is subtype of ref $AnyString — can store in param
        { op: "local.set", index: idx },
      );
    }
    const processed = [];
    for (const instr of body) {
      if (instr.op === "struct.get" && instr.typeIdx === strTypeIdx) {
        processed.push({ op: "ref.cast", typeIdx: strTypeIdx });
      }
      if (instr.op === "if") {
        const ifInstr = instr;
        const newIf = { ...ifInstr };
        if (ifInstr.then) newIf.then = wrapBodyWithFlatten(ifInstr.then, []).slice(0);
        if (ifInstr.else) newIf.else = wrapBodyWithFlatten(ifInstr.else, []).slice(0);
        processed.push(newIf);
        continue;
      }
      if (instr.op === "block" || instr.op === "loop") {
        const blockInstr = instr;
        const newBlock = { ...blockInstr };
        if (blockInstr.body) newBlock.body = wrapBodyWithFlatten(blockInstr.body, []).slice(0);
        processed.push(newBlock);
        continue;
      }
      processed.push(instr);
    }
    return [...preamble, ...processed];
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, strDataRef, { kind: "i32" }], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_copy_tree", funcIdx);
    const body = [
      // if node is FlatString: array.copy and return pos + len
      { op: "local.get", index: 0 },
      { op: "ref.test", typeIdx: strTypeIdx },
      {
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [
          // flat = ref.cast $NativeString node
          { op: "local.get", index: 0 },
          { op: "ref.cast", typeIdx: strTypeIdx },
          { op: "local.set", index: 3 },
          // flatOff = flat.off (field 1)
          { op: "local.get", index: 3 },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
          { op: "local.set", index: 4 },
          // flatLen = flat.len (field 0)
          { op: "local.get", index: 3 },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 5 },
          // array.copy(buf, pos, flat.data, flatOff, flatLen)
          { op: "local.get", index: 1 },
          // dst = buf
          { op: "local.get", index: 2 },
          // dstOffset = pos
          { op: "local.get", index: 3 },
          // flat
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
          // flat.data
          { op: "local.get", index: 4 },
          // srcOffset = flatOff
          { op: "local.get", index: 5 },
          // length = flatLen
          { op: "array.copy", dstTypeIdx: strDataTypeIdx, srcTypeIdx: strDataTypeIdx },
          // return pos + flatLen
          { op: "local.get", index: 2 },
          { op: "local.get", index: 5 },
          { op: "i32.add" },
        ],
        else: [
          // node is ConsString
          // left = cons.left (field 1)
          { op: "local.get", index: 0 },
          { op: "ref.cast", typeIdx: consStrTypeIdx },
          { op: "struct.get", typeIdx: consStrTypeIdx, fieldIdx: 1 },
          // left
          { op: "local.set", index: 6 },
          // left
          // right = cons.right
          { op: "local.get", index: 0 },
          { op: "ref.cast", typeIdx: consStrTypeIdx },
          { op: "struct.get", typeIdx: consStrTypeIdx, fieldIdx: 2 },
          // right
          { op: "local.set", index: 7 },
          // right
          // pos = copy_tree(left, buf, pos)
          { op: "local.get", index: 6 },
          { op: "ref.as_non_null" },
          { op: "local.get", index: 1 },
          { op: "local.get", index: 2 },
          { op: "call", funcIdx },
          // recursive call to self
          // return copy_tree(right, buf, pos)
          // pos is now the return value on the stack — use it directly
          { op: "local.set", index: 2 },
          // update pos
          { op: "local.get", index: 7 },
          { op: "ref.as_non_null" },
          { op: "local.get", index: 1 },
          { op: "local.get", index: 2 },
          { op: "call", funcIdx },
          // recursive call to self
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_copy_tree",
      typeIdx,
      locals: [
        { name: "flat", type: { kind: "ref_null", typeIdx: strTypeIdx } },
        { name: "flatOff", type: { kind: "i32" } },
        { name: "flatLen", type: { kind: "i32" } },
        { name: "left", type: { kind: "ref_null", typeIdx: anyStrTypeIdx } },
        { name: "right", type: { kind: "ref_null", typeIdx: anyStrTypeIdx } },
      ],
      body,
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef], [flatStrRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_flatten", funcIdx);
    const copyTreeIdx = ctx.nativeStrHelpers.get("__str_copy_tree");
    const body = [
      // if s is already a FlatString, return it
      { op: "local.get", index: 0 },
      { op: "ref.test", typeIdx: strTypeIdx },
      {
        op: "if",
        blockType: { kind: "val", type: flatStrRef },
        then: [
          { op: "local.get", index: 0 },
          { op: "ref.cast", typeIdx: strTypeIdx },
        ],
        else: [
          // len = s.len (field 0 of AnyString)
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: anyStrTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 1 },
          // buf = array.new_default(len)
          { op: "local.get", index: 1 },
          { op: "array.new_default", typeIdx: strDataTypeIdx },
          { op: "local.set", index: 2 },
          // copy_tree(s, buf, 0)
          { op: "local.get", index: 0 },
          { op: "local.get", index: 2 },
          { op: "i32.const", value: 0 },
          { op: "call", funcIdx: copyTreeIdx },
          { op: "drop" },
          // discard returned position
          // return struct.new $NativeString(len, 0, buf)
          { op: "local.get", index: 1 },
          // len
          { op: "i32.const", value: 0 },
          // off = 0
          { op: "local.get", index: 2 },
          // data = buf
          { op: "struct.new", typeIdx: strTypeIdx },
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_flatten",
      typeIdx,
      locals: [
        { name: "len", type: { kind: "i32" } },
        { name: "buf", type: strDataRef },
      ],
      body,
      exported: false,
    });
  }
  {
    const flattenIdx = ctx.nativeStrHelpers.get("__str_flatten");
    const typeIdx = addFuncType(ctx, [strRef, strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_concat", funcIdx);
    const body = [
      // lenA = a.len (field 0 of AnyString)
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: anyStrTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 2 },
      // lenA
      // lenB = b.len (field 0 of AnyString)
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: anyStrTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // lenB
      // newLen = lenA + lenB
      { op: "local.get", index: 2 },
      { op: "local.get", index: 3 },
      { op: "i32.add" },
      { op: "local.set", index: 4 },
      // newLen
      // if newLen >= 64, create ConsString (O(1) rope node)
      { op: "local.get", index: 4 },
      { op: "i32.const", value: 64 },
      { op: "i32.ge_u" },
      {
        op: "if",
        blockType: { kind: "val", type: strRef },
        then: [
          // struct.new $ConsString(newLen, a, b)
          { op: "local.get", index: 4 },
          // len = newLen
          { op: "local.get", index: 0 },
          // left = a
          { op: "local.get", index: 1 },
          // right = b
          { op: "struct.new", typeIdx: consStrTypeIdx },
        ],
        else: [
          // Short string: flatten both sides and copy
          // flatA = flatten(a)
          { op: "local.get", index: 0 },
          { op: "call", funcIdx: flattenIdx },
          { op: "local.set", index: 6 },
          // flatB = flatten(b)
          { op: "local.get", index: 1 },
          { op: "call", funcIdx: flattenIdx },
          { op: "local.set", index: 7 },
          // newArr = array.new_default(newLen)
          { op: "local.get", index: 4 },
          { op: "array.new_default", typeIdx: strDataTypeIdx },
          { op: "local.set", index: 5 },
          // array.copy(newArr, 0, flatA.data, flatA.off, lenA)
          { op: "local.get", index: 5 },
          // dst
          { op: "ref.as_non_null" },
          { op: "i32.const", value: 0 },
          // dstOffset
          { op: "local.get", index: 6 },
          // flatA
          { op: "ref.as_non_null" },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
          // flatA.data
          { op: "local.get", index: 6 },
          // flatA
          { op: "ref.as_non_null" },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
          // flatA.off
          { op: "local.get", index: 2 },
          // lenA
          { op: "array.copy", dstTypeIdx: strDataTypeIdx, srcTypeIdx: strDataTypeIdx },
          // array.copy(newArr, lenA, flatB.data, flatB.off, lenB)
          { op: "local.get", index: 5 },
          // dst
          { op: "ref.as_non_null" },
          { op: "local.get", index: 2 },
          // dstOffset = lenA
          { op: "local.get", index: 7 },
          // flatB
          { op: "ref.as_non_null" },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
          // flatB.data
          { op: "local.get", index: 7 },
          // flatB
          { op: "ref.as_non_null" },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
          // flatB.off
          { op: "local.get", index: 3 },
          // lenB
          { op: "array.copy", dstTypeIdx: strDataTypeIdx, srcTypeIdx: strDataTypeIdx },
          // result = struct.new $NativeString(newLen, 0, newArr)
          { op: "local.get", index: 4 },
          // len = newLen
          { op: "i32.const", value: 0 },
          // off = 0
          { op: "local.get", index: 5 },
          // data = newArr
          { op: "ref.as_non_null" },
          { op: "struct.new", typeIdx: strTypeIdx },
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_concat",
      typeIdx,
      locals: [
        { name: "lenA", type: { kind: "i32" } },
        { name: "lenB", type: { kind: "i32" } },
        { name: "newLen", type: { kind: "i32" } },
        { name: "newArr", type: { kind: "ref_null", typeIdx: strDataTypeIdx } },
        { name: "flatA", type: { kind: "ref_null", typeIdx: strTypeIdx } },
        { name: "flatB", type: { kind: "ref_null", typeIdx: strTypeIdx } },
      ],
      body,
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, strRef], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_equals", funcIdx);
    const body = [
      // len = a.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 2 },
      // len
      // if a.len != b.len return 0
      { op: "local.get", index: 2 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "i32.ne" },
      { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 0 }, { op: "return" }] },
      // aOff = a.off
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 6 },
      // bOff = b.off
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 7 },
      // aData = a.data
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 4 },
      // bData = b.data
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 5 },
      // i = 0
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 3 },
      // loop: compare element by element
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              // if i >= len, break (strings are equal)
              { op: "local.get", index: 3 },
              { op: "local.get", index: 2 },
              { op: "i32.ge_u" },
              { op: "br_if", depth: 1 },
              // if aData[aOff + i] != bData[bOff + i], return 0
              { op: "local.get", index: 4 },
              { op: "local.get", index: 6 },
              { op: "local.get", index: 3 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "local.get", index: 5 },
              { op: "local.get", index: 7 },
              { op: "local.get", index: 3 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "i32.ne" },
              { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 0 }, { op: "return" }] },
              // i++
              { op: "local.get", index: 3 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 3 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // return 1 (equal)
      { op: "i32.const", value: 1 },
    ];
    ctx.mod.functions.push({
      name: "__str_equals",
      typeIdx,
      locals: [
        { name: "len", type: { kind: "i32" } },
        { name: "i", type: { kind: "i32" } },
        { name: "aData", type: strDataRef },
        { name: "bData", type: strDataRef },
        { name: "aOff", type: { kind: "i32" } },
        { name: "bOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, strRef], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_compare", funcIdx);
    const body = [
      // lenA = a.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 2 },
      // lenB = b.len
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // minLen = min(lenA, lenB)
      { op: "local.get", index: 2 },
      { op: "local.get", index: 3 },
      { op: "local.get", index: 2 },
      { op: "local.get", index: 3 },
      { op: "i32.lt_u" },
      { op: "select" },
      { op: "local.set", index: 4 },
      // aOff = a.off
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 8 },
      // bOff = b.off
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 9 },
      // aData = a.data
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 6 },
      // bData = b.data
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 7 },
      // i = 0
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 5 },
      // loop: compare element by element
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              // if i >= minLen, break (common prefix is equal)
              { op: "local.get", index: 5 },
              { op: "local.get", index: 4 },
              { op: "i32.ge_u" },
              { op: "br_if", depth: 1 },
              // ca = aData[aOff + i]
              { op: "local.get", index: 6 },
              { op: "local.get", index: 8 },
              { op: "local.get", index: 5 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "local.set", index: 10 },
              // cb = bData[bOff + i]
              { op: "local.get", index: 7 },
              { op: "local.get", index: 9 },
              { op: "local.get", index: 5 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "local.set", index: 11 },
              // if ca < cb return -1
              { op: "local.get", index: 10 },
              { op: "local.get", index: 11 },
              { op: "i32.lt_u" },
              { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: -1 }, { op: "return" }] },
              // if ca > cb return 1
              { op: "local.get", index: 10 },
              { op: "local.get", index: 11 },
              { op: "i32.gt_u" },
              { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 1 }, { op: "return" }] },
              // i++
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 5 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // Common prefix is equal; compare by length
      // if lenA < lenB return -1
      { op: "local.get", index: 2 },
      { op: "local.get", index: 3 },
      { op: "i32.lt_u" },
      { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: -1 }, { op: "return" }] },
      // if lenA > lenB return 1
      { op: "local.get", index: 2 },
      { op: "local.get", index: 3 },
      { op: "i32.gt_u" },
      { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 1 }, { op: "return" }] },
      // return 0 (equal)
      { op: "i32.const", value: 0 },
    ];
    ctx.mod.functions.push({
      name: "__str_compare",
      typeIdx,
      locals: [
        { name: "lenA", type: { kind: "i32" } },
        { name: "lenB", type: { kind: "i32" } },
        { name: "minLen", type: { kind: "i32" } },
        { name: "i", type: { kind: "i32" } },
        { name: "aData", type: strDataRef },
        { name: "bData", type: strDataRef },
        { name: "aOff", type: { kind: "i32" } },
        { name: "bOff", type: { kind: "i32" } },
        { name: "ca", type: { kind: "i32" } },
        { name: "cb", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, { kind: "i32" }, { kind: "i32" }], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_substring", funcIdx);
    const body = [
      // sOff = s.off
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 3 },
      // sLen = s.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 4 },
      // Clamp start: max(0, min(start, sLen))
      { op: "local.get", index: 1 },
      { op: "i32.const", value: 0 },
      { op: "local.get", index: 1 },
      { op: "i32.const", value: 0 },
      { op: "i32.gt_s" },
      { op: "select" },
      { op: "local.tee", index: 1 },
      // start = max(0, start)
      { op: "local.get", index: 4 },
      { op: "local.get", index: 1 },
      { op: "local.get", index: 4 },
      { op: "i32.lt_s" },
      { op: "select" },
      { op: "local.set", index: 1 },
      // start = min(start, sLen)
      // Clamp end: max(0, min(end, sLen))
      { op: "local.get", index: 2 },
      { op: "i32.const", value: 0 },
      { op: "local.get", index: 2 },
      { op: "i32.const", value: 0 },
      { op: "i32.gt_s" },
      { op: "select" },
      { op: "local.tee", index: 2 },
      // end = max(0, end)
      { op: "local.get", index: 4 },
      { op: "local.get", index: 2 },
      { op: "local.get", index: 4 },
      { op: "i32.lt_s" },
      { op: "select" },
      { op: "local.set", index: 2 },
      // end = min(end, sLen)
      // Swap if start > end (JS substring semantics)
      { op: "local.get", index: 1 },
      { op: "local.get", index: 2 },
      { op: "i32.gt_s" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: 2 },
          { op: "local.get", index: 1 },
          { op: "local.set", index: 2 },
          { op: "local.set", index: 1 },
        ],
      },
      // struct.new(len = end - start, off = sOff + start, s.data)
      { op: "local.get", index: 2 },
      // end
      { op: "local.get", index: 1 },
      // start
      { op: "i32.sub" },
      // len = end - start
      { op: "local.get", index: 3 },
      // sOff
      { op: "local.get", index: 1 },
      // start
      { op: "i32.add" },
      // off = sOff + start
      { op: "local.get", index: 0 },
      // s
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      // s.data
      { op: "struct.new", typeIdx: strTypeIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_substring",
      typeIdx,
      locals: [
        { name: "sOff", type: { kind: "i32" } },
        { name: "sLen", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, { kind: "i32" }], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_charAt", funcIdx);
    const body = [
      // Bounds check: if idx < 0 || idx >= s.len, return empty string
      { op: "local.get", index: 1 },
      { op: "i32.const", value: 0 },
      { op: "i32.lt_s" },
      { op: "local.get", index: 1 },
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "i32.ge_s" },
      { op: "i32.or" },
      {
        op: "if",
        blockType: { kind: "val", type: strRef },
        then: [
          // empty string: off=0, len=0, empty array
          { op: "i32.const", value: 0 },
          { op: "i32.const", value: 0 },
          { op: "i32.const", value: 0 },
          { op: "array.new_default", typeIdx: strDataTypeIdx },
          { op: "struct.new", typeIdx: strTypeIdx },
        ],
        else: [
          // Single-char string: len=1, off=0, [char]
          { op: "i32.const", value: 1 },
          // len
          { op: "i32.const", value: 0 },
          // off
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
          // data
          { op: "local.get", index: 0 },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
          // off
          { op: "local.get", index: 1 },
          { op: "i32.add" },
          // off + idx
          { op: "array.get_u", typeIdx: strDataTypeIdx },
          // Create single-element array
          { op: "array.new_fixed", typeIdx: strDataTypeIdx, length: 1 },
          { op: "struct.new", typeIdx: strTypeIdx },
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_charAt",
      typeIdx,
      locals: [],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, { kind: "i32" }, { kind: "i32" }], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_slice", funcIdx);
    const substringIdx = ctx.nativeStrHelpers.get("__str_substring");
    const body = [
      // len = s.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // len
      // Resolve negative start: if start < 0, start = len + start
      { op: "local.get", index: 1 },
      { op: "i32.const", value: 0 },
      { op: "i32.lt_s" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: 3 },
          // len
          { op: "local.get", index: 1 },
          // start (negative)
          { op: "i32.add" },
          { op: "local.set", index: 1 },
        ],
      },
      // Clamp start to >= 0
      { op: "local.get", index: 1 },
      { op: "i32.const", value: 0 },
      { op: "i32.lt_s" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "i32.const", value: 0 },
          { op: "local.set", index: 1 },
        ],
      },
      // Resolve negative end: if end < 0, end = len + end
      { op: "local.get", index: 2 },
      { op: "i32.const", value: 0 },
      { op: "i32.lt_s" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: 3 },
          // len
          { op: "local.get", index: 2 },
          // end (negative)
          { op: "i32.add" },
          { op: "local.set", index: 2 },
        ],
      },
      // Clamp end to >= 0
      { op: "local.get", index: 2 },
      { op: "i32.const", value: 0 },
      { op: "i32.lt_s" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "i32.const", value: 0 },
          { op: "local.set", index: 2 },
        ],
      },
      // Delegate to __str_substring (which handles clamping to len and swapping)
      { op: "local.get", index: 0 },
      { op: "local.get", index: 1 },
      { op: "local.get", index: 2 },
      { op: "call", funcIdx: substringIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_slice",
      typeIdx,
      locals: [{ name: "len", type: { kind: "i32" } }],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, strRef, { kind: "i32" }], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_indexOf", funcIdx);
    const body = [
      // hLen = haystack.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // nLen = needle.len
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 4 },
      // if nLen == 0, return clamp(fromIndex, 0, hLen)
      { op: "local.get", index: 4 },
      { op: "i32.eqz" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: 2 },
          { op: "i32.const", value: 0 },
          { op: "local.get", index: 2 },
          { op: "i32.const", value: 0 },
          { op: "i32.gt_s" },
          { op: "select" },
          { op: "local.tee", index: 5 },
          { op: "local.get", index: 3 },
          { op: "local.get", index: 5 },
          { op: "local.get", index: 3 },
          { op: "i32.lt_s" },
          { op: "select" },
          { op: "return" },
        ],
      },
      // hOff = haystack.off
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 9 },
      // nOff = needle.off
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 10 },
      // hData = haystack.data
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 7 },
      // nData = needle.data
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 8 },
      // i = max(fromIndex, 0)
      { op: "local.get", index: 2 },
      { op: "i32.const", value: 0 },
      { op: "local.get", index: 2 },
      { op: "i32.const", value: 0 },
      { op: "i32.gt_s" },
      { op: "select" },
      { op: "local.set", index: 5 },
      // outer loop: scan i from fromIndex to hLen - nLen
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              // if i > hLen - nLen, break
              { op: "local.get", index: 5 },
              { op: "local.get", index: 3 },
              { op: "local.get", index: 4 },
              { op: "i32.sub" },
              { op: "i32.gt_s" },
              { op: "br_if", depth: 1 },
              // j = 0; inner loop to compare needle chars
              { op: "i32.const", value: 0 },
              { op: "local.set", index: 6 },
              {
                op: "block",
                blockType: { kind: "empty" },
                body: [
                  {
                    op: "loop",
                    blockType: { kind: "empty" },
                    body: [
                      // if j >= nLen, match found — return i
                      { op: "local.get", index: 6 },
                      { op: "local.get", index: 4 },
                      { op: "i32.ge_s" },
                      {
                        op: "if",
                        blockType: { kind: "empty" },
                        then: [{ op: "local.get", index: 5 }, { op: "return" }],
                      },
                      // if hData[hOff + i + j] != nData[nOff + j], break inner
                      { op: "local.get", index: 7 },
                      { op: "local.get", index: 9 },
                      { op: "local.get", index: 5 },
                      { op: "i32.add" },
                      { op: "local.get", index: 6 },
                      { op: "i32.add" },
                      { op: "array.get_u", typeIdx: strDataTypeIdx },
                      { op: "local.get", index: 8 },
                      { op: "local.get", index: 10 },
                      { op: "local.get", index: 6 },
                      { op: "i32.add" },
                      { op: "array.get_u", typeIdx: strDataTypeIdx },
                      { op: "i32.ne" },
                      { op: "br_if", depth: 1 },
                      // j++
                      { op: "local.get", index: 6 },
                      { op: "i32.const", value: 1 },
                      { op: "i32.add" },
                      { op: "local.set", index: 6 },
                      { op: "br", depth: 0 },
                    ],
                  },
                ],
              },
              // i++
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 5 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // not found
      { op: "i32.const", value: -1 },
    ];
    ctx.mod.functions.push({
      name: "__str_indexOf",
      typeIdx,
      locals: [
        { name: "hLen", type: { kind: "i32" } },
        { name: "nLen", type: { kind: "i32" } },
        { name: "i", type: { kind: "i32" } },
        { name: "j", type: { kind: "i32" } },
        { name: "hData", type: strDataRef },
        { name: "nData", type: strDataRef },
        { name: "hOff", type: { kind: "i32" } },
        { name: "nOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, strRef, { kind: "i32" }], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_lastIndexOf", funcIdx);
    const body = [
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 4 },
      // if nLen == 0, return min(fromIndex, hLen)
      { op: "local.get", index: 4 },
      { op: "i32.eqz" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          { op: "local.get", index: 2 },
          { op: "local.get", index: 3 },
          { op: "local.get", index: 2 },
          { op: "local.get", index: 3 },
          { op: "i32.lt_s" },
          { op: "select" },
          { op: "return" },
        ],
      },
      // hOff, nOff
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 9 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 10 },
      // hData, nData
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 7 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 8 },
      // i = min(fromIndex, hLen - nLen)
      { op: "local.get", index: 2 },
      { op: "local.get", index: 3 },
      { op: "local.get", index: 4 },
      { op: "i32.sub" },
      { op: "local.tee", index: 5 },
      { op: "local.get", index: 2 },
      { op: "local.get", index: 5 },
      { op: "i32.lt_s" },
      { op: "select" },
      { op: "local.set", index: 5 },
      // reverse scan
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 0 },
              { op: "i32.lt_s" },
              { op: "br_if", depth: 1 },
              { op: "i32.const", value: 0 },
              { op: "local.set", index: 6 },
              {
                op: "block",
                blockType: { kind: "empty" },
                body: [
                  {
                    op: "loop",
                    blockType: { kind: "empty" },
                    body: [
                      { op: "local.get", index: 6 },
                      { op: "local.get", index: 4 },
                      { op: "i32.ge_s" },
                      {
                        op: "if",
                        blockType: { kind: "empty" },
                        then: [{ op: "local.get", index: 5 }, { op: "return" }],
                      },
                      // hData[hOff + i + j]
                      { op: "local.get", index: 7 },
                      { op: "local.get", index: 9 },
                      { op: "local.get", index: 5 },
                      { op: "i32.add" },
                      { op: "local.get", index: 6 },
                      { op: "i32.add" },
                      { op: "array.get_u", typeIdx: strDataTypeIdx },
                      // nData[nOff + j]
                      { op: "local.get", index: 8 },
                      { op: "local.get", index: 10 },
                      { op: "local.get", index: 6 },
                      { op: "i32.add" },
                      { op: "array.get_u", typeIdx: strDataTypeIdx },
                      { op: "i32.ne" },
                      { op: "br_if", depth: 1 },
                      { op: "local.get", index: 6 },
                      { op: "i32.const", value: 1 },
                      { op: "i32.add" },
                      { op: "local.set", index: 6 },
                      { op: "br", depth: 0 },
                    ],
                  },
                ],
              },
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 1 },
              { op: "i32.sub" },
              { op: "local.set", index: 5 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // not found
      { op: "i32.const", value: -1 },
    ];
    ctx.mod.functions.push({
      name: "__str_lastIndexOf",
      typeIdx,
      locals: [
        { name: "hLen", type: { kind: "i32" } },
        { name: "nLen", type: { kind: "i32" } },
        { name: "i", type: { kind: "i32" } },
        { name: "j", type: { kind: "i32" } },
        { name: "hData", type: strDataRef },
        { name: "nData", type: strDataRef },
        { name: "hOff", type: { kind: "i32" } },
        { name: "nOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, strRef, { kind: "i32" }], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_includes", funcIdx);
    const indexOfIdx = ctx.nativeStrHelpers.get("__str_indexOf");
    const body = [
      { op: "local.get", index: 0 },
      { op: "local.get", index: 1 },
      { op: "local.get", index: 2 },
      { op: "call", funcIdx: indexOfIdx },
      { op: "i32.const", value: -1 },
      { op: "i32.ne" },
    ];
    ctx.mod.functions.push({
      name: "__str_includes",
      typeIdx,
      locals: [],
      body,
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, strRef, { kind: "i32" }], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_startsWith", funcIdx);
    const body = [
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 4 },
      // if position + pLen > sLen, return 0
      { op: "local.get", index: 2 },
      { op: "local.get", index: 4 },
      { op: "i32.add" },
      { op: "local.get", index: 3 },
      { op: "i32.gt_s" },
      { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 0 }, { op: "return" }] },
      // sOff, pOff
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 8 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 9 },
      // sData, pData
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 6 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 7 },
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 5 },
      // compare loop
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              { op: "local.get", index: 5 },
              { op: "local.get", index: 4 },
              { op: "i32.ge_s" },
              { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 1 }, { op: "return" }] },
              // sData[sOff + position + i]
              { op: "local.get", index: 6 },
              { op: "local.get", index: 8 },
              { op: "local.get", index: 2 },
              { op: "i32.add" },
              { op: "local.get", index: 5 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              // pData[pOff + i]
              { op: "local.get", index: 7 },
              { op: "local.get", index: 9 },
              { op: "local.get", index: 5 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "i32.ne" },
              { op: "br_if", depth: 1 },
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 5 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // mismatch found
      { op: "i32.const", value: 0 },
    ];
    ctx.mod.functions.push({
      name: "__str_startsWith",
      typeIdx,
      locals: [
        { name: "sLen", type: { kind: "i32" } },
        { name: "pLen", type: { kind: "i32" } },
        { name: "i", type: { kind: "i32" } },
        { name: "sData", type: strDataRef },
        { name: "pData", type: strDataRef },
        { name: "sOff", type: { kind: "i32" } },
        { name: "pOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, strRef, { kind: "i32" }], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_endsWith", funcIdx);
    const body = [
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // sLen = s.len; clamp endPos to sLen
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 8 },
      // endPos = min(endPos, sLen)
      { op: "local.get", index: 2 },
      { op: "local.get", index: 8 },
      { op: "local.get", index: 2 },
      { op: "local.get", index: 8 },
      { op: "i32.lt_s" },
      { op: "select" },
      { op: "local.set", index: 2 },
      // startPos = endPos - sxLen
      { op: "local.get", index: 2 },
      { op: "local.get", index: 3 },
      { op: "i32.sub" },
      { op: "local.set", index: 7 },
      // if startPos < 0, return 0
      { op: "local.get", index: 7 },
      { op: "i32.const", value: 0 },
      { op: "i32.lt_s" },
      { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 0 }, { op: "return" }] },
      // sOff, xOff
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 9 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 10 },
      // sData, xData
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 5 },
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 6 },
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 4 },
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              { op: "local.get", index: 4 },
              { op: "local.get", index: 3 },
              { op: "i32.ge_s" },
              { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 1 }, { op: "return" }] },
              // sData[sOff + startPos + i]
              { op: "local.get", index: 5 },
              { op: "local.get", index: 9 },
              { op: "local.get", index: 7 },
              { op: "i32.add" },
              { op: "local.get", index: 4 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              // xData[xOff + i]
              { op: "local.get", index: 6 },
              { op: "local.get", index: 10 },
              { op: "local.get", index: 4 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "i32.ne" },
              { op: "br_if", depth: 1 },
              { op: "local.get", index: 4 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 4 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      { op: "i32.const", value: 0 },
    ];
    ctx.mod.functions.push({
      name: "__str_endsWith",
      typeIdx,
      locals: [
        { name: "sxLen", type: { kind: "i32" } },
        { name: "i", type: { kind: "i32" } },
        { name: "sData", type: strDataRef },
        { name: "xData", type: strDataRef },
        { name: "startPos", type: { kind: "i32" } },
        { name: "sLen", type: { kind: "i32" } },
        { name: "sOff", type: { kind: "i32" } },
        { name: "xOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [{ kind: "i32" }], [{ kind: "i32" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_isWhitespace", funcIdx);
    const body = [
      // Check ranges: 0x09 <= c <= 0x0D || c == 0x20 || c == 0xA0 || c == 0xFEFF
      // Use a chain of comparisons
      { op: "local.get", index: 0 },
      { op: "i32.const", value: 32 },
      { op: "i32.eq" },
      { op: "local.get", index: 0 },
      { op: "i32.const", value: 9 },
      { op: "i32.ge_u" },
      { op: "local.get", index: 0 },
      { op: "i32.const", value: 13 },
      { op: "i32.le_u" },
      { op: "i32.and" },
      { op: "i32.or" },
      { op: "local.get", index: 0 },
      { op: "i32.const", value: 160 },
      { op: "i32.eq" },
      { op: "i32.or" },
      { op: "local.get", index: 0 },
      { op: "i32.const", value: 65279 },
      { op: "i32.eq" },
      { op: "i32.or" },
    ];
    ctx.mod.functions.push({
      name: "__str_isWhitespace",
      typeIdx,
      locals: [],
      body,
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_trimStart", funcIdx);
    const isWsIdx = ctx.nativeStrHelpers.get("__str_isWhitespace");
    const substringIdx = ctx.nativeStrHelpers.get("__str_substring");
    const body = [
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 1 },
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 4 },
      // sOff
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 3 },
      // sData
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 2 },
      // scan forward while whitespace
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              { op: "local.get", index: 2 },
              { op: "local.get", index: 1 },
              { op: "i32.ge_s" },
              { op: "br_if", depth: 1 },
              // sData[sOff + i]
              { op: "local.get", index: 3 },
              { op: "local.get", index: 4 },
              { op: "local.get", index: 2 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "call", funcIdx: isWsIdx },
              { op: "i32.eqz" },
              { op: "br_if", depth: 1 },
              { op: "local.get", index: 2 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 2 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // return substring(s, i, len)
      { op: "local.get", index: 0 },
      { op: "local.get", index: 2 },
      { op: "local.get", index: 1 },
      { op: "call", funcIdx: substringIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_trimStart",
      typeIdx,
      locals: [
        { name: "len", type: { kind: "i32" } },
        { name: "i", type: { kind: "i32" } },
        { name: "sData", type: strDataRef },
        { name: "sOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_trimEnd", funcIdx);
    const isWsIdx = ctx.nativeStrHelpers.get("__str_isWhitespace");
    const substringIdx = ctx.nativeStrHelpers.get("__str_substring");
    const body = [
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 1 },
      // end = len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 3 },
      // sOff
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 2 },
      // sData
      // scan backward while whitespace
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              { op: "local.get", index: 1 },
              { op: "i32.const", value: 0 },
              { op: "i32.le_s" },
              { op: "br_if", depth: 1 },
              // sData[sOff + end - 1]
              { op: "local.get", index: 2 },
              { op: "local.get", index: 3 },
              { op: "local.get", index: 1 },
              { op: "i32.add" },
              { op: "i32.const", value: 1 },
              { op: "i32.sub" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "call", funcIdx: isWsIdx },
              { op: "i32.eqz" },
              { op: "br_if", depth: 1 },
              { op: "local.get", index: 1 },
              { op: "i32.const", value: 1 },
              { op: "i32.sub" },
              { op: "local.set", index: 1 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // return substring(s, 0, end)
      { op: "local.get", index: 0 },
      { op: "i32.const", value: 0 },
      { op: "local.get", index: 1 },
      { op: "call", funcIdx: substringIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_trimEnd",
      typeIdx,
      locals: [
        { name: "end", type: { kind: "i32" } },
        { name: "sData", type: strDataRef },
        { name: "sOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_trim", funcIdx);
    const trimStartIdx = ctx.nativeStrHelpers.get("__str_trimStart");
    const trimEndIdx = ctx.nativeStrHelpers.get("__str_trimEnd");
    const body = [
      { op: "local.get", index: 0 },
      { op: "call", funcIdx: trimStartIdx },
      { op: "call", funcIdx: trimEndIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_trim",
      typeIdx,
      locals: [],
      body,
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef, { kind: "i32" }], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_repeat", funcIdx);
    const body = [
      // sLen = s.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 2 },
      // if count <= 0 or sLen == 0, return empty string
      { op: "local.get", index: 1 },
      { op: "i32.const", value: 0 },
      { op: "i32.le_s" },
      {
        op: "if",
        blockType: { kind: "val", type: strRef },
        then: [
          { op: "i32.const", value: 0 },
          // off = 0
          { op: "i32.const", value: 0 },
          // len = 0
          { op: "i32.const", value: 0 },
          { op: "array.new_default", typeIdx: strDataTypeIdx },
          { op: "struct.new", typeIdx: strTypeIdx },
        ],
        else: [
          { op: "local.get", index: 2 },
          { op: "i32.eqz" },
          {
            op: "if",
            blockType: { kind: "val", type: strRef },
            then: [
              { op: "i32.const", value: 0 },
              // off = 0
              { op: "i32.const", value: 0 },
              // len = 0
              { op: "i32.const", value: 0 },
              { op: "array.new_default", typeIdx: strDataTypeIdx },
              { op: "struct.new", typeIdx: strTypeIdx },
            ],
            else: [
              // sOff = s.off
              { op: "local.get", index: 0 },
              { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
              { op: "local.set", index: 8 },
              // newLen = sLen * count
              { op: "local.get", index: 2 },
              { op: "local.get", index: 1 },
              { op: "i32.mul" },
              { op: "local.tee", index: 3 },
              // newArr = array.new_default(newLen)
              { op: "array.new_default", typeIdx: strDataTypeIdx },
              { op: "local.set", index: 4 },
              // srcData = s.data
              { op: "local.get", index: 0 },
              { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
              { op: "local.set", index: 6 },
              // dst = 0
              { op: "i32.const", value: 0 },
              { op: "local.set", index: 5 },
              // outer loop: repeat count times
              {
                op: "block",
                blockType: { kind: "empty" },
                body: [
                  {
                    op: "loop",
                    blockType: { kind: "empty" },
                    body: [
                      { op: "local.get", index: 5 },
                      { op: "local.get", index: 3 },
                      { op: "i32.ge_u" },
                      { op: "br_if", depth: 1 },
                      // array.copy newArr[dst..] <- srcData[sOff..sOff+sLen]
                      { op: "local.get", index: 4 },
                      // dst array
                      { op: "local.get", index: 5 },
                      // dst offset
                      { op: "local.get", index: 6 },
                      // src array
                      { op: "local.get", index: 8 },
                      // src offset = sOff
                      { op: "local.get", index: 2 },
                      // length = sLen
                      { op: "array.copy", dstTypeIdx: strDataTypeIdx, srcTypeIdx: strDataTypeIdx },
                      // dst += sLen
                      { op: "local.get", index: 5 },
                      { op: "local.get", index: 2 },
                      { op: "i32.add" },
                      { op: "local.set", index: 5 },
                      { op: "br", depth: 0 },
                    ],
                  },
                ],
              },
              // return struct.new(newLen, 0, newArr)
              { op: "local.get", index: 3 },
              // len = newLen
              { op: "i32.const", value: 0 },
              // off = 0
              { op: "local.get", index: 4 },
              // data = newArr
              { op: "struct.new", typeIdx: strTypeIdx },
            ],
          },
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_repeat",
      typeIdx,
      locals: [
        { name: "sLen", type: { kind: "i32" } },
        { name: "newLen", type: { kind: "i32" } },
        { name: "newArr", type: strDataRef },
        { name: "dst", type: { kind: "i32" } },
        { name: "srcData", type: strDataRef },
        { name: "copyI", type: { kind: "i32" } },
        { name: "sOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const concatIdx = ctx.nativeStrHelpers.get("__str_concat");
    const repeatIdx = ctx.nativeStrHelpers.get("__str_repeat");
    const substringIdx = ctx.nativeStrHelpers.get("__str_substring");
    const typeIdx = addFuncType(ctx, [strRef, { kind: "i32" }, strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_padStart", funcIdx);
    const body = [
      // sLen = s.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // if sLen >= targetLen, return s
      { op: "local.get", index: 3 },
      { op: "local.get", index: 1 },
      { op: "i32.ge_s" },
      {
        op: "if",
        blockType: { kind: "val", type: strRef },
        then: [{ op: "local.get", index: 0 }],
        else: [
          // padLen = padStr.len
          { op: "local.get", index: 2 },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 4 },
          // if padLen == 0, return s
          { op: "local.get", index: 4 },
          { op: "i32.eqz" },
          {
            op: "if",
            blockType: { kind: "val", type: strRef },
            then: [{ op: "local.get", index: 0 }],
            else: [
              // fillLen = targetLen - sLen
              { op: "local.get", index: 1 },
              { op: "local.get", index: 3 },
              { op: "i32.sub" },
              { op: "local.set", index: 5 },
              // repeated = repeat(padStr, ceil(fillLen / padLen))
              { op: "local.get", index: 2 },
              // padStr (1st arg)
              { op: "local.get", index: 5 },
              // fillLen
              { op: "local.get", index: 4 },
              // padLen
              { op: "i32.add" },
              { op: "i32.const", value: 1 },
              { op: "i32.sub" },
              { op: "local.get", index: 4 },
              { op: "i32.div_u" },
              // count (2nd arg)
              { op: "call", funcIdx: repeatIdx },
              // prefix = repeated.substring(0, fillLen)
              { op: "i32.const", value: 0 },
              { op: "local.get", index: 5 },
              { op: "call", funcIdx: substringIdx },
              // return concat(prefix, s)
              { op: "local.get", index: 0 },
              { op: "call", funcIdx: concatIdx },
            ],
          },
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_padStart",
      typeIdx,
      locals: [
        { name: "sLen", type: { kind: "i32" } },
        { name: "padLen", type: { kind: "i32" } },
        { name: "fillLen", type: { kind: "i32" } },
        { name: "repeated", type: strRef },
        { name: "prefix", type: strRef },
      ],
      body: wrapBodyWithFlatten(body, [0, 2]),
      exported: false,
    });
  }
  {
    const concatIdx = ctx.nativeStrHelpers.get("__str_concat");
    const repeatIdx = ctx.nativeStrHelpers.get("__str_repeat");
    const substringIdx = ctx.nativeStrHelpers.get("__str_substring");
    const typeIdx = addFuncType(ctx, [strRef, { kind: "i32" }, strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_padEnd", funcIdx);
    const body = [
      // sLen = s.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 3 },
      // if sLen >= targetLen, return s
      { op: "local.get", index: 3 },
      { op: "local.get", index: 1 },
      { op: "i32.ge_s" },
      {
        op: "if",
        blockType: { kind: "val", type: strRef },
        then: [{ op: "local.get", index: 0 }],
        else: [
          // padLen = padStr.len
          { op: "local.get", index: 2 },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 4 },
          // if padLen == 0, return s
          { op: "local.get", index: 4 },
          { op: "i32.eqz" },
          {
            op: "if",
            blockType: { kind: "val", type: strRef },
            then: [{ op: "local.get", index: 0 }],
            else: [
              // fillLen = targetLen - sLen
              { op: "local.get", index: 1 },
              { op: "local.get", index: 3 },
              { op: "i32.sub" },
              { op: "local.set", index: 5 },
              // repeated = repeat(padStr, ceil(fillLen / padLen))
              { op: "local.get", index: 2 },
              // padStr (1st arg)
              { op: "local.get", index: 5 },
              // fillLen
              { op: "local.get", index: 4 },
              // padLen
              { op: "i32.add" },
              { op: "i32.const", value: 1 },
              { op: "i32.sub" },
              { op: "local.get", index: 4 },
              { op: "i32.div_u" },
              // count (2nd arg)
              { op: "call", funcIdx: repeatIdx },
              // suffix = repeated.substring(0, fillLen)
              { op: "i32.const", value: 0 },
              { op: "local.get", index: 5 },
              { op: "call", funcIdx: substringIdx },
              // return concat(s, suffix)
              // stack has: suffix on top. Store it, push s, push suffix back
              { op: "local.set", index: 6 },
              // suffix -> local 6
              { op: "local.get", index: 0 },
              // s (1st arg to concat)
              { op: "local.get", index: 6 },
              // suffix (2nd arg to concat)
              { op: "ref.as_non_null" },
              { op: "call", funcIdx: concatIdx },
            ],
          },
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_padEnd",
      typeIdx,
      locals: [
        { name: "sLen", type: { kind: "i32" } },
        { name: "padLen", type: { kind: "i32" } },
        { name: "fillLen", type: { kind: "i32" } },
        { name: "suffix", type: { kind: "ref_null", typeIdx: anyStrTypeIdx } },
      ],
      body: wrapBodyWithFlatten(body, [0, 2]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_toLowerCase", funcIdx);
    const body = [
      // len = s.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 1 },
      // sOff = s.off
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 6 },
      // srcData = s.data
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 2 },
      // newArr = array.new_default(len)
      { op: "local.get", index: 1 },
      { op: "array.new_default", typeIdx: strDataTypeIdx },
      { op: "local.set", index: 3 },
      // i = 0
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 4 },
      // loop over each code unit
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              { op: "local.get", index: 4 },
              { op: "local.get", index: 1 },
              { op: "i32.ge_u" },
              { op: "br_if", depth: 1 },
              // ch = srcData[sOff + i]
              { op: "local.get", index: 2 },
              { op: "local.get", index: 6 },
              { op: "local.get", index: 4 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "local.set", index: 5 },
              // newArr[i] = (ch >= 65 && ch <= 90) ? ch + 32 : ch
              { op: "local.get", index: 3 },
              { op: "local.get", index: 4 },
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 65 },
              { op: "i32.ge_u" },
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 90 },
              { op: "i32.le_u" },
              { op: "i32.and" },
              {
                op: "if",
                blockType: { kind: "val", type: { kind: "i32" } },
                then: [{ op: "local.get", index: 5 }, { op: "i32.const", value: 32 }, { op: "i32.add" }],
                else: [{ op: "local.get", index: 5 }],
              },
              { op: "array.set", typeIdx: strDataTypeIdx },
              // i++
              { op: "local.get", index: 4 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 4 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // return struct.new(len, 0, newArr)
      { op: "local.get", index: 1 },
      // len
      { op: "i32.const", value: 0 },
      // off = 0
      { op: "local.get", index: 3 },
      // data = newArr
      { op: "struct.new", typeIdx: strTypeIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_toLowerCase",
      typeIdx,
      locals: [
        { name: "len", type: { kind: "i32" } },
        { name: "srcData", type: strDataRef },
        { name: "newArr", type: strDataRef },
        { name: "i", type: { kind: "i32" } },
        { name: "ch", type: { kind: "i32" } },
        { name: "sOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_toUpperCase", funcIdx);
    const body = [
      // len = s.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 1 },
      // sOff = s.off
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 6 },
      // srcData = s.data
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 2 },
      // newArr = array.new_default(len)
      { op: "local.get", index: 1 },
      { op: "array.new_default", typeIdx: strDataTypeIdx },
      { op: "local.set", index: 3 },
      // i = 0
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 4 },
      // loop
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              { op: "local.get", index: 4 },
              { op: "local.get", index: 1 },
              { op: "i32.ge_u" },
              { op: "br_if", depth: 1 },
              // ch = srcData[sOff + i]
              { op: "local.get", index: 2 },
              { op: "local.get", index: 6 },
              { op: "local.get", index: 4 },
              { op: "i32.add" },
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "local.set", index: 5 },
              // newArr[i] = (ch >= 97 && ch <= 122) ? ch - 32 : ch
              { op: "local.get", index: 3 },
              { op: "local.get", index: 4 },
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 97 },
              { op: "i32.ge_u" },
              { op: "local.get", index: 5 },
              { op: "i32.const", value: 122 },
              { op: "i32.le_u" },
              { op: "i32.and" },
              {
                op: "if",
                blockType: { kind: "val", type: { kind: "i32" } },
                then: [{ op: "local.get", index: 5 }, { op: "i32.const", value: 32 }, { op: "i32.sub" }],
                else: [{ op: "local.get", index: 5 }],
              },
              { op: "array.set", typeIdx: strDataTypeIdx },
              // i++
              { op: "local.get", index: 4 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 4 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // return struct.new(len, 0, newArr)
      { op: "local.get", index: 1 },
      // len
      { op: "i32.const", value: 0 },
      // off = 0
      { op: "local.get", index: 3 },
      // data = newArr
      { op: "struct.new", typeIdx: strTypeIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_toUpperCase",
      typeIdx,
      locals: [
        { name: "len", type: { kind: "i32" } },
        { name: "srcData", type: strDataRef },
        { name: "newArr", type: strDataRef },
        { name: "i", type: { kind: "i32" } },
        { name: "ch", type: { kind: "i32" } },
        { name: "sOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const indexOfIdx = ctx.nativeStrHelpers.get("__str_indexOf");
    const substringIdx = ctx.nativeStrHelpers.get("__str_substring");
    const concatIdx = ctx.nativeStrHelpers.get("__str_concat");
    const typeIdx = addFuncType(ctx, [strRef, strRef, strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_replace", funcIdx);
    const body = [
      // idx = indexOf(s, search, 0)
      { op: "local.get", index: 0 },
      { op: "local.get", index: 1 },
      { op: "i32.const", value: 0 },
      { op: "call", funcIdx: indexOfIdx },
      { op: "local.set", index: 3 },
      // if idx == -1, return s unchanged
      { op: "local.get", index: 3 },
      { op: "i32.const", value: -1 },
      { op: "i32.eq" },
      {
        op: "if",
        blockType: { kind: "val", type: strRef },
        then: [{ op: "local.get", index: 0 }],
        else: [
          // searchLen = search.len
          { op: "local.get", index: 1 },
          { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
          { op: "local.set", index: 4 },
          // prefix = s.substring(0, idx)
          { op: "local.get", index: 0 },
          { op: "i32.const", value: 0 },
          { op: "local.get", index: 3 },
          { op: "call", funcIdx: substringIdx },
          { op: "local.set", index: 5 },
          // suffix = s.substring(idx + searchLen, MAX)
          { op: "local.get", index: 0 },
          { op: "local.get", index: 3 },
          { op: "local.get", index: 4 },
          { op: "i32.add" },
          { op: "i32.const", value: 2147483647 },
          { op: "call", funcIdx: substringIdx },
          { op: "local.set", index: 6 },
          // return concat(concat(prefix, replacement), suffix)
          { op: "local.get", index: 5 },
          { op: "ref.as_non_null" },
          { op: "local.get", index: 2 },
          { op: "call", funcIdx: concatIdx },
          { op: "local.get", index: 6 },
          { op: "ref.as_non_null" },
          { op: "call", funcIdx: concatIdx },
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_replace",
      typeIdx,
      locals: [
        { name: "idx", type: { kind: "i32" } },
        { name: "searchLen", type: { kind: "i32" } },
        { name: "prefix", type: { kind: "ref_null", typeIdx: anyStrTypeIdx } },
        { name: "suffix", type: { kind: "ref_null", typeIdx: anyStrTypeIdx } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1, 2]),
      exported: false,
    });
  }
  {
    const indexOfIdx = ctx.nativeStrHelpers.get("__str_indexOf");
    const substringIdx = ctx.nativeStrHelpers.get("__str_substring");
    const concatIdx = ctx.nativeStrHelpers.get("__str_concat");
    const typeIdx = addFuncType(ctx, [strRef, strRef, strRef], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_replaceAll", funcIdx);
    const body = [
      // searchLen = search.len
      { op: "local.get", index: 1 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 6 },
      // If searchLen == 0, return s unchanged (avoid infinite loop)
      { op: "local.get", index: 6 },
      { op: "i32.eqz" },
      {
        op: "if",
        blockType: { kind: "val", type: strRef },
        then: [{ op: "local.get", index: 0 }],
        else: [
          // Build an empty result string (len=0, off=0, empty array)
          { op: "i32.const", value: 0 },
          { op: "i32.const", value: 0 },
          { op: "i32.const", value: 0 },
          { op: "array.new_default", typeIdx: ctx.nativeStrDataTypeIdx },
          { op: "struct.new", typeIdx: strTypeIdx },
          { op: "local.set", index: 3 },
          // pos = 0
          { op: "i32.const", value: 0 },
          { op: "local.set", index: 4 },
          // loop: find next occurrence
          {
            op: "block",
            blockType: { kind: "empty" },
            body: [
              {
                op: "loop",
                blockType: { kind: "empty" },
                body: [
                  // idx = indexOf(s, search, pos)
                  { op: "local.get", index: 0 },
                  { op: "local.get", index: 1 },
                  { op: "local.get", index: 4 },
                  { op: "call", funcIdx: indexOfIdx },
                  { op: "local.set", index: 5 },
                  // if idx == -1, break
                  { op: "local.get", index: 5 },
                  { op: "i32.const", value: -1 },
                  { op: "i32.eq" },
                  { op: "br_if", depth: 1 },
                  // prefix = s.substring(pos, idx)
                  { op: "local.get", index: 0 },
                  { op: "local.get", index: 4 },
                  { op: "local.get", index: 5 },
                  { op: "call", funcIdx: substringIdx },
                  { op: "local.set", index: 7 },
                  // result = concat(result, prefix)
                  { op: "local.get", index: 3 },
                  { op: "ref.as_non_null" },
                  { op: "local.get", index: 7 },
                  { op: "ref.as_non_null" },
                  { op: "call", funcIdx: concatIdx },
                  // result = concat(result, replacement)
                  { op: "local.get", index: 2 },
                  { op: "call", funcIdx: concatIdx },
                  { op: "local.set", index: 3 },
                  // pos = idx + searchLen
                  { op: "local.get", index: 5 },
                  { op: "local.get", index: 6 },
                  { op: "i32.add" },
                  { op: "local.set", index: 4 },
                  // continue loop
                  { op: "br", depth: 0 },
                ],
              },
            ],
          },
          // Append remainder: result = concat(result, s.substring(pos, MAX))
          { op: "local.get", index: 3 },
          { op: "ref.as_non_null" },
          { op: "local.get", index: 0 },
          { op: "local.get", index: 4 },
          { op: "i32.const", value: 2147483647 },
          { op: "call", funcIdx: substringIdx },
          { op: "ref.as_non_null" },
          { op: "call", funcIdx: concatIdx },
        ],
      },
    ];
    ctx.mod.functions.push({
      name: "__str_replaceAll",
      typeIdx,
      locals: [
        { name: "result", type: { kind: "ref_null", typeIdx: anyStrTypeIdx } },
        { name: "pos", type: { kind: "i32" } },
        { name: "idx", type: { kind: "i32" } },
        { name: "searchLen", type: { kind: "i32" } },
        { name: "prefix", type: { kind: "ref_null", typeIdx: anyStrTypeIdx } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1, 2]),
      exported: false,
    });
  }
  {
    const nstrElemKey = `ref_${anyStrTypeIdx}`;
    const nstrElemType = { kind: "ref_null", typeIdx: anyStrTypeIdx };
    const nstrArrTypeIdx = getOrRegisterArrayType(ctx, nstrElemKey, nstrElemType);
    const nstrVecTypeIdx = getOrRegisterVecType(ctx, nstrElemKey, nstrElemType);
    const nstrVecRef = { kind: "ref", typeIdx: nstrVecTypeIdx };
    const typeIdx = addFuncType(ctx, [strRef, strRef], [nstrVecRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_split", funcIdx);
    const indexOfIdx = ctx.nativeStrHelpers.get("__str_indexOf");
    const substringIdx = ctx.nativeStrHelpers.get("__str_substring");
    const S = 0,
      SEP = 1;
    const SLEN = 2,
      SEPLEN = 3,
      POS = 4,
      IDX = 5,
      PART = 6;
    const RARR = 7,
      RLEN = 8,
      RCAP = 9,
      NEWARR = 10;
    const body = [
      // sLen = s.len
      { op: "local.get", index: S },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: SLEN },
      // sepLen = sep.len
      { op: "local.get", index: SEP },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: SEPLEN },
      // resultArr = array.new_default(8)
      { op: "i32.const", value: 8 },
      { op: "array.new_default", typeIdx: nstrArrTypeIdx },
      { op: "local.set", index: RARR },
      { op: "i32.const", value: 0 },
      { op: "local.set", index: RLEN },
      { op: "i32.const", value: 8 },
      { op: "local.set", index: RCAP },
      // pos = 0
      { op: "i32.const", value: 0 },
      { op: "local.set", index: POS },
      // Handle empty separator: return array with single element (the whole string)
      { op: "local.get", index: SEPLEN },
      { op: "i32.eqz" },
      {
        op: "if",
        blockType: { kind: "empty" },
        then: [
          // For empty sep, split each character (like JS)
          // But for simplicity and correctness, match JS: "abc".split("") => ["a","b","c"]
          // Realloc if needed for sLen elements
          { op: "local.get", index: SLEN },
          { op: "array.new_default", typeIdx: nstrArrTypeIdx },
          { op: "local.set", index: RARR },
          { op: "local.get", index: SLEN },
          { op: "local.set", index: RCAP },
          // Loop: for each character, create a single-char NativeString
          { op: "i32.const", value: 0 },
          { op: "local.set", index: POS },
          {
            op: "block",
            blockType: { kind: "empty" },
            body: [
              {
                op: "loop",
                blockType: { kind: "empty" },
                body: [
                  { op: "local.get", index: POS },
                  { op: "local.get", index: SLEN },
                  { op: "i32.ge_s" },
                  { op: "br_if", depth: 1 },
                  // part = substring(s, pos, pos+1)
                  { op: "local.get", index: S },
                  { op: "local.get", index: POS },
                  { op: "local.get", index: POS },
                  { op: "i32.const", value: 1 },
                  { op: "i32.add" },
                  { op: "call", funcIdx: substringIdx },
                  { op: "local.set", index: PART },
                  // resultArr[pos] = part
                  { op: "local.get", index: RARR },
                  { op: "local.get", index: POS },
                  { op: "local.get", index: PART },
                  { op: "array.set", typeIdx: nstrArrTypeIdx },
                  { op: "local.get", index: POS },
                  { op: "i32.const", value: 1 },
                  { op: "i32.add" },
                  { op: "local.set", index: POS },
                  { op: "br", depth: 0 },
                ],
              },
            ],
          },
          // return struct.new(sLen, resultArr)
          { op: "local.get", index: SLEN },
          { op: "local.get", index: RARR },
          { op: "ref.as_non_null" },
          { op: "struct.new", typeIdx: nstrVecTypeIdx },
          { op: "return" },
        ],
      },
      // Main split loop: find sep occurrences and extract substrings
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              // idx = indexOf(s, sep, pos)
              { op: "local.get", index: S },
              { op: "local.get", index: SEP },
              { op: "local.get", index: POS },
              { op: "call", funcIdx: indexOfIdx },
              { op: "local.set", index: IDX },
              // if idx == -1: add final part and break
              { op: "local.get", index: IDX },
              { op: "i32.const", value: -1 },
              { op: "i32.eq" },
              {
                op: "if",
                blockType: { kind: "empty" },
                then: [
                  // part = substring(s, pos, sLen)
                  { op: "local.get", index: S },
                  { op: "local.get", index: POS },
                  { op: "local.get", index: SLEN },
                  { op: "call", funcIdx: substringIdx },
                  { op: "local.set", index: PART },
                  // Grow result if needed
                  { op: "local.get", index: RLEN },
                  { op: "local.get", index: RCAP },
                  { op: "i32.ge_s" },
                  {
                    op: "if",
                    blockType: { kind: "empty" },
                    then: [
                      // newCap = cap * 2
                      { op: "local.get", index: RCAP },
                      { op: "i32.const", value: 2 },
                      { op: "i32.mul" },
                      { op: "local.set", index: RCAP },
                      // newArr = array.new_default(newCap)
                      { op: "local.get", index: RCAP },
                      { op: "array.new_default", typeIdx: nstrArrTypeIdx },
                      { op: "local.set", index: NEWARR },
                      // array.copy(newArr, 0, resultArr, 0, resultLen)
                      { op: "local.get", index: NEWARR },
                      { op: "i32.const", value: 0 },
                      { op: "local.get", index: RARR },
                      { op: "i32.const", value: 0 },
                      { op: "local.get", index: RLEN },
                      { op: "array.copy", dstTypeIdx: nstrArrTypeIdx, srcTypeIdx: nstrArrTypeIdx },
                      { op: "local.get", index: NEWARR },
                      { op: "local.set", index: RARR },
                    ],
                  },
                  // resultArr[resultLen] = part
                  { op: "local.get", index: RARR },
                  { op: "local.get", index: RLEN },
                  { op: "local.get", index: PART },
                  { op: "array.set", typeIdx: nstrArrTypeIdx },
                  { op: "local.get", index: RLEN },
                  { op: "i32.const", value: 1 },
                  { op: "i32.add" },
                  { op: "local.set", index: RLEN },
                  { op: "br", depth: 2 },
                  // break outer block
                ],
              },
              // Found separator: part = substring(s, pos, idx)
              { op: "local.get", index: S },
              { op: "local.get", index: POS },
              { op: "local.get", index: IDX },
              { op: "call", funcIdx: substringIdx },
              { op: "local.set", index: PART },
              // Grow result if needed
              { op: "local.get", index: RLEN },
              { op: "local.get", index: RCAP },
              { op: "i32.ge_s" },
              {
                op: "if",
                blockType: { kind: "empty" },
                then: [
                  { op: "local.get", index: RCAP },
                  { op: "i32.const", value: 2 },
                  { op: "i32.mul" },
                  { op: "local.set", index: RCAP },
                  { op: "local.get", index: RCAP },
                  { op: "array.new_default", typeIdx: nstrArrTypeIdx },
                  { op: "local.set", index: NEWARR },
                  { op: "local.get", index: NEWARR },
                  { op: "i32.const", value: 0 },
                  { op: "local.get", index: RARR },
                  { op: "i32.const", value: 0 },
                  { op: "local.get", index: RLEN },
                  { op: "array.copy", dstTypeIdx: nstrArrTypeIdx, srcTypeIdx: nstrArrTypeIdx },
                  { op: "local.get", index: NEWARR },
                  { op: "local.set", index: RARR },
                ],
              },
              // resultArr[resultLen] = part
              { op: "local.get", index: RARR },
              { op: "local.get", index: RLEN },
              { op: "local.get", index: PART },
              { op: "array.set", typeIdx: nstrArrTypeIdx },
              { op: "local.get", index: RLEN },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: RLEN },
              // pos = idx + sepLen
              { op: "local.get", index: IDX },
              { op: "local.get", index: SEPLEN },
              { op: "i32.add" },
              { op: "local.set", index: POS },
              { op: "br", depth: 0 },
              // continue loop
            ],
          },
        ],
      },
      // return struct.new(resultLen, resultArr)
      { op: "local.get", index: RLEN },
      { op: "local.get", index: RARR },
      { op: "ref.as_non_null" },
      { op: "struct.new", typeIdx: nstrVecTypeIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_split",
      typeIdx,
      locals: [
        { name: "sLen", type: { kind: "i32" } },
        { name: "sepLen", type: { kind: "i32" } },
        { name: "pos", type: { kind: "i32" } },
        { name: "idx", type: { kind: "i32" } },
        { name: "part", type: { kind: "ref_null", typeIdx: anyStrTypeIdx } },
        { name: "resultArr", type: { kind: "ref_null", typeIdx: nstrArrTypeIdx } },
        { name: "resultLen", type: { kind: "i32" } },
        { name: "resultCap", type: { kind: "i32" } },
        { name: "newArr", type: { kind: "ref_null", typeIdx: nstrArrTypeIdx } },
      ],
      body: wrapBodyWithFlatten(body, [0, 1]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [strRef], [{ kind: "externref" }]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_to_extern", funcIdx);
    ctx.funcMap.set("__str_to_extern", funcIdx);
    const fromMemIdx = ctx.funcMap.get("__str_from_mem");
    const body = [
      // len = s.len
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 0 },
      { op: "local.set", index: 1 },
      // len
      // sOff = s.off
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 1 },
      { op: "local.set", index: 4 },
      // sOff
      // data = s.data
      { op: "local.get", index: 0 },
      { op: "struct.get", typeIdx: strTypeIdx, fieldIdx: 2 },
      { op: "local.set", index: 3 },
      // data
      // i = 0
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 2 },
      // i
      // loop: copy code units from GC array to linear memory
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              // if i >= len, break
              { op: "local.get", index: 2 },
              { op: "local.get", index: 1 },
              { op: "i32.ge_u" },
              { op: "br_if", depth: 1 },
              // memory[i*2] = data[sOff + i] (i32.store16)
              { op: "local.get", index: 2 },
              { op: "i32.const", value: 1 },
              { op: "i32.shl" },
              // ptr = i * 2
              { op: "local.get", index: 3 },
              { op: "local.get", index: 4 },
              { op: "local.get", index: 2 },
              { op: "i32.add" },
              // sOff + i
              { op: "array.get_u", typeIdx: strDataTypeIdx },
              { op: "i32.store16", align: 1, offset: 0 },
              // i++
              { op: "local.get", index: 2 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 2 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // return __str_from_mem(0, len)
      { op: "i32.const", value: 0 },
      { op: "local.get", index: 1 },
      { op: "call", funcIdx: fromMemIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_to_extern",
      typeIdx,
      locals: [
        { name: "len", type: { kind: "i32" } },
        { name: "i", type: { kind: "i32" } },
        { name: "data", type: strDataRef },
        { name: "sOff", type: { kind: "i32" } },
      ],
      body: wrapBodyWithFlatten(body, [0]),
      exported: false,
    });
  }
  {
    const typeIdx = addFuncType(ctx, [{ kind: "externref" }], [strRef]);
    const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.nativeStrHelpers.set("__str_from_extern", funcIdx);
    ctx.funcMap.set("__str_from_extern", funcIdx);
    const toMemIdx = ctx.funcMap.get("__str_to_mem");
    const externLenIdx = ctx.funcMap.get("__str_extern_len");
    const body = [
      // len = __str_extern_len(s)
      { op: "local.get", index: 0 },
      { op: "call", funcIdx: externLenIdx },
      { op: "local.set", index: 1 },
      // len
      // __str_to_mem(s, 0) — host writes code units to linear memory at ptr=0
      { op: "local.get", index: 0 },
      { op: "i32.const", value: 0 },
      { op: "call", funcIdx: toMemIdx },
      // arr = array.new_default len
      { op: "local.get", index: 1 },
      { op: "array.new_default", typeIdx: strDataTypeIdx },
      { op: "local.set", index: 2 },
      // arr
      // i = 0
      { op: "i32.const", value: 0 },
      { op: "local.set", index: 3 },
      // i
      // loop: copy from linear memory to GC array
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              // if i >= len, break
              { op: "local.get", index: 3 },
              { op: "local.get", index: 1 },
              { op: "i32.ge_u" },
              { op: "br_if", depth: 1 },
              // arr[i] = memory[i*2] (i32.load16_u)
              { op: "local.get", index: 2 },
              { op: "local.get", index: 3 },
              { op: "local.get", index: 3 },
              { op: "i32.const", value: 1 },
              { op: "i32.shl" },
              // ptr = i * 2
              { op: "i32.load16_u", align: 1, offset: 0 },
              { op: "array.set", typeIdx: strDataTypeIdx },
              // i++
              { op: "local.get", index: 3 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: 3 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // struct.new(len, 0, arr)
      { op: "local.get", index: 1 },
      // len
      { op: "i32.const", value: 0 },
      // off = 0
      { op: "local.get", index: 2 },
      // data = arr
      { op: "struct.new", typeIdx: strTypeIdx },
    ];
    ctx.mod.functions.push({
      name: "__str_from_extern",
      typeIdx,
      locals: [
        { name: "len", type: { kind: "i32" } },
        { name: "arr", type: strDataRef },
        { name: "i", type: { kind: "i32" } },
      ],
      body,
      exported: false,
    });
  }
}
function parseRegExpLiteral(text) {
  const lastSlash = text.lastIndexOf("/");
  const pattern = text.slice(1, lastSlash);
  const flags = text.slice(lastSlash + 1);
  return { pattern, flags };
}
var MATH_HOST_METHODS_1ARG = /* @__PURE__ */ new Set([
  "exp",
  "log",
  "log2",
  "log10",
  "sin",
  "cos",
  "tan",
  "asin",
  "acos",
  "atan",
  "sinh",
  "cosh",
  "tanh",
  "acosh",
  "asinh",
  "atanh",
  "cbrt",
  "expm1",
  "log1p",
]);
var MATH_HOST_METHODS_2ARG = /* @__PURE__ */ new Set(["pow", "atan2"]);
var KNOWN_CONSTRUCTORS = /* @__PURE__ */ new Set([
  "Array",
  "Date",
  "Map",
  "Set",
  "RegExp",
  "Error",
  "TypeError",
  "RangeError",
  "SyntaxError",
  "URIError",
  "EvalError",
  "ReferenceError",
  "Test262Error",
  "Object",
  "Function",
  "Promise",
  "WeakMap",
  "WeakSet",
  "WeakRef",
  "Number",
  "String",
  "Boolean",
  "ArrayBuffer",
  "DataView",
  "Proxy",
  "Int8Array",
  "Uint8Array",
  "Uint8ClampedArray",
  "Int16Array",
  "Uint16Array",
  "Int32Array",
  "Uint32Array",
  "Float32Array",
  "Float64Array",
  "SyntaxError",
  "URIError",
  "EvalError",
  "ReferenceError",
]);
function isPromiseHostCallExpr(expr, ctx) {
  if (ts15.isCallExpression(expr) && ts15.isPropertyAccessExpression(expr.expression)) {
    const method = expr.expression.name.text;
    if (
      ts15.isIdentifier(expr.expression.expression) &&
      expr.expression.expression.text === "Promise" &&
      (method === "resolve" ||
        method === "reject" ||
        method === "all" ||
        method === "race" ||
        method === "allSettled" ||
        method === "any")
    ) {
      return true;
    }
    if (method === "then" || method === "catch" || method === "finally") {
      const receiverType = ctx.checker.getTypeAtLocation(expr.expression.expression);
      const symName = receiverType.getSymbol()?.name;
      if (symName === "Promise") return true;
      const apparent = ctx.checker.getApparentType(receiverType);
      if (apparent.getSymbol()?.name === "Promise") return true;
    }
  }
  if (ts15.isNewExpression(expr) && ts15.isIdentifier(expr.expression) && expr.expression.text === "Promise") {
    return true;
  }
  return false;
}
var FUNCTIONAL_ARRAY_METHODS = /* @__PURE__ */ new Set([
  "filter",
  "map",
  "reduce",
  "forEach",
  "find",
  "findIndex",
  "some",
  "every",
]);
function addUnionImports(ctx) {
  if (ctx.hasUnionImports) return;
  ctx.hasUnionImports = true;
  const importsBefore = ctx.numImportFuncs;
  const typeofType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }]);
  addImport(ctx, "env", "__typeof_number", {
    kind: "func",
    typeIdx: typeofType,
  });
  addImport(ctx, "env", "__typeof_string", {
    kind: "func",
    typeIdx: typeofType,
  });
  addImport(ctx, "env", "__typeof_boolean", {
    kind: "func",
    typeIdx: typeofType,
  });
  addImport(ctx, "env", "__typeof_undefined", {
    kind: "func",
    typeIdx: typeofType,
  });
  addImport(ctx, "env", "__typeof_object", {
    kind: "func",
    typeIdx: typeofType,
  });
  addImport(ctx, "env", "__typeof_function", {
    kind: "func",
    typeIdx: typeofType,
  });
  addImport(ctx, "env", "__is_truthy", { kind: "func", typeIdx: typeofType });
  const unboxNumType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "f64" }]);
  addImport(ctx, "env", "__unbox_number", {
    kind: "func",
    typeIdx: unboxNumType,
  });
  addImport(ctx, "env", "__unbox_boolean", {
    kind: "func",
    typeIdx: typeofType,
  });
  const boxNumType = addFuncType(ctx, [{ kind: "f64" }], [{ kind: "externref" }]);
  addImport(ctx, "env", "__box_number", { kind: "func", typeIdx: boxNumType });
  const boxBoolType = addFuncType(ctx, [{ kind: "i32" }], [{ kind: "externref" }]);
  addImport(ctx, "env", "__box_boolean", {
    kind: "func",
    typeIdx: boxBoolType,
  });
  const typeofStrType = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
  addImport(ctx, "env", "__typeof", {
    kind: "func",
    typeIdx: typeofStrType,
  });
  const delta = ctx.numImportFuncs - importsBefore;
  if (delta > 0 && ctx.mod.functions.length > 0) {
    let shiftFuncIndices2 = function (instrs) {
      for (const instr of instrs) {
        if ((instr.op === "call" || instr.op === "return_call") && instr.funcIdx >= importsBefore) {
          instr.funcIdx += delta;
        }
        if (instr.op === "ref.func" && instr.funcIdx >= importsBefore) {
          instr.funcIdx += delta;
        }
        if ("body" in instr && Array.isArray(instr.body)) {
          shiftFuncIndices2(instr.body);
        }
        if ("then" in instr && Array.isArray(instr.then)) {
          shiftFuncIndices2(instr.then);
        }
        if ("else" in instr && Array.isArray(instr.else)) {
          shiftFuncIndices2(instr.else);
        }
        if ("catches" in instr && Array.isArray(instr.catches)) {
          for (const c of instr.catches) {
            if (Array.isArray(c.body)) shiftFuncIndices2(c.body);
          }
        }
        if ("catchAll" in instr && Array.isArray(instr.catchAll)) {
          shiftFuncIndices2(instr.catchAll);
        }
      }
    };
    var shiftFuncIndices = shiftFuncIndices2;
    const newImportNames = /* @__PURE__ */ new Set([
      "__typeof_number",
      "__typeof_string",
      "__typeof_boolean",
      "__typeof_undefined",
      "__typeof_object",
      "__typeof_function",
      "__is_truthy",
      "__unbox_number",
      "__unbox_boolean",
      "__box_number",
      "__box_boolean",
      "__typeof",
    ]);
    for (const [name, idx] of ctx.funcMap) {
      if (!newImportNames.has(name) && idx >= importsBefore) {
        ctx.funcMap.set(name, idx + delta);
      }
    }
    for (const exp of ctx.mod.exports) {
      if (exp.desc.kind === "func" && exp.desc.index >= importsBefore) {
        exp.desc.index += delta;
      }
    }
    const shifted = /* @__PURE__ */ new Set();
    for (const func of ctx.mod.functions) {
      if (!shifted.has(func.body)) {
        shiftFuncIndices2(func.body);
        shifted.add(func.body);
      }
    }
    if (ctx.currentFunc) {
      if (!shifted.has(ctx.currentFunc.body)) {
        shiftFuncIndices2(ctx.currentFunc.body);
        shifted.add(ctx.currentFunc.body);
      }
      for (const sb of ctx.currentFunc.savedBodies) {
        if (shifted.has(sb)) continue;
        shiftFuncIndices2(sb);
        shifted.add(sb);
      }
    }
    for (const parentFctx of ctx.funcStack) {
      if (!shifted.has(parentFctx.body)) {
        shiftFuncIndices2(parentFctx.body);
        shifted.add(parentFctx.body);
      }
      for (const sb of parentFctx.savedBodies) {
        if (!shifted.has(sb)) {
          shiftFuncIndices2(sb);
          shifted.add(sb);
        }
      }
    }
    for (const pb of ctx.parentBodiesStack) {
      if (!shifted.has(pb)) {
        shiftFuncIndices2(pb);
        shifted.add(pb);
      }
    }
    if (ctx.pendingInitBody && !shifted.has(ctx.pendingInitBody)) {
      shiftFuncIndices2(ctx.pendingInitBody);
      shifted.add(ctx.pendingInitBody);
    }
    for (const elem of ctx.mod.elements) {
      if (elem.funcIndices) {
        for (let i = 0; i < elem.funcIndices.length; i++) {
          if (elem.funcIndices[i] >= importsBefore) {
            elem.funcIndices[i] += delta;
          }
        }
      }
    }
    if (ctx.mod.declaredFuncRefs.length > 0) {
      ctx.mod.declaredFuncRefs = ctx.mod.declaredFuncRefs.map((idx) => (idx >= importsBefore ? idx + delta : idx));
    }
  }
}
function addIteratorImports(ctx) {
  if (ctx.funcMap.has("__iterator")) return;
  const extToExt = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
  addImport(ctx, "env", "__iterator", { kind: "func", typeIdx: extToExt });
  addImport(ctx, "env", "__iterator_next", {
    kind: "func",
    typeIdx: extToExt,
  });
  const extToI32 = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }]);
  addImport(ctx, "env", "__iterator_done", {
    kind: "func",
    typeIdx: extToI32,
  });
  addImport(ctx, "env", "__iterator_value", {
    kind: "func",
    typeIdx: extToExt,
  });
  const extToVoid = addFuncType(ctx, [{ kind: "externref" }], []);
  addImport(ctx, "env", "__iterator_return", {
    kind: "func",
    typeIdx: extToVoid,
  });
}
function addArrayIteratorImports(ctx) {
  if (ctx.funcMap.has("__array_entries")) return;
  const extToExt = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
  addImport(ctx, "env", "__array_entries", { kind: "func", typeIdx: extToExt });
  addImport(ctx, "env", "__array_keys", { kind: "func", typeIdx: extToExt });
  addImport(ctx, "env", "__array_values", { kind: "func", typeIdx: extToExt });
}
function addForInImports(ctx) {
  if (ctx.funcMap.has("__for_in_keys")) return;
  const extToExt = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "externref" }]);
  addImport(ctx, "env", "__for_in_keys", { kind: "func", typeIdx: extToExt });
  const extToI32 = addFuncType(ctx, [{ kind: "externref" }], [{ kind: "i32" }]);
  addImport(ctx, "env", "__for_in_len", { kind: "func", typeIdx: extToI32 });
  const extI32ToExt = addFuncType(ctx, [{ kind: "externref" }, { kind: "i32" }], [{ kind: "externref" }]);
  addImport(ctx, "env", "__for_in_get", { kind: "func", typeIdx: extI32ToExt });
}
function boxToExternref(ctx, elemKey) {
  if (elemKey === "externref") {
    return [];
  }
  if (elemKey === "f64") {
    addUnionImports(ctx);
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx !== void 0) {
      return [{ op: "call", funcIdx: boxIdx }];
    }
    return [{ op: "drop" }, { op: "ref.null.extern" }];
  }
  if (elemKey === "i32") {
    addUnionImports(ctx);
    const boxIdx = ctx.funcMap.get("__box_number");
    if (boxIdx !== void 0) {
      return [{ op: "f64.convert_i32_s" }, { op: "call", funcIdx: boxIdx }];
    }
    return [{ op: "drop" }, { op: "ref.null.extern" }];
  }
  return [{ op: "extern.convert_any" }];
}
function isTupleType(type) {
  if (!(type.flags & ts15.TypeFlags.Object)) return false;
  const objType = type;
  if ((objType.objectFlags & ts15.ObjectFlags.Tuple) !== 0) return true;
  if ((objType.objectFlags & ts15.ObjectFlags.Reference) !== 0) {
    const ref = type;
    if (ref.target && (ref.target.objectFlags & ts15.ObjectFlags.Tuple) !== 0) {
      return true;
    }
  }
  return false;
}
function getTupleElementTypes(ctx, tsType) {
  const typeRef = tsType;
  const typeArgs = ctx.checker.getTypeArguments(typeRef);
  return typeArgs.map((t) => resolveWasmType(ctx, t));
}
function tupleTypeKey(elemTypes) {
  return elemTypes
    .map((t) => {
      if (t.kind === "ref" || t.kind === "ref_null") return `${t.kind}_${t.typeIdx}`;
      return t.kind;
    })
    .join(",");
}
function getOrRegisterTupleType(ctx, elemTypes) {
  const key = tupleTypeKey(elemTypes);
  const existing = ctx.tupleTypeMap.get(key);
  if (existing !== void 0) return existing;
  const fields = elemTypes.map((t, i) => ({
    name: `_${i}`,
    type: t,
    mutable: false,
  }));
  const typeIdx = ctx.mod.types.length;
  const structName = `__tuple_${ctx.tupleTypeMap.size}`;
  ctx.mod.types.push({
    kind: "struct",
    name: structName,
    fields,
  });
  ctx.tupleTypeMap.set(key, typeIdx);
  ctx.structMap.set(structName, typeIdx);
  ctx.structFields.set(
    structName,
    fields.map((f) => ({
      name: f.name,
      type: f.type,
      mutable: f.mutable ?? false,
    })),
  );
  return typeIdx;
}
var NATIVE_TYPE_MAP = {
  i32: { kind: "i32" },
  u8: { kind: "i32" },
  // unsigned 8-bit — stored as i32 (masked at boundaries)
  u16: { kind: "i32" },
  // unsigned 16-bit — stored as i32 (masked at boundaries)
  u32: { kind: "i32" },
  // unsigned 32-bit — stored as i32
  i8: { kind: "i32" },
  // signed 8-bit — stored as i32
  i16: { kind: "i32" },
  // signed 16-bit — stored as i32
  f32: { kind: "f32" },
  f64: { kind: "f64" },
  // i64 intentionally omitted — requires BigInt integration, not yet supported
};
function resolveNativeTypeAnnotation(tsType) {
  const aliasName = tsType.aliasSymbol?.name;
  if (aliasName && aliasName in NATIVE_TYPE_MAP) {
    const flags = tsType.flags;
    if (flags & ts15.TypeFlags.Number || flags & ts15.TypeFlags.NumberLiteral) {
      return NATIVE_TYPE_MAP[aliasName];
    }
  }
  return null;
}
function resolveWasmType(ctx, tsType, _depth = 0, _visited) {
  if (_depth > 10) return { kind: "externref" };
  if (_visited && _visited.has(tsType)) return { kind: "externref" };
  if (!_visited) _visited = /* @__PURE__ */ new Set();
  _visited.add(tsType);
  const nativeType = resolveNativeTypeAnnotation(tsType);
  if (nativeType) return nativeType;
  if (ctx.nativeStrings && ctx.anyStrTypeIdx >= 0 && isStringType(tsType)) {
    return { kind: "ref", typeIdx: ctx.anyStrTypeIdx };
  }
  if (isTupleType(tsType)) {
    const elemTypes = getTupleElementTypes(ctx, tsType);
    const tupleIdx = getOrRegisterTupleType(ctx, elemTypes);
    return { kind: "ref", typeIdx: tupleIdx };
  }
  if (tsType.flags & ts15.TypeFlags.Object) {
    const sym = tsType.symbol ?? tsType.symbol;
    if (sym?.name === "Array") {
      const typeArgs = ctx.checker.getTypeArguments(tsType);
      const elemTsType = typeArgs[0];
      const elemWasm = elemTsType ? resolveWasmType(ctx, elemTsType, _depth + 1, _visited) : { kind: "externref" };
      const elemKey =
        elemWasm.kind === "ref" || elemWasm.kind === "ref_null" ? `ref_${elemWasm.typeIdx}` : elemWasm.kind;
      const vecIdx = getOrRegisterVecType(ctx, elemKey, elemWasm);
      return { kind: "ref_null", typeIdx: vecIdx };
    }
    if (sym?.name === "Number" && tsType.flags & ts15.TypeFlags.Object) {
      return { kind: "externref" };
    }
    if (sym?.name === "String" && tsType.flags & ts15.TypeFlags.Object) {
      return { kind: "externref" };
    }
    if (sym?.name === "Boolean" && tsType.flags & ts15.TypeFlags.Object) {
      return { kind: "externref" };
    }
    if (sym?.name === "Promise") {
      const typeArgs = ctx.checker.getTypeArguments(tsType);
      if (typeArgs.length > 0) {
        const inner = typeArgs[0];
        if (isVoidType(inner)) return { kind: "externref" };
        return resolveWasmType(ctx, inner, _depth + 1, _visited);
      }
      return { kind: "externref" };
    }
    const TYPED_ARRAY_NAMES = /* @__PURE__ */ new Set([
      "Int8Array",
      "Uint8Array",
      "Uint8ClampedArray",
      "Int16Array",
      "Uint16Array",
      "Int32Array",
      "Uint32Array",
      "Float32Array",
      "Float64Array",
    ]);
    if (sym?.name && TYPED_ARRAY_NAMES.has(sym.name)) {
      const elemWasm = { kind: "f64" };
      const vecIdx = getOrRegisterVecType(ctx, "f64", elemWasm);
      return { kind: "ref_null", typeIdx: vecIdx };
    }
    if (sym?.name === "Date") {
      const dateTypeIdx = ensureDateStructForCtx(ctx);
      return { kind: "ref", typeIdx: dateTypeIdx };
    }
    if (isExternalDeclaredClass(tsType, ctx.checker)) return { kind: "externref" };
    let name = sym?.name;
    if (name && !ctx.structMap.has(name)) {
      name = ctx.classExprNameMap.get(name) ?? name;
    }
    if (name && name !== "__type" && name !== "__object" && ctx.structMap.has(name)) {
      return { kind: "ref", typeIdx: ctx.structMap.get(name) };
    }
    const anonName = ctx.anonTypeMap.get(tsType);
    if (anonName && ctx.structMap.has(anonName)) {
      return { kind: "ref", typeIdx: ctx.structMap.get(anonName) };
    }
    if (!anonName && (name === "__type" || name === "__object") && tsType.getProperties().length > 0) {
      ensureStructForType(ctx, tsType);
      const registeredName = ctx.anonTypeMap.get(tsType);
      if (registeredName && ctx.structMap.has(registeredName)) {
        return { kind: "ref", typeIdx: ctx.structMap.get(registeredName) };
      }
    }
  }
  if (tsType.isUnion()) {
    const nonNullish = tsType.types.filter(
      (t) => !(t.flags & ts15.TypeFlags.Null) && !(t.flags & ts15.TypeFlags.Undefined),
    );
    if (nonNullish.length === 1 && tsType.types.length === 2) {
      const inner = resolveWasmType(ctx, nonNullish[0], _depth + 1, _visited);
      if (inner.kind === "ref") return { kind: "ref_null", typeIdx: inner.typeIdx };
      return inner;
    }
  }
  if (ctx.fast && tsType.flags & (ts15.TypeFlags.Any | ts15.TypeFlags.Unknown)) {
    ensureAnyValueType(ctx);
    return { kind: "ref_null", typeIdx: ctx.anyValueTypeIdx };
  }
  return mapTsTypeToWasm(tsType, ctx.checker, ctx.fast);
}
function fieldsHashKey(fields) {
  const parts = [];
  for (const f of fields) {
    const t = f.type;
    if (t.kind === "ref" || t.kind === "ref_null") {
      parts.push(`${f.name}:${t.kind}:${t.typeIdx}`);
    } else {
      parts.push(`${f.name}:${t.kind}`);
    }
  }
  return parts.join("|");
}
function ensureDateStructForCtx(ctx) {
  const existing = ctx.structMap.get("__Date");
  if (existing !== void 0) return existing;
  const typeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: "__Date",
    fields: [{ name: "timestamp", type: { kind: "i64" }, mutable: true }],
  });
  ctx.structMap.set("__Date", typeIdx);
  ctx.typeIdxToStructName.set(typeIdx, "__Date");
  ctx.structFields.set("__Date", [{ name: "timestamp", type: { kind: "i64" }, mutable: true }]);
  return typeIdx;
}
var _ensureStructPending = /* @__PURE__ */ new WeakSet();
function ensureStructForType(ctx, tsType) {
  if (!(tsType.flags & ts15.TypeFlags.Object)) return;
  if (isExternalDeclaredClass(tsType, ctx.checker)) return;
  if (isTupleType(tsType)) return;
  if (tsType.getCallSignatures().length > 0) return;
  if (_ensureStructPending.has(tsType)) return;
  _ensureStructPending.add(tsType);
  const name = tsType.symbol?.name;
  if (name && name !== "__type" && name !== "__object" && ctx.structMap.has(name)) return;
  if (ctx.anonTypeMap.has(tsType)) return;
  const props = tsType.getProperties();
  const fields = [];
  for (const prop of props) {
    const propType = ctx.checker.getTypeOfSymbol(prop);
    ensureStructForType(ctx, propType);
    let wasmType = resolveWasmType(ctx, propType);
    if (
      wasmType.kind === "externref" &&
      propType.getCallSignatures().length > 0 &&
      (prop.name === "valueOf" || prop.name === "toString")
    ) {
      wasmType = { kind: "eqref" };
    }
    fields.push({ name: prop.name, type: wasmType, mutable: true });
  }
  const hashKey = fieldsHashKey(fields);
  const existingName = ctx.anonStructHash.get(hashKey);
  if (existingName) {
    ctx.anonTypeMap.set(tsType, existingName);
    return;
  }
  for (const field of fields) {
    if (field.type.kind === "ref") {
      field.type = { kind: "ref_null", typeIdx: field.type.typeIdx };
    }
  }
  const structName = `__anon_${ctx.anonTypeCounter++}`;
  const typeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name: structName,
    fields,
  });
  ctx.structMap.set(structName, typeIdx);
  ctx.typeIdxToStructName.set(typeIdx, structName);
  ctx.structFields.set(structName, fields);
  ctx.anonStructHash.set(hashKey, structName);
  ctx.anonTypeMap.set(tsType, structName);
  for (const prop of props) {
    const propType = ctx.checker.getTypeOfSymbol(prop);
    const callSigs = propType.getCallSignatures();
    if (callSigs.length === 0) continue;
    const decl = prop.valueDeclaration;
    if (!decl) continue;
    if (!ts15.isMethodDeclaration(decl)) continue;
    const declSourceFile = decl.getSourceFile();
    if (declSourceFile && declSourceFile.isDeclarationFile) continue;
    const fullName = `${structName}_${prop.name}`;
    if (ctx.funcMap.has(fullName)) continue;
    const sig = callSigs[0];
    const methodParams = [{ kind: "ref", typeIdx }];
    for (const param of sig.parameters) {
      const paramDecl = param.valueDeclaration;
      if (paramDecl) {
        const pt = ctx.checker.getTypeAtLocation(paramDecl);
        methodParams.push(resolveWasmType(ctx, pt));
      } else {
        methodParams.push({ kind: "f64" });
      }
    }
    const isGenMethod = ts15.isMethodDeclaration(decl) && decl.asteriskToken !== void 0;
    const retType = ctx.checker.getReturnTypeOfSignature(sig);
    const methodResults = isGenMethod
      ? [{ kind: "externref" }]
      : retType && !isVoidType(retType)
        ? [resolveWasmType(ctx, retType)]
        : [];
    const methodTypeIdx = addFuncType(ctx, methodParams, methodResults, `${fullName}_type`);
    const methodFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
    ctx.funcMap.set(fullName, methodFuncIdx);
    const methodFunc = {
      name: fullName,
      typeIdx: methodTypeIdx,
      locals: [],
      body: [],
      exported: false,
    };
    ctx.mod.functions.push(methodFunc);
  }
}
function externMethod(paramCount, returnsExternref = true) {
  const params = [];
  for (let i = 0; i <= paramCount; i++) params.push({ kind: "externref" });
  return {
    params,
    results: returnsExternref ? [{ kind: "externref" }] : [],
    requiredParams: params.length,
  };
}
function registerBuiltinExternClasses(ctx) {
  if (!ctx.externClasses.has("Set")) {
    const methods = /* @__PURE__ */ new Map();
    methods.set("add", externMethod(1));
    methods.set("has", externMethod(1));
    methods.set("delete", externMethod(1));
    methods.set("clear", externMethod(0, false));
    methods.set("forEach", externMethod(1));
    methods.set("entries", externMethod(0));
    methods.set("keys", externMethod(0));
    methods.set("values", externMethod(0));
    methods.set("union", externMethod(1));
    methods.set("intersection", externMethod(1));
    methods.set("difference", externMethod(1));
    methods.set("symmetricDifference", externMethod(1));
    methods.set("isSubsetOf", externMethod(1));
    methods.set("isSupersetOf", externMethod(1));
    methods.set("isDisjointFrom", externMethod(1));
    ctx.externClasses.set("Set", {
      importPrefix: "Set",
      namespacePath: [],
      className: "Set",
      constructorParams: [{ kind: "externref" }],
      // new Set(iterable?)
      methods,
      properties: /* @__PURE__ */ new Map([["size", { type: { kind: "externref" }, readonly: true }]]),
    });
  }
  if (!ctx.externClasses.has("Map")) {
    const methods = /* @__PURE__ */ new Map();
    methods.set("get", externMethod(1));
    methods.set("set", externMethod(2));
    methods.set("has", externMethod(1));
    methods.set("delete", externMethod(1));
    methods.set("clear", externMethod(0, false));
    methods.set("forEach", externMethod(1));
    methods.set("entries", externMethod(0));
    methods.set("keys", externMethod(0));
    methods.set("values", externMethod(0));
    ctx.externClasses.set("Map", {
      importPrefix: "Map",
      namespacePath: [],
      className: "Map",
      constructorParams: [{ kind: "externref" }],
      methods,
      properties: /* @__PURE__ */ new Map([["size", { type: { kind: "externref" }, readonly: true }]]),
    });
  }
  if (!ctx.externClasses.has("WeakMap")) {
    const methods = /* @__PURE__ */ new Map();
    methods.set("get", externMethod(1));
    methods.set("set", externMethod(2));
    methods.set("has", externMethod(1));
    methods.set("delete", externMethod(1));
    ctx.externClasses.set("WeakMap", {
      importPrefix: "WeakMap",
      namespacePath: [],
      className: "WeakMap",
      constructorParams: [{ kind: "externref" }],
      methods,
      properties: /* @__PURE__ */ new Map(),
    });
  }
  if (!ctx.externClasses.has("WeakSet")) {
    const methods = /* @__PURE__ */ new Map();
    methods.set("add", externMethod(1));
    methods.set("has", externMethod(1));
    methods.set("delete", externMethod(1));
    ctx.externClasses.set("WeakSet", {
      importPrefix: "WeakSet",
      namespacePath: [],
      className: "WeakSet",
      constructorParams: [{ kind: "externref" }],
      methods,
      properties: /* @__PURE__ */ new Map(),
    });
  }
}
function collectExternDeclarations(ctx, sourceFile) {
  for (const stmt of sourceFile.statements) {
    if (ts15.isModuleDeclaration(stmt) && hasDeclareModifier(stmt)) {
      collectDeclareNamespace(ctx, stmt, []);
    }
    if (ts15.isClassDeclaration(stmt) && stmt.name && hasDeclareModifier(stmt)) {
      collectExternClass(ctx, stmt, []);
    }
    if (ts15.isVariableStatement(stmt) && hasDeclareModifier(stmt)) {
      for (const decl of stmt.declarationList.declarations) {
        if (!decl.name || !ts15.isIdentifier(decl.name) || !decl.type) continue;
        if (
          ts15.isTypeLiteralNode(decl.type) &&
          decl.type.members.some((m) => ts15.isConstructSignatureDeclaration(m))
        ) {
          collectExternFromDeclareVar(ctx, decl);
        } else if (ts15.isTypeReferenceNode(decl.type)) {
          const varName = decl.name.text;
          const BUILTIN_SKIP = /* @__PURE__ */ new Set([
            "Array",
            "Number",
            "Boolean",
            "String",
            "Object",
            "Function",
            "Symbol",
            "BigInt",
            "Int8Array",
            "Uint8Array",
            "Int16Array",
            "Uint16Array",
            "Int32Array",
            "Uint32Array",
            "Float32Array",
            "Float64Array",
            "ArrayBuffer",
            "DataView",
            "JSON",
            "Math",
            "Error",
            "TypeError",
            "RangeError",
            "SyntaxError",
            "URIError",
            "EvalError",
            "ReferenceError",
          ]);
          if (!BUILTIN_SKIP.has(varName)) {
            const refType = ctx.checker.getTypeAtLocation(decl.type);
            const constructSigs = refType.getConstructSignatures();
            if (constructSigs.length > 0) {
              collectExternFromDeclareVar(ctx, decl);
            }
          }
        }
      }
    }
  }
}
function collectDeclareNamespace(ctx, decl, parentPath) {
  const nsName = decl.name.text;
  const path = [...parentPath, nsName];
  if (decl.body && ts15.isModuleBlock(decl.body)) {
    for (const stmt of decl.body.statements) {
      if (ts15.isClassDeclaration(stmt) && stmt.name) {
        collectExternClass(ctx, stmt, path);
      }
      if (ts15.isModuleDeclaration(stmt)) {
        collectDeclareNamespace(ctx, stmt, path);
      }
    }
  }
}
function collectExternClass(ctx, decl, namespacePath) {
  const className = decl.name.text;
  if (ERROR_TYPES_SKIP.has(className)) return;
  const prefix = [...namespacePath, className].join("_");
  const info = {
    importPrefix: prefix,
    namespacePath,
    className,
    constructorParams: [],
    methods: /* @__PURE__ */ new Map(),
    properties: /* @__PURE__ */ new Map(),
  };
  for (const member of decl.members) {
    if (ts15.isConstructorDeclaration(member)) {
      for (const param of member.parameters) {
        const paramType = ctx.checker.getTypeAtLocation(param);
        info.constructorParams.push(mapTsTypeToWasm(paramType, ctx.checker));
      }
    }
    if (ts15.isMethodDeclaration(member) && member.name) {
      const methodName = member.name.text;
      const sig = ctx.checker.getSignatureFromDeclaration(member);
      if (sig) {
        const params = [{ kind: "externref" }];
        let requiredParams = 1;
        for (const p of member.parameters) {
          const pt = ctx.checker.getTypeAtLocation(p);
          params.push(mapTsTypeToWasm(pt, ctx.checker));
          if (!p.questionToken && !p.initializer) requiredParams++;
        }
        const retType = ctx.checker.getReturnTypeOfSignature(sig);
        const results = isVoidType(retType) ? [] : [mapTsTypeToWasm(retType, ctx.checker)];
        info.methods.set(methodName, { params, results, requiredParams });
      }
    }
    if (ts15.isPropertyDeclaration(member) && member.name) {
      const propName = member.name.text;
      const propType = ctx.checker.getTypeAtLocation(member);
      const wasmType = mapTsTypeToWasm(propType, ctx.checker);
      const isReadonly = member.modifiers?.some((m) => m.kind === ts15.SyntaxKind.ReadonlyKeyword) ?? false;
      info.properties.set(propName, { type: wasmType, readonly: isReadonly });
    }
  }
  if (decl.heritageClauses) {
    for (const clause of decl.heritageClauses) {
      if (clause.token === ts15.SyntaxKind.ExtendsKeyword && clause.types[0]) {
        const baseType = ctx.checker.getTypeAtLocation(clause.types[0]);
        const baseName = baseType.getSymbol()?.name;
        if (baseName) ctx.externClassParent.set(className, baseName);
      }
    }
  }
  ctx.externClasses.set(className, info);
  const fullName = [...namespacePath, className].join(".");
  ctx.externClasses.set(fullName, info);
}
var ERROR_TYPES_SKIP = /* @__PURE__ */ new Set([
  "Error",
  "TypeError",
  "RangeError",
  "SyntaxError",
  "URIError",
  "EvalError",
  "ReferenceError",
  "Date",
]);
function collectExternFromDeclareVar(ctx, decl) {
  const className = decl.name.text;
  if (ERROR_TYPES_SKIP.has(className)) return;
  if (ctx.externClasses.has(className)) return;
  const symbol = ctx.checker.getSymbolAtLocation(decl.name);
  if (!symbol) return;
  const info = {
    importPrefix: className,
    namespacePath: [],
    className,
    constructorParams: [],
    methods: /* @__PURE__ */ new Map(),
    properties: /* @__PURE__ */ new Map(),
  };
  if (decl.type) {
    if (ts15.isTypeLiteralNode(decl.type)) {
      for (const member of decl.type.members) {
        if (ts15.isConstructSignatureDeclaration(member)) {
          for (const param of member.parameters) {
            const paramType = ctx.checker.getTypeAtLocation(param);
            info.constructorParams.push(mapTsTypeToWasm(paramType, ctx.checker));
          }
          break;
        }
      }
    } else if (ts15.isTypeReferenceNode(decl.type)) {
      const refType = ctx.checker.getTypeAtLocation(decl.type);
      const constructSigs = refType.getConstructSignatures();
      const sig =
        constructSigs.length > 0
          ? constructSigs.reduce((a, b) => (b.parameters.length > a.parameters.length ? b : a))
          : void 0;
      if (sig) {
        for (const param of sig.parameters) {
          const paramType = ctx.checker.getTypeOfSymbol(param);
          info.constructorParams.push(mapTsTypeToWasm(paramType, ctx.checker));
        }
      }
    }
  }
  const allDecls = symbol.getDeclarations() ?? [];
  const visited = /* @__PURE__ */ new Set();
  for (const d of allDecls) {
    if (!ts15.isInterfaceDeclaration(d)) continue;
    collectInterfaceMembers(ctx, d, info, decl);
    if (d.heritageClauses) {
      let parentSet = false;
      for (const clause of d.heritageClauses) {
        if (clause.token !== ts15.SyntaxKind.ExtendsKeyword) continue;
        for (const typeRef of clause.types) {
          const baseType = ctx.checker.getTypeAtLocation(typeRef);
          const baseName = baseType.getSymbol()?.name;
          if (!baseName) continue;
          if (!parentSet && !ctx.externClassParent.has(className)) {
            ctx.externClassParent.set(className, baseName);
            parentSet = true;
          }
          if (!isExternalDeclaredClass(baseType, ctx.checker)) {
            collectMixinMembers(ctx, baseType, info, decl, visited);
          }
        }
      }
    }
  }
  ctx.externClasses.set(className, info);
}
function collectInterfaceMembers(ctx, iface, info, locationNode) {
  for (const member of iface.members) {
    if (ts15.isMethodSignature(member) && member.name && ts15.isIdentifier(member.name)) {
      const methodName = member.name.text;
      if (info.methods.has(methodName)) continue;
      const sig = ctx.checker.getSignatureFromDeclaration(member);
      if (sig) {
        const params = [{ kind: "externref" }];
        let requiredParams = 1;
        for (const p of member.parameters) {
          const pt = ctx.checker.getTypeAtLocation(p);
          params.push(mapTsTypeToWasm(pt, ctx.checker));
          if (!p.questionToken && !p.initializer) requiredParams++;
        }
        const retType = ctx.checker.getReturnTypeOfSignature(sig);
        const results = isVoidType(retType) ? [] : [mapTsTypeToWasm(retType, ctx.checker)];
        info.methods.set(methodName, { params, results, requiredParams });
      }
    }
    if (ts15.isPropertySignature(member) && member.name && ts15.isIdentifier(member.name)) {
      const propName = member.name.text;
      if (info.properties.has(propName)) continue;
      const propType = ctx.checker.getTypeAtLocation(member);
      const wasmType = mapTsTypeToWasm(propType, ctx.checker);
      const isReadonly = member.modifiers?.some((m) => m.kind === ts15.SyntaxKind.ReadonlyKeyword) ?? false;
      info.properties.set(propName, { type: wasmType, readonly: isReadonly });
    }
    if (ts15.isGetAccessorDeclaration(member) && member.name && ts15.isIdentifier(member.name)) {
      const propName = member.name.text;
      if (info.properties.has(propName)) continue;
      const propType = ctx.checker.getTypeAtLocation(member);
      const wasmType = mapTsTypeToWasm(propType, ctx.checker);
      const hasSetter = iface.members.some(
        (m) => ts15.isSetAccessorDeclaration(m) && ts15.isIdentifier(m.name) && m.name.text === propName,
      );
      info.properties.set(propName, { type: wasmType, readonly: !hasSetter });
    }
  }
}
function collectMixinMembers(ctx, mixinType, info, locationNode, visited) {
  const mixinSymbol = mixinType.getSymbol();
  if (!mixinSymbol) return;
  const mixinName = mixinSymbol.name;
  if (visited.has(mixinName)) return;
  visited.add(mixinName);
  for (const d of mixinSymbol.getDeclarations() ?? []) {
    if (!ts15.isInterfaceDeclaration(d)) continue;
    collectInterfaceMembers(ctx, d, info, locationNode);
    if (d.heritageClauses) {
      for (const clause of d.heritageClauses) {
        if (clause.token !== ts15.SyntaxKind.ExtendsKeyword) continue;
        for (const typeRef of clause.types) {
          const baseType = ctx.checker.getTypeAtLocation(typeRef);
          if (!isExternalDeclaredClass(baseType, ctx.checker)) {
            collectMixinMembers(ctx, baseType, info, locationNode, visited);
          }
        }
      }
    }
  }
}
function collectUsedExternImports(ctx, sourceFile) {
  const registered = /* @__PURE__ */ new Set();
  function resolveExtern(className, memberName, kind) {
    let current = className;
    while (current) {
      const info = ctx.externClasses.get(current);
      if (info) {
        if (kind === "method" && info.methods.has(memberName)) return info;
        if (kind === "property" && info.properties.has(memberName)) return info;
      }
      current = ctx.externClassParent.get(current);
    }
    return null;
  }
  function register(importName, params, results) {
    if (registered.has(importName)) return;
    registered.add(importName);
    const t = addFuncType(ctx, params, results);
    addImport(ctx, "env", importName, { kind: "func", typeIdx: t });
  }
  function visit(node) {
    if (ts15.isNewExpression(node)) {
      const type = ctx.checker.getTypeAtLocation(node);
      const className = type.getSymbol()?.name;
      if (className) {
        const info = ctx.externClasses.get(className);
        if (info) register(`${info.importPrefix}_new`, info.constructorParams, [{ kind: "externref" }]);
      }
    }
    if (node.kind === ts15.SyntaxKind.RegularExpressionLiteral) {
      const info = ctx.externClasses.get("RegExp");
      if (info) {
        register(`${info.importPrefix}_new`, info.constructorParams, [{ kind: "externref" }]);
      }
    }
    if (ts15.isPropertyAccessExpression(node)) {
      const isAssignTarget =
        node.parent &&
        ts15.isBinaryExpression(node.parent) &&
        node.parent.operatorToken.kind === ts15.SyntaxKind.EqualsToken &&
        node.parent.left === node;
      if (!isAssignTarget) {
        const objType = ctx.checker.getTypeAtLocation(node.expression);
        const className = objType.getSymbol()?.name;
        const memberName = node.name.text;
        if (className) {
          const isCall = node.parent && ts15.isCallExpression(node.parent) && node.parent.expression === node;
          if (isCall) {
            const info = resolveExtern(className, memberName, "method");
            if (info) {
              const sig = info.methods.get(memberName);
              register(`${info.importPrefix}_${memberName}`, sig.params, sig.results);
            }
          } else {
            const info = resolveExtern(className, memberName, "property");
            if (info) {
              const propInfo = info.properties.get(memberName);
              register(`${info.importPrefix}_get_${memberName}`, [{ kind: "externref" }], [propInfo.type]);
            }
          }
        }
      }
    }
    if (
      ts15.isBinaryExpression(node) &&
      node.operatorToken.kind === ts15.SyntaxKind.EqualsToken &&
      ts15.isPropertyAccessExpression(node.left)
    ) {
      const objType = ctx.checker.getTypeAtLocation(node.left.expression);
      const className = objType.getSymbol()?.name;
      const propName = node.left.name.text;
      if (className) {
        const info = resolveExtern(className, propName, "property");
        if (info) {
          const propInfo = info.properties.get(propName);
          register(`${info.importPrefix}_set_${propName}`, [{ kind: "externref" }, propInfo.type], []);
        }
      }
    }
    if (ts15.isElementAccessExpression(node)) {
      const isCallCallee = node.parent && ts15.isCallExpression(node.parent) && node.parent.expression === node;
      const objType = ctx.checker.getTypeAtLocation(node.expression);
      const sym = objType.getSymbol();
      const isWidenedVar = ts15.isIdentifier(node.expression) && ctx.widenedVarStructMap.has(node.expression.text);
      if (
        !isCallCallee &&
        sym?.name !== "Array" &&
        sym?.name !== "__type" &&
        sym?.name !== "__object" &&
        !isTupleType(objType) &&
        !isWidenedVar
      ) {
        const wasmType = mapTsTypeToWasm(objType, ctx.checker);
        if (wasmType.kind === "externref") {
          register("__extern_get", [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
        }
      }
    }
    ts15.forEachChild(node, visit);
  }
  for (const stmt of sourceFile.statements) {
    ts15.forEachChild(stmt, visit);
  }
}
function collectDeclaredGlobals(ctx, libFile, userFile) {
  const referencedNames = /* @__PURE__ */ new Set();
  const collectRefs = (node) => {
    if (ts15.isIdentifier(node)) referencedNames.add(node.text);
    ts15.forEachChild(node, collectRefs);
  };
  for (const stmt of userFile.statements) {
    ts15.forEachChild(stmt, collectRefs);
  }
  for (const stmt of libFile.statements) {
    if (!ts15.isVariableStatement(stmt) || !hasDeclareModifier(stmt)) continue;
    for (const decl of stmt.declarationList.declarations) {
      if (!ts15.isIdentifier(decl.name)) continue;
      const name = decl.name.text;
      if (!referencedNames.has(name)) continue;
      if (ctx.declaredGlobals.has(name)) continue;
      const type = ctx.checker.getTypeAtLocation(decl);
      if (!isExternalDeclaredClass(type, ctx.checker)) continue;
      const importName = `global_${name}`;
      const typeIdx = addFuncType(ctx, [], [{ kind: "externref" }]);
      addImport(ctx, "env", importName, { kind: "func", typeIdx });
      const funcIdx = ctx.funcMap.get(importName);
      if (funcIdx !== void 0) {
        ctx.declaredGlobals.set(name, { type: { kind: "externref" }, funcIdx });
      }
    }
  }
}
var LIB_GLOBALS = /* @__PURE__ */ new Set([
  "document",
  "window",
  "Date",
  "Map",
  "Set",
  "WeakMap",
  "WeakSet",
  "WeakRef",
  "RegExp",
  "Error",
  "HTMLElement",
  "Element",
  "Node",
  "Event",
]);
function sourceUsesLibGlobals(sourceFile) {
  let found = false;
  const visit = (node) => {
    if (found) return;
    if (ts15.isIdentifier(node) && LIB_GLOBALS.has(node.text)) {
      found = true;
      return;
    }
    if (node.kind === ts15.SyntaxKind.RegularExpressionLiteral) {
      found = true;
      return;
    }
    ts15.forEachChild(node, visit);
  };
  for (const stmt of sourceFile.statements) {
    ts15.forEachChild(stmt, visit);
    if (found) break;
  }
  return found;
}
function collectEnumDeclarations(ctx, sourceFile) {
  const stringEnumLiterals = [];
  for (const stmt of sourceFile.statements) {
    if (!ts15.isEnumDeclaration(stmt)) continue;
    const enumName = stmt.name.text;
    let nextValue = 0;
    for (const member of stmt.members) {
      const memberName = member.name.text;
      const key = `${enumName}.${memberName}`;
      if (member.initializer) {
        if (ts15.isStringLiteral(member.initializer)) {
          const strVal = member.initializer.text;
          ctx.enumStringValues.set(key, strVal);
          if (!ctx.stringGlobalMap.has(strVal)) {
            stringEnumLiterals.push(strVal);
          }
          continue;
        }
        if (ts15.isNumericLiteral(member.initializer)) {
          nextValue = Number(member.initializer.text.replace(/_/g, ""));
        } else if (
          ts15.isPrefixUnaryExpression(member.initializer) &&
          member.initializer.operator === ts15.SyntaxKind.MinusToken &&
          ts15.isNumericLiteral(member.initializer.operand)
        ) {
          nextValue = -Number(member.initializer.operand.text.replace(/_/g, ""));
        }
      }
      ctx.enumValues.set(key, nextValue);
      nextValue++;
    }
  }
  if (stringEnumLiterals.length > 0) {
    if (ctx.nativeStrings) {
      ensureNativeStringHelpers(ctx);
      for (const value of stringEnumLiterals) {
        if (!ctx.stringGlobalMap.has(value)) ctx.stringGlobalMap.set(value, -1);
      }
    } else {
      addStringImports(ctx);
      for (const value of stringEnumLiterals) {
        addStringConstantGlobal(ctx, value);
      }
    }
  }
}
function resolveClassMemberName(ctx, name) {
  if (ts15.isIdentifier(name)) return name.text;
  if (ts15.isPrivateIdentifier(name)) return "__priv_" + name.text.slice(1);
  if (ts15.isStringLiteral(name)) return name.text;
  if (ts15.isNumericLiteral(name)) return String(Number(name.text));
  if (ts15.isComputedPropertyName(name)) {
    return resolveComputedKeyExpression(ctx, name.expression);
  }
  return void 0;
}
function collectClassDeclaration(ctx, decl, syntheticName) {
  const className = syntheticName ?? decl.name.text;
  ctx.classSet.add(className);
  ctx.classDeclarationMap.set(className, decl);
  const esName = decl.name ? decl.name.text : (syntheticName ?? "");
  ctx.functionNameMap.set(className, esName);
  if (syntheticName) {
    const tsType = ctx.checker.getTypeAtLocation(decl);
    const symbolName = tsType.getSymbol()?.name;
    if (symbolName && symbolName !== syntheticName) {
      ctx.classExprNameMap.set(symbolName, syntheticName);
    }
  }
  let parentClassName;
  let parentStructTypeIdx;
  let parentFields = [];
  if (decl.heritageClauses) {
    for (const clause of decl.heritageClauses) {
      if (clause.token === ts15.SyntaxKind.ExtendsKeyword && clause.types.length > 0) {
        const baseExpr = clause.types[0].expression;
        if (ts15.isIdentifier(baseExpr)) {
          parentClassName = baseExpr.text;
          if (parentClassName === className) {
            parentClassName = void 0;
            break;
          }
          parentStructTypeIdx = ctx.structMap.get(parentClassName);
          parentFields = ctx.structFields.get(parentClassName) ?? [];
          ctx.classParentMap.set(className, parentClassName);
          if (parentStructTypeIdx !== void 0) {
            const parentTypeDef = ctx.mod.types[parentStructTypeIdx];
            if (parentTypeDef && parentTypeDef.superTypeIdx === void 0) {
              parentTypeDef.superTypeIdx = -1;
            }
          }
        }
      }
    }
  }
  const structTypeIdx = ctx.mod.types.length;
  const placeholderDef = { kind: "struct", name: className, fields: [] };
  ctx.mod.types.push(placeholderDef);
  ctx.structMap.set(className, structTypeIdx);
  ctx.typeIdxToStructName.set(structTypeIdx, className);
  const ctor = decl.members.find(ts15.isConstructorDeclaration);
  const ownFields = [];
  if (ctor?.body) {
    for (const stmt of ctor.body.statements) {
      if (
        ts15.isExpressionStatement(stmt) &&
        ts15.isCallExpression(stmt.expression) &&
        stmt.expression.expression.kind === ts15.SyntaxKind.SuperKeyword
      ) {
        continue;
      }
      if (
        ts15.isExpressionStatement(stmt) &&
        ts15.isBinaryExpression(stmt.expression) &&
        stmt.expression.operatorToken.kind === ts15.SyntaxKind.EqualsToken &&
        ts15.isPropertyAccessExpression(stmt.expression.left) &&
        stmt.expression.left.expression.kind === ts15.SyntaxKind.ThisKeyword
      ) {
        const rawName = stmt.expression.left.name.text;
        const fieldName = ts15.isPrivateIdentifier(stmt.expression.left.name) ? "__priv_" + rawName.slice(1) : rawName;
        if (parentFields.some((f) => f.name === fieldName)) continue;
        const fieldTsType = ctx.checker.getTypeAtLocation(stmt.expression.left);
        const fieldType = resolveWasmType(ctx, fieldTsType);
        if (!ownFields.some((f) => f.name === fieldName)) {
          ownFields.push({ name: fieldName, type: fieldType, mutable: true });
        }
      }
    }
  }
  for (const member of decl.members) {
    if (ts15.isPropertyDeclaration(member) && member.name) {
      const fieldName = resolveClassMemberName(ctx, member.name);
      if (!fieldName) continue;
      if (hasStaticModifier(member)) continue;
      if (parentFields.some((f) => f.name === fieldName)) continue;
      if (!ownFields.some((f) => f.name === fieldName)) {
        const fieldTsType = ctx.checker.getTypeAtLocation(member);
        const fieldType = resolveWasmType(ctx, fieldTsType);
        ownFields.push({ name: fieldName, type: fieldType, mutable: true });
      }
    }
  }
  const fields = [...parentFields, ...ownFields];
  for (const field of fields) {
    if (field.type.kind === "ref") {
      field.type = { kind: "ref_null", typeIdx: field.type.typeIdx };
    }
  }
  const classTag = ctx.classTagCounter++;
  ctx.classTagMap.set(className, classTag);
  if (!parentClassName || parentStructTypeIdx === void 0) {
    fields.unshift({ name: "__tag", type: { kind: "i32" }, mutable: false });
  }
  const structDef = { kind: "struct", name: className, fields };
  if (parentStructTypeIdx !== void 0) {
    structDef.superTypeIdx = parentStructTypeIdx;
  }
  ctx.mod.types[structTypeIdx] = structDef;
  ctx.structFields.set(className, fields);
  {
    const protoGlobalIdx = nextModuleGlobalIdx(ctx);
    ctx.mod.globals.push({
      name: `__proto_${className}`,
      type: { kind: "externref" },
      mutable: true,
      init: [{ op: "ref.null.extern" }],
    });
    ctx.protoGlobals.set(className, protoGlobalIdx);
  }
  const ctorParams = [];
  const ctorName = `${className}_new`;
  if (ctor) {
    for (let i = 0; i < ctor.parameters.length; i++) {
      const param = ctor.parameters[i];
      if (param.dotDotDotToken) {
        const paramType = ctx.checker.getTypeAtLocation(param);
        const typeArgs = ctx.checker.getTypeArguments(paramType);
        const elemTsType = typeArgs[0];
        const elemType = elemTsType ? resolveWasmType(ctx, elemTsType) : { kind: "f64" };
        const elemKey =
          elemType.kind === "ref" || elemType.kind === "ref_null" ? `ref_${elemType.typeIdx}` : elemType.kind;
        const vecTypeIdx = getOrRegisterVecType(ctx, elemKey, elemType);
        const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
        ctorParams.push({ kind: "ref_null", typeIdx: vecTypeIdx });
        ctx.funcRestParams.set(ctorName, {
          restIndex: i,
          elemType,
          arrayTypeIdx: arrTypeIdx,
          vecTypeIdx,
        });
      } else {
        const paramType = ctx.checker.getTypeAtLocation(param);
        let wasmType = resolveWasmType(ctx, paramType);
        if (param.initializer && wasmType.kind === "ref") {
          wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
        }
        ctorParams.push(wasmType);
      }
    }
  }
  const ctorResults = [{ kind: "ref", typeIdx: structTypeIdx }];
  const ctorTypeIdx = addFuncType(ctx, ctorParams, ctorResults, `${className}_new_type`);
  const ctorFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set(ctorName, ctorFuncIdx);
  ctx.mod.functions.push({
    name: ctorName,
    typeIdx: ctorTypeIdx,
    locals: [],
    body: [],
    exported: false,
  });
  const ownMethodNames = /* @__PURE__ */ new Set();
  for (const member of decl.members) {
    if (ts15.isMethodDeclaration(member) && member.name) {
      const methodName = resolveClassMemberName(ctx, member.name);
      if (!methodName) continue;
      ownMethodNames.add(methodName);
      if (hasAbstractModifier(member)) continue;
      const fullName = `${className}_${methodName}`;
      const isStatic = hasStaticModifier(member);
      if (isStatic && methodName === "prototype") {
        ctx.classThrowsOnEval.add(className);
      }
      if (isStatic) {
        ctx.staticMethodSet.add(fullName);
      } else {
        ctx.classMethodSet.add(fullName);
      }
      const isGeneratorMethod = member.asteriskToken !== void 0;
      if (isGeneratorMethod) {
        ctx.generatorFunctions.add(fullName);
      }
      if (ctx.funcMap.has(fullName)) continue;
      const methodParams = isStatic ? [] : [{ kind: "ref", typeIdx: structTypeIdx }];
      for (const param of member.parameters) {
        const paramType = ctx.checker.getTypeAtLocation(param);
        let wasmType = resolveWasmType(ctx, paramType);
        if (param.initializer && wasmType.kind === "ref") {
          wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
        }
        methodParams.push(wasmType);
      }
      const sig = ctx.checker.getSignatureFromDeclaration(member);
      let methodResults = [];
      if (isGeneratorMethod) {
        methodResults = [{ kind: "externref" }];
      } else if (sig) {
        const retType = ctx.checker.getReturnTypeOfSignature(sig);
        if (!isVoidType(retType)) {
          methodResults = [resolveWasmType(ctx, retType)];
        }
      }
      const methodTypeIdx = addFuncType(ctx, methodParams, methodResults, `${fullName}_type`);
      const methodFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
      ctx.funcMap.set(fullName, methodFuncIdx);
      ctx.mod.functions.push({
        name: fullName,
        typeIdx: methodTypeIdx,
        locals: [],
        body: [],
        exported: false,
      });
    }
  }
  for (const member of decl.members) {
    if (
      (ts15.isGetAccessorDeclaration(member) || ts15.isSetAccessorDeclaration(member)) &&
      member.name &&
      hasStaticModifier(member)
    ) {
      const accName = resolveClassMemberName(ctx, member.name);
      if (accName === "prototype") {
        ctx.classThrowsOnEval.add(className);
      }
    }
    if (ts15.isGetAccessorDeclaration(member) && member.name) {
      const propName = resolveClassMemberName(ctx, member.name);
      if (!propName) continue;
      const accessorKey = `${className}_${propName}`;
      ctx.classAccessorSet.add(accessorKey);
      if (hasStaticModifier(member)) {
        ctx.staticAccessorSet.add(accessorKey);
      }
      const getterName = `${className}_get_${propName}`;
      if (ctx.funcMap.has(getterName)) continue;
      const getterParams = [{ kind: "ref", typeIdx: structTypeIdx }];
      const sig = ctx.checker.getSignatureFromDeclaration(member);
      let getterResults = [];
      if (sig) {
        const retType = ctx.checker.getReturnTypeOfSignature(sig);
        if (!isVoidType(retType)) {
          getterResults = [resolveWasmType(ctx, retType)];
        }
      }
      const getterTypeIdx = addFuncType(ctx, getterParams, getterResults, `${getterName}_type`);
      const getterFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
      ctx.funcMap.set(getterName, getterFuncIdx);
      ctx.mod.functions.push({
        name: getterName,
        typeIdx: getterTypeIdx,
        locals: [],
        body: [],
        exported: false,
      });
    }
    if (ts15.isSetAccessorDeclaration(member) && member.name) {
      const propName = resolveClassMemberName(ctx, member.name);
      if (!propName) continue;
      const accessorKey = `${className}_${propName}`;
      ctx.classAccessorSet.add(accessorKey);
      if (hasStaticModifier(member)) {
        ctx.staticAccessorSet.add(accessorKey);
      }
      const setterName = `${className}_set_${propName}`;
      if (ctx.funcMap.has(setterName)) continue;
      const setterParams = [{ kind: "ref", typeIdx: structTypeIdx }];
      for (const param of member.parameters) {
        const paramType = ctx.checker.getTypeAtLocation(param);
        setterParams.push(resolveWasmType(ctx, paramType));
      }
      const setterTypeIdx = addFuncType(ctx, setterParams, [], `${setterName}_type`);
      const setterFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
      ctx.funcMap.set(setterName, setterFuncIdx);
      ctx.mod.functions.push({
        name: setterName,
        typeIdx: setterTypeIdx,
        locals: [],
        body: [],
        exported: false,
      });
    }
  }
  if (parentClassName) {
    const ownAccessorNames = /* @__PURE__ */ new Set();
    for (const member of decl.members) {
      if ((ts15.isGetAccessorDeclaration(member) || ts15.isSetAccessorDeclaration(member)) && member.name) {
        const accName = resolveClassMemberName(ctx, member.name);
        if (accName) ownAccessorNames.add(accName);
      }
    }
    const visitedAncestors = /* @__PURE__ */ new Set();
    let ancestor = parentClassName;
    while (ancestor && !visitedAncestors.has(ancestor)) {
      visitedAncestors.add(ancestor);
      for (const [key, funcIdx] of ctx.funcMap) {
        if (key.startsWith(`${ancestor}_`) && !key.endsWith("_new") && !key.endsWith("_type")) {
          const suffix = key.substring(ancestor.length + 1);
          if (suffix === "new" || suffix.startsWith("new_")) continue;
          const getMatch = suffix.match(/^get_(.+)$/);
          const setMatch = suffix.match(/^set_(.+)$/);
          if (getMatch || setMatch) {
            const accPropName = (getMatch || setMatch)[1];
            if (!ownAccessorNames.has(accPropName)) {
              const childFullName = `${className}_${suffix}`;
              if (!ctx.funcMap.has(childFullName)) {
                ctx.funcMap.set(childFullName, funcIdx);
              }
              const parentAccessorKey = `${ancestor}_${accPropName}`;
              const childAccessorKey = `${className}_${accPropName}`;
              if (ctx.classAccessorSet.has(parentAccessorKey) && !ctx.classAccessorSet.has(childAccessorKey)) {
                ctx.classAccessorSet.add(childAccessorKey);
              }
            }
          } else if (!suffix.includes("_")) {
            const childFullName = `${className}_${suffix}`;
            if (!ownMethodNames.has(suffix) && !ctx.funcMap.has(childFullName)) {
              ctx.funcMap.set(childFullName, funcIdx);
              ctx.classMethodSet.add(childFullName);
            }
          } else {
            const childFullName = `${className}_${suffix}`;
            if (!ownMethodNames.has(suffix) && !ctx.funcMap.has(childFullName) && ctx.classMethodSet.has(key)) {
              ctx.funcMap.set(childFullName, funcIdx);
              ctx.classMethodSet.add(childFullName);
            }
          }
        }
      }
      ancestor = ctx.classParentMap.get(ancestor);
    }
  }
  for (const member of decl.members) {
    if (ts15.isPropertyDeclaration(member) && member.name && hasStaticModifier(member)) {
      const propName = resolveClassMemberName(ctx, member.name);
      if (!propName) continue;
      const fullName = `${className}_${propName}`;
      if (ctx.staticProps.has(fullName)) continue;
      const propTsType = ctx.checker.getTypeAtLocation(member);
      const wasmType = resolveWasmType(ctx, propTsType);
      const init =
        wasmType.kind === "f64"
          ? [{ op: "f64.const", value: 0 }]
          : wasmType.kind === "i32"
            ? [{ op: "i32.const", value: 0 }]
            : wasmType.kind === "i64"
              ? [{ op: "i64.const", value: 0n }]
              : wasmType.kind === "ref_null" || wasmType.kind === "ref"
                ? [
                    {
                      op: "ref.null",
                      typeIdx: wasmType.typeIdx,
                    },
                  ]
                : [{ op: "ref.null.extern" }];
      const globalType =
        wasmType.kind === "ref"
          ? {
              kind: "ref_null",
              typeIdx: wasmType.typeIdx,
            }
          : wasmType;
      const globalIdx = nextModuleGlobalIdx(ctx);
      ctx.mod.globals.push({
        name: `__static_${fullName}`,
        type: globalType,
        mutable: true,
        init,
      });
      ctx.staticProps.set(fullName, globalIdx);
      if (member.initializer) {
        ctx.staticInitExprs.push({
          globalIdx,
          initializer: member.initializer,
        });
      }
    }
  }
}
function resolveGenericCallSiteTypes(ctx, funcName, sourceFile) {
  let found = null;
  function visit(node) {
    if (found) return;
    if (ts15.isCallExpression(node) && ts15.isIdentifier(node.expression) && node.expression.text === funcName) {
      const sig = ctx.checker.getResolvedSignature(node);
      if (sig) {
        const params = [];
        const sigParams = sig.getParameters();
        for (let i = 0; i < sigParams.length; i++) {
          const paramType = ctx.checker.getTypeOfSymbol(sigParams[i]);
          params.push(resolveWasmType(ctx, paramType));
        }
        const retType = ctx.checker.getReturnTypeOfSignature(sig);
        const results = isVoidType(retType) ? [] : [resolveWasmType(ctx, retType)];
        found = { params, results };
      }
    }
    ts15.forEachChild(node, visit);
  }
  ts15.forEachChild(sourceFile, visit);
  return found;
}
function inferParamTypeFromCallSites(ctx, funcName, paramIndex, sourceFile) {
  let agreed = null;
  let conflict = false;
  function visit(node) {
    if (conflict) return;
    if (ts15.isCallExpression(node) && ts15.isIdentifier(node.expression) && node.expression.text === funcName) {
      const arg = node.arguments[paramIndex];
      if (arg) {
        const argType = ctx.checker.getTypeAtLocation(arg);
        if (argType.flags & (ts15.TypeFlags.Any | ts15.TypeFlags.Unknown)) {
        } else {
          const wasmType = resolveWasmType(ctx, argType);
          if (agreed === null) {
            agreed = wasmType;
          } else if (agreed.kind !== wasmType.kind) {
            conflict = true;
          } else if (
            (agreed.kind === "ref" || agreed.kind === "ref_null") &&
            (wasmType.kind === "ref" || wasmType.kind === "ref_null") &&
            agreed.typeIdx !== wasmType.typeIdx
          ) {
            conflict = true;
          }
        }
      }
    }
    ts15.forEachChild(node, visit);
  }
  ts15.forEachChild(sourceFile, visit);
  return conflict ? null : agreed;
}
function collectEmptyObjectWidening(ctx, checker, sourceFile) {
  function scanStatements(stmts) {
    for (const stmt of stmts) {
      if (ts15.isVariableStatement(stmt)) {
        for (const decl of stmt.declarationList.declarations) {
          if (!ts15.isIdentifier(decl.name)) continue;
          if (!decl.initializer || !ts15.isObjectLiteralExpression(decl.initializer)) continue;
          if (decl.initializer.properties.length > 0) continue;
          const varName = decl.name.text;
          const extraProps = [];
          const seenProps = /* @__PURE__ */ new Set();
          collectPropsFromStatements(checker, ctx, stmts, varName, extraProps, seenProps);
          if (extraProps.length > 0) {
            ctx.widenedTypeProperties.set(varName, extraProps);
            const fields = extraProps.map((wp) => ({
              name: wp.name,
              type: wp.type,
              mutable: true,
            }));
            const structName = `__anon_${ctx.anonTypeCounter++}`;
            const typeIdx = ctx.mod.types.length;
            ctx.mod.types.push({
              kind: "struct",
              name: structName,
              fields,
            });
            ctx.structMap.set(structName, typeIdx);
            ctx.typeIdxToStructName.set(typeIdx, structName);
            ctx.structFields.set(structName, fields);
            ctx.widenedVarStructMap.set(varName, structName);
            const varType = checker.getTypeAtLocation(decl.name);
            ctx.anonTypeMap.set(varType, structName);
            const initType = checker.getTypeAtLocation(decl.initializer);
            ctx.anonTypeMap.set(initType, structName);
          }
        }
      }
      if (ts15.isFunctionDeclaration(stmt) && stmt.body) {
        scanStatements(stmt.body.statements);
      }
      if (ts15.isTryStatement(stmt)) {
        scanStatements(stmt.tryBlock.statements);
        if (stmt.catchClause) {
          scanStatements(stmt.catchClause.block.statements);
        }
        if (stmt.finallyBlock) {
          scanStatements(stmt.finallyBlock.statements);
        }
      }
    }
  }
  scanStatements(sourceFile.statements);
}
function collectPropsFromStatements(checker, ctx, stmts, varName, extraProps, seenProps) {
  for (const s of stmts) {
    if (ts15.isExpressionStatement(s) && ts15.isBinaryExpression(s.expression)) {
      const bin = s.expression;
      if (
        bin.operatorToken.kind === ts15.SyntaxKind.EqualsToken &&
        ts15.isPropertyAccessExpression(bin.left) &&
        ts15.isIdentifier(bin.left.expression) &&
        bin.left.expression.text === varName
      ) {
        const propName = bin.left.name.text;
        if (!seenProps.has(propName)) {
          seenProps.add(propName);
          const rhsType = checker.getTypeAtLocation(bin.right);
          const wasmType = resolveWasmType(ctx, rhsType);
          extraProps.push({ name: propName, type: wasmType });
        }
      }
    }
    if (ts15.isExpressionStatement(s) && ts15.isCallExpression(s.expression)) {
      const call2 = s.expression;
      if (
        ts15.isPropertyAccessExpression(call2.expression) &&
        ts15.isIdentifier(call2.expression.expression) &&
        call2.expression.expression.text === "Object" &&
        ts15.isIdentifier(call2.expression.name) &&
        call2.expression.name.text === "defineProperty" &&
        call2.arguments.length >= 3
      ) {
        const objArg = call2.arguments[0];
        const propArg = call2.arguments[1];
        const descArg = call2.arguments[2];
        if (ts15.isIdentifier(objArg) && objArg.text === varName && ts15.isStringLiteral(propArg)) {
          const propName = propArg.text;
          if (!seenProps.has(propName)) {
            seenProps.add(propName);
            let wasmType = { kind: "externref" };
            if (ts15.isObjectLiteralExpression(descArg)) {
              for (const prop of descArg.properties) {
                if (ts15.isPropertyAssignment(prop) && ts15.isIdentifier(prop.name) && prop.name.text === "value") {
                  const rhsType = checker.getTypeAtLocation(prop.initializer);
                  wasmType = resolveWasmType(ctx, rhsType);
                  break;
                }
              }
            }
            extraProps.push({ name: propName, type: wasmType });
          }
        }
      }
    }
    if (ts15.isVariableStatement(s)) {
      for (const decl of s.declarationList.declarations) {
        if (decl.initializer && ts15.isCallExpression(decl.initializer)) {
          const call2 = decl.initializer;
          if (
            ts15.isPropertyAccessExpression(call2.expression) &&
            ts15.isIdentifier(call2.expression.expression) &&
            call2.expression.expression.text === "Object" &&
            ts15.isIdentifier(call2.expression.name) &&
            call2.expression.name.text === "defineProperty" &&
            call2.arguments.length >= 3
          ) {
            const objArg = call2.arguments[0];
            const propArg = call2.arguments[1];
            const descArg = call2.arguments[2];
            if (ts15.isIdentifier(objArg) && objArg.text === varName && ts15.isStringLiteral(propArg)) {
              const propName = propArg.text;
              if (!seenProps.has(propName)) {
                seenProps.add(propName);
                let wasmType = { kind: "externref" };
                if (ts15.isObjectLiteralExpression(descArg)) {
                  for (const prop of descArg.properties) {
                    if (ts15.isPropertyAssignment(prop) && ts15.isIdentifier(prop.name) && prop.name.text === "value") {
                      const rhsType = checker.getTypeAtLocation(prop.initializer);
                      wasmType = resolveWasmType(ctx, rhsType);
                      break;
                    }
                  }
                }
                extraProps.push({ name: propName, type: wasmType });
              }
            }
          }
        }
      }
    }
    if (ts15.isBlock(s)) {
      collectPropsFromStatements(checker, ctx, s.statements, varName, extraProps, seenProps);
    }
    if (ts15.isIfStatement(s)) {
      if (ts15.isBlock(s.thenStatement)) {
        collectPropsFromStatements(checker, ctx, s.thenStatement.statements, varName, extraProps, seenProps);
      }
      if (s.elseStatement && ts15.isBlock(s.elseStatement)) {
        collectPropsFromStatements(checker, ctx, s.elseStatement.statements, varName, extraProps, seenProps);
      }
    }
    if (ts15.isTryStatement(s)) {
      collectPropsFromStatements(checker, ctx, s.tryBlock.statements, varName, extraProps, seenProps);
      if (s.catchClause) {
        collectPropsFromStatements(checker, ctx, s.catchClause.block.statements, varName, extraProps, seenProps);
      }
      if (s.finallyBlock) {
        collectPropsFromStatements(checker, ctx, s.finallyBlock.statements, varName, extraProps, seenProps);
      }
    }
    if (
      ts15.isForStatement(s) ||
      ts15.isForInStatement(s) ||
      ts15.isForOfStatement(s) ||
      ts15.isWhileStatement(s) ||
      ts15.isDoStatement(s)
    ) {
      if (ts15.isBlock(s.statement)) {
        collectPropsFromStatements(checker, ctx, s.statement.statements, varName, extraProps, seenProps);
      }
    }
    if (ts15.isSwitchStatement(s)) {
      for (const clause of s.caseBlock.clauses) {
        collectPropsFromStatements(checker, ctx, clause.statements, varName, extraProps, seenProps);
      }
    }
  }
}
function applyShapeInference(ctx, checker, sourceFile) {
  const shapes = collectShapes(checker, sourceFile);
  if (shapes.size === 0) return;
  for (const [varName, shape] of shapes) {
    const globalIdx = ctx.moduleGlobals.get(varName);
    if (globalIdx === void 0) continue;
    let elemType;
    let elemKey;
    if (shape.numericValueType === "number") {
      if (ctx.fast) {
        elemType = { kind: "i32" };
        elemKey = "i32";
      } else {
        elemType = { kind: "f64" };
        elemKey = "f64";
      }
    } else if (shape.numericValueType === "string") {
      elemType = { kind: "externref" };
      elemKey = "externref";
    } else {
      if (ctx.fast) {
        elemType = { kind: "i32" };
        elemKey = "i32";
      } else {
        elemType = { kind: "f64" };
        elemKey = "f64";
      }
    }
    const vecTypeIdx = getOrRegisterVecType(ctx, elemKey, elemType);
    const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
    const localIdx = localGlobalIdx(ctx, globalIdx);
    const globalDef = ctx.mod.globals[localIdx];
    if (globalDef) {
      const newType = { kind: "ref_null", typeIdx: vecTypeIdx };
      globalDef.type = newType;
      globalDef.init = [{ op: "ref.null", typeIdx: vecTypeIdx }];
    }
    ctx.shapeMap.set(varName, { vecTypeIdx, arrTypeIdx, elemType });
  }
}
function collectDeclarations(ctx, sourceFile, isEntryFile = true) {
  collectEnumDeclarations(ctx, sourceFile);
  for (const stmt of sourceFile.statements) {
    if (ts15.isInterfaceDeclaration(stmt)) {
      collectInterface(ctx, stmt);
    } else if (ts15.isTypeAliasDeclaration(stmt)) {
      const aliasType = ctx.checker.getTypeAtLocation(stmt);
      if (aliasType.flags & ts15.TypeFlags.Object) {
        collectObjectType(ctx, stmt.name.text, aliasType);
      }
    }
  }
  resolveStructFieldTypes(ctx, sourceFile);
  function registerClassExpression(classExpr, nameHint) {
    if (ctx.anonClassExprNames.has(classExpr)) return;
    const syntheticName = nameHint
      ? `__anonClass_${nameHint}_${ctx.anonTypeCounter++}`
      : classExpr.name
        ? `__anonClass_${classExpr.name.text}_${ctx.anonTypeCounter++}`
        : `__anonClass_${ctx.anonTypeCounter++}`;
    ctx.anonClassExprNames.set(classExpr, syntheticName);
    collectClassDeclaration(ctx, classExpr, syntheticName);
  }
  function collectAnonymousClassesInNewExpr(node) {
    if (ts15.isNewExpression(node)) {
      let inner = node.expression;
      while (ts15.isParenthesizedExpression(inner)) {
        inner = inner.expression;
      }
      if (ts15.isClassExpression(inner)) {
        registerClassExpression(inner);
      }
    }
    if (ts15.isBinaryExpression(node) && node.operatorToken.kind === ts15.SyntaxKind.EqualsToken) {
      let rhs = node.right;
      while (ts15.isParenthesizedExpression(rhs)) {
        rhs = rhs.expression;
      }
      if (ts15.isClassExpression(rhs)) {
        const nameHint = ts15.isIdentifier(node.left) ? node.left.text : void 0;
        registerClassExpression(rhs, nameHint);
        if (nameHint) {
          const syntheticName = ctx.anonClassExprNames.get(rhs);
          if (syntheticName) {
            ctx.classExprNameMap.set(nameHint, syntheticName);
          }
        }
      }
    }
    if (ts15.isClassExpression(node)) {
      registerClassExpression(node);
    }
    ts15.forEachChild(node, collectAnonymousClassesInNewExpr);
  }
  function collectClassesFromStatements(stmts) {
    for (const stmt of stmts) {
      if (ts15.isClassDeclaration(stmt) && stmt.name && !hasDeclareModifier(stmt)) {
        collectClassDeclaration(ctx, stmt);
        ctx.functionNameMap.set(stmt.name.text, stmt.name.text);
      } else if (ts15.isVariableStatement(stmt) && !hasDeclareModifier(stmt)) {
        for (const decl of stmt.declarationList.declarations) {
          if (ts15.isIdentifier(decl.name) && decl.initializer && ts15.isClassExpression(decl.initializer)) {
            collectClassDeclaration(ctx, decl.initializer, decl.name.text);
            const esName = decl.initializer.name ? decl.initializer.name.text : decl.name.text;
            ctx.functionNameMap.set(decl.name.text, esName);
          }
          if (decl.initializer) {
            collectClassesFromFunctionBody(decl.initializer);
          }
        }
      } else if (ts15.isFunctionDeclaration(stmt) && stmt.body) {
        collectClassesFromStatements(stmt.body.statements);
      } else if (ts15.isIfStatement(stmt)) {
        if (ts15.isBlock(stmt.thenStatement)) {
          collectClassesFromStatements(stmt.thenStatement.statements);
        }
        if (stmt.elseStatement && ts15.isBlock(stmt.elseStatement)) {
          collectClassesFromStatements(stmt.elseStatement.statements);
        }
      } else if (ts15.isBlock(stmt)) {
        collectClassesFromStatements(stmt.statements);
      } else if (
        ts15.isForStatement(stmt) ||
        ts15.isForInStatement(stmt) ||
        ts15.isForOfStatement(stmt) ||
        ts15.isWhileStatement(stmt) ||
        ts15.isDoStatement(stmt)
      ) {
        const body = stmt.statement;
        if (ts15.isBlock(body)) {
          collectClassesFromStatements(body.statements);
        }
      } else if (ts15.isSwitchStatement(stmt)) {
        for (const clause of stmt.caseBlock.clauses) {
          collectClassesFromStatements(clause.statements);
        }
      } else if (ts15.isTryStatement(stmt)) {
        collectClassesFromStatements(stmt.tryBlock.statements);
        if (stmt.catchClause) {
          collectClassesFromStatements(stmt.catchClause.block.statements);
        }
        if (stmt.finallyBlock) {
          collectClassesFromStatements(stmt.finallyBlock.statements);
        }
      } else if (ts15.isLabeledStatement(stmt)) {
        if (ts15.isBlock(stmt.statement)) {
          collectClassesFromStatements(stmt.statement.statements);
        }
      } else if (ts15.isExportAssignment(stmt) || ts15.isExportDeclaration(stmt)) {
      }
      collectAnonymousClassesInNewExpr(stmt);
    }
  }
  function collectClassesFromFunctionBody(expr) {
    if (ts15.isArrowFunction(expr)) {
      if (ts15.isBlock(expr.body)) {
        collectClassesFromStatements(expr.body.statements);
      }
    } else if (ts15.isFunctionExpression(expr)) {
      if (expr.body) {
        collectClassesFromStatements(expr.body.statements);
      }
      collectAnonymousClassesInNewExpr(expr);
    }
  }
  collectClassesFromStatements(sourceFile.statements);
  for (const stmt of sourceFile.statements) {
    if (ts15.isFunctionDeclaration(stmt) && stmt.name) {
      if (hasDeclareModifier(stmt)) continue;
      const name = stmt.name.text;
      ctx.functionNameMap.set(name, name);
      const sig = ctx.checker.getSignatureFromDeclaration(stmt);
      if (!sig) continue;
      const isGeneric = stmt.typeParameters && stmt.typeParameters.length > 0;
      const resolved = isGeneric ? resolveGenericCallSiteTypes(ctx, name, sourceFile) : null;
      if (resolved) {
        ctx.genericResolved.set(name, resolved);
      }
      const isAsync = hasAsyncModifier(stmt);
      if (isAsync) {
        ctx.asyncFunctions.add(name);
      }
      const isGenerator = isGeneratorFunction(stmt);
      if (isGenerator) {
        ctx.generatorFunctions.add(name);
        const retType2 = ctx.checker.getReturnTypeOfSignature(sig);
        const yieldType = unwrapGeneratorYieldType(retType2, ctx);
        ctx.generatorYieldType.set(name, yieldType);
      }
      const retType = ctx.checker.getReturnTypeOfSignature(sig);
      const unwrappedRetType = isAsync ? unwrapPromiseType(retType, ctx.checker) : retType;
      if (!isGenerator && !isVoidType(unwrappedRetType)) ensureStructForType(ctx, unwrappedRetType);
      for (const p of stmt.parameters) {
        const pt = ctx.checker.getTypeAtLocation(p);
        ensureStructForType(ctx, pt);
      }
      let params;
      let results;
      if (isGenerator) {
        params = [];
        for (let i = 0; i < stmt.parameters.length; i++) {
          const param = stmt.parameters[i];
          const paramType = ctx.checker.getTypeAtLocation(param);
          let wasmType = resolveWasmType(ctx, paramType);
          if (param.initializer && wasmType.kind === "ref") {
            wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
          }
          if (
            !param.type &&
            paramType.flags & (ts15.TypeFlags.Any | ts15.TypeFlags.Unknown) &&
            (wasmType.kind === "externref" ||
              (wasmType.kind === "ref_null" && ctx.anyValueTypeIdx >= 0 && wasmType.typeIdx === ctx.anyValueTypeIdx))
          ) {
            const inferred = inferParamTypeFromCallSites(ctx, name, i, sourceFile);
            if (inferred) {
              wasmType = inferred;
            }
          }
          params.push(wasmType);
        }
        results = [{ kind: "externref" }];
      } else if (resolved) {
        params = resolved.params;
        results = resolved.results;
      } else {
        params = [];
        for (let i = 0; i < stmt.parameters.length; i++) {
          const param = stmt.parameters[i];
          if (param.dotDotDotToken) {
            const paramType = ctx.checker.getTypeAtLocation(param);
            const typeArgs = ctx.checker.getTypeArguments(paramType);
            const elemTsType = typeArgs[0];
            const elemType = elemTsType ? resolveWasmType(ctx, elemTsType) : { kind: "f64" };
            const elemKey =
              elemType.kind === "ref" || elemType.kind === "ref_null" ? `ref_${elemType.typeIdx}` : elemType.kind;
            const vecTypeIdx = getOrRegisterVecType(ctx, elemKey, elemType);
            const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
            params.push({ kind: "ref_null", typeIdx: vecTypeIdx });
            ctx.funcRestParams.set(name, {
              restIndex: i,
              elemType,
              arrayTypeIdx: arrTypeIdx,
              vecTypeIdx,
            });
          } else {
            const paramType = ctx.checker.getTypeAtLocation(param);
            let wasmType = resolveWasmType(ctx, paramType);
            if (param.initializer && wasmType.kind === "ref") {
              wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
            }
            if (
              !param.type &&
              paramType.flags & (ts15.TypeFlags.Any | ts15.TypeFlags.Unknown) &&
              (wasmType.kind === "externref" ||
                (wasmType.kind === "ref_null" && ctx.anyValueTypeIdx >= 0 && wasmType.typeIdx === ctx.anyValueTypeIdx))
            ) {
              const inferred = inferParamTypeFromCallSites(ctx, name, i, sourceFile);
              if (inferred) {
                wasmType = inferred;
              }
            }
            params.push(wasmType);
          }
        }
        const r = ctx.checker.getReturnTypeOfSignature(sig);
        const rUnwrapped = isAsync ? unwrapPromiseType(r, ctx.checker) : r;
        results = isVoidType(rUnwrapped) ? [] : [resolveWasmType(ctx, rUnwrapped)];
      }
      const optionalParams = [];
      for (let i = 0; i < stmt.parameters.length; i++) {
        const param = stmt.parameters[i];
        if (param.questionToken || param.initializer) {
          const info = { index: i, type: params[i] };
          if (param.initializer) {
            const cd = extractConstantDefault(param.initializer, params[i]);
            if (cd) {
              info.constantDefault = cd;
            } else {
              info.hasExpressionDefault = true;
            }
          }
          optionalParams.push(info);
        }
      }
      if (optionalParams.length > 0) {
        ctx.funcOptionalParams.set(name, optionalParams);
      }
      const typeIdx = addFuncType(ctx, params, results, `${name}_type`);
      const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
      ctx.funcMap.set(name, funcIdx);
      const isExported = isEntryFile && hasExportModifier(stmt);
      const func = {
        name,
        typeIdx,
        locals: [],
        body: [],
        exported: isExported,
      };
      ctx.mod.functions.push(func);
      if (isExported) {
        ctx.mod.exports.push({
          name,
          desc: { kind: "func", index: funcIdx },
        });
      }
    }
  }
  function registerModuleGlobal(name, wasmType) {
    if (ctx.funcMap.has(name)) return;
    if (ctx.moduleGlobals.has(name)) return;
    if (ctx.classSet.has(name)) return;
    const init =
      wasmType.kind === "f64"
        ? [{ op: "f64.const", value: 0 }]
        : wasmType.kind === "i32"
          ? [{ op: "i32.const", value: 0 }]
          : wasmType.kind === "i64"
            ? [{ op: "i64.const", value: 0n }]
            : wasmType.kind === "ref_null" || wasmType.kind === "ref"
              ? [
                  {
                    op: "ref.null",
                    typeIdx: wasmType.typeIdx,
                  },
                ]
              : [{ op: "ref.null.extern" }];
    const globalType =
      wasmType.kind === "ref"
        ? {
            kind: "ref_null",
            typeIdx: wasmType.typeIdx,
          }
        : wasmType;
    const globalIdx = nextModuleGlobalIdx(ctx);
    ctx.mod.globals.push({
      name: `__mod_${name}`,
      type: globalType,
      mutable: true,
      init,
    });
    ctx.moduleGlobals.set(name, globalIdx);
  }
  function registerBindingNames(pattern) {
    for (const element of pattern.elements) {
      if (ts15.isOmittedExpression(element)) continue;
      if (ts15.isIdentifier(element.name)) {
        const elemType = ctx.checker.getTypeAtLocation(element);
        const wasmType = resolveWasmType(ctx, elemType);
        registerModuleGlobal(element.name.text, wasmType);
      } else if (ts15.isObjectBindingPattern(element.name) || ts15.isArrayBindingPattern(element.name)) {
        registerBindingNames(element.name);
      }
    }
  }
  function registerVarDeclListGlobals(list) {
    if (list.flags & (ts15.NodeFlags.Let | ts15.NodeFlags.Const)) return;
    for (const decl of list.declarations) {
      if (ts15.isIdentifier(decl.name)) {
        const varType = ctx.checker.getTypeAtLocation(decl);
        const wasmType =
          decl.initializer && isPromiseHostCallExpr(decl.initializer, ctx)
            ? { kind: "externref" }
            : resolveWasmType(ctx, varType);
        registerModuleGlobal(decl.name.text, wasmType);
      } else if (ts15.isObjectBindingPattern(decl.name) || ts15.isArrayBindingPattern(decl.name)) {
        registerBindingNames(decl.name);
      }
    }
  }
  function walkModuleStmtForVars(stmt) {
    if (ts15.isVariableStatement(stmt)) {
      if (hasDeclareModifier(stmt)) return;
      registerVarDeclListGlobals(stmt.declarationList);
      return;
    }
    if (ts15.isBlock(stmt)) {
      for (const s of stmt.statements) walkModuleStmtForVars(s);
      return;
    }
    if (ts15.isIfStatement(stmt)) {
      walkModuleStmtForVars(stmt.thenStatement);
      if (stmt.elseStatement) walkModuleStmtForVars(stmt.elseStatement);
      return;
    }
    if (ts15.isWhileStatement(stmt) || ts15.isDoStatement(stmt)) {
      walkModuleStmtForVars(stmt.statement);
      return;
    }
    if (ts15.isForStatement(stmt)) {
      if (stmt.initializer && ts15.isVariableDeclarationList(stmt.initializer)) {
        registerVarDeclListGlobals(stmt.initializer);
      }
      walkModuleStmtForVars(stmt.statement);
      return;
    }
    if (ts15.isForInStatement(stmt) || ts15.isForOfStatement(stmt)) {
      if (stmt.initializer && ts15.isVariableDeclarationList(stmt.initializer)) {
        registerVarDeclListGlobals(stmt.initializer);
      }
      walkModuleStmtForVars(stmt.statement);
      return;
    }
    if (ts15.isLabeledStatement(stmt)) {
      walkModuleStmtForVars(stmt.statement);
      return;
    }
    if (ts15.isTryStatement(stmt)) {
      for (const s of stmt.tryBlock.statements) walkModuleStmtForVars(s);
      if (stmt.catchClause) {
        for (const s of stmt.catchClause.block.statements) walkModuleStmtForVars(s);
      }
      if (stmt.finallyBlock) {
        for (const s of stmt.finallyBlock.statements) walkModuleStmtForVars(s);
      }
      return;
    }
    if (ts15.isSwitchStatement(stmt)) {
      for (const clause of stmt.caseBlock.clauses) {
        for (const s of clause.statements) walkModuleStmtForVars(s);
      }
    }
  }
  for (const stmt of sourceFile.statements) {
    if (ts15.isVariableStatement(stmt)) {
      if (hasDeclareModifier(stmt)) continue;
      const isLetOrConst = (stmt.declarationList.flags & (ts15.NodeFlags.Let | ts15.NodeFlags.Const)) !== 0;
      for (const decl of stmt.declarationList.declarations) {
        if (ts15.isIdentifier(decl.name)) {
          const varType = ctx.checker.getTypeAtLocation(decl);
          const wasmType =
            decl.initializer && isPromiseHostCallExpr(decl.initializer, ctx)
              ? { kind: "externref" }
              : resolveWasmType(ctx, varType);
          registerModuleGlobal(decl.name.text, wasmType);
          if (isLetOrConst) {
            ctx.tdzLetConstNames.add(decl.name.text);
          }
        } else if (ts15.isObjectBindingPattern(decl.name) || ts15.isArrayBindingPattern(decl.name)) {
          registerBindingNames(decl.name);
        }
      }
      const hasNonClassDecl = stmt.declarationList.declarations.some(
        (d) => !(ts15.isIdentifier(d.name) && d.initializer && ts15.isClassExpression(d.initializer)),
      );
      if (hasNonClassDecl) {
        ctx.moduleInitStatements.push(stmt);
      }
      continue;
    }
    if (
      ts15.isForStatement(stmt) ||
      ts15.isForInStatement(stmt) ||
      ts15.isForOfStatement(stmt) ||
      ts15.isWhileStatement(stmt) ||
      ts15.isDoStatement(stmt)
    ) {
      walkModuleStmtForVars(stmt);
      ctx.moduleInitStatements.push(stmt);
      continue;
    }
    if (ts15.isIfStatement(stmt)) {
      walkModuleStmtForVars(stmt);
      ctx.moduleInitStatements.push(stmt);
      continue;
    }
    if (ts15.isTryStatement(stmt)) {
      walkModuleStmtForVars(stmt);
      ctx.moduleInitStatements.push(stmt);
      continue;
    }
    if (ts15.isSwitchStatement(stmt)) {
      walkModuleStmtForVars(stmt);
      ctx.moduleInitStatements.push(stmt);
      continue;
    }
    if (ts15.isLabeledStatement(stmt)) {
      walkModuleStmtForVars(stmt);
      ctx.moduleInitStatements.push(stmt);
      continue;
    }
    if (ts15.isBlock(stmt)) {
      walkModuleStmtForVars(stmt);
      ctx.moduleInitStatements.push(stmt);
    }
  }
  for (const stmt of sourceFile.statements) {
    if (!ts15.isExpressionStatement(stmt)) continue;
    const expr = stmt.expression;
    if (ts15.isNewExpression(expr)) {
      ctx.moduleInitStatements.push(stmt);
      continue;
    }
    if (ts15.isCallExpression(expr)) {
      ctx.moduleInitStatements.push(stmt);
      continue;
    }
    if (ts15.isPrefixUnaryExpression(expr) || ts15.isPostfixUnaryExpression(expr)) {
      ctx.moduleInitStatements.push(stmt);
      continue;
    }
    if (!ts15.isBinaryExpression(expr)) continue;
    const opKind = expr.operatorToken.kind;
    const isAssignOp =
      opKind === ts15.SyntaxKind.EqualsToken ||
      opKind === ts15.SyntaxKind.PlusEqualsToken ||
      opKind === ts15.SyntaxKind.MinusEqualsToken ||
      opKind === ts15.SyntaxKind.AsteriskEqualsToken ||
      opKind === ts15.SyntaxKind.SlashEqualsToken ||
      opKind === ts15.SyntaxKind.PercentEqualsToken ||
      opKind === ts15.SyntaxKind.AmpersandEqualsToken ||
      opKind === ts15.SyntaxKind.BarEqualsToken ||
      opKind === ts15.SyntaxKind.CaretEqualsToken ||
      opKind === ts15.SyntaxKind.LessThanLessThanEqualsToken ||
      opKind === ts15.SyntaxKind.GreaterThanGreaterThanEqualsToken ||
      opKind === ts15.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken;
    if (!isAssignOp) continue;
    let targetName;
    if (ts15.isPropertyAccessExpression(expr.left) && ts15.isIdentifier(expr.left.expression)) {
      targetName = expr.left.expression.text;
    } else if (ts15.isElementAccessExpression(expr.left) && ts15.isIdentifier(expr.left.expression)) {
      targetName = expr.left.expression.text;
    }
    if (targetName && ctx.moduleGlobals.has(targetName)) {
      ctx.moduleInitStatements.push(stmt);
    }
  }
}
function collectInterface(ctx, decl) {
  const name = decl.name.text;
  const fields = [];
  for (const member of decl.members) {
    if (ts15.isPropertySignature(member) && member.name) {
      const memberName = member.name.text;
      const memberType = ctx.checker.getTypeAtLocation(member);
      const wasmType = mapTsTypeToWasm(memberType, ctx.checker);
      fields.push({
        name: memberName,
        type: wasmType,
        mutable: true,
      });
    }
  }
  const typeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "struct",
    name,
    fields,
  });
  ctx.structMap.set(name, typeIdx);
  ctx.typeIdxToStructName.set(typeIdx, name);
  ctx.structFields.set(name, fields);
}
function resolveStructFieldTypes(ctx, sourceFile) {
  for (const stmt of sourceFile.statements) {
    if (!ts15.isInterfaceDeclaration(stmt) && !ts15.isTypeAliasDeclaration(stmt)) continue;
    const name = ts15.isInterfaceDeclaration(stmt) ? stmt.name.text : stmt.name.text;
    const fields = ctx.structFields.get(name);
    const structTypeIdx = ctx.structMap.get(name);
    if (!fields || structTypeIdx === void 0) continue;
    let changed = false;
    for (let i = 0; i < fields.length; i++) {
      const field = fields[i];
      if (field.type.kind !== "externref") continue;
      let memberTsType;
      if (ts15.isInterfaceDeclaration(stmt)) {
        for (const member of stmt.members) {
          if (ts15.isPropertySignature(member) && member.name) {
            const memberName = member.name.text;
            if (memberName === field.name) {
              memberTsType = ctx.checker.getTypeAtLocation(member);
              break;
            }
          }
        }
      } else {
        const aliasType = ctx.checker.getTypeAtLocation(stmt);
        const props = aliasType.getProperties();
        for (const prop of props) {
          if (prop.name === field.name) {
            memberTsType = ctx.checker.getTypeOfSymbol(prop);
            break;
          }
        }
      }
      if (!memberTsType) continue;
      const resolved = resolveWasmType(ctx, memberTsType);
      if (resolved.kind === "ref" || resolved.kind === "ref_null") {
        field.type = resolved;
        changed = true;
      }
    }
    if (changed) {
      const typeDef = ctx.mod.types[structTypeIdx];
      if (typeDef && typeDef.kind === "struct") {
        typeDef.fields = fields;
      }
    }
  }
}
function collectObjectType(ctx, name, type) {
  const fields = [];
  for (const prop of type.getProperties()) {
    const propType = ctx.checker.getTypeOfSymbol(prop);
    const wasmType = mapTsTypeToWasm(propType, ctx.checker);
    fields.push({
      name: prop.name,
      type: wasmType,
      mutable: true,
    });
  }
  if (fields.length > 0) {
    const typeIdx = ctx.mod.types.length;
    ctx.mod.types.push({
      kind: "struct",
      name,
      fields,
    });
    ctx.structMap.set(name, typeIdx);
    ctx.typeIdxToStructName.set(typeIdx, name);
    ctx.structFields.set(name, fields);
  }
}
function compileDeclarations(ctx, sourceFile) {
  const funcByName = /* @__PURE__ */ new Map();
  for (let i = 0; i < ctx.mod.functions.length; i++) {
    funcByName.set(ctx.mod.functions[i].name, i);
  }
  function compileClassesFromStatements(stmts, insideFunction = false) {
    for (const stmt of stmts) {
      if (ts15.isClassDeclaration(stmt) && stmt.name && !hasDeclareModifier(stmt)) {
        if (insideFunction) {
          ctx.deferredClassBodies.add(stmt.name.text);
        } else {
          try {
            compileClassBodies(ctx, stmt, funcByName);
          } catch (e) {
            const msg = e instanceof Error ? e.message : String(e);
            reportError(ctx, stmt, `Internal error compiling class '${stmt.name.text}': ${msg}`);
          }
        }
      } else if (ts15.isVariableStatement(stmt) && !hasDeclareModifier(stmt)) {
        for (const decl of stmt.declarationList.declarations) {
          if (ts15.isIdentifier(decl.name) && decl.initializer && ts15.isClassExpression(decl.initializer)) {
            if (insideFunction) {
              ctx.deferredClassBodies.add(decl.name.text);
            } else {
              try {
                compileClassBodies(ctx, decl.initializer, funcByName, decl.name.text);
              } catch (e) {
                const msg = e instanceof Error ? e.message : String(e);
                reportError(ctx, decl, `Internal error compiling class expression: ${msg}`);
              }
            }
          }
          if (decl.initializer) {
            compileClassesFromFunctionBody(decl.initializer);
          }
        }
      } else if (ts15.isFunctionDeclaration(stmt) && stmt.body) {
        compileClassesFromStatements(stmt.body.statements, true);
      } else if (ts15.isIfStatement(stmt)) {
        if (ts15.isBlock(stmt.thenStatement)) {
          compileClassesFromStatements(stmt.thenStatement.statements);
        }
        if (stmt.elseStatement && ts15.isBlock(stmt.elseStatement)) {
          compileClassesFromStatements(stmt.elseStatement.statements);
        }
      } else if (ts15.isBlock(stmt)) {
        compileClassesFromStatements(stmt.statements);
      } else if (
        ts15.isForStatement(stmt) ||
        ts15.isForInStatement(stmt) ||
        ts15.isForOfStatement(stmt) ||
        ts15.isWhileStatement(stmt) ||
        ts15.isDoStatement(stmt)
      ) {
        const body = stmt.statement;
        if (ts15.isBlock(body)) {
          compileClassesFromStatements(body.statements);
        }
      } else if (ts15.isSwitchStatement(stmt)) {
        for (const clause of stmt.caseBlock.clauses) {
          compileClassesFromStatements(clause.statements);
        }
      } else if (ts15.isTryStatement(stmt)) {
        compileClassesFromStatements(stmt.tryBlock.statements);
        if (stmt.catchClause) {
          compileClassesFromStatements(stmt.catchClause.block.statements);
        }
        if (stmt.finallyBlock) {
          compileClassesFromStatements(stmt.finallyBlock.statements);
        }
      } else if (ts15.isLabeledStatement(stmt)) {
        if (ts15.isBlock(stmt.statement)) {
          compileClassesFromStatements(stmt.statement.statements);
        }
      }
      compileAnonymousClassBodiesInNode(stmt);
    }
  }
  function compileClassesFromFunctionBody(expr) {
    if (ts15.isArrowFunction(expr)) {
      if (ts15.isBlock(expr.body)) {
        compileClassesFromStatements(expr.body.statements, true);
      }
    } else if (ts15.isFunctionExpression(expr)) {
      if (expr.body) {
        compileClassesFromStatements(expr.body.statements, true);
      }
      compileAnonymousClassBodiesInNode(expr);
    }
  }
  const compiledAnonClasses = /* @__PURE__ */ new Set();
  function compileAnonClassIfNeeded(classExpr) {
    if (compiledAnonClasses.has(classExpr)) return;
    const syntheticName = ctx.anonClassExprNames.get(classExpr);
    if (syntheticName) {
      compiledAnonClasses.add(classExpr);
      try {
        compileClassBodies(ctx, classExpr, funcByName, syntheticName);
      } catch (e) {
        const msg = e instanceof Error ? e.message : String(e);
        reportError(ctx, classExpr, `Internal error compiling anonymous class: ${msg}`);
      }
    }
  }
  function compileAnonymousClassBodiesInNode(node) {
    if (ts15.isNewExpression(node)) {
      let inner = node.expression;
      while (ts15.isParenthesizedExpression(inner)) {
        inner = inner.expression;
      }
      if (ts15.isClassExpression(inner)) {
        compileAnonClassIfNeeded(inner);
      }
    }
    if (ts15.isClassExpression(node)) {
      compileAnonClassIfNeeded(node);
    }
    ts15.forEachChild(node, compileAnonymousClassBodiesInNode);
  }
  compileClassesFromStatements(sourceFile.statements);
  for (const name of ctx.tdzLetConstNames) {
    if (!ctx.moduleGlobals.has(name)) continue;
    const flagGlobalIdx = nextModuleGlobalIdx(ctx);
    ctx.mod.globals.push({
      name: `__tdz_${name}`,
      type: { kind: "i32" },
      mutable: true,
      init: [{ op: "i32.const", value: 0 }],
    });
    ctx.tdzGlobals.set(name, flagGlobalIdx);
  }
  const hasModuleInits = ctx.moduleInitStatements.length > 0;
  const hasStaticInits = ctx.staticInitExprs.length > 0;
  let compiledInitFctx = null;
  function compileModuleInitBody() {
    const initFctx = {
      name: "__module_init",
      params: [],
      locals: [],
      localMap: /* @__PURE__ */ new Map(),
      returnType: null,
      body: [],
      blockDepth: 0,
      breakStack: [],
      continueStack: [],
      labelMap: /* @__PURE__ */ new Map(),
      savedBodies: [],
    };
    ctx.currentFunc = initFctx;
    for (const { globalIdx, initializer } of ctx.staticInitExprs) {
      const globalDef = ctx.mod.globals[localGlobalIdx(ctx, globalIdx)];
      compileExpression2(ctx, initFctx, initializer, globalDef?.type);
      initFctx.body.push({ op: "global.set", index: globalIdx });
    }
    for (const stmt of ctx.moduleInitStatements) {
      compileStatement(ctx, initFctx, stmt);
    }
    ctx.currentFunc = null;
    return initFctx;
  }
  if (hasModuleInits || hasStaticInits) {
    compiledInitFctx = compileModuleInitBody();
    ctx.pendingInitBody = compiledInitFctx.body;
  }
  for (const stmt of sourceFile.statements) {
    if (ts15.isFunctionDeclaration(stmt) && stmt.name && !hasDeclareModifier(stmt)) {
      if (stmt.body) {
        const idx = funcByName.get(stmt.name.text);
        if (idx !== void 0) {
          const func = ctx.mod.functions[idx];
          try {
            compileFunctionBody(ctx, stmt, func);
            registerInlinableFunction(ctx, stmt.name.text, func);
          } catch (e) {
            const msg = e instanceof Error ? e.message : String(e);
            reportError(ctx, stmt, `Internal error compiling function '${stmt.name.text}': ${msg}`);
          }
        }
      }
    }
  }
  if (hasModuleInits || hasStaticInits) {
    compiledInitFctx = compileModuleInitBody();
    ctx.pendingInitBody = compiledInitFctx.body;
  }
  ctx.pendingInitBody = null;
  function shiftLocalIndices(instrs, shift) {
    for (const instr of instrs) {
      if (
        (instr.op === "local.get" || instr.op === "local.set" || instr.op === "local.tee") &&
        typeof instr.index === "number"
      ) {
        instr.index += shift;
      }
      const a = instr;
      if (a.then) shiftLocalIndices(a.then, shift);
      if (a.else) shiftLocalIndices(a.else, shift);
      if (a.body) shiftLocalIndices(a.body, shift);
      if (a.instrs) shiftLocalIndices(a.instrs, shift);
    }
  }
  if (compiledInitFctx && compiledInitFctx.body.length > 0) {
    ctx.mod.hasTopLevelStatements = true;
    const mainIdx = funcByName.get("main");
    if (mainIdx !== void 0) {
      const mainFunc = ctx.mod.functions[mainIdx];
      mainFunc.body = [...compiledInitFctx.body, ...mainFunc.body];
      const existingLocals = mainFunc.locals.length;
      mainFunc.locals = [...mainFunc.locals, ...compiledInitFctx.locals];
      if (existingLocals > 0) {
        shiftLocalIndices(compiledInitFctx.body, existingLocals);
      }
    } else {
      const guardGlobalIdx = nextModuleGlobalIdx(ctx);
      ctx.mod.globals.push({
        name: "__init_done",
        type: { kind: "i32" },
        mutable: true,
        init: [{ op: "i32.const", value: 0 }],
      });
      const initTypeIdx = addFuncType(ctx, [], [], "__module_init_type");
      const initFuncIdx = ctx.numImportFuncs + ctx.mod.functions.length;
      ctx.mod.functions.push({
        name: "__module_init",
        typeIdx: initTypeIdx,
        locals: compiledInitFctx.locals,
        body: compiledInitFctx.body,
        exported: false,
      });
      for (const func of ctx.mod.functions) {
        if (func.exported && func.name !== "__module_init") {
          const guardPreamble = [
            { op: "global.get", index: guardGlobalIdx },
            { op: "i32.eqz" },
            {
              op: "if",
              blockType: { kind: "empty" },
              then: [
                { op: "i32.const", value: 1 },
                { op: "global.set", index: guardGlobalIdx },
                { op: "call", funcIdx: initFuncIdx },
              ],
            },
          ];
          func.body = [...guardPreamble, ...func.body];
        }
      }
    }
  }
}
function fixupStructNewArgCounts(ctx) {
  const typeIdxToClass = /* @__PURE__ */ new Map();
  for (const [className, typeIdx] of ctx.structMap.entries()) {
    if (ctx.classSet.has(className)) {
      typeIdxToClass.set(typeIdx, className);
    }
  }
  if (typeIdxToClass.size === 0) return;
  function defaultInstrForType(type) {
    switch (type.kind) {
      case "f64":
        return [{ op: "f64.const", value: 0 }];
      case "i32":
        return [{ op: "i32.const", value: 0 }];
      case "externref":
        return [{ op: "ref.null.extern" }];
      case "ref":
        return [{ op: "ref.null", typeIdx: type.typeIdx }, { op: "ref.as_non_null" }];
      case "ref_null":
        return [{ op: "ref.null", typeIdx: type.typeIdx }];
      default:
        if (type.kind === "i64") {
          return [{ op: "i64.const", value: 0n }];
        }
        if (type.kind === "eqref") {
          return [{ op: "ref.null.eq" }];
        }
        return [{ op: "i32.const", value: 0 }];
    }
  }
  function fixupInstrs(instrs) {
    for (let i = 0; i < instrs.length; i++) {
      const instr = instrs[i];
      if ("body" in instr && Array.isArray(instr.body)) {
        fixupInstrs(instr.body);
      }
      if ("then" in instr && Array.isArray(instr.then)) {
        fixupInstrs(instr.then);
      }
      if ("else" in instr && Array.isArray(instr.else)) {
        fixupInstrs(instr.else);
      }
      if ("catches" in instr && Array.isArray(instr.catches)) {
        for (const c of instr.catches) {
          if (Array.isArray(c.body)) fixupInstrs(c.body);
        }
      }
      if ("catchAll" in instr && Array.isArray(instr.catchAll)) {
        fixupInstrs(instr.catchAll);
      }
      if (instr.op !== "struct.new") continue;
      const typeIdx = instr.typeIdx;
      const typeDef = ctx.mod.types[typeIdx];
      if (!typeDef || typeDef.kind !== "struct") continue;
      const className = typeIdxToClass.get(typeIdx);
      if (!className) continue;
      const expectedFieldCount = typeDef.fields.length;
      const fields = ctx.structFields.get(className);
      if (!fields) continue;
      let pushedCount = 0;
      let j = i - 1;
      while (j >= 0) {
        const prev = instrs[j];
        const op = prev.op;
        if (
          op === "f64.const" ||
          op === "i32.const" ||
          op === "i64.const" ||
          op === "ref.null" ||
          op === "ref.null.extern" ||
          op === "ref.null.eq" ||
          op === "ref.as_non_null"
        ) {
          if (op !== "ref.as_non_null") {
            pushedCount++;
          }
          j--;
        } else {
          break;
        }
      }
      if (pushedCount < expectedFieldCount && pushedCount > 0) {
        const newInstrs = [];
        for (let k = pushedCount; k < expectedFieldCount; k++) {
          const field = typeDef.fields[k];
          if (field.name === "__tag") {
            const tagValue = ctx.classTagMap.get(className) ?? 0;
            newInstrs.push({ op: "i32.const", value: tagValue });
          } else {
            newInstrs.push(...defaultInstrForType(field.type));
          }
        }
        instrs.splice(i, 0, ...newInstrs);
        i += newInstrs.length;
      }
    }
  }
  for (const func of ctx.mod.functions) {
    if (func.body.length > 0) {
      fixupInstrs(func.body);
    }
  }
}
var INTERNAL_FIELD_NAMES = /* @__PURE__ */ new Set(["__tag", "__proto__"]);
var PROP_FLAGS_DEFAULT = 7;
function buildShapePropFlagsTable(ctx) {
  for (const [name, typeIdx] of ctx.structMap) {
    const fields = ctx.structFields.get(name);
    if (!fields || fields.length === 0) continue;
    const userFields = fields.filter((f) => !INTERNAL_FIELD_NAMES.has(f.name));
    if (userFields.length === 0) continue;
    const flags = new Uint8Array(userFields.length);
    flags.fill(PROP_FLAGS_DEFAULT);
    ctx.shapePropFlags.set(typeIdx, flags);
  }
}
function fixupStructNewResultCoercion(ctx) {
  function getLocalType2(func, localIdx) {
    const funcType = ctx.mod.types[func.typeIdx];
    if (!funcType || funcType.kind !== "func") return null;
    const ft = funcType;
    if (localIdx < ft.params.length) {
      return ft.params[localIdx];
    }
    const bodyLocalIdx = localIdx - ft.params.length;
    if (bodyLocalIdx < func.locals.length) {
      return func.locals[bodyLocalIdx].type;
    }
    return null;
  }
  function fixupInstrs(func, instrs) {
    for (let i = 0; i < instrs.length; i++) {
      const instr = instrs[i];
      if ("body" in instr && Array.isArray(instr.body)) {
        fixupInstrs(func, instr.body);
      }
      if ("then" in instr && Array.isArray(instr.then)) {
        fixupInstrs(func, instr.then);
      }
      if ("else" in instr && Array.isArray(instr.else)) {
        fixupInstrs(func, instr.else);
      }
      if ("catches" in instr && Array.isArray(instr.catches)) {
        for (const c of instr.catches) {
          if (Array.isArray(c.body)) fixupInstrs(func, c.body);
        }
      }
      if ("catchAll" in instr && Array.isArray(instr.catchAll)) {
        fixupInstrs(func, instr.catchAll);
      }
      if (instr.op === "array.new_default" && i > 0) {
        const prev = instrs[i - 1];
        if (prev.op === "ref.null.extern") {
          instrs[i - 1] = { op: "i32.const", value: 0 };
        } else {
          let isExternref = false;
          if (prev.op === "local.get") {
            const lt = getLocalType2(func, prev.index);
            if (lt && lt.kind === "externref") isExternref = true;
          } else if (prev.op === "global.get") {
            const gIdx = prev.index;
            const gDef = ctx.mod.globals[gIdx];
            if (gDef && gDef.type.kind === "externref") isExternref = true;
          }
          if (isExternref) {
            const unboxIdx = ctx.funcMap.get("__unbox_number");
            if (unboxIdx !== void 0) {
              instrs.splice(i, 0, { op: "call", funcIdx: unboxIdx }, { op: "i32.trunc_sat_f64_s" });
              i += 2;
            } else {
              instrs[i - 1] = { op: "i32.const", value: 0 };
            }
          }
        }
      }
      if (instr.op !== "struct.new") continue;
      const typeIdx = instr.typeIdx;
      const typeDef = ctx.mod.types[typeIdx];
      const next = instrs[i + 1];
      if (!next) continue;
      if (next.op === "local.set" || next.op === "local.tee") {
        const localIdx = next.index;
        const localType = getLocalType2(func, localIdx);
        if (localType && localType.kind === "externref") {
          instrs.splice(i + 1, 0, { op: "extern.convert_any" });
          i++;
        }
      } else if (next.op === "global.set") {
        const globalIdx = next.index;
        const globalDef = ctx.mod.globals[globalIdx];
        if (globalDef && globalDef.type.kind === "externref") {
          instrs.splice(i + 1, 0, { op: "extern.convert_any" });
          i++;
        }
      }
    }
    for (let j = instrs.length - 1; j > 0; j--) {
      if (instrs[j].op === "extern.convert_any") {
        const prev = instrs[j - 1];
        let isAlreadyExternref = false;
        let isFuncref = false;
        if (prev.op === "extern.convert_any") {
          isAlreadyExternref = true;
        } else if (prev.op === "ref.null.extern") {
          isAlreadyExternref = true;
        } else if (prev.op === "ref.func") {
          isFuncref = true;
        } else if (prev.op === "local.get") {
          const lt = getLocalType2(func, prev.index);
          if (lt && lt.kind === "externref") isAlreadyExternref = true;
          if (lt && lt.kind === "funcref") isFuncref = true;
        } else if (prev.op === "global.get") {
          const gIdx = prev.index;
          const gDef = ctx.mod.globals[gIdx];
          if (gDef && gDef.type.kind === "externref") isAlreadyExternref = true;
          if (gDef && gDef.type.kind === "funcref") isFuncref = true;
        } else if (prev.op === "struct.get") {
          const sTypeIdx = prev.typeIdx;
          const sFieldIdx = prev.fieldIdx;
          const sDef = ctx.mod.types[sTypeIdx];
          if (sDef && sDef.kind === "struct") {
            const sField = sDef.fields[sFieldIdx];
            if (sField) {
              const ft = sField.type;
              if (ft.kind === "funcref") isFuncref = true;
            }
          }
        } else if (prev.op === "ref.cast" || prev.op === "ref.cast_null") {
          const castTypeIdx = prev.typeIdx;
          const castDef = ctx.mod.types[castTypeIdx];
          if (castDef && castDef.kind === "func") isFuncref = true;
        }
        if (isAlreadyExternref || isFuncref) {
          instrs.splice(j, 1);
        }
      }
    }
  }
  for (const func of ctx.mod.functions) {
    if (func.body.length > 0) {
      fixupInstrs(func, func.body);
    }
  }
}
function fixupExternConvertAny(ctx) {
  function getLocalType2(func, localIdx) {
    const funcType = ctx.mod.types[func.typeIdx];
    if (!funcType || funcType.kind !== "func") return null;
    const ft = funcType;
    if (localIdx < ft.params.length) return ft.params[localIdx];
    const bodyLocalIdx = localIdx - ft.params.length;
    if (bodyLocalIdx < func.locals.length) return func.locals[bodyLocalIdx].type;
    return null;
  }
  function fixupInstrs(func, instrs) {
    for (const instr of instrs) {
      if ("body" in instr && Array.isArray(instr.body)) {
        fixupInstrs(func, instr.body);
      }
      if ("then" in instr && Array.isArray(instr.then)) {
        fixupInstrs(func, instr.then);
      }
      if ("else" in instr && Array.isArray(instr.else)) {
        fixupInstrs(func, instr.else);
      }
      if ("catches" in instr && Array.isArray(instr.catches)) {
        for (const c of instr.catches) {
          if (Array.isArray(c.body)) fixupInstrs(func, c.body);
        }
      }
      if ("catchAll" in instr && Array.isArray(instr.catchAll)) {
        fixupInstrs(func, instr.catchAll);
      }
    }
    for (let j = instrs.length - 1; j > 0; j--) {
      if (instrs[j].op !== "extern.convert_any") continue;
      const prev = instrs[j - 1];
      let isAlreadyExternref = false;
      let isFuncref = false;
      if (prev.op === "extern.convert_any") {
        isAlreadyExternref = true;
      } else if (prev.op === "ref.null.extern") {
        isAlreadyExternref = true;
      } else if (prev.op === "ref.func") {
        isFuncref = true;
      } else if (prev.op === "local.get") {
        const lt = getLocalType2(func, prev.index);
        if (lt && lt.kind === "externref") isAlreadyExternref = true;
        if (lt && lt.kind === "funcref") isFuncref = true;
        if (lt && (lt.kind === "ref" || lt.kind === "ref_null") && lt.typeIdx !== void 0) {
          const refDef = ctx.mod.types[lt.typeIdx];
          if (refDef && refDef.kind === "func") isFuncref = true;
        }
      } else if (prev.op === "global.get") {
        const gIdx = prev.index;
        const gDef = ctx.mod.globals[gIdx];
        if (gDef && gDef.type.kind === "externref") isAlreadyExternref = true;
        if (gDef && gDef.type.kind === "funcref") isFuncref = true;
        if (gDef && (gDef.type.kind === "externref" || gDef.type.kind === "ref_extern")) isAlreadyExternref = true;
      } else if (prev.op === "struct.get") {
        const sTypeIdx = prev.typeIdx;
        const sFieldIdx = prev.fieldIdx;
        const sDef = ctx.mod.types[sTypeIdx];
        if (sDef && sDef.kind === "struct") {
          const sField = sDef.fields[sFieldIdx];
          if (sField && sField.type.kind === "funcref") isFuncref = true;
        }
      } else if (prev.op === "ref.cast" || prev.op === "ref.cast_null") {
        const castTypeIdx = prev.typeIdx;
        const castDef = ctx.mod.types[castTypeIdx];
        if (castDef && castDef.kind === "func") isFuncref = true;
      }
      if (isAlreadyExternref || isFuncref) {
        instrs.splice(j, 1);
      }
    }
    for (let j = 0; j < instrs.length - 1; j++) {
      if (instrs[j].op !== "extern.convert_any") continue;
      const next = instrs[j + 1];
      if (next.op !== "struct.new") continue;
      const snTypeIdx = next.typeIdx;
      const snTypeDef = ctx.mod.types[snTypeIdx];
      if (!snTypeDef || snTypeDef.kind !== "struct") continue;
      const fields = snTypeDef.fields;
      const lastField = fields[fields.length - 1];
      if (!lastField) continue;
      if (lastField.type.kind === "ref" || lastField.type.kind === "ref_null") {
        instrs.splice(j, 1);
        j--;
      }
    }
    for (let j = 0; j < instrs.length; j++) {
      const instr = instrs[j];
      if (instr.op !== "return_call" && instr.op !== "call") continue;
      const funcIdx = instr.funcIdx;
      if (funcIdx === void 0) continue;
      const totalImports = ctx.mod.imports.filter((imp) => imp.desc?.kind === "func").length;
      let targetTypeIdx;
      if (funcIdx < totalImports) {
        let importIdx = 0;
        for (const imp of ctx.mod.imports) {
          if (imp.desc?.kind === "func") {
            if (importIdx === funcIdx) {
              targetTypeIdx = imp.desc.typeIdx;
              break;
            }
            importIdx++;
          }
        }
      } else {
        const localFuncIdx = funcIdx - totalImports;
        const targetFunc = ctx.mod.functions[localFuncIdx];
        if (targetFunc) targetTypeIdx = targetFunc.typeIdx;
      }
      if (targetTypeIdx === void 0) continue;
      const targetType = ctx.mod.types[targetTypeIdx];
      if (!targetType || targetType.kind !== "func") continue;
      const params = targetType.params;
      let pos = j;
      for (let pi = params.length - 1; pi >= 0; pi--) {
        pos--;
        if (pos < 0) break;
        const argInstr = instrs[pos];
        if (argInstr.op === "struct.new") {
          const snTypeIdx = argInstr.typeIdx;
          const snTypeDef = ctx.mod.types[snTypeIdx];
          if (snTypeDef && snTypeDef.kind === "struct") {
            const numFields = snTypeDef.fields.length;
            pos -= numFields;
          }
          continue;
        }
        if (argInstr.op === "array.new_fixed") {
          const size = argInstr.size ?? 0;
          pos -= size;
          continue;
        }
        if (argInstr.op === "call" || argInstr.op === "return_call") {
          const callFuncIdx = argInstr.funcIdx;
          if (callFuncIdx !== void 0) {
            const callTotalImports = ctx.mod.imports.filter((imp) => imp.desc?.kind === "func").length;
            let callTargetTypeIdx;
            if (callFuncIdx < callTotalImports) {
              let ci = 0;
              for (const imp of ctx.mod.imports) {
                if (imp.desc?.kind === "func") {
                  if (ci === callFuncIdx) {
                    callTargetTypeIdx = imp.desc.typeIdx;
                    break;
                  }
                  ci++;
                }
              }
            } else {
              const f = ctx.mod.functions[callFuncIdx - callTotalImports];
              if (f) callTargetTypeIdx = f.typeIdx;
            }
            if (callTargetTypeIdx !== void 0) {
              const callFt = ctx.mod.types[callTargetTypeIdx];
              if (callFt && callFt.kind === "func") {
                pos -= callFt.params.length;
              }
            }
          }
          continue;
        }
        const paramType = params[pi];
        if (argInstr.op === "ref.null.extern" && (paramType.kind === "ref" || paramType.kind === "ref_null")) {
          instrs[pos] = { op: "ref.null", typeIdx: paramType.typeIdx };
        }
      }
    }
  }
  for (const func of ctx.mod.functions) {
    if (func.body.length > 0) {
      fixupInstrs(func, func.body);
    }
  }
}
function collectDeclaredFuncRefs(ctx) {
  const refs = /* @__PURE__ */ new Set();
  function scanInstrs(instrs) {
    for (const instr of instrs) {
      if (instr.op === "ref.func") {
        refs.add(instr.funcIdx);
      }
      if ("body" in instr && Array.isArray(instr.body)) {
        scanInstrs(instr.body);
      }
      if ("then" in instr && Array.isArray(instr.then)) {
        scanInstrs(instr.then);
      }
      if ("else" in instr && Array.isArray(instr.else)) {
        scanInstrs(instr.else);
      }
      if ("catches" in instr && Array.isArray(instr.catches)) {
        for (const c of instr.catches) {
          if (Array.isArray(c.body)) scanInstrs(c.body);
        }
      }
      if ("catchAll" in instr && Array.isArray(instr.catchAll)) {
        scanInstrs(instr.catchAll);
      }
    }
  }
  for (const func of ctx.mod.functions) {
    scanInstrs(func.body);
  }
  if (refs.size > 0) {
    ctx.mod.declaredFuncRefs = [...refs].sort((a, b) => a - b);
  }
}
function compileClassBodies(ctx, decl, funcByName, syntheticName) {
  const className = syntheticName ?? decl.name?.text;
  if (!className) {
    reportError(ctx, decl, "Cannot compile unnamed class");
    return;
  }
  const structTypeIdx = ctx.structMap.get(className);
  const fields = ctx.structFields.get(className);
  if (structTypeIdx === void 0 || !fields) {
    reportError(ctx, decl, `Unknown class struct type: ${className}`);
    return;
  }
  const ctor = decl.members.find(ts15.isConstructorDeclaration);
  const ctorName = `${className}_new`;
  const ctorLocalIdx = funcByName.get(ctorName);
  if (ctorLocalIdx !== void 0) {
    const func = ctx.mod.functions[ctorLocalIdx];
    const params = [];
    if (ctor) {
      for (let pi = 0; pi < ctor.parameters.length; pi++) {
        const param = ctor.parameters[pi];
        const paramName = ts15.isIdentifier(param.name) ? param.name.text : `__param${pi}`;
        const paramType = ctx.checker.getTypeAtLocation(param);
        let wasmType = resolveWasmType(ctx, paramType);
        if ((param.initializer || param.questionToken) && wasmType.kind === "ref") {
          wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
        }
        params.push({ name: paramName, type: wasmType });
      }
    }
    const fctx = {
      name: ctorName,
      params,
      locals: [],
      localMap: /* @__PURE__ */ new Map(),
      returnType: { kind: "ref", typeIdx: structTypeIdx },
      body: [],
      blockDepth: 0,
      breakStack: [],
      continueStack: [],
      labelMap: /* @__PURE__ */ new Map(),
      savedBodies: [],
      isConstructor: true,
    };
    {
      const resolvedParams = params.map((p) => p.type);
      const resolvedResults = [{ kind: "ref", typeIdx: structTypeIdx }];
      const updatedTypeIdx = addFuncType(ctx, resolvedParams, resolvedResults, `${ctorName}_type`);
      if (updatedTypeIdx !== func.typeIdx) {
        func.typeIdx = updatedTypeIdx;
      }
    }
    for (let i = 0; i < params.length; i++) {
      fctx.localMap.set(params[i].name, i);
    }
    const selfLocal = allocLocal(fctx, "__self", {
      kind: "ref",
      typeIdx: structTypeIdx,
    });
    for (const field of fields) {
      if (field.name === "__tag") {
        const tagValue = ctx.classTagMap.get(className) ?? 0;
        fctx.body.push({ op: "i32.const", value: tagValue });
      } else if (field.type.kind === "f64") {
        fctx.body.push({ op: "f64.const", value: 0 });
      } else if (field.type.kind === "i32") {
        fctx.body.push({ op: "i32.const", value: 0 });
      } else if (field.type.kind === "externref") {
        fctx.body.push({ op: "ref.null.extern" });
      } else if (field.type.kind === "ref" || field.type.kind === "ref_null") {
        fctx.body.push({ op: "ref.null", typeIdx: field.type.typeIdx });
      } else if (field.type.kind === "i64") {
        fctx.body.push({ op: "i64.const", value: 0n });
      } else if (field.type.kind === "eqref") {
        fctx.body.push({ op: "ref.null.eq" });
      } else {
        fctx.body.push({ op: "i32.const", value: 0 });
      }
    }
    fctx.body.push({ op: "struct.new", typeIdx: structTypeIdx });
    fctx.body.push({ op: "local.set", index: selfLocal });
    fctx.localMap.set("this", selfLocal);
    ctx.currentFunc = fctx;
    if (ctor) {
      for (let i = 0; i < ctor.parameters.length; i++) {
        const param = ctor.parameters[i];
        if (!param.initializer) continue;
        const paramIdx = i;
        const paramType = params[i].type;
        const savedBody = pushBody(fctx);
        const ctorDfltType = compileExpression2(ctx, fctx, param.initializer, paramType);
        if (ctorDfltType && !valTypesMatch(ctorDfltType, paramType)) {
          coerceType3(ctx, fctx, ctorDfltType, paramType);
        }
        fctx.body.push({ op: "local.set", index: paramIdx });
        const thenInstrs = fctx.body;
        popBody(fctx, savedBody);
        if (paramType.kind === "externref") {
          fctx.body.push({ op: "local.get", index: paramIdx });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
          });
        } else if (paramType.kind === "ref_null" || paramType.kind === "ref") {
          fctx.body.push({ op: "local.get", index: paramIdx });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
          });
        } else if (paramType.kind === "i32") {
          fctx.body.push({ op: "local.get", index: paramIdx });
          fctx.body.push({ op: "i32.eqz" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
          });
        } else if (paramType.kind === "f64") {
          fctx.body.push({ op: "local.get", index: paramIdx });
          fctx.body.push({ op: "local.get", index: paramIdx });
          fctx.body.push({ op: "f64.ne" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
          });
        }
      }
    }
    if (!ctor) {
      const parentClassName = ctx.classParentMap.get(className);
      if (parentClassName) {
        const ancestors = [];
        const visitedAnc = /* @__PURE__ */ new Set();
        let anc = parentClassName;
        while (anc && !visitedAnc.has(anc)) {
          visitedAnc.add(anc);
          ancestors.unshift(anc);
          anc = ctx.classParentMap.get(anc);
        }
        for (const ancName of ancestors) {
          const ancDecl = ctx.classDeclarationMap.get(ancName);
          if (!ancDecl) continue;
          for (const member of ancDecl.members) {
            if (ts15.isPropertyDeclaration(member) && member.name && member.initializer && !hasStaticModifier(member)) {
              const fieldName = resolveClassMemberName(ctx, member.name);
              if (!fieldName) continue;
              const fieldIdx = fields.findIndex((f) => f.name === fieldName);
              if (fieldIdx !== -1) {
                fctx.body.push({ op: "local.get", index: selfLocal });
                compileExpression2(ctx, fctx, member.initializer, fields[fieldIdx].type);
                fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
              }
            }
          }
          const ancCtor = ancDecl.members.find(ts15.isConstructorDeclaration);
          if (ancCtor?.body) {
            for (const stmt of ancCtor.body.statements) {
              if (
                ts15.isExpressionStatement(stmt) &&
                ts15.isCallExpression(stmt.expression) &&
                stmt.expression.expression.kind === ts15.SyntaxKind.SuperKeyword
              ) {
                continue;
              }
              if (
                ts15.isExpressionStatement(stmt) &&
                ts15.isBinaryExpression(stmt.expression) &&
                stmt.expression.operatorToken.kind === ts15.SyntaxKind.EqualsToken &&
                ts15.isPropertyAccessExpression(stmt.expression.left) &&
                stmt.expression.left.expression.kind === ts15.SyntaxKind.ThisKeyword
              ) {
                const rawName = stmt.expression.left.name.text;
                const fieldName = ts15.isPrivateIdentifier(stmt.expression.left.name)
                  ? "__priv_" + rawName.slice(1)
                  : rawName;
                const fieldIdx = fields.findIndex((f) => f.name === fieldName);
                if (fieldIdx !== -1) {
                  fctx.body.push({ op: "local.get", index: selfLocal });
                  compileExpression2(ctx, fctx, stmt.expression.right, fields[fieldIdx].type);
                  fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
                }
              }
            }
          }
        }
      }
    }
    for (const member of decl.members) {
      if (ts15.isPropertyDeclaration(member) && member.name && member.initializer && !hasStaticModifier(member)) {
        const fieldName = resolveClassMemberName(ctx, member.name);
        if (!fieldName) continue;
        const fieldIdx = fields.findIndex((f) => f.name === fieldName);
        if (fieldIdx !== -1) {
          fctx.body.push({ op: "local.get", index: selfLocal });
          compileExpression2(ctx, fctx, member.initializer, fields[fieldIdx].type);
          fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
        }
      }
    }
    if (ctor?.body) {
      for (const stmt of ctor.body.statements) {
        if (
          ts15.isExpressionStatement(stmt) &&
          ts15.isCallExpression(stmt.expression) &&
          stmt.expression.expression.kind === ts15.SyntaxKind.SuperKeyword
        ) {
          compileSuperCall(ctx, fctx, className, selfLocal, stmt.expression, fields);
          continue;
        }
        compileStatement(ctx, fctx, stmt);
      }
    }
    fctx.body.push({ op: "local.get", index: selfLocal });
    cacheStringLiterals(ctx, fctx);
    func.locals = fctx.locals;
    func.body = fctx.body;
    ctx.currentFunc = null;
  }
  const compiledMethods = /* @__PURE__ */ new Set();
  for (const member of decl.members) {
    if (ts15.isMethodDeclaration(member) && member.name) {
      const methodName = resolveClassMemberName(ctx, member.name);
      if (!methodName) continue;
      const fullName = `${className}_${methodName}`;
      if (compiledMethods.has(fullName)) continue;
      compiledMethods.add(fullName);
      const isStatic = ctx.staticMethodSet.has(fullName);
      const methodLocalIdx = funcByName.get(fullName);
      if (methodLocalIdx === void 0) continue;
      const func = ctx.mod.functions[methodLocalIdx];
      const sig = ctx.checker.getSignatureFromDeclaration(member);
      const retType = sig ? ctx.checker.getReturnTypeOfSignature(sig) : void 0;
      const params = isStatic ? [] : [{ name: "this", type: { kind: "ref", typeIdx: structTypeIdx } }];
      for (let pi = 0; pi < member.parameters.length; pi++) {
        const param = member.parameters[pi];
        const paramName = ts15.isIdentifier(param.name) ? param.name.text : `__param${pi}`;
        const paramType = ctx.checker.getTypeAtLocation(param);
        let wasmType = resolveWasmType(ctx, paramType);
        if ((param.initializer || param.questionToken) && wasmType.kind === "ref") {
          wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
        }
        params.push({ name: paramName, type: wasmType });
      }
      const isGeneratorMethod = member.asteriskToken !== void 0;
      const fctx = {
        name: fullName,
        params,
        locals: [],
        localMap: /* @__PURE__ */ new Map(),
        returnType: isGeneratorMethod
          ? { kind: "externref" }
          : retType && !isVoidType(retType)
            ? resolveWasmType(ctx, retType)
            : null,
        body: [],
        blockDepth: 0,
        breakStack: [],
        continueStack: [],
        labelMap: /* @__PURE__ */ new Map(),
        savedBodies: [],
        isGenerator: isGeneratorMethod,
      };
      {
        const resolvedParams = params.map((p) => p.type);
        const resolvedResults = fctx.returnType ? [fctx.returnType] : [];
        const updatedTypeIdx = addFuncType(ctx, resolvedParams, resolvedResults, `${fullName}_type`);
        if (updatedTypeIdx !== func.typeIdx) {
          func.typeIdx = updatedTypeIdx;
        }
      }
      for (let i = 0; i < params.length; i++) {
        fctx.localMap.set(params[i].name, i);
      }
      ctx.currentFunc = fctx;
      for (let pi = 0; pi < member.parameters.length; pi++) {
        const param = member.parameters[pi];
        if (!param.initializer) continue;
        const paramLocalIdx = isStatic ? pi : pi + 1;
        const paramType = params[paramLocalIdx].type;
        const savedBody = pushBody(fctx);
        const methDfltType = compileExpression2(ctx, fctx, param.initializer, paramType);
        if (methDfltType && !valTypesMatch(methDfltType, paramType)) {
          coerceType3(ctx, fctx, methDfltType, paramType);
        }
        fctx.body.push({ op: "local.set", index: paramLocalIdx });
        const thenInstrs = fctx.body;
        popBody(fctx, savedBody);
        if (paramType.kind === "externref") {
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
          });
        } else if (paramType.kind === "ref_null" || paramType.kind === "ref") {
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
          });
        } else if (paramType.kind === "i32") {
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "i32.eqz" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
          });
        } else if (paramType.kind === "f64") {
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "f64.ne" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: thenInstrs,
          });
        }
      }
      for (let pi = 0; pi < member.parameters.length; pi++) {
        const param = member.parameters[pi];
        const paramLocalIdx = isStatic ? pi : pi + 1;
        if (ts15.isObjectBindingPattern(param.name)) {
          destructureParamObject2(ctx, fctx, paramLocalIdx, param.name, params[paramLocalIdx].type);
        } else if (ts15.isArrayBindingPattern(param.name)) {
          destructureParamArray(ctx, fctx, paramLocalIdx, param.name, params[paramLocalIdx].type);
        }
      }
      if (member.body && bodyUsesArguments2(member.body)) {
        const methodParamTypes = params.slice(isStatic ? 0 : 1).map((p) => p.type);
        const paramOffset = isStatic ? 0 : 1;
        emitArgumentsObject(ctx, fctx, methodParamTypes, paramOffset);
      }
      if (isGeneratorMethod && member.body) {
        const bufferLocal = allocLocal(fctx, "__gen_buffer", { kind: "externref" });
        const createBufIdx = ctx.funcMap.get("__gen_create_buffer");
        fctx.body.push({ op: "call", funcIdx: createBufIdx });
        fctx.body.push({ op: "local.set", index: bufferLocal });
        const bodyInstrs = [];
        const outerBody = fctx.body;
        fctx.body = bodyInstrs;
        fctx.generatorReturnDepth = 0;
        fctx.blockDepth++;
        for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]++;
        for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]++;
        for (const stmt of member.body.statements) {
          compileStatement(ctx, fctx, stmt);
        }
        fctx.blockDepth--;
        for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]--;
        for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]--;
        fctx.generatorReturnDepth = void 0;
        fctx.body = outerBody;
        fctx.body.push({
          op: "block",
          blockType: { kind: "empty" },
          body: bodyInstrs,
        });
        const isAsyncMember = member.modifiers?.some((m) => m.kind === 134) ?? false;
        const createGenIdx = ctx.funcMap.get(isAsyncMember ? "__create_async_generator" : "__create_generator");
        fctx.body.push({ op: "local.get", index: bufferLocal });
        fctx.body.push({ op: "call", funcIdx: createGenIdx });
      } else if (member.body) {
        for (const stmt of member.body.statements) {
          compileStatement(ctx, fctx, stmt);
        }
      }
      if (fctx.returnType && !isGeneratorMethod) {
        const lastInstr = fctx.body[fctx.body.length - 1];
        if (!lastInstr || lastInstr.op !== "return") {
          if (fctx.returnType.kind === "f64") {
            fctx.body.push({ op: "f64.const", value: 0 });
          } else if (fctx.returnType.kind === "i32") {
            fctx.body.push({ op: "i32.const", value: 0 });
          } else if (fctx.returnType.kind === "externref") {
            fctx.body.push({ op: "ref.null.extern" });
          } else if (fctx.returnType.kind === "ref" || fctx.returnType.kind === "ref_null") {
            fctx.body.push({
              op: "ref.null",
              typeIdx: fctx.returnType.typeIdx,
            });
          }
        }
      }
      cacheStringLiterals(ctx, fctx);
      func.locals = fctx.locals;
      func.body = fctx.body;
      ctx.currentFunc = null;
    }
  }
  const compiledAccessors = /* @__PURE__ */ new Set();
  for (const member of decl.members) {
    if (ts15.isGetAccessorDeclaration(member) && member.name) {
      const propName = resolveClassMemberName(ctx, member.name);
      if (!propName) continue;
      const getterName = `${className}_get_${propName}`;
      if (compiledAccessors.has(getterName)) continue;
      compiledAccessors.add(getterName);
      const getterLocalIdx = funcByName.get(getterName);
      if (getterLocalIdx === void 0) continue;
      const func = ctx.mod.functions[getterLocalIdx];
      const sig = ctx.checker.getSignatureFromDeclaration(member);
      const retType = sig ? ctx.checker.getReturnTypeOfSignature(sig) : void 0;
      const params = [{ name: "this", type: { kind: "ref", typeIdx: structTypeIdx } }];
      const fctx = {
        name: getterName,
        params,
        locals: [],
        localMap: /* @__PURE__ */ new Map(),
        returnType: retType && !isVoidType(retType) ? resolveWasmType(ctx, retType) : null,
        body: [],
        blockDepth: 0,
        breakStack: [],
        continueStack: [],
        labelMap: /* @__PURE__ */ new Map(),
        savedBodies: [],
      };
      {
        const resolvedParams = params.map((p) => p.type);
        const resolvedResults = fctx.returnType ? [fctx.returnType] : [];
        const updatedTypeIdx = addFuncType(ctx, resolvedParams, resolvedResults, `${getterName}_type`);
        if (updatedTypeIdx !== func.typeIdx) {
          func.typeIdx = updatedTypeIdx;
        }
      }
      for (let i = 0; i < params.length; i++) {
        fctx.localMap.set(params[i].name, i);
      }
      ctx.currentFunc = fctx;
      if (member.body) {
        for (const stmt of member.body.statements) {
          compileStatement(ctx, fctx, stmt);
        }
      }
      if (fctx.returnType) {
        const lastInstr = fctx.body[fctx.body.length - 1];
        if (!lastInstr || lastInstr.op !== "return") {
          if (fctx.returnType.kind === "f64") {
            fctx.body.push({ op: "f64.const", value: 0 });
          } else if (fctx.returnType.kind === "i32") {
            fctx.body.push({ op: "i32.const", value: 0 });
          } else if (fctx.returnType.kind === "externref") {
            fctx.body.push({ op: "ref.null.extern" });
          } else if (fctx.returnType.kind === "ref" || fctx.returnType.kind === "ref_null") {
            fctx.body.push({
              op: "ref.null",
              typeIdx: fctx.returnType.typeIdx,
            });
          }
        }
      }
      cacheStringLiterals(ctx, fctx);
      func.locals = fctx.locals;
      func.body = fctx.body;
      ctx.currentFunc = null;
    }
    if (ts15.isSetAccessorDeclaration(member) && member.name) {
      const propName = resolveClassMemberName(ctx, member.name);
      if (!propName) continue;
      const setterName = `${className}_set_${propName}`;
      if (compiledAccessors.has(setterName)) continue;
      compiledAccessors.add(setterName);
      const setterLocalIdx = funcByName.get(setterName);
      if (setterLocalIdx === void 0) continue;
      const func = ctx.mod.functions[setterLocalIdx];
      const params = [{ name: "this", type: { kind: "ref", typeIdx: structTypeIdx } }];
      for (let pi = 0; pi < member.parameters.length; pi++) {
        const param = member.parameters[pi];
        const paramName = ts15.isIdentifier(param.name) ? param.name.text : `__param${pi}`;
        const paramType = ctx.checker.getTypeAtLocation(param);
        let wasmType = resolveWasmType(ctx, paramType);
        if ((param.initializer || param.questionToken) && wasmType.kind === "ref") {
          wasmType = { kind: "ref_null", typeIdx: wasmType.typeIdx };
        }
        params.push({ name: paramName, type: wasmType });
      }
      const fctx = {
        name: setterName,
        params,
        locals: [],
        localMap: /* @__PURE__ */ new Map(),
        returnType: null,
        // setters always return void
        body: [],
        blockDepth: 0,
        breakStack: [],
        continueStack: [],
        labelMap: /* @__PURE__ */ new Map(),
        savedBodies: [],
      };
      {
        const resolvedParams = params.map((p) => p.type);
        const resolvedResults = [];
        const updatedTypeIdx = addFuncType(ctx, resolvedParams, resolvedResults, `${setterName}_type`);
        if (updatedTypeIdx !== func.typeIdx) {
          func.typeIdx = updatedTypeIdx;
        }
      }
      for (let i = 0; i < params.length; i++) {
        fctx.localMap.set(params[i].name, i);
      }
      ctx.currentFunc = fctx;
      for (let pi = 0; pi < member.parameters.length; pi++) {
        const param = member.parameters[pi];
        if (!param.initializer) continue;
        const paramLocalIdx = pi + 1;
        const paramType = params[paramLocalIdx].type;
        const savedBody = pushBody(fctx);
        const getSetDfltType = compileExpression2(ctx, fctx, param.initializer, paramType);
        if (getSetDfltType && !valTypesMatch(getSetDfltType, paramType)) {
          coerceType3(ctx, fctx, getSetDfltType, paramType);
        }
        fctx.body.push({ op: "local.set", index: paramLocalIdx });
        const thenInstrs = fctx.body;
        popBody(fctx, savedBody);
        if (paramType.kind === "externref") {
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
        } else if (paramType.kind === "ref_null" || paramType.kind === "ref") {
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
        } else if (paramType.kind === "i32") {
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "i32.eqz" });
          fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
        } else if (paramType.kind === "f64") {
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "local.get", index: paramLocalIdx });
          fctx.body.push({ op: "f64.ne" });
          fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs });
        }
      }
      if (member.body) {
        for (const stmt of member.body.statements) {
          compileStatement(ctx, fctx, stmt);
        }
      }
      cacheStringLiterals(ctx, fctx);
      func.locals = fctx.locals;
      func.body = fctx.body;
      ctx.currentFunc = null;
    }
  }
}
function compileSuperCall(ctx, fctx, childClassName, selfLocal, callExpr, _allFields) {
  const parentClassName = ctx.classParentMap.get(childClassName);
  if (!parentClassName) return;
  const parentFields = ctx.structFields.get(parentClassName) ?? [];
  const structTypeIdx = ctx.structMap.get(childClassName);
  const assignableParentFields = parentFields
    .map((f, idx) => ({ field: f, fieldIdx: idx }))
    .filter((e) => e.field.name !== "__tag");
  const hasSuperSpread = callExpr.arguments.some((a) => ts15.isSpreadElement(a));
  if (hasSuperSpread) {
    let fieldIdx2 = 0;
    for (const arg of callExpr.arguments) {
      if (ts15.isSpreadElement(arg)) {
        const vecType = compileExpression2(ctx, fctx, arg.expression);
        if (!vecType || (vecType.kind !== "ref" && vecType.kind !== "ref_null")) continue;
        const vecLocal = allocLocal(fctx, `__super_spread_vec_${fctx.locals.length}`, vecType);
        fctx.body.push({ op: "local.set", index: vecLocal });
        const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecType.typeIdx);
        if (arrTypeIdx < 0) continue;
        const dataLocal = allocLocal(fctx, `__super_spread_data_${fctx.locals.length}`, {
          kind: "ref_null",
          typeIdx: arrTypeIdx,
        });
        fctx.body.push({ op: "local.get", index: vecLocal });
        fctx.body.push({ op: "struct.get", typeIdx: vecType.typeIdx, fieldIdx: 1 });
        fctx.body.push({ op: "local.set", index: dataLocal });
        const arrDefSpread = ctx.mod.types[arrTypeIdx];
        const spreadElemType = arrDefSpread && arrDefSpread.kind === "array" ? arrDefSpread.element : { kind: "f64" };
        const remaining = assignableParentFields.length - fieldIdx2;
        for (let i = 0; i < remaining; i++) {
          const { fieldIdx } = assignableParentFields[fieldIdx2];
          fctx.body.push({ op: "local.get", index: selfLocal });
          fctx.body.push({ op: "local.get", index: dataLocal });
          fctx.body.push({ op: "i32.const", value: i });
          emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, spreadElemType);
          fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
          fieldIdx2++;
        }
      } else {
        if (fieldIdx2 < assignableParentFields.length) {
          const { field, fieldIdx } = assignableParentFields[fieldIdx2];
          fctx.body.push({ op: "local.get", index: selfLocal });
          compileExpression2(ctx, fctx, arg, field.type);
          fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
          fieldIdx2++;
        }
      }
    }
  } else {
    for (let i = 0; i < callExpr.arguments.length && i < assignableParentFields.length; i++) {
      const { field, fieldIdx } = assignableParentFields[i];
      fctx.body.push({ op: "local.get", index: selfLocal });
      compileExpression2(ctx, fctx, callExpr.arguments[i], field.type);
      fctx.body.push({ op: "struct.set", typeIdx: structTypeIdx, fieldIdx });
    }
  }
}
function hoistVarDeclarations(ctx, fctx, stmts) {
  for (const stmt of stmts) {
    walkStmtForVars(ctx, fctx, stmt);
  }
}
function hoistBindingPattern(ctx, fctx, pattern) {
  for (const element of pattern.elements) {
    if (ts15.isOmittedExpression(element)) continue;
    if (ts15.isIdentifier(element.name)) {
      const name = element.name.text;
      if (fctx.localMap.has(name)) continue;
      if (ctx.moduleGlobals.has(name)) continue;
      const elemType = ctx.checker.getTypeAtLocation(element);
      const wasmType = resolveWasmType(ctx, elemType);
      const localIdx = allocLocal(fctx, name, wasmType);
      if (wasmType.kind === "externref") {
        emitUndefined(ctx, fctx);
        fctx.body.push({ op: "local.set", index: localIdx });
      }
    } else if (ts15.isObjectBindingPattern(element.name) || ts15.isArrayBindingPattern(element.name)) {
      hoistBindingPattern(ctx, fctx, element.name);
    }
  }
}
function hoistVarDecl(ctx, fctx, decl) {
  if (ts15.isIdentifier(decl.name)) {
    const name = decl.name.text;
    if (fctx.localMap.has(name)) return;
    if (ctx.moduleGlobals.has(name)) return;
    const varType = ctx.checker.getTypeAtLocation(decl);
    const wasmType =
      decl.initializer && isPromiseHostCallExpr(decl.initializer, ctx)
        ? { kind: "externref" }
        : resolveWasmType(ctx, varType);
    const localIdx = allocLocal(fctx, name, wasmType);
    if (wasmType.kind === "externref") {
      emitUndefined(ctx, fctx);
      fctx.body.push({ op: "local.set", index: localIdx });
    }
    return;
  }
  if (ts15.isObjectBindingPattern(decl.name) || ts15.isArrayBindingPattern(decl.name)) {
    hoistBindingPattern(ctx, fctx, decl.name);
  }
}
function walkStmtForVars(ctx, fctx, stmt) {
  if (ts15.isVariableStatement(stmt)) {
    const list = stmt.declarationList;
    if (list.flags & (ts15.NodeFlags.Let | ts15.NodeFlags.Const)) return;
    for (const decl of list.declarations) {
      hoistVarDecl(ctx, fctx, decl);
    }
    return;
  }
  if (ts15.isBlock(stmt)) {
    for (const s of stmt.statements) walkStmtForVars(ctx, fctx, s);
    return;
  }
  if (ts15.isIfStatement(stmt)) {
    walkStmtForVars(ctx, fctx, stmt.thenStatement);
    if (stmt.elseStatement) walkStmtForVars(ctx, fctx, stmt.elseStatement);
    return;
  }
  if (ts15.isWhileStatement(stmt) || ts15.isDoStatement(stmt)) {
    walkStmtForVars(ctx, fctx, stmt.statement);
    return;
  }
  if (ts15.isForStatement(stmt)) {
    if (stmt.initializer && ts15.isVariableDeclarationList(stmt.initializer)) {
      const list = stmt.initializer;
      if (!(list.flags & (ts15.NodeFlags.Let | ts15.NodeFlags.Const))) {
        for (const decl of list.declarations) {
          hoistVarDecl(ctx, fctx, decl);
        }
      }
    }
    walkStmtForVars(ctx, fctx, stmt.statement);
    return;
  }
  if (ts15.isForInStatement(stmt) || ts15.isForOfStatement(stmt)) {
    if (stmt.initializer && ts15.isVariableDeclarationList(stmt.initializer)) {
      const list = stmt.initializer;
      if (!(list.flags & (ts15.NodeFlags.Let | ts15.NodeFlags.Const))) {
        for (const decl of list.declarations) {
          hoistVarDecl(ctx, fctx, decl);
        }
      }
    }
    walkStmtForVars(ctx, fctx, stmt.statement);
    return;
  }
  if (ts15.isLabeledStatement(stmt)) {
    walkStmtForVars(ctx, fctx, stmt.statement);
    return;
  }
  if (ts15.isTryStatement(stmt)) {
    for (const s of stmt.tryBlock.statements) walkStmtForVars(ctx, fctx, s);
    if (stmt.catchClause) {
      for (const s of stmt.catchClause.block.statements) walkStmtForVars(ctx, fctx, s);
    }
    if (stmt.finallyBlock) {
      for (const s of stmt.finallyBlock.statements) walkStmtForVars(ctx, fctx, s);
    }
    return;
  }
  if (ts15.isSwitchStatement(stmt)) {
    for (const clause of stmt.caseBlock.clauses) {
      for (const s of clause.statements) walkStmtForVars(ctx, fctx, s);
    }
  }
}
function hoistLetConstWithTdz(ctx, fctx, stmts) {
  for (const stmt of stmts) {
    walkStmtForLetConst(ctx, fctx, stmt);
  }
}
function needsTdzFlag(ctx, decl) {
  const symbol = ctx.checker.getSymbolAtLocation(decl.name);
  if (!symbol) return true;
  const declEnd = decl.getEnd();
  const declFunc = getContainingFunctionForTdz(decl);
  const funcBody = declFunc && "body" in declFunc ? declFunc.body : void 0;
  const scope = funcBody || decl.getSourceFile();
  let needsFlag = false;
  function visit(node) {
    if (needsFlag) return;
    if (ts15.isIdentifier(node) && node !== decl.name) {
      const sym = ctx.checker.getSymbolAtLocation(node);
      if (sym === symbol) {
        const accessPos = node.getStart();
        const accessFunc = getContainingFunctionForTdz(node);
        if (accessFunc !== declFunc) {
          needsFlag = true;
          return;
        }
        if (accessPos < declEnd) {
          needsFlag = true;
          return;
        }
        if (isInsideLoopContainingForTdz(node, decl)) {
          needsFlag = true;
          return;
        }
      }
    }
    if (
      node !== scope &&
      (ts15.isFunctionDeclaration(node) ||
        ts15.isFunctionExpression(node) ||
        ts15.isArrowFunction(node) ||
        ts15.isMethodDeclaration(node))
    ) {
      ts15.forEachChild(node, visit);
      return;
    }
    ts15.forEachChild(node, visit);
  }
  ts15.forEachChild(scope, visit);
  return needsFlag;
}
function getContainingFunctionForTdz(node) {
  let current = node.parent;
  while (current) {
    if (
      ts15.isFunctionDeclaration(current) ||
      ts15.isFunctionExpression(current) ||
      ts15.isArrowFunction(current) ||
      ts15.isMethodDeclaration(current) ||
      ts15.isConstructorDeclaration(current) ||
      ts15.isGetAccessorDeclaration(current) ||
      ts15.isSetAccessorDeclaration(current) ||
      ts15.isSourceFile(current)
    ) {
      return current;
    }
    current = current.parent;
  }
  return void 0;
}
function isInsideLoopContainingForTdz(access, decl) {
  let current = access.parent;
  while (current) {
    if (
      ts15.isFunctionDeclaration(current) ||
      ts15.isFunctionExpression(current) ||
      ts15.isArrowFunction(current) ||
      ts15.isMethodDeclaration(current) ||
      ts15.isSourceFile(current)
    ) {
      return false;
    }
    if (
      ts15.isForStatement(current) ||
      ts15.isForInStatement(current) ||
      ts15.isForOfStatement(current) ||
      ts15.isWhileStatement(current) ||
      ts15.isDoStatement(current)
    ) {
      if (isDescendantOfNode(decl, current)) {
        if (ts15.isForStatement(current) && current.initializer && isDescendantOfNode(decl, current.initializer)) {
          return false;
        }
        if (
          (ts15.isForInStatement(current) || ts15.isForOfStatement(current)) &&
          isDescendantOfNode(decl, current.initializer)
        ) {
          return false;
        }
        const body = getLoopBodyNode(current);
        if (body && isDescendantOfNode(decl, body) && access.getStart() >= decl.getEnd()) {
          return false;
        }
        return true;
      }
    }
    current = current.parent;
  }
  return false;
}
function isDescendantOfNode(node, ancestor) {
  let current = node;
  while (current) {
    if (current === ancestor) return true;
    current = current.parent;
  }
  return false;
}
function getLoopBodyNode(loop) {
  if (ts15.isForStatement(loop)) return loop.statement;
  if (ts15.isForInStatement(loop)) return loop.statement;
  if (ts15.isForOfStatement(loop)) return loop.statement;
  if (ts15.isWhileStatement(loop)) return loop.statement;
  if (ts15.isDoStatement(loop)) return loop.statement;
  return void 0;
}
function walkStmtForLetConst(ctx, fctx, stmt) {
  if (ts15.isVariableStatement(stmt)) {
    const list = stmt.declarationList;
    if (!(list.flags & (ts15.NodeFlags.Let | ts15.NodeFlags.Const))) return;
    for (const decl of list.declarations) {
      if (ts15.isIdentifier(decl.name)) {
        const name = decl.name.text;
        if (fctx.localMap.has(name)) continue;
        if (ctx.moduleGlobals.has(name)) continue;
        const varType = ctx.checker.getTypeAtLocation(decl);
        const wasmType =
          decl.initializer && isPromiseHostCallExpr(decl.initializer, ctx)
            ? { kind: "externref" }
            : resolveWasmType(ctx, varType);
        allocLocal(fctx, name, wasmType);
        if (needsTdzFlag(ctx, decl)) {
          if (!fctx.tdzFlagLocals) fctx.tdzFlagLocals = /* @__PURE__ */ new Map();
          const flagIdx = allocLocal(fctx, `__tdz_${name}`, { kind: "i32" });
          fctx.tdzFlagLocals.set(name, flagIdx);
        }
      }
    }
    return;
  }
  if (ts15.isBlock(stmt)) {
    for (const s of stmt.statements) walkStmtForLetConst(ctx, fctx, s);
    return;
  }
  if (ts15.isIfStatement(stmt)) {
    walkStmtForLetConst(ctx, fctx, stmt.thenStatement);
    if (stmt.elseStatement) walkStmtForLetConst(ctx, fctx, stmt.elseStatement);
    return;
  }
  if (ts15.isForStatement(stmt)) {
    if (stmt.initializer && ts15.isVariableDeclarationList(stmt.initializer)) {
      const list = stmt.initializer;
      if (list.flags & (ts15.NodeFlags.Let | ts15.NodeFlags.Const)) {
        for (const decl of list.declarations) {
          if (ts15.isIdentifier(decl.name)) {
            const name = decl.name.text;
            if (fctx.localMap.has(name) || ctx.moduleGlobals.has(name)) continue;
            const varType = ctx.checker.getTypeAtLocation(decl);
            const wasmType = resolveWasmType(ctx, varType);
            allocLocal(fctx, name, wasmType);
            if (needsTdzFlag(ctx, decl)) {
              if (!fctx.tdzFlagLocals) fctx.tdzFlagLocals = /* @__PURE__ */ new Map();
              const flagIdx = allocLocal(fctx, `__tdz_${name}`, { kind: "i32" });
              fctx.tdzFlagLocals.set(name, flagIdx);
            }
          }
        }
      }
    }
    walkStmtForLetConst(ctx, fctx, stmt.statement);
    return;
  }
  if (ts15.isTryStatement(stmt)) {
    for (const s of stmt.tryBlock.statements) walkStmtForLetConst(ctx, fctx, s);
    if (stmt.catchClause) {
      for (const s of stmt.catchClause.block.statements) walkStmtForLetConst(ctx, fctx, s);
    }
    if (stmt.finallyBlock) {
      for (const s of stmt.finallyBlock.statements) walkStmtForLetConst(ctx, fctx, s);
    }
    return;
  }
  if (ts15.isSwitchStatement(stmt)) {
    for (const clause of stmt.caseBlock.clauses) {
      for (const s of clause.statements) walkStmtForLetConst(ctx, fctx, s);
    }
  }
}
function bodyUsesArguments2(node) {
  if (ts15.isIdentifier(node) && node.text === "arguments") return true;
  if (ts15.isFunctionDeclaration(node) || ts15.isFunctionExpression(node)) {
    return false;
  }
  return ts15.forEachChild(node, bodyUsesArguments2) ?? false;
}
var INLINE_MAX_INSTRS = 10;
var INLINE_DISALLOWED_OPS = /* @__PURE__ */ new Set([
  "block",
  "loop",
  "if",
  "br",
  "br_if",
  "try",
  "throw",
  "rethrow",
  "unreachable",
  "call",
  "call_ref",
  "call_indirect",
  "return_call",
  "return_call_ref",
  "local.set",
  "local.tee",
]);
function registerInlinableFunction(ctx, funcName, func) {
  if (ctx.funcRestParams.has(funcName)) return;
  if (ctx.nestedFuncCaptures.has(funcName)) return;
  const body = func.body;
  if (body.length === 0 || body.length > INLINE_MAX_INSTRS) return;
  const realBody = body.filter((instr) => instr.op !== "nop");
  if (realBody.length === 0 || realBody.length > INLINE_MAX_INSTRS) return;
  const normalizedBody =
    realBody.length > 0 && realBody[realBody.length - 1]?.op === "return" ? realBody.slice(0, -1) : realBody;
  if (normalizedBody.length === 0 || normalizedBody.length > INLINE_MAX_INSTRS) return;
  const funcType = ctx.mod.types[func.typeIdx];
  if (!funcType || funcType.kind !== "func") return;
  const paramCount = funcType.params.length;
  if (func.locals.length > 0) return;
  for (const instr of normalizedBody) {
    if (INLINE_DISALLOWED_OPS.has(instr.op)) return;
    if (instr.op === "local.get") {
      if (instr.index >= paramCount) return;
    }
  }
  const returnType = funcType.results.length > 0 ? funcType.results[0] : null;
  ctx.inlinableFunctions.set(funcName, {
    body: normalizedBody,
    paramCount,
    paramTypes: funcType.params.slice(),
    returnType,
  });
}
function compileFunctionBody(ctx, decl, func) {
  const sig = ctx.checker.getSignatureFromDeclaration(decl);
  if (!sig) {
    reportError(ctx, decl, `Cannot resolve signature for function '${func.name}'`);
    return;
  }
  const retType = ctx.checker.getReturnTypeOfSignature(sig);
  const isAsync = ctx.asyncFunctions.has(func.name);
  const isGenerator = ctx.generatorFunctions.has(func.name);
  const effectiveRetType = isAsync ? unwrapPromiseType(retType, ctx.checker) : retType;
  const resolved = ctx.genericResolved.get(func.name);
  const restInfo = ctx.funcRestParams.get(func.name);
  const params = [];
  for (let i = 0; i < decl.parameters.length; i++) {
    const param = decl.parameters[i];
    const paramName = ts15.isIdentifier(param.name) ? param.name.text : `__param${i}`;
    if (restInfo && i === restInfo.restIndex) {
      params.push({
        name: paramName,
        type: { kind: "ref_null", typeIdx: restInfo.vecTypeIdx },
      });
    } else {
      const funcType = ctx.mod.types[func.typeIdx];
      const sigParamType = funcType?.kind === "func" ? funcType.params[i] : void 0;
      const paramType =
        resolved?.params[i] ?? sigParamType ?? resolveWasmType(ctx, ctx.checker.getTypeAtLocation(param));
      params.push({ name: paramName, type: paramType });
    }
  }
  let returnType;
  if (isGenerator) {
    returnType = { kind: "externref" };
  } else if (resolved) {
    returnType = resolved.results.length > 0 ? (resolved.results[0] ?? null) : null;
  } else {
    returnType = isVoidType(effectiveRetType) ? null : resolveWasmType(ctx, effectiveRetType);
  }
  const fctx = {
    name: func.name,
    params,
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    labelMap: /* @__PURE__ */ new Map(),
    savedBodies: [],
    isGenerator,
  };
  for (let i = 0; i < params.length; i++) {
    fctx.localMap.set(params[i].name, i);
  }
  ctx.currentFunc = fctx;
  const funcPos = getSourcePos(ctx, decl);
  if (funcPos) {
    const nop = { op: "nop" };
    attachSourcePos(nop, funcPos);
    fctx.body.push(nop);
  }
  const funcOptInfo = ctx.funcOptionalParams.get(func.name);
  for (let i = 0; i < decl.parameters.length; i++) {
    const param = decl.parameters[i];
    if (!param.initializer) continue;
    const optEntry = funcOptInfo?.find((o) => o.index === i);
    if (optEntry?.constantDefault) continue;
    const paramIdx = i;
    const paramType = params[i].type;
    const savedBody = pushBody(fctx);
    const defaultResultType = compileExpression2(ctx, fctx, param.initializer, paramType);
    if (defaultResultType && !valTypesMatch(defaultResultType, paramType)) {
      coerceType3(ctx, fctx, defaultResultType, paramType);
    }
    fctx.body.push({ op: "local.set", index: paramIdx });
    const thenInstrs = fctx.body;
    popBody(fctx, savedBody);
    if (paramType.kind === "externref") {
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: thenInstrs,
      });
    } else if (paramType.kind === "ref_null" || paramType.kind === "ref") {
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: thenInstrs,
      });
    } else if (paramType.kind === "i32") {
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "i32.eqz" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: thenInstrs,
      });
    } else if (paramType.kind === "f64") {
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "i64.reinterpret_f64" });
      fctx.body.push({ op: "i64.const", value: 0x7ff00000deadc0den });
      fctx.body.push({ op: "i64.eq" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: thenInstrs,
      });
    }
  }
  for (let i = 0; i < decl.parameters.length; i++) {
    const param = decl.parameters[i];
    if (ts15.isObjectBindingPattern(param.name)) {
      destructureParamObject2(ctx, fctx, i, param.name, params[i].type);
    } else if (ts15.isArrayBindingPattern(param.name)) {
      destructureParamArray(ctx, fctx, i, param.name, params[i].type);
    }
  }
  if (decl.body && bodyUsesArguments2(decl.body)) {
    const hasNumericParam = params.some((p) => p.type.kind === "f64" || p.type.kind === "i32");
    if (hasNumericParam) {
      ensureLateImport2(ctx, "__box_number", [{ kind: "f64" }], [{ kind: "externref" }]);
      ensureLateImport2(ctx, "__unbox_number", [{ kind: "externref" }], [{ kind: "f64" }]);
      flushLateImportShifts2(ctx, fctx);
    }
    const elemType = { kind: "externref" };
    const vecTypeIdx = getOrRegisterVecType(ctx, "externref", elemType);
    const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
    const vecRef = { kind: "ref", typeIdx: vecTypeIdx };
    const argsLocal = allocLocal(fctx, "arguments", vecRef);
    const arrTmp = allocLocal(fctx, "__args_arr_tmp", { kind: "ref", typeIdx: arrTypeIdx });
    const allSimpleParams = decl.parameters.every((p) => ts15.isIdentifier(p.name) && !p.dotDotDotToken);
    if (allSimpleParams && params.length > 0) {
      fctx.mappedArgsInfo = {
        argsLocalIdx: argsLocal,
        arrTypeIdx,
        vecTypeIdx,
        paramCount: params.length,
        paramOffset: 0,
        paramTypes: params.map((p) => p.type),
      };
    }
    for (let i = 0; i < params.length; i++) {
      const paramType = params[i].type;
      fctx.body.push({ op: "local.get", index: i });
      if (paramType.kind === "f64") {
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: boxIdx });
        } else {
          fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "ref.null.extern" });
        }
      } else if (paramType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
        const boxIdx = ctx.funcMap.get("__box_number");
        if (boxIdx !== void 0) {
          fctx.body.push({ op: "call", funcIdx: boxIdx });
        } else {
          fctx.body.push({ op: "drop" });
          fctx.body.push({ op: "ref.null.extern" });
        }
      } else if (paramType.kind === "ref" || paramType.kind === "ref_null") {
        fctx.body.push({ op: "extern.convert_any" });
      }
    }
    fctx.body.push({ op: "array.new_fixed", typeIdx: arrTypeIdx, length: params.length });
    fctx.body.push({ op: "local.set", index: arrTmp });
    fctx.body.push({ op: "i32.const", value: params.length });
    fctx.body.push({ op: "local.get", index: arrTmp });
    fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
    fctx.body.push({ op: "local.set", index: argsLocal });
  }
  if (isGenerator) {
    const bufferLocal = allocLocal(fctx, "__gen_buffer", { kind: "externref" });
    const createBufIdx = ctx.funcMap.get("__gen_create_buffer");
    fctx.body.push({ op: "call", funcIdx: createBufIdx });
    fctx.body.push({ op: "local.set", index: bufferLocal });
    const bodyInstrs = [];
    const outerBody = fctx.body;
    fctx.body = bodyInstrs;
    fctx.generatorReturnDepth = 0;
    fctx.blockDepth++;
    for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]++;
    for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]++;
    if (decl.body) {
      hoistVarDeclarations(ctx, fctx, decl.body.statements);
      hoistLetConstWithTdz(ctx, fctx, decl.body.statements);
      hoistFunctionDeclarations(ctx, fctx, decl.body.statements);
      for (const stmt of decl.body.statements) {
        compileStatement(ctx, fctx, stmt);
      }
    }
    fctx.blockDepth--;
    for (let i = 0; i < fctx.breakStack.length; i++) fctx.breakStack[i]--;
    for (let i = 0; i < fctx.continueStack.length; i++) fctx.continueStack[i]--;
    fctx.generatorReturnDepth = void 0;
    fctx.body = outerBody;
    fctx.body.push({
      op: "block",
      blockType: { kind: "empty" },
      body: bodyInstrs,
    });
    const createGenIdx = ctx.funcMap.get(isAsync ? "__create_async_generator" : "__create_generator");
    fctx.body.push({ op: "local.get", index: bufferLocal });
    fctx.body.push({ op: "call", funcIdx: createGenIdx });
  } else {
    if (decl.body) {
      hoistVarDeclarations(ctx, fctx, decl.body.statements);
      hoistLetConstWithTdz(ctx, fctx, decl.body.statements);
      hoistFunctionDeclarations(ctx, fctx, decl.body.statements);
      for (const stmt of decl.body.statements) {
        compileStatement(ctx, fctx, stmt);
      }
    }
    if (fctx.returnType) {
      const lastInstr = fctx.body[fctx.body.length - 1];
      if (!lastInstr || lastInstr.op !== "return") {
        if (fctx.returnType.kind === "f64") {
          fctx.body.push({ op: "f64.const", value: 0 });
        } else if (fctx.returnType.kind === "i32") {
          fctx.body.push({ op: "i32.const", value: 0 });
        } else if (fctx.returnType.kind === "externref") {
          fctx.body.push({ op: "ref.null.extern" });
        } else if (fctx.returnType.kind === "ref" || fctx.returnType.kind === "ref_null") {
          fctx.body.push({ op: "ref.null", typeIdx: fctx.returnType.typeIdx });
        }
      }
    }
  }
  cacheStringLiterals(ctx, fctx);
  func.locals = fctx.locals;
  func.body = fctx.body;
  ctx.currentFunc = null;
}
function buildDestructureNullThrow(ctx) {
  const msg = "TypeError: Cannot destructure 'null' or 'undefined'";
  addStringConstantGlobal(ctx, msg);
  const strIdx = ctx.stringGlobalMap.get(msg);
  const tagIdx = ensureExnTag(ctx);
  return [
    { op: "global.get", index: strIdx },
    { op: "throw", tagIdx },
  ];
}
function destructureParamObjectExternref(ctx, fctx, paramIdx, pattern) {
  let getIdx = ctx.funcMap.get("__extern_get");
  if (getIdx === void 0) {
    const importsBefore = ctx.numImportFuncs;
    const getType = addFuncType(ctx, [{ kind: "externref" }, { kind: "externref" }], [{ kind: "externref" }]);
    addImport(ctx, "env", "__extern_get", { kind: "func", typeIdx: getType });
    shiftLateImportIndices(ctx, fctx, importsBefore, ctx.numImportFuncs - importsBefore);
    getIdx = ctx.funcMap.get("__extern_get");
  }
  if (getIdx === void 0) return;
  for (const element of pattern.elements) {
    if (!ts15.isBindingElement(element)) continue;
    const propNameNode = element.propertyName ?? element.name;
    let propNameText;
    if (ts15.isIdentifier(propNameNode)) {
      propNameText = propNameNode.text;
    } else if (ts15.isStringLiteral(propNameNode)) {
      propNameText = propNameNode.text;
    } else if (ts15.isNumericLiteral(propNameNode)) {
      propNameText = propNameNode.text;
    }
    if (!propNameText) continue;
    addStringConstantGlobal(ctx, propNameText);
    const strGlobalIdx = ctx.stringGlobalMap.get(propNameText);
    if (strGlobalIdx === void 0) continue;
    getIdx = ctx.funcMap.get("__extern_get");
    if (getIdx === void 0) continue;
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "global.get", index: strGlobalIdx });
    fctx.body.push({ op: "call", funcIdx: getIdx });
    const elemType = { kind: "externref" };
    if (ts15.isIdentifier(element.name)) {
      const localName = element.name.text;
      let localIdx = fctx.localMap.get(localName);
      if (localIdx === void 0) {
        localIdx = allocLocal(fctx, localName, elemType);
      }
      const localType = getLocalType(fctx, localIdx);
      if (element.initializer) {
        const tmpElem = allocLocal(fctx, `__ext_dparam_dflt_${fctx.locals.length}`, elemType);
        fctx.body.push({ op: "local.tee", index: tmpElem });
        const undefIdx = ensureLateImport2(ctx, "__extern_is_undefined", [{ kind: "externref" }], [{ kind: "i32" }]);
        if (undefIdx !== void 0) {
          flushLateImportShifts2(ctx, fctx);
          getIdx = ctx.funcMap.get("__extern_get");
        }
        fctx.body.push({ op: "ref.is_null" });
        if (undefIdx !== void 0) {
          fctx.body.push({ op: "local.get", index: tmpElem });
          fctx.body.push({ op: "call", funcIdx: undefIdx });
          fctx.body.push({ op: "i32.or" });
        }
        const savedBody = fctx.body;
        const thenInstrs = [];
        fctx.body = thenInstrs;
        compileExpression2(ctx, fctx, element.initializer, localType ?? elemType);
        fctx.body.push({ op: "local.set", index: localIdx });
        fctx.body = savedBody;
        const elseCoerce = [];
        if (localType && !valTypesMatch(elemType, localType)) {
          const savedBody2 = fctx.body;
          fctx.body = elseCoerce;
          coerceType3(ctx, fctx, elemType, localType);
          fctx.body = savedBody2;
        }
        fctx.body.push({
          op: "if",
          blockType: { kind: "empty" },
          then: thenInstrs,
          else: [{ op: "local.get", index: tmpElem }, ...elseCoerce, { op: "local.set", index: localIdx }],
        });
      } else {
        if (localType && !valTypesMatch(elemType, localType)) {
          coerceType3(ctx, fctx, elemType, localType);
        }
        fctx.body.push({ op: "local.set", index: localIdx });
      }
    } else if (ts15.isObjectBindingPattern(element.name) || ts15.isArrayBindingPattern(element.name)) {
      const nestedLocal = allocLocal(fctx, `__ext_dparam_nested_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.set", index: nestedLocal });
      ensureBindingLocals(ctx, fctx, element.name);
      if (ts15.isObjectBindingPattern(element.name)) {
        destructureParamObjectExternref(ctx, fctx, nestedLocal, element.name);
      } else {
        destructureParamArray(ctx, fctx, nestedLocal, element.name, elemType);
      }
    }
  }
}
function emitExternrefDestructureGuard(ctx, fctx, paramIdx) {
  fctx.body.push({ op: "local.get", index: paramIdx });
  fctx.body.push({ op: "ref.is_null" });
  fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: buildDestructureNullThrow(ctx), else: [] });
  const undefIdx = ensureLateImport2(ctx, "__extern_is_undefined", [{ kind: "externref" }], [{ kind: "i32" }]);
  if (undefIdx !== void 0) {
    flushLateImportShifts2(ctx, fctx);
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "call", funcIdx: undefIdx });
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: buildDestructureNullThrow(ctx), else: [] });
  }
}
function destructureParamObject2(ctx, fctx, paramIdx, pattern, paramType) {
  if (paramType.kind !== "ref" && paramType.kind !== "ref_null") {
    if (paramType.kind === "externref") {
      emitExternrefDestructureGuard(ctx, fctx, paramIdx);
      ensureBindingLocals(ctx, fctx, pattern);
      if (pattern.elements.length === 0) return;
      const tsType = ctx.checker.getTypeAtLocation(pattern);
      let structTypeIdx2;
      if (tsType) {
        ensureStructForType(ctx, tsType);
        const typeName = ctx.anonTypeMap.get(tsType) ?? tsType.getSymbol()?.name ?? tsType.aliasSymbol?.name;
        structTypeIdx2 = typeName ? ctx.structMap.get(typeName) : void 0;
      }
      if (structTypeIdx2 !== void 0) {
        const anyTmp = allocLocal(fctx, `__dparam_any_${fctx.locals.length}`, { kind: "anyref" });
        fctx.body.push({ op: "local.get", index: paramIdx });
        fctx.body.push({ op: "any.convert_extern" });
        fctx.body.push({ op: "local.set", index: anyTmp });
        fctx.body.push({ op: "local.get", index: anyTmp });
        fctx.body.push({ op: "ref.test", typeIdx: structTypeIdx2 });
        const convertedType = { kind: "ref_null", typeIdx: structTypeIdx2 };
        const tmpLocal = allocLocal(fctx, `__dparam_cvt_${fctx.locals.length}`, convertedType);
        const thenInstrs = [];
        const savedBody2 = fctx.body;
        fctx.body = thenInstrs;
        fctx.body.push({ op: "local.get", index: anyTmp });
        fctx.body.push({ op: "ref.cast", typeIdx: structTypeIdx2 });
        fctx.body.push({ op: "local.set", index: tmpLocal });
        destructureParamObject2(ctx, fctx, tmpLocal, pattern, convertedType);
        fctx.body = savedBody2;
        const elseInstrs = [];
        fctx.body = elseInstrs;
        destructureParamObjectExternref(ctx, fctx, paramIdx, pattern);
        fctx.body = savedBody2;
        fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs, else: elseInstrs });
      } else {
        destructureParamObjectExternref(ctx, fctx, paramIdx, pattern);
      }
      return;
    }
    for (const element of pattern.elements) {
      if (ts15.isOmittedExpression(element)) continue;
      if (ts15.isIdentifier(element.name)) {
        const name = element.name.text;
        if (!fctx.localMap.has(name)) {
          const elemType = ctx.checker.getTypeAtLocation(element);
          allocLocal(fctx, name, resolveWasmType(ctx, elemType));
        }
      }
    }
    return;
  }
  const structTypeIdx = paramType.typeIdx;
  const structName = ctx.typeIdxToStructName.get(structTypeIdx);
  const fields = structName ? ctx.structFields.get(structName) : void 0;
  if (!fields) {
    for (const element of pattern.elements) {
      if (ts15.isOmittedExpression(element)) continue;
      if (ts15.isIdentifier(element.name)) {
        const name = element.name.text;
        if (!fctx.localMap.has(name)) {
          const elemType = ctx.checker.getTypeAtLocation(element);
          allocLocal(fctx, name, resolveWasmType(ctx, elemType));
        }
      }
    }
    return;
  }
  ensureBindingLocals(ctx, fctx, pattern);
  const isNullable = paramType.kind === "ref_null" || paramType.kind === "ref";
  const savedBody = fctx.body;
  const destructInstrs = [];
  if (isNullable) {
    fctx.body = destructInstrs;
  }
  for (const element of pattern.elements) {
    if (!ts15.isBindingElement(element)) continue;
    const propName = element.propertyName ?? element.name;
    if (!ts15.isIdentifier(element.name)) {
      if (ts15.isObjectBindingPattern(element.name) || ts15.isArrayBindingPattern(element.name)) {
        const fieldIdx2 = fields.findIndex((f) => f.name === propName.text);
        if (fieldIdx2 === -1) continue;
        const fieldType2 = fields[fieldIdx2].type;
        const tmpLocal = allocLocal(fctx, `__dparam_${fctx.locals.length}`, fieldType2);
        fctx.body.push({ op: "local.get", index: paramIdx });
        fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx: fieldIdx2 });
        fctx.body.push({ op: "local.set", index: tmpLocal });
        if (element.initializer) {
          ctx._arrayLiteralForceVec = true;
          try {
            emitNestedBindingDefault(ctx, fctx, tmpLocal, fieldType2, element.initializer);
          } finally {
            ctx._arrayLiteralForceVec = false;
          }
        }
        if (ts15.isObjectBindingPattern(element.name)) {
          destructureParamObject2(ctx, fctx, tmpLocal, element.name, fieldType2);
        } else {
          destructureParamArray(ctx, fctx, tmpLocal, element.name, fieldType2);
        }
      }
      continue;
    }
    const localName = element.name.text;
    const fieldIdx = fields.findIndex((f) => f.name === propName.text);
    if (fieldIdx === -1) {
      continue;
    }
    const fieldType = fields[fieldIdx].type;
    if (!fctx.localMap.has(localName)) {
      allocLocal(fctx, localName, fieldType);
    }
    const localIdx = fctx.localMap.get(localName);
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "struct.get", typeIdx: structTypeIdx, fieldIdx });
    if (element.initializer) {
      emitDefaultValueCheck(ctx, fctx, fieldType, localIdx, element.initializer);
    } else {
      const objLocalType = getLocalType(fctx, localIdx);
      if (objLocalType && !valTypesMatch(fieldType, objLocalType)) {
        coerceType3(ctx, fctx, fieldType, objLocalType);
      }
      fctx.body.push({ op: "local.set", index: localIdx });
    }
  }
  if (isNullable) {
    fctx.body = savedBody;
    if (destructInstrs.length > 0) {
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: buildDestructureNullThrow(ctx),
        else: destructInstrs,
      });
    }
  }
}
function destructureParamArray(ctx, fctx, paramIdx, pattern, paramType) {
  if (paramType.kind !== "ref" && paramType.kind !== "ref_null") {
    if (paramType.kind === "externref") {
      emitExternrefDestructureGuard(ctx, fctx, paramIdx);
      const extVecIdx = getOrRegisterVecType(ctx, "externref", { kind: "externref" });
      const extArrTypeIdx = getArrTypeIdxFromVec(ctx, extVecIdx);
      const convertedType = { kind: "ref_null", typeIdx: extVecIdx };
      const resultLocal = allocLocal(fctx, `__dparam_cvt_${fctx.locals.length}`, convertedType);
      const anyTmp = allocLocal(fctx, `__dparam_any_${fctx.locals.length}`, { kind: "anyref" });
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "any.convert_extern" });
      fctx.body.push({ op: "local.set", index: anyTmp });
      fctx.body.push({ op: "local.get", index: anyTmp });
      fctx.body.push({ op: "ref.test", typeIdx: extVecIdx });
      const directCastInstrs = [
        { op: "local.get", index: anyTmp },
        { op: "ref.cast", typeIdx: extVecIdx },
        { op: "local.set", index: resultLocal },
      ];
      const convertInstrs = [];
      for (const [key, vecIdx] of ctx.vecTypeMap) {
        if (vecIdx === extVecIdx) continue;
        const vecDef = ctx.mod.types[vecIdx];
        if (!vecDef || vecDef.kind !== "struct") continue;
        const dataField = vecDef.fields[1];
        if (!dataField || dataField.name !== "data") continue;
        const srcArrTypeIdx = dataField.type.typeIdx;
        const cvtTmp = allocLocal(fctx, `__dparam_src_${key}_${fctx.locals.length}`, {
          kind: "ref_null",
          typeIdx: vecIdx,
        });
        const lenTmp = allocLocal(fctx, `__dparam_len_${key}_${fctx.locals.length}`, { kind: "i32" });
        const dstArrTmp = allocLocal(fctx, `__dparam_darr_${key}_${fctx.locals.length}`, {
          kind: "ref",
          typeIdx: extArrTypeIdx,
        });
        const idxTmp = allocLocal(fctx, `__dparam_idx_${key}_${fctx.locals.length}`, { kind: "i32" });
        const thenInstrs = [
          // Cast and get length
          { op: "local.get", index: anyTmp },
          { op: "ref.cast", typeIdx: vecIdx },
          { op: "local.set", index: cvtTmp },
          { op: "local.get", index: cvtTmp },
          { op: "struct.get", typeIdx: vecIdx, fieldIdx: 0 },
          // length
          { op: "local.set", index: lenTmp },
          // Create new externref array
          { op: "local.get", index: lenTmp },
          { op: "array.new_default", typeIdx: extArrTypeIdx },
          { op: "local.set", index: dstArrTmp },
          // Loop: copy elements with boxing
          { op: "i32.const", value: 0 },
          { op: "local.set", index: idxTmp },
          {
            op: "block",
            blockType: { kind: "empty" },
            body: [
              {
                op: "loop",
                blockType: { kind: "empty" },
                body: [
                  // if idx >= len, break
                  { op: "local.get", index: idxTmp },
                  { op: "local.get", index: lenTmp },
                  { op: "i32.ge_s" },
                  { op: "br_if", depth: 1 },
                  // dstArr[idx] = extern.convert_any(srcArr[idx])
                  { op: "local.get", index: dstArrTmp },
                  { op: "local.get", index: idxTmp },
                  { op: "local.get", index: cvtTmp },
                  { op: "struct.get", typeIdx: vecIdx, fieldIdx: 1 },
                  // src data
                  { op: "local.get", index: idxTmp },
                  { op: "array.get", typeIdx: srcArrTypeIdx },
                  // Box primitive types before storing as externref
                  ...boxToExternref(ctx, key),
                  { op: "array.set", typeIdx: extArrTypeIdx },
                  // idx++
                  { op: "local.get", index: idxTmp },
                  { op: "i32.const", value: 1 },
                  { op: "i32.add" },
                  { op: "local.set", index: idxTmp },
                  { op: "br", depth: 0 },
                ],
              },
            ],
          },
          // Create __vec_externref: struct.new(len, dstArr)
          { op: "local.get", index: lenTmp },
          { op: "local.get", index: dstArrTmp },
          { op: "struct.new", typeIdx: extVecIdx },
          { op: "local.set", index: resultLocal },
        ];
        convertInstrs.push({ op: "local.get", index: anyTmp });
        convertInstrs.push({ op: "ref.test", typeIdx: vecIdx });
        convertInstrs.push({ op: "if", blockType: { kind: "empty" }, then: thenInstrs, else: [] });
      }
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: directCastInstrs,
        else: convertInstrs,
      });
      destructureParamArray(ctx, fctx, resultLocal, pattern, convertedType);
      return;
    }
    for (const element of pattern.elements) {
      if (ts15.isOmittedExpression(element)) continue;
      if (ts15.isIdentifier(element.name)) {
        const name = element.name.text;
        if (!fctx.localMap.has(name)) {
          const elemType2 = ctx.checker.getTypeAtLocation(element);
          allocLocal(fctx, name, resolveWasmType(ctx, elemType2));
        }
      }
    }
    return;
  }
  const vecTypeIdx = paramType.typeIdx;
  const arrTypeIdx = getArrTypeIdxFromVec(ctx, vecTypeIdx);
  const arrDef = ctx.mod.types[arrTypeIdx];
  if (!arrDef || arrDef.kind !== "array") {
    const tupleDef = ctx.mod.types[vecTypeIdx];
    if (tupleDef && tupleDef.kind === "struct" && tupleDef.fields.length > 0 && tupleDef.fields[0].name === "_0") {
      const isNullable2 = paramType.kind === "ref_null" || paramType.kind === "ref";
      ensureBindingLocals(ctx, fctx, pattern);
      const savedBody2 = fctx.body;
      const destructInstrs2 = [];
      if (isNullable2) {
        fctx.body = destructInstrs2;
      }
      for (let i = 0; i < pattern.elements.length; i++) {
        const element = pattern.elements[i];
        if (ts15.isOmittedExpression(element)) continue;
        if (i >= tupleDef.fields.length) break;
        const fieldType = tupleDef.fields[i].type;
        if (
          ts15.isBindingElement(element) &&
          (ts15.isObjectBindingPattern(element.name) || ts15.isArrayBindingPattern(element.name))
        ) {
          const tmpLocal = allocLocal(fctx, `__dparam_${fctx.locals.length}`, fieldType);
          fctx.body.push({ op: "local.get", index: paramIdx });
          fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: i });
          fctx.body.push({ op: "local.set", index: tmpLocal });
          if (element.initializer) {
            ctx._arrayLiteralForceVec = true;
            try {
              emitNestedBindingDefault(ctx, fctx, tmpLocal, fieldType, element.initializer);
            } finally {
              ctx._arrayLiteralForceVec = false;
            }
          }
          if (ts15.isObjectBindingPattern(element.name)) {
            destructureParamObject2(ctx, fctx, tmpLocal, element.name, fieldType);
          } else {
            destructureParamArray(ctx, fctx, tmpLocal, element.name, fieldType);
          }
          continue;
        }
        if (!ts15.isIdentifier(element.name)) continue;
        const localName = element.name.text;
        if (!fctx.localMap.has(localName)) {
          allocLocal(fctx, localName, fieldType);
        }
        const localIdx = fctx.localMap.get(localName);
        fctx.body.push({ op: "local.get", index: paramIdx });
        fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: i });
        const localType = getLocalType(fctx, localIdx);
        if (localType && !valTypesMatch(fieldType, localType)) {
          coerceType3(ctx, fctx, fieldType, localType);
        }
        fctx.body.push({ op: "local.set", index: localIdx });
        if (ts15.isBindingElement(element) && element.initializer) {
          const effType = localType || fieldType;
          emitNestedBindingDefault(ctx, fctx, localIdx, effType, element.initializer);
        }
      }
      if (isNullable2) {
        fctx.body = savedBody2;
        if (destructInstrs2.length > 0) {
          const nullDefaultInstrs = [];
          for (const element of pattern.elements) {
            if (ts15.isOmittedExpression(element)) continue;
            if (!ts15.isBindingElement(element) || !element.initializer) continue;
            if (!ts15.isIdentifier(element.name)) continue;
            const localName = element.name.text;
            const localIdx = fctx.localMap.get(localName);
            if (localIdx === void 0) continue;
            const localType = getLocalType(fctx, localIdx);
            if (!localType) continue;
            const prevBody = fctx.body;
            fctx.body = nullDefaultInstrs;
            compileExpression2(ctx, fctx, element.initializer, localType);
            fctx.body.push({ op: "local.set", index: localIdx });
            fctx.body = prevBody;
          }
          fctx.body.push({ op: "local.get", index: paramIdx });
          fctx.body.push({ op: "ref.is_null" });
          fctx.body.push({
            op: "if",
            blockType: { kind: "empty" },
            then: nullDefaultInstrs.length > 0 ? nullDefaultInstrs : buildDestructureNullThrow(ctx),
            else: destructInstrs2,
          });
        }
      }
      return;
    }
    for (const element of pattern.elements) {
      if (ts15.isOmittedExpression(element)) continue;
      if (ts15.isIdentifier(element.name)) {
        const name = element.name.text;
        if (!fctx.localMap.has(name)) {
          const elemType2 = ctx.checker.getTypeAtLocation(element);
          allocLocal(fctx, name, resolveWasmType(ctx, elemType2));
        }
      }
    }
    return;
  }
  const elemType = arrDef.element;
  ensureBindingLocals(ctx, fctx, pattern);
  const isNullable = paramType.kind === "ref_null" || paramType.kind === "ref";
  const savedBody = fctx.body;
  const destructInstrs = [];
  if (isNullable) {
    fctx.body = destructInstrs;
  }
  for (let i = 0; i < pattern.elements.length; i++) {
    const element = pattern.elements[i];
    if (ts15.isOmittedExpression(element)) continue;
    if (
      ts15.isBindingElement(element) &&
      (ts15.isObjectBindingPattern(element.name) || ts15.isArrayBindingPattern(element.name))
    ) {
      const tmpLocal = allocLocal(fctx, `__dparam_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
      fctx.body.push({ op: "i32.const", value: i });
      emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elemType);
      fctx.body.push({ op: "local.set", index: tmpLocal });
      if (element.initializer) {
        ctx._arrayLiteralForceVec = true;
        try {
          emitNestedBindingDefault(ctx, fctx, tmpLocal, elemType, element.initializer);
        } finally {
          ctx._arrayLiteralForceVec = false;
        }
      }
      if (ts15.isObjectBindingPattern(element.name)) {
        destructureParamObject2(ctx, fctx, tmpLocal, element.name, elemType);
      } else {
        destructureParamArray(ctx, fctx, tmpLocal, element.name, elemType);
      }
      continue;
    }
    if (element.dotDotDotToken) {
      const restLenLocal = allocLocal(fctx, `__rest_len_${fctx.locals.length}`, { kind: "i32" });
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 0 });
      fctx.body.push({ op: "i32.const", value: i });
      fctx.body.push({ op: "i32.sub" });
      fctx.body.push({ op: "local.set", index: restLenLocal });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "local.get", index: restLenLocal });
      fctx.body.push({ op: "local.get", index: restLenLocal });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "i32.lt_s" });
      fctx.body.push({ op: "select" });
      fctx.body.push({ op: "local.set", index: restLenLocal });
      const restArrLocal = allocLocal(fctx, `__rest_arr_${fctx.locals.length}`, { kind: "ref", typeIdx: arrTypeIdx });
      fctx.body.push({ op: "local.get", index: restLenLocal });
      fctx.body.push({ op: "array.new_default", typeIdx: arrTypeIdx });
      fctx.body.push({ op: "local.set", index: restArrLocal });
      fctx.body.push({ op: "local.get", index: restArrLocal });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
      fctx.body.push({ op: "i32.const", value: i });
      fctx.body.push({ op: "local.get", index: restLenLocal });
      fctx.body.push({ op: "array.copy", dstTypeIdx: arrTypeIdx, srcTypeIdx: arrTypeIdx });
      fctx.body.push({ op: "local.get", index: restLenLocal });
      fctx.body.push({ op: "local.get", index: restArrLocal });
      fctx.body.push({ op: "struct.new", typeIdx: vecTypeIdx });
      if (ts15.isIdentifier(element.name)) {
        const restName = element.name.text;
        if (!fctx.localMap.has(restName)) {
          allocLocal(fctx, restName, paramType);
        }
        const restLocal = fctx.localMap.get(restName);
        fctx.body.push({ op: "local.set", index: restLocal });
      } else if (ts15.isArrayBindingPattern(element.name)) {
        const nestedTmpLocal = allocLocal(fctx, `__rest_nested_${fctx.locals.length}`, paramType);
        fctx.body.push({ op: "local.set", index: nestedTmpLocal });
        destructureParamArray(ctx, fctx, nestedTmpLocal, element.name, paramType);
      } else {
        fctx.body.push({ op: "drop" });
      }
      continue;
    }
    if (!ts15.isIdentifier(element.name)) continue;
    const localName = element.name.text;
    if (!fctx.localMap.has(localName)) {
      allocLocal(fctx, localName, elemType);
    }
    const localIdx = fctx.localMap.get(localName);
    fctx.body.push({ op: "local.get", index: paramIdx });
    fctx.body.push({ op: "struct.get", typeIdx: vecTypeIdx, fieldIdx: 1 });
    fctx.body.push({ op: "i32.const", value: i });
    emitBoundsCheckedArrayGet2(fctx, arrTypeIdx, elemType);
    if (element.initializer) {
      const dfltTmpLocal = allocLocal(fctx, `__dparam_dflt_${fctx.locals.length}`, elemType);
      fctx.body.push({ op: "local.set", index: dfltTmpLocal });
      emitNestedBindingDefault(ctx, fctx, dfltTmpLocal, elemType, element.initializer);
      fctx.body.push({ op: "local.get", index: dfltTmpLocal });
    }
    const vecLocalType = getLocalType(fctx, localIdx);
    if (vecLocalType && !valTypesMatch(elemType, vecLocalType)) {
      coerceType3(ctx, fctx, elemType, vecLocalType);
    }
    fctx.body.push({ op: "local.set", index: localIdx });
  }
  if (isNullable) {
    fctx.body = savedBody;
    if (destructInstrs.length > 0) {
      fctx.body.push({ op: "local.get", index: paramIdx });
      fctx.body.push({ op: "ref.is_null" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: buildDestructureNullThrow(ctx),
        else: destructInstrs,
      });
    }
  }
}
function cacheStringLiterals(ctx, fctx) {
  const strFuncIdxSet = /* @__PURE__ */ new Set();
  for (const [, importName] of ctx.stringLiteralMap) {
    const funcIdx = ctx.funcMap.get(importName);
    if (funcIdx !== void 0) strFuncIdxSet.add(funcIdx);
  }
  if (strFuncIdxSet.size === 0) return;
  const usedFuncIdxs = /* @__PURE__ */ new Set();
  collectStringCalls(fctx.body, strFuncIdxSet, usedFuncIdxs);
  if (usedFuncIdxs.size === 0) return;
  const cacheMap = /* @__PURE__ */ new Map();
  for (const funcIdx of usedFuncIdxs) {
    const localIdx = allocLocal(fctx, `__cached_str_${funcIdx}`, {
      kind: "externref",
    });
    cacheMap.set(funcIdx, localIdx);
  }
  const preamble = [];
  for (const [funcIdx, localIdx] of cacheMap) {
    preamble.push({ op: "call", funcIdx });
    preamble.push({ op: "local.set", index: localIdx });
  }
  replaceStringCalls(fctx.body, cacheMap);
  fctx.body.unshift(...preamble);
}
function collectStringCalls(instrs, strFuncIdxSet, found) {
  for (const instr of instrs) {
    if ((instr.op === "call" || instr.op === "return_call") && strFuncIdxSet.has(instr.funcIdx)) {
      found.add(instr.funcIdx);
    }
    if (instr.op === "block" || instr.op === "loop") {
      collectStringCalls(instr.body, strFuncIdxSet, found);
    } else if (instr.op === "if") {
      collectStringCalls(instr.then, strFuncIdxSet, found);
      if (instr.else) collectStringCalls(instr.else, strFuncIdxSet, found);
    } else if (instr.op === "try") {
      collectStringCalls(instr.body, strFuncIdxSet, found);
      for (const c of instr.catches) {
        collectStringCalls(c.body, strFuncIdxSet, found);
      }
      if (instr.catchAll) collectStringCalls(instr.catchAll, strFuncIdxSet, found);
    }
  }
}
function replaceStringCalls(instrs, cacheMap) {
  for (let i = 0; i < instrs.length; i++) {
    const instr = instrs[i];
    if ((instr.op === "call" || instr.op === "return_call") && cacheMap.has(instr.funcIdx)) {
      const localIdx = cacheMap.get(instr.funcIdx);
      instrs[i] = { op: "local.get", index: localIdx };
    }
    if (instr.op === "block" || instr.op === "loop") {
      replaceStringCalls(instr.body, cacheMap);
    } else if (instr.op === "if") {
      replaceStringCalls(instr.then, cacheMap);
      if (instr.else) replaceStringCalls(instr.else, cacheMap);
    } else if (instr.op === "try") {
      replaceStringCalls(instr.body, cacheMap);
      for (const c of instr.catches) {
        replaceStringCalls(c.body, cacheMap);
      }
      if (instr.catchAll) replaceStringCalls(instr.catchAll, cacheMap);
    }
  }
}
function hasExportModifier(node) {
  const modifiers = ts15.canHaveModifiers(node) ? ts15.getModifiers(node) : void 0;
  return modifiers?.some((m) => m.kind === ts15.SyntaxKind.ExportKeyword) ?? false;
}
function hasDeclareModifier(node) {
  const modifiers = ts15.canHaveModifiers(node) ? ts15.getModifiers(node) : void 0;
  return modifiers?.some((m) => m.kind === ts15.SyntaxKind.DeclareKeyword) ?? false;
}
function hasAsyncModifier(node) {
  const modifiers = ts15.canHaveModifiers(node) ? ts15.getModifiers(node) : void 0;
  return modifiers?.some((m) => m.kind === ts15.SyntaxKind.AsyncKeyword) ?? false;
}
function hasAbstractModifier(node) {
  return (ts15.getCombinedModifierFlags(node) & ts15.ModifierFlags.Abstract) !== 0;
}
function hasStaticModifier(node) {
  return (ts15.getCombinedModifierFlags(node) & ts15.ModifierFlags.Static) !== 0;
}
function isGeneratorFunction(node) {
  return node.asteriskToken !== void 0;
}
function unwrapGeneratorYieldType(type, ctx) {
  const symbol = type.getSymbol();
  if (symbol && symbol.name === "Generator") {
    const typeArgs = ctx.checker.getTypeArguments(type);
    if (typeArgs.length > 0) {
      return resolveWasmType(ctx, typeArgs[0]);
    }
  }
  if (symbol && (symbol.name === "Iterator" || symbol.name === "IterableIterator")) {
    const typeArgs = ctx.checker.getTypeArguments(type);
    if (typeArgs.length > 0) {
      return resolveWasmType(ctx, typeArgs[0]);
    }
  }
  return { kind: "f64" };
}
function ensureI32Condition(fctx, condType, ctx) {
  if (!condType) {
    fctx.body.push({ op: "i32.const", value: 0 });
    return;
  }
  if (condType.kind === "f64") {
    fctx.body.push({ op: "f64.abs" });
    fctx.body.push({ op: "f64.const", value: 0 });
    fctx.body.push({ op: "f64.gt" });
  } else if (condType.kind === "externref") {
    if (ctx) {
      addUnionImports(ctx);
      const funcIdx = ctx.funcMap.get("__is_truthy");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({ op: "i32.eqz" });
  } else if (condType.kind === "ref" || condType.kind === "ref_null") {
    if (ctx && isAnyValue(condType, ctx)) {
      ensureAnyHelpers(ctx);
      const funcIdx = ctx.funcMap.get("__any_unbox_bool");
      if (funcIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx });
        return;
      }
    }
    if (ctx && condType.typeIdx === ctx.anyStrTypeIdx) {
      const lengthIdx = ctx.nativeStrHelpers.get("__str_flatten");
      if (lengthIdx !== void 0) {
        fctx.body.push({ op: "call", funcIdx: lengthIdx });
        fctx.body.push({
          op: "struct.get",
          typeIdx: ctx.nativeStrTypeIdx,
          fieldIdx: 0,
        });
        fctx.body.push({ op: "i32.const", value: 0 });
        fctx.body.push({ op: "i32.gt_s" });
        return;
      }
    }
    fctx.body.push({ op: "ref.is_null" });
    fctx.body.push({ op: "i32.eqz" });
  } else if (condType.kind === "i64") {
    fctx.body.push({ op: "i64.eqz" });
    fctx.body.push({ op: "i32.eqz" });
  }
}

// src/codegen-linear/index.ts
import ts16 from "typescript";

// src/codegen-linear/context.ts
function addLocal(fctx, name, type) {
  const index = fctx.params.length + fctx.locals.length;
  fctx.locals.push({ name, type });
  fctx.localMap.set(name, index);
  return index;
}

// src/codegen-linear/layout.ts
function computeClassLayout(name, fieldDefs) {
  const HEADER_SIZE = 8;
  const FIELD_SIZE = 8;
  const fields = /* @__PURE__ */ new Map();
  let offset = HEADER_SIZE;
  for (const f of fieldDefs) {
    fields.set(f.name, { offset, type: f.type });
    offset += FIELD_SIZE;
  }
  return {
    name,
    totalSize: offset,
    fields,
    fieldCollectionKinds: /* @__PURE__ */ new Map(),
    methods: /* @__PURE__ */ new Map(),
    getters: /* @__PURE__ */ new Map(),
    ctorFuncName: `${name}_ctor`,
  };
}

// src/codegen-linear/runtime.ts
var HEAP_START = 1024;
function addRuntime(mod) {
  if (mod.memories.length === 0) {
    mod.memories.push({ min: 1, max: 256 });
    mod.exports.push({
      name: "memory",
      desc: { kind: "memory", index: 0 },
    });
  }
  const heapPtrGlobalIdx = mod.globals.length;
  const heapPtrGlobal = {
    name: "__heap_ptr",
    type: { kind: "i32" },
    mutable: true,
    init: [{ op: "i32.const", value: HEAP_START }],
  };
  mod.globals.push(heapPtrGlobal);
  const mallocTypeIdx = mod.types.length;
  const mallocType = {
    kind: "func",
    name: "$type___malloc",
    params: [{ kind: "i32" }],
    // size
    results: [{ kind: "i32" }],
    // pointer
  };
  mod.types.push(mallocType);
  const mallocBody = [
    // Save current heap pointer as return value
    { op: "global.get", index: heapPtrGlobalIdx },
    // Compute new heap pointer: old + size
    { op: "global.get", index: heapPtrGlobalIdx },
    { op: "local.get", index: 0 },
    // size param
    { op: "i32.add" },
    // Align to 8: (ptr + 7) & ~7
    { op: "i32.const", value: 7 },
    { op: "i32.add" },
    { op: "i32.const", value: -8 },
    // ~7 = 0xFFFFFFF8 = -8 in two's complement
    { op: "i32.and" },
    // Store new heap pointer
    { op: "global.set", index: heapPtrGlobalIdx },
    // Return old pointer (already on stack from first global.get)
  ];
  const mallocFuncIdx = mod.functions.length;
  mod.functions.push({
    name: "__malloc",
    typeIdx: mallocTypeIdx,
    locals: [],
    body: mallocBody,
    exported: false,
  });
}
function addUint8ArrayRuntime(mod) {
  const mallocIdx = findFuncIndex(mod, "__malloc");
  addRuntimeFunc(
    mod,
    "__u8arr_new",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (local1Idx) => [
      // Allocate: 12 + len bytes
      { op: "i32.const", value: 12 },
      { op: "local.get", index: 0 },
      // len
      { op: "i32.add" },
      { op: "call", funcIdx: mallocIdx },
      { op: "local.set", index: local1Idx },
      // Store tag byte 0x02 (Uint8Array) at ptr+0
      { op: "local.get", index: local1Idx },
      { op: "i32.const", value: 2 },
      { op: "i32.store8", align: 0, offset: 0 },
      // Store len at ptr+8
      { op: "local.get", index: local1Idx },
      { op: "local.get", index: 0 },
      // len
      { op: "i32.store", align: 2, offset: 8 },
      // Return ptr
      { op: "local.get", index: local1Idx },
    ],
    1,
  );
  addRuntimeFunc(mod, "__u8arr_get", [{ kind: "i32" }, { kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    // ptr
    { op: "local.get", index: 1 },
    // idx
    { op: "i32.add" },
    { op: "i32.load8_u", align: 0, offset: 12 },
  ]);
  addRuntimeFunc(mod, "__u8arr_set", [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }], [], [], () => [
    { op: "local.get", index: 0 },
    // ptr
    { op: "local.get", index: 1 },
    // idx
    { op: "i32.add" },
    { op: "local.get", index: 2 },
    // val
    { op: "i32.store8", align: 0, offset: 12 },
  ]);
  addRuntimeFunc(mod, "__u8arr_len", [{ kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    // ptr
    { op: "i32.load", align: 2, offset: 8 },
  ]);
  addRuntimeFunc(
    mod,
    "__u8arr_from_raw",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const newPtrLocal = firstLocalIdx;
      const iLocal = firstLocalIdx + 1;
      return [
        // newPtr = __u8arr_new(len)
        { op: "local.get", index: 1 },
        // len
        { op: "call", funcIdx: findFuncIndex(mod, "__u8arr_new") },
        { op: "local.set", index: newPtrLocal },
        // Copy loop
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: 1 },
                // len
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // newPtr[12+i] = rawPtr[i]
                { op: "local.get", index: newPtrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "local.get", index: 0 },
                // rawPtr
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 0 },
                { op: "i32.store8", align: 0, offset: 12 },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: newPtrLocal },
      ];
    },
    2,
  );
  const u8NewIdx = findFuncIndex(mod, "__u8arr_new");
  addRuntimeFunc(
    mod,
    "__u8arr_slice",
    [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (local3Idx) => [
      // newLen = end - start
      { op: "local.get", index: 2 },
      // end
      { op: "local.get", index: 1 },
      // start
      { op: "i32.sub" },
      { op: "local.set", index: local3Idx },
      // local3 = newLen
      // newPtr = __u8arr_new(newLen)
      { op: "local.get", index: local3Idx },
      { op: "call", funcIdx: u8NewIdx },
      { op: "local.set", index: local3Idx + 1 },
      // local4 = newPtr
      // Copy loop: i = 0
      { op: "i32.const", value: 0 },
      { op: "local.set", index: local3Idx + 2 },
      // local5 = 0
      {
        op: "block",
        blockType: { kind: "empty" },
        body: [
          {
            op: "loop",
            blockType: { kind: "empty" },
            body: [
              // if (i >= newLen) break
              { op: "local.get", index: local3Idx + 2 },
              // i
              { op: "local.get", index: local3Idx },
              // newLen
              { op: "i32.ge_u" },
              { op: "br_if", depth: 1 },
              // newPtr[12 + i] = src[12 + start + i]
              { op: "local.get", index: local3Idx + 1 },
              // newPtr
              { op: "local.get", index: local3Idx + 2 },
              // i
              { op: "i32.add" },
              // load src byte
              { op: "local.get", index: 0 },
              // src ptr
              { op: "local.get", index: 1 },
              // start
              { op: "i32.add" },
              { op: "local.get", index: local3Idx + 2 },
              // i
              { op: "i32.add" },
              { op: "i32.load8_u", align: 0, offset: 12 },
              // store into newPtr
              { op: "i32.store8", align: 0, offset: 12 },
              // i++
              { op: "local.get", index: local3Idx + 2 },
              { op: "i32.const", value: 1 },
              { op: "i32.add" },
              { op: "local.set", index: local3Idx + 2 },
              { op: "br", depth: 0 },
            ],
          },
        ],
      },
      // Return newPtr
      { op: "local.get", index: local3Idx + 1 },
    ],
    3,
  );
  addRuntimeFunc(
    mod,
    "__u8arr_from_arr",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (local1Idx) => {
      const lenLocal = local1Idx;
      const newPtrLocal = local1Idx + 1;
      const iLocal = local1Idx + 2;
      return [
        // len = arrPtr.len (at +8)
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 8 },
        { op: "local.set", index: lenLocal },
        // newPtr = __u8arr_new(len)
        { op: "local.get", index: lenLocal },
        { op: "call", funcIdx: findFuncIndex(mod, "__u8arr_new") },
        { op: "local.set", index: newPtrLocal },
        // i = 0
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        // Copy loop
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: lenLocal },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // newPtr[12+i] = (u8) arrPtr[16 + i*4]
                { op: "local.get", index: newPtrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                // Load element from array: arrPtr + 16 + i*4
                { op: "local.get", index: 0 },
                // arrPtr
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 4 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "i32.load", align: 2, offset: 16 },
                // Store as byte
                { op: "i32.store8", align: 0, offset: 12 },
                // i++
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: newPtrLocal },
      ];
    },
    3,
  );
}
function addArrayRuntime(mod) {
  const mallocIdx = findFuncIndex(mod, "__malloc");
  addRuntimeFunc(
    mod,
    "__arr_new",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (local1Idx) => [
      // Allocate: 16 + cap*4
      { op: "i32.const", value: 16 },
      { op: "local.get", index: 0 },
      // cap
      { op: "i32.const", value: 4 },
      { op: "i32.mul" },
      { op: "i32.add" },
      { op: "call", funcIdx: mallocIdx },
      { op: "local.set", index: local1Idx },
      // Store tag byte 0x01 (Array) at ptr+0
      { op: "local.get", index: local1Idx },
      { op: "i32.const", value: 1 },
      { op: "i32.store8", align: 0, offset: 0 },
      // Store len=0 at ptr+8
      { op: "local.get", index: local1Idx },
      { op: "i32.const", value: 0 },
      { op: "i32.store", align: 2, offset: 8 },
      // Store cap at ptr+12
      { op: "local.get", index: local1Idx },
      { op: "local.get", index: 0 },
      // cap
      { op: "i32.store", align: 2, offset: 12 },
      // Return ptr
      { op: "local.get", index: local1Idx },
    ],
    1,
  );
  addRuntimeFunc(
    mod,
    "__arr_push",
    [{ kind: "i32" }, { kind: "i32" }],
    [],
    [],
    (local2Idx) => [
      // Load current len
      { op: "local.get", index: 0 },
      // ptr
      { op: "i32.load", align: 2, offset: 8 },
      { op: "local.set", index: local2Idx },
      // Store val at ptr + 16 + len*4
      { op: "local.get", index: 0 },
      // ptr
      { op: "local.get", index: local2Idx },
      // len
      { op: "i32.const", value: 4 },
      { op: "i32.mul" },
      { op: "i32.add" },
      { op: "local.get", index: 1 },
      // val
      { op: "i32.store", align: 2, offset: 16 },
      // Increment len: store len+1 at ptr+8
      { op: "local.get", index: 0 },
      // ptr
      { op: "local.get", index: local2Idx },
      // len
      { op: "i32.const", value: 1 },
      { op: "i32.add" },
      { op: "i32.store", align: 2, offset: 8 },
    ],
    1,
  );
  addRuntimeFunc(mod, "__arr_get", [{ kind: "i32" }, { kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    // ptr
    { op: "local.get", index: 1 },
    // idx
    { op: "i32.const", value: 4 },
    { op: "i32.mul" },
    { op: "i32.add" },
    { op: "i32.load", align: 2, offset: 16 },
  ]);
  addRuntimeFunc(mod, "__arr_set", [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }], [], [], () => [
    { op: "local.get", index: 0 },
    // ptr
    { op: "local.get", index: 1 },
    // idx
    { op: "i32.const", value: 4 },
    { op: "i32.mul" },
    { op: "i32.add" },
    { op: "local.get", index: 2 },
    // val
    { op: "i32.store", align: 2, offset: 16 },
  ]);
  addRuntimeFunc(mod, "__arr_len", [{ kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    // ptr
    { op: "i32.load", align: 2, offset: 8 },
  ]);
  const arrNewIdx = findFuncIndex(mod, "__arr_new");
  const arrGetIdx = findFuncIndex(mod, "__arr_get");
  const arrPushIdx = findFuncIndex(mod, "__arr_push");
  addRuntimeFunc(
    mod,
    "__arr_slice",
    [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const newArrLocal = firstLocalIdx;
      const iLocal2 = firstLocalIdx + 1;
      return [
        // newArr = __arr_new(16)
        { op: "i32.const", value: 16 },
        { op: "call", funcIdx: arrNewIdx },
        { op: "local.set", index: newArrLocal },
        // i = start
        { op: "local.get", index: 1 },
        { op: "local.set", index: iLocal2 },
        // loop: while i < end, push arr[i]
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal2 },
                { op: "local.get", index: 2 },
                { op: "i32.ge_s" },
                { op: "br_if", depth: 1 },
                // __arr_push(newArr, __arr_get(arr, i))
                { op: "local.get", index: newArrLocal },
                { op: "local.get", index: 0 },
                { op: "local.get", index: iLocal2 },
                { op: "call", funcIdx: arrGetIdx },
                { op: "call", funcIdx: arrPushIdx },
                // i++
                { op: "local.get", index: iLocal2 },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal2 },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: newArrLocal },
      ];
    },
    2,
  );
}
function addStringRuntime(mod) {
  const mallocIdx = findFuncIndex(mod, "__malloc");
  addRuntimeFunc(
    mod,
    "__str_from_data",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const ptrLocal = firstLocalIdx;
      const iLocal = firstLocalIdx + 1;
      return [
        // Allocate: 12 + len
        { op: "i32.const", value: 12 },
        { op: "local.get", index: 1 },
        // len
        { op: "i32.add" },
        { op: "call", funcIdx: mallocIdx },
        { op: "local.set", index: ptrLocal },
        // Store len at ptr+8
        { op: "local.get", index: ptrLocal },
        { op: "local.get", index: 1 },
        // len
        { op: "i32.store", align: 2, offset: 8 },
        // Copy bytes: for i=0; i<len; i++ { mem[ptr+12+i] = mem[offset+i] }
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                // break if i >= len
                { op: "local.get", index: iLocal },
                { op: "local.get", index: 1 },
                // len
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // Store byte: mem[ptr+12+i] = mem[offset+i]
                { op: "local.get", index: ptrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                // Load source byte
                { op: "local.get", index: 0 },
                // offset
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 0 },
                // Store at dest
                { op: "i32.store8", align: 0, offset: 12 },
                // i++
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        // Return ptr
        { op: "local.get", index: ptrLocal },
      ];
    },
    2,
  );
  addRuntimeFunc(mod, "__str_len", [{ kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    { op: "i32.load", align: 2, offset: 8 },
  ]);
  addRuntimeFunc(
    mod,
    "__str_eq",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const lenALocal = firstLocalIdx;
      const iLocal = firstLocalIdx + 1;
      return [
        // Load lenA
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 8 },
        { op: "local.set", index: lenALocal },
        // If lenA != lenB, return 0
        { op: "local.get", index: lenALocal },
        { op: "local.get", index: 1 },
        { op: "i32.load", align: 2, offset: 8 },
        { op: "i32.ne" },
        { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 0 }, { op: "return" }] },
        // Compare bytes
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                // break if i >= lenA
                { op: "local.get", index: iLocal },
                { op: "local.get", index: lenALocal },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // Compare bytes
                { op: "local.get", index: 0 },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 12 },
                { op: "local.get", index: 1 },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 12 },
                { op: "i32.ne" },
                { op: "if", blockType: { kind: "empty" }, then: [{ op: "i32.const", value: 0 }, { op: "return" }] },
                // i++
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        // All bytes match
        { op: "i32.const", value: 1 },
      ];
    },
    2,
  );
  addRuntimeFunc(
    mod,
    "__str_hash",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const lenLocal = firstLocalIdx + 1;
      const iLocal = firstLocalIdx + 2;
      return [
        // hash = FNV offset basis
        { op: "i32.const", value: 2166136261 | 0 },
        // sign-extend to i32
        { op: "local.set", index: hashLocal },
        // len = str.len
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 8 },
        { op: "local.set", index: lenLocal },
        // i = 0
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                // break if i >= len
                { op: "local.get", index: iLocal },
                { op: "local.get", index: lenLocal },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // hash ^= byte[i]
                { op: "local.get", index: hashLocal },
                { op: "local.get", index: 0 },
                // ptr
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 12 },
                { op: "i32.xor" },
                // hash *= FNV prime
                { op: "i32.const", value: 16777619 },
                { op: "i32.mul" },
                { op: "local.set", index: hashLocal },
                // i++
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: hashLocal },
      ];
    },
    3,
  );
  addRuntimeFunc(
    mod,
    "__str_concat",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const lenALocal = firstLocalIdx;
      const lenBLocal = firstLocalIdx + 1;
      const ptrLocal = firstLocalIdx + 2;
      const iLocal = firstLocalIdx + 3;
      return [
        // lenA
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 8 },
        { op: "local.set", index: lenALocal },
        // lenB
        { op: "local.get", index: 1 },
        { op: "i32.load", align: 2, offset: 8 },
        { op: "local.set", index: lenBLocal },
        // Allocate: 12 + lenA + lenB
        { op: "i32.const", value: 12 },
        { op: "local.get", index: lenALocal },
        { op: "i32.add" },
        { op: "local.get", index: lenBLocal },
        { op: "i32.add" },
        { op: "call", funcIdx: mallocIdx },
        { op: "local.set", index: ptrLocal },
        // Store total len at ptr+8
        { op: "local.get", index: ptrLocal },
        { op: "local.get", index: lenALocal },
        { op: "local.get", index: lenBLocal },
        { op: "i32.add" },
        { op: "i32.store", align: 2, offset: 8 },
        // Copy first string bytes
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: lenALocal },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                { op: "local.get", index: ptrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "local.get", index: 0 },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 12 },
                { op: "i32.store8", align: 0, offset: 12 },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        // Copy second string bytes
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: lenBLocal },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // dest: ptr + 12 + lenA + i
                { op: "local.get", index: ptrLocal },
                { op: "local.get", index: lenALocal },
                { op: "i32.add" },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                // src byte
                { op: "local.get", index: 1 },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 12 },
                { op: "i32.store8", align: 0, offset: 12 },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: ptrLocal },
      ];
    },
    4,
  );
  const u8LenIdx = findFuncIndex(mod, "__u8arr_len");
  addRuntimeFunc(
    mod,
    "__str_from_u8arr",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const lenLocal = firstLocalIdx;
      const ptrLocalFu = firstLocalIdx + 1;
      const iLocalFu = firstLocalIdx + 2;
      return [
        // len = __u8arr_len(u8arr)
        { op: "local.get", index: 0 },
        { op: "call", funcIdx: u8LenIdx },
        { op: "local.set", index: lenLocal },
        // newPtr = malloc(12 + len)
        { op: "i32.const", value: 12 },
        { op: "local.get", index: lenLocal },
        { op: "i32.add" },
        { op: "call", funcIdx: mallocIdx },
        { op: "local.set", index: ptrLocalFu },
        // Store tag byte 0x02 (Uint8Array) at newPtr+0
        { op: "local.get", index: ptrLocalFu },
        { op: "i32.const", value: 2 },
        { op: "i32.store8", align: 0, offset: 0 },
        // Store len at newPtr+8
        { op: "local.get", index: ptrLocalFu },
        { op: "local.get", index: lenLocal },
        { op: "i32.store", align: 2, offset: 8 },
        // Copy bytes
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocalFu },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocalFu },
                { op: "local.get", index: lenLocal },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // newPtr[12+i] = u8arr[12+i]
                { op: "local.get", index: ptrLocalFu },
                { op: "local.get", index: iLocalFu },
                { op: "i32.add" },
                { op: "local.get", index: 0 },
                // u8arr ptr
                { op: "local.get", index: iLocalFu },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 12 },
                { op: "i32.store8", align: 0, offset: 12 },
                { op: "local.get", index: iLocalFu },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocalFu },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: ptrLocalFu },
      ];
    },
    3,
  );
  const strLenIdx = findFuncIndex(mod, "__str_len");
  addRuntimeFunc(
    mod,
    "__str_starts_with",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const strLenLocal = firstLocalIdx;
      const prefixLenLocal = firstLocalIdx + 1;
      const iLocal = firstLocalIdx + 2;
      const resultLocal = firstLocalIdx + 3;
      return [
        // strLen = __str_len(str)
        { op: "local.get", index: 0 },
        { op: "call", funcIdx: strLenIdx },
        { op: "local.set", index: strLenLocal },
        // prefixLen = __str_len(prefix)
        { op: "local.get", index: 1 },
        { op: "call", funcIdx: strLenIdx },
        { op: "local.set", index: prefixLenLocal },
        // result = 1 (assume true)
        { op: "i32.const", value: 1 },
        { op: "local.set", index: resultLocal },
        // if strLen < prefixLen, result = 0
        { op: "local.get", index: strLenLocal },
        { op: "local.get", index: prefixLenLocal },
        { op: "i32.lt_u" },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "i32.const", value: 0 },
            { op: "local.set", index: resultLocal },
          ],
          else: [
            // Compare prefix bytes: i = 0
            { op: "i32.const", value: 0 },
            { op: "local.set", index: iLocal },
            {
              op: "block",
              blockType: { kind: "empty" },
              body: [
                {
                  op: "loop",
                  blockType: { kind: "empty" },
                  body: [
                    // if i >= prefixLen, break (result stays 1)
                    { op: "local.get", index: iLocal },
                    { op: "local.get", index: prefixLenLocal },
                    { op: "i32.ge_u" },
                    { op: "br_if", depth: 1 },
                    // if str[12+i] != prefix[12+i], result = 0 and break
                    { op: "local.get", index: 0 },
                    { op: "local.get", index: iLocal },
                    { op: "i32.add" },
                    { op: "i32.load8_u", align: 0, offset: 12 },
                    { op: "local.get", index: 1 },
                    { op: "local.get", index: iLocal },
                    { op: "i32.add" },
                    { op: "i32.load8_u", align: 0, offset: 12 },
                    { op: "i32.ne" },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [
                        { op: "i32.const", value: 0 },
                        { op: "local.set", index: resultLocal },
                        { op: "br", depth: 2 },
                        // break to outer block
                      ],
                    },
                    // i++
                    { op: "local.get", index: iLocal },
                    { op: "i32.const", value: 1 },
                    { op: "i32.add" },
                    { op: "local.set", index: iLocal },
                    { op: "br", depth: 0 },
                    // continue loop
                  ],
                },
              ],
            },
          ],
        },
        // return result
        { op: "local.get", index: resultLocal },
      ];
    },
    4,
  );
  addRuntimeFunc(
    mod,
    "__str_slice",
    [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const newLenLocal = firstLocalIdx;
      const ptrLocal = firstLocalIdx + 1;
      const iLocal = firstLocalIdx + 2;
      return [
        // newLen = end - start
        { op: "local.get", index: 2 },
        { op: "local.get", index: 1 },
        { op: "i32.sub" },
        { op: "local.set", index: newLenLocal },
        // Clamp: if newLen < 0, set to 0
        { op: "local.get", index: newLenLocal },
        { op: "i32.const", value: 0 },
        { op: "i32.lt_s" },
        {
          op: "if",
          blockType: { kind: "empty" },
          then: [
            { op: "i32.const", value: 0 },
            { op: "local.set", index: newLenLocal },
          ],
        },
        // ptr = malloc(12 + newLen)
        { op: "i32.const", value: 12 },
        { op: "local.get", index: newLenLocal },
        { op: "i32.add" },
        { op: "call", funcIdx: mallocIdx },
        { op: "local.set", index: ptrLocal },
        // store length at ptr+8
        { op: "local.get", index: ptrLocal },
        { op: "local.get", index: newLenLocal },
        { op: "i32.store", align: 2, offset: 8 },
        // copy bytes: for i = 0; i < newLen; i++
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: newLenLocal },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // dest: ptr + 12 + i
                { op: "local.get", index: ptrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                // src: str + 12 + start + i
                { op: "local.get", index: 0 },
                { op: "local.get", index: 1 },
                { op: "i32.add" },
                { op: "local.get", index: iLocal },
                { op: "i32.add" },
                { op: "i32.load8_u", align: 0, offset: 12 },
                { op: "i32.store8", align: 0, offset: 12 },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: ptrLocal },
      ];
    },
    3,
  );
  addRuntimeFunc(
    mod,
    "__str_index_of",
    [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const strLenLocal = firstLocalIdx;
      const sepLenLocal = firstLocalIdx + 1;
      const iLocal2 = firstLocalIdx + 2;
      const jLocal = firstLocalIdx + 3;
      const matchLocal = firstLocalIdx + 4;
      return [
        // strLen = str.length
        { op: "local.get", index: 0 },
        { op: "call", funcIdx: strLenIdx },
        { op: "local.set", index: strLenLocal },
        // sepLen = sep.length
        { op: "local.get", index: 1 },
        { op: "call", funcIdx: strLenIdx },
        { op: "local.set", index: sepLenLocal },
        // for i = fromIdx; i <= strLen - sepLen; i++
        { op: "local.get", index: 2 },
        { op: "local.set", index: iLocal2 },
        {
          op: "block",
          blockType: { kind: "val", type: { kind: "i32" } },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                // if i > strLen - sepLen, return -1
                { op: "local.get", index: iLocal2 },
                { op: "local.get", index: strLenLocal },
                { op: "local.get", index: sepLenLocal },
                { op: "i32.sub" },
                { op: "i32.gt_s" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "i32.const", value: -1 },
                    { op: "br", depth: 3 },
                    // return -1 (break out of block)
                  ],
                },
                // match = true
                { op: "i32.const", value: 1 },
                { op: "local.set", index: matchLocal },
                // for j = 0; j < sepLen; j++
                { op: "i32.const", value: 0 },
                { op: "local.set", index: jLocal },
                {
                  op: "block",
                  blockType: { kind: "empty" },
                  body: [
                    {
                      op: "loop",
                      blockType: { kind: "empty" },
                      body: [
                        { op: "local.get", index: jLocal },
                        { op: "local.get", index: sepLenLocal },
                        { op: "i32.ge_u" },
                        { op: "br_if", depth: 1 },
                        // compare str[i+j] with sep[j]
                        { op: "local.get", index: 0 },
                        { op: "local.get", index: iLocal2 },
                        { op: "i32.add" },
                        { op: "local.get", index: jLocal },
                        { op: "i32.add" },
                        { op: "i32.load8_u", align: 0, offset: 12 },
                        { op: "local.get", index: 1 },
                        { op: "local.get", index: jLocal },
                        { op: "i32.add" },
                        { op: "i32.load8_u", align: 0, offset: 12 },
                        { op: "i32.ne" },
                        {
                          op: "if",
                          blockType: { kind: "empty" },
                          then: [
                            { op: "i32.const", value: 0 },
                            { op: "local.set", index: matchLocal },
                            { op: "br", depth: 2 },
                            // break inner loop
                          ],
                        },
                        { op: "local.get", index: jLocal },
                        { op: "i32.const", value: 1 },
                        { op: "i32.add" },
                        { op: "local.set", index: jLocal },
                        { op: "br", depth: 0 },
                      ],
                    },
                  ],
                },
                // if match, return i
                { op: "local.get", index: matchLocal },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: iLocal2 },
                    { op: "br", depth: 2 },
                    // return i
                  ],
                },
                // i++
                { op: "local.get", index: iLocal2 },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal2 },
                { op: "br", depth: 0 },
              ],
            },
            // Loop fallthrough (unreachable in practice): return -1
            { op: "i32.const", value: -1 },
          ],
        },
      ];
    },
    5,
  );
  const strSliceIdx = findFuncIndex(mod, "__str_slice");
  const strIndexOfIdx = findFuncIndex(mod, "__str_index_of");
  const arrNewIdx = findFuncIndex(mod, "__arr_new");
  const arrPushIdx = findFuncIndex(mod, "__arr_push");
  addRuntimeFunc(
    mod,
    "__str_split",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const resultLocal = firstLocalIdx;
      const strLenLocal2 = firstLocalIdx + 1;
      const startLocal = firstLocalIdx + 2;
      const posLocal = firstLocalIdx + 3;
      const sepLenLocal2 = firstLocalIdx + 4;
      return [
        // result = __arr_new(16)
        { op: "i32.const", value: 16 },
        { op: "call", funcIdx: arrNewIdx },
        { op: "local.set", index: resultLocal },
        // strLen
        { op: "local.get", index: 0 },
        { op: "call", funcIdx: strLenIdx },
        { op: "local.set", index: strLenLocal2 },
        // sepLen
        { op: "local.get", index: 1 },
        { op: "call", funcIdx: strLenIdx },
        { op: "local.set", index: sepLenLocal2 },
        // start = 0
        { op: "i32.const", value: 0 },
        { op: "local.set", index: startLocal },
        // loop: find sep, push substring, advance
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                // pos = __str_index_of(str, sep, start)
                { op: "local.get", index: 0 },
                { op: "local.get", index: 1 },
                { op: "local.get", index: startLocal },
                { op: "call", funcIdx: strIndexOfIdx },
                { op: "local.set", index: posLocal },
                // if pos == -1, break
                { op: "local.get", index: posLocal },
                { op: "i32.const", value: -1 },
                { op: "i32.eq" },
                { op: "br_if", depth: 1 },
                // push substring [start, pos)
                { op: "local.get", index: resultLocal },
                { op: "local.get", index: 0 },
                { op: "local.get", index: startLocal },
                { op: "local.get", index: posLocal },
                { op: "call", funcIdx: strSliceIdx },
                { op: "call", funcIdx: arrPushIdx },
                // start = pos + sepLen
                { op: "local.get", index: posLocal },
                { op: "local.get", index: sepLenLocal2 },
                { op: "i32.add" },
                { op: "local.set", index: startLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        // push final substring [start, strLen)
        { op: "local.get", index: resultLocal },
        { op: "local.get", index: 0 },
        { op: "local.get", index: startLocal },
        { op: "local.get", index: strLenLocal2 },
        { op: "call", funcIdx: strSliceIdx },
        { op: "call", funcIdx: arrPushIdx },
        // return result
        { op: "local.get", index: resultLocal },
      ];
    },
    5,
  );
}
function addMapRuntime(mod) {
  const mallocIdx = findFuncIndex(mod, "__malloc");
  const strHashIdx = findFuncIndex(mod, "__str_hash");
  const strEqIdx = findFuncIndex(mod, "__str_eq");
  addRuntimeFunc(
    mod,
    "__map_new",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const ptrLocal = firstLocalIdx;
      const totalSizeLocal = firstLocalIdx + 1;
      const iLocal = firstLocalIdx + 2;
      return [
        // totalSize = 16 + cap * 12
        { op: "i32.const", value: 16 },
        { op: "local.get", index: 0 },
        // cap
        { op: "i32.const", value: 12 },
        { op: "i32.mul" },
        { op: "i32.add" },
        { op: "local.set", index: totalSizeLocal },
        // Allocate
        { op: "local.get", index: totalSizeLocal },
        { op: "call", funcIdx: mallocIdx },
        { op: "local.set", index: ptrLocal },
        // Store count=0
        { op: "local.get", index: ptrLocal },
        { op: "i32.const", value: 0 },
        { op: "i32.store", align: 2, offset: 8 },
        // Store cap
        { op: "local.get", index: ptrLocal },
        { op: "local.get", index: 0 },
        // cap
        { op: "i32.store", align: 2, offset: 12 },
        // Zero out entries (hash=0 means empty)
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: 0 },
                // cap
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                // Zero out hash at entry[i]
                { op: "local.get", index: ptrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 12 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "i32.const", value: 0 },
                { op: "i32.store", align: 2, offset: 16 },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: ptrLocal },
      ];
    },
    3,
  );
  addRuntimeFunc(
    mod,
    "__map_set",
    [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }],
    [],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        // hash = __str_hash(key) | ensure non-zero by OR with 1
        { op: "local.get", index: 1 },
        // key
        { op: "call", funcIdx: strHashIdx },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        // ensure hash != 0 (0 = empty sentinel)
        { op: "local.set", index: hashLocal },
        // cap = map.cap
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        // idx = hash % cap (unsigned)
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        // Linear probe loop
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                // entryAddr = map + 16 + idx * 12
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 12 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                // entryHash = load hash at entryAddr+16
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                // If empty slot (hash=0): insert here
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    // Store hash
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: hashLocal },
                    { op: "i32.store", align: 2, offset: 16 },
                    // Store key
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: 1 },
                    { op: "i32.store", align: 2, offset: 20 },
                    // Store val
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: 2 },
                    { op: "i32.store", align: 2, offset: 24 },
                    // Increment count
                    { op: "local.get", index: 0 },
                    { op: "local.get", index: 0 },
                    { op: "i32.load", align: 2, offset: 8 },
                    { op: "i32.const", value: 1 },
                    { op: "i32.add" },
                    { op: "i32.store", align: 2, offset: 8 },
                    { op: "return" },
                  ],
                },
                // If same hash AND keys equal: update value
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    // Check string equality
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    // existing key
                    { op: "local.get", index: 1 },
                    // new key
                    { op: "call", funcIdx: strEqIdx },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [
                        // Update value
                        { op: "local.get", index: entryAddrLocal },
                        { op: "local.get", index: 2 },
                        { op: "i32.store", align: 2, offset: 24 },
                        { op: "return" },
                      ],
                    },
                  ],
                },
                // Advance: idx = (idx + 1) % cap
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
      ];
    },
    5,
  );
  addRuntimeFunc(
    mod,
    "__map_get",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        { op: "local.get", index: 1 },
        { op: "call", funcIdx: strHashIdx },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 12 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                // Empty slot → not found
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [{ op: "i32.const", value: 0 }, { op: "return" }],
                },
                // Check hash + key equality
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "call", funcIdx: strEqIdx },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [
                        { op: "local.get", index: entryAddrLocal },
                        { op: "i32.load", align: 2, offset: 24 },
                        { op: "return" },
                      ],
                    },
                  ],
                },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "i32.const", value: 0 },
      ];
    },
    5,
  );
  addRuntimeFunc(
    mod,
    "__map_has",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        { op: "local.get", index: 1 },
        { op: "call", funcIdx: strHashIdx },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 12 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [{ op: "i32.const", value: 0 }, { op: "return" }],
                },
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "call", funcIdx: strEqIdx },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [{ op: "i32.const", value: 1 }, { op: "return" }],
                    },
                  ],
                },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "i32.const", value: 0 },
      ];
    },
    5,
  );
  addRuntimeFunc(mod, "__map_size", [{ kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    { op: "i32.load", align: 2, offset: 8 },
  ]);
}
function addSetRuntime(mod) {
  const mallocIdx = findFuncIndex(mod, "__malloc");
  const strHashIdx = findFuncIndex(mod, "__str_hash");
  const strEqIdx = findFuncIndex(mod, "__str_eq");
  addRuntimeFunc(
    mod,
    "__set_new",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const ptrLocal = firstLocalIdx;
      const iLocal = firstLocalIdx + 1;
      return [
        // Allocate: 16 + cap * 8
        { op: "i32.const", value: 16 },
        { op: "local.get", index: 0 },
        { op: "i32.const", value: 8 },
        { op: "i32.mul" },
        { op: "i32.add" },
        { op: "call", funcIdx: mallocIdx },
        { op: "local.set", index: ptrLocal },
        // count = 0
        { op: "local.get", index: ptrLocal },
        { op: "i32.const", value: 0 },
        { op: "i32.store", align: 2, offset: 8 },
        // cap
        { op: "local.get", index: ptrLocal },
        { op: "local.get", index: 0 },
        { op: "i32.store", align: 2, offset: 12 },
        // Zero entries
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: 0 },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                { op: "local.get", index: ptrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 8 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "i32.const", value: 0 },
                { op: "i32.store", align: 2, offset: 16 },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: ptrLocal },
      ];
    },
    2,
  );
  addRuntimeFunc(
    mod,
    "__set_add",
    [{ kind: "i32" }, { kind: "i32" }],
    [],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        { op: "local.get", index: 1 },
        { op: "call", funcIdx: strHashIdx },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 8 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                // Empty slot → insert
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: hashLocal },
                    { op: "i32.store", align: 2, offset: 16 },
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: 1 },
                    { op: "i32.store", align: 2, offset: 20 },
                    // Increment count
                    { op: "local.get", index: 0 },
                    { op: "local.get", index: 0 },
                    { op: "i32.load", align: 2, offset: 8 },
                    { op: "i32.const", value: 1 },
                    { op: "i32.add" },
                    { op: "i32.store", align: 2, offset: 8 },
                    { op: "return" },
                  ],
                },
                // Same hash → check equality
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "call", funcIdx: strEqIdx },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [
                        { op: "return" },
                        // already in set
                      ],
                    },
                  ],
                },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
      ];
    },
    5,
  );
  addRuntimeFunc(
    mod,
    "__set_has",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        { op: "local.get", index: 1 },
        { op: "call", funcIdx: strHashIdx },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 8 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [{ op: "i32.const", value: 0 }, { op: "return" }],
                },
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "call", funcIdx: strEqIdx },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [{ op: "i32.const", value: 1 }, { op: "return" }],
                    },
                  ],
                },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "i32.const", value: 0 },
      ];
    },
    5,
  );
  addRuntimeFunc(mod, "__set_size", [{ kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    { op: "i32.load", align: 2, offset: 8 },
  ]);
}
function addNumericMapRuntime(mod) {
  const mallocIdx = findFuncIndex(mod, "__malloc");
  addRuntimeFunc(
    mod,
    "__nmap_new",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const ptrLocal = firstLocalIdx;
      const iLocal = firstLocalIdx + 1;
      return [
        // totalSize = 16 + cap * 12
        { op: "i32.const", value: 16 },
        { op: "local.get", index: 0 },
        { op: "i32.const", value: 12 },
        { op: "i32.mul" },
        { op: "i32.add" },
        { op: "call", funcIdx: mallocIdx },
        { op: "local.set", index: ptrLocal },
        // count = 0
        { op: "local.get", index: ptrLocal },
        { op: "i32.const", value: 0 },
        { op: "i32.store", align: 2, offset: 8 },
        // cap
        { op: "local.get", index: ptrLocal },
        { op: "local.get", index: 0 },
        { op: "i32.store", align: 2, offset: 12 },
        // Zero out entries
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: 0 },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                { op: "local.get", index: ptrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 12 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "i32.const", value: 0 },
                { op: "i32.store", align: 2, offset: 16 },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: ptrLocal },
      ];
    },
    2,
  );
  addRuntimeFunc(
    mod,
    "__nmap_set",
    [{ kind: "i32" }, { kind: "i32" }, { kind: "i32" }],
    [],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        // hash = (key * 2654435761) | 1
        { op: "local.get", index: 1 },
        { op: "i32.const", value: 2654435761 | 0 },
        { op: "i32.mul" },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        // cap
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        // idx = hash % cap
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                // entryAddr = map + idx * 12
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 12 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                // entryHash
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                // Empty slot → insert
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: hashLocal },
                    { op: "i32.store", align: 2, offset: 16 },
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: 1 },
                    { op: "i32.store", align: 2, offset: 20 },
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: 2 },
                    { op: "i32.store", align: 2, offset: 24 },
                    // Increment count
                    { op: "local.get", index: 0 },
                    { op: "local.get", index: 0 },
                    { op: "i32.load", align: 2, offset: 8 },
                    { op: "i32.const", value: 1 },
                    { op: "i32.add" },
                    { op: "i32.store", align: 2, offset: 8 },
                    { op: "return" },
                  ],
                },
                // Same hash → check key equality (numeric)
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "i32.eq" },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [
                        // Update value
                        { op: "local.get", index: entryAddrLocal },
                        { op: "local.get", index: 2 },
                        { op: "i32.store", align: 2, offset: 24 },
                        { op: "return" },
                      ],
                    },
                  ],
                },
                // Advance
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
      ];
    },
    5,
  );
  addRuntimeFunc(
    mod,
    "__nmap_get",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        { op: "local.get", index: 1 },
        { op: "i32.const", value: 2654435761 | 0 },
        { op: "i32.mul" },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 12 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                // Empty → not found
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [{ op: "i32.const", value: 0 }, { op: "return" }],
                },
                // Check hash + key
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "i32.eq" },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [
                        { op: "local.get", index: entryAddrLocal },
                        { op: "i32.load", align: 2, offset: 24 },
                        { op: "return" },
                      ],
                    },
                  ],
                },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "i32.const", value: 0 },
      ];
    },
    5,
  );
  addRuntimeFunc(
    mod,
    "__nmap_has",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        { op: "local.get", index: 1 },
        { op: "i32.const", value: 2654435761 | 0 },
        { op: "i32.mul" },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 12 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [{ op: "i32.const", value: 0 }, { op: "return" }],
                },
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "i32.eq" },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [{ op: "i32.const", value: 1 }, { op: "return" }],
                    },
                  ],
                },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "i32.const", value: 0 },
      ];
    },
    5,
  );
  addRuntimeFunc(mod, "__nmap_size", [{ kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    { op: "i32.load", align: 2, offset: 8 },
  ]);
}
function addNumericSetRuntime(mod) {
  const mallocIdx = findFuncIndex(mod, "__malloc");
  addRuntimeFunc(
    mod,
    "__nset_new",
    [{ kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const ptrLocal = firstLocalIdx;
      const iLocal = firstLocalIdx + 1;
      return [
        // 16 + cap * 8
        { op: "i32.const", value: 16 },
        { op: "local.get", index: 0 },
        { op: "i32.const", value: 8 },
        { op: "i32.mul" },
        { op: "i32.add" },
        { op: "call", funcIdx: mallocIdx },
        { op: "local.set", index: ptrLocal },
        { op: "local.get", index: ptrLocal },
        { op: "i32.const", value: 0 },
        { op: "i32.store", align: 2, offset: 8 },
        { op: "local.get", index: ptrLocal },
        { op: "local.get", index: 0 },
        { op: "i32.store", align: 2, offset: 12 },
        // Zero entries
        { op: "i32.const", value: 0 },
        { op: "local.set", index: iLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: iLocal },
                { op: "local.get", index: 0 },
                { op: "i32.ge_u" },
                { op: "br_if", depth: 1 },
                { op: "local.get", index: ptrLocal },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 8 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "i32.const", value: 0 },
                { op: "i32.store", align: 2, offset: 16 },
                { op: "local.get", index: iLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.set", index: iLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "local.get", index: ptrLocal },
      ];
    },
    2,
  );
  addRuntimeFunc(
    mod,
    "__nset_add",
    [{ kind: "i32" }, { kind: "i32" }],
    [],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        { op: "local.get", index: 1 },
        { op: "i32.const", value: 2654435761 | 0 },
        { op: "i32.mul" },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 8 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                // Empty → insert
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: hashLocal },
                    { op: "i32.store", align: 2, offset: 16 },
                    { op: "local.get", index: entryAddrLocal },
                    { op: "local.get", index: 1 },
                    { op: "i32.store", align: 2, offset: 20 },
                    { op: "local.get", index: 0 },
                    { op: "local.get", index: 0 },
                    { op: "i32.load", align: 2, offset: 8 },
                    { op: "i32.const", value: 1 },
                    { op: "i32.add" },
                    { op: "i32.store", align: 2, offset: 8 },
                    { op: "return" },
                  ],
                },
                // Same hash → check key
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "i32.eq" },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [
                        { op: "return" },
                        // already in set
                      ],
                    },
                  ],
                },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
      ];
    },
    5,
  );
  addRuntimeFunc(
    mod,
    "__nset_has",
    [{ kind: "i32" }, { kind: "i32" }],
    [{ kind: "i32" }],
    [],
    (firstLocalIdx) => {
      const hashLocal = firstLocalIdx;
      const capLocal = firstLocalIdx + 1;
      const idxLocal = firstLocalIdx + 2;
      const entryAddrLocal = firstLocalIdx + 3;
      const entryHashLocal = firstLocalIdx + 4;
      return [
        { op: "local.get", index: 1 },
        { op: "i32.const", value: 2654435761 | 0 },
        { op: "i32.mul" },
        { op: "i32.const", value: 1 },
        { op: "i32.or" },
        { op: "local.set", index: hashLocal },
        { op: "local.get", index: 0 },
        { op: "i32.load", align: 2, offset: 12 },
        { op: "local.set", index: capLocal },
        { op: "local.get", index: hashLocal },
        { op: "local.get", index: capLocal },
        { op: "i32.rem_u" },
        { op: "local.set", index: idxLocal },
        {
          op: "block",
          blockType: { kind: "empty" },
          body: [
            {
              op: "loop",
              blockType: { kind: "empty" },
              body: [
                { op: "local.get", index: 0 },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 8 },
                { op: "i32.mul" },
                { op: "i32.add" },
                { op: "local.set", index: entryAddrLocal },
                { op: "local.get", index: entryAddrLocal },
                { op: "i32.load", align: 2, offset: 16 },
                { op: "local.set", index: entryHashLocal },
                { op: "local.get", index: entryHashLocal },
                { op: "i32.eqz" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [{ op: "i32.const", value: 0 }, { op: "return" }],
                },
                { op: "local.get", index: entryHashLocal },
                { op: "local.get", index: hashLocal },
                { op: "i32.eq" },
                {
                  op: "if",
                  blockType: { kind: "empty" },
                  then: [
                    { op: "local.get", index: entryAddrLocal },
                    { op: "i32.load", align: 2, offset: 20 },
                    { op: "local.get", index: 1 },
                    { op: "i32.eq" },
                    {
                      op: "if",
                      blockType: { kind: "empty" },
                      then: [{ op: "i32.const", value: 1 }, { op: "return" }],
                    },
                  ],
                },
                { op: "local.get", index: idxLocal },
                { op: "i32.const", value: 1 },
                { op: "i32.add" },
                { op: "local.get", index: capLocal },
                { op: "i32.rem_u" },
                { op: "local.set", index: idxLocal },
                { op: "br", depth: 0 },
              ],
            },
          ],
        },
        { op: "i32.const", value: 0 },
      ];
    },
    5,
  );
  addRuntimeFunc(mod, "__nset_size", [{ kind: "i32" }], [{ kind: "i32" }], [], () => [
    { op: "local.get", index: 0 },
    { op: "i32.load", align: 2, offset: 8 },
  ]);
}
function findFuncIndex(mod, name) {
  const numImports = mod.imports.filter((i) => i.desc.kind === "func").length;
  for (let i = 0; i < mod.functions.length; i++) {
    if (mod.functions[i].name === name) {
      return numImports + i;
    }
  }
  throw new Error(`Runtime function not found: ${name}`);
}
function addRuntimeFunc(mod, name, params, results, _extraLocalPlaceholders, bodyFn, extraLocalsCount) {
  const typeIdx = mod.types.length;
  mod.types.push({
    kind: "func",
    name: `$type_${name}`,
    params,
    results,
  });
  const numExtraLocals = extraLocalsCount ?? _extraLocalPlaceholders.length;
  const locals = [];
  for (let i = 0; i < numExtraLocals; i++) {
    locals.push({ name: `$l${i}`, type: { kind: "i32" } });
  }
  const firstExtraLocalIdx = params.length;
  const body = bodyFn(firstExtraLocalIdx);
  mod.functions.push({
    name,
    typeIdx,
    locals,
    body,
    exported: false,
  });
}

// src/codegen-linear/index.ts
var CLASS_TYPE_TAG = 5;
var DATA_SEGMENT_BASE = 64;
function generateLinearModule(ast) {
  const mod = createEmptyModule();
  addRuntime(mod);
  addUint8ArrayRuntime(mod);
  addArrayRuntime(mod);
  addStringRuntime(mod);
  addMapRuntime(mod);
  addSetRuntime(mod);
  addNumericMapRuntime(mod);
  addNumericSetRuntime(mod);
  const closureEnvGlobalIdx = mod.globals.length;
  mod.globals.push({
    name: "__closure_env",
    type: { kind: "i32" },
    mutable: true,
    init: [{ op: "i32.const", value: 0 }],
  });
  const ctx = {
    mod,
    checker: ast.checker,
    funcMap: /* @__PURE__ */ new Map(),
    numImportFuncs: 0,
    currentFunc: null,
    errors: [],
    classLayouts: /* @__PURE__ */ new Map(),
    stringLiterals: /* @__PURE__ */ new Map(),
    dataSegmentOffset: DATA_SEGMENT_BASE,
    lambdaCounter: 0,
    tableEntries: [],
    closureEnvGlobalIdx,
    moduleGlobals: /* @__PURE__ */ new Map(),
    moduleCollectionTypes: /* @__PURE__ */ new Map(),
  };
  for (let i = 0; i < mod.functions.length; i++) {
    ctx.funcMap.set(mod.functions[i].name, ctx.numImportFuncs + i);
  }
  const classDecls = [];
  for (const stmt of ast.sourceFile.statements) {
    if (ts16.isClassDeclaration(stmt) && stmt.name) {
      classDecls.push(stmt);
      scanClassDeclaration(ctx, stmt);
    }
  }
  const allFuncEntries = [];
  for (const classDecl of classDecls) {
    const className = classDecl.name.text;
    const layout = ctx.classLayouts.get(className);
    allFuncEntries.push({ kind: "ctor", node: classDecl, name: layout.ctorFuncName, className });
    for (const member of classDecl.members) {
      if (ts16.isMethodDeclaration(member) && member.name && ts16.isIdentifier(member.name)) {
        const methodName = member.name.text;
        const wasmMethodName = `${className}_${methodName}`;
        layout.methods.set(methodName, wasmMethodName);
        allFuncEntries.push({ kind: "method", node: member, name: wasmMethodName, className });
      }
      if (ts16.isGetAccessorDeclaration(member) && member.name && ts16.isIdentifier(member.name)) {
        const getterName = member.name.text;
        const wasmGetterName = `${className}_get_${getterName}`;
        layout.getters.set(getterName, wasmGetterName);
        allFuncEntries.push({ kind: "method", node: member, name: wasmGetterName, className });
      }
    }
  }
  const funcDecls = [];
  for (const stmt of ast.sourceFile.statements) {
    if (ts16.isFunctionDeclaration(stmt) && stmt.name) {
      funcDecls.push(stmt);
      allFuncEntries.push({ kind: "func", node: stmt, name: stmt.name.text });
    }
  }
  const runtimeFuncCount = ctx.mod.functions.length;
  for (let i = 0; i < allFuncEntries.length; i++) {
    const entry = allFuncEntries[i];
    const funcIdx = ctx.numImportFuncs + runtimeFuncCount + i;
    ctx.funcMap.set(entry.name, funcIdx);
  }
  collectModuleGlobals(ctx, ast.sourceFile);
  for (const classDecl of classDecls) {
    compileClassDeclaration(ctx, classDecl);
  }
  for (const decl of funcDecls) {
    compileFunction(ctx, decl);
  }
  if (ctx.stringLiterals.size > 0) {
    const totalSize = ctx.dataSegmentOffset - DATA_SEGMENT_BASE;
    const bytes = new Uint8Array(totalSize);
    for (const [str, offset] of ctx.stringLiterals) {
      const encoded = new TextEncoder().encode(str);
      bytes.set(encoded, offset - DATA_SEGMENT_BASE);
    }
    mod.dataSegments.push({ offset: DATA_SEGMENT_BASE, bytes });
  }
  emitClosureTable(ctx);
  return mod;
}
function compileFunction(ctx, decl) {
  const name = decl.name.text;
  const isExported = decl.modifiers?.some((m) => m.kind === ts16.SyntaxKind.ExportKeyword) ?? false;
  const params = [];
  for (const p of decl.parameters) {
    const paramName = ts16.isIdentifier(p.name) ? p.name.text : "_";
    const type = resolveParamTypeFromChecker(ctx, p);
    params.push({ name: paramName, type });
  }
  const returnType = resolveType(ctx, decl.type);
  const isVoid = returnType === null;
  const paramTypes = params.map((p) => p.type);
  const resultTypes = isVoid ? [] : [returnType];
  const typeIdx = ctx.mod.types.length;
  const funcTypeDef = {
    kind: "func",
    name: `$type_${name}`,
    params: paramTypes,
    results: resultTypes,
  };
  ctx.mod.types.push(funcTypeDef);
  const fctx = {
    name,
    params,
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: isVoid ? null : returnType,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    collectionTypes: /* @__PURE__ */ new Map(),
    callbackParams: /* @__PURE__ */ new Map(),
  };
  for (let i = 0; i < params.length; i++) {
    fctx.localMap.set(params[i].name, i);
  }
  ctx.currentFunc = fctx;
  detectCallbackParams(ctx, fctx, decl.parameters);
  detectParamCollectionTypes(ctx, fctx, decl.parameters);
  const funcIdx = ctx.funcMap.get(name);
  if (decl.body) {
    for (const stmt of decl.body.statements) {
      compileStatement2(ctx, fctx, stmt);
    }
  }
  if (!isVoid) {
    fctx.body.push({ op: "unreachable" });
  }
  ctx.mod.functions.push({
    name,
    typeIdx,
    locals: fctx.locals,
    body: fctx.body,
    exported: isExported,
  });
  if (isExported) {
    ctx.mod.exports.push({
      name,
      desc: { kind: "func", index: funcIdx },
    });
  }
  ctx.currentFunc = null;
}
function compileStatement2(ctx, fctx, stmt) {
  if (ts16.isReturnStatement(stmt)) {
    if (stmt.expression) {
      compileExpression3(ctx, fctx, stmt.expression);
    }
    fctx.body.push({ op: "return" });
  } else if (ts16.isVariableStatement(stmt)) {
    for (const decl of stmt.declarationList.declarations) {
      if (ts16.isIdentifier(decl.name)) {
        const varName = decl.name.text;
        const collKind = detectCollectionKind(ctx, decl);
        let type = { kind: "f64" };
        if (collKind) {
          type = { kind: "i32" };
          fctx.collectionTypes.set(varName, collKind);
        } else if (decl.type) {
          const resolved = resolveType(ctx, decl.type);
          if (resolved) type = resolved;
        } else if (decl.initializer) {
          type = inferExprType(ctx, fctx, decl.initializer);
        }
        const localIdx = addLocal(fctx, varName, type);
        if (decl.initializer) {
          compileExpression3(ctx, fctx, decl.initializer);
          fctx.body.push({ op: "local.set", index: localIdx });
        }
      } else if (ts16.isArrayBindingPattern(decl.name) && decl.initializer) {
        compileArrayDestructuring2(ctx, fctx, decl.name, decl.initializer);
      } else if (ts16.isObjectBindingPattern(decl.name) && decl.initializer) {
        compileObjectDestructuring2(ctx, fctx, decl.name, decl.initializer);
      }
    }
  } else if (ts16.isIfStatement(stmt)) {
    compileExpression3(ctx, fctx, stmt.expression);
    emitTruthyCoercion(fctx, inferExprType(ctx, fctx, stmt.expression));
    const thenBody = [];
    const savedBody = fctx.body;
    fctx.body = thenBody;
    fctx.blockDepth++;
    compileStatement2(ctx, fctx, stmt.thenStatement);
    fctx.blockDepth--;
    let elseBody;
    if (stmt.elseStatement) {
      elseBody = [];
      fctx.body = elseBody;
      fctx.blockDepth++;
      compileStatement2(ctx, fctx, stmt.elseStatement);
      fctx.blockDepth--;
    }
    fctx.body = savedBody;
    const blockType = { kind: "empty" };
    fctx.body.push({
      op: "if",
      blockType,
      then: thenBody,
      ...(elseBody ? { else: elseBody } : {}),
    });
  } else if (ts16.isBlock(stmt)) {
    for (const s of stmt.statements) {
      compileStatement2(ctx, fctx, s);
    }
  } else if (ts16.isWhileStatement(stmt)) {
    const loopBody = [];
    const savedBody = fctx.body;
    fctx.body = loopBody;
    compileExpression3(ctx, fctx, stmt.expression);
    emitTruthyCoercion(fctx, inferExprType(ctx, fctx, stmt.expression));
    fctx.body.push({ op: "i32.eqz" });
    fctx.body.push({ op: "br_if", depth: 1 });
    fctx.breakStack.push(fctx.blockDepth);
    fctx.continueStack.push(fctx.blockDepth + 1);
    fctx.blockDepth += 2;
    compileStatement2(ctx, fctx, stmt.statement);
    fctx.blockDepth -= 2;
    fctx.breakStack.pop();
    fctx.continueStack.pop();
    fctx.body.push({ op: "br", depth: 0 });
    fctx.body = savedBody;
    fctx.body.push({
      op: "block",
      blockType: { kind: "empty" },
      body: [
        {
          op: "loop",
          blockType: { kind: "empty" },
          body: loopBody,
        },
      ],
    });
  } else if (ts16.isForStatement(stmt)) {
    if (stmt.initializer) {
      if (ts16.isVariableDeclarationList(stmt.initializer)) {
        compileStatement2(ctx, fctx, ts16.factory.createVariableStatement(void 0, stmt.initializer));
      } else {
        compileExpression3(ctx, fctx, stmt.initializer);
        fctx.body.push({ op: "drop" });
      }
    }
    const loopBody = [];
    const savedBody = fctx.body;
    fctx.body = loopBody;
    if (stmt.condition) {
      compileExpression3(ctx, fctx, stmt.condition);
      emitTruthyCoercion(fctx, inferExprType(ctx, fctx, stmt.condition));
      fctx.body.push({ op: "i32.eqz" });
      fctx.body.push({ op: "br_if", depth: 1 });
    }
    fctx.breakStack.push(fctx.blockDepth);
    fctx.continueStack.push(fctx.blockDepth + 1);
    fctx.blockDepth += 2;
    compileStatement2(ctx, fctx, stmt.statement);
    fctx.blockDepth -= 2;
    fctx.breakStack.pop();
    fctx.continueStack.pop();
    if (stmt.incrementor) {
      compileExpression3(ctx, fctx, stmt.incrementor);
      fctx.body.push({ op: "drop" });
    }
    fctx.body.push({ op: "br", depth: 0 });
    fctx.body = savedBody;
    fctx.body.push({
      op: "block",
      blockType: { kind: "empty" },
      body: [
        {
          op: "loop",
          blockType: { kind: "empty" },
          body: loopBody,
        },
      ],
    });
  } else if (ts16.isForOfStatement(stmt)) {
    compileForOfStatement2(ctx, fctx, stmt);
  } else if (ts16.isDoStatement(stmt)) {
    compileDoWhileStatement2(ctx, fctx, stmt);
  } else if (ts16.isSwitchStatement(stmt)) {
    compileSwitchStatement2(ctx, fctx, stmt);
  } else if (ts16.isTryStatement(stmt)) {
    for (const s of stmt.tryBlock.statements) {
      compileStatement2(ctx, fctx, s);
    }
  } else if (ts16.isExpressionStatement(stmt)) {
    compileExpression3(ctx, fctx, stmt.expression);
    if (!isVoidExpression(ctx, stmt.expression)) {
      fctx.body.push({ op: "drop" });
    }
  } else if (ts16.isThrowStatement(stmt)) {
    fctx.body.push({ op: "unreachable" });
  }
}
function compileForOfStatement2(ctx, fctx, stmt) {
  compileExpression3(ctx, fctx, stmt.expression);
  const arrLocal = addLocal(fctx, `__forof_arr_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.set", index: arrLocal });
  const idxLocal = addLocal(fctx, `__forof_idx_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: idxLocal });
  const iterKind = getExprCollectionKind(ctx, fctx, stmt.expression);
  const lenLocal = addLocal(fctx, `__forof_len_${fctx.locals.length}`, { kind: "i32" });
  if (iterKind === "ArrayOrUint8Array") {
    const arrLenIdx = ctx.funcMap.get("__arr_len");
    const u8LenIdx = ctx.funcMap.get("__u8arr_len");
    fctx.body.push({ op: "local.get", index: arrLocal });
    fctx.body.push({ op: "i32.load8_u", align: 0, offset: 0 });
    fctx.body.push({ op: "i32.const", value: 2 });
    fctx.body.push({ op: "i32.eq" });
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "i32" } },
      then: [
        { op: "local.get", index: arrLocal },
        { op: "call", funcIdx: u8LenIdx },
      ],
      else: [
        { op: "local.get", index: arrLocal },
        { op: "call", funcIdx: arrLenIdx },
      ],
    });
  } else {
    const lenFuncName = iterKind === "Uint8Array" ? "__u8arr_len" : "__arr_len";
    const arrLenIdx = ctx.funcMap.get(lenFuncName);
    fctx.body.push({ op: "local.get", index: arrLocal });
    fctx.body.push({ op: "call", funcIdx: arrLenIdx });
  }
  fctx.body.push({ op: "local.set", index: lenLocal });
  const initDecl = stmt.initializer;
  let loopVarName = null;
  let destructuredNames = null;
  if (ts16.isVariableDeclarationList(initDecl)) {
    const decl = initDecl.declarations[0];
    if (ts16.isIdentifier(decl.name)) {
      loopVarName = decl.name.text;
    } else if (ts16.isArrayBindingPattern(decl.name)) {
      destructuredNames = [];
      for (const el of decl.name.elements) {
        if (ts16.isBindingElement(el) && ts16.isIdentifier(el.name)) {
          destructuredNames.push(el.name.text);
        }
      }
    }
  }
  if (destructuredNames && iterKind === "Map") {
    compileForOfMap(ctx, fctx, stmt, arrLocal, destructuredNames);
    return;
  }
  let elementIsI32 = false;
  if (loopVarName) {
    try {
      const iterType = ctx.checker.getTypeAtLocation(stmt.expression);
      const iterTypeStr = ctx.checker.typeToString(iterType);
      if (iterTypeStr.endsWith("[]") && !iterTypeStr.startsWith("number") && !iterTypeStr.startsWith("boolean")) {
        elementIsI32 = true;
      }
    } catch {}
  }
  let loopVarIdx;
  if (loopVarName) {
    loopVarIdx = addLocal(fctx, loopVarName, elementIsI32 ? { kind: "i32" } : { kind: "f64" });
  }
  const loopBody = [];
  const savedBody = fctx.body;
  fctx.body = loopBody;
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "local.get", index: lenLocal });
  fctx.body.push({ op: "i32.ge_s" });
  fctx.body.push({ op: "br_if", depth: 1 });
  if (loopVarIdx !== void 0) {
    if (iterKind === "ArrayOrUint8Array") {
      const arrGetIdx = ctx.funcMap.get("__arr_get");
      const u8GetIdx = ctx.funcMap.get("__u8arr_get");
      fctx.body.push({ op: "local.get", index: arrLocal });
      fctx.body.push({ op: "i32.load8_u", align: 0, offset: 0 });
      fctx.body.push({ op: "i32.const", value: 2 });
      fctx.body.push({ op: "i32.eq" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [
          { op: "local.get", index: arrLocal },
          { op: "local.get", index: idxLocal },
          { op: "call", funcIdx: u8GetIdx },
        ],
        else: [
          { op: "local.get", index: arrLocal },
          { op: "local.get", index: idxLocal },
          { op: "call", funcIdx: arrGetIdx },
        ],
      });
    } else {
      const getFuncName = iterKind === "Uint8Array" ? "__u8arr_get" : "__arr_get";
      const arrGetIdx = ctx.funcMap.get(getFuncName);
      fctx.body.push({ op: "local.get", index: arrLocal });
      fctx.body.push({ op: "local.get", index: idxLocal });
      fctx.body.push({ op: "call", funcIdx: arrGetIdx });
    }
    if (!elementIsI32) {
      fctx.body.push({ op: "f64.convert_i32_s" });
    }
    fctx.body.push({ op: "local.set", index: loopVarIdx });
  }
  fctx.breakStack.push(fctx.blockDepth);
  fctx.continueStack.push(fctx.blockDepth + 1);
  fctx.blockDepth += 2;
  compileStatement2(ctx, fctx, stmt.statement);
  fctx.blockDepth -= 2;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: idxLocal });
  fctx.body.push({ op: "br", depth: 0 });
  fctx.body = savedBody;
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
}
function compileForOfMap(ctx, fctx, stmt, mapLocal, destructuredNames) {
  const idxLocal = addLocal(fctx, `__forof_map_idx_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: idxLocal });
  const capLocal = addLocal(fctx, `__forof_map_cap_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.get", index: mapLocal });
  fctx.body.push({ op: "i32.load", align: 2, offset: 12 });
  fctx.body.push({ op: "local.set", index: capLocal });
  let keyIsI32 = false;
  let valIsI32 = false;
  try {
    const mapType = ctx.checker.getTypeAtLocation(stmt.expression);
    const mapStr = ctx.checker.typeToString(ctx.checker.getNonNullableType(mapType));
    const match = mapStr.match(/^Map<(.+),\s*(.+)>$/);
    if (match) {
      const keyStr = match[1].trim();
      const valStr = match[2].trim();
      if (keyStr !== "number" && keyStr !== "boolean") keyIsI32 = true;
      if (valStr !== "number" && valStr !== "boolean") valIsI32 = true;
    } else {
      keyIsI32 = true;
      valIsI32 = true;
    }
  } catch {}
  const keyVarIdx =
    destructuredNames.length > 0
      ? addLocal(fctx, destructuredNames[0], keyIsI32 ? { kind: "i32" } : { kind: "f64" })
      : void 0;
  const valVarIdx =
    destructuredNames.length > 1
      ? addLocal(fctx, destructuredNames[1], valIsI32 ? { kind: "i32" } : { kind: "f64" })
      : void 0;
  try {
    const mapType = ctx.checker.getTypeAtLocation(stmt.expression);
    const mapStr = ctx.checker.typeToString(ctx.checker.getNonNullableType(mapType));
    const match = mapStr.match(/^Map<(.+),\s*(.+)>$/);
    if (match) {
      const valStr = match[2].trim();
      if (valStr.endsWith("[]") || valStr.startsWith("Array<")) {
        if (destructuredNames.length > 1) {
          fctx.collectionTypes.set(destructuredNames[1], "Array");
        }
      } else if (valStr.startsWith("Map<") || valStr === "Map") {
        if (destructuredNames.length > 1) {
          fctx.collectionTypes.set(destructuredNames[1], "Map");
        }
      } else if (valStr.startsWith("Set<") || valStr === "Set") {
        if (destructuredNames.length > 1) {
          fctx.collectionTypes.set(destructuredNames[1], "Set");
        }
      }
    }
  } catch {}
  const loopBody = [];
  const savedBody = fctx.body;
  fctx.body = loopBody;
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "local.get", index: capLocal });
  fctx.body.push({ op: "i32.ge_s" });
  fctx.body.push({ op: "br_if", depth: 1 });
  const entryAddrLocal = addLocal(fctx, `__forof_entry_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.get", index: mapLocal });
  fctx.body.push({ op: "i32.const", value: 16 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "i32.const", value: 12 });
  fctx.body.push({ op: "i32.mul" });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: entryAddrLocal });
  fctx.body.push({ op: "local.get", index: entryAddrLocal });
  fctx.body.push({ op: "i32.load", align: 2, offset: 0 });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "i32.ne" });
  const thenBody = [];
  const savedBody2 = fctx.body;
  fctx.body = thenBody;
  if (keyVarIdx !== void 0) {
    fctx.body.push({ op: "local.get", index: entryAddrLocal });
    fctx.body.push({ op: "i32.load", align: 2, offset: 4 });
    if (!keyIsI32) fctx.body.push({ op: "f64.convert_i32_s" });
    fctx.body.push({ op: "local.set", index: keyVarIdx });
  }
  if (valVarIdx !== void 0) {
    fctx.body.push({ op: "local.get", index: entryAddrLocal });
    fctx.body.push({ op: "i32.load", align: 2, offset: 8 });
    if (!valIsI32) fctx.body.push({ op: "f64.convert_i32_s" });
    fctx.body.push({ op: "local.set", index: valVarIdx });
  }
  fctx.breakStack.push(fctx.blockDepth);
  fctx.continueStack.push(fctx.blockDepth + 1);
  fctx.blockDepth += 2;
  compileStatement2(ctx, fctx, stmt.statement);
  fctx.blockDepth -= 2;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  fctx.body = savedBody2;
  fctx.body.push({
    op: "if",
    blockType: { kind: "empty" },
    then: thenBody,
  });
  fctx.body.push({ op: "local.get", index: idxLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: idxLocal });
  fctx.body.push({ op: "br", depth: 0 });
  fctx.body = savedBody;
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
}
function compileDoWhileStatement2(ctx, fctx, stmt) {
  const loopBody = [];
  const savedBody = fctx.body;
  fctx.body = loopBody;
  fctx.breakStack.push(fctx.blockDepth);
  fctx.continueStack.push(fctx.blockDepth + 1);
  fctx.blockDepth += 2;
  compileStatement2(ctx, fctx, stmt.statement);
  fctx.blockDepth -= 2;
  fctx.breakStack.pop();
  fctx.continueStack.pop();
  compileExpression3(ctx, fctx, stmt.expression);
  emitTruthyCoercion(fctx, inferExprType(ctx, fctx, stmt.expression));
  fctx.body.push({ op: "br_if", depth: 0 });
  fctx.body = savedBody;
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
}
function compileSwitchStatement2(ctx, fctx, stmt) {
  compileExpression3(ctx, fctx, stmt.expression);
  const switchExprType = inferExprType(ctx, fctx, stmt.expression);
  const switchLocal = addLocal(fctx, `__switch_${fctx.locals.length}`, switchExprType);
  fctx.body.push({ op: "local.set", index: switchLocal });
  let defaultClause = null;
  let matchedLocal;
  for (const c of stmt.caseBlock.clauses) {
    if (ts16.isDefaultClause(c)) {
      matchedLocal = addLocal(fctx, `__switch_matched_${fctx.locals.length}`, { kind: "i32" });
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "local.set", index: matchedLocal });
      break;
    }
  }
  const clauseArr = Array.from(stmt.caseBlock.clauses);
  let i = 0;
  while (i < clauseArr.length) {
    const clause = clauseArr[i];
    if (ts16.isDefaultClause(clause)) {
      defaultClause = clause;
      i++;
      continue;
    }
    const caseExprs = [clause.expression];
    let bodyClause = clause;
    while (bodyClause.statements.length === 0 && i + 1 < clauseArr.length) {
      i++;
      const next = clauseArr[i];
      if (ts16.isDefaultClause(next)) {
        defaultClause = next;
        break;
      }
      caseExprs.push(next.expression);
      bodyClause = next;
    }
    for (let j = 0; j < caseExprs.length; j++) {
      fctx.body.push({ op: "local.get", index: switchLocal });
      compileExpression3(ctx, fctx, caseExprs[j]);
      if (switchExprType.kind === "f64") {
        fctx.body.push({ op: "f64.eq" });
      } else {
        fctx.body.push({ op: "i32.eq" });
      }
      if (j > 0) {
        fctx.body.push({ op: "i32.or" });
      }
    }
    const thenBody = [];
    const savedBody = fctx.body;
    fctx.body = thenBody;
    if (matchedLocal !== void 0) {
      fctx.body.push({ op: "i32.const", value: 1 });
      fctx.body.push({ op: "local.set", index: matchedLocal });
    }
    for (const s of bodyClause.statements) {
      compileStatement2(ctx, fctx, s);
    }
    fctx.body = savedBody;
    fctx.body.push({
      op: "if",
      blockType: { kind: "empty" },
      then: thenBody,
    });
    i++;
  }
  if (defaultClause) {
    if (matchedLocal !== void 0) {
      fctx.body.push({ op: "local.get", index: matchedLocal });
      fctx.body.push({ op: "i32.eqz" });
      const defaultBody = [];
      const savedBody = fctx.body;
      fctx.body = defaultBody;
      for (const s of defaultClause.statements) {
        compileStatement2(ctx, fctx, s);
      }
      fctx.body = savedBody;
      fctx.body.push({
        op: "if",
        blockType: { kind: "empty" },
        then: defaultBody,
      });
    } else {
      for (const s of defaultClause.statements) {
        compileStatement2(ctx, fctx, s);
      }
    }
  }
}
function compileExpression3(ctx, fctx, expr) {
  if (ts16.isNumericLiteral(expr)) {
    fctx.body.push({ op: "f64.const", value: Number(expr.text) });
  } else if (ts16.isStringLiteral(expr) || ts16.isNoSubstitutionTemplateLiteral(expr)) {
    compileStringLiteral2(ctx, fctx, expr.text);
  } else if (expr.kind === ts16.SyntaxKind.ThisKeyword) {
    fctx.body.push({ op: "local.get", index: 0 });
  } else if (expr.kind === ts16.SyntaxKind.TrueKeyword) {
    fctx.body.push({ op: "f64.const", value: 1 });
  } else if (expr.kind === ts16.SyntaxKind.FalseKeyword) {
    fctx.body.push({ op: "f64.const", value: 0 });
  } else if (expr.kind === ts16.SyntaxKind.NullKeyword) {
    fctx.body.push({ op: "i32.const", value: 0 });
  } else if (ts16.isBigIntLiteral(expr)) {
    const text = expr.text.replace(/n$/, "");
    fctx.body.push({ op: "f64.const", value: Number(text) });
  } else if (ts16.isBinaryExpression(expr)) {
    compileBinaryExpression2(ctx, fctx, expr);
  } else if (ts16.isParenthesizedExpression(expr)) {
    compileExpression3(ctx, fctx, expr.expression);
  } else if (ts16.isIdentifier(expr)) {
    const name = expr.text;
    if (name === "undefined") {
      fctx.body.push({ op: "i32.const", value: 0 });
    } else if (name === "Infinity") {
      fctx.body.push({ op: "f64.const", value: Infinity });
    } else if (name === "NaN") {
      fctx.body.push({ op: "f64.const", value: NaN });
    } else {
      const localIdx = fctx.localMap.get(name);
      if (localIdx !== void 0) {
        fctx.body.push({ op: "local.get", index: localIdx });
      } else {
        const globalIdx = ctx.moduleGlobals.get(name);
        if (globalIdx !== void 0) {
          fctx.body.push({ op: "global.get", index: globalIdx });
        } else {
          ctx.errors.push({
            message: `Unknown identifier: ${name}`,
            line: 0,
            column: 0,
          });
        }
      }
    }
  } else if (ts16.isPrefixUnaryExpression(expr)) {
    if (expr.operator === ts16.SyntaxKind.MinusToken) {
      compileExpression3(ctx, fctx, expr.operand);
      fctx.body.push({ op: "f64.neg" });
    } else if (expr.operator === ts16.SyntaxKind.PlusToken) {
      compileExpression3(ctx, fctx, expr.operand);
    } else if (expr.operator === ts16.SyntaxKind.ExclamationToken) {
      compileExpression3(ctx, fctx, expr.operand);
      emitTruthyCoercion(fctx, inferExprType(ctx, fctx, expr.operand));
      fctx.body.push({ op: "i32.eqz" });
      fctx.body.push({ op: "f64.convert_i32_s" });
    } else if (expr.operator === ts16.SyntaxKind.TildeToken) {
      compileExprToI32(ctx, fctx, expr.operand);
      fctx.body.push({ op: "i32.const", value: -1 });
      fctx.body.push({ op: "i32.xor" });
      fctx.body.push({ op: "f64.convert_i32_s" });
    } else if (expr.operator === ts16.SyntaxKind.PlusPlusToken) {
      if (ts16.isIdentifier(expr.operand)) {
        const idx = fctx.localMap.get(expr.operand.text);
        if (idx !== void 0) {
          fctx.body.push({ op: "local.get", index: idx });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: "f64.add" });
          fctx.body.push({ op: "local.tee", index: idx });
        }
      }
    } else if (expr.operator === ts16.SyntaxKind.MinusMinusToken) {
      if (ts16.isIdentifier(expr.operand)) {
        const idx = fctx.localMap.get(expr.operand.text);
        if (idx !== void 0) {
          fctx.body.push({ op: "local.get", index: idx });
          fctx.body.push({ op: "f64.const", value: 1 });
          fctx.body.push({ op: "f64.sub" });
          fctx.body.push({ op: "local.tee", index: idx });
        }
      }
    }
  } else if (ts16.isPostfixUnaryExpression(expr)) {
    if (ts16.isIdentifier(expr.operand)) {
      const idx = fctx.localMap.get(expr.operand.text);
      if (idx !== void 0) {
        fctx.body.push({ op: "local.get", index: idx });
        fctx.body.push({ op: "local.get", index: idx });
        fctx.body.push({ op: "f64.const", value: 1 });
        if (expr.operator === ts16.SyntaxKind.PlusPlusToken) {
          fctx.body.push({ op: "f64.add" });
        } else {
          fctx.body.push({ op: "f64.sub" });
        }
        fctx.body.push({ op: "local.set", index: idx });
      }
    } else if (
      ts16.isPropertyAccessExpression(expr.operand) &&
      expr.operand.expression.kind === ts16.SyntaxKind.ThisKeyword
    ) {
      const propName = expr.operand.name.text;
      const className = inferClassName(ctx, fctx, expr.operand.expression);
      if (className) {
        const layout = ctx.classLayouts.get(className);
        if (layout) {
          const field = layout.fields.get(propName);
          if (field) {
            const tempLocal = addLocal(fctx, `$postfix_${propName}`, { kind: field.type });
            const thisIdx = fctx.localMap.get("this") ?? 0;
            fctx.body.push({ op: "local.get", index: thisIdx });
            if (field.type === "f64") {
              fctx.body.push({ op: "f64.load", align: 3, offset: field.offset });
            } else {
              fctx.body.push({ op: "i32.load", align: 2, offset: field.offset });
            }
            fctx.body.push({ op: "local.set", index: tempLocal });
            fctx.body.push({ op: "local.get", index: thisIdx });
            fctx.body.push({ op: "local.get", index: tempLocal });
            if (field.type === "f64") {
              fctx.body.push({ op: "f64.const", value: expr.operator === ts16.SyntaxKind.PlusPlusToken ? 1 : -1 });
              fctx.body.push({ op: "f64.add" });
              fctx.body.push({ op: "f64.store", align: 3, offset: field.offset });
            } else {
              fctx.body.push({ op: "i32.const", value: expr.operator === ts16.SyntaxKind.PlusPlusToken ? 1 : -1 });
              fctx.body.push({ op: "i32.add" });
              fctx.body.push({ op: "i32.store", align: 2, offset: field.offset });
            }
            fctx.body.push({ op: "local.get", index: tempLocal });
          }
        }
      }
    }
  } else if (ts16.isArrayLiteralExpression(expr)) {
    compileArrayLiteral2(ctx, fctx, expr);
  } else if (ts16.isNewExpression(expr)) {
    compileNewExpression2(ctx, fctx, expr);
  } else if (ts16.isPropertyAccessExpression(expr)) {
    compilePropertyAccess2(ctx, fctx, expr);
  } else if (ts16.isElementAccessExpression(expr)) {
    compileElementAccess2(ctx, fctx, expr);
  } else if (ts16.isCallExpression(expr)) {
    if (ts16.isPropertyAccessExpression(expr.expression)) {
      compileMethodCall(ctx, fctx, expr);
    } else if (ts16.isIdentifier(expr.expression)) {
      const funcName = expr.expression.text;
      if (funcName === "Number" || funcName === "Boolean") {
        if (expr.arguments.length > 0) {
          compileExpression3(ctx, fctx, expr.arguments[0]);
        } else {
          fctx.body.push({ op: "f64.const", value: 0 });
        }
      } else if (funcName === "String") {
        if (expr.arguments.length > 0) {
          compileExpression3(ctx, fctx, expr.arguments[0]);
        } else {
          compileStringLiteral2(ctx, fctx, "");
        }
      } else {
        const cbTypeIdx = fctx.callbackParams.get(funcName);
        if (cbTypeIdx !== void 0) {
          for (const arg of expr.arguments) {
            compileCallArg(ctx, fctx, arg);
          }
          const localIdx = fctx.localMap.get(funcName);
          fctx.body.push({ op: "local.get", index: localIdx });
          fctx.body.push({ op: "call_indirect", typeIdx: cbTypeIdx, tableIdx: 0 });
        } else {
          const funcIdx = ctx.funcMap.get(funcName);
          if (funcIdx !== void 0) {
            for (const arg of expr.arguments) {
              compileCallArg(ctx, fctx, arg);
            }
            emitDefaultArgs(ctx, fctx, funcName, expr.arguments.length);
            fctx.body.push({ op: "call", funcIdx });
          } else {
            ctx.errors.push({
              message: `Unknown function: ${funcName}`,
              line: 0,
              column: 0,
            });
          }
        }
      }
    }
  } else if (ts16.isNonNullExpression(expr)) {
    compileExpression3(ctx, fctx, expr.expression);
  } else if (ts16.isTemplateExpression(expr)) {
    compileTemplateExpression2(ctx, fctx, expr);
  } else if (ts16.isConditionalExpression(expr)) {
    compileExpression3(ctx, fctx, expr.condition);
    emitTruthyCoercion(fctx, inferExprType(ctx, fctx, expr.condition));
    const resultType = inferExprType(ctx, fctx, expr.whenTrue);
    const thenBody = [];
    const elseBody = [];
    const savedBody = fctx.body;
    fctx.body = thenBody;
    compileExpression3(ctx, fctx, expr.whenTrue);
    fctx.body = elseBody;
    compileExpression3(ctx, fctx, expr.whenFalse);
    const elseType = inferExprType(ctx, fctx, expr.whenFalse);
    if (resultType.kind === "f64" && elseType.kind === "i32") {
      fctx.body.push({ op: "f64.convert_i32_s" });
    } else if (resultType.kind === "i32" && elseType.kind === "f64") {
      fctx.body.push({ op: "i32.trunc_f64_s" });
    }
    fctx.body = savedBody;
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: resultType },
      then: thenBody,
      else: elseBody,
    });
  } else if (ts16.isObjectLiteralExpression(expr)) {
    compileObjectLiteral2(ctx, fctx, expr);
  }
}
function isCallVoid(ctx, expr) {
  try {
    const callType = ctx.checker.getTypeAtLocation(expr);
    const typeStr = ctx.checker.typeToString(callType);
    if (typeStr === "void") return true;
  } catch {}
  if (ts16.isIdentifier(expr.expression)) {
    const funcName = expr.expression.text;
    const wasmFunc = ctx.mod.functions.find((f) => f.name === funcName);
    if (wasmFunc) {
      const funcType = ctx.mod.types[wasmFunc.typeIdx];
      if (funcType && funcType.kind === "func" && funcType.results.length === 0) return true;
    }
  }
  return false;
}
function isVoidExpression(ctx, expr) {
  if (!ts16.isCallExpression(expr)) return false;
  if (ts16.isPropertyAccessExpression(expr.expression)) {
    try {
      const callType = ctx.checker.getTypeAtLocation(expr);
      const typeStr = ctx.checker.typeToString(callType);
      if (typeStr === "void") return true;
    } catch {}
    return false;
  }
  return isCallVoid(ctx, expr);
}
function emitDefaultArgs(ctx, fctx, funcName, providedArgCount) {
  const wasmFunc = ctx.mod.functions.find((f) => f.name === funcName);
  if (!wasmFunc) return;
  const funcType = ctx.mod.types[wasmFunc.typeIdx];
  if (!funcType || funcType.kind !== "func") return;
  const expectedArgCount = funcType.params.length;
  for (let i = providedArgCount; i < expectedArgCount; i++) {
    const paramType = funcType.params[i];
    if (paramType.kind === "i32") {
      fctx.body.push({ op: "i32.const", value: 0 });
    } else {
      fctx.body.push({ op: "f64.const", value: 0 });
    }
  }
}
function classifyFieldType(typeStr, baseType, collKinds, propName) {
  if (typeStr.endsWith("[]") || typeStr.startsWith("Array<")) {
    collKinds.set(propName, "Array");
    return "i32";
  }
  if (typeStr === "Uint8Array" || typeStr.includes("Uint8Array")) {
    collKinds.set(propName, "Uint8Array");
    return "i32";
  }
  if (typeStr.startsWith("Map<") || typeStr === "Map") {
    collKinds.set(propName, "Map");
    return "i32";
  }
  if (typeStr.startsWith("Set<") || typeStr === "Set") {
    collKinds.set(propName, "Set");
    return "i32";
  }
  if (typeStr === "string") return "i32";
  if (typeStr === "number") return "f64";
  if (typeStr === "boolean") return "f64";
  if (/^\d+(\s*\|\s*\d+)*$/.test(typeStr)) return "f64";
  if (baseType.getFlags() & ts16.TypeFlags.NumberLike) return "f64";
  if (baseType.getFlags() & ts16.TypeFlags.BooleanLike) return "f64";
  return "i32";
}
function compileObjectLiteral2(ctx, fctx, expr) {
  let typeName;
  try {
    const ctxType = ctx.checker.getContextualType(expr);
    if (ctxType) {
      const sym = ctxType.getSymbol() ?? ctxType.aliasSymbol;
      if (sym) {
        const name = sym.getName();
        if (name && name !== "__object" && name !== "__type") typeName = name;
      }
    }
    if (!typeName && expr.parent && ts16.isVariableDeclaration(expr.parent) && expr.parent.type) {
      const annotText = expr.parent.type.getText();
      if (annotText && !annotText.includes("{")) typeName = annotText;
    }
    if (!typeName) {
      const type = ctx.checker.getTypeAtLocation(expr);
      const sym = type.getSymbol() ?? type.aliasSymbol;
      if (sym) {
        const name = sym.getName();
        if (name && name !== "__object" && name !== "__type") typeName = name;
      }
    }
  } catch {}
  const propDefs = [];
  const collKinds = /* @__PURE__ */ new Map();
  let usedInterfaceFields = false;
  try {
    const ctxType = ctx.checker.getContextualType(expr) ?? ctx.checker.getTypeAtLocation(expr);
    if (ctxType) {
      const props = ctxType.getProperties();
      if (props && props.length > 0) {
        usedInterfaceFields = true;
        for (const prop of props) {
          const propName = prop.getName();
          const rawType = ctx.checker.getTypeOfSymbolAtLocation(prop, expr);
          const baseType = ctx.checker.getNonNullableType(rawType);
          const typeStr = ctx.checker.typeToString(baseType);
          const fieldType = classifyFieldType(typeStr, baseType, collKinds, propName);
          propDefs.push({ name: propName, type: fieldType });
        }
      }
    }
  } catch {}
  if (!usedInterfaceFields) {
    for (const prop of expr.properties) {
      if (ts16.isPropertyAssignment(prop) && ts16.isIdentifier(prop.name)) {
        const propName = prop.name.text;
        const exprType = inferExprType(ctx, fctx, prop.initializer);
        propDefs.push({ name: propName, type: exprType.kind === "i32" ? "i32" : "f64" });
      } else if (ts16.isShorthandPropertyAssignment(prop)) {
        const propName = prop.name.text;
        const exprType = inferExprType(ctx, fctx, prop.name);
        propDefs.push({ name: propName, type: exprType.kind === "i32" ? "i32" : "f64" });
      }
    }
  }
  if (typeName && !ctx.classLayouts.has(typeName)) {
    const layout2 = computeClassLayout(typeName, propDefs);
    for (const [k, v] of collKinds) layout2.fieldCollectionKinds.set(k, v);
    ctx.classLayouts.set(typeName, layout2);
  }
  if (!typeName && propDefs.length > 0) {
    typeName = `__anon_${ctx.lambdaCounter++}`;
    const anonLayout = computeClassLayout(typeName, propDefs);
    for (const [k, v] of collKinds) anonLayout.fieldCollectionKinds.set(k, v);
    ctx.classLayouts.set(typeName, anonLayout);
  }
  const layout = typeName ? ctx.classLayouts.get(typeName) : void 0;
  const HEADER_SIZE = 8;
  const FIELD_SIZE = 8;
  const totalSize = layout ? layout.totalSize : HEADER_SIZE + FIELD_SIZE * propDefs.length;
  const mallocIdx = ctx.funcMap.get("__malloc");
  fctx.body.push({ op: "i32.const", value: totalSize });
  fctx.body.push({ op: "call", funcIdx: mallocIdx });
  const tmpLocal = addLocal(fctx, `__obj_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.tee", index: tmpLocal });
  fctx.body.push({ op: "i32.const", value: 16 });
  fctx.body.push({ op: "i32.store8", align: 0, offset: 0 });
  fctx.body.push({ op: "local.get", index: tmpLocal });
  fctx.body.push({ op: "i32.const", value: totalSize - HEADER_SIZE });
  fctx.body.push({ op: "i32.store", align: 2, offset: 4 });
  for (const prop of expr.properties) {
    let pName;
    let initExpr;
    if (ts16.isPropertyAssignment(prop) && ts16.isIdentifier(prop.name)) {
      pName = prop.name.text;
      initExpr = prop.initializer;
    } else if (ts16.isShorthandPropertyAssignment(prop)) {
      pName = prop.name.text;
      initExpr = prop.name;
    }
    if (!pName || !initExpr) continue;
    const field = layout?.fields.get(pName);
    const fieldOffset = field ? field.offset : void 0;
    if (fieldOffset === void 0) continue;
    fctx.body.push({ op: "local.get", index: tmpLocal });
    compileExpression3(ctx, fctx, initExpr);
    const valType = inferExprType(ctx, fctx, initExpr);
    if (field.type === "i32") {
      if (valType.kind !== "i32") {
        fctx.body.push({ op: "i32.trunc_f64_s" });
      }
      fctx.body.push({ op: "i32.store", align: 2, offset: fieldOffset });
    } else {
      if (valType.kind === "i32") {
        fctx.body.push({ op: "f64.convert_i32_s" });
      }
      fctx.body.push({ op: "f64.store", align: 3, offset: fieldOffset });
    }
  }
  fctx.body.push({ op: "local.get", index: tmpLocal });
}
function compileCallArg(ctx, fctx, arg) {
  if (ts16.isArrowFunction(arg) || ts16.isFunctionExpression(arg)) {
    emitClosureSetup(ctx, fctx, arg);
  } else {
    compileExpression3(ctx, fctx, arg);
  }
}
function compileBinaryExpression2(ctx, fctx, expr) {
  const op = expr.operatorToken.kind;
  if (op === ts16.SyntaxKind.EqualsToken) {
    if (ts16.isElementAccessExpression(expr.left)) {
      compileElementAccessAssignment(ctx, fctx, expr.left, expr.right);
      return;
    }
    if (ts16.isPropertyAccessExpression(expr.left)) {
      compilePropertyAssignment2(ctx, fctx, expr.left, expr.right);
      return;
    }
    if (ts16.isIdentifier(expr.left)) {
      const idx = fctx.localMap.get(expr.left.text);
      if (idx !== void 0) {
        compileExpression3(ctx, fctx, expr.right);
        fctx.body.push({ op: "local.tee", index: idx });
        return;
      }
      const gIdx = ctx.moduleGlobals.get(expr.left.text);
      if (gIdx !== void 0) {
        compileExpression3(ctx, fctx, expr.right);
        fctx.body.push({ op: "global.set", index: gIdx });
        fctx.body.push({ op: "global.get", index: gIdx });
        return;
      }
    }
  }
  if (isCompoundAssignment2(op) && ts16.isIdentifier(expr.left)) {
    const idx = fctx.localMap.get(expr.left.text);
    if (idx !== void 0) {
      if (isBitwiseCompoundAssignment(op)) {
        compileExprToI32(ctx, fctx, expr.left);
        compileExprToI32(ctx, fctx, expr.right);
        fctx.body.push(bitwiseOp(bitwiseCompoundToOp(op)));
        if (op === ts16.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken) {
          fctx.body.push({ op: "f64.convert_i32_u" });
        } else {
          fctx.body.push({ op: "f64.convert_i32_s" });
        }
        fctx.body.push({ op: "local.tee", index: idx });
      } else {
        fctx.body.push({ op: "local.get", index: idx });
        compileExpression3(ctx, fctx, expr.right);
        fctx.body.push(compoundAssignmentOp(op));
        fctx.body.push({ op: "local.tee", index: idx });
      }
      return;
    }
    const gIdx = ctx.moduleGlobals.get(expr.left.text);
    if (gIdx !== void 0) {
      fctx.body.push({ op: "global.get", index: gIdx });
      compileExpression3(ctx, fctx, expr.right);
      fctx.body.push(compoundAssignmentOp(op));
      fctx.body.push({ op: "global.set", index: gIdx });
      fctx.body.push({ op: "global.get", index: gIdx });
      return;
    }
  }
  if (isCompoundAssignment2(op) && ts16.isPropertyAccessExpression(expr.left)) {
    const propName = expr.left.name.text;
    const className = inferClassName(ctx, fctx, expr.left.expression);
    if (className) {
      const layout = ctx.classLayouts.get(className);
      const field = layout?.fields.get(propName);
      if (field) {
        compileExpression3(ctx, fctx, expr.left.expression);
        const objLocal = addLocal(fctx, `$compound_obj`, { kind: "i32" });
        fctx.body.push({ op: "local.tee", index: objLocal });
        if (field.type === "f64") {
          fctx.body.push({ op: "f64.load", align: 3, offset: field.offset });
        } else {
          fctx.body.push({ op: "i32.load", align: 2, offset: field.offset });
        }
        compileExpression3(ctx, fctx, expr.right);
        fctx.body.push(compoundAssignmentOp(op));
        const tempLocal = addLocal(fctx, `$compound_val`, field.type === "f64" ? { kind: "f64" } : { kind: "i32" });
        fctx.body.push({ op: "local.tee", index: tempLocal });
        const objLocal2 = objLocal;
        fctx.body.push({ op: "local.set", index: tempLocal });
        fctx.body.push({ op: "local.get", index: objLocal2 });
        fctx.body.push({ op: "local.get", index: tempLocal });
        if (field.type === "f64") {
          fctx.body.push({ op: "f64.store", align: 3, offset: field.offset });
        } else {
          fctx.body.push({ op: "i32.store", align: 2, offset: field.offset });
        }
        fctx.body.push({ op: "local.get", index: tempLocal });
        return;
      }
    }
  }
  if (isBitwiseOp(op)) {
    compileExprToI32(ctx, fctx, expr.left);
    compileExprToI32(ctx, fctx, expr.right);
    fctx.body.push(bitwiseOp(op));
    if (op === ts16.SyntaxKind.GreaterThanGreaterThanGreaterThanToken) {
      fctx.body.push({ op: "f64.convert_i32_u" });
    } else {
      fctx.body.push({ op: "f64.convert_i32_s" });
    }
    return;
  }
  const isUndefinedOrNull = (e) =>
    (ts16.isIdentifier(e) && e.text === "undefined") || e.kind === ts16.SyntaxKind.NullKeyword;
  if (isComparisonOp(op) && (isUndefinedOrNull(expr.left) || isUndefinedOrNull(expr.right))) {
    const valueExpr = isUndefinedOrNull(expr.left) ? expr.right : expr.left;
    const valueType = inferExprType(ctx, fctx, valueExpr);
    compileExpression3(ctx, fctx, valueExpr);
    if (valueType.kind === "i32") {
      fctx.body.push({ op: "i32.const", value: 0 });
      if (op === ts16.SyntaxKind.EqualsEqualsEqualsToken || op === ts16.SyntaxKind.EqualsEqualsToken) {
        fctx.body.push({ op: "i32.eq" });
      } else {
        fctx.body.push({ op: "i32.ne" });
      }
    } else {
      fctx.body.push({ op: "f64.const", value: 0 });
      if (op === ts16.SyntaxKind.EqualsEqualsEqualsToken || op === ts16.SyntaxKind.EqualsEqualsToken) {
        fctx.body.push({ op: "f64.eq" });
      } else {
        fctx.body.push({ op: "f64.ne" });
      }
    }
    fctx.body.push({ op: "f64.convert_i32_s" });
    return;
  }
  if (isStringExpr(ctx, fctx, expr.left) && isStringExpr(ctx, fctx, expr.right)) {
    if (op === ts16.SyntaxKind.EqualsEqualsEqualsToken || op === ts16.SyntaxKind.EqualsEqualsToken) {
      compileExpression3(ctx, fctx, expr.left);
      compileExpression3(ctx, fctx, expr.right);
      const strEqIdx = ctx.funcMap.get("__str_eq");
      fctx.body.push({ op: "call", funcIdx: strEqIdx });
      fctx.body.push({ op: "f64.convert_i32_s" });
      return;
    }
    if (op === ts16.SyntaxKind.PlusToken) {
      compileExpression3(ctx, fctx, expr.left);
      compileExpression3(ctx, fctx, expr.right);
      const strConcatIdx = ctx.funcMap.get("__str_concat");
      fctx.body.push({ op: "call", funcIdx: strConcatIdx });
      return;
    }
  }
  if (op === ts16.SyntaxKind.AmpersandAmpersandToken || op === ts16.SyntaxKind.BarBarToken) {
    compileExpression3(ctx, fctx, expr.left);
    const leftType2 = inferExprType(ctx, fctx, expr.left);
    emitTruthyCoercion(fctx, leftType2);
    const thenBody = [];
    const elseBody = [];
    const savedBody = fctx.body;
    if (op === ts16.SyntaxKind.AmpersandAmpersandToken) {
      fctx.body = thenBody;
      compileExprToF64(ctx, fctx, expr.right);
      fctx.body = savedBody;
      elseBody.push({ op: "f64.const", value: 0 });
    } else {
      thenBody.push({ op: "f64.const", value: 1 });
      fctx.body = elseBody;
      compileExprToF64(ctx, fctx, expr.right);
      fctx.body = savedBody;
    }
    fctx.body.push({
      op: "if",
      blockType: { kind: "val", type: { kind: "f64" } },
      then: thenBody,
      else: elseBody,
    });
    return;
  }
  const leftType = inferExprType(ctx, fctx, expr.left);
  const rightType = inferExprType(ctx, fctx, expr.right);
  const bothI32 = leftType.kind === "i32" && rightType.kind === "i32";
  if (bothI32 && isComparisonOp(op)) {
    compileExpression3(ctx, fctx, expr.left);
    compileExpression3(ctx, fctx, expr.right);
    switch (op) {
      case ts16.SyntaxKind.EqualsEqualsEqualsToken:
      case ts16.SyntaxKind.EqualsEqualsToken:
        fctx.body.push({ op: "i32.eq" });
        break;
      case ts16.SyntaxKind.ExclamationEqualsEqualsToken:
      case ts16.SyntaxKind.ExclamationEqualsToken:
        fctx.body.push({ op: "i32.ne" });
        break;
      case ts16.SyntaxKind.LessThanToken:
        fctx.body.push({ op: "i32.lt_s" });
        break;
      case ts16.SyntaxKind.LessThanEqualsToken:
        fctx.body.push({ op: "i32.le_s" });
        break;
      case ts16.SyntaxKind.GreaterThanToken:
        fctx.body.push({ op: "i32.gt_s" });
        break;
      case ts16.SyntaxKind.GreaterThanEqualsToken:
        fctx.body.push({ op: "i32.ge_s" });
        break;
    }
    fctx.body.push({ op: "f64.convert_i32_s" });
    return;
  }
  compileExpression3(ctx, fctx, expr.left);
  compileExpression3(ctx, fctx, expr.right);
  switch (op) {
    case ts16.SyntaxKind.PlusToken:
      fctx.body.push({ op: "f64.add" });
      break;
    case ts16.SyntaxKind.MinusToken:
      fctx.body.push({ op: "f64.sub" });
      break;
    case ts16.SyntaxKind.AsteriskToken:
      fctx.body.push({ op: "f64.mul" });
      break;
    case ts16.SyntaxKind.SlashToken:
      fctx.body.push({ op: "f64.div" });
      break;
    case ts16.SyntaxKind.PercentToken:
      break;
    case ts16.SyntaxKind.LessThanToken:
      fctx.body.push({ op: "f64.lt" });
      break;
    case ts16.SyntaxKind.LessThanEqualsToken:
      fctx.body.push({ op: "f64.le" });
      break;
    case ts16.SyntaxKind.GreaterThanToken:
      fctx.body.push({ op: "f64.gt" });
      break;
    case ts16.SyntaxKind.GreaterThanEqualsToken:
      fctx.body.push({ op: "f64.ge" });
      break;
    case ts16.SyntaxKind.EqualsEqualsEqualsToken:
    case ts16.SyntaxKind.EqualsEqualsToken:
      fctx.body.push({ op: "f64.eq" });
      break;
    case ts16.SyntaxKind.ExclamationEqualsEqualsToken:
    case ts16.SyntaxKind.ExclamationEqualsToken:
      fctx.body.push({ op: "f64.ne" });
      break;
    default:
      ctx.errors.push({
        message: `Unsupported binary operator: ${ts16.SyntaxKind[op]}`,
        line: 0,
        column: 0,
      });
  }
  if (isComparisonOp(op)) {
    fctx.body.push({ op: "f64.convert_i32_s" });
  }
}
function isComparisonOp(op) {
  return (
    op === ts16.SyntaxKind.LessThanToken ||
    op === ts16.SyntaxKind.LessThanEqualsToken ||
    op === ts16.SyntaxKind.GreaterThanToken ||
    op === ts16.SyntaxKind.GreaterThanEqualsToken ||
    op === ts16.SyntaxKind.EqualsEqualsEqualsToken ||
    op === ts16.SyntaxKind.EqualsEqualsToken ||
    op === ts16.SyntaxKind.ExclamationEqualsEqualsToken ||
    op === ts16.SyntaxKind.ExclamationEqualsToken
  );
}
function isBitwiseOp(op) {
  return (
    op === ts16.SyntaxKind.AmpersandToken ||
    op === ts16.SyntaxKind.BarToken ||
    op === ts16.SyntaxKind.CaretToken ||
    op === ts16.SyntaxKind.LessThanLessThanToken ||
    op === ts16.SyntaxKind.GreaterThanGreaterThanToken ||
    op === ts16.SyntaxKind.GreaterThanGreaterThanGreaterThanToken
  );
}
function bitwiseOp(op) {
  switch (op) {
    case ts16.SyntaxKind.AmpersandToken:
      return { op: "i32.and" };
    case ts16.SyntaxKind.BarToken:
      return { op: "i32.or" };
    case ts16.SyntaxKind.CaretToken:
      return { op: "i32.xor" };
    case ts16.SyntaxKind.LessThanLessThanToken:
      return { op: "i32.shl" };
    case ts16.SyntaxKind.GreaterThanGreaterThanToken:
      return { op: "i32.shr_s" };
    case ts16.SyntaxKind.GreaterThanGreaterThanGreaterThanToken:
      return { op: "i32.shr_u" };
    default:
      return { op: "unreachable" };
  }
}
function bitwiseCompoundToOp(op) {
  switch (op) {
    case ts16.SyntaxKind.BarEqualsToken:
      return ts16.SyntaxKind.BarToken;
    case ts16.SyntaxKind.AmpersandEqualsToken:
      return ts16.SyntaxKind.AmpersandToken;
    case ts16.SyntaxKind.CaretEqualsToken:
      return ts16.SyntaxKind.CaretToken;
    case ts16.SyntaxKind.LessThanLessThanEqualsToken:
      return ts16.SyntaxKind.LessThanLessThanToken;
    case ts16.SyntaxKind.GreaterThanGreaterThanEqualsToken:
      return ts16.SyntaxKind.GreaterThanGreaterThanToken;
    case ts16.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken:
      return ts16.SyntaxKind.GreaterThanGreaterThanGreaterThanToken;
    default:
      return op;
  }
}
function isCompoundAssignment2(op) {
  return (
    op === ts16.SyntaxKind.PlusEqualsToken ||
    op === ts16.SyntaxKind.MinusEqualsToken ||
    op === ts16.SyntaxKind.AsteriskEqualsToken ||
    op === ts16.SyntaxKind.SlashEqualsToken ||
    op === ts16.SyntaxKind.BarEqualsToken ||
    op === ts16.SyntaxKind.AmpersandEqualsToken ||
    op === ts16.SyntaxKind.CaretEqualsToken ||
    op === ts16.SyntaxKind.LessThanLessThanEqualsToken ||
    op === ts16.SyntaxKind.GreaterThanGreaterThanEqualsToken ||
    op === ts16.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken
  );
}
function isBitwiseCompoundAssignment(op) {
  return (
    op === ts16.SyntaxKind.BarEqualsToken ||
    op === ts16.SyntaxKind.AmpersandEqualsToken ||
    op === ts16.SyntaxKind.CaretEqualsToken ||
    op === ts16.SyntaxKind.LessThanLessThanEqualsToken ||
    op === ts16.SyntaxKind.GreaterThanGreaterThanEqualsToken ||
    op === ts16.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken
  );
}
function compoundAssignmentOp(op) {
  switch (op) {
    case ts16.SyntaxKind.PlusEqualsToken:
      return { op: "f64.add" };
    case ts16.SyntaxKind.MinusEqualsToken:
      return { op: "f64.sub" };
    case ts16.SyntaxKind.AsteriskEqualsToken:
      return { op: "f64.mul" };
    case ts16.SyntaxKind.SlashEqualsToken:
      return { op: "f64.div" };
    default:
      return { op: "unreachable" };
  }
}
function emitTruthyCoercion(fctx, type) {
  if (type.kind === "f64") {
    fctx.body.push({ op: "f64.const", value: 0 });
    fctx.body.push({ op: "f64.ne" });
  } else if (type.kind === "i32") {
  }
}
function detectCollectionKind(ctx, decl) {
  if (decl.type) {
    const text = decl.type.getText();
    if (text === "number[]" || text.startsWith("Array<")) return "Array";
    if (text === "Uint8Array") return "Uint8Array";
    if (text.startsWith("Map<") || text === "Map") return "Map";
    if (text.startsWith("Set<") || text === "Set") return "Set";
  }
  if (decl.initializer) {
    if (ts16.isArrayLiteralExpression(decl.initializer)) return "Array";
    if (ts16.isNewExpression(decl.initializer) && ts16.isIdentifier(decl.initializer.expression)) {
      const ctorName = decl.initializer.expression.text;
      if (ctorName === "Uint8Array") return "Uint8Array";
      if (ctorName === "Map") return "Map";
      if (ctorName === "Set") return "Set";
    }
    if (ts16.isCallExpression(decl.initializer) && ts16.isPropertyAccessExpression(decl.initializer.expression)) {
      const pa = decl.initializer.expression;
      if (
        pa.name.text === "encode" &&
        ts16.isNewExpression(pa.expression) &&
        ts16.isIdentifier(pa.expression.expression) &&
        pa.expression.expression.text === "TextEncoder"
      ) {
        return "Uint8Array";
      }
    }
    try {
      const rawType = ctx.checker.getTypeAtLocation(decl.initializer);
      const type = ctx.checker.getNonNullableType(rawType);
      const typeStr = ctx.checker.typeToString(type);
      if (typeStr === "Uint8Array" || typeStr.includes("Uint8Array")) return "Uint8Array";
      if (typeStr.startsWith("Map<") || typeStr === "Map") return "Map";
      if (typeStr.startsWith("Set<") || typeStr === "Set") return "Set";
      if (typeStr === "number[]" || typeStr.endsWith("[]") || typeStr.startsWith("Array<")) return "Array";
    } catch {}
  }
  return null;
}
function getExprCollectionKind(ctx, fctx, expr) {
  if (ts16.isIdentifier(expr)) {
    return fctx.collectionTypes.get(expr.text) ?? ctx.moduleCollectionTypes.get(expr.text) ?? null;
  }
  if (ts16.isPropertyAccessExpression(expr)) {
    const className = inferClassName(ctx, fctx, expr.expression);
    if (className) {
      const layout = ctx.classLayouts.get(className);
      if (layout) {
        const kind = layout.fieldCollectionKinds.get(expr.name.text);
        if (kind) return kind;
      }
    }
  }
  if (ts16.isArrayLiteralExpression(expr)) {
    return "Array";
  }
  if (ts16.isNewExpression(expr) && ts16.isIdentifier(expr.expression)) {
    const name = expr.expression.text;
    if (name === "Map") return "Map";
    if (name === "Set") return "Set";
    if (name === "Uint8Array") return "Uint8Array";
  }
  try {
    const rawType = ctx.checker.getTypeAtLocation(expr);
    const type = ctx.checker.getNonNullableType(rawType);
    const typeStr = ctx.checker.typeToString(type);
    if (typeStr === "Uint8Array" || typeStr.includes("Uint8Array")) return "Uint8Array";
    if (typeStr.startsWith("Map<") || typeStr === "Map") return "Map";
    if (typeStr.startsWith("Set<") || typeStr === "Set") return "Set";
    if (typeStr.endsWith("[]") || typeStr.startsWith("Array<")) return "Array";
  } catch {}
  return null;
}
function compileArrayLiteral2(ctx, fctx, expr) {
  const elements = expr.elements;
  const cap = Math.max(elements.length, 16);
  const arrNewIdx = ctx.funcMap.get("__arr_new");
  const arrPushIdx = ctx.funcMap.get("__arr_push");
  fctx.body.push({ op: "i32.const", value: cap });
  fctx.body.push({ op: "call", funcIdx: arrNewIdx });
  if (elements.length > 0) {
    const tmpLocal = addLocal(fctx, `__arr_tmp_${fctx.locals.length}`, { kind: "i32" });
    fctx.body.push({ op: "local.set", index: tmpLocal });
    for (const elem of elements) {
      fctx.body.push({ op: "local.get", index: tmpLocal });
      compileExprToI32(ctx, fctx, elem);
      fctx.body.push({ op: "call", funcIdx: arrPushIdx });
    }
    fctx.body.push({ op: "local.get", index: tmpLocal });
  }
}
function compileObjectDestructuring2(ctx, fctx, pattern, initializer) {
  compileExpression3(ctx, fctx, initializer);
  const objLocal = addLocal(fctx, `__obj_destr_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.set", index: objLocal });
  let objType = null;
  try {
    objType = ctx.checker.getTypeAtLocation(initializer);
  } catch {}
  const propOffsets = /* @__PURE__ */ new Map();
  if (objType) {
    const props = objType.getProperties();
    let offset = 0;
    for (const prop of props) {
      let isF64 = false;
      try {
        const propType = ctx.checker.getTypeOfSymbolAtLocation(prop, initializer);
        const baseType = ctx.checker.getNonNullableType(propType);
        if (baseType.flags & (ts16.TypeFlags.NumberLike | ts16.TypeFlags.BooleanLike)) {
          isF64 = true;
        }
      } catch {}
      const fieldSize = isF64 ? 8 : 4;
      if (isF64 && offset % 8 !== 0) offset = Math.ceil(offset / 8) * 8;
      else if (!isF64 && offset % 4 !== 0) offset = Math.ceil(offset / 4) * 4;
      propOffsets.set(prop.getName(), { offset, type: isF64 ? "f64" : "i32" });
      offset += fieldSize;
    }
  }
  for (const element of pattern.elements) {
    if (ts16.isOmittedExpression(element)) continue;
    if (!ts16.isBindingElement(element)) continue;
    const propName =
      element.propertyName && ts16.isIdentifier(element.propertyName)
        ? element.propertyName.text
        : ts16.isIdentifier(element.name)
          ? element.name.text
          : null;
    const varName = ts16.isIdentifier(element.name) ? element.name.text : null;
    if (!propName || !varName) continue;
    const fieldInfo = propOffsets.get(propName);
    if (!fieldInfo) {
      ctx.errors.push({ message: `Object destructuring: unknown property "${propName}"`, line: 0, column: 0 });
      continue;
    }
    const localType = fieldInfo.type === "f64" ? { kind: "f64" } : { kind: "i32" };
    const localIdx = addLocal(fctx, varName, localType);
    fctx.body.push({ op: "local.get", index: objLocal });
    if (fieldInfo.type === "f64") {
      fctx.body.push({ op: "f64.load", align: 3, offset: fieldInfo.offset });
    } else {
      fctx.body.push({ op: "i32.load", align: 2, offset: fieldInfo.offset });
    }
    fctx.body.push({ op: "local.set", index: localIdx });
    try {
      const propType = ctx.checker.getTypeAtLocation(element);
      const typeStr = ctx.checker.typeToString(ctx.checker.getNonNullableType(propType));
      if (typeStr.endsWith("[]")) fctx.collectionTypes.set(varName, "Array");
    } catch {}
  }
}
function compileArrayDestructuring2(ctx, fctx, pattern, initializer) {
  compileExpression3(ctx, fctx, initializer);
  const arrLocal = addLocal(fctx, `__destr_arr_${fctx.locals.length}`, { kind: "i32" });
  fctx.body.push({ op: "local.set", index: arrLocal });
  const arrGetIdx = ctx.funcMap.get("__arr_get");
  let elemIsI32 = false;
  try {
    const type = ctx.checker.getTypeAtLocation(initializer);
    const typeStr = ctx.checker.typeToString(ctx.checker.getNonNullableType(type));
    if (typeStr.endsWith("[]") && !typeStr.startsWith("number") && !typeStr.startsWith("boolean")) {
      elemIsI32 = true;
    }
    if (typeStr === "string[]") elemIsI32 = true;
  } catch {}
  for (let i = 0; i < pattern.elements.length; i++) {
    const element = pattern.elements[i];
    if (ts16.isOmittedExpression(element)) continue;
    if (!ts16.isBindingElement(element)) continue;
    if (element.dotDotDotToken) {
      if (ts16.isIdentifier(element.name)) {
        const varName = element.name.text;
        const localIdx = addLocal(fctx, varName, { kind: "i32" });
        const arrSliceIdx = ctx.funcMap.get("__arr_slice");
        const arrLenIdx = ctx.funcMap.get("__arr_len");
        fctx.body.push({ op: "local.get", index: arrLocal });
        fctx.body.push({ op: "i32.const", value: i });
        fctx.body.push({ op: "local.get", index: arrLocal });
        fctx.body.push({ op: "call", funcIdx: arrLenIdx });
        fctx.body.push({ op: "call", funcIdx: arrSliceIdx });
        fctx.body.push({ op: "local.set", index: localIdx });
        fctx.collectionTypes.set(varName, "Array");
      }
      continue;
    }
    if (ts16.isIdentifier(element.name)) {
      const varName = element.name.text;
      const localIdx = addLocal(fctx, varName, elemIsI32 ? { kind: "i32" } : { kind: "f64" });
      fctx.body.push({ op: "local.get", index: arrLocal });
      fctx.body.push({ op: "i32.const", value: i });
      fctx.body.push({ op: "call", funcIdx: arrGetIdx });
      if (!elemIsI32) {
        fctx.body.push({ op: "f64.convert_i32_s" });
      }
      fctx.body.push({ op: "local.set", index: localIdx });
    }
  }
}
function compileNewExpression2(ctx, fctx, expr) {
  if (!ts16.isIdentifier(expr.expression)) {
    ctx.errors.push({ message: "Unsupported new expression", line: 0, column: 0 });
    return;
  }
  const ctorName = expr.expression.text;
  if (ctorName === "Uint8Array") {
    if (expr.arguments && expr.arguments.length > 0) {
      const argType = inferExprType(ctx, fctx, expr.arguments[0]);
      let isArrayBuffer = false;
      try {
        const argTsType = ctx.checker.getTypeAtLocation(expr.arguments[0]);
        const argTypeStr = ctx.checker.typeToString(argTsType);
        isArrayBuffer = argTypeStr === "ArrayBuffer";
      } catch {}
      if (isArrayBuffer) {
        const fromRawIdx = ctx.funcMap.get("__u8arr_from_raw");
        const abTmp = addLocal(fctx, "$u8_ab_tmp", { kind: "i32" });
        compileExpression3(ctx, fctx, expr.arguments[0]);
        fctx.body.push({ op: "local.tee", index: abTmp });
        fctx.body.push({ op: "i32.const", value: 4 });
        fctx.body.push({ op: "i32.add" });
        fctx.body.push({ op: "local.get", index: abTmp });
        fctx.body.push({ op: "i32.load", align: 2, offset: 0 });
        fctx.body.push({ op: "call", funcIdx: fromRawIdx });
      } else {
        let isNumberArray = false;
        try {
          const argTypeStr = ctx.checker.typeToString(ctx.checker.getTypeAtLocation(expr.arguments[0]));
          isNumberArray = argTypeStr === "number[]" || argTypeStr.endsWith("[]");
        } catch {}
        if (isNumberArray) {
          const fromArrIdx = ctx.funcMap.get("__u8arr_from_arr");
          compileExpression3(ctx, fctx, expr.arguments[0]);
          fctx.body.push({ op: "call", funcIdx: fromArrIdx });
        } else {
          const u8NewIdx = ctx.funcMap.get("__u8arr_new");
          compileExprToI32(ctx, fctx, expr.arguments[0]);
          fctx.body.push({ op: "call", funcIdx: u8NewIdx });
        }
      }
    } else {
      const u8NewIdx = ctx.funcMap.get("__u8arr_new");
      fctx.body.push({ op: "i32.const", value: 0 });
      fctx.body.push({ op: "call", funcIdx: u8NewIdx });
    }
  } else if (ctorName === "ArrayBuffer") {
    const mallocIdx = ctx.funcMap.get("__malloc");
    const tmpPtr = addLocal(fctx, "$ab_ptr", { kind: "i32" });
    if (expr.arguments && expr.arguments.length > 0) {
      compileExprToI32(ctx, fctx, expr.arguments[0]);
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    const tmpLen = addLocal(fctx, "$ab_len", { kind: "i32" });
    fctx.body.push({ op: "local.tee", index: tmpLen });
    fctx.body.push({ op: "i32.const", value: 4 });
    fctx.body.push({ op: "i32.add" });
    fctx.body.push({ op: "call", funcIdx: mallocIdx });
    fctx.body.push({ op: "local.tee", index: tmpPtr });
    fctx.body.push({ op: "local.get", index: tmpLen });
    fctx.body.push({ op: "i32.store", align: 2, offset: 0 });
    fctx.body.push({ op: "local.get", index: tmpPtr });
  } else if (ctorName === "Float64Array" || ctorName === "Float32Array") {
    if (expr.arguments && expr.arguments.length > 0) {
      compileExpression3(ctx, fctx, expr.arguments[0]);
      fctx.body.push({ op: "i32.const", value: 4 });
      fctx.body.push({ op: "i32.add" });
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
  } else if (ctorName === "Map") {
    const nmapNewIdx = ctx.funcMap.get("__nmap_new");
    fctx.body.push({ op: "i32.const", value: 16 });
    fctx.body.push({ op: "call", funcIdx: nmapNewIdx });
  } else if (ctorName === "Set") {
    const nsetNewIdx = ctx.funcMap.get("__nset_new");
    fctx.body.push({ op: "i32.const", value: 16 });
    fctx.body.push({ op: "call", funcIdx: nsetNewIdx });
  } else {
    const layout = ctx.classLayouts.get(ctorName);
    if (layout) {
      compileClassNewExpression(ctx, fctx, expr, ctorName, layout);
    } else {
      ctx.errors.push({ message: `Unsupported constructor: ${ctorName}`, line: 0, column: 0 });
    }
  }
}
function compilePropertyAccess2(ctx, fctx, expr) {
  const propName = expr.name.text;
  try {
    const type = ctx.checker.getTypeAtLocation(expr);
    if (type.isNumberLiteral()) {
      fctx.body.push({ op: "f64.const", value: type.value });
      return;
    }
    if (type.isStringLiteral()) {
      compileStringLiteral2(ctx, fctx, type.value);
      return;
    }
  } catch {}
  const objKind = getExprCollectionKind(ctx, fctx, expr.expression);
  if (propName === "length" && (objKind === "Array" || objKind === "Uint8Array" || objKind === "ArrayOrUint8Array")) {
    compileExpression3(ctx, fctx, expr.expression);
    if (objKind === "ArrayOrUint8Array") {
      const arrLenIdx = ctx.funcMap.get("__arr_len");
      const u8LenIdx = ctx.funcMap.get("__u8arr_len");
      const ptrLocal = addLocal(fctx, `__len_tmp_${fctx.locals.length}`, { kind: "i32" });
      fctx.body.push({ op: "local.tee", index: ptrLocal });
      fctx.body.push({ op: "i32.load8_u", align: 0, offset: 0 });
      fctx.body.push({ op: "i32.const", value: 2 });
      fctx.body.push({ op: "i32.eq" });
      fctx.body.push({
        op: "if",
        blockType: { kind: "val", type: { kind: "i32" } },
        then: [
          { op: "local.get", index: ptrLocal },
          { op: "call", funcIdx: u8LenIdx },
        ],
        else: [
          { op: "local.get", index: ptrLocal },
          { op: "call", funcIdx: arrLenIdx },
        ],
      });
    } else {
      const lenFunc = objKind === "Array" ? "__arr_len" : "__u8arr_len";
      const funcIdx = ctx.funcMap.get(lenFunc);
      fctx.body.push({ op: "call", funcIdx });
    }
    fctx.body.push({ op: "f64.convert_i32_s" });
    return;
  }
  if (propName === "length" && isStringExpr(ctx, fctx, expr.expression)) {
    compileExpression3(ctx, fctx, expr.expression);
    const strLenIdx = ctx.funcMap.get("__str_len");
    fctx.body.push({ op: "call", funcIdx: strLenIdx });
    fctx.body.push({ op: "f64.convert_i32_s" });
    return;
  }
  if (propName === "size" && (objKind === "Map" || objKind === "Set")) {
    compileExpression3(ctx, fctx, expr.expression);
    const sizeFunc = objKind === "Map" ? "__nmap_size" : "__nset_size";
    const funcIdx = ctx.funcMap.get(sizeFunc);
    fctx.body.push({ op: "call", funcIdx });
    fctx.body.push({ op: "f64.convert_i32_s" });
    return;
  }
  const className = inferClassName(ctx, fctx, expr.expression);
  if (className) {
    const layout = ctx.classLayouts.get(className);
    if (layout) {
      const field = layout.fields.get(propName);
      if (field) {
        compileExpression3(ctx, fctx, expr.expression);
        if (field.type === "f64") {
          fctx.body.push({ op: "f64.load", align: 3, offset: field.offset });
        } else {
          fctx.body.push({ op: "i32.load", align: 2, offset: field.offset });
        }
        return;
      }
      const getterFuncName = layout.getters.get(propName);
      if (getterFuncName) {
        const funcIdx = ctx.funcMap.get(getterFuncName);
        if (funcIdx !== void 0) {
          compileExpression3(ctx, fctx, expr.expression);
          fctx.body.push({ op: "call", funcIdx });
          return;
        }
      }
    }
  }
  try {
    const objType = ctx.checker.getTypeAtLocation(expr.expression);
    const baseType = ctx.checker.getNonNullableType(objType);
    const props = baseType.getProperties();
    if (props.length > 0) {
      const HEADER_SIZE = 8;
      const FIELD_SIZE = 8;
      let offset = HEADER_SIZE;
      let foundField = null;
      for (const prop of props) {
        const rawPropType = ctx.checker.getTypeOfSymbolAtLocation(prop, expr);
        const propType = ctx.checker.getNonNullableType(rawPropType);
        const typeStr = ctx.checker.typeToString(propType);
        const isF64 =
          typeStr === "number" ||
          typeStr === "boolean" ||
          (propType.getFlags() & (ts16.TypeFlags.NumberLike | ts16.TypeFlags.BooleanLike)) !== 0;
        if (prop.getName() === propName) {
          foundField = { offset, type: isF64 ? "f64" : "i32" };
          break;
        }
        offset += FIELD_SIZE;
      }
      if (foundField) {
        compileExpression3(ctx, fctx, expr.expression);
        if (foundField.type === "f64") {
          fctx.body.push({ op: "f64.load", align: 3, offset: foundField.offset });
        } else {
          fctx.body.push({ op: "i32.load", align: 2, offset: foundField.offset });
        }
        return;
      }
    }
  } catch {}
  ctx.errors.push({
    message: `Unsupported property access: .${propName}`,
    line: 0,
    column: 0,
  });
}
function compileElementAccess2(ctx, fctx, expr) {
  const objKind = getExprCollectionKind(ctx, fctx, expr.expression);
  if (objKind === "Array") {
    const getIdx = ctx.funcMap.get("__arr_get");
    compileExpression3(ctx, fctx, expr.expression);
    compileExprToI32(ctx, fctx, expr.argumentExpression);
    fctx.body.push({ op: "call", funcIdx: getIdx });
    let elemIsNum = true;
    try {
      const elemType = ctx.checker.getTypeAtLocation(expr);
      const typeStr = ctx.checker.typeToString(elemType);
      if (typeStr !== "number" && typeStr !== "boolean") elemIsNum = false;
    } catch {}
    if (elemIsNum) {
      fctx.body.push({ op: "f64.convert_i32_s" });
    }
  } else if (objKind === "Uint8Array") {
    const getIdx = ctx.funcMap.get("__u8arr_get");
    compileExpression3(ctx, fctx, expr.expression);
    compileExprToI32(ctx, fctx, expr.argumentExpression);
    fctx.body.push({ op: "call", funcIdx: getIdx });
    fctx.body.push({ op: "f64.convert_i32_s" });
  } else {
    ctx.errors.push({
      message: "Unsupported element access on non-collection type",
      line: 0,
      column: 0,
    });
  }
}
function compileElementAccessAssignment(ctx, fctx, left, right) {
  if (ts16.isNewExpression(left.expression) && ts16.isIdentifier(left.expression.expression)) {
    const typeName = left.expression.expression.text;
    if (typeName === "Float64Array" && left.expression.arguments?.length) {
      compileExpression3(ctx, fctx, left.expression.arguments[0]);
      compileExprToI32(ctx, fctx, left.argumentExpression);
      fctx.body.push({ op: "i32.const", value: 3 });
      fctx.body.push({ op: "i32.shl" });
      fctx.body.push({ op: "i32.add" });
      compileExpression3(ctx, fctx, right);
      fctx.body.push({ op: "f64.store", align: 3, offset: 0 });
      compileExpression3(ctx, fctx, right);
      return;
    }
    if (typeName === "Float32Array" && left.expression.arguments?.length) {
      compileExpression3(ctx, fctx, left.expression.arguments[0]);
      compileExprToI32(ctx, fctx, left.argumentExpression);
      fctx.body.push({ op: "i32.const", value: 2 });
      fctx.body.push({ op: "i32.shl" });
      fctx.body.push({ op: "i32.add" });
      compileExpression3(ctx, fctx, right);
      fctx.body.push({ op: "f32.demote_f64" });
      fctx.body.push({ op: "f32.store", align: 2, offset: 0 });
      compileExpression3(ctx, fctx, right);
      return;
    }
  }
  const objKind = getExprCollectionKind(ctx, fctx, left.expression);
  if (objKind === "Array") {
    const setIdx = ctx.funcMap.get("__arr_set");
    compileExpression3(ctx, fctx, left.expression);
    compileExprToI32(ctx, fctx, left.argumentExpression);
    compileExprToI32(ctx, fctx, right);
    fctx.body.push({ op: "call", funcIdx: setIdx });
    compileExpression3(ctx, fctx, right);
  } else if (objKind === "Uint8Array") {
    const setIdx = ctx.funcMap.get("__u8arr_set");
    compileExpression3(ctx, fctx, left.expression);
    compileExprToI32(ctx, fctx, left.argumentExpression);
    compileExprToI32(ctx, fctx, right);
    fctx.body.push({ op: "call", funcIdx: setIdx });
    compileExpression3(ctx, fctx, right);
  } else {
    ctx.errors.push({
      message: "Unsupported element access assignment",
      line: 0,
      column: 0,
    });
  }
}
function compileMethodCall(ctx, fctx, expr) {
  const propAccess = expr.expression;
  const methodName = propAccess.name.text;
  if (
    methodName === "decode" &&
    ts16.isNewExpression(propAccess.expression) &&
    ts16.isIdentifier(propAccess.expression.expression) &&
    propAccess.expression.expression.text === "TextDecoder"
  ) {
    const strFromU8Idx = ctx.funcMap.get("__str_from_u8arr");
    if (expr.arguments.length > 0) {
      compileExpression3(ctx, fctx, expr.arguments[0]);
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    fctx.body.push({ op: "call", funcIdx: strFromU8Idx });
    return;
  }
  if (
    methodName === "encode" &&
    ts16.isNewExpression(propAccess.expression) &&
    ts16.isIdentifier(propAccess.expression.expression) &&
    propAccess.expression.expression.text === "TextEncoder"
  ) {
    const strFromU8Idx = ctx.funcMap.get("__str_from_u8arr");
    if (expr.arguments.length > 0) {
      compileExpression3(ctx, fctx, expr.arguments[0]);
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    fctx.body.push({ op: "call", funcIdx: strFromU8Idx });
    return;
  }
  const objKind = getExprCollectionKind(ctx, fctx, propAccess.expression);
  if (objKind === "Array") {
    compileArrayMethodCall2(ctx, fctx, expr, propAccess, methodName);
  } else if (objKind === "Uint8Array") {
    compileUint8ArrayMethodCall(ctx, fctx, expr, propAccess, methodName);
  } else if (objKind === "Map") {
    compileMapMethodCall(ctx, fctx, expr, propAccess, methodName);
  } else if (objKind === "Set") {
    compileSetMethodCall(ctx, fctx, expr, propAccess, methodName);
  } else if (methodName === "split" && isStringExpr(ctx, fctx, propAccess.expression)) {
    const splitIdx = ctx.funcMap.get("__str_split");
    compileExpression3(ctx, fctx, propAccess.expression);
    if (expr.arguments.length > 0) {
      compileExpression3(ctx, fctx, expr.arguments[0]);
    } else {
      compileStringLiteral2(ctx, fctx, "");
    }
    fctx.body.push({ op: "call", funcIdx: splitIdx });
    return;
  } else if (methodName === "slice" && isStringExpr(ctx, fctx, propAccess.expression)) {
    const sliceIdx = ctx.funcMap.get("__str_slice");
    compileExpression3(ctx, fctx, propAccess.expression);
    compileExprToI32(ctx, fctx, expr.arguments[0]);
    if (expr.arguments.length > 1) {
      compileExprToI32(ctx, fctx, expr.arguments[1]);
    } else {
      compileExpression3(ctx, fctx, propAccess.expression);
      const strLenIdx = ctx.funcMap.get("__str_len");
      fctx.body.push({ op: "call", funcIdx: strLenIdx });
    }
    fctx.body.push({ op: "call", funcIdx: sliceIdx });
    return;
  } else if (methodName === "indexOf" && isStringExpr(ctx, fctx, propAccess.expression)) {
    const indexOfIdx = ctx.funcMap.get("__str_index_of");
    compileExpression3(ctx, fctx, propAccess.expression);
    compileExpression3(ctx, fctx, expr.arguments[0]);
    if (expr.arguments.length > 1) {
      compileExprToI32(ctx, fctx, expr.arguments[1]);
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
    }
    fctx.body.push({ op: "call", funcIdx: indexOfIdx });
    fctx.body.push({ op: "f64.convert_i32_s" });
    return;
  } else if (methodName === "startsWith" && isStringExpr(ctx, fctx, propAccess.expression)) {
    const startsWithIdx = ctx.funcMap.get("__str_starts_with");
    compileExpression3(ctx, fctx, propAccess.expression);
    if (expr.arguments.length > 0) {
      compileExpression3(ctx, fctx, expr.arguments[0]);
    } else {
      compileStringLiteral2(ctx, fctx, "");
    }
    fctx.body.push({ op: "call", funcIdx: startsWithIdx });
    fctx.body.push({ op: "f64.convert_i32_s" });
    return;
  } else if (methodName === "toString") {
    compileExpression3(ctx, fctx, propAccess.expression);
    return;
  } else {
    const className = inferClassName(ctx, fctx, propAccess.expression);
    if (className) {
      const layout = ctx.classLayouts.get(className);
      if (layout) {
        const wasmMethodName = layout.methods.get(methodName);
        if (wasmMethodName) {
          const funcIdx = ctx.funcMap.get(wasmMethodName);
          if (funcIdx !== void 0) {
            compileExpression3(ctx, fctx, propAccess.expression);
            for (const arg of expr.arguments) {
              compileCallArg(ctx, fctx, arg);
            }
            emitDefaultArgs(ctx, fctx, wasmMethodName, expr.arguments.length + 1);
            fctx.body.push({ op: "call", funcIdx });
            return;
          }
        }
      }
    }
    ctx.errors.push({
      message: `Unsupported method call: .${methodName}()`,
      line: 0,
      column: 0,
    });
  }
}
function compileArrayMethodCall2(ctx, fctx, expr, propAccess, methodName) {
  if (methodName === "push") {
    const pushIdx = ctx.funcMap.get("__arr_push");
    compileExpression3(ctx, fctx, propAccess.expression);
    compileExprToI32(ctx, fctx, expr.arguments[0]);
    fctx.body.push({ op: "call", funcIdx: pushIdx });
    fctx.body.push({ op: "f64.const", value: 0 });
  } else if (
    methodName === "filter" ||
    methodName === "map" ||
    methodName === "some" ||
    methodName === "find" ||
    methodName === "flatMap"
  ) {
    compileArrayHOF(ctx, fctx, expr, propAccess, methodName);
  } else if (methodName === "join") {
    compileArrayJoin2(ctx, fctx, expr, propAccess);
  } else if (methodName === "length") {
    compileExpression3(ctx, fctx, propAccess.expression);
    const lenIdx = ctx.funcMap.get("__arr_len");
    fctx.body.push({ op: "call", funcIdx: lenIdx });
    fctx.body.push({ op: "f64.convert_i32_s" });
  } else {
    ctx.errors.push({
      message: `Unsupported Array method: .${methodName}()`,
      line: 0,
      column: 0,
    });
  }
}
function compileArrayHOF(ctx, fctx, expr, propAccess, method) {
  const arrLenIdx = ctx.funcMap.get("__arr_len");
  const arrGetIdx = ctx.funcMap.get("__arr_get");
  const arrNewIdx = ctx.funcMap.get("__arr_new");
  const arrPushIdx = ctx.funcMap.get("__arr_push");
  const callback = expr.arguments[0];
  if (!callback || !(ts16.isArrowFunction(callback) || ts16.isFunctionExpression(callback))) {
    ctx.errors.push({ message: `Array.${method}() requires inline arrow function`, line: 0, column: 0 });
    return;
  }
  const paramName =
    callback.parameters[0] && ts16.isIdentifier(callback.parameters[0].name)
      ? callback.parameters[0].name.text
      : "__hof_param";
  const indexParamName =
    callback.parameters[1] && ts16.isIdentifier(callback.parameters[1].name) ? callback.parameters[1].name.text : null;
  let elemIsI32 = true;
  try {
    const arrType = ctx.checker.getTypeAtLocation(propAccess.expression);
    const arrStr = ctx.checker.typeToString(ctx.checker.getNonNullableType(arrType));
    if (arrStr === "number[]" || arrStr === "boolean[]") elemIsI32 = false;
  } catch {}
  const elemType = elemIsI32 ? { kind: "i32" } : { kind: "f64" };
  const arrLocal = addLocal(fctx, `__hof_arr_${fctx.locals.length}`, { kind: "i32" });
  const iLocal = addLocal(fctx, `__hof_i_${fctx.locals.length}`, { kind: "i32" });
  const lenLocal = addLocal(fctx, `__hof_len_${fctx.locals.length}`, { kind: "i32" });
  const elemLocal = addLocal(fctx, paramName, elemType);
  const indexLocal = indexParamName ? addLocal(fctx, indexParamName, { kind: "f64" }) : void 0;
  let resultLocal;
  if (method === "filter" || method === "map" || method === "flatMap") {
    resultLocal = addLocal(fctx, `__hof_result_${fctx.locals.length}`, { kind: "i32" });
  } else if (method === "some") {
    resultLocal = addLocal(fctx, `__hof_result_${fctx.locals.length}`, { kind: "f64" });
  } else if (method === "find") {
    resultLocal = addLocal(fctx, `__hof_result_${fctx.locals.length}`, { kind: "i32" });
  }
  compileExpression3(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.set", index: arrLocal });
  fctx.body.push({ op: "local.get", index: arrLocal });
  fctx.body.push({ op: "call", funcIdx: arrLenIdx });
  fctx.body.push({ op: "local.set", index: lenLocal });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iLocal });
  if (method === "filter" || method === "map" || method === "flatMap") {
    fctx.body.push({ op: "i32.const", value: 16 });
    fctx.body.push({ op: "call", funcIdx: arrNewIdx });
    fctx.body.push({ op: "local.set", index: resultLocal });
  } else if (method === "some") {
    fctx.body.push({ op: "f64.const", value: 0 });
    fctx.body.push({ op: "local.set", index: resultLocal });
  } else if (method === "find") {
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "local.set", index: resultLocal });
  }
  const loopBody = [];
  const savedBody = fctx.body;
  fctx.body = loopBody;
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "local.get", index: lenLocal });
  fctx.body.push({ op: "i32.ge_s" });
  fctx.body.push({ op: "br_if", depth: 1 });
  fctx.body.push({ op: "local.get", index: arrLocal });
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "call", funcIdx: arrGetIdx });
  if (!elemIsI32) {
    fctx.body.push({ op: "f64.convert_i32_s" });
  }
  fctx.body.push({ op: "local.set", index: elemLocal });
  if (indexLocal !== void 0) {
    fctx.body.push({ op: "local.get", index: iLocal });
    fctx.body.push({ op: "f64.convert_i32_s" });
    fctx.body.push({ op: "local.set", index: indexLocal });
  }
  const bodyExpr = ts16.isBlock(callback.body)
    ? callback.body.statements[0] && ts16.isReturnStatement(callback.body.statements[0])
      ? callback.body.statements[0].expression
      : void 0
    : callback.body;
  if (!bodyExpr) {
    ctx.errors.push({ message: `Array.${method}() callback must have a simple expression body`, line: 0, column: 0 });
    fctx.body = savedBody;
    return;
  }
  if (method === "filter") {
    compileExpression3(ctx, fctx, bodyExpr);
    emitTruthyCoercion(fctx, inferExprType(ctx, fctx, bodyExpr));
    const pushBody3 = [];
    const savedBody2 = fctx.body;
    fctx.body = pushBody3;
    fctx.body.push({ op: "local.get", index: resultLocal });
    fctx.body.push({ op: "local.get", index: elemLocal });
    if (!elemIsI32) {
      fctx.body.push({ op: "i32.trunc_f64_s" });
    }
    fctx.body.push({ op: "call", funcIdx: arrPushIdx });
    fctx.body = savedBody2;
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: pushBody3 });
  } else if (method === "map") {
    fctx.body.push({ op: "local.get", index: resultLocal });
    compileExpression3(ctx, fctx, bodyExpr);
    const mappedType = inferExprType(ctx, fctx, bodyExpr);
    if (mappedType.kind === "f64") {
      fctx.body.push({ op: "i32.trunc_f64_s" });
    }
    fctx.body.push({ op: "call", funcIdx: arrPushIdx });
  } else if (method === "some") {
    compileExpression3(ctx, fctx, bodyExpr);
    emitTruthyCoercion(fctx, inferExprType(ctx, fctx, bodyExpr));
    const foundBody = [];
    const savedBody2 = fctx.body;
    fctx.body = foundBody;
    fctx.body.push({ op: "f64.const", value: 1 });
    fctx.body.push({ op: "local.set", index: resultLocal });
    fctx.body.push({ op: "br", depth: 2 });
    fctx.body = savedBody2;
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: foundBody });
  } else if (method === "find") {
    compileExpression3(ctx, fctx, bodyExpr);
    emitTruthyCoercion(fctx, inferExprType(ctx, fctx, bodyExpr));
    const foundBody = [];
    const savedBody2 = fctx.body;
    fctx.body = foundBody;
    fctx.body.push({ op: "local.get", index: elemLocal });
    fctx.body.push({ op: "local.set", index: resultLocal });
    fctx.body.push({ op: "br", depth: 2 });
    fctx.body = savedBody2;
    fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: foundBody });
  } else if (method === "flatMap") {
    const innerArrLocal = addLocal(fctx, `__hof_inner_${fctx.locals.length}`, { kind: "i32" });
    const jLocal = addLocal(fctx, `__hof_j_${fctx.locals.length}`, { kind: "i32" });
    const innerLenLocal = addLocal(fctx, `__hof_ilen_${fctx.locals.length}`, { kind: "i32" });
    compileExpression3(ctx, fctx, bodyExpr);
    const innerType = inferExprType(ctx, fctx, bodyExpr);
    if (innerType.kind === "f64") {
      fctx.body.push({ op: "i32.trunc_f64_s" });
    }
    fctx.body.push({ op: "local.set", index: innerArrLocal });
    fctx.body.push({ op: "local.get", index: innerArrLocal });
    fctx.body.push({ op: "call", funcIdx: arrLenIdx });
    fctx.body.push({ op: "local.set", index: innerLenLocal });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "local.set", index: jLocal });
    const innerLoopBody = [];
    innerLoopBody.push({ op: "local.get", index: jLocal });
    innerLoopBody.push({ op: "local.get", index: innerLenLocal });
    innerLoopBody.push({ op: "i32.ge_s" });
    innerLoopBody.push({ op: "br_if", depth: 1 });
    innerLoopBody.push({ op: "local.get", index: resultLocal });
    innerLoopBody.push({ op: "local.get", index: innerArrLocal });
    innerLoopBody.push({ op: "local.get", index: jLocal });
    innerLoopBody.push({ op: "call", funcIdx: arrGetIdx });
    innerLoopBody.push({ op: "call", funcIdx: arrPushIdx });
    innerLoopBody.push({ op: "local.get", index: jLocal });
    innerLoopBody.push({ op: "i32.const", value: 1 });
    innerLoopBody.push({ op: "i32.add" });
    innerLoopBody.push({ op: "local.set", index: jLocal });
    innerLoopBody.push({ op: "br", depth: 0 });
    fctx.body.push({
      op: "block",
      blockType: { kind: "empty" },
      body: [
        {
          op: "loop",
          blockType: { kind: "empty" },
          body: innerLoopBody,
        },
      ],
    });
  }
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: iLocal });
  fctx.body.push({ op: "br", depth: 0 });
  fctx.body = savedBody;
  fctx.blockDepth += 2;
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
  fctx.blockDepth -= 2;
  if (method === "filter" || method === "map" || method === "find" || method === "flatMap") {
    fctx.body.push({ op: "local.get", index: resultLocal });
  } else if (method === "some") {
    fctx.body.push({ op: "local.get", index: resultLocal });
  }
}
function compileArrayJoin2(ctx, fctx, expr, propAccess) {
  const arrLenIdx = ctx.funcMap.get("__arr_len");
  const arrGetIdx = ctx.funcMap.get("__arr_get");
  const strConcatIdx = ctx.funcMap.get("__str_concat");
  const sepArg = expr.arguments[0];
  const arrLocal = addLocal(fctx, `__join_arr_${fctx.locals.length}`, { kind: "i32" });
  const iLocal = addLocal(fctx, `__join_i_${fctx.locals.length}`, { kind: "i32" });
  const lenLocal = addLocal(fctx, `__join_len_${fctx.locals.length}`, { kind: "i32" });
  const resultLocal = addLocal(fctx, `__join_result_${fctx.locals.length}`, { kind: "i32" });
  const sepLocal = addLocal(fctx, `__join_sep_${fctx.locals.length}`, { kind: "i32" });
  compileExpression3(ctx, fctx, propAccess.expression);
  fctx.body.push({ op: "local.set", index: arrLocal });
  fctx.body.push({ op: "local.get", index: arrLocal });
  fctx.body.push({ op: "call", funcIdx: arrLenIdx });
  fctx.body.push({ op: "local.set", index: lenLocal });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: iLocal });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "local.set", index: resultLocal });
  if (sepArg) {
    compileExprToI32(ctx, fctx, sepArg);
  } else {
    fctx.body.push({ op: "i32.const", value: 0 });
  }
  fctx.body.push({ op: "local.set", index: sepLocal });
  const loopBody = [];
  const savedBody = fctx.body;
  fctx.body = loopBody;
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "local.get", index: lenLocal });
  fctx.body.push({ op: "i32.ge_s" });
  fctx.body.push({ op: "br_if", depth: 1 });
  const appendSepBody = [];
  const savedBody2 = fctx.body;
  fctx.body = appendSepBody;
  fctx.body.push({ op: "local.get", index: resultLocal });
  fctx.body.push({ op: "local.get", index: sepLocal });
  fctx.body.push({ op: "call", funcIdx: strConcatIdx });
  fctx.body.push({ op: "local.set", index: resultLocal });
  fctx.body = savedBody2;
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "i32.const", value: 0 });
  fctx.body.push({ op: "i32.gt_s" });
  fctx.body.push({ op: "if", blockType: { kind: "empty" }, then: appendSepBody });
  fctx.body.push({ op: "local.get", index: resultLocal });
  fctx.body.push({ op: "local.get", index: arrLocal });
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "call", funcIdx: arrGetIdx });
  fctx.body.push({ op: "call", funcIdx: strConcatIdx });
  fctx.body.push({ op: "local.set", index: resultLocal });
  fctx.body.push({ op: "local.get", index: iLocal });
  fctx.body.push({ op: "i32.const", value: 1 });
  fctx.body.push({ op: "i32.add" });
  fctx.body.push({ op: "local.set", index: iLocal });
  fctx.body.push({ op: "br", depth: 0 });
  fctx.body = savedBody;
  fctx.blockDepth += 2;
  fctx.body.push({
    op: "block",
    blockType: { kind: "empty" },
    body: [
      {
        op: "loop",
        blockType: { kind: "empty" },
        body: loopBody,
      },
    ],
  });
  fctx.blockDepth -= 2;
  fctx.body.push({ op: "local.get", index: resultLocal });
}
function compileUint8ArrayMethodCall(ctx, fctx, expr, propAccess, methodName) {
  if (methodName === "slice") {
    const sliceIdx = ctx.funcMap.get("__u8arr_slice");
    compileExpression3(ctx, fctx, propAccess.expression);
    if (expr.arguments.length >= 2) {
      compileExprToI32(ctx, fctx, expr.arguments[0]);
      compileExprToI32(ctx, fctx, expr.arguments[1]);
    } else if (expr.arguments.length === 1) {
      compileExprToI32(ctx, fctx, expr.arguments[0]);
      compileExpression3(ctx, fctx, propAccess.expression);
      const lenIdx = ctx.funcMap.get("__u8arr_len");
      fctx.body.push({ op: "call", funcIdx: lenIdx });
    } else {
      fctx.body.push({ op: "i32.const", value: 0 });
      compileExpression3(ctx, fctx, propAccess.expression);
      const lenIdx = ctx.funcMap.get("__u8arr_len");
      fctx.body.push({ op: "call", funcIdx: lenIdx });
    }
    fctx.body.push({ op: "call", funcIdx: sliceIdx });
  } else if (methodName === "set") {
    const u8LenIdx = ctx.funcMap.get("__u8arr_len");
    const destLocal = addLocal(fctx, `__u8set_dest_${fctx.locals.length}`, { kind: "i32" });
    const srcLocal = addLocal(fctx, `__u8set_src_${fctx.locals.length}`, { kind: "i32" });
    const lenLocal = addLocal(fctx, `__u8set_len_${fctx.locals.length}`, { kind: "i32" });
    const iLocal = addLocal(fctx, `__u8set_i_${fctx.locals.length}`, { kind: "i32" });
    compileExpression3(ctx, fctx, propAccess.expression);
    fctx.body.push({ op: "local.set", index: destLocal });
    compileExprToI32(ctx, fctx, expr.arguments[0]);
    fctx.body.push({ op: "local.set", index: srcLocal });
    fctx.body.push({ op: "local.get", index: srcLocal });
    fctx.body.push({ op: "call", funcIdx: u8LenIdx });
    fctx.body.push({ op: "local.set", index: lenLocal });
    fctx.body.push({ op: "i32.const", value: 0 });
    fctx.body.push({ op: "local.set", index: iLocal });
    fctx.body.push({
      op: "block",
      blockType: { kind: "empty" },
      body: [
        {
          op: "loop",
          blockType: { kind: "empty" },
          body: [
            { op: "local.get", index: iLocal },
            { op: "local.get", index: lenLocal },
            { op: "i32.ge_u" },
            { op: "br_if", depth: 1 },
            // dest[12+i] = src[12+i]
            { op: "local.get", index: destLocal },
            { op: "local.get", index: iLocal },
            { op: "i32.add" },
            { op: "local.get", index: srcLocal },
            { op: "local.get", index: iLocal },
            { op: "i32.add" },
            { op: "i32.load8_u", align: 0, offset: 12 },
            { op: "i32.store8", align: 0, offset: 12 },
            // i++
            { op: "local.get", index: iLocal },
            { op: "i32.const", value: 1 },
            { op: "i32.add" },
            { op: "local.set", index: iLocal },
            { op: "br", depth: 0 },
          ],
        },
      ],
    });
  } else {
    ctx.errors.push({
      message: `Unsupported Uint8Array method: .${methodName}()`,
      line: 0,
      column: 0,
    });
  }
}
function compileMapMethodCall(ctx, fctx, expr, propAccess, methodName) {
  if (methodName === "set") {
    const setIdx = ctx.funcMap.get("__nmap_set");
    compileExpression3(ctx, fctx, propAccess.expression);
    compileExprToI32(ctx, fctx, expr.arguments[0]);
    compileExprToI32(ctx, fctx, expr.arguments[1]);
    fctx.body.push({ op: "call", funcIdx: setIdx });
    fctx.body.push({ op: "f64.const", value: 0 });
  } else if (methodName === "get") {
    const getIdx = ctx.funcMap.get("__nmap_get");
    compileExpression3(ctx, fctx, propAccess.expression);
    compileExprToI32(ctx, fctx, expr.arguments[0]);
    fctx.body.push({ op: "call", funcIdx: getIdx });
    let valIsNum = true;
    try {
      const retType = ctx.checker.getTypeAtLocation(expr);
      const retStr = ctx.checker.typeToString(ctx.checker.getNonNullableType(retType));
      if (retStr !== "number" && retStr !== "boolean") valIsNum = false;
    } catch {}
    if (valIsNum) {
      fctx.body.push({ op: "f64.convert_i32_s" });
    }
  } else if (methodName === "has") {
    const hasIdx = ctx.funcMap.get("__nmap_has");
    compileExpression3(ctx, fctx, propAccess.expression);
    compileExprToI32(ctx, fctx, expr.arguments[0]);
    fctx.body.push({ op: "call", funcIdx: hasIdx });
    fctx.body.push({ op: "f64.convert_i32_s" });
  } else {
    ctx.errors.push({
      message: `Unsupported Map method: .${methodName}()`,
      line: 0,
      column: 0,
    });
  }
}
function compileSetMethodCall(ctx, fctx, expr, propAccess, methodName) {
  if (methodName === "add") {
    const addIdx = ctx.funcMap.get("__nset_add");
    compileExpression3(ctx, fctx, propAccess.expression);
    compileExprToI32(ctx, fctx, expr.arguments[0]);
    fctx.body.push({ op: "call", funcIdx: addIdx });
    fctx.body.push({ op: "f64.const", value: 0 });
  } else if (methodName === "has") {
    const hasIdx = ctx.funcMap.get("__nset_has");
    compileExpression3(ctx, fctx, propAccess.expression);
    compileExprToI32(ctx, fctx, expr.arguments[0]);
    fctx.body.push({ op: "call", funcIdx: hasIdx });
    fctx.body.push({ op: "f64.convert_i32_s" });
  } else {
    ctx.errors.push({
      message: `Unsupported Set method: .${methodName}()`,
      line: 0,
      column: 0,
    });
  }
}
function compileExprToI32(ctx, fctx, expr) {
  const exprType = inferExprType(ctx, fctx, expr);
  compileExpression3(ctx, fctx, expr);
  if (exprType.kind === "f64") {
    fctx.body.push({ op: "i32.trunc_f64_s" });
  }
}
function compileExprToF64(ctx, fctx, expr) {
  const exprType = inferExprType(ctx, fctx, expr);
  compileExpression3(ctx, fctx, expr);
  if (exprType.kind === "i32") {
    fctx.body.push({ op: "f64.convert_i32_s" });
  }
}
function inferExprType(ctx, fctx, expr) {
  if (ts16.isStringLiteral(expr) || ts16.isNoSubstitutionTemplateLiteral(expr) || ts16.isTemplateExpression(expr)) {
    return { kind: "i32" };
  }
  if (expr.kind === ts16.SyntaxKind.ThisKeyword) {
    return { kind: "i32" };
  }
  if (expr.kind === ts16.SyntaxKind.NullKeyword) {
    return { kind: "i32" };
  }
  if (ts16.isIdentifier(expr) && expr.text === "undefined") {
    return { kind: "i32" };
  }
  if (ts16.isArrayLiteralExpression(expr)) return { kind: "i32" };
  if (ts16.isObjectLiteralExpression(expr)) return { kind: "i32" };
  if (ts16.isNewExpression(expr) && ts16.isIdentifier(expr.expression)) {
    const name = expr.expression.text;
    if (
      name === "Uint8Array" ||
      name === "Map" ||
      name === "Set" ||
      name === "ArrayBuffer" ||
      name === "Float64Array" ||
      name === "Float32Array"
    ) {
      return { kind: "i32" };
    }
    if (ctx.classLayouts.has(name)) {
      return { kind: "i32" };
    }
  }
  if (ts16.isIdentifier(expr)) {
    const kind = getExprCollectionKind(ctx, fctx, expr);
    if (kind) return { kind: "i32" };
    const localIdx = fctx.localMap.get(expr.text);
    if (localIdx !== void 0) {
      if (localIdx < fctx.params.length) {
        return fctx.params[localIdx].type;
      } else {
        const localDef = fctx.locals[localIdx - fctx.params.length];
        if (localDef) return localDef.type;
      }
    }
    const gIdx = ctx.moduleGlobals.get(expr.text);
    if (gIdx !== void 0) {
      return ctx.mod.globals[gIdx].type;
    }
  }
  if (ts16.isPropertyAccessExpression(expr)) {
    const propName = expr.name.text;
    const objKind = getExprCollectionKind(ctx, fctx, expr.expression);
    if (
      (propName === "length" && (objKind === "Array" || objKind === "Uint8Array" || objKind === "ArrayOrUint8Array")) ||
      (propName === "size" && (objKind === "Map" || objKind === "Set"))
    ) {
      return { kind: "f64" };
    }
    if (propName === "length" && isStringExpr(ctx, fctx, expr.expression)) {
      return { kind: "f64" };
    }
    const className = inferClassName(ctx, fctx, expr.expression);
    if (className) {
      const layout = ctx.classLayouts.get(className);
      if (layout) {
        const field = layout.fields.get(propName);
        if (field) {
          return { kind: field.type };
        }
      }
    }
    try {
      const type = ctx.checker.getTypeAtLocation(expr);
      const baseType = ctx.checker.getNonNullableType(type);
      if (baseType.flags & (ts16.TypeFlags.NumberLike | ts16.TypeFlags.BooleanLike)) return { kind: "f64" };
      return { kind: "i32" };
    } catch {}
  }
  if (ts16.isNonNullExpression(expr)) {
    return inferExprType(ctx, fctx, expr.expression);
  }
  if (ts16.isParenthesizedExpression(expr)) {
    return inferExprType(ctx, fctx, expr.expression);
  }
  if (ts16.isElementAccessExpression(expr)) {
    try {
      const type = ctx.checker.getTypeAtLocation(expr);
      const typeStr = ctx.checker.typeToString(type);
      if (typeStr === "number" || typeStr === "boolean") return { kind: "f64" };
      return { kind: "i32" };
    } catch {}
  }
  if (ts16.isPostfixUnaryExpression(expr) || ts16.isPrefixUnaryExpression(expr)) {
    if (
      ts16.isPrefixUnaryExpression(expr) &&
      (expr.operator === ts16.SyntaxKind.ExclamationToken || expr.operator === ts16.SyntaxKind.TildeToken)
    ) {
      return { kind: "f64" };
    }
    return inferExprType(ctx, fctx, expr.operand);
  }
  if (ts16.isCallExpression(expr)) {
    try {
      const type = ctx.checker.getTypeAtLocation(expr);
      const baseType = ctx.checker.getNonNullableType(type);
      if (baseType.flags & (ts16.TypeFlags.NumberLike | ts16.TypeFlags.BooleanLike)) return { kind: "f64" };
      const typeStr = ctx.checker.typeToString(baseType);
      if (typeStr !== "void") return { kind: "i32" };
    } catch {}
  }
  if (ts16.isConditionalExpression(expr)) {
    return inferExprType(ctx, fctx, expr.whenTrue);
  }
  return { kind: "f64" };
}
function resolveType(_ctx, typeNode) {
  if (!typeNode) return null;
  const text = typeNode
    .getText()
    .replace(/\s*\|\s*(undefined|null)/g, "")
    .trim();
  switch (text) {
    case "number":
      return { kind: "f64" };
    case "boolean":
      return { kind: "f64" };
    // booleans as f64 (0.0/1.0)
    case "bigint":
      return { kind: "f64" };
    // bigints as f64 (best-effort)
    case "void":
      return null;
    case "string":
      return { kind: "i32" };
    // strings are pointers
    default:
      return { kind: "i32" };
  }
}
function resolveParamTypeFromChecker(ctx, param) {
  if (param.type) {
    const resolved = resolveType(ctx, param.type);
    if (resolved) return resolved;
  }
  try {
    const type = ctx.checker.getTypeAtLocation(param);
    const typeStr = ctx.checker.typeToString(type);
    if (typeStr === "number" || typeStr === "boolean" || typeStr === "bigint") return { kind: "f64" };
    if (typeStr === "void") return { kind: "f64" };
    return { kind: "i32" };
  } catch {
    return { kind: "f64" };
  }
}
function scanClassDeclaration(ctx, classDecl) {
  const className = classDecl.name.text;
  const fieldDefs = [];
  const seenFields = /* @__PURE__ */ new Set();
  const fieldCollectionKinds = /* @__PURE__ */ new Map();
  for (const member of classDecl.members) {
    if (ts16.isPropertyDeclaration(member) && member.name && ts16.isIdentifier(member.name)) {
      const fieldName = member.name.text;
      const fieldType = resolveFieldType(member.type);
      fieldDefs.push({ name: fieldName, type: fieldType });
      seenFields.add(fieldName);
      if (member.type) {
        const typeText = member.type.getText();
        if (typeText.endsWith("[]") || typeText.startsWith("Array<")) {
          fieldCollectionKinds.set(fieldName, "Array");
        } else if (typeText === "Uint8Array") {
          fieldCollectionKinds.set(fieldName, "Uint8Array");
        } else if (typeText.startsWith("Map<") || typeText === "Map") {
          fieldCollectionKinds.set(fieldName, "Map");
        } else if (typeText.startsWith("Set<") || typeText === "Set") {
          fieldCollectionKinds.set(fieldName, "Set");
        }
      }
    }
  }
  for (const member of classDecl.members) {
    if (ts16.isConstructorDeclaration(member) && member.body) {
      for (const stmt of member.body.statements) {
        if (ts16.isExpressionStatement(stmt) && ts16.isBinaryExpression(stmt.expression)) {
          const bin = stmt.expression;
          if (
            bin.operatorToken.kind === ts16.SyntaxKind.EqualsToken &&
            ts16.isPropertyAccessExpression(bin.left) &&
            bin.left.expression.kind === ts16.SyntaxKind.ThisKeyword &&
            ts16.isIdentifier(bin.left.name)
          ) {
            const fieldName = bin.left.name.text;
            if (!seenFields.has(fieldName)) {
              let fieldType = "f64";
              if (ts16.isIdentifier(bin.right)) {
                for (const p of member.parameters) {
                  if (ts16.isIdentifier(p.name) && p.name.text === bin.right.text && p.type) {
                    const resolved = resolveType(ctx, p.type);
                    if (resolved && resolved.kind === "i32") fieldType = "i32";
                  }
                }
              }
              fieldDefs.push({ name: fieldName, type: fieldType });
              seenFields.add(fieldName);
            }
          }
        }
      }
    }
  }
  const layout = computeClassLayout(className, fieldDefs);
  for (const [fieldName, kind] of fieldCollectionKinds) {
    layout.fieldCollectionKinds.set(fieldName, kind);
  }
  ctx.classLayouts.set(className, layout);
}
function resolveFieldType(typeNode) {
  if (!typeNode) return "f64";
  const text = typeNode.getText();
  switch (text) {
    case "number":
      return "f64";
    case "boolean":
      return "f64";
    default:
      return "i32";
  }
}
function compileClassDeclaration(ctx, classDecl) {
  const className = classDecl.name.text;
  const layout = ctx.classLayouts.get(className);
  let ctorDecl;
  for (const member of classDecl.members) {
    if (ts16.isConstructorDeclaration(member)) {
      ctorDecl = member;
      break;
    }
  }
  compileClassCtor(ctx, className, layout, ctorDecl, classDecl);
  for (const member of classDecl.members) {
    if (ts16.isMethodDeclaration(member) && member.name && ts16.isIdentifier(member.name)) {
      compileClassMethod(ctx, className, layout, member);
    }
    if (ts16.isGetAccessorDeclaration(member) && member.name && ts16.isIdentifier(member.name)) {
      compileClassGetter(ctx, className, layout, member);
    }
  }
}
function compileClassCtor(ctx, _className, layout, ctorDecl, classDecl) {
  const ctorName = layout.ctorFuncName;
  const params = [{ name: "this", type: { kind: "i32" } }];
  if (ctorDecl) {
    for (const p of ctorDecl.parameters) {
      const paramName = ts16.isIdentifier(p.name) ? p.name.text : "_";
      const type = resolveParamTypeFromChecker(ctx, p);
      params.push({ name: paramName, type });
    }
  }
  const paramTypes = params.map((p) => p.type);
  const typeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "func",
    name: `$type_${ctorName}`,
    params: paramTypes,
    results: [],
  });
  const fctx = {
    name: ctorName,
    params,
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: null,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    collectionTypes: /* @__PURE__ */ new Map(),
    callbackParams: /* @__PURE__ */ new Map(),
  };
  for (let i = 0; i < params.length; i++) {
    fctx.localMap.set(params[i].name, i);
  }
  ctx.currentFunc = fctx;
  if (classDecl) {
    for (const member of classDecl.members) {
      if (ts16.isPropertyDeclaration(member) && member.initializer && member.name && ts16.isIdentifier(member.name)) {
        const fieldName = member.name.text;
        const field = layout.fields.get(fieldName);
        if (field) {
          fctx.body.push({ op: "local.get", index: 0 });
          compileExpression3(ctx, fctx, member.initializer);
          const valType = inferExprType(ctx, fctx, member.initializer);
          if (field.type === "i32") {
            if (valType.kind !== "i32") {
              fctx.body.push({ op: "i32.trunc_f64_s" });
            }
            fctx.body.push({ op: "i32.store", align: 2, offset: field.offset });
          } else {
            if (valType.kind === "i32") {
              fctx.body.push({ op: "f64.convert_i32_s" });
            }
            fctx.body.push({ op: "f64.store", align: 3, offset: field.offset });
          }
        }
      }
    }
  }
  if (ctorDecl?.body) {
    for (const stmt of ctorDecl.body.statements) {
      compileStatement2(ctx, fctx, stmt);
    }
  }
  ctx.mod.functions.push({
    name: ctorName,
    typeIdx,
    locals: fctx.locals,
    body: fctx.body,
    exported: false,
  });
  ctx.currentFunc = null;
}
function compileClassMethod(ctx, _className, layout, methodDecl) {
  const methodName = methodDecl.name.text;
  const wasmMethodName = layout.methods.get(methodName);
  const params = [{ name: "this", type: { kind: "i32" } }];
  for (const p of methodDecl.parameters) {
    const paramName = ts16.isIdentifier(p.name) ? p.name.text : "_";
    const type = resolveParamTypeFromChecker(ctx, p);
    params.push({ name: paramName, type });
  }
  const returnType = resolveType(ctx, methodDecl.type);
  const isVoid = returnType === null;
  const paramTypes = params.map((p) => p.type);
  const resultTypes = isVoid ? [] : [returnType];
  const typeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "func",
    name: `$type_${wasmMethodName}`,
    params: paramTypes,
    results: resultTypes,
  });
  const fctx = {
    name: wasmMethodName,
    params,
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: isVoid ? null : returnType,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    collectionTypes: /* @__PURE__ */ new Map(),
    callbackParams: /* @__PURE__ */ new Map(),
  };
  for (let i = 0; i < params.length; i++) {
    fctx.localMap.set(params[i].name, i);
  }
  ctx.currentFunc = fctx;
  detectCallbackParams(ctx, fctx, methodDecl.parameters);
  detectParamCollectionTypes(ctx, fctx, methodDecl.parameters);
  if (methodDecl.body) {
    for (const stmt of methodDecl.body.statements) {
      compileStatement2(ctx, fctx, stmt);
    }
  }
  if (!isVoid) {
    fctx.body.push({ op: "unreachable" });
  }
  ctx.mod.functions.push({
    name: wasmMethodName,
    typeIdx,
    locals: fctx.locals,
    body: fctx.body,
    exported: false,
  });
  ctx.currentFunc = null;
}
function compileClassGetter(ctx, _className, layout, getterDecl) {
  const getterName = getterDecl.name.text;
  const wasmGetterName = layout.getters.get(getterName);
  const params = [{ name: "this", type: { kind: "i32" } }];
  const returnType = resolveType(ctx, getterDecl.type);
  const isVoid = returnType === null;
  const paramTypes = params.map((p) => p.type);
  const resultTypes = isVoid ? [] : [returnType];
  const typeIdx = ctx.mod.types.length;
  ctx.mod.types.push({
    kind: "func",
    name: `$type_${wasmGetterName}`,
    params: paramTypes,
    results: resultTypes,
  });
  const fctx = {
    name: wasmGetterName,
    params,
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: isVoid ? null : returnType,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    collectionTypes: /* @__PURE__ */ new Map(),
    callbackParams: /* @__PURE__ */ new Map(),
  };
  for (let i = 0; i < params.length; i++) {
    fctx.localMap.set(params[i].name, i);
  }
  ctx.currentFunc = fctx;
  if (getterDecl.body) {
    for (const stmt of getterDecl.body.statements) {
      compileStatement2(ctx, fctx, stmt);
    }
  }
  if (!isVoid) {
    fctx.body.push({ op: "unreachable" });
  }
  ctx.mod.functions.push({
    name: wasmGetterName,
    typeIdx,
    locals: fctx.locals,
    body: fctx.body,
    exported: false,
  });
  ctx.currentFunc = null;
}
function compileClassNewExpression(ctx, fctx, expr, className, layout) {
  const mallocIdx = ctx.funcMap.get("__malloc");
  const ctorIdx = ctx.funcMap.get(layout.ctorFuncName);
  const ptrLocal = addLocal(fctx, `$new_${className}`, { kind: "i32" });
  fctx.body.push({ op: "i32.const", value: layout.totalSize });
  fctx.body.push({ op: "call", funcIdx: mallocIdx });
  fctx.body.push({ op: "local.set", index: ptrLocal });
  fctx.body.push({ op: "local.get", index: ptrLocal });
  fctx.body.push({ op: "i32.const", value: CLASS_TYPE_TAG });
  fctx.body.push({ op: "i32.store8", align: 0, offset: 0 });
  fctx.body.push({ op: "local.get", index: ptrLocal });
  fctx.body.push({ op: "i32.const", value: layout.totalSize - 8 });
  fctx.body.push({ op: "i32.store", align: 2, offset: 4 });
  fctx.body.push({ op: "local.get", index: ptrLocal });
  const providedArgCount = expr.arguments ? expr.arguments.length : 0;
  if (expr.arguments) {
    for (const arg of expr.arguments) {
      compileExpression3(ctx, fctx, arg);
    }
  }
  const ctorTypeIdx = ctx.mod.functions.find((f) => f.name === layout.ctorFuncName)?.typeIdx;
  if (ctorTypeIdx !== void 0) {
    const ctorType = ctx.mod.types[ctorTypeIdx];
    if (ctorType && ctorType.kind === "func") {
      const expectedArgCount = ctorType.params.length - 1;
      for (let i = providedArgCount; i < expectedArgCount; i++) {
        const paramType = ctorType.params[i + 1];
        if (paramType.kind === "i32") {
          fctx.body.push({ op: "i32.const", value: 0 });
        } else {
          fctx.body.push({ op: "f64.const", value: 0 });
        }
      }
    }
  }
  fctx.body.push({ op: "call", funcIdx: ctorIdx });
  fctx.body.push({ op: "local.get", index: ptrLocal });
}
function compilePropertyAssignment2(ctx, fctx, propExpr, value) {
  const propName = propExpr.name.text;
  const className = inferClassName(ctx, fctx, propExpr.expression);
  if (className) {
    const layout = ctx.classLayouts.get(className);
    if (layout) {
      const field = layout.fields.get(propName);
      if (field) {
        compileExpression3(ctx, fctx, propExpr.expression);
        compileExpression3(ctx, fctx, value);
        const tempLocal = addLocal(fctx, `$prop_tmp`, field.type === "f64" ? { kind: "f64" } : { kind: "i32" });
        fctx.body.push({ op: "local.set", index: tempLocal });
        fctx.body.push({ op: "local.get", index: tempLocal });
        if (field.type === "f64") {
          fctx.body.push({ op: "f64.store", align: 3, offset: field.offset });
        } else {
          fctx.body.push({ op: "i32.store", align: 2, offset: field.offset });
        }
        fctx.body.push({ op: "local.get", index: tempLocal });
        return;
      }
    }
  }
  ctx.errors.push({
    message: `Unknown property assignment: .${propName}`,
    line: 0,
    column: 0,
  });
}
function inferClassName(ctx, fctx, expr) {
  if (expr.kind === ts16.SyntaxKind.ThisKeyword) {
    const funcName = fctx.name;
    for (const [className] of ctx.classLayouts) {
      if (funcName === `${className}_ctor` || funcName.startsWith(`${className}_`)) {
        return className;
      }
    }
    return void 0;
  }
  if (ts16.isIdentifier(expr)) {
    try {
      const type = ctx.checker.getTypeAtLocation(expr);
      const symbol = type.getSymbol();
      if (symbol) {
        const typeName = symbol.getName();
        if (ctx.classLayouts.has(typeName)) {
          return typeName;
        }
      }
    } catch {}
    return void 0;
  }
  if (ts16.isNewExpression(expr) && ts16.isIdentifier(expr.expression)) {
    const className = expr.expression.text;
    if (ctx.classLayouts.has(className)) {
      return className;
    }
  }
  return void 0;
}
function compileStringLiteral2(ctx, fctx, value) {
  const encoded = new TextEncoder().encode(value);
  let dataOffset = ctx.stringLiterals.get(value);
  if (dataOffset === void 0) {
    dataOffset = ctx.dataSegmentOffset;
    ctx.stringLiterals.set(value, dataOffset);
    ctx.dataSegmentOffset += encoded.length;
  }
  const strFromDataIdx = ctx.funcMap.get("__str_from_data");
  fctx.body.push({ op: "i32.const", value: dataOffset });
  fctx.body.push({ op: "i32.const", value: encoded.length });
  fctx.body.push({ op: "call", funcIdx: strFromDataIdx });
}
function compileTemplateExpression2(ctx, fctx, expr) {
  const strConcatIdx = ctx.funcMap.get("__str_concat");
  compileStringLiteral2(ctx, fctx, expr.head.text);
  for (const span of expr.templateSpans) {
    const spanExprType = inferExprType(ctx, fctx, span.expression);
    if (spanExprType.kind === "i32") {
      compileExpression3(ctx, fctx, span.expression);
    } else {
      const strFromI32Idx = ctx.funcMap.get("__str_from_i32");
      if (strFromI32Idx !== void 0) {
        compileExprToI32(ctx, fctx, span.expression);
        fctx.body.push({ op: "call", funcIdx: strFromI32Idx });
      } else {
        compileStringLiteral2(ctx, fctx, "");
      }
    }
    fctx.body.push({ op: "call", funcIdx: strConcatIdx });
    if (span.literal.text.length > 0) {
      compileStringLiteral2(ctx, fctx, span.literal.text);
      fctx.body.push({ op: "call", funcIdx: strConcatIdx });
    }
  }
}
function isStringExpr(ctx, fctx, expr) {
  if (ts16.isStringLiteral(expr) || ts16.isNoSubstitutionTemplateLiteral(expr) || ts16.isTemplateExpression(expr)) {
    return true;
  }
  try {
    const type = ctx.checker.getTypeAtLocation(expr);
    if (type.flags & ts16.TypeFlags.StringLike) {
      return true;
    }
    const nonNull = ctx.checker.getNonNullableType(type);
    if (nonNull.flags & ts16.TypeFlags.StringLike) {
      return true;
    }
  } catch {}
  return false;
}
function collectModuleGlobals(ctx, sf) {
  for (const stmt of sf.statements) {
    if (!ts16.isVariableStatement(stmt)) continue;
    if (stmt.modifiers?.some((m) => m.kind === ts16.SyntaxKind.DeclareKeyword)) continue;
    const isConst = (stmt.declarationList.flags & ts16.NodeFlags.Const) !== 0;
    for (const decl of stmt.declarationList.declarations) {
      if (!ts16.isIdentifier(decl.name)) continue;
      const name = decl.name.text;
      if (ctx.funcMap.has(name)) continue;
      if (ctx.moduleGlobals.has(name)) continue;
      if (ctx.classLayouts.has(name)) continue;
      const wasmType = inferDeclType(ctx, decl);
      let initInstr;
      if (isConst && decl.initializer && ts16.isNumericLiteral(decl.initializer)) {
        const val = Number(decl.initializer.text);
        initInstr = wasmType.kind === "i32" ? [{ op: "i32.const", value: val | 0 }] : [{ op: "f64.const", value: val }];
      } else {
        initInstr = wasmType.kind === "i32" ? [{ op: "i32.const", value: 0 }] : [{ op: "f64.const", value: 0 }];
      }
      const globalIdx = ctx.mod.globals.length;
      ctx.mod.globals.push({
        name: `__mod_${name}`,
        type: wasmType,
        mutable: !isConst || !decl.initializer || !ts16.isNumericLiteral(decl.initializer),
        init: initInstr,
      });
      ctx.moduleGlobals.set(name, globalIdx);
      if (decl.initializer) {
        if (ts16.isNewExpression(decl.initializer) && ts16.isIdentifier(decl.initializer.expression)) {
          const ctorName = decl.initializer.expression.text;
          if (ctorName === "Set") ctx.moduleCollectionTypes.set(name, "Set");
          else if (ctorName === "Map") ctx.moduleCollectionTypes.set(name, "Map");
          else if (ctorName === "Uint8Array") ctx.moduleCollectionTypes.set(name, "Uint8Array");
        } else if (ts16.isArrayLiteralExpression(decl.initializer)) {
          ctx.moduleCollectionTypes.set(name, "Array");
        }
      }
      if (decl.type) {
        const text = decl.type.getText();
        if (text.startsWith("Set<") || text === "Set") ctx.moduleCollectionTypes.set(name, "Set");
        else if (text.startsWith("Map<") || text === "Map") ctx.moduleCollectionTypes.set(name, "Map");
        else if (text === "Uint8Array") ctx.moduleCollectionTypes.set(name, "Uint8Array");
        else if (text.endsWith("[]") || text.startsWith("Array<")) ctx.moduleCollectionTypes.set(name, "Array");
      }
    }
  }
}
function inferDeclType(ctx, decl) {
  if (decl.initializer) {
    if (ts16.isNewExpression(decl.initializer) && ts16.isIdentifier(decl.initializer.expression)) {
      return { kind: "i32" };
    }
  }
  try {
    const type = ctx.checker.getTypeAtLocation(decl);
    const baseType = ctx.checker.getNonNullableType(type);
    if (baseType.flags & (ts16.TypeFlags.NumberLike | ts16.TypeFlags.BooleanLike)) return { kind: "f64" };
    return { kind: "i32" };
  } catch {}
  return { kind: "f64" };
}
function emitClosureTable(ctx) {
  if (ctx.tableEntries.length === 0) return;
  ctx.mod.tables.push({
    elementType: "funcref",
    min: ctx.tableEntries.length,
    max: ctx.tableEntries.length,
  });
  ctx.mod.elements.push({
    tableIdx: 0,
    offset: [{ op: "i32.const", value: 0 }],
    funcIndices: ctx.tableEntries,
  });
}
function detectParamCollectionTypes(ctx, fctx, params) {
  for (const param of params) {
    if (!ts16.isIdentifier(param.name)) continue;
    const paramName = param.name.text;
    if (param.type) {
      const text = param.type.getText();
      if (text === "number[] | Uint8Array" || text === "Uint8Array | number[]") {
        fctx.collectionTypes.set(paramName, "ArrayOrUint8Array");
        continue;
      }
      if (text === "number[]" || text.endsWith("[]") || text.startsWith("Array<")) {
        fctx.collectionTypes.set(paramName, "Array");
        continue;
      }
      if (text === "Uint8Array") {
        fctx.collectionTypes.set(paramName, "Uint8Array");
        continue;
      }
      if (text.startsWith("Map<") || text === "Map") {
        fctx.collectionTypes.set(paramName, "Map");
        continue;
      }
      if (text.startsWith("Set<") || text === "Set") {
        fctx.collectionTypes.set(paramName, "Set");
        continue;
      }
    }
    try {
      const type = ctx.checker.getTypeAtLocation(param);
      const typeStr = ctx.checker.typeToString(type);
      if (typeStr === "number[] | Uint8Array" || typeStr === "Uint8Array | number[]") {
        fctx.collectionTypes.set(paramName, "ArrayOrUint8Array");
        continue;
      }
      if (typeStr === "Uint8Array") {
        fctx.collectionTypes.set(paramName, "Uint8Array");
        continue;
      }
      if (typeStr.startsWith("Map<")) {
        fctx.collectionTypes.set(paramName, "Map");
        continue;
      }
      if (typeStr.startsWith("Set<")) {
        fctx.collectionTypes.set(paramName, "Set");
        continue;
      }
      if (typeStr.endsWith("[]") || typeStr.startsWith("Array<")) {
        fctx.collectionTypes.set(paramName, "Array");
      }
    } catch {}
  }
}
function detectCallbackParams(ctx, fctx, params) {
  for (const param of params) {
    if (!param.type || !ts16.isIdentifier(param.name)) continue;
    if (ts16.isFunctionTypeNode(param.type)) {
      const paramName = param.name.text;
      const cbParams = [];
      for (const p of param.type.parameters) {
        cbParams.push(resolveParamTypeFromChecker(ctx, p));
      }
      const cbReturn = resolveType(ctx, param.type.type);
      const cbResults = cbReturn ? [cbReturn] : [];
      let typeIdx = -1;
      for (let ti = 0; ti < ctx.mod.types.length; ti++) {
        const t = ctx.mod.types[ti];
        if (t.kind !== "func") continue;
        if (
          t.params.length === cbParams.length &&
          t.results.length === cbResults.length &&
          t.params.every((p, j) => p.kind === cbParams[j].kind) &&
          t.results.every((r, j) => r.kind === cbResults[j].kind)
        ) {
          typeIdx = ti;
          break;
        }
      }
      if (typeIdx < 0) {
        typeIdx = ctx.mod.types.length;
        ctx.mod.types.push({
          kind: "func",
          name: `$cb_type_${paramName}`,
          params: cbParams,
          results: cbResults,
        });
      }
      fctx.callbackParams.set(paramName, typeIdx);
    }
  }
}
function compileArrowFunctionArg(ctx, outerFctx, arrow) {
  const lambdaName = `$lambda_${ctx.lambdaCounter++}`;
  const captures = [];
  const capturedNames = /* @__PURE__ */ new Set();
  function scanCaptures(node) {
    if (ts16.isIdentifier(node)) {
      const name = node.text;
      if (capturedNames.has(name)) return;
      const isArrowParam = arrow.parameters.some((p) => ts16.isIdentifier(p.name) && p.name.text === name);
      if (!isArrowParam) {
        const outerIdx = outerFctx.localMap.get(name);
        if (outerIdx !== void 0) {
          capturedNames.add(name);
          const outerType =
            outerIdx < outerFctx.params.length
              ? outerFctx.params[outerIdx].type
              : (outerFctx.locals[outerIdx - outerFctx.params.length]?.type ?? { kind: "f64" });
          captures.push({ name, outerIdx, type: outerType });
        }
      }
    } else if (node.kind === ts16.SyntaxKind.ThisKeyword) {
      if (!capturedNames.has("this")) {
        const outerIdx = outerFctx.localMap.get("this");
        if (outerIdx !== void 0) {
          capturedNames.add("this");
          captures.push({ name: "this", outerIdx, type: { kind: "i32" } });
        }
      }
    }
    ts16.forEachChild(node, scanCaptures);
  }
  if (arrow.body) scanCaptures(arrow.body);
  const params = [];
  for (const p of arrow.parameters) {
    const paramName = ts16.isIdentifier(p.name) ? p.name.text : "_";
    const type = resolveParamTypeFromChecker(ctx, p);
    params.push({ name: paramName, type });
  }
  const returnType = arrow.type ? resolveType(ctx, arrow.type) : null;
  const isVoid = returnType === null;
  const paramTypes = params.map((p) => p.type);
  const resultTypes = isVoid ? [] : [returnType];
  let typeIdx = -1;
  for (let ti = 0; ti < ctx.mod.types.length; ti++) {
    const t = ctx.mod.types[ti];
    if (t.kind !== "func") continue;
    if (
      t.params.length === paramTypes.length &&
      t.results.length === resultTypes.length &&
      t.params.every((p, j) => p.kind === paramTypes[j].kind) &&
      t.results.every((r, j) => r.kind === resultTypes[j].kind)
    ) {
      typeIdx = ti;
      break;
    }
  }
  if (typeIdx < 0) {
    typeIdx = ctx.mod.types.length;
    ctx.mod.types.push({
      kind: "func",
      name: `$type_${lambdaName}`,
      params: paramTypes,
      results: resultTypes,
    });
  }
  const fctx = {
    name: lambdaName,
    params,
    locals: [],
    localMap: /* @__PURE__ */ new Map(),
    returnType: isVoid ? null : returnType,
    body: [],
    blockDepth: 0,
    breakStack: [],
    continueStack: [],
    collectionTypes: /* @__PURE__ */ new Map(),
    callbackParams: /* @__PURE__ */ new Map(),
  };
  for (let i = 0; i < params.length; i++) {
    fctx.localMap.set(params[i].name, i);
  }
  if (captures.length > 0) {
    const envLocal = addLocal(fctx, "$env", { kind: "i32" });
    fctx.body.push({ op: "global.get", index: ctx.closureEnvGlobalIdx });
    fctx.body.push({ op: "local.set", index: envLocal });
    for (let i = 0; i < captures.length; i++) {
      const cap = captures[i];
      const capLocal = addLocal(fctx, cap.name, cap.type);
      fctx.body.push({ op: "local.get", index: envLocal });
      if (cap.type.kind === "f64") {
        fctx.body.push({ op: "f64.load", align: 3, offset: i * 8 });
      } else {
        fctx.body.push({ op: "i32.load", align: 2, offset: i * 8 });
      }
      fctx.body.push({ op: "local.set", index: capLocal });
      const outerCollKind = outerFctx.collectionTypes.get(cap.name);
      if (outerCollKind) {
        fctx.collectionTypes.set(cap.name, outerCollKind);
      }
    }
  }
  ctx.currentFunc = fctx;
  if (ts16.isBlock(arrow.body)) {
    for (const stmt of arrow.body.statements) {
      compileStatement2(ctx, fctx, stmt);
    }
  } else {
    compileExpression3(ctx, fctx, arrow.body);
  }
  if (!isVoid) {
    fctx.body.push({ op: "unreachable" });
  }
  const funcIdx = ctx.numImportFuncs + ctx.mod.functions.length;
  ctx.funcMap.set(lambdaName, funcIdx);
  ctx.mod.functions.push({
    name: lambdaName,
    typeIdx,
    locals: fctx.locals,
    body: fctx.body,
    exported: false,
  });
  const tableIdx = ctx.tableEntries.length;
  ctx.tableEntries.push(funcIdx);
  ctx.currentFunc = outerFctx;
  return tableIdx;
}
function emitClosureSetup(ctx, fctx, arrow) {
  const tableIdx = compileArrowFunctionArg(ctx, fctx, arrow);
  const captures = [];
  const capturedNames = /* @__PURE__ */ new Set();
  function scanCaptures(node) {
    if (ts16.isIdentifier(node)) {
      const name = node.text;
      if (capturedNames.has(name)) return;
      const isArrowParam = arrow.parameters.some((p) => ts16.isIdentifier(p.name) && p.name.text === name);
      if (!isArrowParam) {
        const outerIdx = fctx.localMap.get(name);
        if (outerIdx !== void 0) {
          capturedNames.add(name);
          const outerType =
            outerIdx < fctx.params.length
              ? fctx.params[outerIdx].type
              : (fctx.locals[outerIdx - fctx.params.length]?.type ?? { kind: "f64" });
          captures.push({ name, outerIdx, type: outerType });
        }
      }
    } else if (node.kind === ts16.SyntaxKind.ThisKeyword) {
      if (!capturedNames.has("this")) {
        const outerIdx = fctx.localMap.get("this");
        if (outerIdx !== void 0) {
          capturedNames.add("this");
          captures.push({ name: "this", outerIdx, type: { kind: "i32" } });
        }
      }
    }
    ts16.forEachChild(node, scanCaptures);
  }
  if (arrow.body) scanCaptures(arrow.body);
  if (captures.length > 0) {
    const envSize = captures.length * 8;
    const envLocal = addLocal(fctx, `$env_${ctx.lambdaCounter}`, { kind: "i32" });
    fctx.body.push({ op: "i32.const", value: envSize });
    const mallocIdx = ctx.funcMap.get("__malloc");
    fctx.body.push({ op: "call", funcIdx: mallocIdx });
    fctx.body.push({ op: "local.set", index: envLocal });
    for (let i = 0; i < captures.length; i++) {
      const cap = captures[i];
      fctx.body.push({ op: "local.get", index: envLocal });
      fctx.body.push({ op: "local.get", index: cap.outerIdx });
      if (cap.type.kind === "f64") {
        fctx.body.push({ op: "f64.store", align: 3, offset: i * 8 });
      } else {
        fctx.body.push({ op: "i32.store", align: 2, offset: i * 8 });
      }
    }
    fctx.body.push({ op: "local.get", index: envLocal });
    fctx.body.push({ op: "global.set", index: ctx.closureEnvGlobalIdx });
  }
  fctx.body.push({ op: "i32.const", value: tableIdx });
}

// src/emit/encoder.ts
var WasmEncoder = class _WasmEncoder {
  buf = [];
  byte(b) {
    this.buf.push(b & 255);
  }
  bytes(bs) {
    for (const b of bs) this.byte(b);
  }
  /** Unsigned LEB128 */
  u32(value) {
    do {
      let b = value & 127;
      value >>>= 7;
      if (value !== 0) b |= 128;
      this.byte(b);
    } while (value !== 0);
  }
  /** Signed LEB128 */
  i32(value) {
    let more = true;
    while (more) {
      let b = value & 127;
      value >>= 7;
      if ((value === 0 && (b & 64) === 0) || (value === -1 && (b & 64) !== 0)) {
        more = false;
      } else {
        b |= 128;
      }
      this.byte(b);
    }
  }
  /** Signed LEB128 i64 — truncate to 64 bits to prevent overflow */
  i64(value) {
    value = BigInt.asIntN(64, value);
    let more = true;
    while (more) {
      let b = Number(value & 0x7fn);
      value >>= 7n;
      if ((value === 0n && (b & 64) === 0) || (value === -1n && (b & 64) !== 0)) {
        more = false;
      } else {
        b |= 128;
      }
      this.byte(b);
    }
  }
  /** IEEE 754 f64 little-endian */
  f64(value) {
    const buf = new ArrayBuffer(8);
    new Float64Array(buf)[0] = value;
    this.bytes(new Uint8Array(buf));
  }
  /** IEEE 754 f32 little-endian */
  f32(value) {
    const buf = new ArrayBuffer(4);
    new Float32Array(buf)[0] = value;
    this.bytes(new Uint8Array(buf));
  }
  /** v128 constant — 16 bytes little-endian */
  v128(bytes) {
    if (bytes.length !== 16) throw new Error("v128 must be exactly 16 bytes");
    this.bytes(bytes);
  }
  /** UTF-8 string with length prefix */
  name(s) {
    const encoded = new TextEncoder().encode(s);
    this.u32(encoded.length);
    this.bytes(encoded);
  }
  /** Section: id + length-prefixed content */
  section(id, content) {
    const sub2 = new _WasmEncoder();
    content(sub2);
    const data = sub2.finish();
    this.byte(id);
    this.u32(data.length);
    this.bytes(data);
  }
  /** Vector: u32 count + items */
  vector(items, encode) {
    this.u32(items.length);
    for (const item of items) encode(item, this);
  }
  /** Get current buffer length */
  get length() {
    return this.buf.length;
  }
  /** Get current write position (alias for length, used by relocation tracking) */
  get position() {
    return this.buf.length;
  }
  finish() {
    return new Uint8Array(this.buf);
  }
};

// src/emit/opcodes.ts
var OP = {
  unreachable: 0,
  nop: 1,
  block: 2,
  loop: 3,
  if: 4,
  else: 5,
  try: 6,
  catch: 7,
  throw: 8,
  rethrow: 9,
  end: 11,
  br: 12,
  br_if: 13,
  return: 15,
  call: 16,
  call_indirect: 17,
  return_call: 18,
  return_call_indirect: 19,
  delegate: 24,
  catch_all: 25,
  drop: 26,
  select: 27,
  local_get: 32,
  local_set: 33,
  local_tee: 34,
  global_get: 35,
  global_set: 36,
  table_get: 37,
  table_set: 38,
  i32_load: 40,
  i32_load8_s: 44,
  i32_load8_u: 45,
  i32_load16_u: 47,
  i32_store: 54,
  i32_store8: 58,
  i32_store16: 59,
  f32_load: 42,
  f64_load: 43,
  f32_store: 56,
  f64_store: 57,
  i32_const: 65,
  i64_const: 66,
  f32_const: 67,
  f64_const: 68,
  i32_eqz: 69,
  i32_eq: 70,
  i32_ne: 71,
  i32_lt_s: 72,
  i32_lt_u: 73,
  i32_gt_s: 74,
  i32_gt_u: 75,
  i32_le_s: 76,
  i32_le_u: 77,
  i32_ge_s: 78,
  i32_ge_u: 79,
  i32_add: 106,
  i32_sub: 107,
  i32_mul: 108,
  i32_div_s: 109,
  i32_div_u: 110,
  i32_rem_s: 111,
  i32_rem_u: 112,
  i32_and: 113,
  i32_or: 114,
  i32_xor: 115,
  i32_shl: 116,
  i32_shr_s: 117,
  i32_shr_u: 118,
  i32_clz: 103,
  f64_eq: 97,
  f64_ne: 98,
  f64_lt: 99,
  f64_gt: 100,
  f64_le: 101,
  f64_ge: 102,
  f64_abs: 153,
  f64_neg: 154,
  f64_ceil: 155,
  f64_floor: 156,
  f64_trunc: 157,
  f64_nearest: 158,
  f64_sqrt: 159,
  f64_add: 160,
  f64_sub: 161,
  f64_mul: 162,
  f64_div: 163,
  f64_min: 164,
  f64_max: 165,
  f64_copysign: 166,
  i32_trunc_f64_s: 170,
  f32_demote_f64: 182,
  f64_promote_f32: 187,
  // i64 comparison
  i64_eqz: 80,
  i64_eq: 81,
  i64_ne: 82,
  i64_lt_s: 83,
  i64_lt_u: 84,
  i64_gt_s: 85,
  i64_gt_u: 86,
  i64_le_s: 87,
  i64_le_u: 88,
  i64_ge_s: 89,
  i64_ge_u: 90,
  // i64 arithmetic
  i64_add: 124,
  i64_sub: 125,
  i64_mul: 126,
  i64_div_s: 127,
  i64_div_u: 128,
  i64_rem_s: 129,
  i64_rem_u: 130,
  i64_and: 131,
  i64_or: 132,
  i64_xor: 133,
  i64_shl: 134,
  i64_shr_s: 135,
  i64_shr_u: 136,
  // i64 conversions
  i64_extend_i32_s: 172,
  i64_extend_i32_u: 173,
  i64_trunc_f64_s: 176,
  f64_convert_i64_s: 185,
  f64_convert_i32_s: 183,
  f64_convert_i32_u: 184,
  // reinterpret (bit-cast) conversions
  i64_reinterpret_f64: 189,
  f64_reinterpret_i64: 191,
  ref_null: 208,
  ref_is_null: 209,
  ref_eq: 211,
  ref_as_non_null: 212,
  ref_func: 210,
  call_ref: 20,
  return_call_ref: 21,
  memory_size: 63,
  memory_grow: 64,
  misc_prefix: 252,
  i32_trunc_sat_f64_s: 2,
  i32_trunc_sat_f64_u: 3,
  i64_trunc_sat_f64_s: 6,
  i64_trunc_sat_f64_u: 7,
  /** SIMD prefix byte — all SIMD instructions start with 0xFD */
  simd_prefix: 253,
};
var SIMD = {
  // Memory operations
  v128_load: 0,
  v128_load8x8_s: 1,
  v128_load8x8_u: 2,
  v128_load16x4_s: 3,
  v128_load16x4_u: 4,
  v128_load32x2_s: 5,
  v128_load32x2_u: 6,
  v128_load8_splat: 7,
  v128_load16_splat: 8,
  v128_load32_splat: 9,
  v128_load64_splat: 10,
  v128_store: 11,
  // Constants
  v128_const: 12,
  // Shuffle / swizzle
  i8x16_shuffle: 13,
  i8x16_swizzle: 14,
  // Splat
  i8x16_splat: 15,
  i16x8_splat: 16,
  i32x4_splat: 17,
  i64x2_splat: 18,
  f32x4_splat: 19,
  f64x2_splat: 20,
  // Extract / replace lane
  i8x16_extract_lane_s: 21,
  i8x16_extract_lane_u: 22,
  i8x16_replace_lane: 23,
  i16x8_extract_lane_s: 24,
  i16x8_extract_lane_u: 25,
  i16x8_replace_lane: 26,
  i32x4_extract_lane: 27,
  i32x4_replace_lane: 28,
  i64x2_extract_lane: 29,
  i64x2_replace_lane: 30,
  f32x4_extract_lane: 31,
  f32x4_replace_lane: 32,
  f64x2_extract_lane: 33,
  f64x2_replace_lane: 34,
  // i8x16 comparisons
  i8x16_eq: 35,
  i8x16_ne: 36,
  i8x16_lt_s: 37,
  i8x16_lt_u: 38,
  i8x16_gt_s: 39,
  i8x16_gt_u: 40,
  i8x16_le_s: 41,
  i8x16_le_u: 42,
  i8x16_ge_s: 43,
  i8x16_ge_u: 44,
  // i16x8 comparisons
  i16x8_eq: 45,
  i16x8_ne: 46,
  i16x8_lt_s: 47,
  i16x8_lt_u: 48,
  i16x8_gt_s: 49,
  i16x8_gt_u: 50,
  i16x8_le_s: 51,
  i16x8_le_u: 52,
  i16x8_ge_s: 53,
  i16x8_ge_u: 54,
  // i32x4 comparisons
  i32x4_eq: 55,
  i32x4_ne: 56,
  i32x4_lt_s: 57,
  i32x4_lt_u: 58,
  i32x4_gt_s: 59,
  i32x4_gt_u: 60,
  i32x4_le_s: 61,
  i32x4_le_u: 62,
  i32x4_ge_s: 63,
  i32x4_ge_u: 64,
  // f32x4 comparisons
  f32x4_eq: 65,
  f32x4_ne: 66,
  f32x4_lt: 67,
  f32x4_gt: 68,
  f32x4_le: 69,
  f32x4_ge: 70,
  // f64x2 comparisons
  f64x2_eq: 71,
  f64x2_ne: 72,
  f64x2_lt: 73,
  f64x2_gt: 74,
  f64x2_le: 75,
  f64x2_ge: 76,
  // v128 bitwise
  v128_not: 77,
  v128_and: 78,
  v128_andnot: 79,
  v128_or: 80,
  v128_xor: 81,
  v128_bitselect: 82,
  v128_any_true: 83,
  // i8x16 arithmetic
  i8x16_abs: 96,
  i8x16_neg: 97,
  i8x16_popcnt: 98,
  i8x16_all_true: 99,
  i8x16_bitmask: 100,
  i8x16_narrow_i16x8_s: 101,
  i8x16_narrow_i16x8_u: 102,
  i8x16_shl: 107,
  i8x16_shr_s: 108,
  i8x16_shr_u: 109,
  i8x16_add: 110,
  i8x16_add_sat_s: 111,
  i8x16_add_sat_u: 112,
  i8x16_sub: 113,
  i8x16_sub_sat_s: 114,
  i8x16_sub_sat_u: 115,
  i8x16_min_s: 118,
  i8x16_min_u: 119,
  i8x16_max_s: 120,
  i8x16_max_u: 121,
  // i16x8 arithmetic
  i16x8_abs: 128,
  i16x8_neg: 129,
  i16x8_all_true: 131,
  i16x8_bitmask: 132,
  i16x8_narrow_i32x4_s: 133,
  i16x8_narrow_i32x4_u: 134,
  i16x8_extend_low_i8x16_s: 135,
  i16x8_extend_high_i8x16_s: 136,
  i16x8_extend_low_i8x16_u: 137,
  i16x8_extend_high_i8x16_u: 138,
  i16x8_shl: 139,
  i16x8_shr_s: 140,
  i16x8_shr_u: 141,
  i16x8_add: 142,
  i16x8_add_sat_s: 143,
  i16x8_add_sat_u: 144,
  i16x8_sub: 145,
  i16x8_sub_sat_s: 146,
  i16x8_sub_sat_u: 147,
  i16x8_mul: 149,
  i16x8_min_s: 150,
  i16x8_min_u: 151,
  i16x8_max_s: 152,
  i16x8_max_u: 153,
  // i32x4 arithmetic
  i32x4_abs: 160,
  i32x4_neg: 161,
  i32x4_all_true: 163,
  i32x4_bitmask: 164,
  i32x4_extend_low_i16x8_s: 167,
  i32x4_extend_high_i16x8_s: 168,
  i32x4_extend_low_i16x8_u: 169,
  i32x4_extend_high_i16x8_u: 170,
  i32x4_shl: 171,
  i32x4_shr_s: 172,
  i32x4_shr_u: 173,
  i32x4_add: 174,
  i32x4_sub: 177,
  i32x4_mul: 181,
  i32x4_min_s: 182,
  i32x4_min_u: 183,
  i32x4_max_s: 184,
  i32x4_max_u: 185,
  // i64x2 arithmetic
  i64x2_abs: 192,
  i64x2_neg: 193,
  i64x2_all_true: 195,
  i64x2_bitmask: 196,
  i64x2_shl: 203,
  i64x2_shr_s: 204,
  i64x2_shr_u: 205,
  i64x2_add: 206,
  i64x2_sub: 209,
  i64x2_mul: 213,
  i64x2_eq: 214,
  i64x2_ne: 215,
  i64x2_lt_s: 216,
  i64x2_gt_s: 217,
  i64x2_le_s: 218,
  i64x2_ge_s: 219,
  // f32x4 arithmetic
  f32x4_abs: 224,
  f32x4_neg: 225,
  f32x4_sqrt: 227,
  f32x4_add: 228,
  f32x4_sub: 229,
  f32x4_mul: 230,
  f32x4_div: 231,
  f32x4_min: 232,
  f32x4_max: 233,
  f32x4_pmin: 234,
  f32x4_pmax: 235,
  // f64x2 arithmetic
  f64x2_abs: 236,
  f64x2_neg: 237,
  f64x2_sqrt: 239,
  f64x2_add: 240,
  f64x2_sub: 241,
  f64x2_mul: 242,
  f64x2_div: 243,
  f64x2_min: 244,
  f64x2_max: 245,
  f64x2_pmin: 246,
  f64x2_pmax: 247,
  // Conversions
  i32x4_trunc_sat_f32x4_s: 248,
  i32x4_trunc_sat_f32x4_u: 249,
  f32x4_convert_i32x4_s: 250,
  f32x4_convert_i32x4_u: 251,
  i32x4_trunc_sat_f64x2_s_zero: 252,
  i32x4_trunc_sat_f64x2_u_zero: 253,
  f64x2_convert_low_i32x4_s: 254,
  f64x2_convert_low_i32x4_u: 255,
  // Load lane / store lane (multi-byte opcodes)
  v128_load8_lane: 84,
  v128_load16_lane: 85,
  v128_load32_lane: 86,
  v128_load64_lane: 87,
  v128_store8_lane: 88,
  v128_store16_lane: 89,
  v128_store32_lane: 90,
  v128_store64_lane: 91,
  v128_load32_zero: 92,
  v128_load64_zero: 93,
};
var GC = {
  prefix: 251,
  struct_new: 0,
  struct_new_default: 1,
  struct_get: 2,
  struct_get_s: 3,
  struct_get_u: 4,
  struct_set: 5,
  array_new: 6,
  array_new_default: 7,
  array_new_fixed: 8,
  array_get: 11,
  array_get_s: 12,
  array_get_u: 13,
  array_set: 14,
  array_len: 15,
  array_fill: 16,
  array_copy: 17,
  ref_test: 20,
  ref_test_null: 21,
  ref_cast: 22,
  ref_cast_null: 23,
  any_convert_extern: 26,
  extern_convert_any: 27,
};
var TYPE = {
  i32: 127,
  i64: 126,
  f32: 125,
  f64: 124,
  funcref: 112,
  externref: 111,
  struct: 95,
  array: 94,
  func: 96,
  rec: 78,
  sub: 80,
  sub_final: 79,
  ref: 100,
  ref_null: 99,
  any: 110,
  eq: 109,
  i31: 108,
  struct_ht: 107,
  array_ht: 106,
  none: 110,
  // alias – bottom of ref hierarchy
  v128: 123,
  i8: 120,
  i16: 119,
  const_field: 0,
  mut_field: 1,
};
var SECTION = {
  custom: 0,
  type: 1,
  import: 2,
  function: 3,
  table: 4,
  memory: 5,
  global: 6,
  export: 7,
  start: 8,
  element: 9,
  code: 10,
  data: 11,
  tag: 13,
};

// src/emit/binary.ts
function emitBinary(mod) {
  return emitBinaryWithSourceMap(mod).binary;
}
function emitBinaryWithSourceMap(mod) {
  const enc = new WasmEncoder();
  const sourceMapEntries = [];
  enc.bytes([0, 97, 115, 109]);
  enc.bytes([1, 0, 0, 0]);
  const numImportFuncs = mod.imports.filter((i) => i.desc.kind === "func").length;
  if (mod.types.length > 0) {
    enc.section(SECTION.type, (s) => {
      s.vector(mod.types, (t, e) => encodeTypeDef(t, e));
    });
  }
  if (mod.imports.length > 0) {
    enc.section(SECTION.import, (s) => {
      s.vector(mod.imports, (imp, e) => encodeImport(imp, e));
    });
  }
  if (mod.functions.length > 0) {
    enc.section(SECTION.function, (s) => {
      s.vector(mod.functions, (f, e) => e.u32(f.typeIdx));
    });
  }
  if (mod.tables.length > 0) {
    enc.section(SECTION.table, (s) => {
      s.vector(mod.tables, (t, e) => {
        e.byte(t.elementType === "funcref" ? TYPE.funcref : TYPE.externref);
        if (t.max !== void 0) {
          e.byte(1);
          e.u32(t.min);
          e.u32(t.max);
        } else {
          e.byte(0);
          e.u32(t.min);
        }
      });
    });
  }
  if (mod.memories && mod.memories.length > 0) {
    enc.section(SECTION.memory, (s) => {
      s.u32(mod.memories.length);
      for (const mem of mod.memories) {
        if (mem.max !== void 0) {
          s.byte(1);
          s.u32(mem.min);
          s.u32(mem.max);
        } else {
          s.byte(0);
          s.u32(mem.min);
        }
      }
    });
  }
  if (mod.tags.length > 0) {
    enc.section(SECTION.tag, (s) => {
      s.vector(mod.tags, (tag, e) => {
        e.byte(0);
        e.u32(tag.typeIdx);
      });
    });
  }
  if (mod.globals.length > 0) {
    enc.section(SECTION.global, (s) => {
      s.vector(mod.globals, (g, e) => encodeGlobal(g, e));
    });
  }
  if (mod.exports.length > 0) {
    enc.section(SECTION.export, (s) => {
      s.vector(mod.exports, (exp, e) => encodeExport(exp, e, numImportFuncs));
    });
  }
  const hasActiveElems = mod.elements.length > 0;
  const hasDeclaredRefs = mod.declaredFuncRefs.length > 0;
  if (hasActiveElems || hasDeclaredRefs) {
    enc.section(SECTION.element, (s) => {
      const totalSegments = mod.elements.length + (hasDeclaredRefs ? 1 : 0);
      s.u32(totalSegments);
      for (const elem of mod.elements) {
        s.byte(0);
        for (const instr of elem.offset) encodeInstr(instr, s);
        s.byte(OP.end);
        s.u32(elem.funcIndices.length);
        for (const idx of elem.funcIndices) s.u32(idx);
      }
      if (hasDeclaredRefs) {
        s.byte(3);
        s.byte(0);
        s.u32(mod.declaredFuncRefs.length);
        for (const idx of mod.declaredFuncRefs) s.u32(idx);
      }
    });
  }
  if (mod.functions.length > 0) {
    const codeSectionBody = new WasmEncoder();
    const funcRelativeEntries = [];
    codeSectionBody.u32(mod.functions.length);
    for (const f of mod.functions) {
      const bodyStartInSection = codeSectionBody.length;
      encodeFunctionWithSourceMap(f, codeSectionBody, bodyStartInSection, funcRelativeEntries);
    }
    const codeSectionData = codeSectionBody.finish();
    const sectionIdPos = enc.length;
    enc.byte(SECTION.code);
    const lengthBefore = enc.length;
    enc.u32(codeSectionData.length);
    const codeSectionPayloadStart = enc.length;
    enc.bytes(codeSectionData);
    for (const entry of funcRelativeEntries) {
      sourceMapEntries.push({
        wasmOffset: codeSectionPayloadStart + entry.instrOffset,
        sourcePos: entry.sourcePos,
      });
    }
  }
  if (mod.dataSegments && mod.dataSegments.length > 0) {
    enc.section(SECTION.data, (s) => {
      s.u32(mod.dataSegments.length);
      for (const seg of mod.dataSegments) {
        s.byte(0);
        s.byte(OP.i32_const);
        s.i32(seg.offset);
        s.byte(OP.end);
        s.u32(seg.bytes.length);
        s.bytes(seg.bytes);
      }
    });
  }
  {
    const nameEntries = [];
    let funcIdx = 0;
    for (const imp of mod.imports) {
      if (imp.desc.kind === "func") {
        nameEntries.push({ index: funcIdx, name: imp.name.replace(/_import$/, "") });
        funcIdx++;
      }
    }
    for (const f of mod.functions) {
      if (f.name) {
        nameEntries.push({ index: funcIdx, name: f.name });
      }
      funcIdx++;
    }
    if (nameEntries.length > 0) {
      enc.section(SECTION.custom, (s) => {
        s.name("name");
        const sub2 = new WasmEncoder();
        sub2.u32(nameEntries.length);
        for (const entry of nameEntries) {
          sub2.u32(entry.index);
          sub2.name(entry.name);
        }
        const subData = sub2.finish();
        s.byte(1);
        s.u32(subData.length);
        s.bytes(subData);
      });
    }
  }
  return { binary: enc.finish(), sourceMapEntries };
}
function encodeFunctionWithSourceMap(f, enc, _bodyStartInSection, entries) {
  const body = new WasmEncoder();
  const localGroups = groupLocals(f.locals);
  body.vector(localGroups, (group, e) => {
    e.u32(group.count);
    encodeValType(group.type, e);
  });
  for (const instr of f.body) {
    encodeInstrWithSourceMap(instr, body, entries, _bodyStartInSection, enc);
  }
  body.byte(OP.end);
  const bodyBytes = body.finish();
  const u32PrefixSize = leb128UnsignedSize(bodyBytes.length);
  for (const entry of entries) {
    if (entry.bodyOffset === _bodyStartInSection) {
      entry.instrOffset = _bodyStartInSection + u32PrefixSize + entry.instrOffset;
    }
  }
  enc.u32(bodyBytes.length);
  enc.bytes(bodyBytes);
}
function encodeInstrWithSourceMap(instr, enc, entries, bodyStartInSection, _sectionEnc) {
  if (instr.sourcePos) {
    entries.push({
      bodyOffset: bodyStartInSection,
      instrOffset: enc.length,
      // position within the body encoder
      sourcePos: instr.sourcePos,
    });
  }
  encodeInstr(instr, enc);
}
function leb128UnsignedSize(value) {
  let size = 0;
  do {
    value >>>= 7;
    size++;
  } while (value !== 0);
  return size;
}
function encodeTypeDef(t, enc) {
  switch (t.kind) {
    case "func":
      enc.byte(TYPE.func);
      enc.vector(t.params, (p, e) => encodeValType(p, e));
      enc.vector(t.results, (r, e) => encodeValType(r, e));
      break;
    case "struct":
      if (t.superTypeIdx !== void 0) {
        enc.byte(t.final ? TYPE.sub_final : TYPE.sub);
        if (t.superTypeIdx >= 0) {
          enc.u32(1);
          enc.u32(t.superTypeIdx);
        } else {
          enc.u32(0);
        }
        enc.byte(TYPE.struct);
        enc.vector(t.fields, (f, e) => encodeFieldDef(f, e));
      } else {
        enc.byte(TYPE.struct);
        enc.vector(t.fields, (f, e) => encodeFieldDef(f, e));
      }
      break;
    case "array":
      enc.byte(TYPE.array);
      encodeStorageType(t.element, enc);
      enc.byte(t.mutable ? TYPE.mut_field : TYPE.const_field);
      break;
    case "rec":
      enc.byte(TYPE.rec);
      enc.u32(t.types.length);
      for (const sub2 of t.types) encodeTypeDef(sub2, enc);
      break;
    case "sub":
      if (t.superType !== null) {
        enc.byte(t.final ? TYPE.sub_final : TYPE.sub);
        enc.u32(1);
        enc.u32(t.superType);
      } else if (!t.final) {
        enc.byte(TYPE.sub);
        enc.u32(0);
      }
      if (t.superType !== null || !t.final) {
        encodeTypeDef(t.type, enc);
      } else {
        encodeTypeDef(t.type, enc);
      }
      break;
  }
}
function encodeFieldDef(f, enc) {
  encodeStorageType(f.type, enc);
  enc.byte(f.mutable ? TYPE.mut_field : TYPE.const_field);
}
function encodeStorageType(t, enc) {
  if (t.kind === "i8") {
    enc.byte(TYPE.i8);
    return;
  }
  if (t.kind === "i16") {
    enc.byte(TYPE.i16);
    return;
  }
  encodeValType(t, enc);
}
function encodeValType(t, enc) {
  switch (t.kind) {
    case "i32":
      enc.byte(TYPE.i32);
      break;
    case "i64":
      enc.byte(TYPE.i64);
      break;
    case "f32":
      enc.byte(TYPE.f32);
      break;
    case "f64":
      enc.byte(TYPE.f64);
      break;
    case "v128":
      enc.byte(TYPE.v128);
      break;
    case "funcref":
      enc.byte(TYPE.funcref);
      break;
    case "externref":
      enc.byte(TYPE.externref);
      break;
    case "ref_extern":
      enc.byte(TYPE.ref);
      enc.byte(TYPE.externref);
      break;
    case "ref":
      enc.byte(TYPE.ref);
      enc.i32(t.typeIdx);
      break;
    case "ref_null":
      enc.byte(TYPE.ref_null);
      enc.i32(t.typeIdx);
      break;
    case "eqref":
      enc.byte(TYPE.ref_null);
      enc.byte(TYPE.eq);
      break;
    case "anyref":
      enc.byte(TYPE.ref_null);
      enc.byte(TYPE.any);
      break;
    case "i8":
      enc.byte(TYPE.i32);
      break;
    case "i16":
      enc.byte(TYPE.i32);
      break;
  }
}
function encodeImport(imp, enc) {
  enc.name(imp.module);
  enc.name(imp.name);
  switch (imp.desc.kind) {
    case "func":
      enc.byte(0);
      enc.u32(imp.desc.typeIdx);
      break;
    case "table":
      enc.byte(1);
      enc.byte(imp.desc.elementType === "funcref" ? TYPE.funcref : TYPE.externref);
      if (imp.desc.max !== void 0) {
        enc.byte(1);
        enc.u32(imp.desc.min);
        enc.u32(imp.desc.max);
      } else {
        enc.byte(0);
        enc.u32(imp.desc.min);
      }
      break;
    case "global":
      enc.byte(3);
      encodeValType(imp.desc.type, enc);
      enc.byte(imp.desc.mutable ? 1 : 0);
      break;
    case "tag":
      enc.byte(4);
      enc.byte(0);
      enc.u32(imp.desc.typeIdx);
      break;
  }
}
function encodeGlobal(g, enc) {
  encodeValType(g.type, enc);
  enc.byte(g.mutable ? 1 : 0);
  for (const instr of g.init) encodeInstr(instr, enc);
  enc.byte(OP.end);
}
function encodeExport(exp, enc, _numImportFuncs) {
  enc.name(exp.name);
  const kindByte =
    exp.desc.kind === "func"
      ? 0
      : exp.desc.kind === "table"
        ? 1
        : exp.desc.kind === "memory"
          ? 2
          : exp.desc.kind === "tag"
            ? 4
            : 3;
  enc.byte(kindByte);
  enc.u32(exp.desc.index);
}
function groupLocals(locals) {
  const groups = [];
  for (const local of locals) {
    const last = groups[groups.length - 1];
    if (last && valTypeEq(last.type, local.type)) {
      last.count++;
    } else {
      groups.push({ count: 1, type: local.type });
    }
  }
  return groups;
}
function valTypeEq(a, b) {
  if (a.kind !== b.kind) return false;
  if ((a.kind === "ref" || a.kind === "ref_null") && (b.kind === "ref" || b.kind === "ref_null")) {
    return a.typeIdx === b.typeIdx;
  }
  return true;
}
function encodeBlockType(bt, enc) {
  switch (bt.kind) {
    case "empty":
      enc.byte(64);
      break;
    case "val":
      encodeValType(bt.type, enc);
      break;
    case "type":
      enc.i32(bt.typeIdx);
      break;
  }
}
function encodeInstr(instr, enc) {
  switch (instr.op) {
    case "unreachable":
      enc.byte(OP.unreachable);
      break;
    case "nop":
      enc.byte(OP.nop);
      break;
    case "block":
      enc.byte(OP.block);
      encodeBlockType(instr.blockType, enc);
      for (const i of instr.body) encodeInstr(i, enc);
      enc.byte(OP.end);
      break;
    case "loop":
      enc.byte(OP.loop);
      encodeBlockType(instr.blockType, enc);
      for (const i of instr.body) encodeInstr(i, enc);
      enc.byte(OP.end);
      break;
    case "if": {
      enc.byte(OP.if);
      encodeBlockType(instr.blockType, enc);
      for (const i of instr.then) encodeInstr(i, enc);
      const hasElse = instr.else && instr.else.length > 0;
      const needsElse = hasElse || instr.blockType.kind === "val";
      if (needsElse) {
        enc.byte(OP.else);
        if (hasElse) {
          for (const i of instr.else) encodeInstr(i, enc);
        } else {
          enc.byte(OP.unreachable);
        }
      }
      enc.byte(OP.end);
      break;
    }
    case "br":
      enc.byte(OP.br);
      enc.u32(instr.depth);
      break;
    case "br_if":
      enc.byte(OP.br_if);
      enc.u32(instr.depth);
      break;
    case "return":
      enc.byte(OP.return);
      break;
    case "call":
      enc.byte(OP.call);
      enc.u32(instr.funcIdx);
      break;
    case "return_call":
      enc.byte(OP.return_call);
      enc.u32(instr.funcIdx);
      break;
    case "call_indirect":
      enc.byte(OP.call_indirect);
      enc.u32(instr.typeIdx);
      enc.u32(instr.tableIdx);
      break;
    case "drop":
      enc.byte(OP.drop);
      break;
    case "select":
      enc.byte(OP.select);
      break;
    case "local.get":
      enc.byte(OP.local_get);
      enc.u32(instr.index);
      break;
    case "local.set":
      enc.byte(OP.local_set);
      enc.u32(instr.index);
      break;
    case "local.tee":
      enc.byte(OP.local_tee);
      enc.u32(instr.index);
      break;
    case "global.get":
      enc.byte(OP.global_get);
      enc.u32(instr.index);
      break;
    case "global.set":
      enc.byte(OP.global_set);
      enc.u32(instr.index);
      break;
    case "i32.const":
      enc.byte(OP.i32_const);
      enc.i32(instr.value);
      break;
    case "i64.const":
      enc.byte(OP.i64_const);
      enc.i64(instr.value);
      break;
    case "i64.add":
      enc.byte(OP.i64_add);
      break;
    case "i64.sub":
      enc.byte(OP.i64_sub);
      break;
    case "i64.mul":
      enc.byte(OP.i64_mul);
      break;
    case "i64.div_s":
      enc.byte(OP.i64_div_s);
      break;
    case "i64.rem_s":
      enc.byte(OP.i64_rem_s);
      break;
    case "i64.eq":
      enc.byte(OP.i64_eq);
      break;
    case "i64.ne":
      enc.byte(OP.i64_ne);
      break;
    case "i64.lt_s":
      enc.byte(OP.i64_lt_s);
      break;
    case "i64.le_s":
      enc.byte(OP.i64_le_s);
      break;
    case "i64.gt_s":
      enc.byte(OP.i64_gt_s);
      break;
    case "i64.ge_s":
      enc.byte(OP.i64_ge_s);
      break;
    case "i64.eqz":
      enc.byte(OP.i64_eqz);
      break;
    case "i64.and":
      enc.byte(OP.i64_and);
      break;
    case "i64.or":
      enc.byte(OP.i64_or);
      break;
    case "i64.xor":
      enc.byte(OP.i64_xor);
      break;
    case "i64.shl":
      enc.byte(OP.i64_shl);
      break;
    case "i64.shr_s":
      enc.byte(OP.i64_shr_s);
      break;
    case "i64.shr_u":
      enc.byte(OP.i64_shr_u);
      break;
    case "i64.extend_i32_s":
      enc.byte(OP.i64_extend_i32_s);
      break;
    case "i64.extend_i32_u":
      enc.byte(OP.i64_extend_i32_u);
      break;
    case "i64.trunc_f64_s":
      enc.byte(OP.i64_trunc_f64_s);
      break;
    case "f64.convert_i64_s":
      enc.byte(OP.f64_convert_i64_s);
      break;
    case "i64.reinterpret_f64":
      enc.byte(OP.i64_reinterpret_f64);
      break;
    case "f64.reinterpret_i64":
      enc.byte(OP.f64_reinterpret_i64);
      break;
    case "f64.const":
      enc.byte(OP.f64_const);
      enc.f64(instr.value);
      break;
    case "f32.const":
      enc.byte(OP.f32_const);
      enc.f32(instr.value);
      break;
    case "i32.eqz":
      enc.byte(OP.i32_eqz);
      break;
    case "i32.eq":
      enc.byte(OP.i32_eq);
      break;
    case "i32.ne":
      enc.byte(OP.i32_ne);
      break;
    case "i32.lt_s":
      enc.byte(OP.i32_lt_s);
      break;
    case "i32.le_s":
      enc.byte(OP.i32_le_s);
      break;
    case "i32.gt_s":
      enc.byte(OP.i32_gt_s);
      break;
    case "i32.ge_s":
      enc.byte(OP.i32_ge_s);
      break;
    case "i32.ge_u":
      enc.byte(OP.i32_ge_u);
      break;
    case "i32.add":
      enc.byte(OP.i32_add);
      break;
    case "i32.sub":
      enc.byte(OP.i32_sub);
      break;
    case "i32.mul":
      enc.byte(OP.i32_mul);
      break;
    case "i32.and":
      enc.byte(OP.i32_and);
      break;
    case "i32.or":
      enc.byte(OP.i32_or);
      break;
    case "i32.xor":
      enc.byte(OP.i32_xor);
      break;
    case "i32.shl":
      enc.byte(OP.i32_shl);
      break;
    case "i32.shr_s":
      enc.byte(OP.i32_shr_s);
      break;
    case "i32.shr_u":
      enc.byte(OP.i32_shr_u);
      break;
    case "i32.clz":
      enc.byte(OP.i32_clz);
      break;
    case "i32.trunc_sat_f64_s":
      enc.byte(OP.misc_prefix);
      enc.byte(OP.i32_trunc_sat_f64_s);
      break;
    case "i32.trunc_sat_f64_u":
      enc.byte(OP.misc_prefix);
      enc.byte(OP.i32_trunc_sat_f64_u);
      break;
    case "i64.trunc_sat_f64_s":
      enc.byte(OP.misc_prefix);
      enc.byte(OP.i64_trunc_sat_f64_s);
      break;
    case "f64.eq":
      enc.byte(OP.f64_eq);
      break;
    case "f64.ne":
      enc.byte(OP.f64_ne);
      break;
    case "f64.lt":
      enc.byte(OP.f64_lt);
      break;
    case "f64.le":
      enc.byte(OP.f64_le);
      break;
    case "f64.gt":
      enc.byte(OP.f64_gt);
      break;
    case "f64.ge":
      enc.byte(OP.f64_ge);
      break;
    case "f64.abs":
      enc.byte(OP.f64_abs);
      break;
    case "f64.neg":
      enc.byte(OP.f64_neg);
      break;
    case "f64.ceil":
      enc.byte(OP.f64_ceil);
      break;
    case "f64.floor":
      enc.byte(OP.f64_floor);
      break;
    case "f64.trunc":
      enc.byte(OP.f64_trunc);
      break;
    case "f64.nearest":
      enc.byte(OP.f64_nearest);
      break;
    case "f64.sqrt":
      enc.byte(OP.f64_sqrt);
      break;
    case "f64.add":
      enc.byte(OP.f64_add);
      break;
    case "f64.sub":
      enc.byte(OP.f64_sub);
      break;
    case "f64.mul":
      enc.byte(OP.f64_mul);
      break;
    case "f64.div":
      enc.byte(OP.f64_div);
      break;
    case "f64.min":
      enc.byte(OP.f64_min);
      break;
    case "f64.max":
      enc.byte(OP.f64_max);
      break;
    case "f64.copysign":
      enc.byte(OP.f64_copysign);
      break;
    case "i32.trunc_f64_s":
      enc.byte(OP.i32_trunc_f64_s);
      break;
    case "f64.convert_i32_s":
      enc.byte(OP.f64_convert_i32_s);
      break;
    case "f64.convert_i32_u":
      enc.byte(OP.f64_convert_i32_u);
      break;
    case "ref.null":
      enc.byte(OP.ref_null);
      enc.i32(instr.typeIdx);
      break;
    case "ref.null.extern":
      enc.byte(OP.ref_null);
      enc.byte(TYPE.externref);
      break;
    case "ref.null.eq":
      enc.byte(OP.ref_null);
      enc.byte(TYPE.eq);
      break;
    case "ref.null.func":
      enc.byte(OP.ref_null);
      enc.byte(TYPE.funcref);
      break;
    case "ref.is_null":
      enc.byte(OP.ref_is_null);
      break;
    case "ref.as_non_null":
      enc.byte(OP.ref_as_non_null);
      break;
    case "ref.eq":
      enc.byte(OP.ref_eq);
      break;
    case "ref.cast":
      enc.byte(GC.prefix);
      enc.byte(GC.ref_cast);
      enc.i32(instr.typeIdx);
      break;
    case "ref.cast_null":
      enc.byte(GC.prefix);
      enc.byte(GC.ref_cast_null);
      enc.i32(instr.typeIdx);
      break;
    case "any.convert_extern":
      enc.byte(GC.prefix);
      enc.byte(GC.any_convert_extern);
      break;
    case "extern.convert_any":
      enc.byte(GC.prefix);
      enc.byte(GC.extern_convert_any);
      break;
    case "ref.test":
      enc.byte(GC.prefix);
      enc.byte(GC.ref_test);
      enc.i32(instr.typeIdx);
      break;
    case "struct.new":
      enc.byte(GC.prefix);
      enc.byte(GC.struct_new);
      enc.u32(instr.typeIdx);
      break;
    case "struct.get":
      enc.byte(GC.prefix);
      enc.byte(GC.struct_get);
      enc.u32(instr.typeIdx);
      enc.u32(instr.fieldIdx);
      break;
    case "struct.set":
      enc.byte(GC.prefix);
      enc.byte(GC.struct_set);
      enc.u32(instr.typeIdx);
      enc.u32(instr.fieldIdx);
      break;
    case "array.new":
      enc.byte(GC.prefix);
      enc.byte(GC.array_new);
      enc.u32(instr.typeIdx);
      break;
    case "array.new_fixed":
      enc.byte(GC.prefix);
      enc.byte(GC.array_new_fixed);
      enc.u32(instr.typeIdx);
      enc.u32(instr.length);
      break;
    case "array.new_default":
      enc.byte(GC.prefix);
      enc.byte(GC.array_new_default);
      enc.u32(instr.typeIdx);
      break;
    case "array.get":
      enc.byte(GC.prefix);
      enc.byte(GC.array_get);
      enc.u32(instr.typeIdx);
      break;
    case "array.get_s":
      enc.byte(GC.prefix);
      enc.byte(GC.array_get_s);
      enc.u32(instr.typeIdx);
      break;
    case "array.get_u":
      enc.byte(GC.prefix);
      enc.byte(GC.array_get_u);
      enc.u32(instr.typeIdx);
      break;
    case "array.set":
      enc.byte(GC.prefix);
      enc.byte(GC.array_set);
      enc.u32(instr.typeIdx);
      break;
    case "array.len":
      enc.byte(GC.prefix);
      enc.byte(GC.array_len);
      break;
    case "array.copy":
      enc.byte(GC.prefix);
      enc.byte(GC.array_copy);
      enc.u32(instr.dstTypeIdx);
      enc.u32(instr.srcTypeIdx);
      break;
    case "array.fill":
      enc.byte(GC.prefix);
      enc.byte(GC.array_fill);
      enc.u32(instr.typeIdx);
      break;
    case "ref.func":
      enc.byte(OP.ref_func);
      enc.u32(instr.funcIdx);
      break;
    case "call_ref":
      enc.byte(OP.call_ref);
      enc.u32(instr.typeIdx);
      break;
    case "return_call_ref":
      enc.byte(OP.return_call_ref);
      enc.u32(instr.typeIdx);
      break;
    case "memory.size":
      enc.byte(OP.memory_size);
      enc.byte(0);
      break;
    case "memory.grow":
      enc.byte(OP.memory_grow);
      enc.byte(0);
      break;
    case "throw":
      enc.byte(OP.throw);
      enc.u32(instr.tagIdx);
      break;
    case "rethrow":
      enc.byte(OP.rethrow);
      enc.u32(instr.depth);
      break;
    case "try": {
      enc.byte(OP.try);
      encodeBlockType(instr.blockType, enc);
      for (const i of instr.body) encodeInstr(i, enc);
      for (const c of instr.catches) {
        enc.byte(OP.catch);
        enc.u32(c.tagIdx);
        for (const i of c.body) encodeInstr(i, enc);
      }
      if (instr.catchAll) {
        enc.byte(OP.catch_all);
        for (const i of instr.catchAll) encodeInstr(i, enc);
      }
      enc.byte(OP.end);
      break;
    }
    // Memory load/store (linear memory)
    case "i32.load":
      enc.byte(OP.i32_load);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "i32.load8_u":
      enc.byte(OP.i32_load8_u);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "i32.load8_s":
      enc.byte(OP.i32_load8_s);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "i32.load16_u":
      enc.byte(OP.i32_load16_u);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "i32.store":
      enc.byte(OP.i32_store);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "i32.store8":
      enc.byte(OP.i32_store8);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "i32.store16":
      enc.byte(OP.i32_store16);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    // Integer division and remainder
    case "i32.div_s":
      enc.byte(OP.i32_div_s);
      break;
    case "i32.div_u":
      enc.byte(OP.i32_div_u);
      break;
    case "i32.rem_s":
      enc.byte(OP.i32_rem_s);
      break;
    case "i32.rem_u":
      enc.byte(OP.i32_rem_u);
      break;
    // Unsigned comparisons
    case "i32.lt_u":
      enc.byte(OP.i32_lt_u);
      break;
    case "i32.le_u":
      enc.byte(OP.i32_le_u);
      break;
    case "i32.gt_u":
      enc.byte(OP.i32_gt_u);
      break;
    // f64 memory load/store
    case "f64.load":
      enc.byte(OP.f64_load);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "f64.store":
      enc.byte(OP.f64_store);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    // f32 memory load/store and conversion
    case "f32.load":
      enc.byte(OP.f32_load);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "f32.store":
      enc.byte(OP.f32_store);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "f32.demote_f64":
      enc.byte(OP.f32_demote_f64);
      break;
    case "f64.promote_f32":
      enc.byte(OP.f64_promote_f32);
      break;
    // ---- SIMD v128 instructions ----
    case "v128.const":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_const);
      enc.v128(instr.bytes);
      break;
    case "v128.load":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_load);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "v128.store":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_store);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "v128.not":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_not);
      break;
    case "v128.and":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_and);
      break;
    case "v128.andnot":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_andnot);
      break;
    case "v128.or":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_or);
      break;
    case "v128.xor":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_xor);
      break;
    case "v128.bitselect":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_bitselect);
      break;
    case "v128.any_true":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_any_true);
      break;
    // i8x16
    case "i8x16.splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_splat);
      break;
    case "i8x16.extract_lane_s":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_extract_lane_s);
      enc.byte(instr.lane);
      break;
    case "i8x16.extract_lane_u":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_extract_lane_u);
      enc.byte(instr.lane);
      break;
    case "i8x16.replace_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_replace_lane);
      enc.byte(instr.lane);
      break;
    case "i8x16.eq":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_eq);
      break;
    case "i8x16.ne":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_ne);
      break;
    case "i8x16.all_true":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_all_true);
      break;
    case "i8x16.bitmask":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_bitmask);
      break;
    case "i8x16.swizzle":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_swizzle);
      break;
    case "i8x16.shuffle":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_shuffle);
      for (const lane of instr.lanes) enc.byte(lane);
      break;
    case "i8x16.add":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_add);
      break;
    case "i8x16.sub":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_sub);
      break;
    case "i8x16.min_u":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_min_u);
      break;
    case "i8x16.max_u":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i8x16_max_u);
      break;
    // i16x8
    case "i16x8.splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_splat);
      break;
    case "i16x8.extract_lane_s":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_extract_lane_s);
      enc.byte(instr.lane);
      break;
    case "i16x8.extract_lane_u":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_extract_lane_u);
      enc.byte(instr.lane);
      break;
    case "i16x8.replace_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_replace_lane);
      enc.byte(instr.lane);
      break;
    case "i16x8.eq":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_eq);
      break;
    case "i16x8.ne":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_ne);
      break;
    case "i16x8.lt_s":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_lt_s);
      break;
    case "i16x8.gt_s":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_gt_s);
      break;
    case "i16x8.all_true":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_all_true);
      break;
    case "i16x8.bitmask":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_bitmask);
      break;
    case "i16x8.add":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_add);
      break;
    case "i16x8.sub":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_sub);
      break;
    case "i16x8.mul":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_mul);
      break;
    case "i16x8.shl":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_shl);
      break;
    case "i16x8.shr_u":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i16x8_shr_u);
      break;
    // i32x4
    case "i32x4.splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_splat);
      break;
    case "i32x4.extract_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_extract_lane);
      enc.byte(instr.lane);
      break;
    case "i32x4.replace_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_replace_lane);
      enc.byte(instr.lane);
      break;
    case "i32x4.eq":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_eq);
      break;
    case "i32x4.ne":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_ne);
      break;
    case "i32x4.all_true":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_all_true);
      break;
    case "i32x4.bitmask":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_bitmask);
      break;
    case "i32x4.add":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_add);
      break;
    case "i32x4.sub":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_sub);
      break;
    case "i32x4.mul":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_mul);
      break;
    case "i32x4.shl":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_shl);
      break;
    case "i32x4.shr_s":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_shr_s);
      break;
    case "i32x4.shr_u":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i32x4_shr_u);
      break;
    // i64x2
    case "i64x2.splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i64x2_splat);
      break;
    case "i64x2.extract_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i64x2_extract_lane);
      enc.byte(instr.lane);
      break;
    case "i64x2.replace_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i64x2_replace_lane);
      enc.byte(instr.lane);
      break;
    case "i64x2.add":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i64x2_add);
      break;
    case "i64x2.sub":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i64x2_sub);
      break;
    case "i64x2.mul":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i64x2_mul);
      break;
    case "i64x2.eq":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i64x2_eq);
      break;
    case "i64x2.ne":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.i64x2_ne);
      break;
    // f32x4
    case "f32x4.splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f32x4_splat);
      break;
    case "f32x4.extract_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f32x4_extract_lane);
      enc.byte(instr.lane);
      break;
    case "f32x4.replace_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f32x4_replace_lane);
      enc.byte(instr.lane);
      break;
    case "f32x4.eq":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f32x4_eq);
      break;
    case "f32x4.add":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f32x4_add);
      break;
    case "f32x4.sub":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f32x4_sub);
      break;
    case "f32x4.mul":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f32x4_mul);
      break;
    case "f32x4.div":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f32x4_div);
      break;
    // f64x2
    case "f64x2.splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_splat);
      break;
    case "f64x2.extract_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_extract_lane);
      enc.byte(instr.lane);
      break;
    case "f64x2.replace_lane":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_replace_lane);
      enc.byte(instr.lane);
      break;
    case "f64x2.eq":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_eq);
      break;
    case "f64x2.ne":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_ne);
      break;
    case "f64x2.add":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_add);
      break;
    case "f64x2.sub":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_sub);
      break;
    case "f64x2.mul":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_mul);
      break;
    case "f64x2.div":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.f64x2_div);
      break;
    // SIMD load splat variants
    case "v128.load8_splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_load8_splat);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "v128.load16_splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_load16_splat);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "v128.load32_splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_load32_splat);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "v128.load64_splat":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_load64_splat);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "v128.load32_zero":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_load32_zero);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
    case "v128.load64_zero":
      enc.byte(OP.simd_prefix);
      enc.u32(SIMD.v128_load64_zero);
      enc.u32(instr.align);
      enc.u32(instr.offset);
      break;
  }
}
function emitSourceMappingURLSection(enc, url) {
  enc.section(SECTION.custom, (s) => {
    s.name("sourceMappingURL");
    s.name(url);
  });
}

// src/emit/sourcemap.ts
var BASE64_CHARS = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
function encodeVLQ(value) {
  let vlq = value < 0 ? (-value << 1) | 1 : value << 1;
  let encoded = "";
  do {
    let digit = vlq & 31;
    vlq >>>= 5;
    if (vlq > 0) {
      digit |= 32;
    }
    encoded += BASE64_CHARS[digit];
  } while (vlq > 0);
  return encoded;
}
function generateSourceMap(entries, sourcesContent) {
  if (entries.length === 0) {
    return {
      version: 3,
      sources: [],
      sourcesContent: [],
      names: [],
      mappings: "",
    };
  }
  const sorted = [...entries].sort((a, b) => a.wasmOffset - b.wasmOffset);
  const sourceIndex = /* @__PURE__ */ new Map();
  const sources = [];
  for (const entry of sorted) {
    if (!sourceIndex.has(entry.sourcePos.file)) {
      sourceIndex.set(entry.sourcePos.file, sources.length);
      sources.push(entry.sourcePos.file);
    }
  }
  const contentArray = sources.map((s) => sourcesContent?.get(s) ?? null);
  let prevWasmOffset = 0;
  let prevSourceIdx = 0;
  let prevSourceLine = 0;
  let prevSourceCol = 0;
  const segments = [];
  for (const entry of sorted) {
    const srcIdx = sourceIndex.get(entry.sourcePos.file);
    const wasmDelta = entry.wasmOffset - prevWasmOffset;
    const srcIdxDelta = srcIdx - prevSourceIdx;
    const lineDelta = entry.sourcePos.line - prevSourceLine;
    const colDelta = entry.sourcePos.column - prevSourceCol;
    const segment = encodeVLQ(wasmDelta) + encodeVLQ(srcIdxDelta) + encodeVLQ(lineDelta) + encodeVLQ(colDelta);
    segments.push(segment);
    prevWasmOffset = entry.wasmOffset;
    prevSourceIdx = srcIdx;
    prevSourceLine = entry.sourcePos.line;
    prevSourceCol = entry.sourcePos.column;
  }
  const mappings = segments.join(",");
  return {
    version: 3,
    sources,
    sourcesContent: contentArray,
    names: [],
    mappings,
  };
}

// src/emit/wat.ts
function computeInlineableTypes(mod) {
  const refCount = /* @__PURE__ */ new Map();
  const nonFuncRef = /* @__PURE__ */ new Set();
  const bump = (idx) => refCount.set(idx, (refCount.get(idx) ?? 0) + 1);
  const markNonFunc = (idx) => {
    bump(idx);
    nonFuncRef.add(idx);
  };
  for (const imp of mod.imports) {
    if (imp.desc.kind === "func") markNonFunc(imp.desc.typeIdx);
  }
  for (const tag of mod.tags) {
    markNonFunc(tag.typeIdx);
  }
  for (const f of mod.functions) {
    bump(f.typeIdx);
    walkInstrs(f.body, (instr) => {
      if (instr.op === "call_indirect") markNonFunc(instr.typeIdx);
      if (instr.op === "call_ref" || instr.op === "return_call_ref") markNonFunc(instr.typeIdx);
    });
    walkBlockTypes(f.body, (bt) => {
      if (bt.kind === "type") markNonFunc(bt.typeIdx);
    });
  }
  const inlineable = /* @__PURE__ */ new Set();
  for (let i = 0; i < mod.types.length; i++) {
    const t = mod.types[i];
    if (t.kind === "func" && (refCount.get(i) ?? 0) === 1 && !nonFuncRef.has(i)) {
      inlineable.add(i);
    }
  }
  return inlineable;
}
var walkInstrs = walkInstructions;
function walkBlockTypes(instrs, visitor) {
  walkInstructions(instrs, (instr) => {
    if ("blockType" in instr) {
      visitor(instr.blockType);
    }
  });
}
function escapeWatString(s) {
  let out = "";
  for (let i = 0; i < s.length; i++) {
    const ch = s.charCodeAt(i);
    if (ch === 92) out += "\\\\";
    else if (ch === 34) out += '\\"';
    else if (ch === 10) out += "\\n";
    else if (ch === 13) out += "\\r";
    else if (ch === 9) out += "\\t";
    else if (ch < 32 || ch === 127) {
      out += "\\" + ch.toString(16).padStart(2, "0");
    } else {
      out += s[i];
    }
  }
  return out;
}
function emitWat(mod) {
  const lines = [];
  const indent = (depth) => "  ".repeat(depth);
  const inlineableTypes = computeInlineableTypes(mod);
  lines.push("(module");
  for (let i = 0; i < mod.types.length; i++) {
    if (inlineableTypes.has(i)) continue;
    const t = mod.types[i];
    lines.push(`${indent(1)}${formatTypeDef(t, i)}`);
  }
  for (const imp of mod.imports) {
    const desc =
      imp.desc.kind === "func"
        ? `(func $${imp.name}_import (type ${imp.desc.typeIdx}))`
        : imp.desc.kind === "global"
          ? `(global $${imp.name} ${imp.desc.mutable ? `(mut ${formatValType(imp.desc.type)})` : formatValType(imp.desc.type)})`
          : imp.desc.kind === "table"
            ? `(table ${imp.desc.min} ${imp.desc.max ?? ""} ${imp.desc.elementType})`
            : `(tag (type ${imp.desc.typeIdx}))`;
    lines.push(`${indent(1)}(import "${escapeWatString(imp.module)}" "${escapeWatString(imp.name)}" ${desc})`);
  }
  for (const g of mod.globals) {
    const mutStr = g.mutable ? `(mut ${formatValType(g.type)})` : formatValType(g.type);
    const initStr = g.init.map((i) => formatInstr(i, 0)).join(" ");
    lines.push(`${indent(1)}(global $${g.name} ${mutStr} (${initStr}))`);
  }
  for (const t of mod.tables) {
    lines.push(`${indent(1)}(table ${t.min} ${t.max !== void 0 ? t.max : ""} ${t.elementType})`);
  }
  if (mod.memories) {
    for (const mem of mod.memories) {
      if (mem.max !== void 0) {
        lines.push(`${indent(1)}(memory ${mem.min} ${mem.max})`);
      } else {
        lines.push(`${indent(1)}(memory ${mem.min})`);
      }
    }
  }
  for (const elem of mod.elements) {
    const offsetStr = elem.offset.map((i) => formatInstr(i, 0)).join(" ");
    const funcStr = elem.funcIndices.join(" ");
    lines.push(`${indent(1)}(elem (offset ${offsetStr}) func ${funcStr})`);
  }
  if (mod.declaredFuncRefs.length > 0) {
    const funcStr = mod.declaredFuncRefs.join(" ");
    lines.push(`${indent(1)}(elem declare func ${funcStr})`);
  }
  for (let i = 0; i < mod.tags.length; i++) {
    const tag = mod.tags[i];
    lines.push(`${indent(1)}(tag $${tag.name} (type ${tag.typeIdx}))`);
  }
  const numImportFuncs = mod.imports.filter((i) => i.desc.kind === "func").length;
  for (let i = 0; i < mod.functions.length; i++) {
    const f = mod.functions[i];
    lines.push(formatFunction(f, i + numImportFuncs, mod, inlineableTypes));
  }
  for (const exp of mod.exports) {
    lines.push(`${indent(1)}(export "${escapeWatString(exp.name)}" (${exp.desc.kind} ${exp.desc.index}))`);
  }
  if (mod.dataSegments && mod.dataSegments.length > 0) {
    for (const seg of mod.dataSegments) {
      const hexBytes = Array.from(seg.bytes)
        .map((b) => `\\${b.toString(16).padStart(2, "0")}`)
        .join("");
      lines.push(`${indent(1)}(data (i32.const ${seg.offset}) "${hexBytes}")`);
    }
  }
  lines.push(")");
  return lines.join("\n");
}
function formatTypeDef(t, idx) {
  switch (t.kind) {
    case "func": {
      const params = t.params.map((p) => formatValType(p)).join(" ");
      const results = t.results.map((r) => formatValType(r)).join(" ");
      return `(type $${t.name || `type${idx}`} (func${params ? ` (param ${params})` : ""}${results ? ` (result ${results})` : ""}))`;
    }
    case "struct": {
      const fields = t.fields.map((f) => formatFieldDef(f)).join(" ");
      if (t.superTypeIdx !== void 0) {
        const finalStr = t.final ? " final" : "";
        const superStr = t.superTypeIdx >= 0 ? ` $type${t.superTypeIdx}` : "";
        return `(type $${t.name} (sub${finalStr}${superStr} (struct ${fields})))`;
      }
      return `(type $${t.name} (struct ${fields}))`;
    }
    case "array":
      return `(type $${t.name} (array ${t.mutable ? "(mut " : ""}${formatStorageType(t.element)}${t.mutable ? ")" : ""}))`;
    case "rec": {
      const inner = t.types.map((sub2, i) => `    ${formatTypeDef(sub2, idx + i)}`).join("\n");
      return `(rec
${inner}
  )`;
    }
    case "sub": {
      const superStr = t.superType !== null ? ` $type${t.superType}` : "";
      const innerType = formatTypeDef(t.type, idx);
      return `(type $${t.name} (sub${superStr} ${innerType.replace(/^\(type \$\S+ /, "").replace(/\)$/, "")}))`;
    }
  }
}
function formatStorageType(t) {
  if (t.kind === "i8") return "i8";
  if (t.kind === "i16") return "i16";
  return formatValType(t);
}
function formatFieldDef(f) {
  const mutStr = f.mutable ? `(mut ${formatStorageType(f.type)})` : formatStorageType(f.type);
  return `(field $${f.name} ${mutStr})`;
}
function formatValType(t) {
  switch (t.kind) {
    case "i32":
      return "i32";
    case "i64":
      return "i64";
    case "f32":
      return "f32";
    case "f64":
      return "f64";
    case "funcref":
      return "funcref";
    case "externref":
      return "externref";
    case "eqref":
      return "eqref";
    case "anyref":
      return "anyref";
    case "ref_extern":
      return "(ref extern)";
    case "ref":
      return `(ref ${t.typeIdx})`;
    case "ref_null":
      return `(ref null ${t.typeIdx})`;
    case "v128":
      return "v128";
    case "i8":
      return "i8";
    case "i16":
      return "i16";
  }
}
function formatFunction(f, _globalIdx, mod, inlineableTypes) {
  const lines = [];
  let sigStr;
  if (inlineableTypes.has(f.typeIdx)) {
    const t = mod.types[f.typeIdx];
    const params = t.params.map((p) => formatValType(p)).join(" ");
    const results = t.results.map((r) => formatValType(r)).join(" ");
    sigStr = `${params ? ` (param ${params})` : ""}${results ? ` (result ${results})` : ""}`;
  } else {
    sigStr = ` (type ${f.typeIdx})`;
  }
  lines.push(`  (func $${f.name}${sigStr}`);
  for (const local of f.locals) {
    lines.push(`    (local $${local.name} ${formatValType(local.type)})`);
  }
  for (const instr of f.body) {
    lines.push(formatInstrIndented(instr, 2));
  }
  lines.push("  )");
  return lines.join("\n");
}
function formatInstrIndented(instr, depth) {
  const pad = "  ".repeat(depth);
  switch (instr.op) {
    case "block": {
      const bt = formatBlockType(instr.blockType);
      const inner = instr.body.map((i) => formatInstrIndented(i, depth + 1)).join("\n");
      return `${pad}(block${bt}
${inner}
${pad})`;
    }
    case "loop": {
      const bt = formatBlockType(instr.blockType);
      const inner = instr.body.map((i) => formatInstrIndented(i, depth + 1)).join("\n");
      return `${pad}(loop${bt}
${inner}
${pad})`;
    }
    case "if": {
      const bt = formatBlockType(instr.blockType);
      const thenStr = instr.then.map((i) => formatInstrIndented(i, depth + 1)).join("\n");
      const hasElse = instr.else && instr.else.length > 0;
      const needsElse = hasElse || instr.blockType.kind === "val";
      if (needsElse) {
        const elseStr = hasElse
          ? instr.else.map((i) => formatInstrIndented(i, depth + 1)).join("\n")
          : `${pad}    unreachable`;
        return `${pad}(if${bt}
${pad}  (then
${thenStr}
${pad}  )
${pad}  (else
${elseStr}
${pad}  )
${pad})`;
      }
      return `${pad}(if${bt}
${pad}  (then
${thenStr}
${pad}  )
${pad})`;
    }
    case "try": {
      const bt = formatBlockType(instr.blockType);
      let result = `${pad}(try${bt}
${pad}  (do
`;
      result += instr.body.map((i) => formatInstrIndented(i, depth + 2)).join("\n");
      result += `
${pad}  )`;
      for (const c of instr.catches) {
        result += `
${pad}  (catch ${c.tagIdx}
`;
        result += c.body.map((i) => formatInstrIndented(i, depth + 2)).join("\n");
        result += `
${pad}  )`;
      }
      if (instr.catchAll) {
        result += `
${pad}  (catch_all
`;
        result += instr.catchAll.map((i) => formatInstrIndented(i, depth + 2)).join("\n");
        result += `
${pad}  )`;
      }
      result += `
${pad})`;
      return result;
    }
    default:
      return `${pad}${formatInstr(instr, depth)}`;
  }
}
function formatBlockType(bt) {
  switch (bt.kind) {
    case "empty":
      return "";
    case "val":
      return ` (result ${formatValType(bt.type)})`;
    case "type":
      return ` (type ${bt.typeIdx})`;
  }
}
function formatInstr(instr, _depth) {
  switch (instr.op) {
    case "local.get":
      return `local.get ${instr.index}`;
    case "local.set":
      return `local.set ${instr.index}`;
    case "local.tee":
      return `local.tee ${instr.index}`;
    case "global.get":
      return `global.get ${instr.index}`;
    case "global.set":
      return `global.set ${instr.index}`;
    case "i32.const":
      return `i32.const ${instr.value}`;
    case "i64.const":
      return `i64.const ${instr.value}`;
    case "f64.const":
      return `f64.const ${instr.value}`;
    case "f32.const":
      return `f32.const ${instr.value}`;
    case "br":
      return `br ${instr.depth}`;
    case "br_if":
      return `br_if ${instr.depth}`;
    case "call":
      return `call ${instr.funcIdx}`;
    case "return_call":
      return `return_call ${instr.funcIdx}`;
    case "call_indirect":
      return `call_indirect (type ${instr.typeIdx})`;
    case "struct.new":
      return `struct.new ${instr.typeIdx}`;
    case "struct.get":
      return `struct.get ${instr.typeIdx} ${instr.fieldIdx}`;
    case "struct.set":
      return `struct.set ${instr.typeIdx} ${instr.fieldIdx}`;
    case "array.new":
      return `array.new ${instr.typeIdx}`;
    case "array.new_fixed":
      return `array.new_fixed ${instr.typeIdx} ${instr.length}`;
    case "array.new_default":
      return `array.new_default ${instr.typeIdx}`;
    case "array.get":
      return `array.get ${instr.typeIdx}`;
    case "array.get_s":
      return `array.get_s ${instr.typeIdx}`;
    case "array.get_u":
      return `array.get_u ${instr.typeIdx}`;
    case "array.set":
      return `array.set ${instr.typeIdx}`;
    case "array.copy":
      return `array.copy ${instr.dstTypeIdx} ${instr.srcTypeIdx}`;
    case "array.fill":
      return `array.fill ${instr.typeIdx}`;
    case "ref.null":
      return `ref.null ${instr.typeIdx}`;
    case "ref.null.extern":
      return "ref.null extern";
    case "ref.null.eq":
      return "ref.null eq";
    case "ref.null.func":
      return "ref.null func";
    case "ref.cast":
      return `ref.cast (ref ${instr.typeIdx})`;
    case "ref.cast_null":
      return `ref.cast null (ref null ${instr.typeIdx})`;
    case "any.convert_extern":
      return "any.convert_extern";
    case "extern.convert_any":
      return "extern.convert_any";
    case "ref.test":
      return `ref.test (ref ${instr.typeIdx})`;
    case "ref.func":
      return `ref.func ${instr.funcIdx}`;
    case "call_ref":
      return `call_ref ${instr.typeIdx}`;
    case "return_call_ref":
      return `return_call_ref ${instr.typeIdx}`;
    case "throw":
      return `throw ${instr.tagIdx}`;
    case "rethrow":
      return `rethrow ${instr.depth}`;
    // Memory load/store (linear memory)
    case "i32.load":
    case "i32.load8_u":
    case "i32.load8_s":
    case "i32.load16_u":
    case "i32.store":
    case "i32.store8":
    case "i32.store16":
    case "f32.load":
    case "f32.store":
    case "f64.load":
    case "f64.store":
      return `${instr.op} offset=${instr.offset} align=${1 << instr.align}`;
    // SIMD v128 instructions
    case "v128.const": {
      const hexParts = Array.from(instr.bytes).map((b) => `0x${b.toString(16).padStart(2, "0")}`);
      return `v128.const i8x16 ${hexParts.join(" ")}`;
    }
    case "v128.load":
    case "v128.store":
    case "v128.load8_splat":
    case "v128.load16_splat":
    case "v128.load32_splat":
    case "v128.load64_splat":
    case "v128.load32_zero":
    case "v128.load64_zero":
      return `${instr.op} offset=${instr.offset} align=${1 << instr.align}`;
    // SIMD lane operations
    case "i8x16.extract_lane_s":
    case "i8x16.extract_lane_u":
    case "i8x16.replace_lane":
    case "i16x8.extract_lane_s":
    case "i16x8.extract_lane_u":
    case "i16x8.replace_lane":
    case "i32x4.extract_lane":
    case "i32x4.replace_lane":
    case "i64x2.extract_lane":
    case "i64x2.replace_lane":
    case "f32x4.extract_lane":
    case "f32x4.replace_lane":
    case "f64x2.extract_lane":
    case "f64x2.replace_lane":
      return `${instr.op} ${instr.lane}`;
    case "i8x16.shuffle":
      return `i8x16.shuffle ${instr.lanes.join(" ")}`;
    default:
      return instr.op;
  }
}

// src/import-resolver.ts
import ts17 from "typescript";
function preprocessImports(source) {
  const sf = ts17.createSourceFile("__preprocess__.ts", source, ts17.ScriptTarget.Latest, true);
  const nsImports = /* @__PURE__ */ new Map();
  const otherImports = [];
  for (const stmt of sf.statements) {
    if (!ts17.isImportDeclaration(stmt)) continue;
    const clause = stmt.importClause;
    if (!clause) {
      otherImports.push({ start: stmt.getStart(sf), end: stmt.end });
      continue;
    }
    if (clause.namedBindings && ts17.isNamespaceImport(clause.namedBindings)) {
      const name = clause.namedBindings.name.text;
      nsImports.set(name, { start: stmt.getStart(sf), end: stmt.end });
      continue;
    }
    const defaultName = clause.name?.text;
    const namedBindings = [];
    if (clause.namedBindings && ts17.isNamedImports(clause.namedBindings)) {
      for (const el of clause.namedBindings.elements) {
        namedBindings.push(el.name.text);
      }
    }
    otherImports.push({
      start: stmt.getStart(sf),
      end: stmt.end,
      defaultName,
      namedBindings: namedBindings.length > 0 ? namedBindings : void 0,
    });
  }
  if (nsImports.size === 0 && otherImports.length === 0) return source;
  const definedNames = /* @__PURE__ */ new Set();
  for (const stmt of sf.statements) {
    if (ts17.isFunctionDeclaration(stmt) && stmt.name) {
      definedNames.add(stmt.name.text);
    }
    if (ts17.isVariableStatement(stmt)) {
      for (const decl of stmt.declarationList.declarations) {
        if (ts17.isIdentifier(decl.name)) {
          definedNames.add(decl.name.text);
        }
      }
    }
    if (ts17.isClassDeclaration(stmt) && stmt.name) {
      definedNames.add(stmt.name.text);
    }
  }
  const namespaces = /* @__PURE__ */ new Map();
  const nestedNs = /* @__PURE__ */ new Map();
  for (const ns of nsImports.keys()) {
    namespaces.set(ns, /* @__PURE__ */ new Map());
    nestedNs.set(ns, /* @__PURE__ */ new Map());
  }
  const typedVars = /* @__PURE__ */ new Map();
  const calledAsFunction = /* @__PURE__ */ new Set();
  const maxCallArgs = /* @__PURE__ */ new Map();
  function getOrCreateClass(ns, className) {
    const classes = namespaces.get(ns);
    if (!classes.has(className)) {
      classes.set(className, {
        constructorArgCounts: [],
        properties: /* @__PURE__ */ new Set(),
        methods: /* @__PURE__ */ new Map(),
      });
    }
    return classes.get(className);
  }
  function getOrCreateNestedNs(ns, subNs) {
    const map = nestedNs.get(ns);
    if (!map.has(subNs)) {
      map.set(subNs, { properties: /* @__PURE__ */ new Set(), methods: /* @__PURE__ */ new Map() });
    }
    return map.get(subNs);
  }
  function tryResolveQualifiedName(typeRef) {
    if (ts17.isQualifiedName(typeRef.typeName)) {
      if (ts17.isIdentifier(typeRef.typeName.left) && nsImports.has(typeRef.typeName.left.text)) {
        return {
          ns: typeRef.typeName.left.text,
          className: typeRef.typeName.right.text,
        };
      }
    }
    return null;
  }
  function visit(node) {
    if (ts17.isNewExpression(node) && ts17.isPropertyAccessExpression(node.expression)) {
      if (ts17.isIdentifier(node.expression.expression) && nsImports.has(node.expression.expression.text)) {
        const ns = node.expression.expression.text;
        const cls = getOrCreateClass(ns, node.expression.name.text);
        cls.constructorArgCounts.push(node.arguments?.length ?? 0);
      }
    }
    if (
      ts17.isCallExpression(node) &&
      ts17.isPropertyAccessExpression(node.expression) &&
      ts17.isPropertyAccessExpression(node.expression.expression)
    ) {
      const outer = node.expression.expression;
      if (ts17.isIdentifier(outer.expression) && nsImports.has(outer.expression.text)) {
        const ns = outer.expression.text;
        const subNsName = outer.name.text;
        const methodName = node.expression.name.text;
        const info = getOrCreateNestedNs(ns, subNsName);
        const existing = info.methods.get(methodName) ?? 0;
        info.methods.set(methodName, Math.max(existing, node.arguments.length));
      }
    }
    if (
      ts17.isPropertyAccessExpression(node) &&
      ts17.isPropertyAccessExpression(node.expression) &&
      ts17.isIdentifier(node.expression.expression) &&
      nsImports.has(node.expression.expression.text) &&
      !(node.parent && ts17.isCallExpression(node.parent) && node.parent.expression === node)
    ) {
      const ns = node.expression.expression.text;
      const subNsName = node.expression.name.text;
      const classes = namespaces.get(ns);
      if (!classes.has(subNsName)) {
        const info = getOrCreateNestedNs(ns, subNsName);
        info.properties.add(node.name.text);
      }
    }
    if (ts17.isParameter(node) && node.type && ts17.isTypeReferenceNode(node.type)) {
      const info = tryResolveQualifiedName(node.type);
      if (info && ts17.isIdentifier(node.name)) {
        getOrCreateClass(info.ns, info.className);
        typedVars.set(node.name.text, info);
      }
    }
    if (ts17.isVariableDeclaration(node) && node.type && ts17.isTypeReferenceNode(node.type)) {
      const info = tryResolveQualifiedName(node.type);
      if (info && ts17.isIdentifier(node.name)) {
        getOrCreateClass(info.ns, info.className);
        typedVars.set(node.name.text, info);
      }
    }
    if (ts17.isFunctionDeclaration(node) && node.type && ts17.isTypeReferenceNode(node.type)) {
      const info = tryResolveQualifiedName(node.type);
      if (info) {
        getOrCreateClass(info.ns, info.className);
      }
    }
    if (ts17.isPropertyAccessExpression(node) && ts17.isIdentifier(node.expression)) {
      const varInfo = typedVars.get(node.expression.text);
      if (varInfo) {
        const cls = getOrCreateClass(varInfo.ns, varInfo.className);
        const memberName = node.name.text;
        if (node.parent && ts17.isCallExpression(node.parent) && node.parent.expression === node) {
          const existing = cls.methods.get(memberName) ?? 0;
          cls.methods.set(memberName, Math.max(existing, node.parent.arguments.length));
        } else {
          cls.properties.add(memberName);
        }
      }
    }
    if (ts17.isCallExpression(node) && ts17.isIdentifier(node.expression)) {
      const name = node.expression.text;
      calledAsFunction.add(name);
      const existing = maxCallArgs.get(name) ?? 0;
      maxCallArgs.set(name, Math.max(existing, node.arguments.length));
    }
    ts17.forEachChild(node, visit);
  }
  visit(sf);
  const replacements = [];
  for (const [nsName, { start, end }] of nsImports) {
    const classes = namespaces.get(nsName);
    const nested = nestedNs.get(nsName);
    if (classes.size === 0 && nested.size === 0) {
      replacements.push({
        start,
        end,
        text: `/* import ${nsName}: no usage detected */`,
      });
      continue;
    }
    let declare = `declare namespace ${nsName} {
`;
    for (const [className, usage] of classes) {
      declare += `  class ${className} {
`;
      const maxCtorArgs = Math.max(0, ...usage.constructorArgCounts);
      if (maxCtorArgs > 0) {
        const params = Array.from({ length: maxCtorArgs }, (_, i) => `a${i}: any`).join(", ");
        declare += `    constructor(${params});
`;
      } else {
        declare += `    constructor(...args: any[]);
`;
      }
      for (const prop of usage.properties) {
        declare += `    ${prop}: any;
`;
      }
      for (const [method, argCount] of usage.methods) {
        const params = Array.from({ length: argCount }, (_, i) => `a${i}: any`).join(", ");
        declare += `    ${method}(${params}): any;
`;
      }
      declare += `  }
`;
    }
    for (const [subNsName, info] of nested) {
      if (classes.has(subNsName)) continue;
      declare += `  namespace ${subNsName} {
`;
      for (const prop of info.properties) {
        declare += `    const ${prop}: any;
`;
      }
      for (const [method, argCount] of info.methods) {
        const params = Array.from({ length: argCount }, (_, i) => `a${i}: any`).join(", ");
        declare += `    function ${method}(${params}): any;
`;
      }
      declare += `  }
`;
    }
    declare += `}`;
    replacements.push({ start, end, text: declare });
  }
  for (const imp of otherImports) {
    const lines = [];
    if (imp.defaultName && !definedNames.has(imp.defaultName)) {
      if (calledAsFunction.has(imp.defaultName)) {
        const argCount = maxCallArgs.get(imp.defaultName) ?? 0;
        const params = Array.from({ length: argCount }, (_, i) => `a${i}: any`).join(", ");
        lines.push(`declare function ${imp.defaultName}(${params}): any;`);
      } else {
        lines.push(`declare const ${imp.defaultName}: any;`);
      }
    }
    if (imp.namedBindings) {
      for (const name of imp.namedBindings) {
        if (definedNames.has(name)) continue;
        if (calledAsFunction.has(name)) {
          const argCount = maxCallArgs.get(name) ?? 0;
          const params = Array.from({ length: argCount }, (_, i) => `a${i}: any`).join(", ");
          lines.push(`declare function ${name}(${params}): any;`);
        } else {
          lines.push(`declare const ${name}: any;`);
        }
      }
    }
    replacements.push({
      start: imp.start,
      end: imp.end,
      text: lines.length > 0 ? lines.join("\n") : `/* side-effect import removed */`,
    });
  }
  let result = source;
  replacements.sort((a, b) => b.start - a.start);
  for (const r of replacements) {
    result = result.substring(0, r.start) + r.text + result.substring(r.end);
  }
  return result;
}

// src/optimize.ts
var _nodeImports = null;
function getNodeImportsSync() {
  if (_nodeImports) return _nodeImports;
  try {
    const cp = __require("node:child_process");
    const fs = __require("node:fs");
    const path = __require("node:path");
    const os = __require("node:os");
    _nodeImports = {
      execFileSync: cp.execFileSync,
      writeFileSync: fs.writeFileSync,
      readFileSync: fs.readFileSync,
      unlinkSync: fs.unlinkSync,
      mkdtempSync: fs.mkdtempSync,
      join: path.join,
      tmpdir: os.tmpdir,
    };
    return _nodeImports;
  } catch {
    return null;
  }
}
function optimizeBinary(binary, options = {}) {
  const level = options.level ?? 3;
  const gc = options.gc !== false;
  const referenceTypes = options.referenceTypes !== false;
  const exceptionHandling = options.exceptionHandling !== false;
  try {
    const result = optimizeWithBinaryenPackage(binary, level, gc, referenceTypes, exceptionHandling);
    if (result) return result;
  } catch {}
  try {
    const result = optimizeWithSystemBinary(binary, level, gc, referenceTypes, exceptionHandling);
    if (result) return result;
  } catch {}
  return {
    binary,
    optimized: false,
    warning:
      "wasm-opt not available: install the 'binaryen' npm package or add wasm-opt to PATH. Skipping optimization.",
  };
}
function optimizeWithBinaryenPackage(binary, level, gc, referenceTypes, exceptionHandling) {
  let binaryen;
  try {
    binaryen = __require("binaryen");
  } catch {
    return null;
  }
  return optimizeWithBinaryenModule(binaryen, binary, level, gc, referenceTypes, exceptionHandling);
}
function optimizeWithBinaryenModule(binaryen, binary, level, gc, referenceTypes, exceptionHandling) {
  const featureFlags = binaryen.Features ?? binaryen.features;
  if (!featureFlags) return null;
  let mod;
  try {
    mod = binaryen.readBinary(binary);
  } catch (e) {
    return null;
  }
  try {
    const previousOptimizeLevel =
      typeof binaryen.getOptimizeLevel === "function" ? binaryen.getOptimizeLevel() : void 0;
    const previousShrinkLevel = typeof binaryen.getShrinkLevel === "function" ? binaryen.getShrinkLevel() : void 0;
    let features = 0;
    if (gc) features |= featureFlags.GC | featureFlags.ReferenceTypes;
    if (referenceTypes) features |= featureFlags.ReferenceTypes;
    if (exceptionHandling) features |= featureFlags.ExceptionHandling;
    features |= featureFlags.BulkMemory;
    features |= featureFlags.MutableGlobals;
    mod.setFeatures(features);
    if (typeof binaryen.setOptimizeLevel === "function") {
      binaryen.setOptimizeLevel(level >= 4 ? 3 : level);
    }
    if (typeof binaryen.setShrinkLevel === "function") {
      binaryen.setShrinkLevel(level >= 4 ? 1 : 0);
    }
    try {
      mod.optimize();
      if (level >= 4) mod.optimize();
    } finally {
      if (typeof binaryen.setOptimizeLevel === "function" && previousOptimizeLevel !== void 0) {
        binaryen.setOptimizeLevel(previousOptimizeLevel);
      }
      if (typeof binaryen.setShrinkLevel === "function" && previousShrinkLevel !== void 0) {
        binaryen.setShrinkLevel(previousShrinkLevel);
      }
    }
    const optimizedBinary = mod.emitBinary();
    return { binary: new Uint8Array(optimizedBinary), optimized: true };
  } finally {
    mod.dispose();
  }
}
function optimizeWithSystemBinary(binary, level, gc, referenceTypes, exceptionHandling) {
  const n = getNodeImportsSync();
  if (!n) return null;
  let wasmOptPath;
  try {
    wasmOptPath = n.execFileSync("which", ["wasm-opt"], { encoding: "utf-8" }).trim();
  } catch {
    return null;
  }
  if (!wasmOptPath) return null;
  const tmpDir = n.mkdtempSync(n.join(n.tmpdir(), "js2wasm-opt-"));
  const inputPath = n.join(tmpDir, "input.wasm");
  const outputPath = n.join(tmpDir, "output.wasm");
  try {
    n.writeFileSync(inputPath, binary);
    const args = [inputPath, `-O${level}`, "-o", outputPath];
    if (gc) {
      args.push("--enable-gc");
    }
    if (referenceTypes) {
      args.push("--enable-reference-types");
    }
    if (exceptionHandling) {
      args.push("--enable-exception-handling");
    }
    n.execFileSync(wasmOptPath, args, {
      timeout: 6e4,
      // 60 second timeout
      stdio: "pipe",
    });
    const optimizedBinary = n.readFileSync(outputPath);
    return { binary: new Uint8Array(optimizedBinary), optimized: true };
  } finally {
    try {
      n.unlinkSync(inputPath);
    } catch {}
    try {
      n.unlinkSync(outputPath);
    } catch {}
    try {
      n.unlinkSync(tmpDir);
    } catch {
      try {
        const fs = __require("node:fs");
        fs.rmdirSync(tmpDir);
      } catch {}
    }
  }
}

// src/emit/c-header.ts
function wasmTypeToCType(type) {
  switch (type.kind) {
    case "i32":
      return "int32_t";
    case "i64":
      return "int64_t";
    case "f32":
      return "float";
    case "f64":
      return "double";
    default:
      return "int32_t";
  }
}
function generateCHeader(moduleName, exports) {
  const guard = moduleName.toUpperCase().replace(/[^A-Z0-9]/g, "_") + "_H";
  const lines = [];
  lines.push("// Generated by js2wasm");
  lines.push(`#ifndef ${guard}`);
  lines.push(`#define ${guard}`);
  lines.push("");
  lines.push("#include <stdint.h>");
  lines.push("");
  for (const exp of exports) {
    if (exp.name === "memory") continue;
    const returnType = exp.results.length === 0 ? "void" : wasmTypeToCType(exp.results[0]);
    const paramList =
      exp.params.length === 0 ? "void" : exp.params.map((p, i) => `${wasmTypeToCType(p)} p${i}`).join(", ");
    if (exp.results.length > 1) {
      const outParams = exp.results
        .slice(1)
        .map((r, i) => `${wasmTypeToCType(r)}* out_${i}`)
        .join(", ");
      const allParams = exp.params.length === 0 ? outParams : `${paramList}, ${outParams}`;
      lines.push(`${returnType} ${exp.name}(${allParams});`);
    } else {
      lines.push(`${returnType} ${exp.name}(${paramList});`);
    }
  }
  lines.push("");
  lines.push(`#endif /* ${guard} */`);
  lines.push("");
  return lines.join("\n");
}
function extractCHeaderExports(mod) {
  const numImportFuncs = mod.imports.filter((i) => i.desc.kind === "func").length;
  const result = [];
  for (const exp of mod.exports) {
    if (exp.desc.kind !== "func") continue;
    if (exp.name === "memory") continue;
    const funcIdx = exp.desc.index;
    const localIdx = funcIdx - numImportFuncs;
    if (localIdx < 0 || localIdx >= mod.functions.length) continue;
    const func = mod.functions[localIdx];
    const typeDef = mod.types[func.typeIdx];
    if (!typeDef || typeDef.kind !== "func") continue;
    result.push({
      name: exp.name,
      params: typeDef.params,
      results: typeDef.results,
    });
  }
  return result;
}

// src/codegen-linear/c-abi.ts
function mapParamsToCabi(params) {
  const result = [];
  for (let i = 0; i < params.length; i++) {
    const p = params[i];
    switch (p.semantic) {
      case "string":
      case "array":
        result.push({
          name: `${p.name}_ptr`,
          wasmType: { kind: "i32" },
          sourceParamIdx: i,
          role: "ptr",
        });
        result.push({
          name: `${p.name}_len`,
          wasmType: { kind: "i32" },
          sourceParamIdx: i,
          role: "len",
        });
        break;
      case "boolean":
        result.push({
          name: p.name,
          wasmType: { kind: "i32" },
          sourceParamIdx: i,
          role: "direct",
        });
        break;
      case "number_i32":
        result.push({
          name: p.name,
          wasmType: { kind: "i32" },
          sourceParamIdx: i,
          role: "direct",
        });
        break;
      case "number_f64":
        result.push({
          name: p.name,
          wasmType: { kind: "f64" },
          sourceParamIdx: i,
          role: "direct",
        });
        break;
      default:
        result.push({
          name: p.name,
          wasmType: { kind: "i32" },
          sourceParamIdx: i,
          role: "direct",
        });
        break;
    }
  }
  return result;
}
function mapResultToCabi(result, semantic) {
  if (result === null || semantic === "void") {
    return { wasmTypes: [], semantic: "void" };
  }
  switch (semantic) {
    case "string":
    case "array":
      return { wasmTypes: [{ kind: "i32" }, { kind: "i32" }], semantic };
    case "boolean":
      return { wasmTypes: [{ kind: "i32" }], semantic };
    case "number_i32":
      return { wasmTypes: [{ kind: "i32" }], semantic };
    case "number_f64":
      return { wasmTypes: [{ kind: "f64" }], semantic };
    default:
      return { wasmTypes: [{ kind: "i32" }], semantic };
  }
}
function emitCabiWrappers(mod, exportInfos) {
  const exportReplacements = /* @__PURE__ */ new Map();
  for (const info of exportInfos) {
    const needsWrapper =
      info.params.some((p) => p.role === "ptr" || p.role === "len") ||
      info.result.semantic === "string" ||
      info.result.semantic === "array";
    if (!needsWrapper) {
      if (info.tsName !== info.cabiName) {
        for (const exp of mod.exports) {
          if (exp.name === info.tsName && exp.desc.kind === "func") {
            exp.name = info.cabiName;
            break;
          }
        }
      }
      continue;
    }
    let origFuncIdx = -1;
    for (const exp of mod.exports) {
      if (exp.name === info.tsName && exp.desc.kind === "func") {
        origFuncIdx = exp.desc.index;
        break;
      }
    }
    if (origFuncIdx === -1) continue;
    const numImportFuncs = mod.imports.filter((i) => i.desc.kind === "func").length;
    const origFunc = origFuncIdx >= numImportFuncs ? mod.functions[origFuncIdx - numImportFuncs] : null;
    if (!origFunc) continue;
    const origType = mod.types[origFunc.typeIdx];
    const wrapperParamTypes = info.params.map((p) => p.wasmType);
    const wrapperResultTypes = info.result.wasmTypes;
    const wrapperTypeIdx = mod.types.length;
    mod.types.push({
      kind: "func",
      name: `$type___cabi_${info.cabiName}`,
      params: wrapperParamTypes,
      results: wrapperResultTypes,
    });
    const body = [];
    let cabiParamIdx = 0;
    for (let origIdx = 0; origIdx < (origType.params?.length ?? 0); origIdx++) {
      const cabiParam = info.params[cabiParamIdx];
      if (cabiParam && cabiParam.role === "ptr") {
        body.push({ op: "local.get", index: cabiParamIdx });
        cabiParamIdx += 2;
      } else {
        body.push({ op: "local.get", index: cabiParamIdx });
        cabiParamIdx++;
      }
    }
    body.push({ op: "call", funcIdx: origFuncIdx });
    if (info.result.semantic === "string" || info.result.semantic === "array") {
      const retLocal = wrapperParamTypes.length;
      const wrapperLocals = [{ name: "__ret_ptr", type: { kind: "i32" } }];
      body.splice(body.length, 0);
      const callIdx = body.length - 1;
      body.push({ op: "local.tee", index: retLocal });
      body.push({ op: "i32.const", value: 4 });
      body.push({ op: "i32.add" });
      body.push({ op: "local.get", index: retLocal });
      body.push({ op: "i32.load", align: 2, offset: 0 });
      const wrapperFuncIdx = numImportFuncs + mod.functions.length;
      mod.functions.push({
        name: `__cabi_${info.cabiName}`,
        typeIdx: wrapperTypeIdx,
        locals: wrapperLocals,
        body,
        exported: true,
      });
      exportReplacements.set(info.tsName, wrapperFuncIdx);
      mod.exports.push({
        name: info.cabiName,
        desc: { kind: "func", index: wrapperFuncIdx },
      });
    } else {
      const wrapperFuncIdx = numImportFuncs + mod.functions.length;
      mod.functions.push({
        name: `__cabi_${info.cabiName}`,
        typeIdx: wrapperTypeIdx,
        locals: [],
        body,
        exported: true,
      });
      exportReplacements.set(info.tsName, wrapperFuncIdx);
      mod.exports.push({
        name: info.cabiName,
        desc: { kind: "func", index: wrapperFuncIdx },
      });
    }
    const origExportIdx = mod.exports.findIndex((e) => e.name === info.tsName && e.desc.kind === "func");
    if (origExportIdx !== -1) {
      mod.exports.splice(origExportIdx, 1);
    }
  }
}

// src/wit-generator.ts
import ts18 from "typescript";
function generateWit(ast, options) {
  const packageName = options?.packageName ?? "local:module";
  const worldName = options?.worldName ?? "module";
  const records = [];
  const recordNames = /* @__PURE__ */ new Set();
  const funcs = [];
  const sf = ast.sourceFile;
  const checker = ast.checker;
  for (const stmt of sf.statements) {
    if (ts18.isInterfaceDeclaration(stmt) && hasExportModifier2(stmt)) {
      const rec = interfaceToRecord(stmt, sf, checker, records, recordNames);
      if (rec && !recordNames.has(rec.name)) {
        records.push(rec);
        recordNames.add(rec.name);
      }
    }
    if (ts18.isTypeAliasDeclaration(stmt) && hasExportModifier2(stmt)) {
      const rec = typeAliasToRecord(stmt, sf, checker, records, recordNames);
      if (rec && !recordNames.has(rec.name)) {
        records.push(rec);
        recordNames.add(rec.name);
      }
    }
  }
  for (const stmt of sf.statements) {
    if (ts18.isFunctionDeclaration(stmt) && stmt.name && hasExportModifier2(stmt)) {
      const witFunc = functionToWit(stmt, sf, checker, records, recordNames);
      if (witFunc) {
        funcs.push(witFunc);
      }
    }
  }
  const lines = [];
  lines.push(`package ${packageName};`);
  lines.push("");
  lines.push(`world ${worldName} {`);
  for (const rec of records) {
    lines.push(`  record ${rec.name} {`);
    for (const field of rec.fields) {
      lines.push(`    ${field.name}: ${field.type},`);
    }
    lines.push("  }");
    lines.push("");
  }
  for (const func of funcs) {
    const params = func.params.map((p) => `${p.name}: ${p.type}`).join(", ");
    const returnPart = func.result ? ` -> ${func.result}` : "";
    lines.push(`  export ${func.name}: func(${params})${returnPart};`);
  }
  lines.push("}");
  lines.push("");
  return lines.join("\n");
}
function mapTypeToWit(typeNode, sf, checker, records, recordNames) {
  if (!typeNode) return null;
  if (ts18.isToken(typeNode)) {
    switch (typeNode.kind) {
      case ts18.SyntaxKind.NumberKeyword:
        return "f64";
      case ts18.SyntaxKind.StringKeyword:
        return "string";
      case ts18.SyntaxKind.BooleanKeyword:
        return "bool";
      case ts18.SyntaxKind.VoidKeyword:
        return null;
      case ts18.SyntaxKind.UndefinedKeyword:
        return null;
      case ts18.SyntaxKind.NullKeyword:
        return null;
      case ts18.SyntaxKind.AnyKeyword:
        return "string";
    }
  }
  if (ts18.isArrayTypeNode(typeNode)) {
    const elemType = mapTypeToWit(typeNode.elementType, sf, checker, records, recordNames);
    if (elemType) return `list<${elemType}>`;
    return null;
  }
  if (ts18.isTypeReferenceNode(typeNode)) {
    const typeName = typeNode.typeName.getText(sf);
    if (typeName === "Array" && typeNode.typeArguments?.length === 1) {
      const elemType = mapTypeToWit(typeNode.typeArguments[0], sf, checker, records, recordNames);
      if (elemType) return `list<${elemType}>`;
      return null;
    }
    const witName = toKebabCase(typeName);
    if (recordNames.has(witName)) {
      return witName;
    }
    const type = checker.getTypeAtLocation(typeNode);
    const rec = resolveObjectTypeToRecord(witName, type, checker, records, recordNames);
    if (rec) {
      return witName;
    }
    return null;
  }
  if (ts18.isUnionTypeNode(typeNode)) {
    const nonNullTypes = typeNode.types.filter((t) => {
      if (ts18.isToken(t)) {
        return t.kind !== ts18.SyntaxKind.NullKeyword && t.kind !== ts18.SyntaxKind.UndefinedKeyword;
      }
      if (ts18.isLiteralTypeNode(t) && t.literal.kind === ts18.SyntaxKind.NullKeyword) {
        return false;
      }
      return true;
    });
    const hasNull = nonNullTypes.length < typeNode.types.length;
    if (nonNullTypes.length === 1) {
      const inner = mapTypeToWit(nonNullTypes[0], sf, checker, records, recordNames);
      if (inner && hasNull) return `option<${inner}>`;
      return inner;
    }
    return null;
  }
  if (ts18.isTypeLiteralNode(typeNode)) {
    const fields = [];
    for (const member of typeNode.members) {
      if (ts18.isPropertySignature(member) && member.name && member.type) {
        const fieldName = toKebabCase(member.name.getText(sf));
        const fieldType = mapTypeToWit(member.type, sf, checker, records, recordNames);
        if (fieldType) {
          fields.push({ name: fieldName, type: fieldType });
        }
      }
    }
    if (fields.length > 0) {
      const anonName = `anon-record-${records.length}`;
      if (!recordNames.has(anonName)) {
        records.push({ name: anonName, fields });
        recordNames.add(anonName);
      }
      return anonName;
    }
    return null;
  }
  if (ts18.isParenthesizedTypeNode(typeNode)) {
    return mapTypeToWit(typeNode.type, sf, checker, records, recordNames);
  }
  return null;
}
function interfaceToRecord(node, sf, checker, records, recordNames) {
  const name = toKebabCase(node.name.text);
  const fields = [];
  for (const member of node.members) {
    if (ts18.isPropertySignature(member) && member.name && member.type) {
      const fieldName = toKebabCase(member.name.getText(sf));
      const fieldType = mapTypeToWit(member.type, sf, checker, records, recordNames);
      if (fieldType) {
        fields.push({ name: fieldName, type: fieldType });
      }
    }
  }
  if (fields.length === 0) return null;
  return { name, fields };
}
function typeAliasToRecord(node, sf, checker, records, recordNames) {
  const name = toKebabCase(node.name.text);
  if (ts18.isTypeLiteralNode(node.type)) {
    const fields = [];
    for (const member of node.type.members) {
      if (ts18.isPropertySignature(member) && member.name && member.type) {
        const fieldName = toKebabCase(member.name.getText(sf));
        const fieldType = mapTypeToWit(member.type, sf, checker, records, recordNames);
        if (fieldType) {
          fields.push({ name: fieldName, type: fieldType });
        }
      }
    }
    if (fields.length > 0) return { name, fields };
  }
  return null;
}
function resolveObjectTypeToRecord(witName, type, checker, records, recordNames) {
  if (recordNames.has(witName)) return null;
  const props = type.getProperties();
  if (props.length === 0) return null;
  recordNames.add(witName);
  const fields = [];
  for (const prop of props) {
    const decl = prop.valueDeclaration;
    if (!decl) continue;
    const propType = checker.getTypeOfSymbolAtLocation(prop, decl);
    const witType = mapTsTypeToWit(propType, checker, records, recordNames);
    if (witType) {
      fields.push({ name: toKebabCase(prop.name), type: witType });
    }
  }
  if (fields.length === 0) {
    recordNames.delete(witName);
    return null;
  }
  const rec = { name: witName, fields };
  records.push(rec);
  return rec;
}
function mapTsTypeToWit(type, checker, records, recordNames) {
  if (type.flags & ts18.TypeFlags.Number) return "f64";
  if (type.flags & ts18.TypeFlags.String) return "string";
  if (type.flags & ts18.TypeFlags.Boolean || type.flags & ts18.TypeFlags.BooleanLiteral) return "bool";
  if (type.flags & ts18.TypeFlags.Void) return null;
  if (type.flags & ts18.TypeFlags.Undefined) return null;
  if (type.flags & ts18.TypeFlags.Null) return null;
  const numberIndex = type.getNumberIndexType();
  if (numberIndex && checker.isArrayType(type)) {
    const elemType = mapTsTypeToWit(numberIndex, checker, records, recordNames);
    if (elemType) return `list<${elemType}>`;
  }
  return null;
}
function functionToWit(node, sf, checker, records, recordNames) {
  if (!node.name) return null;
  const name = toKebabCase(node.name.text);
  const params = [];
  for (const param of node.parameters) {
    const paramName = ts18.isIdentifier(param.name) ? toKebabCase(param.name.text) : `p${params.length}`;
    const paramType = mapTypeToWit(param.type, sf, checker, records, recordNames);
    if (paramType) {
      params.push({ name: paramName, type: paramType });
    }
  }
  const result = mapTypeToWit(node.type, sf, checker, records, recordNames);
  return { name, params, result };
}
function hasExportModifier2(node) {
  const modifiers = ts18.canHaveModifiers(node) ? ts18.getModifiers(node) : void 0;
  return modifiers?.some((m) => m.kind === ts18.SyntaxKind.ExportKeyword) ?? false;
}
function toKebabCase(name) {
  return name
    .replace(/([a-z0-9])([A-Z])/g, "$1-$2")
    .replace(/([A-Z])([A-Z][a-z])/g, "$1-$2")
    .toLowerCase();
}

// src/compiler.ts
var DEFAULT_BLOCKED_MEMBERS = /* @__PURE__ */ new Set([
  "__proto__",
  "constructor",
  "prototype",
  "valueOf",
  "toString",
  "innerHTML",
  "outerHTML",
  "insertAdjacentHTML",
]);
function validateSafeMode(sourceFile, checker, options) {
  const errors = [];
  const allowedGlobals = new Set(options.allowedGlobals ?? []);
  const allowedMembers = options.allowedExternMembers ?? {};
  function pos(node) {
    const { line, character } = sourceFile.getLineAndCharacterOfPosition(node.getStart());
    return { line: line + 1, column: character + 1 };
  }
  function visit(node) {
    if (ts19.isVariableStatement(node) && node.modifiers?.some((m) => m.kind === ts19.SyntaxKind.DeclareKeyword)) {
      for (const decl of node.declarationList.declarations) {
        const name = decl.name.getText();
        if (!allowedGlobals.has(name)) {
          const p = pos(decl);
          errors.push({
            message: `Safe mode: declared global "${name}" is not in allowedGlobals`,
            line: p.line,
            column: p.column,
            severity: "error",
          });
        }
        if (decl.type) {
          const t = checker.getTypeAtLocation(decl.type);
          if (t.flags & ts19.TypeFlags.Any) {
            const p = pos(decl.type);
            errors.push({
              message: `Safe mode: "any" type on declared global "${name}" is not allowed`,
              line: p.line,
              column: p.column,
              severity: "error",
            });
          }
        }
      }
    }
    if (ts19.isClassDeclaration(node) && node.modifiers?.some((m) => m.kind === ts19.SyntaxKind.DeclareKeyword)) {
      const className = node.name?.getText() ?? "(anonymous)";
      const allowed = allowedMembers[className];
      for (const member of node.members) {
        const memberName = member.name?.getText();
        if (!memberName) continue;
        if (DEFAULT_BLOCKED_MEMBERS.has(memberName)) {
          const p = pos(member);
          errors.push({
            message: `Safe mode: extern class "${className}" member "${memberName}" is blocked`,
            line: p.line,
            column: p.column,
            severity: "error",
          });
          continue;
        }
        if (allowed && !allowed.includes(memberName)) {
          const p = pos(member);
          errors.push({
            message: `Safe mode: extern class "${className}" member "${memberName}" is not in allowedExternMembers`,
            line: p.line,
            column: p.column,
            severity: "error",
          });
          continue;
        }
        if (ts19.isPropertyDeclaration(member) && member.type) {
          const t = checker.getTypeAtLocation(member.type);
          if (t.flags & ts19.TypeFlags.Any) {
            const p = pos(member.type);
            errors.push({
              message: `Safe mode: "any" type on extern class "${className}.${memberName}" is not allowed`,
              line: p.line,
              column: p.column,
              severity: "error",
            });
          }
        }
      }
    }
    if (ts19.isElementAccessExpression(node)) {
      const objType = checker.getTypeAtLocation(node.expression);
      const objSymbol = objType.getSymbol();
      if (objSymbol) {
        const decls = objSymbol.getDeclarations() ?? [];
        const isDeclaredClass = decls.some(
          (d) => ts19.isClassDeclaration(d) && d.modifiers?.some((m) => m.kind === ts19.SyntaxKind.DeclareKeyword),
        );
        if (isDeclaredClass) {
          const p = pos(node);
          errors.push({
            message: `Safe mode: dynamic property access on extern class "${objSymbol.getName()}" is not allowed`,
            line: p.line,
            column: p.column,
            severity: "error",
          });
        }
      }
    }
    ts19.forEachChild(node, visit);
  }
  visit(sourceFile);
  return errors;
}
function detectEarlyErrors(sourceFile) {
  const errors = [];
  function pos(node) {
    const { line, character } = sourceFile.getLineAndCharacterOfPosition(node.getStart());
    return { line: line + 1, column: character + 1 };
  }
  function addError(node, message) {
    const p = pos(node);
    errors.push({ message, line: p.line, column: p.column, severity: "error" });
  }
  function isStrictMode(node) {
    let current = node;
    while (current) {
      if (ts19.isSourceFile(current)) {
        for (const stmt of current.statements) {
          if (ts19.isExpressionStatement(stmt) && ts19.isStringLiteral(stmt.expression)) {
            if (stmt.expression.text === "use strict") return true;
          } else {
            break;
          }
        }
        return false;
      }
      if (ts19.isClassDeclaration(current) || ts19.isClassExpression(current)) {
        return true;
      }
      if (
        ts19.isFunctionDeclaration(current) ||
        ts19.isFunctionExpression(current) ||
        ts19.isArrowFunction(current) ||
        ts19.isMethodDeclaration(current)
      ) {
        if (current.body && ts19.isBlock(current.body)) {
          for (const stmt of current.body.statements) {
            if (ts19.isExpressionStatement(stmt) && ts19.isStringLiteral(stmt.expression)) {
              if (stmt.expression.text === "use strict") return true;
            } else {
              break;
            }
          }
        }
      }
      current = current.parent;
    }
    return false;
  }
  function isArgumentsOrEval(node) {
    if (ts19.isIdentifier(node)) {
      if (node.text === "arguments" || node.text === "eval") {
        return node.text;
      }
    }
    if (ts19.isParenthesizedExpression(node)) {
      return isArgumentsOrEval(node.expression);
    }
    return null;
  }
  function isSimpleAssignmentTarget(node) {
    if (ts19.isIdentifier(node)) return true;
    if (ts19.isPropertyAccessExpression(node)) return true;
    if (ts19.isElementAccessExpression(node)) return true;
    if (ts19.isParenthesizedExpression(node)) {
      return isSimpleAssignmentTarget(node.expression);
    }
    return false;
  }
  function collectBindingNamesWithDuplicateCheck(name, out, dupes) {
    if (ts19.isIdentifier(name)) {
      if (out.has(name.text)) dupes.add(name.text);
      out.add(name.text);
    } else if (ts19.isObjectBindingPattern(name)) {
      for (const el of name.elements) {
        collectBindingNamesWithDuplicateCheck(el.name, out, dupes);
      }
    } else if (ts19.isArrayBindingPattern(name)) {
      for (const el of name.elements) {
        if (ts19.isBindingElement(el)) {
          collectBindingNamesWithDuplicateCheck(el.name, out, dupes);
        }
      }
    }
  }
  function collectBindingNames2(name, out) {
    if (ts19.isIdentifier(name)) {
      out.add(name.text);
    } else if (ts19.isObjectBindingPattern(name)) {
      for (const el of name.elements) {
        collectBindingNames2(el.name, out);
      }
    } else if (ts19.isArrayBindingPattern(name)) {
      for (const el of name.elements) {
        if (ts19.isBindingElement(el)) {
          collectBindingNames2(el.name, out);
        }
      }
    }
  }
  function checkDuplicateParams(params, node) {
    const alwaysForbid =
      ts19.isArrowFunction(node) ||
      ts19.isMethodDeclaration(node) ||
      ts19.isConstructorDeclaration(node) ||
      ts19.isGetAccessorDeclaration(node) ||
      ts19.isSetAccessorDeclaration(node) ||
      ((ts19.isFunctionDeclaration(node) || ts19.isFunctionExpression(node)) &&
        (node.asteriskToken !== void 0 || node.modifiers?.some((m) => m.kind === ts19.SyntaxKind.AsyncKeyword))) ||
      params.some((p) => p.initializer !== void 0 || p.dotDotDotToken !== void 0 || !ts19.isIdentifier(p.name));
    if (!alwaysForbid && !isStrictMode(node)) return;
    const seen = /* @__PURE__ */ new Set();
    for (const param of params) {
      const names = /* @__PURE__ */ new Set();
      collectBindingNames2(param.name, names);
      for (const name of names) {
        if (seen.has(name)) {
          addError(param, `Duplicate parameter name '${name}' not allowed`);
        }
        seen.add(name);
      }
    }
  }
  function isInvalidAssignmentTarget(node, allowDestructuring = false) {
    let expr = node;
    while (ts19.isParenthesizedExpression(expr)) expr = expr.expression;
    if (ts19.isIdentifier(expr)) return false;
    if (ts19.isPropertyAccessExpression(expr)) return false;
    if (ts19.isElementAccessExpression(expr)) return false;
    if (allowDestructuring) {
      if (ts19.isObjectLiteralExpression(expr)) return false;
      if (ts19.isArrayLiteralExpression(expr)) return false;
    }
    return true;
  }
  function isCallExpressionTarget(node) {
    let expr = node;
    while (ts19.isParenthesizedExpression(expr)) expr = expr.expression;
    return ts19.isCallExpression(expr);
  }
  function hasOptionalChain(node) {
    let expr = node;
    while (ts19.isParenthesizedExpression(expr)) expr = expr.expression;
    if (
      (ts19.isPropertyAccessExpression(expr) || ts19.isElementAccessExpression(expr) || ts19.isCallExpression(expr)) &&
      expr.questionDotToken
    ) {
      return true;
    }
    if (ts19.isPropertyAccessExpression(expr) || ts19.isElementAccessExpression(expr)) {
      return hasOptionalChain(expr.expression);
    }
    return false;
  }
  function visit(node) {
    if (
      ts19.isPrefixUnaryExpression(node) &&
      (node.operator === ts19.SyntaxKind.PlusPlusToken || node.operator === ts19.SyntaxKind.MinusMinusToken)
    ) {
      const name = isArgumentsOrEval(node.operand);
      if (name && isStrictMode(node)) {
        addError(node, `Invalid use of '${name}' in strict mode`);
      }
      if (hasOptionalChain(node.operand)) {
        addError(node, "Optional chaining is not valid in the left-hand side of an update expression");
      }
      if (isInvalidAssignmentTarget(node.operand)) {
        addError(node, "Invalid left-hand side expression in prefix operation");
      }
      if (isCallExpressionTarget(node.operand) && isStrictMode(node)) {
        addError(node, "Invalid left-hand side expression in prefix operation");
      }
    }
    if (
      ts19.isPostfixUnaryExpression(node) &&
      (node.operator === ts19.SyntaxKind.PlusPlusToken || node.operator === ts19.SyntaxKind.MinusMinusToken)
    ) {
      const name = isArgumentsOrEval(node.operand);
      if (name && isStrictMode(node)) {
        addError(node, `Invalid use of '${name}' in strict mode`);
      }
      if (hasOptionalChain(node.operand)) {
        addError(node, "Optional chaining is not valid in the left-hand side of an update expression");
      }
      if (isInvalidAssignmentTarget(node.operand)) {
        addError(node, "Invalid left-hand side in postfix operation");
      }
      if (isCallExpressionTarget(node.operand) && isStrictMode(node)) {
        addError(node, "Invalid left-hand side in postfix operation");
      }
    }
    if (ts19.isBinaryExpression(node) && node.operatorToken.kind === ts19.SyntaxKind.EqualsToken) {
      const name = isArgumentsOrEval(node.left);
      if (name && isStrictMode(node)) {
        addError(node.left, `Cannot assign to '${name}' in strict mode`);
      }
      if (
        isInvalidAssignmentTarget(
          node.left,
          /* allowDestructuring */
          true,
        )
      ) {
        addError(node, "Invalid left-hand side in assignment");
      }
    }
    if (ts19.isBinaryExpression(node)) {
      const op = node.operatorToken.kind;
      const compoundOps = [
        ts19.SyntaxKind.PlusEqualsToken,
        ts19.SyntaxKind.MinusEqualsToken,
        ts19.SyntaxKind.AsteriskEqualsToken,
        ts19.SyntaxKind.SlashEqualsToken,
        ts19.SyntaxKind.PercentEqualsToken,
        ts19.SyntaxKind.AmpersandEqualsToken,
        ts19.SyntaxKind.BarEqualsToken,
        ts19.SyntaxKind.CaretEqualsToken,
        ts19.SyntaxKind.LessThanLessThanEqualsToken,
        ts19.SyntaxKind.GreaterThanGreaterThanEqualsToken,
        ts19.SyntaxKind.GreaterThanGreaterThanGreaterThanEqualsToken,
        ts19.SyntaxKind.AsteriskAsteriskEqualsToken,
        ts19.SyntaxKind.AmpersandAmpersandEqualsToken,
        ts19.SyntaxKind.BarBarEqualsToken,
        ts19.SyntaxKind.QuestionQuestionEqualsToken,
      ];
      if (compoundOps.includes(op)) {
        const name = isArgumentsOrEval(node.left);
        if (name && isStrictMode(node)) {
          addError(node.left, `Cannot assign to '${name}' in strict mode`);
        }
        if (isInvalidAssignmentTarget(node.left)) {
          addError(node, "Invalid left-hand side in assignment");
        }
      }
    }
    if (
      (ts19.isForInStatement(node) || ts19.isForOfStatement(node)) &&
      !ts19.isVariableDeclarationList(node.initializer)
    ) {
      if (isInvalidAssignmentTarget(node.initializer)) {
        addError(node.initializer, "Invalid left-hand side in for-in/for-of");
      }
    }
    if (ts19.isFunctionDeclaration(node) && node.parameters) {
      checkDuplicateParams(node.parameters, node);
    }
    if (ts19.isFunctionExpression(node) && node.parameters) {
      checkDuplicateParams(node.parameters, node);
    }
    if (ts19.isArrowFunction(node) && node.parameters) {
      checkDuplicateParams(node.parameters, node);
    }
    if (ts19.isMethodDeclaration(node) && node.parameters) {
      checkDuplicateParams(node.parameters, node);
    }
    if (ts19.isVariableDeclaration(node) && ts19.isIdentifier(node.name) && node.name.text === "yield") {
      let parent = node.parent;
      while (parent) {
        if ((ts19.isFunctionDeclaration(parent) || ts19.isFunctionExpression(parent)) && parent.asteriskToken) {
          addError(node.name, "'yield' is a reserved word and cannot be used as an identifier in generator functions");
          break;
        }
        if (
          ts19.isFunctionDeclaration(parent) ||
          ts19.isFunctionExpression(parent) ||
          ts19.isArrowFunction(parent) ||
          ts19.isMethodDeclaration(parent)
        ) {
          break;
        }
        parent = parent.parent;
      }
    }
    if (ts19.isFunctionDeclaration(node)) {
      const parent = node.parent;
      if (parent && isStatementPosition(parent, node)) {
        if (node.asteriskToken) {
          addError(node, "Generator declarations are not allowed in statement position");
        } else if (hasAsyncModifier2(node)) {
          addError(node, "Async function declarations are not allowed in statement position");
        } else if (isStrictMode(node)) {
          addError(node, "In strict mode code, functions can only be declared at top level or inside a block");
        } else if (!ts19.isIfStatement(parent) && !ts19.isLabeledStatement(parent)) {
          addError(node, "Function declarations are not allowed in statement position");
        }
      }
    }
    if (ts19.isClassDeclaration(node)) {
      const parent = node.parent;
      if (parent && isStatementPosition(parent, node)) {
        addError(node, "Class declaration not allowed in statement position");
      }
    }
    if (ts19.isLabeledStatement(node)) {
      const parent = node.parent;
      if (parent && isStatementPosition(parent, node)) {
        let inner = node.statement;
        while (ts19.isLabeledStatement(inner)) inner = inner.statement;
        if (ts19.isFunctionDeclaration(inner)) {
          addError(node, "Function declaration in a labeled statement within iteration/if body is a SyntaxError");
        }
        if (ts19.isClassDeclaration(inner)) {
          addError(node, "Class declaration not allowed in statement position");
        }
      }
    }
    if (ts19.isPrivateIdentifier(node)) {
      if (!isInsideClassWithPrivateName(node, node.escapedText)) {
        addError(node, `Private field '${node.text}' must be declared in an enclosing class`);
      }
    }
    if (ts19.isBlock(node)) {
      checkVarLexicalConflicts(node);
    }
    if (ts19.isSourceFile(node) || ts19.isBlock(node) || ts19.isCaseClause(node) || ts19.isDefaultClause(node)) {
      const stmts = ts19.isSourceFile(node) ? node.statements : ts19.isBlock(node) ? node.statements : node.statements;
      checkTDZInStatements(stmts);
    }
    if (ts19.isWithStatement(node) && isStrictMode(node)) {
      addError(node, "Strict mode code may not include a with statement");
    }
    if (ts19.isNumericLiteral(node) && isStrictMode(node)) {
      const text = node.getText(sourceFile);
      if (/^0[0-7]+$/.test(text) && text.length > 1) {
        addError(node, "Octal literals are not allowed in strict mode");
      }
      if (/^0\d+$/.test(text) && text.length > 1 && !/^0[oOxXbB]/.test(text)) {
        if (/[89]/.test(text)) {
          addError(node, "Decimals with leading zeros are not allowed in strict mode");
        }
      }
    }
    if (ts19.isDeleteExpression(node) && isStrictMode(node)) {
      let operand = node.expression;
      while (ts19.isParenthesizedExpression(operand)) {
        operand = operand.expression;
      }
      if (ts19.isIdentifier(operand)) {
        addError(node, `Delete of an unqualified identifier in strict mode`);
      }
    }
    if (ts19.isDeleteExpression(node)) {
      let operand = node.expression;
      while (ts19.isParenthesizedExpression(operand)) {
        operand = operand.expression;
      }
      if (ts19.isPropertyAccessExpression(operand) && ts19.isPrivateIdentifier(operand.name)) {
        addError(node, `Deleting a private field is a SyntaxError`);
      }
    }
    if (ts19.isForInStatement(node)) {
      const init = node.initializer;
      if (ts19.isVariableDeclarationList(init)) {
        const isLexical = (init.flags & ts19.NodeFlags.Let) !== 0 || (init.flags & ts19.NodeFlags.Const) !== 0;
        for (const decl of init.declarations) {
          if (decl.initializer) {
            const hasDestructuring = !ts19.isIdentifier(decl.name);
            if (isLexical || isStrictMode(node) || hasDestructuring) {
              addError(node, "for-in loop head declarations may not have initializers");
              break;
            }
          }
        }
        if (isLexical && init.declarations.length > 1) {
          addError(node, "Only a single declaration is allowed in a for-in statement");
        }
      }
    }
    if (ts19.isForOfStatement(node)) {
      const init = node.initializer;
      if (ts19.isVariableDeclarationList(init)) {
        const isLexical = (init.flags & ts19.NodeFlags.Let) !== 0 || (init.flags & ts19.NodeFlags.Const) !== 0;
        if (isLexical) {
          for (const decl of init.declarations) {
            if (decl.initializer) {
              addError(node, "for-of loop head declarations may not have initializers");
              break;
            }
          }
          if (init.declarations.length > 1) {
            addError(node, "Only a single declaration is allowed in a for-of statement");
          }
        }
      }
    }
    if (ts19.isLabeledStatement(node) && isStrictMode(node)) {
      if (ts19.isFunctionDeclaration(node.statement)) {
        addError(node, "In strict mode code, functions can only be declared at top level or inside a block");
      }
    }
    if (ts19.isParameter(node) && node.dotDotDotToken && node.initializer) {
      addError(node, "Rest parameter may not have a default initializer");
    }
    if (ts19.isBindingElement(node) && node.dotDotDotToken && node.initializer) {
      addError(node, "Rest element may not have a default initializer");
    }
    if (ts19.isArrayBindingPattern(node)) {
      let foundRest = false;
      for (const element of node.elements) {
        if (foundRest) {
          addError(element, "A rest element must be last in a destructuring pattern");
          break;
        }
        if (ts19.isBindingElement(element) && element.dotDotDotToken) {
          foundRest = true;
        }
      }
    }
    if (
      (ts19.isFunctionDeclaration(node) ||
        ts19.isFunctionExpression(node) ||
        ts19.isArrowFunction(node) ||
        ts19.isMethodDeclaration(node) ||
        ts19.isConstructorDeclaration(node) ||
        ts19.isGetAccessorDeclaration(node) ||
        ts19.isSetAccessorDeclaration(node)) &&
      node.parameters.length > 0
    ) {
      const lastParam = node.parameters[node.parameters.length - 1];
      if (lastParam.dotDotDotToken) {
        const paramEnd = lastParam.end;
        const parenClose = node.parameters.end;
        const textBetween = sourceFile.text.substring(paramEnd, parenClose);
        if (textBetween.includes(",")) {
          addError(lastParam, "A rest parameter or binding pattern may not have a trailing comma");
        }
      }
    }
    if (ts19.isIdentifier(node) && (node.text === "await" || node.text === "yield")) {
      const parent = node.parent;
      if (parent && !ts19.isYieldExpression(parent) && !ts19.isAwaitExpression(parent)) {
        const isPropertyName =
          parent &&
          ((ts19.isPropertyAccessExpression(parent) && parent.name === node) ||
            (ts19.isPropertyAssignment(parent) && parent.name === node) ||
            (ts19.isMethodDeclaration(parent) && parent.name === node) ||
            (ts19.isPropertyDeclaration(parent) && parent.name === node) ||
            (ts19.isGetAccessorDeclaration(parent) && parent.name === node) ||
            (ts19.isSetAccessorDeclaration(parent) && parent.name === node) ||
            (ts19.isEnumMember(parent) && parent.name === node) ||
            (ts19.isPropertySignature(parent) && parent.name === node) ||
            (ts19.isMethodSignature(parent) && parent.name === node));
        if (!isPropertyName) {
          if (node.text === "await" && isInsideAsyncFunction(node)) {
            addError(node, "'await' is not allowed as an identifier in an async function");
          }
          if (node.text === "yield" && isInsideGeneratorFunction(node)) {
            addError(node, "'yield' is not allowed as an identifier in a generator function");
          }
        }
      }
    }
    if (ts19.isIdentifier(node) && isStrictMode(node)) {
      const strictReserved = /* @__PURE__ */ new Set([
        "implements",
        "interface",
        "package",
        "private",
        "protected",
        "public",
        "static",
      ]);
      if (strictReserved.has(node.text)) {
        const parent = node.parent;
        const isPropertyName =
          parent &&
          ((ts19.isPropertyAccessExpression(parent) && parent.name === node) ||
            (ts19.isPropertyAssignment(parent) && parent.name === node) ||
            (ts19.isMethodDeclaration(parent) && parent.name === node) ||
            (ts19.isPropertyDeclaration(parent) && parent.name === node) ||
            (ts19.isGetAccessorDeclaration(parent) && parent.name === node) ||
            (ts19.isSetAccessorDeclaration(parent) && parent.name === node) ||
            (ts19.isPropertySignature(parent) && parent.name === node) ||
            (ts19.isMethodSignature(parent) && parent.name === node));
        const isLabel = parent && ts19.isLabeledStatement(parent) && parent.label === node;
        const isBreakContinueTarget =
          parent &&
          ((ts19.isBreakStatement(parent) && parent.label === node) ||
            (ts19.isContinueStatement(parent) && parent.label === node));
        if (!isPropertyName && !isLabel && !isBreakContinueTarget) {
          const isBinding =
            parent &&
            ((ts19.isVariableDeclaration(parent) && parent.name === node) ||
              (ts19.isParameter(parent) && parent.name === node) ||
              (ts19.isFunctionDeclaration(parent) && parent.name === node) ||
              (ts19.isFunctionExpression(parent) && parent.name === node) ||
              (ts19.isClassDeclaration(parent) && parent.name === node) ||
              (ts19.isClassExpression(parent) && parent.name === node) ||
              (ts19.isBindingElement(parent) && parent.name === node) || // Shorthand property in object literal: {implements} — IdentifierReference
              (ts19.isShorthandPropertyAssignment(parent) && parent.name === node));
          if (isBinding) {
            addError(node, `'${node.text}' is a reserved word in strict mode and cannot be used as an identifier`);
          }
        }
      }
    }
    if (
      (ts19.isFunctionDeclaration(node) ||
        ts19.isFunctionExpression(node) ||
        ts19.isArrowFunction(node) ||
        ts19.isMethodDeclaration(node) ||
        ts19.isConstructorDeclaration(node) ||
        ts19.isGetAccessorDeclaration(node) ||
        ts19.isSetAccessorDeclaration(node)) &&
      node.body &&
      ts19.isBlock(node.body)
    ) {
      const hasNonSimpleParams = node.parameters.some(
        (p) => p.initializer !== void 0 || p.dotDotDotToken !== void 0 || !ts19.isIdentifier(p.name),
        // destructuring pattern
      );
      if (hasNonSimpleParams) {
        for (const stmt of node.body.statements) {
          if (ts19.isExpressionStatement(stmt) && ts19.isStringLiteral(stmt.expression)) {
            if (stmt.expression.text === "use strict") {
              addError(stmt, "Illegal 'use strict' directive in function with non-simple parameter list");
              break;
            }
          } else {
            break;
          }
        }
      }
    }
    if (
      (ts19.isFunctionDeclaration(node) ||
        ts19.isFunctionExpression(node) ||
        ts19.isArrowFunction(node) ||
        ts19.isMethodDeclaration(node) ||
        ts19.isConstructorDeclaration(node) ||
        ts19.isGetAccessorDeclaration(node) ||
        ts19.isSetAccessorDeclaration(node)) &&
      node.body &&
      ts19.isBlock(node.body)
    ) {
      const paramNames = /* @__PURE__ */ new Set();
      for (const p of node.parameters) {
        collectBindingNames2(p.name, paramNames);
      }
      if (paramNames.size > 0) {
        for (const stmt of node.body.statements) {
          if (ts19.isVariableStatement(stmt)) {
            const flags = stmt.declarationList.flags;
            if ((flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0) {
              for (const decl of stmt.declarationList.declarations) {
                if (ts19.isIdentifier(decl.name) && paramNames.has(decl.name.text)) {
                  addError(
                    decl.name,
                    `Duplicate identifier '${decl.name.text}' \u2014 parameter and lexical declaration`,
                  );
                }
              }
            }
          }
        }
      }
    }
    if (ts19.isLabeledStatement(node)) {
      const stmt = node.statement;
      if (ts19.isVariableStatement(stmt)) {
        const flags = stmt.declarationList.flags;
        if ((flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0) {
          addError(node, "Lexical declaration (let/const) cannot appear in a labeled statement");
        }
      }
      if (ts19.isClassDeclaration(stmt)) {
        addError(node, "Class declaration cannot appear in a labeled statement");
      }
    }
    if (ts19.isVariableStatement(node)) {
      const flags = node.declarationList.flags;
      if ((flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0) {
        const parent = node.parent;
        if (parent && isStatementPosition(parent, node)) {
          addError(node, "Lexical declaration cannot appear in a single-statement context");
        }
      }
    }
    if (ts19.isVariableDeclaration(node) && !node.initializer) {
      const declList = node.parent;
      if (ts19.isVariableDeclarationList(declList) && (declList.flags & ts19.NodeFlags.Const) !== 0) {
        const declListParent = declList.parent;
        const isForOfOrIn =
          declListParent && (ts19.isForOfStatement(declListParent) || ts19.isForInStatement(declListParent));
        if (!isForOfOrIn) {
          addError(node, "Missing initializer in const declaration");
        }
      }
    }
    if (ts19.isVariableDeclaration(node) && ts19.isIdentifier(node.name) && node.name.text === "let") {
      const declList = node.parent;
      if (ts19.isVariableDeclarationList(declList)) {
        if ((declList.flags & ts19.NodeFlags.Let) !== 0 || (declList.flags & ts19.NodeFlags.Const) !== 0) {
          addError(node.name, "'let' is disallowed as a lexically bound name");
        }
      }
    }
    if (ts19.isForStatement(node) || ts19.isForInStatement(node) || ts19.isForOfStatement(node)) {
      const init = ts19.isForStatement(node) ? node.initializer : node.initializer;
      if (init && ts19.isVariableDeclarationList(init)) {
        const isLexical = (init.flags & ts19.NodeFlags.Let) !== 0 || (init.flags & ts19.NodeFlags.Const) !== 0;
        if (isLexical) {
          const lexNames = /* @__PURE__ */ new Set();
          for (const decl of init.declarations) {
            collectBindingNames2(decl.name, lexNames);
          }
          if (lexNames.size > 0) {
            const body = ts19.isForStatement(node)
              ? node.statement
              : ts19.isForInStatement(node)
                ? node.statement
                : node.statement;
            if (ts19.isBlock(body)) {
              collectVarDeclaredNamesInBlock(body, lexNames);
            }
          }
        }
      }
    }
    if (ts19.isIdentifier(node) && (node.text === "eval" || node.text === "arguments") && isStrictMode(node)) {
      const parent = node.parent;
      const isBinding =
        parent &&
        ((ts19.isVariableDeclaration(parent) && parent.name === node) ||
          (ts19.isParameter(parent) && parent.name === node) ||
          (ts19.isFunctionDeclaration(parent) && parent.name === node) ||
          (ts19.isFunctionExpression(parent) && parent.name === node) ||
          (ts19.isClassDeclaration(parent) && parent.name === node) ||
          (ts19.isClassExpression(parent) && parent.name === node) ||
          (ts19.isBindingElement(parent) && parent.name === node) ||
          (ts19.isCatchClause(parent) &&
            parent.variableDeclaration &&
            ts19.isIdentifier(parent.variableDeclaration.name) &&
            parent.variableDeclaration.name === node));
      if (isBinding) {
        addError(node, `Binding '${node.text}' in strict mode is not allowed`);
      }
    }
    if (ts19.isCaseBlock(node)) {
      checkSwitchCaseLexicalDuplicates(node);
    }
    if (ts19.isClassDeclaration(node) || ts19.isClassExpression(node)) {
      for (const member of node.members) {
        if (member.name && !ts19.isPrivateIdentifier(member.name)) {
          const isStatic = ts19.canHaveModifiers(member)
            ? (ts19.getModifiers(member)?.some((m) => m.kind === ts19.SyntaxKind.StaticKeyword) ?? false)
            : false;
          if (isStatic) {
            const memberName = ts19.isIdentifier(member.name)
              ? member.name.text
              : ts19.isStringLiteral(member.name)
                ? member.name.text
                : null;
            if (memberName === "prototype") {
              addError(member, "Classes may not have a static property named 'prototype'");
            }
          }
        }
      }
    }
    if (ts19.isClassDeclaration(node) || ts19.isClassExpression(node)) {
      checkDuplicatePrivateNames(node);
    }
    if (node.kind === ts19.SyntaxKind.RegularExpressionLiteral) {
      const text = node.text;
      const lastSlash = text.lastIndexOf("/");
      if (lastSlash > 0) {
        const pattern = text.slice(1, lastSlash);
        const flags = text.slice(lastSlash + 1);
        try {
          new RegExp(pattern, flags);
        } catch {
          addError(node, `Invalid regular expression: ${text}`);
        }
      }
    }
    if (ts19.isClassDeclaration(node) || ts19.isClassExpression(node)) {
      for (const member of node.members) {
        const memberName = getMemberName(member);
        if (memberName === "constructor") {
          const isStaticMember = member.modifiers?.some((m) => m.kind === ts19.SyntaxKind.StaticKeyword);
          if (isStaticMember) continue;
          if (ts19.isMethodDeclaration(member) && member.asteriskToken) {
            addError(member, "Class constructor may not be a generator");
          }
          if (
            ts19.isMethodDeclaration(member) &&
            member.modifiers?.some((m) => m.kind === ts19.SyntaxKind.AsyncKeyword)
          ) {
            addError(member, "Class constructor may not be an async method");
          }
          if (ts19.isGetAccessorDeclaration(member)) {
            addError(member, "Class constructor may not be a getter");
          }
          if (ts19.isSetAccessorDeclaration(member)) {
            addError(member, "Class constructor may not be a setter");
          }
        }
      }
    }
    if (ts19.isCallExpression(node) && node.expression.kind === ts19.SyntaxKind.SuperKeyword) {
      if (!isInsideClassConstructor(node)) {
        addError(node, "super() is only valid inside a class constructor");
      }
    }
    if (
      (ts19.isPropertyAccessExpression(node) || ts19.isElementAccessExpression(node)) &&
      node.expression.kind === ts19.SyntaxKind.SuperKeyword
    ) {
      if (!isInsideMethod(node)) {
        addError(node, "'super' keyword unexpected here");
      }
    }
    if (ts19.isObjectLiteralExpression(node)) {
      let protoCount = 0;
      for (const prop of node.properties) {
        if (ts19.isPropertyAssignment(prop)) {
          const propName = ts19.isIdentifier(prop.name)
            ? prop.name.text
            : ts19.isStringLiteral(prop.name)
              ? prop.name.text
              : null;
          if (propName === "__proto__") {
            protoCount++;
            if (protoCount > 1) {
              addError(prop, "Duplicate __proto__ fields are not allowed in object literals");
              break;
            }
          }
        }
      }
    }
    if (ts19.isGetAccessorDeclaration(node) && node.parameters.length > 0) {
      addError(node, "Getter must not have any formal parameters");
    }
    if (ts19.isSetAccessorDeclaration(node) && node.parameters.length !== 1) {
      addError(node, "Setter must have exactly one formal parameter");
    }
    if (ts19.isSetAccessorDeclaration(node) && node.parameters.length === 1) {
      const param = node.parameters[0];
      if (ts19.isIdentifier(param.name) && (param.name.text === "eval" || param.name.text === "arguments")) {
        if (node.body) {
          for (const stmt of node.body.statements) {
            if (
              ts19.isExpressionStatement(stmt) &&
              ts19.isStringLiteral(stmt.expression) &&
              stmt.expression.text === "use strict"
            ) {
              addError(param.name, `Binding '${param.name.text}' in strict mode is not allowed`);
              break;
            } else {
              break;
            }
          }
        }
      }
    }
    if (ts19.isShorthandPropertyAssignment(node) && node.objectAssignmentInitializer) {
      const objLit = node.parent;
      if (ts19.isObjectLiteralExpression(objLit)) {
        if (!isAssignmentPatternContext(objLit)) {
          addError(node, "Invalid shorthand property initializer");
        }
      }
    }
    if (ts19.isShorthandPropertyAssignment(node) && node.name.text === "let" && isStrictMode(node)) {
      addError(node, "'let' is not allowed as a shorthand property in strict mode");
    }
    if (ts19.isCatchClause(node) && node.variableDeclaration) {
      const catchParam = node.variableDeclaration;
      const catchNames = /* @__PURE__ */ new Set();
      const dupeNames = /* @__PURE__ */ new Set();
      collectBindingNamesWithDuplicateCheck(catchParam.name, catchNames, dupeNames);
      for (const name of dupeNames) {
        addError(catchParam, `Duplicate binding '${name}' in catch parameter`);
      }
      if (node.block && catchNames.size > 0) {
        for (const stmt of node.block.statements) {
          if (ts19.isVariableStatement(stmt)) {
            const flags = stmt.declarationList.flags;
            if ((flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0) {
              for (const decl of stmt.declarationList.declarations) {
                if (ts19.isIdentifier(decl.name) && catchNames.has(decl.name.text)) {
                  addError(decl.name, `Cannot redeclare catch variable '${decl.name.text}' with lexical declaration`);
                }
              }
            }
          }
          if (ts19.isFunctionDeclaration(stmt) && stmt.name && stmt.body) {
            if (catchNames.has(stmt.name.text)) {
              addError(stmt.name, `Cannot redeclare catch variable '${stmt.name.text}' with function declaration`);
            }
          }
          if (ts19.isClassDeclaration(stmt) && stmt.name) {
            if (catchNames.has(stmt.name.text)) {
              addError(stmt.name, `Cannot redeclare catch variable '${stmt.name.text}' with class declaration`);
            }
          }
        }
      }
    }
    if (ts19.isBlock(node) || ts19.isSourceFile(node)) {
      checkDuplicateLexicalDeclarations(node);
    }
    if (ts19.isContinueStatement(node)) {
      if (!isInsideIteration(node, node.label?.text)) {
        addError(
          node,
          node.label
            ? `A 'continue' statement can only jump to a label of an enclosing iteration statement`
            : `A 'continue' statement can only be used within an enclosing iteration statement`,
        );
      }
    }
    if (ts19.isBreakStatement(node)) {
      if (!isInsideBreakable(node, node.label?.text)) {
        addError(
          node,
          node.label
            ? `A 'break' statement can only jump to a label of an enclosing statement`
            : `A 'break' statement can only be used within an enclosing iteration or switch statement`,
        );
      }
    }
    if (ts19.isNewExpression(node)) {
      const expr = node.expression;
      if (ts19.isIdentifier(expr) && expr.text === "") {
        const start = node.getStart(sourceFile);
        const textAfter = sourceFile.text.substring(start, start + 30);
        if (/^new\s+import\s*[\.(]/.test(textAfter)) {
          addError(node, "Cannot use new with import()");
        }
      }
    }
    if (ts19.isTypeOfExpression(node)) {
      const expr = node.expression;
      if (ts19.isIdentifier(expr) && expr.text === "") {
        const start = node.getStart(sourceFile);
        const textAfter = sourceFile.text.substring(start, start + 30);
        if (/^typeof\s+import\b/.test(textAfter)) {
          addError(node, "Cannot use typeof with import");
        }
      }
    }
    ts19.forEachChild(node, visit);
  }
  function getMemberName(member) {
    if (!member.name) return null;
    if (ts19.isIdentifier(member.name)) return member.name.text;
    if (ts19.isStringLiteral(member.name)) return member.name.text;
    if (ts19.isComputedPropertyName(member.name)) {
      const expr = member.name.expression;
      if (ts19.isStringLiteral(expr)) return expr.text;
    }
    return null;
  }
  function isInsideClassConstructor(node) {
    let current = node.parent;
    while (current) {
      if (ts19.isConstructorDeclaration(current)) return true;
      if (ts19.isArrowFunction(current)) {
        current = current.parent;
        continue;
      }
      if (
        ts19.isFunctionDeclaration(current) ||
        ts19.isFunctionExpression(current) ||
        ts19.isMethodDeclaration(current) ||
        ts19.isGetAccessorDeclaration(current) ||
        ts19.isSetAccessorDeclaration(current)
      ) {
        return false;
      }
      current = current.parent;
    }
    return false;
  }
  function isInsideMethod(node) {
    let current = node.parent;
    while (current) {
      if (
        ts19.isMethodDeclaration(current) ||
        ts19.isConstructorDeclaration(current) ||
        ts19.isGetAccessorDeclaration(current) ||
        ts19.isSetAccessorDeclaration(current)
      ) {
        return true;
      }
      if (ts19.isPropertyDeclaration(current) && ts19.isClassDeclaration(current.parent)) {
        return true;
      }
      if (ts19.isPropertyDeclaration(current) && ts19.isClassExpression(current.parent)) {
        return true;
      }
      if (ts19.isArrowFunction(current)) {
        current = current.parent;
        continue;
      }
      if (ts19.isFunctionDeclaration(current) || ts19.isFunctionExpression(current)) {
        return false;
      }
      current = current.parent;
    }
    return false;
  }
  function isIterationStatement(node) {
    return (
      ts19.isForStatement(node) ||
      ts19.isForInStatement(node) ||
      ts19.isForOfStatement(node) ||
      ts19.isWhileStatement(node) ||
      ts19.isDoStatement(node)
    );
  }
  function isInsideIteration(node, label) {
    let current = node.parent;
    while (current) {
      if (
        ts19.isFunctionDeclaration(current) ||
        ts19.isFunctionExpression(current) ||
        ts19.isArrowFunction(current) ||
        ts19.isMethodDeclaration(current) ||
        ts19.isConstructorDeclaration(current) ||
        ts19.isGetAccessorDeclaration(current) ||
        ts19.isSetAccessorDeclaration(current)
      ) {
        return false;
      }
      if (label) {
        if (ts19.isLabeledStatement(current) && current.label.text === label) {
          return isIterationStatement(current.statement);
        }
      } else {
        if (isIterationStatement(current)) return true;
      }
      current = current.parent;
    }
    return false;
  }
  function isInsideBreakable(node, label) {
    let current = node.parent;
    while (current) {
      if (
        ts19.isFunctionDeclaration(current) ||
        ts19.isFunctionExpression(current) ||
        ts19.isArrowFunction(current) ||
        ts19.isMethodDeclaration(current) ||
        ts19.isConstructorDeclaration(current) ||
        ts19.isGetAccessorDeclaration(current) ||
        ts19.isSetAccessorDeclaration(current)
      ) {
        return false;
      }
      if (label) {
        if (ts19.isLabeledStatement(current) && current.label.text === label) {
          return true;
        }
      } else {
        if (isIterationStatement(current) || ts19.isSwitchStatement(current)) return true;
      }
      current = current.parent;
    }
    return false;
  }
  function checkDuplicateLexicalDeclarations(block) {
    const stmts = block.statements;
    const lexNames = /* @__PURE__ */ new Map();
    function addLexName(name, errorNode) {
      if (lexNames.has(name)) {
        addError(errorNode, `Duplicate identifier '${name}'`);
      } else {
        lexNames.set(name, errorNode);
      }
    }
    for (const stmt of stmts) {
      if (ts19.isClassDeclaration(stmt) && stmt.name) {
        addLexName(stmt.name.text, stmt.name);
      }
      if (ts19.isFunctionDeclaration(stmt) && stmt.name && stmt.body) {
        addLexName(stmt.name.text, stmt.name);
      }
      if (ts19.isVariableStatement(stmt)) {
        const flags = stmt.declarationList.flags;
        if ((flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0) {
          for (const decl of stmt.declarationList.declarations) {
            if (ts19.isIdentifier(decl.name)) {
              addLexName(decl.name.text, decl.name);
            }
          }
        }
      }
    }
  }
  function checkSwitchCaseLexicalDuplicates(caseBlock) {
    const lexNames = /* @__PURE__ */ new Map();
    const varNames = /* @__PURE__ */ new Map();
    for (const clause of caseBlock.clauses) {
      for (const stmt of clause.statements) {
        if (ts19.isVariableStatement(stmt)) {
          const flags = stmt.declarationList.flags;
          if ((flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0) {
            for (const decl of stmt.declarationList.declarations) {
              if (ts19.isIdentifier(decl.name)) {
                const name = decl.name.text;
                if (lexNames.has(name)) {
                  addError(decl.name, `Cannot redeclare block-scoped variable '${name}'`);
                } else {
                  lexNames.set(name, decl.name);
                }
                if (varNames.has(name)) {
                  addError(decl.name, `Cannot redeclare block-scoped variable '${name}'`);
                }
              }
            }
          } else {
            for (const decl of stmt.declarationList.declarations) {
              if (ts19.isIdentifier(decl.name)) {
                const name = decl.name.text;
                if (!varNames.has(name)) varNames.set(name, decl.name);
                if (lexNames.has(name)) {
                  addError(decl.name, `Cannot redeclare block-scoped variable '${name}'`);
                }
              }
            }
          }
        } else if (ts19.isFunctionDeclaration(stmt) && stmt.name) {
          const name = stmt.name.text;
          if (lexNames.has(name)) {
            addError(stmt.name, `Cannot redeclare block-scoped variable '${name}'`);
          } else {
            lexNames.set(name, stmt.name);
          }
          if (varNames.has(name)) {
            addError(stmt.name, `Cannot redeclare block-scoped variable '${name}'`);
          }
        } else if (ts19.isClassDeclaration(stmt) && stmt.name) {
          const name = stmt.name.text;
          if (lexNames.has(name)) {
            addError(stmt.name, `Cannot redeclare block-scoped variable '${name}'`);
          } else {
            lexNames.set(name, stmt.name);
          }
          if (varNames.has(name)) {
            addError(stmt.name, `Cannot redeclare block-scoped variable '${name}'`);
          }
        }
      }
    }
  }
  function checkDuplicatePrivateNames(classNode) {
    const privateNames = /* @__PURE__ */ new Map();
    for (const member of classNode.members) {
      if (member.name && ts19.isPrivateIdentifier(member.name)) {
        const name = member.name.text;
        let kind;
        if (ts19.isGetAccessorDeclaration(member)) {
          kind = "get";
        } else if (ts19.isSetAccessorDeclaration(member)) {
          kind = "set";
        } else if (ts19.isMethodDeclaration(member)) {
          kind = "method";
        } else if (ts19.isPropertyDeclaration(member)) {
          kind = "field";
        } else {
          kind = "other";
        }
        const existing = privateNames.get(name);
        if (!existing) {
          privateNames.set(name, { kinds: /* @__PURE__ */ new Set([kind]) });
        } else {
          const combined = /* @__PURE__ */ new Set([...existing.kinds, kind]);
          if (combined.size === 2 && combined.has("get") && combined.has("set")) {
            existing.kinds.add(kind);
          } else {
            addError(member.name, `Duplicate private name '${name}'`);
          }
        }
      }
    }
  }
  function isInsideAsyncFunction(node) {
    let current = node.parent;
    while (current) {
      if (
        ts19.isFunctionDeclaration(current) ||
        ts19.isFunctionExpression(current) ||
        ts19.isMethodDeclaration(current)
      ) {
        return current.modifiers?.some((m) => m.kind === ts19.SyntaxKind.AsyncKeyword) ?? false;
      }
      if (ts19.isArrowFunction(current)) {
        return current.modifiers?.some((m) => m.kind === ts19.SyntaxKind.AsyncKeyword) ?? false;
      }
      current = current.parent;
    }
    return false;
  }
  function isInsideGeneratorFunction(node) {
    let current = node.parent;
    while (current) {
      if ((ts19.isFunctionDeclaration(current) || ts19.isFunctionExpression(current)) && current.asteriskToken) {
        return true;
      }
      if (ts19.isMethodDeclaration(current) && current.asteriskToken) {
        return true;
      }
      if (
        ts19.isFunctionDeclaration(current) ||
        ts19.isFunctionExpression(current) ||
        ts19.isMethodDeclaration(current)
      ) {
        return false;
      }
      current = current.parent;
    }
    return false;
  }
  function isStatementPosition(parent, child) {
    if (ts19.isBlock(parent) || ts19.isSourceFile(parent)) return false;
    if (ts19.isIfStatement(parent)) {
      return parent.thenStatement === child || parent.elseStatement === child;
    }
    if (ts19.isWhileStatement(parent)) return parent.statement === child;
    if (ts19.isDoStatement(parent)) return parent.statement === child;
    if (ts19.isForStatement(parent)) return parent.statement === child;
    if (ts19.isForInStatement(parent)) return parent.statement === child;
    if (ts19.isForOfStatement(parent)) return parent.statement === child;
    if (ts19.isLabeledStatement(parent)) return parent.statement === child;
    if (ts19.isWithStatement(parent)) return parent.statement === child;
    return false;
  }
  function hasAsyncModifier2(node) {
    return node.modifiers?.some((m) => m.kind === ts19.SyntaxKind.AsyncKeyword) ?? false;
  }
  function isInsideClassWithPrivateName(node, privateName) {
    let current = node.parent;
    while (current) {
      if (ts19.isClassDeclaration(current) || ts19.isClassExpression(current)) {
        for (const member of current.members) {
          if (member.name && ts19.isPrivateIdentifier(member.name)) {
            if (member.name.escapedText === privateName) {
              return true;
            }
          }
        }
      }
      current = current.parent;
    }
    return false;
  }
  function checkVarLexicalConflicts(block) {
    const lexicalNames = /* @__PURE__ */ new Set();
    for (const stmt of block.statements) {
      if (ts19.isVariableStatement(stmt)) {
        const flags = stmt.declarationList.flags;
        if ((flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0) {
          for (const decl of stmt.declarationList.declarations) {
            if (ts19.isIdentifier(decl.name)) {
              lexicalNames.add(decl.name.text);
            }
          }
        }
      } else if (ts19.isFunctionDeclaration(stmt) && stmt.name) {
        lexicalNames.add(stmt.name.text);
      } else if (ts19.isClassDeclaration(stmt) && stmt.name) {
        lexicalNames.add(stmt.name.text);
      }
    }
    if (lexicalNames.size === 0) return;
    collectVarDeclaredNamesInBlock(block, lexicalNames);
  }
  function collectVarDeclaredNamesInBlock(node, lexicalNames) {
    if (ts19.isVariableStatement(node)) {
      const flags = node.declarationList.flags;
      if ((flags & ts19.NodeFlags.Let) === 0 && (flags & ts19.NodeFlags.Const) === 0) {
        for (const decl of node.declarationList.declarations) {
          if (ts19.isIdentifier(decl.name) && lexicalNames.has(decl.name.text)) {
            addError(decl.name, `Cannot redeclare block-scoped variable '${decl.name.text}'`);
          }
        }
      }
      return;
    }
    if (
      ts19.isFunctionDeclaration(node) ||
      ts19.isFunctionExpression(node) ||
      ts19.isArrowFunction(node) ||
      ts19.isMethodDeclaration(node) ||
      ts19.isConstructorDeclaration(node) ||
      ts19.isGetAccessorDeclaration(node) ||
      ts19.isSetAccessorDeclaration(node) ||
      ts19.isClassDeclaration(node) ||
      ts19.isClassExpression(node)
    ) {
      return;
    }
    ts19.forEachChild(node, (child) => collectVarDeclaredNamesInBlock(child, lexicalNames));
  }
  function checkTDZInStatements(stmts) {
    const letConstDecls = /* @__PURE__ */ new Map();
    for (const stmt of stmts) {
      if (ts19.isVariableStatement(stmt)) {
        const declList = stmt.declarationList;
        const flags = declList.flags;
        const isLetOrConst = (flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0;
        if (isLetOrConst) {
          for (const decl of declList.declarations) {
            if (ts19.isIdentifier(decl.name)) {
              letConstDecls.set(decl.name.text, decl);
            }
          }
        }
      }
    }
    if (letConstDecls.size === 0) return;
    const declaredSoFar = /* @__PURE__ */ new Set();
    for (const stmt of stmts) {
      if (ts19.isVariableStatement(stmt)) {
        const declList = stmt.declarationList;
        const flags = declList.flags;
        const isLetOrConst = (flags & ts19.NodeFlags.Let) !== 0 || (flags & ts19.NodeFlags.Const) !== 0;
        if (isLetOrConst) {
          for (const decl of declList.declarations) {
            if (ts19.isIdentifier(decl.name) && decl.initializer) {
              const varName = decl.name.text;
              if (letConstDecls.has(varName) && !declaredSoFar.has(varName)) {
                checkForTDZRef(decl.initializer, varName);
              }
            }
            if (ts19.isIdentifier(decl.name)) {
              declaredSoFar.add(decl.name.text);
            }
          }
          continue;
        }
      }
      for (const [name] of letConstDecls) {
        if (!declaredSoFar.has(name)) {
          checkForTDZRef(stmt, name);
        }
      }
    }
  }
  function checkForTDZRef(node, name) {
    if (ts19.isIdentifier(node) && node.text === name) {
      const parent = node.parent;
      if (parent && ts19.isPropertyAccessExpression(parent) && parent.name === node) {
        return;
      }
      if (parent && ts19.isPropertyAssignment(parent) && parent.name === node) {
        return;
      }
      const p = pos(node);
      errors.push({
        message: `Cannot access '${name}' before initialization`,
        line: p.line,
        column: p.column,
        severity: "warning",
      });
      return;
    }
    if (
      ts19.isFunctionDeclaration(node) ||
      ts19.isFunctionExpression(node) ||
      ts19.isArrowFunction(node) ||
      ts19.isClassDeclaration(node) ||
      ts19.isClassExpression(node)
    ) {
      return;
    }
    ts19.forEachChild(node, (child) => checkForTDZRef(child, name));
  }
  function isAssignmentPatternContext(objLit) {
    const parent = objLit.parent;
    if (!parent) return false;
    if (
      ts19.isBinaryExpression(parent) &&
      parent.operatorToken.kind === ts19.SyntaxKind.EqualsToken &&
      parent.left === objLit
    ) {
      return true;
    }
    if (ts19.isForOfStatement(parent) || ts19.isForInStatement(parent)) {
      return parent.initializer === objLit;
    }
    if (ts19.isArrayLiteralExpression(parent)) return isAssignmentPatternContext_expr(parent);
    if (ts19.isPropertyAssignment(parent)) {
      const grandParent = parent.parent;
      if (ts19.isObjectLiteralExpression(grandParent)) return isAssignmentPatternContext(grandParent);
    }
    if (ts19.isSpreadElement(parent)) {
      const grandParent = parent.parent;
      if (ts19.isArrayLiteralExpression(grandParent)) return isAssignmentPatternContext_expr(grandParent);
    }
    return false;
  }
  function isAssignmentPatternContext_expr(arrLit) {
    const parent = arrLit.parent;
    if (!parent) return false;
    if (
      ts19.isBinaryExpression(parent) &&
      parent.operatorToken.kind === ts19.SyntaxKind.EqualsToken &&
      parent.left === arrLit
    )
      return true;
    if (ts19.isForOfStatement(parent) || ts19.isForInStatement(parent)) return parent.initializer === arrLit;
    if (ts19.isArrayLiteralExpression(parent)) return isAssignmentPatternContext_expr(parent);
    if (ts19.isPropertyAssignment(parent)) {
      const gp = parent.parent;
      if (ts19.isObjectLiteralExpression(gp)) return isAssignmentPatternContext(gp);
    }
    return false;
  }
  visit(sourceFile);
  return errors;
}
function classifyImport(name, mod) {
  const strValue = mod.stringLiteralValues.get(name);
  if (strValue !== void 0) return { type: "string_literal", value: strValue };
  if (name === "console_log_number") return { type: "console_log", variant: "number" };
  if (name === "console_log_bool") return { type: "console_log", variant: "bool" };
  if (name === "console_log_string") return { type: "console_log", variant: "string" };
  if (name === "console_log_externref") return { type: "console_log", variant: "externref" };
  for (const cm of ["warn", "error"]) {
    if (name === `console_${cm}_number`) return { type: "console_log", variant: `${cm}_number` };
    if (name === `console_${cm}_bool`) return { type: "console_log", variant: `${cm}_bool` };
    if (name === `console_${cm}_string`) return { type: "console_log", variant: `${cm}_string` };
    if (name === `console_${cm}_externref`) return { type: "console_log", variant: `${cm}_externref` };
  }
  if (name.startsWith("Math_")) return { type: "math", method: name.slice(5) };
  if (name === "string_compare") return { type: "builtin", name };
  if (name.startsWith("string_")) return { type: "string_method", method: name.slice(7) };
  if (name === "number_toString") return { type: "builtin", name };
  if (name === "number_toFixed") return { type: "builtin", name };
  if (name === "number_toPrecision") return { type: "builtin", name };
  if (name === "number_toExponential") return { type: "builtin", name };
  if (name === "Date_new") return { type: "date_new" };
  if (name === "__date_now") return { type: "date_now" };
  if (name.startsWith("Date_")) return { type: "date_method", method: name.slice(5) };
  for (const ec of mod.externClasses) {
    const prefix = ec.importPrefix;
    if (name === `${prefix}_new`) return { type: "extern_class", className: ec.className, action: "new" };
    for (const [methodName] of ec.methods) {
      if (name === `${prefix}_${methodName}`)
        return { type: "extern_class", className: ec.className, action: "method", member: methodName };
    }
    for (const [propName] of ec.properties) {
      if (name === `${prefix}_get_${propName}`)
        return { type: "extern_class", className: ec.className, action: "get", member: propName };
      if (name === `${prefix}_set_${propName}`)
        return { type: "extern_class", className: ec.className, action: "set", member: propName };
    }
  }
  if (name === "__make_callback") return { type: "callback_maker" };
  if (name === "__await") return { type: "await" };
  if (name === "__dynamic_import") return { type: "dynamic_import" };
  if (name === "__proxy_create") return { type: "proxy_create" };
  if (name === "__typeof_number") return { type: "typeof_check", targetType: "number" };
  if (name === "__typeof_string") return { type: "typeof_check", targetType: "string" };
  if (name === "__typeof_boolean") return { type: "typeof_check", targetType: "boolean" };
  if (name === "__typeof_undefined") return { type: "typeof_check", targetType: "undefined" };
  if (name === "__typeof_object") return { type: "typeof_check", targetType: "object" };
  if (name === "__typeof_function") return { type: "typeof_check", targetType: "function" };
  if (name === "__unbox_number") return { type: "unbox", targetType: "number" };
  if (name === "__unbox_boolean") return { type: "unbox", targetType: "boolean" };
  if (name === "__box_number") return { type: "box", targetType: "number" };
  if (name === "__box_boolean") return { type: "box", targetType: "boolean" };
  if (name === "__is_truthy") return { type: "truthy_check" };
  if (name === "__typeof") return { type: "builtin", name: "__typeof" };
  if (name === "__get_globalThis") return { type: "declared_global", name: "globalThis" };
  if (name === "__extern_get") return { type: "extern_get" };
  if (name === "__extern_set") return { type: "extern_set" };
  if (name.startsWith("global_")) return { type: "declared_global", name: name.slice(7) };
  if (name.startsWith("__new_")) {
    return { type: "extern_class", className: name.slice(6), action: "new" };
  }
  return { type: "builtin", name };
}
function buildImportManifest(mod) {
  const manifest = [];
  for (const imp of mod.imports) {
    if (imp.module !== "env") continue;
    manifest.push({
      module: "env",
      name: imp.name,
      kind: imp.desc.kind === "func" ? "func" : "global",
      intent: classifyImport(imp.name, mod),
    });
  }
  return manifest;
}
function looksLikeTsSyntaxOnJs(diagnostics) {
  for (const d of diagnostics) {
    const msg = typeof d.messageText === "string" ? d.messageText : d.messageText.messageText;
    if (msg.includes("Type annotations can only be used in TypeScript files")) return true;
    if (msg.includes("types can only be used in a .ts file")) return true;
    if (msg.includes("'type' modifier cannot be used in a JavaScript file")) return true;
  }
  return false;
}
function checkJsTypeCoverage(ast) {
  const warnings = [];
  const sf = ast.sourceFile;
  function visit(node) {
    if (ts19.isFunctionDeclaration(node) && node.name && hasExportModifier3(node)) {
      const fnName = node.name.text;
      for (const param of node.parameters) {
        const paramType = ast.checker.getTypeAtLocation(param);
        if (paramType.flags & ts19.TypeFlags.Any) {
          const paramName = ts19.isIdentifier(param.name) ? param.name.text : "?";
          const { line, character } = sf.getLineAndCharacterOfPosition(param.getStart());
          warnings.push({
            message: `Parameter '${paramName}' in function '${fnName}' has implicit 'any' type. Add a JSDoc annotation: /** @param {number} ${paramName} */`,
            line: line + 1,
            column: character + 1,
            severity: "warning",
          });
        }
      }
      const sig = ast.checker.getSignatureFromDeclaration(node);
      if (sig) {
        const retType = ast.checker.getReturnTypeOfSignature(sig);
        if (retType.flags & ts19.TypeFlags.Any) {
          const { line, character } = sf.getLineAndCharacterOfPosition(node.name.getStart());
          warnings.push({
            message: `Function '${fnName}' has implicit 'any' return type. Add a JSDoc annotation: /** @returns {number} */`,
            line: line + 1,
            column: character + 1,
            severity: "warning",
          });
        }
      }
    }
    ts19.forEachChild(node, visit);
  }
  visit(sf);
  return warnings;
}
var DOWNGRADE_DIAG_CODES = /* @__PURE__ */ new Set([
  2304,
  // "Cannot find name 'X'" — unknown identifiers compiled as externref/unreachable
  2345,
  // "Argument of type 'X' is not assignable to parameter of type 'Y'"
  2322,
  // "Type 'X' is not assignable to type 'Y'"
  2339,
  // "Property 'X' does not exist on type 'Y'" — dynamic property access
  2551,
  // "Property 'X' does not exist on type 'Y'. Did you mean 'Z'?" — variant of 2339 with suggestion (#613)
  2454,
  // "Variable 'X' is used before being assigned"
  2531,
  // "Object is possibly 'null'" — codegen has null guards (#406)
  2532,
  // "Object is possibly 'undefined'" — codegen has null guards (#406)
  2533,
  // "Object is possibly 'null' or 'undefined'" — codegen has null guards (#406)
  2367,
  // "This comparison appears to be unintentional" (always truthy/falsy)
  2554,
  // "Expected N arguments, but got M"
  2683,
  // "'this' implicitly has type 'any'"
  2695,
  // "Left side of comma operator is unused and has no side effects"
  2769,
  // "No overload matches this call"
  18047,
  // "'X' is possibly 'null'" — codegen has null guards (#406)
  18049,
  // "'X' is possibly 'null' or 'undefined'" — codegen has null guards (#406)
  2358,
  // "The left-hand side of an 'instanceof' expression must be..."
  2356,
  // "An arithmetic operand must be of type 'any', 'number', 'bigint' or an enum type" — prefix/postfix inc/dec on non-number
  2362,
  // "The left-hand side of an arithmetic operation must be..."
  2365,
  // "Operator 'X' cannot be applied to types 'Y' and 'Z'"
  1503,
  // "This regular expression flag is only available when targeting 'es2024'" (#654)
  1232,
  // "An import declaration can only be used at the top level of a namespace or module" (#654)
  18050,
  // "The value 'null'/'undefined' cannot be used here"
  2698,
  // "Spread types may only be created from object types" — codegen handles spread generically (#536)
  2872,
  // "This kind of expression is always truthy"
  2873,
  // "This kind of expression is always falsy"
  2363,
  // "The right-hand side of an arithmetic operation must be..."
  2869,
  // "Right operand of ?? is unreachable because the left operand is never nullish"
  2349,
  // "This expression is not callable"
  2552,
  // "Cannot find name 'X'. Did you mean 'Y'?"
  18046,
  // "'X' is of type 'unknown'"
  2881,
  // "This expression is never nullish" — false positive in allowJs mode
  2871,
  // "This expression is always nullish"
  18048,
  // "'X' is possibly 'undefined'"
  2839,
  // "This condition will always return true/false since JS compares objects by reference"
  2703,
  // "The operand of a 'delete' operator must be a property reference"
  2704,
  // "The operand of a 'delete' operator cannot be a read-only property" — valid JS delete (#520)
  2790,
  // "The operand of a 'delete' operator must be optional" — valid JS delete (#520)
  2630,
  // "Cannot assign to 'X' because it is a function"
  2447,
  // "The '|'/'&' operator is not allowed for boolean types"
  2300,
  // "Duplicate identifier 'X'"
  2408,
  // "Setters cannot return a value" — valid in JS, codegen handles it
  1345,
  // "An expression of type 'void' cannot be tested for truthiness"
  2350,
  // "Only a void function can be called with the 'new' keyword"
  2403,
  // "Subsequent variable declarations must have the same type" — var re-declarations legal in JS
  2377,
  // "Constructors for derived classes must contain a 'super' call" — valid JS pattern
  2376,
  // "A 'super' call must be the first statement in the constructor" — valid JS pattern
  17009,
  // "'super' must be called before accessing 'this' in derived class constructor"
  17011,
  // "'super' must be called before accessing a property of 'super' in derived class constructor"
  2540,
  // "Cannot assign to 'X' because it is a read-only property" — private fields are writable in JS
  2803,
  // "Cannot assign to private method 'X'. Private methods are not writable" — valid JS pattern
  2806,
  // "Private accessor was defined without a getter" — valid JS pattern
  18030,
  // "An optional chain cannot contain private identifiers" — valid JS pattern
  2729,
  // "Property 'X' is used before its initialization" — valid JS pattern
  1163,
  // "A 'yield' expression is only allowed in a generator body" — #267
  1435,
  // "Unknown keyword or identifier. Did you mean 'X'?" — yield in nested generator contexts (#521)
  1220,
  // "Generators are not allowed in an ambient context" — valid JS pattern (#267)
  1166,
  // "A computed property name in a class property declaration must have a simple literal type" — #265
  2464,
  // "A computed property name must be of type 'string', 'number', 'symbol', or 'any'" — #276
  2488,
  // "Type must have a '[Symbol.iterator]()' method" — #268
  2489,
  // "An iterator must have a 'next()' method" — iterator protocol (#153)
  1103,
  // "'for await' loops are only allowed within async functions and at the top levels of modules" — #612
  2504,
  // "Type must have a '[Symbol.asyncIterator]()' method that returns an async iterator" — #612
  2519,
  // "An async iterator must have a 'next()' method" — async iterator protocol (#612)
  2547,
  // "The type returned by the 'next()' method of an async iterator must be a promise for a type with a 'value' property" — #612
  2548,
  // "Type is not an array type or does not have '[Symbol.iterator]()'" — #268
  2549,
  // "Type is not an array/string type or does not have '[Symbol.iterator]()'" — #268
  2768,
  // "The 'next' property of an async iterator must be a method" — #612
  2763,
  // "Cannot iterate value because the 'next' method expects type X" — for-of iteration (#153)
  2764,
  // "Cannot iterate value because the 'next' method expects type X (array spread)" — (#153)
  2765,
  // "Cannot iterate value because the 'next' method expects type X (destructuring)" — (#153)
  18014,
  // "The property '#x' cannot be accessed on type 'X' within this class because it is shadowed" — valid JS
  2538,
  // "Type 'X' cannot be used as an index type" — valid JS pattern (e.g. symbol/boolean as index)
  1468,
  // "A computed property name must be of type 'string', 'number', 'symbol', or 'any'" — valid JS
  2556,
  // "A spread argument must either have a tuple type or be passed to a rest parameter" — valid JS spread (#382)
  2741,
  // "Property 'X' is missing in type 'Y' but required in type 'Z'" — valid JS object patterns
  2493,
  // "Tuple type '[]' of length 'N' has no element at index 'M'" — destructuring empty/short tuples (#379)
  2689,
  // "Cannot extend an interface 'X'. Did you mean 'implements'?" — Iterator/Generator class patterns (#616)
  2700,
  // "Rest types may only be created from object types" — object rest on primitives (#379)
  1212,
  // "Identifier expected. 'X' is a reserved word in strict mode" — valid in sloppy JS (#270)
  1214,
  // "Identifier expected. 'yield' is a reserved word in strict mode. Modules are automatically in strict mode." — yield as identifier in sloppy JS (#241)
  1308,
  // "'await' expressions are only allowed within async functions and at the top levels of modules" — codegen handles await in non-async as identity (#666)
  2378,
  // "A 'get' accessor must return a value" — valid JS pattern, getter can return undefined implicitly (#377)
  1052,
  // "A 'set' accessor parameter cannot have an initializer" — valid JS pattern, setter params can have defaults (#377)
  7033,
  // "Property 'X' implicitly has type 'any', because its get accessor lacks a return type annotation" — valid JS (#377)
  1100,
  // "Invalid use of 'X' in strict mode" — sloppy-mode JS allows assignment to eval/arguments (#331)
  1215,
  // "Invalid use of 'X'. Modules are automatically in strict mode" — sloppy-mode JS allows eval/arguments as targets (#331)
  1210,
  // "Code contained in a class is evaluated in strict mode which does not allow this use of 'X'" — sloppy-mode JS pattern (#331)
  1156,
  // "'let' declarations can only be declared inside a block" — sloppy-mode JS pattern (#383)
  1313,
  // "The body of an 'if' statement cannot be the empty statement" — sloppy-mode JS pattern (#383)
  1344,
  // "A label is not allowed here" — labeled function declarations in sloppy-mode JS (#383)
  1182,
  // "A destructuring declaration must have an initializer" — valid JS pattern (#383)
  1228,
  // "A type predicate is only allowed in return type position" — valid JS pattern (#383)
  7053,
  // "Element implicitly has an 'any' type because expression can't be used to index type" — numeric/string index on plain objects (#391)
  2318,
  // "Cannot find global type 'X'" — e.g. ClassDecoratorContext, AsyncIterableIterator (#271)
  2468,
  // "Cannot find global value 'X'" — e.g. Promise (#271)
  2583,
  // "Cannot find name 'X'. Do you need to change your target library?" — e.g. BigInt, Reflect (#271)
  2585,
  // "'X' only refers to a type, but is being used as a value here" (target library) (#271)
  2693,
  // "'X' only refers to a type, but is being used as a value here" (#271)
  2697,
  // "An async function or method must return a 'Promise'" (#271)
  2705,
  // "An async function or method in ES5 requires the 'Promise' constructor" (#271)
  1206,
  // "Decorators are not valid here" — decorator syntax suppressed, decorators ignored (#376)
  1207,
  // "Decorators cannot be applied to multiple get/set accessors of the same name" (#376)
  1236,
  // "The return type of a property decorator function must be either 'void' or 'any'" (#376)
  1237,
  // "The return type of a parameter decorator function must be either 'void' or 'any'" (#376)
  1238,
  // "Unable to resolve signature of class decorator when called as an expression" (#271)
  1239,
  // "Unable to resolve signature of parameter decorator when called as an expression" (#271)
  1240,
  // "Unable to resolve signature of property decorator when called as an expression" (#271)
  1241,
  // "Unable to resolve signature of method decorator when called as an expression" (#271)
  1249,
  // "A decorator can only decorate a method implementation, not an overload" (#376)
  1270,
  // "Decorator function return type is not assignable" (#376)
  1271,
  // "Decorator function return type is not void or any" (#376)
  1278,
  // "The runtime will invoke the decorator with N arguments, but the decorator expects M" (#376)
  1279,
  // "The runtime will invoke the decorator with N arguments, but the decorator expects at least M" (#376)
  1329,
  // "Decorator accepts too few arguments" (#376)
  1433,
  // "Neither decorators nor modifiers may be applied to 'this' parameters" (#376)
  1436,
  // "Decorators must precede the name and all keywords of property declarations" (#376)
  1486,
  // "Decorator used before 'export' here" (#376)
  1497,
  // "Expression must be enclosed in parentheses to be used as a decorator" (#376)
  1498,
  // "Invalid syntax in decorator" (#376)
  8038,
  // "Decorators may not appear after 'export' or 'export default'" (#376)
  18036,
  // "Class decorators can't be used with static private identifier" (#376)
  2372,
  // "Parameter 'x' cannot reference itself" — valid JS pattern (#413)
  2373,
  // "Parameter 'x' cannot reference identifier 'y' declared after it" — valid JS pattern (#413)
  2735,
  // "Initializer of parameter 'x' cannot reference identifier 'y'" — valid JS pattern (#413)
  1106,
  // "The left-hand side of a 'for...of' statement may not be 'async'" — valid in sloppy-mode JS (#425)
  2711,
  // "A dynamic import call returns a 'Promise'" — dynamic import() (#440)
  2792,
  // "Cannot find module 'X'" — dynamic import() module resolution (#440)
  2739,
  // "Type 'X' is missing properties: next, return, throw" — generator type mismatch (#439)
  2802,
  // "Type 'X' can only be iterated through when using '--downlevelIteration'" — generators in for-of (#439)
  1102,
  // "'delete' cannot be called on an identifier in strict mode" — valid sloppy-mode JS (#535)
  1184,
  // "Modifiers cannot appear here" — valid JS patterns in test262 (#537)
  1109,
  // "Expression expected" — valid JS patterns in test262 (#537)
  1135,
  // "Argument expression expected" — valid JS patterns in test262 (#537)
  2351,
  // "This expression is not constructable" — valid JS patterns in test262 (#537)
  2335,
  // "'super' can only be referenced in a derived class" — valid JS patterns in test262 (#537)
  2660,
  // "'super' can only be referenced in members of derived classes or object literal expressions" — valid JS (#537)
  2508,
  // "No base constructor has the specified number of type arguments" — valid JS patterns in test262 (#537)
  1262,
  // "Identifier expected. 'X' is a reserved word at the top-level of a module" — await as identifier (#537)
  2393,
  // "Duplicate function implementation" — valid JS function re-declarations (#537)
  2721,
  // "Cannot invoke an object which is possibly 'null' or 'undefined'" — codegen has null guards (#406)
  2722,
  // "Cannot invoke an object which is possibly 'null'" — codegen has null guards (#406)
  2723,
  // "Cannot invoke an object which is possibly 'undefined'" — codegen has null guards (#406)
  2448,
  // "Block-scoped variable 'X' used before its declaration" — valid JS TDZ pattern, runtime ReferenceError (#723)
  2474,
  // "Cannot access 'X' before initialization" — valid JS TDZ pattern, runtime ReferenceError (#723)
  1489,
  // "Decimals with leading zeros are not allowed" — valid sloppy-mode JS octal literals
  1121,
  // "Octal literals are not allowed in strict mode" — valid sloppy-mode JS
]);
function compileSource(source, options = {}, languageService) {
  resetCompileDepth();
  const errors = [];
  const emitWatOutput = options.emitWat !== false;
  const processedSource = preprocessImports(source);
  let isJsMode = options.allowJs === true || (options.fileName?.endsWith(".js") ?? false);
  const defaultFileName = options.fileName ?? (isJsMode ? "input.js" : "input.ts");
  const effectiveFileName = options.moduleName ?? defaultFileName;
  let ast;
  if (languageService) {
    languageService.updateSource(processedSource, effectiveFileName);
    ast = languageService.analyze({
      allowJs: options.allowJs,
      skipSemanticDiagnostics: options.skipSemanticDiagnostics,
    });
  } else {
    ast = analyzeSource(processedSource, effectiveFileName, {
      allowJs: options.allowJs,
      skipSemanticDiagnostics: options.skipSemanticDiagnostics,
    });
  }
  if (!isJsMode) {
    const syntaxErrors = ast.syntacticDiagnostics.filter((d) => d.category === 1 && d.file === ast.sourceFile);
    if (syntaxErrors.length > 0 && looksLikeTsSyntaxOnJs(syntaxErrors)) {
      isJsMode = true;
      const jsFileName = effectiveFileName.replace(/\.ts$/, ".js");
      if (languageService) {
        languageService.updateSource(processedSource, jsFileName);
        ast = languageService.analyze({ allowJs: true });
      } else {
        ast = analyzeSource(processedSource, jsFileName, { allowJs: true });
      }
    }
  }
  if (isJsMode) {
    const typeWarnings = checkJsTypeCoverage(ast);
    errors.push(...typeWarnings);
  }
  for (const diag of ast.diagnostics) {
    if (diag.category === 1) {
      const pos = diag.file ? diag.file.getLineAndCharacterOfPosition(diag.start ?? 0) : { line: 0, character: 0 };
      const severity = DOWNGRADE_DIAG_CODES.has(diag.code) ? "warning" : "error";
      errors.push({
        message: typeof diag.messageText === "string" ? diag.messageText : diag.messageText.messageText,
        line: pos.line + 1,
        column: pos.character + 1,
        severity,
        code: diag.code,
      });
    }
  }
  const TOLERATED_SYNTAX_CODES = /* @__PURE__ */ new Set([
    1156,
    // "'let' declarations can only be declared inside a block"
    1313,
    // "The body of an 'if' statement cannot be the empty statement"
    1344,
    // "A label is not allowed here"
    1182,
    // "A destructuring declaration must have an initializer"
    1228,
    // "A type predicate is only allowed in return type position"
    1163,
    // "A 'yield' expression is only allowed in a generator body" — syntactic diagnostic (#267)
    1206,
    // "Decorators are not valid here" — decorator syntax tolerated, decorators ignored (#376)
    1207,
    // "Decorators cannot be applied to multiple get/set accessors" (#376)
    1436,
    // "Decorators must precede the name and all keywords of property declarations" (#376)
    1486,
    // "Decorator used before 'export' here" (#376)
    1497,
    // "Expression must be enclosed in parentheses to be used as a decorator" (#376)
    1498,
    // "Invalid syntax in decorator" (#376)
    8038,
    // "Decorators may not appear after 'export' or 'export default'" (#376)
    1184,
    // "Modifiers cannot appear here" — valid JS patterns in test262 (#537)
    1109,
    // "Expression expected" — valid JS patterns in test262 (#537)
    1135,
    // "Argument expression expected" — valid JS patterns in test262 (#537)
    1262,
    // "Identifier expected. 'X' is a reserved word at the top-level of a module" — await as identifier (#537)
    1435,
    // "Unknown keyword or identifier. Did you mean 'X'?" — yield in nested generator contexts (#521)
    1503,
    // "This regular expression flag is only available when targeting 'es2024'" (#654)
    1232,
    // "An import declaration can only be used at the top level of a namespace or module" (#654)
    1102,
    // "'delete' cannot be called on an identifier in strict mode" — valid sloppy-mode JS (#535)
    1100,
    // "Invalid use of 'X' in strict mode" — sloppy-mode JS allows eval/arguments (#331)
    1121,
    // "Octal literals are not allowed in strict mode" — valid sloppy-mode JS
    1489,
    // "Decimals with leading zeros are not allowed" — valid sloppy-mode JS octal literals
  ]);
  const hasSyntaxErrors = ast.syntacticDiagnostics.some(
    (d) => d.category === 1 && d.file === ast.sourceFile && !TOLERATED_SYNTAX_CODES.has(d.code),
  );
  if (hasSyntaxErrors && errors.length > 0) {
    return {
      binary: new Uint8Array(0),
      wat: "",
      dts: "",
      importsHelper: "",
      success: false,
      errors,
      stringPool: [],
      imports: [],
      hasMain: false,
      hasTopLevelStatements: false,
    };
  }
  const earlyErrors = detectEarlyErrors(ast.sourceFile);
  errors.push(...earlyErrors);
  const hasHardEarlyErrors = earlyErrors.some((e) => e.severity !== "warning");
  if (hasHardEarlyErrors) {
    return {
      binary: new Uint8Array(0),
      wat: "",
      dts: "",
      importsHelper: "",
      success: false,
      errors,
      stringPool: [],
      imports: [],
      hasMain: false,
      hasTopLevelStatements: false,
    };
  }
  if (options.safe) {
    const safeErrors = validateSafeMode(ast.sourceFile, ast.checker, options);
    errors.push(...safeErrors);
    if (safeErrors.length > 0) {
      return {
        binary: new Uint8Array(0),
        wat: "",
        dts: "",
        importsHelper: "",
        success: false,
        errors,
        stringPool: [],
        imports: [],
        hasMain: false,
        hasTopLevelStatements: false,
      };
    }
  }
  if (options.hardened) {
    const hardenedErrors = validateHardenedMode(ast.sourceFile);
    errors.push(...hardenedErrors);
    if (hardenedErrors.length > 0) {
      return {
        binary: new Uint8Array(0),
        wat: "",
        dts: "",
        importsHelper: "",
        success: false,
        errors,
        stringPool: [],
        imports: [],
        hasMain: false,
        hasTopLevelStatements: false,
      };
    }
  }
  const emitSourceMap = options.sourceMap === true;
  const useLinear = options.target === "linear";
  let mod;
  try {
    if (useLinear) {
      mod = generateLinearModule(ast);
    } else {
      const result = generateModule(ast, {
        sourceMap: emitSourceMap,
        fast: options.fast,
        nativeStrings: options.nativeStrings,
        wasi: options.target === "wasi",
      });
      mod = result.module;
      for (const err of result.errors) {
        errors.push({
          message: err.message,
          line: err.line,
          column: err.column,
          severity: err.severity ?? "error",
        });
      }
    }
  } catch (e) {
    errors.push({
      message: `Codegen error: ${e instanceof Error ? e.message : String(e)}`,
      line: 0,
      column: 0,
      severity: "error",
    });
    return {
      binary: new Uint8Array(0),
      wat: "",
      dts: "",
      importsHelper: "",
      success: false,
      errors,
      stringPool: [],
      imports: [],
      hasMain: false,
      hasTopLevelStatements: false,
    };
  }
  let cHeader;
  if (options.abi === "c" && options.target === "linear") {
    const cabiResult = applyCabiTransform(mod, options.moduleName ?? "module");
    cHeader = cabiResult.cHeader;
  }
  widenNonDefaultableTypes(mod);
  let binary;
  let sourceMapJson;
  try {
    if (emitSourceMap) {
      const emitResult = emitBinaryWithSourceMap(mod);
      const sourcesContent = /* @__PURE__ */ new Map();
      sourcesContent.set(effectiveFileName, source);
      const sourceMap = generateSourceMap(emitResult.sourceMapEntries, sourcesContent);
      sourceMapJson = JSON.stringify(sourceMap);
      const sourceMapUrl = options.sourceMapUrl ?? "module.wasm.map";
      const urlSection = new WasmEncoder();
      emitSourceMappingURLSection(urlSection, sourceMapUrl);
      const urlSectionBytes = urlSection.finish();
      const combined = new Uint8Array(emitResult.binary.length + urlSectionBytes.length);
      combined.set(emitResult.binary);
      combined.set(urlSectionBytes, emitResult.binary.length);
      binary = combined;
    } else {
      binary = emitBinary(mod);
    }
  } catch (e) {
    errors.push({
      message: `Binary emit error: ${e instanceof Error ? e.message : String(e)}`,
      line: 0,
      column: 0,
      severity: "error",
    });
    return {
      binary: new Uint8Array(0),
      wat: "",
      dts: "",
      importsHelper: "",
      success: false,
      errors,
      stringPool: [],
      imports: [],
      hasMain: false,
      hasTopLevelStatements: false,
    };
  }
  if (options.optimize) {
    const level = typeof options.optimize === "number" ? options.optimize : 3;
    const optResult = optimizeBinary(binary, { level });
    if (optResult.optimized) {
      binary = optResult.binary;
    }
    if (optResult.warning) {
      errors.push({
        message: optResult.warning,
        line: 0,
        column: 0,
        severity: "warning",
      });
    }
  }
  let wat = "";
  if (emitWatOutput) {
    try {
      wat = emitWat(mod);
    } catch (e) {
      errors.push({
        message: `WAT emit warning: ${e instanceof Error ? e.message : String(e)}`,
        line: 0,
        column: 0,
        severity: "warning",
      });
    }
  }
  const dts = generateDts(ast, mod);
  const importsHelper = generateImportsHelper(mod);
  let witOutput;
  if (options.wit) {
    const witOpts = typeof options.wit === "object" ? options.wit : void 0;
    witOutput = generateWit(ast, witOpts);
  }
  return {
    binary,
    wat,
    dts,
    importsHelper,
    success: true,
    errors,
    stringPool: mod.stringPool,
    sourceMap: sourceMapJson,
    imports: buildImportManifest(mod),
    cHeader,
    wit: witOutput,
    hasMain: mod.exports.some((e) => e.name === "main" && e.desc.kind === "func"),
    hasTopLevelStatements: mod.hasTopLevelStatements === true,
  };
}
function applyCabiTransform(mod, moduleName) {
  const numImportFuncs = mod.imports.filter((i) => i.desc.kind === "func").length;
  const exportInfos = [];
  for (const exp of mod.exports) {
    if (exp.desc.kind !== "func") continue;
    if (exp.name === "memory") continue;
    const funcIdx = exp.desc.index;
    const localIdx = funcIdx - numImportFuncs;
    if (localIdx < 0 || localIdx >= mod.functions.length) continue;
    const func = mod.functions[localIdx];
    const typeDef = mod.types[func.typeIdx];
    if (!typeDef || typeDef.kind !== "func") continue;
    const paramDefs = typeDef.params.map((wt, i) => {
      const semantic = wt.kind === "f64" ? "number_f64" : "number_i32";
      return { name: `p${i}`, wasmType: wt, semantic };
    });
    const cabiParams = mapParamsToCabi(paramDefs);
    const resultSemantic =
      typeDef.results.length === 0 ? "void" : typeDef.results[0].kind === "f64" ? "number_f64" : "number_i32";
    const cabiResult = mapResultToCabi(typeDef.results.length > 0 ? typeDef.results[0] : null, resultSemantic);
    const cabiName = exp.name;
    exportInfos.push({
      tsName: exp.name,
      cabiName,
      params: cabiParams,
      result: cabiResult,
    });
  }
  emitCabiWrappers(mod, exportInfos);
  const headerExports = extractCHeaderExports(mod);
  const cHeader = generateCHeader(moduleName, headerExports);
  return { cHeader };
}
function generateDts(ast, mod) {
  const lines = ["// Generated by js2wasm", ""];
  const exportLines = [];
  for (const stmt of ast.sourceFile.statements) {
    if (ts19.isFunctionDeclaration(stmt) && stmt.name && hasExportModifier3(stmt)) {
      const name = stmt.name.text;
      const isAsync = mod.asyncFunctions.has(name);
      const params = stmt.parameters
        .map((p) => {
          const paramName = ts19.isIdentifier(p.name) ? p.name.text : "_";
          const typeText = mapTypeForDts(p.type, ast.sourceFile);
          const optional = p.questionToken ? "?" : "";
          return `${paramName}${optional}: ${typeText}`;
        })
        .join(", ");
      let returnType = mapTypeForDts(stmt.type, ast.sourceFile);
      if (isAsync && !returnType.startsWith("Promise<")) {
        returnType = `Promise<${returnType}>`;
      }
      exportLines.push(`  ${name}(${params}): ${returnType};`);
    }
  }
  if (exportLines.length > 0) {
    lines.push(
      ...exportLines.map((l) => {
        const m = l.match(/^\s+(\w+)\(([^)]*)\):\s*(.+);$/);
        if (m) return `export declare function ${m[1]}(${m[2]}): ${m[3]};`;
        return l;
      }),
    );
    lines.push("");
  }
  return lines.join("\n");
}
function generateImportsHelper(mod) {
  const lines = [
    "// Generated by js2wasm \u2014 runtime imports helper",
    "// Usage: const imports = createImports(deps);",
    "//        const { instance } = await WebAssembly.instantiate(wasmBytes, imports,",
    "//          { builtins: ['js-string'], importedStringConstants: 'string_constants' }",
    "//        );",
    "",
  ];
  const hasDeps = mod.externClasses.length > 0;
  const hasStringPool = mod.stringPool.length > 0;
  const hasJsString = mod.imports.some((i) => i.module === "wasm:js-string");
  const hasCallbacks = mod.imports.some((i) => i.name === "__make_callback");
  if (hasCallbacks) {
    lines.push("let wasmExports;");
    lines.push("export function setExports(exports) { wasmExports = exports; }");
    lines.push("");
  }
  lines.push(`export function createImports(${hasDeps ? "deps" : ""}) {`);
  lines.push("  const env = {");
  for (const imp of mod.imports) {
    if (imp.module !== "env") continue;
    if (imp.desc.kind !== "func") continue;
    const line = generateEnvImportLine(imp.name, mod);
    lines.push(`    ${line},`);
  }
  lines.push("  };");
  if (hasStringPool) {
    lines.push("");
    lines.push("  // String constants as WebAssembly.Global values");
    lines.push("  const string_constants = {");
    for (const s of mod.stringPool) {
      lines.push(
        `    ${JSON.stringify(s)}: new WebAssembly.Global({ value: "externref", mutable: false }, ${JSON.stringify(s)}),`,
      );
    }
    lines.push("  };");
  }
  if (hasJsString) {
    lines.push("");
    lines.push("  // Polyfill for engines without native wasm:js-string support");
    lines.push("  const jsString = {");
    lines.push("    concat: (a, b) => a + b,");
    lines.push("    length: (s) => s.length,");
    lines.push("    equals: (a, b) => a === b ? 1 : 0,");
    lines.push("    substring: (s, start, end) => s.substring(start, end),");
    lines.push("    charCodeAt: (s, i) => s.charCodeAt(i),");
    lines.push("  };");
  }
  lines.push("");
  const parts = ["env"];
  if (hasStringPool) parts.push("string_constants");
  if (hasJsString) parts.push('"wasm:js-string": jsString');
  lines.push(`  return { ${parts.join(", ")} };`);
  lines.push("}");
  lines.push("");
  return lines.join("\n");
}
function generateEnvImportLine(name, mod) {
  const strValue = mod.stringLiteralValues.get(name);
  if (strValue !== void 0) {
    return `${name}: () => ${JSON.stringify(strValue)}`;
  }
  for (const cm of ["log", "warn", "error"]) {
    if (name === `console_${cm}_number`) return `console_${cm}_number: (v) => console.${cm}(v)`;
    if (name === `console_${cm}_bool`) return `console_${cm}_bool: (v) => console.${cm}(Boolean(v))`;
    if (name === `console_${cm}_string`) return `console_${cm}_string: (v) => console.${cm}(v)`;
    if (name === `console_${cm}_externref`) return `console_${cm}_externref: (v) => console.${cm}(v)`;
  }
  if (name === "number_toString") return "number_toString: (v) => String(v)";
  if (name === "string_compare") return "string_compare: (a, b) => (a < b ? -1 : a > b ? 1 : 0)";
  if (name.startsWith("string_")) {
    const method = name.slice(7);
    return `${name}: (s, ...a) => s.${method}(...a)`;
  }
  if (name === "String_fromCharCode") return "String_fromCharCode: (code) => String.fromCharCode(code)";
  if (name === "__toUint32") return "__toUint32: (x) => x >>> 0";
  if (name.startsWith("Math_")) {
    const method = name.slice(5);
    return `${name}: Math.${method}`;
  }
  for (const ec of mod.externClasses) {
    const prefix = ec.importPrefix;
    const nsAccess = ec.namespacePath.length > 0 ? `deps.${ec.namespacePath.join(".")}` : `deps`;
    if (name === `${prefix}_new`) {
      const paramList = ec.constructorParams.map((_, i) => `a${i}`).join(", ");
      return `${name}: (${paramList}) => new ${nsAccess}.${ec.className}(${paramList})`;
    }
    for (const [methodName, sig] of ec.methods) {
      if (name === `${prefix}_${methodName}`) {
        const paramList = sig.params
          .slice(1)
          .map((_, i) => `a${i}`)
          .join(", ");
        return `${name}: (self${paramList ? ", " + paramList : ""}) => self.${methodName}(${paramList})`;
      }
    }
    for (const [propName, propInfo] of ec.properties) {
      if (name === `${prefix}_get_${propName}`) {
        return `${name}: (self) => self.${propName}`;
      }
      if (name === `${prefix}_set_${propName}`) {
        return `${name}: (self, v) => { self.${propName} = v; }`;
      }
    }
  }
  if (name === "__make_callback") {
    return `${name}: (id, cap) => (...args) => wasmExports[\`__cb_\${id}\`](cap, ...args)`;
  }
  if (name === "__await") return `${name}: (v) => v`;
  if (name === "__gen_create_buffer") return `${name}: () => []`;
  if (name === "__gen_push_f64") return `${name}: (buf, v) => { buf.push(v); }`;
  if (name === "__gen_push_i32") return `${name}: (buf, v) => { buf.push(v); }`;
  if (name === "__gen_push_ref") return `${name}: (buf, v) => { buf.push(v); }`;
  if (name === "__create_generator")
    return `${name}: (buf) => { let i = 0; return { next() { if (i < buf.length) return { value: buf[i++], done: false }; return { value: undefined, done: true }; }, return(v) { i = buf.length; return { value: v, done: true }; }, throw(e) { i = buf.length; throw e; }, [Symbol.iterator]() { return this; } }; }`;
  if (name === "__create_async_generator")
    return `${name}: (buf, pendingThrow) => { let i = 0; function mkR(v, d) { const p = { value: v, done: d }; return { value: v, done: d, then(res, rej) { return Promise.resolve(p).then(res, rej); } }; } function mkE(e) { return { done: true, value: undefined, then(res, rej) { return Promise.reject(e).then(res, rej); } }; } return { next() { if (i < buf.length) return mkR(buf[i++], false); if (pendingThrow !== null && pendingThrow !== undefined) { const e = pendingThrow; pendingThrow = null; return mkE(e); } return mkR(undefined, true); }, return(v) { i = buf.length; return mkR(v, true); }, throw(e) { i = buf.length; return mkE(e); }, [Symbol.asyncIterator]() { return this; } }; }`;
  if (name === "__gen_next") return `${name}: (gen) => gen.next()`;
  if (name === "__gen_result_value") return `${name}: (r) => r.value`;
  if (name === "__gen_result_value_f64") return `${name}: (r) => Number(r.value)`;
  if (name === "__gen_result_done") return `${name}: (r) => r.done ? 1 : 0`;
  if (name === "__typeof_number") return `${name}: (v) => typeof v === "number" ? 1 : 0`;
  if (name === "__typeof_string") return `${name}: (v) => typeof v === "string" ? 1 : 0`;
  if (name === "__typeof_boolean") return `${name}: (v) => typeof v === "boolean" ? 1 : 0`;
  if (name === "__typeof_undefined") return `${name}: (v) => typeof v === "undefined" ? 1 : 0`;
  if (name === "__typeof_object") return `${name}: (v) => typeof v === "object" ? 1 : 0`;
  if (name === "__typeof_function") return `${name}: (v) => typeof v === "function" ? 1 : 0`;
  if (name === "__unbox_number") return `${name}: (v) => Number(v)`;
  if (name === "__unbox_boolean") return `${name}: (v) => v ? 1 : 0`;
  if (name === "__box_number") return `${name}: (v) => v`;
  if (name === "__box_boolean") return `${name}: (v) => Boolean(v)`;
  if (name === "__box_symbol")
    return `${name}: (() => { const c = new Map([[1,Symbol.iterator],[2,Symbol.hasInstance],[3,Symbol.toPrimitive],[4,Symbol.toStringTag],[5,Symbol.species],[6,Symbol.isConcatSpreadable],[7,Symbol.match],[8,Symbol.replace],[9,Symbol.search],[10,Symbol.split],[11,Symbol.unscopables],[12,Symbol.asyncIterator]]); return (id) => { let s = c.get(id); if (!s) { s = Symbol("wasm_"+id); c.set(id, s); } return s; }; })()`;
  if (name === "__is_truthy") return `${name}: (v) => v ? 1 : 0`;
  if (name === "__typeof") return `${name}: (v) => typeof v`;
  if (name === "__call_1_f64") return `${name}: (fn, a) => fn(a)`;
  if (name === "__call_2_f64") return `${name}: (fn, a, b) => fn(a, b)`;
  return `${name}: () => {}`;
}
function mapTypeForDts(typeNode, sf) {
  if (!typeNode) return "void";
  const text = typeNode.getText(sf);
  if (text === "number" || text === "boolean" || text === "string" || text === "void") {
    return text;
  }
  if (ts19.isTypeReferenceNode(typeNode)) {
    const typeName = typeNode.typeName.getText(sf);
    if (typeName === "Promise" && typeNode.typeArguments?.length === 1) {
      const innerType = mapTypeForDts(typeNode.typeArguments[0], sf);
      return `Promise<${innerType}>`;
    }
  }
  return "any";
}
function hasExportModifier3(node) {
  const modifiers = ts19.canHaveModifiers(node) ? ts19.getModifiers(node) : void 0;
  return modifiers?.some((m) => m.kind === ts19.SyntaxKind.ExportKeyword) ?? false;
}
function validateHardenedMode(sourceFile) {
  const errors = [];
  function visit(node) {
    if (ts19.isCallExpression(node) && ts19.isIdentifier(node.expression) && node.expression.text === "eval") {
      const { line, character } = sourceFile.getLineAndCharacterOfPosition(node.getStart());
      errors.push({
        message: "[hardened] eval() is not allowed",
        line: line + 1,
        column: character,
        severity: "error",
      });
    }
    if (ts19.isNewExpression(node) && ts19.isIdentifier(node.expression) && node.expression.text === "Function") {
      const { line, character } = sourceFile.getLineAndCharacterOfPosition(node.getStart());
      errors.push({
        message: "[hardened] new Function() is not allowed",
        line: line + 1,
        column: character,
        severity: "error",
      });
    }
    if (ts19.isWithStatement(node)) {
      const { line, character } = sourceFile.getLineAndCharacterOfPosition(node.getStart());
      errors.push({
        message: "[hardened] with statement is not allowed",
        line: line + 1,
        column: character,
        severity: "error",
      });
    }
    if (ts19.isBinaryExpression(node) && node.operatorToken.kind === ts19.SyntaxKind.EqualsToken) {
      const left = node.left;
      if (ts19.isPropertyAccessExpression(left) && left.name.text === "__proto__") {
        const { line, character } = sourceFile.getLineAndCharacterOfPosition(node.getStart());
        errors.push({
          message: "[hardened] __proto__ assignment is not allowed",
          line: line + 1,
          column: character,
          severity: "error",
        });
      }
    }
    ts19.forEachChild(node, visit);
  }
  visit(sourceFile);
  return errors;
}
function widenNonDefaultableTypes(mod) {
  function widenValType(t) {
    return t.kind === "ref" ? { kind: "ref_null", typeIdx: t.typeIdx } : t;
  }
  function widenTypeDef(typeDef) {
    switch (typeDef.kind) {
      case "func":
        for (let i = 0; i < typeDef.params.length; i++) {
          typeDef.params[i] = widenValType(typeDef.params[i]);
        }
        for (let i = 0; i < typeDef.results.length; i++) {
          typeDef.results[i] = widenValType(typeDef.results[i]);
        }
        break;
      case "struct":
        for (const field of typeDef.fields) {
          field.type = widenValType(field.type);
        }
        break;
      case "array":
        typeDef.element = widenValType(typeDef.element);
        break;
      case "rec":
        for (const inner of typeDef.types) {
          widenTypeDef(inner);
        }
        break;
      case "sub":
        widenTypeDef(typeDef.type);
        break;
    }
  }
  for (const typeDef of mod.types) {
    widenTypeDef(typeDef);
  }
  for (const func of mod.functions) {
    for (const local of func.locals) {
      local.type = widenValType(local.type);
    }
    widenBlockTypesInBody(func.body, widenValType);
  }
  for (const global of mod.globals) {
    global.type = widenValType(global.type);
  }
  for (const imp of mod.imports) {
    if (imp.desc.kind === "global") {
      imp.desc.type = widenValType(imp.desc.type);
    }
  }
}
function widenBlockTypesInBody(body, widenValType) {
  for (const instr of body) {
    const a = instr;
    if (a.blockType && a.blockType.kind === "val") {
      a.blockType.type = widenValType(a.blockType.type);
    }
    if (a.then) widenBlockTypesInBody(a.then, widenValType);
    if (a.else) widenBlockTypesInBody(a.else, widenValType);
    if (a.body && Array.isArray(a.body)) widenBlockTypesInBody(a.body, widenValType);
    if (a.catches) {
      for (const c of a.catches) {
        if (c.body) widenBlockTypesInBody(c.body, widenValType);
      }
    }
    if (a.catchAll) widenBlockTypesInBody(a.catchAll, widenValType);
  }
}

// src/resolve.ts
import ts20 from "typescript";
var _fs = null;
try {
  _fs = await import("node:fs");
} catch {
  _fs = null;
}

// src/treeshake.ts
import ts21 from "typescript";

// src/runtime.ts
var _wasmStructProps = /* @__PURE__ */ new WeakMap();
var _wasmPropDescs = /* @__PURE__ */ new WeakMap();
var _SC_WRITABLE = 1;
var _SC_ENUMERABLE = 2;
var _SC_CONFIGURABLE = 4;
var _SC_DEFINED = 8;
var _SC_ACCESSOR = 16;
function _getSidecarDescs(obj) {
  let m = _wasmPropDescs.get(obj);
  if (!m) {
    m = /* @__PURE__ */ new Map();
    _wasmPropDescs.set(obj, m);
  }
  return m;
}
function _validatePropertyDescriptor(descs, prop, desc) {
  const existing = descs.get(prop);
  let newFlags = _SC_DEFINED;
  if (desc.writable) newFlags |= _SC_WRITABLE;
  if (desc.enumerable) newFlags |= _SC_ENUMERABLE;
  if (desc.configurable) newFlags |= _SC_CONFIGURABLE;
  if (desc.get !== void 0 || desc.set !== void 0) newFlags |= _SC_ACCESSOR;
  if (existing === void 0) return newFlags;
  const isConfigurable = !!(existing & _SC_CONFIGURABLE);
  if (isConfigurable) return newFlags;
  if (desc.configurable === true) {
    throw new TypeError("Cannot redefine property: " + String(prop));
  }
  if (desc.enumerable !== void 0) {
    const wasEnumerable = !!(existing & _SC_ENUMERABLE);
    if (desc.enumerable !== wasEnumerable) {
      throw new TypeError("Cannot redefine property: " + String(prop));
    }
  }
  const wasAccessor = !!(existing & _SC_ACCESSOR);
  const isAccessor = desc.get !== void 0 || desc.set !== void 0;
  if (isAccessor && !wasAccessor) {
    throw new TypeError("Cannot redefine property: " + String(prop));
  }
  if (!isAccessor && wasAccessor && (desc.value !== void 0 || desc.writable !== void 0)) {
    throw new TypeError("Cannot redefine property: " + String(prop));
  }
  if (!wasAccessor && !isAccessor) {
    const wasWritable = !!(existing & _SC_WRITABLE);
    if (!wasWritable) {
      if (desc.writable === true) {
        throw new TypeError("Cannot redefine property: " + String(prop));
      }
      if (desc.value !== void 0) {
        throw new TypeError("Cannot redefine property: " + String(prop));
      }
    }
  }
  let resultFlags = existing;
  if (desc.writable === false) resultFlags &= ~_SC_WRITABLE;
  return resultFlags;
}
function _isWasmStruct(obj) {
  if (obj == null || typeof obj !== "object") return false;
  try {
    if (Object.getPrototypeOf(obj) !== null) return false;
    const probe = Symbol();
    obj[probe] = 1;
    delete obj[probe];
    return false;
  } catch {
    return true;
  }
}
function _getSidecar(obj) {
  let sc = _wasmStructProps.get(obj);
  if (!sc) {
    sc = /* @__PURE__ */ Object.create(null);
    _wasmStructProps.set(obj, sc);
  }
  return sc;
}
function _sidecarGet(obj, key) {
  const sc = _wasmStructProps.get(obj);
  return sc?.[key];
}
function _sidecarSet(obj, key, val) {
  _getSidecar(obj)[key] = val;
}
function _toPrimitive(obj, hint, callbackState) {
  const scToPrim = _sidecarGet(obj, Symbol.toPrimitive);
  if (typeof scToPrim === "function") {
    try {
      const prim = scToPrim.call(obj, hint);
      if (prim == null || typeof prim !== "object") return prim;
    } catch {}
  }
  const exports = callbackState?.getExports();
  const tryMethod = (name) => {
    const scFn = _sidecarGet(obj, name);
    if (typeof scFn === "function") {
      try {
        const prim = scFn.call(obj);
        if (prim == null || typeof prim !== "object") return prim;
      } catch {}
    }
    if (exports) {
      const sget = exports[`__sget_${name}`];
      if (typeof sget === "function") {
        try {
          const field = sget(obj);
          if (typeof field === "function") {
            const prim = field.call(obj);
            if (prim == null || typeof prim !== "object") return prim;
          } else if (field != null && typeof field !== "object") {
            return field;
          }
          if (field != null && typeof field === "object") {
            const callFn = exports[`__call_${name}`];
            if (typeof callFn === "function") {
              try {
                const prim = callFn(obj);
                if (prim == null || typeof prim !== "object") return prim;
              } catch {}
            }
          }
        } catch {}
      }
    }
    return void 0;
  };
  if (hint === "string") {
    const ts22 = tryMethod("toString");
    if (ts22 !== void 0) return ts22;
    const vo = tryMethod("valueOf");
    if (vo !== void 0) return vo;
  } else {
    const vo = tryMethod("valueOf");
    if (vo !== void 0) return vo;
    const ts22 = tryMethod("toString");
    if (ts22 !== void 0) return ts22;
  }
  return void 0;
}
function _toPrimitiveSync(v, hint) {
  if (v == null || typeof v !== "object") return v;
  return _toPrimitive(v, hint) ?? "[object Object]";
}
function _getStructFieldNames(obj, exports) {
  if (!exports) return null;
  const fn = exports.__struct_field_names;
  if (typeof fn !== "function") return null;
  const csv = fn(obj);
  if (csv == null || typeof csv !== "string" || csv === "") return null;
  return csv.split(",");
}
function _structToPlainObject(obj, exports) {
  const fieldNames = _getStructFieldNames(obj, exports);
  if (!fieldNames) return void 0;
  const result = {};
  for (const key of fieldNames) {
    const getter = exports?.[`__sget_${key}`];
    if (typeof getter === "function") {
      let val = getter(obj);
      val = _wasmToPlain(val, exports);
      result[key] = val;
    }
  }
  const sc = _wasmStructProps.get(obj);
  if (sc) {
    for (const key of Object.keys(sc)) {
      if (!(key in result)) result[key] = sc[key];
    }
  }
  return result;
}
function _wasmToPlain(val, exports) {
  if (val == null || typeof val !== "object") return val;
  if (!_isWasmStruct(val)) return val;
  const fieldNames = _getStructFieldNames(val, exports);
  if (fieldNames) {
    return _structToPlainObject(val, exports);
  }
  if (exports) {
    const vecLen = exports.__vec_len;
    const vecGet = exports.__vec_get;
    if (typeof vecLen === "function" && typeof vecGet === "function") {
      try {
        const len = vecLen(val);
        if (typeof len === "number" && len > 0) {
          const arr = [];
          for (let i = 0; i < len; i++) {
            arr.push(_wasmToPlain(vecGet(val, i), exports));
          }
          return arr;
        }
        if (len === 0) {
          return [];
        }
      } catch {}
    }
  }
  return val;
}
var _symbolToWasm = /* @__PURE__ */ new Map([
  [Symbol.iterator, "@@iterator"],
  [Symbol.hasInstance, "@@hasInstance"],
  [Symbol.toPrimitive, "@@toPrimitive"],
  [Symbol.toStringTag, "@@toStringTag"],
  [Symbol.species, "@@species"],
  [Symbol.isConcatSpreadable, "@@isConcatSpreadable"],
  [Symbol.match, "@@match"],
  [Symbol.replace, "@@replace"],
  [Symbol.search, "@@search"],
  [Symbol.split, "@@split"],
  [Symbol.unscopables, "@@unscopables"],
  [Symbol.asyncIterator, "@@asyncIterator"],
]);
function _safeGet(obj, key) {
  if (obj == null) return void 0;
  const direct = obj[key];
  if (direct !== void 0) return direct;
  const sc = _sidecarGet(obj, key);
  if (sc !== void 0) return sc;
  if (typeof key === "symbol") {
    const wasmKey = _symbolToWasm.get(key);
    if (wasmKey) return _sidecarGet(obj, wasmKey);
  }
  return void 0;
}
function _safeSet(obj, key, val) {
  if (obj == null) return;
  try {
    obj[key] = val;
  } catch {
    _sidecarSet(obj, key, val);
    if (typeof key === "symbol") {
      const wasmKey = _symbolToWasm.get(key);
      if (wasmKey) _sidecarSet(obj, wasmKey, val);
    }
    if (typeof key === "string" && key.startsWith("@@")) {
      for (const [sym, wk] of _symbolToWasm) {
        if (wk === key) {
          _sidecarSet(obj, sym, val);
          break;
        }
      }
    }
  }
}
var jsString = {
  concat: (a, b) => {
    try {
      return a + b;
    } catch {
      const sa = typeof a === "string" ? a : _toPrimitiveSync(a, "default");
      const sb = typeof b === "string" ? b : _toPrimitiveSync(b, "default");
      return String(sa) + String(sb);
    }
  },
  length: (s) => s.length,
  equals: (a, b) => (a === b ? 1 : 0),
  substring: (s, start, end) => s.substring(start, end),
  charCodeAt: (s, i) => s.charCodeAt(i),
};
function resolveImport(intent, deps, callbackState) {
  switch (intent.type) {
    case "string_literal":
      return () => intent.value;
    case "math":
      return Math[intent.method];
    case "console_log": {
      const variant = intent.variant;
      let consoleFn = console.log;
      let isBool = variant === "bool";
      if (variant.startsWith("warn_")) {
        consoleFn = console.warn;
        isBool = variant === "warn_bool";
      } else if (variant.startsWith("error_")) {
        consoleFn = console.error;
        isBool = variant === "error_bool";
      } else if (variant.startsWith("log_")) {
        isBool = variant === "log_bool";
      } else if (variant === "bool") {
        isBool = true;
      }
      return isBool ? (v) => consoleFn(Boolean(v)) : (v) => consoleFn(v);
    }
    case "string_method": {
      const method = intent.method;
      return (s, ...a) => String(s)[method](...a);
    }
    case "extern_class": {
      if (intent.className === "Document" && intent.action === "get" && intent.member === "body") {
        return (self) => self.body;
      }
      if (intent.className === "Document" && intent.action === "method" && intent.member === "createElement") {
        return (self, tagName, options) =>
          options == null ? self.createElement(tagName) : self.createElement(tagName, options);
      }
      if (intent.action === "method" && intent.member === "addEventListener") {
        return (self, type, listener, options) =>
          options == null ? self.addEventListener(type, listener) : self.addEventListener(type, listener, options);
      }
      if (intent.action === "new") {
        class Test262Error extends Error {
          constructor(msg) {
            super(msg);
            this.name = "Test262Error";
          }
        }
        const builtinCtors = {
          Map,
          Set,
          WeakMap,
          WeakSet,
          WeakRef,
          RegExp,
          ArrayBuffer,
          DataView,
          Error,
          TypeError,
          RangeError,
          SyntaxError,
          URIError,
          EvalError,
          ReferenceError,
          AggregateError,
          Test262Error,
        };
        const Ctor = deps?.[intent.className] ?? builtinCtors[intent.className];
        if (!Ctor)
          return (...args) => {
            throw new Error(`No dependency provided for extern class "${intent.className}"`);
          };
        return (...args) => {
          let len = args.length;
          while (len > 0 && args[len - 1] == null) len--;
          return new Ctor(...args.slice(0, len));
        };
      }
      if (intent.action === "get") {
        const member = intent.member;
        return (self) => _safeGet(self, member);
      }
      if (intent.action === "set") {
        const member = intent.member;
        return (self, v) => _safeSet(self, member, v);
      }
      const m = intent.member;
      return (self, ...args) => {
        if (self == null) return void 0;
        const fn = self[m] ?? _sidecarGet(self, m);
        if (typeof fn === "function") return fn.call(self, ...args);
        return void 0;
      };
    }
    case "builtin": {
      const name = intent.name;
      if (name === "number_toString") return (v) => String(v);
      if (name === "number_toFixed") return (v, d) => v.toFixed(d);
      if (name === "number_toPrecision") return (v, p) => v.toPrecision(p);
      if (name === "number_toExponential") return (v, d) => (isNaN(d) ? v.toExponential() : v.toExponential(d));
      if (name === "JSON_stringify")
        return (v, replacer, space) => {
          const exports = callbackState?.getExports();
          const plain = _wasmToPlain(v, exports);
          const rep = replacer == null || (typeof replacer === "number" && isNaN(replacer)) ? void 0 : replacer;
          const sp = space == null || (typeof space === "number" && isNaN(space)) ? void 0 : space;
          return JSON.stringify(plain, rep, sp);
        };
      if (name === "JSON_parse") return (s) => JSON.parse(s);
      if (name === "__extern_get")
        return (obj, key) => {
          const val = _safeGet(obj, key);
          if (val !== void 0) return val;
          if (typeof key === "string") {
            const exports = callbackState?.getExports();
            const getter = exports?.[`__sget_${key}`];
            if (typeof getter === "function") return getter(obj);
          }
          return void 0;
        };
      if (name === "__extern_set") return _safeSet;
      if (name === "__extern_length")
        return (obj) => {
          if (obj == null) return 0;
          const len = obj.length;
          if (len !== void 0) return len;
          return _sidecarGet(obj, "length") ?? 0;
        };
      if (name === "__extern_toString")
        return (v) => {
          if (v == null) return String(v);
          if (typeof v.toString === "function") return v.toString();
          if (typeof v === "object") {
            const prim = _toPrimitive(v, "string", callbackState);
            if (prim !== void 0) return String(prim);
          }
          try {
            return String(v);
          } catch {
            return "[object Object]";
          }
        };
      if (name === "__extern_is_undefined") return (v) => (v === void 0 ? 1 : 0);
      if (name === "__get_undefined") return () => void 0;
      if (name === "__box_symbol") {
        const symbolCache = /* @__PURE__ */ new Map([
          [1, Symbol.iterator],
          [2, Symbol.hasInstance],
          [3, Symbol.toPrimitive],
          [4, Symbol.toStringTag],
          [5, Symbol.species],
          [6, Symbol.isConcatSpreadable],
          [7, Symbol.match],
          [8, Symbol.replace],
          [9, Symbol.search],
          [10, Symbol.split],
          [11, Symbol.unscopables],
          [12, Symbol.asyncIterator],
        ]);
        return (id) => {
          let sym = symbolCache.get(id);
          if (sym === void 0) {
            sym = Symbol(`wasm_${id}`);
            symbolCache.set(id, sym);
          }
          return sym;
        };
      }
      if (name === "__object_create") return (proto) => Object.create(proto);
      if (name === "__object_freeze")
        return (obj) => {
          try {
            return Object.freeze(obj);
          } catch {
            return obj;
          }
        };
      if (name === "__object_seal")
        return (obj) => {
          try {
            return Object.seal(obj);
          } catch {
            return obj;
          }
        };
      if (name === "__object_preventExtensions")
        return (obj) => {
          try {
            return Object.preventExtensions(obj);
          } catch {
            return obj;
          }
        };
      if (name === "__object_keys")
        return (obj) => {
          if (obj == null) return [];
          if (_isWasmStruct(obj)) {
            const exports = callbackState?.getExports();
            const fieldNames = _getStructFieldNames(obj, exports);
            if (fieldNames) return fieldNames;
          }
          return Object.keys(obj);
        };
      if (name === "__object_values")
        return (obj) => {
          if (obj == null) return [];
          if (_isWasmStruct(obj)) {
            const exports = callbackState?.getExports();
            const fieldNames = _getStructFieldNames(obj, exports);
            if (fieldNames) {
              return fieldNames.map((key) => {
                const getter = exports?.[`__sget_${key}`];
                return typeof getter === "function" ? getter(obj) : void 0;
              });
            }
          }
          return Object.values(obj);
        };
      if (name === "__object_entries")
        return (obj) => {
          if (obj == null) return [];
          if (_isWasmStruct(obj)) {
            const exports = callbackState?.getExports();
            const fieldNames = _getStructFieldNames(obj, exports);
            if (fieldNames) {
              return fieldNames.map((key) => {
                const getter = exports?.[`__sget_${key}`];
                const val = typeof getter === "function" ? getter(obj) : void 0;
                return [key, val];
              });
            }
          }
          return Object.entries(obj);
        };
      if (name === "__extern_slice")
        return (arr, start) => {
          if (Array.isArray(arr)) return arr.slice(start);
          if (typeof arr === "string") return Array.from(arr).slice(start);
          if (_isWasmStruct(arr)) {
            const exports = callbackState?.getExports();
            const fieldNames = _getStructFieldNames(arr, exports);
            if (fieldNames && exports) {
              const result = [];
              for (let i = Math.max(0, start); i < fieldNames.length; i++) {
                const getter = exports[`__sget_${fieldNames[i]}`];
                if (typeof getter === "function") {
                  let val = getter(arr);
                  if (_isWasmStruct(val)) val = _structToPlainObject(val, exports) ?? val;
                  result.push(val);
                }
              }
              return result;
            }
          }
          if (arr != null && typeof arr[Symbol.iterator] === "function") return Array.from(arr).slice(start);
          return [];
        };
      if (name === "__extern_rest_object")
        return (obj, excludedKeysStr) => {
          if (obj == null) return {};
          const excluded = new Set(excludedKeysStr ? String(excludedKeysStr).split(",") : []);
          const result = {};
          if (_isWasmStruct(obj)) {
            const exports = callbackState?.getExports();
            const fieldNames = _getStructFieldNames(obj, exports);
            if (fieldNames) {
              for (const key of fieldNames) {
                if (!excluded.has(key)) {
                  const getter = exports?.[`__sget_${key}`];
                  if (typeof getter === "function") result[key] = getter(obj);
                }
              }
            }
          } else {
            for (const key of Object.keys(obj)) {
              if (!excluded.has(key)) result[key] = obj[key];
            }
          }
          const sc = _wasmStructProps.get(obj);
          if (sc) {
            for (const key of Object.keys(sc)) {
              if (!excluded.has(key) && !(key in result)) result[key] = sc[key];
            }
          }
          return result;
        };
      if (name === "__defineProperty_value")
        return (obj, prop, value, flags) => {
          if (obj == null || (typeof obj !== "object" && typeof obj !== "function")) {
            throw new TypeError("Object.defineProperty called on non-object");
          }
          const desc = {};
          if (flags & (1 << 7)) desc.value = value;
          if (flags & (1 << 3)) desc.writable = !!(flags & 1);
          if (flags & (1 << 4)) desc.enumerable = !!(flags & (1 << 1));
          if (flags & (1 << 5)) desc.configurable = !!(flags & (1 << 2));
          try {
            Object.defineProperty(obj, prop, desc);
          } catch (e) {
            if (e instanceof TypeError) {
              const msg = e.message || "";
              if (msg.includes("opaque") || msg.includes("WebAssembly")) {
                const sDescs = _getSidecarDescs(obj);
                const newFlags = _validatePropertyDescriptor(sDescs, prop, desc);
                sDescs.set(prop, newFlags);
                if (desc.value !== void 0) _sidecarSet(obj, prop, desc.value);
              } else {
                throw e;
              }
            } else {
              if (desc.value !== void 0) _sidecarSet(obj, prop, desc.value);
            }
          }
          return obj;
        };
      if (name === "__defineProperties")
        return (obj, descsObj) => {
          if (obj == null || (typeof obj !== "object" && typeof obj !== "function")) {
            throw new TypeError("Object.defineProperties called on non-object");
          }
          if (descsObj == null) return obj;
          const getKeys = (o) => {
            if (_isWasmStruct(o)) {
              const exps = callbackState?.getExports();
              const fieldNames = _getStructFieldNames(o, exps) ?? [];
              const sc = _wasmStructProps.get(o);
              if (sc) {
                for (const k of Object.keys(sc)) if (!fieldNames.includes(k)) fieldNames.push(k);
              }
              return fieldNames;
            }
            return Object.keys(o);
          };
          const getField = (o, field) => {
            if (!_isWasmStruct(o)) return o[field];
            let v = _sidecarGet(o, field);
            if (v === void 0) {
              const exps = callbackState?.getExports();
              const g = exps?.[`__sget_${field}`];
              if (typeof g === "function") v = g(o);
            }
            return v;
          };
          try {
            Object.defineProperties(obj, descsObj);
          } catch (e) {
            if (e instanceof TypeError) {
              const msg = e.message || "";
              if (msg.includes("opaque") || msg.includes("WebAssembly")) {
                const sDescs = _getSidecarDescs(obj);
                const keys = getKeys(descsObj);
                for (const key of keys) {
                  const rawDesc = getField(descsObj, key);
                  if (rawDesc && typeof rawDesc === "object") {
                    const desc = {};
                    const val = getField(rawDesc, "value");
                    if (val !== void 0) desc.value = val;
                    const wr = getField(rawDesc, "writable");
                    if (wr !== void 0) desc.writable = !!wr;
                    const en = getField(rawDesc, "enumerable");
                    if (en !== void 0) desc.enumerable = !!en;
                    const conf = getField(rawDesc, "configurable");
                    if (conf !== void 0) desc.configurable = !!conf;
                    const getFn = getField(rawDesc, "get");
                    if (getFn !== void 0) desc.get = getFn;
                    const setFn = getField(rawDesc, "set");
                    if (setFn !== void 0) desc.set = setFn;
                    const newFlags = _validatePropertyDescriptor(sDescs, key, desc);
                    sDescs.set(key, newFlags);
                    if (desc.value !== void 0) _sidecarSet(obj, key, desc.value);
                  }
                }
              } else {
                throw e;
              }
            } else {
              const keys = getKeys(descsObj);
              for (const key of keys) {
                const rawDesc = getField(descsObj, key);
                if (rawDesc && typeof rawDesc === "object") {
                  const val = getField(rawDesc, "value");
                  if (val !== void 0) _sidecarSet(obj, key, val);
                }
              }
            }
          }
          return obj;
        };
      if (name === "__getOwnPropertyDescriptor")
        return (obj, prop) => {
          if (obj == null) return void 0;
          return Object.getOwnPropertyDescriptor(obj, prop);
        };
      if (name === "__create_descriptor")
        return (value, flags) => {
          return {
            value,
            writable: !!(flags & 1),
            enumerable: !!(flags & 2),
            configurable: !!(flags & 4),
          };
        };
      if (name === "__js_array_new") return () => [];
      if (name === "__js_array_push")
        return (arr, val) => {
          arr.push(val);
        };
      if (name === "__tagged_template") return (tag, strings, subs) => tag(strings, ...subs);
      if (name === "__hasOwnProperty")
        return (obj, key) => {
          if (obj == null) return 0;
          if (!_isWasmStruct(obj)) {
            try {
              return Object.prototype.hasOwnProperty.call(obj, key) ? 1 : 0;
            } catch {
              return 0;
            }
          }
          const sc = _wasmStructProps.get(obj);
          if (sc && key in sc) return 1;
          const exports = callbackState?.getExports();
          const fieldNames = _getStructFieldNames(obj, exports) ?? [];
          return fieldNames.includes(String(key)) ? 1 : 0;
        };
      if (name === "__propertyIsEnumerable")
        return (obj, key) => {
          if (obj == null) return 0;
          if (!_isWasmStruct(obj)) {
            try {
              return Object.prototype.propertyIsEnumerable.call(obj, key) ? 1 : 0;
            } catch {
              return 0;
            }
          }
          const descs = _wasmPropDescs.get(obj);
          if (descs) {
            const flags = descs.get(String(key));
            if (flags !== void 0) return flags & _SC_ENUMERABLE ? 1 : 0;
          }
          const sc = _wasmStructProps.get(obj);
          if (sc && String(key) in sc) return 1;
          const exports = callbackState?.getExports();
          const fieldNames = _getStructFieldNames(obj, exports) ?? [];
          return fieldNames.includes(String(key)) ? 1 : 0;
        };
      if (name === "__for_in_keys")
        return (obj) => {
          if (obj == null) return [];
          if (!_isWasmStruct(obj)) {
            try {
              const keys2 = [];
              for (const k in obj) keys2.push(k);
              return keys2;
            } catch (e) {
              if (
                !(e instanceof TypeError) ||
                !(typeof e.message === "string" && (e.message.includes("opaque") || e.message.includes("WebAssembly")))
              ) {
                throw e;
              }
            }
          }
          const exports = callbackState?.getExports();
          const keys = [];
          const seen = /* @__PURE__ */ new Set();
          let current = obj;
          while (current != null) {
            if (_isWasmStruct(current)) {
              const fieldNames = _getStructFieldNames(current, exports) ?? [];
              for (const k of fieldNames) {
                if (!seen.has(k)) {
                  keys.push(k);
                  seen.add(k);
                }
              }
              const sc = _wasmStructProps.get(current);
              if (sc) {
                const descs = _wasmPropDescs.get(current);
                for (const k of Object.keys(sc)) {
                  if (seen.has(k)) continue;
                  if (descs) {
                    const flags = descs.get(k);
                    if (flags !== void 0 && flags & _SC_DEFINED && !(flags & _SC_ENUMERABLE)) continue;
                  }
                  keys.push(k);
                  seen.add(k);
                }
              }
            } else {
              try {
                for (const k of Object.keys(current)) {
                  if (!seen.has(k)) {
                    keys.push(k);
                    seen.add(k);
                  }
                }
                for (const k of Object.getOwnPropertyNames(current)) {
                  seen.add(k);
                }
              } catch {
                break;
              }
            }
            try {
              current = Object.getPrototypeOf(current);
            } catch {
              break;
            }
          }
          return keys;
        };
      if (name === "__for_in_len")
        return (keys) => {
          if (keys == null || !Array.isArray(keys)) return 0;
          return keys.length;
        };
      if (name === "__for_in_get")
        return (keys, i) => {
          if (keys == null || !Array.isArray(keys)) return void 0;
          return keys[i];
        };
      const _vecToArray = (arr) => {
        if (Array.isArray(arr) || arr == null) return arr;
        if (arr[Symbol.iterator]) return arr;
        const exps = callbackState?.getExports();
        const vecLen = exps?.__vec_len;
        const vecGet = exps?.__vec_get;
        if (typeof vecLen === "function" && typeof vecGet === "function") {
          try {
            const len = vecLen(arr);
            if (typeof len === "number") {
              const result = [];
              for (let i = 0; i < len; i++) {
                result.push(vecGet(arr, i));
              }
              return result;
            }
          } catch {}
        }
        return arr;
      };
      if (name === "Promise_all") return (arr) => Promise.all(_vecToArray(arr));
      if (name === "Promise_race") return (arr) => Promise.race(_vecToArray(arr));
      if (name === "Promise_allSettled") return (arr) => Promise.allSettled(_vecToArray(arr));
      if (name === "Promise_any") return (arr) => Promise.any(_vecToArray(arr));
      if (name === "Promise_resolve") return (val) => Promise.resolve(val);
      if (name === "Promise_reject") return (val) => Promise.reject(val);
      if (name === "Promise_new") return (executor) => new Promise(executor);
      if (name === "Promise_then") return (p, cb) => p.then(cb);
      if (name === "Promise_then2") return (p, cb1, cb2) => p.then(cb1, cb2);
      if (name === "Promise_catch") return (p, cb) => p.catch(cb);
      if (name === "Promise_finally") return (p, cb) => p.finally(cb);
      if (name === "__gen_create_buffer") return () => [];
      if (name === "__gen_push_f64")
        return (buf, v) => {
          buf.push(v);
        };
      if (name === "__gen_push_i32")
        return (buf, v) => {
          buf.push(v);
        };
      if (name === "__gen_push_ref")
        return (buf, v) => {
          buf.push(v);
        };
      if (name === "__create_generator")
        return (buf) => {
          let index = 0;
          return {
            next() {
              if (index < buf.length) {
                return { value: buf[index++], done: false };
              }
              return { value: void 0, done: true };
            },
            return(value) {
              index = buf.length;
              return { value, done: true };
            },
            throw(e) {
              index = buf.length;
              throw e;
            },
            [Symbol.iterator]() {
              return this;
            },
          };
        };
      if (name === "__create_async_generator")
        return (buf, pendingThrow) => {
          let index = 0;
          function mkResult(value, done) {
            const plain = { value, done };
            return {
              value,
              done,
              then(res, rej) {
                return Promise.resolve(plain).then(res, rej);
              },
            };
          }
          function mkError(e) {
            return {
              done: true,
              value: void 0,
              then(res, rej) {
                return Promise.reject(e).then(res, rej);
              },
            };
          }
          return {
            next() {
              if (index < buf.length) return mkResult(buf[index++], false);
              if (pendingThrow !== null && pendingThrow !== undefined) {
                const e = pendingThrow;
                pendingThrow = null;
                return mkError(e);
              }
              return mkResult(void 0, true);
            },
            return(value) {
              index = buf.length;
              return mkResult(value, true);
            },
            throw(e) {
              index = buf.length;
              return mkError(e);
            },
            [Symbol.asyncIterator]() {
              return this;
            },
          };
        };
      if (name === "__gen_next")
        return (gen) => {
          const next = gen.next ?? _sidecarGet(gen, "next");
          if (typeof next === "function") return next.call(gen);
          throw new TypeError("generator.next is not a function");
        };
      if (name === "__gen_return")
        return (gen, val) => {
          const ret2 = gen.return ?? _sidecarGet(gen, "return");
          if (typeof ret2 === "function") return ret2.call(gen, val);
          return { value: val, done: true };
        };
      if (name === "__gen_throw")
        return (gen, err) => {
          const thr = gen.throw ?? _sidecarGet(gen, "throw");
          if (typeof thr === "function") return thr.call(gen, err);
          throw err;
        };
      if (name === "__gen_result_value")
        return (result) => {
          let val = result.value;
          if (val !== void 0) return val;
          val = _sidecarGet(result, "value");
          if (val !== void 0) return val;
          const exports = callbackState?.getExports();
          return exports?.__sget_value?.(result);
        };
      if (name === "__gen_result_value_f64")
        return (result) => {
          let val = result.value ?? _sidecarGet(result, "value");
          if (val === void 0) {
            const exports = callbackState?.getExports();
            val = exports?.__sget_value?.(result);
          }
          return Number(val);
        };
      if (name === "__gen_result_done")
        return (result) => {
          let done = result.done ?? _sidecarGet(result, "done");
          if (done === void 0) {
            const exports = callbackState?.getExports();
            done = exports?.__sget_done?.(result);
          }
          return done ? 1 : 0;
        };
      if (name === "__iterator")
        return (obj) => {
          const fn = obj[Symbol.iterator] ?? _sidecarGet(obj, Symbol.iterator) ?? _sidecarGet(obj, "@@iterator");
          if (typeof fn === "function") return fn.call(obj);
          if (_isWasmStruct(obj)) {
            const exports = callbackState?.getExports();
            const callIter = exports?.["__call_@@iterator"];
            if (typeof callIter === "function") {
              const iter = callIter(obj);
              if (iter != null) return iter;
            }
            const vecLen = exports?.__vec_len;
            const vecGet = exports?.__vec_get;
            if (typeof vecLen === "function" && typeof vecGet === "function") {
              const len = vecLen(obj);
              if (typeof len === "number" && len >= 0) {
                let i = 0;
                return {
                  next() {
                    if (i >= len) return { value: void 0, done: true };
                    const val = vecGet(obj, i);
                    i++;
                    return { value: val, done: false };
                  },
                  [Symbol.iterator]() {
                    return this;
                  },
                };
              }
            }
          }
          throw new TypeError(
            (typeof obj === "object" ? Object.prototype.toString.call(obj) : String(obj)) + " is not iterable",
          );
        };
      if (name === "__async_iterator")
        return (obj) => {
          const asyncIter =
            obj[Symbol.asyncIterator] ?? _sidecarGet(obj, Symbol.asyncIterator) ?? _sidecarGet(obj, "@@asyncIterator");
          if (asyncIter) return asyncIter.call(obj);
          const syncIter = obj[Symbol.iterator] ?? _sidecarGet(obj, Symbol.iterator) ?? _sidecarGet(obj, "@@iterator");
          if (typeof syncIter === "function") return syncIter.call(obj);
          if (_isWasmStruct(obj)) {
            const exports = callbackState?.getExports();
            const callIter = exports?.["__call_@@iterator"];
            if (typeof callIter === "function") {
              const iter = callIter(obj);
              if (iter != null) return iter;
            }
            const vecLen = exports?.__vec_len;
            const vecGet = exports?.__vec_get;
            if (typeof vecLen === "function" && typeof vecGet === "function") {
              const len = vecLen(obj);
              if (typeof len === "number" && len >= 0) {
                let i = 0;
                return {
                  next() {
                    if (i >= len) return { value: void 0, done: true };
                    const val = vecGet(obj, i);
                    i++;
                    return { value: val, done: false };
                  },
                  [Symbol.iterator]() {
                    return this;
                  },
                };
              }
            }
          }
          throw new TypeError("object is not iterable");
        };
      if (name === "__iterator_next")
        return (iter) => {
          let next = iter.next ?? _sidecarGet(iter, "next");
          if (next === void 0) {
            const exports = callbackState?.getExports();
            next = exports?.__sget_next?.(iter);
          }
          if (typeof next === "function") return next.call(iter);
          {
            const exports = callbackState?.getExports();
            const callNext = exports?.["__call_next"];
            if (typeof callNext === "function") {
              const result = callNext(iter);
              if (result != null) return result;
            }
          }
          throw new TypeError("iterator.next is not a function");
        };
      if (name === "__iterator_done")
        return (result) => {
          let done = result.done ?? _sidecarGet(result, "done");
          if (done === void 0) {
            const exports = callbackState?.getExports();
            done = exports?.__sget_done?.(result);
          }
          return done ? 1 : 0;
        };
      if (name === "__iterator_value")
        return (result) => {
          let val = result.value;
          if (val !== void 0) return val;
          val = _sidecarGet(result, "value");
          if (val !== void 0) return val;
          const exports = callbackState?.getExports();
          return exports?.__sget_value?.(result);
        };
      if (name === "__iterator_return")
        return (iter) => {
          let ret2 = iter?.return ?? _sidecarGet(iter, "return");
          if (ret2 === void 0) {
            const exports = callbackState?.getExports();
            ret2 = exports?.__sget_return?.(iter);
          }
          if (typeof ret2 === "function") ret2.call(iter);
        };
      if (name === "__make_iterable") {
        const convertToJS = (obj) => {
          if (obj == null || typeof obj !== "object") return obj;
          if (obj[Symbol.iterator]) return obj;
          const exports = callbackState?.getExports();
          if (!exports) return obj;
          const fieldNames = exports.__struct_field_names;
          if (typeof fieldNames === "function") {
            const names = fieldNames(obj);
            if (typeof names === "string" && names.length > 0) {
              const parts = names.split(",");
              const isNumeric = parts.every((p) => /^_\d+$/.test(p));
              if (isNumeric) {
                const arr = new Array(parts.length);
                for (let i = 0; i < parts.length; i++) {
                  const getter = exports[`__sget_${parts[i]}`];
                  arr[i] = getter ? convertToJS(getter(obj)) : void 0;
                }
                return arr;
              }
            }
          }
          const vecLen = exports.__vec_len;
          const vecGet = exports.__vec_get;
          if (typeof vecLen === "function" && typeof vecGet === "function") {
            const len = vecLen(obj);
            if (typeof len === "number" && len >= 0) {
              const arr = new Array(len);
              for (let i = 0; i < len; i++) {
                arr[i] = convertToJS(vecGet(obj, i));
              }
              return arr;
            }
          }
          return obj;
        };
        return convertToJS;
      }
      if (name === "__array_entries")
        return (arr) => {
          let i = 0;
          let len;
          return {
            next() {
              const exports = callbackState?.getExports();
              const vecLen = exports?.__vec_len;
              const vecGet = exports?.__vec_get;
              if (typeof vecLen !== "function" || typeof vecGet !== "function") return { value: void 0, done: true };
              if (len === void 0) len = vecLen(arr);
              if (i >= len) return { value: void 0, done: true };
              const val = vecGet(arr, i);
              const entry = [i, val];
              i++;
              return { value: entry, done: false };
            },
            [Symbol.iterator]() {
              return this;
            },
          };
        };
      if (name === "__array_keys")
        return (arr) => {
          let i = 0;
          let len;
          return {
            next() {
              const exports = callbackState?.getExports();
              const vecLen = exports?.__vec_len;
              if (typeof vecLen !== "function") return { value: void 0, done: true };
              if (len === void 0) len = vecLen(arr);
              if (i >= len) return { value: void 0, done: true };
              return { value: i++, done: false };
            },
            [Symbol.iterator]() {
              return this;
            },
          };
        };
      if (name === "__array_values")
        return (arr) => {
          let i = 0;
          let len;
          return {
            next() {
              const exports = callbackState?.getExports();
              const vecLen = exports?.__vec_len;
              const vecGet = exports?.__vec_get;
              if (typeof vecLen !== "function" || typeof vecGet !== "function") return { value: void 0, done: true };
              if (len === void 0) len = vecLen(arr);
              if (i >= len) return { value: void 0, done: true };
              return { value: vecGet(arr, i++), done: false };
            },
            [Symbol.iterator]() {
              return this;
            },
          };
        };
      if (name === "__call_1_f64") return (fn, a) => fn(a);
      if (name === "__call_2_f64") return (fn, a, b) => fn(a, b);
      if (name === "__call_1_i32") return (fn, a) => fn(a);
      if (name === "__call_2_i32") return (fn, a, b) => fn(a, b);
      if (name === "__typeof") return (v) => typeof v;
      if (name === "__instanceof")
        return (v, ctorName) => {
          try {
            const ctor = globalThis[ctorName];
            if (typeof ctor !== "function") return 0;
            return v instanceof ctor ? 1 : 0;
          } catch {
            return 0;
          }
        };
      if (name === "parseInt")
        return (s, radix) => {
          const r = Number.isNaN(radix) ? void 0 : radix;
          return parseInt(String(s), r);
        };
      if (name === "parseFloat") return (s) => parseFloat(String(s));
      if (name === "decodeURI") return (s) => decodeURI(String(s));
      if (name === "decodeURIComponent") return (s) => decodeURIComponent(String(s));
      if (name === "encodeURI") return (s) => encodeURI(String(s));
      if (name === "encodeURIComponent") return (s) => encodeURIComponent(String(s));
      if (name === "String_fromCharCode") return (code) => String.fromCharCode(code);
      if (name === "string_compare") return (a, b) => (a < b ? -1 : a > b ? 1 : 0);
      if (name === "__toUint32") return (x) => x >>> 0;
      if (name === "__str_extern_len") return (s) => s.length;
      if (name === "__str_from_mem") {
        return (ptr, len) => {
          const exports = callbackState?.getExports();
          const mem = exports?.__str_mem;
          if (!mem) return "";
          if (len <= 0) return "";
          const byteLen = len * 2;
          if (ptr < 0 || ptr + byteLen > mem.buffer.byteLength) return "";
          const u16 = new Uint16Array(mem.buffer, ptr, len);
          if (len <= 4096) return String.fromCharCode(...u16);
          const parts = [];
          for (let i = 0; i < len; i += 4096) {
            const chunk = u16.subarray(i, Math.min(i + 4096, len));
            parts.push(String.fromCharCode(...chunk));
          }
          return parts.join("");
        };
      }
      if (name === "__str_to_mem") {
        return (s, ptr) => {
          const exports = callbackState?.getExports();
          const mem = exports?.__str_mem;
          if (!mem) return;
          const byteLen = s.length * 2;
          if (ptr < 0 || ptr + byteLen > mem.buffer.byteLength) return;
          const u16 = new Uint16Array(mem.buffer, ptr, s.length);
          for (let i = 0; i < s.length; i++) {
            u16[i] = s.charCodeAt(i);
          }
        };
      }
      return () => {};
    }
    case "callback_maker":
      return (id, cap) =>
        (...args) => {
          const exports = callbackState?.getExports();
          return exports?.[`__cb_${id}`]?.(cap, ...args);
        };
    case "await":
      return (v) => v;
    case "dynamic_import":
      return (specifier) =>
        import(
          /* @vite-ignore */
          specifier
        );
    case "typeof_check":
      return (v) => (typeof v === intent.targetType ? 1 : 0);
    case "box":
      return intent.targetType === "boolean" ? (v) => Boolean(v) : (v) => v;
    case "unbox":
      return intent.targetType === "boolean"
        ? (v) => (v ? 1 : 0)
        : (v) => {
            if (v != null && typeof v === "object") {
              const prim = _toPrimitive(v, "number", callbackState);
              if (prim !== void 0) {
                try {
                  return Number(prim);
                } catch {}
              }
            }
            try {
              return Number(v);
            } catch {
              if (v != null && typeof v === "object") {
                const prim = _toPrimitive(v, "number", callbackState);
                if (prim !== void 0) {
                  try {
                    return Number(prim);
                  } catch {}
                }
              }
              return NaN;
            }
          };
    case "truthy_check":
      return (v) => (v ? 1 : 0);
    case "extern_get":
      return (obj, key) => {
        const val = _safeGet(obj, key);
        if (val !== void 0) return val;
        if (typeof key === "string") {
          const exports = callbackState?.getExports();
          const getter = exports?.[`__sget_${key}`];
          if (typeof getter === "function") return getter(obj);
        }
        return void 0;
      };
    case "extern_set":
      return _safeSet;
    case "date_new":
      return () => /* @__PURE__ */ new Date();
    case "date_now":
      return () => Date.now();
    case "date_method": {
      const m = intent.method;
      return (d) => d[m]();
    }
    case "declared_global": {
      const val = deps?.[intent.name];
      if (val !== void 0) return () => val;
      if (intent.name === "globalThis") return () => globalThis;
      return () => {};
    }
    case "proxy_create":
      return (target, handler) => {
        const t = target ?? {};
        const h = handler ?? {};
        try {
          return new Proxy(t, h);
        } catch {
          return t;
        }
      };
    default:
      return () => {};
  }
}
function buildStringConstants(stringPool = []) {
  const constants = {};
  for (const s of stringPool) {
    if (!(s in constants)) {
      constants[s] = new WebAssembly.Global({ value: "externref", mutable: false }, s);
    }
  }
  return constants;
}
function wrapWithContainment(fn, intent, domRoot) {
  const { className, action, member } = intent;
  const traversalProps = /* @__PURE__ */ new Set(["parentElement", "parentNode", "offsetParent"]);
  const blockedProps = /* @__PURE__ */ new Set(["ownerDocument", "baseURI", "getRootNode"]);
  const mutationMethods = /* @__PURE__ */ new Set([
    "appendChild",
    "removeChild",
    "insertBefore",
    "replaceChild",
    "remove",
    "append",
    "prepend",
    "after",
    "before",
    "replaceWith",
    "insertAdjacentElement",
    "insertAdjacentHTML",
    "insertAdjacentText",
  ]);
  function isContained(el) {
    if (el === domRoot) return true;
    if (typeof domRoot.contains === "function") {
      return domRoot.contains(el);
    }
    return true;
  }
  function isNodeLike(v) {
    if (v == null || typeof v !== "object") return false;
    if (typeof Node !== "undefined") return v instanceof Node;
    return typeof v.nodeType === "number";
  }
  if (action === "new" && className === "Document") {
    return () => domRoot;
  }
  if (action === "get" && member) {
    if (blockedProps.has(member)) {
      return (_self) => null;
    }
    if (traversalProps.has(member)) {
      return (self) => {
        const result = self[member];
        if (result == null) return result;
        if (isNodeLike(result) && !isContained(result)) return null;
        return result;
      };
    }
    return (self) => {
      if (self !== domRoot && isNodeLike(self) && !isContained(self)) {
        throw new Error(`DOM containment violation: accessing "${member}" on element outside container`);
      }
      return self[member];
    };
  }
  if (action === "set" && member) {
    return (self, v) => {
      if (self !== domRoot && isNodeLike(self) && !isContained(self)) {
        throw new Error(`DOM containment violation: setting "${member}" on element outside container`);
      }
      self[member] = v;
    };
  }
  if (action === "method" && member) {
    if (
      (className === "Document" || className === "document") &&
      (member === "querySelector" ||
        member === "querySelectorAll" ||
        member === "getElementById" ||
        member === "getElementsByClassName" ||
        member === "getElementsByTagName")
    ) {
      return (_self, ...args) => domRoot[member](...args);
    }
    if ((className === "Document" || className === "document") && member === "createElement") {
      return fn;
    }
    if (mutationMethods.has(member)) {
      return (self, ...args) => {
        if (self !== domRoot && isNodeLike(self) && !isContained(self)) {
          throw new Error(`DOM containment violation: calling "${member}" on element outside container`);
        }
        return self[member](...args);
      };
    }
    return (self, ...args) => {
      if (self !== domRoot && isNodeLike(self) && !isContained(self)) {
        throw new Error(`DOM containment violation: calling "${member}" on element outside container`);
      }
      return self[member](...args);
    };
  }
  return fn;
}
var runtimeReflectApply = Reflect.apply;
var runtimeInstanceExportsGetter = Object.getOwnPropertyDescriptor(WebAssembly.Instance.prototype, "exports")?.get;
function brandedInstanceExports(value) {
  if (!runtimeInstanceExportsGetter) return void 0;
  try {
    return runtimeReflectApply(runtimeInstanceExportsGetter, value, []);
  } catch {
    return void 0;
  }
}
function buildImports(manifest, deps, stringPool, options) {
  const env = {};
  let wasmExports;
  const callbackState = { getExports: () => wasmExports };
  let hasCallbacks = false;
  let lastCaughtException = void 0;
  const MAX_HOST_RECURSION_DEPTH = 100;
  let hostCallDepth = 0;
  for (const imp of manifest) {
    if (imp.module !== "env") continue;
    let fn;
    if (imp.name === "__get_caught_exception") {
      fn = () => lastCaughtException;
      env[imp.name] = fn;
      continue;
    }
    fn = resolveImport(imp.intent, deps, callbackState);
    if (options?.domRoot) {
      if (imp.intent.type === "extern_class") {
        fn = wrapWithContainment(fn, imp.intent, options.domRoot);
      }
      if (imp.intent.type === "declared_global" && imp.intent.name === "document") {
        fn = () => options.domRoot;
      }
    }
    {
      const original = fn;
      fn = function (...args) {
        if (hostCallDepth >= MAX_HOST_RECURSION_DEPTH) {
          const err = new RangeError("Maximum call stack size exceeded");
          lastCaughtException = err;
          throw err;
        }
        hostCallDepth++;
        try {
          return original.apply(this, args);
        } catch (e) {
          lastCaughtException = e;
          throw e;
        } finally {
          hostCallDepth--;
        }
      };
    }
    env[imp.name] = fn;
    if (imp.intent.type === "callback_maker") hasCallbacks = true;
    if (imp.name === "__str_from_mem" || imp.name === "__str_to_mem") hasCallbacks = true;
  }
  const result = {
    env,
    "wasm:js-string": jsString,
    string_constants: buildStringConstants(stringPool),
  };
  result.setExports = (exports) => {
    wasmExports = exports;
  };
  result.setInstance = (instance) => {
    const exports = brandedInstanceExports(instance);
    if (exports === void 0) {
      throw new TypeError("setInstance: expected a genuine WebAssembly.Instance");
    }
    result.setExports(exports);
  };
  return result;
}

// src/index.ts
function compile(source, options) {
  return compileSource(source, options);
}

// tests/test262-runner.ts
var PROPOSAL_FEATURES = /* @__PURE__ */ new Map([
  ["Temporal", "proposal feature: Temporal"],
  ["import-defer", "proposal feature: import defer"],
  ["source-phase-imports", "proposal feature: source phase imports"],
  ["upsert", "proposal feature: Map/WeakMap upsert"],
]);
function getTest262RelativePath(filePath) {
  if (!filePath) return void 0;
  return filePath.replace(/.*test262\//, "");
}
function classifyTestScope(source, meta, filePath) {
  const relPath = getTest262RelativePath(filePath) ?? "";
  if (relPath.startsWith("test/staging/") || relPath.startsWith("staging/")) {
    return {
      scope: "proposal",
      official: false,
      reason: "test262 staging proposal",
    };
  }
  if (relPath.startsWith("test/annexB/") || relPath.startsWith("annexB/")) {
    return {
      scope: "annex_b",
      official: true,
      reason: "Annex B",
    };
  }
  if (relPath.includes("built-ins/Temporal/")) {
    return {
      scope: "proposal",
      official: false,
      reason: "proposal feature: Temporal",
    };
  }
  if (meta.features) {
    for (const feat of meta.features) {
      const reason = PROPOSAL_FEATURES.get(feat);
      if (reason) {
        return {
          scope: "proposal",
          official: false,
          reason,
        };
      }
    }
  }
  return {
    scope: "standard",
    official: true,
  };
}
function parseMeta(source) {
  const match = source.match(/\/\*---\s*([\s\S]*?)\s*---\*\//);
  if (!match) return {};
  const yaml = match[1];
  const meta = {};
  const descMatch = yaml.match(/^description:\s*(.+)$/m);
  if (descMatch) {
    const raw = descMatch[1].trim();
    if (raw === ">" || raw === "|") {
      const blockMatch = yaml.match(/^description:\s*[>|]\s*\n((?:[ \t]+.+\n?)+)/m);
      meta.description = blockMatch ? blockMatch[1].replace(/\n\s*/g, " ").trim() : "";
    } else {
      meta.description = raw;
    }
  }
  const infoMatch = yaml.match(/^info:\s*\|?\s*\n([\s\S]*?)(?=^\w|\Z)/m);
  if (infoMatch) meta.info = infoMatch[1].trim();
  const featMatch = yaml.match(/^features:\s*\[([^\]]*)\]/m);
  if (featMatch)
    meta.features = featMatch[1]
      .split(",")
      .map((s) => s.trim())
      .filter(Boolean);
  const flagMatch = yaml.match(/^flags:\s*\[([^\]]*)\]/m);
  if (flagMatch)
    meta.flags = flagMatch[1]
      .split(",")
      .map((s) => s.trim())
      .filter(Boolean);
  const inclMatch = yaml.match(/^includes:\s*\[([^\]]*)\]/m);
  if (inclMatch)
    meta.includes = inclMatch[1]
      .split(",")
      .map((s) => s.trim())
      .filter(Boolean);
  if (yaml.includes("negative:")) {
    const phaseMatch = yaml.match(/phase:\s*(\w+)/);
    const typeMatch = yaml.match(/type:\s*(\w+)/);
    if (phaseMatch && typeMatch) {
      meta.negative = { phase: phaseMatch[1], type: typeMatch[1] };
    }
  }
  return meta;
}
var HANGING_TESTS = /* @__PURE__ */ new Set([
  "test/built-ins/Promise/race/invoke-then.js",
  // #408: Promise.race compilation hang
  "test/built-ins/Map/prototype/forEach/iterates-values-deleted-then-readded.js",
  // hangs: Map mutation during iteration
  "test/built-ins/Temporal/Duration/from/argument-non-string.js",
  // hangs: Temporal runtime loop
]);
function shouldSkip(source, meta, filePath) {
  const scope = classifyTestScope(source, meta, filePath);
  if (filePath && /_FIXTURE\.js$/.test(filePath)) {
    return {
      skip: true,
      reason: "FIXTURE helper file (not a standalone test)",
    };
  }
  if (/_FIXTURE\.js/.test(source)) {
    const hasDynamicFixture = /import\s*\([^)]*_FIXTURE/.test(source);
    if (hasDynamicFixture) {
      return { skip: true, reason: "ES2020: dynamic import()" };
    }
  }
  if (meta.features?.includes("with") || /\bwith\s*\(/.test(source)) {
    return {
      skip: true,
      reason: "ES5 legacy: with statement (strict mode disallowed)",
    };
  }
  const SLOPPY_MODE_TESTS = /* @__PURE__ */ new Set([
    "test/annexB/language/function-code/function-redeclaration-block.js",
    "test/annexB/language/function-code/function-redeclaration-switch.js",
    "test/annexB/language/statements/for-in/const-initializer.js",
    "test/annexB/language/statements/for-in/strict-initializer.js",
    "test/annexB/language/statements/for-in/var-arraybindingpattern-initializer.js",
    "test/annexB/language/statements/for-in/let-initializer.js",
    "test/annexB/language/statements/for-in/var-objectbindingpattern-initializer.js",
    "test/annexB/language/statements/labeled/function-declaration.js",
  ]);
  if (filePath) {
    const rel = filePath.replace(/.*test262\//, "");
    if (SLOPPY_MODE_TESTS.has(rel)) {
      return { skip: true, reason: "ES5 sloppy mode: not supported in strict/module mode (#833)" };
    }
  }
  if (filePath && /legacy-octal-escape|legacy-non-octal-escape|S7\.8\.4_A4\.3/.test(filePath)) {
    return {
      skip: true,
      reason: "ES5 strict mode: octal escape sequences forbidden in modules (#833)",
    };
  }
  if (filePath && /unicode-16\.0\.0/.test(filePath)) {
    return {
      skip: true,
      reason: "TypeScript 5.x: Unicode 16.0.0 identifiers not supported (#832)",
    };
  }
  if ((filePath && /built-ins\/SharedArrayBuffer/.test(filePath)) || meta.features?.includes("SharedArrayBuffer")) {
    return {
      skip: true,
      reason: "ES2017: SharedArrayBuffer (requires shared Wasm memory) (#674)",
    };
  }
  if (filePath) {
    const relPath = filePath.replace(/.*test262\//, "");
    if (HANGING_TESTS.has(relPath)) {
      return { skip: true, reason: "compiler hang (see HANGING_TESTS)" };
    }
  }
  if (filePath && /BigInt64Array|BigUint64Array/.test(filePath)) {
    return { skip: true, reason: "ES2020: BigInt typed arrays not implemented (#838)" };
  }
  if (scope.scope === "proposal" && process.env.TEST262_INCLUDE_PROPOSALS !== "1") {
    return {
      skip: true,
      reason: `Proposal excluded from default scope${scope.reason ? `: ${scope.reason}` : ""}`,
    };
  }
  return { skip: false };
}
function stripThirdArg(code, fnName) {
  let result = "";
  let i = 0;
  while (i < code.length) {
    const idx = code.indexOf(fnName + "(", i);
    if (idx === -1) {
      result += code.slice(i);
      break;
    }
    result += code.slice(i, idx + fnName.length + 1);
    let pos = idx + fnName.length + 1;
    let depth = 1;
    let bracketDepth = 0;
    let braceDepth = 0;
    let commaCount = 0;
    let secondCommaPos = -1;
    let closeParenPos = -1;
    while (pos < code.length && depth > 0) {
      const ch = code[pos];
      if (ch === "(") depth++;
      else if (ch === ")") {
        depth--;
        if (depth === 0) {
          closeParenPos = pos;
          break;
        }
      } else if (ch === "[") bracketDepth++;
      else if (ch === "]") bracketDepth--;
      else if (ch === "{") braceDepth++;
      else if (ch === "}") braceDepth--;
      else if (ch === "," && depth === 1 && bracketDepth === 0 && braceDepth === 0) {
        commaCount++;
        if (commaCount === 2) secondCommaPos = pos;
      } else if (ch === "'" || ch === '"') {
        const quote = ch;
        pos++;
        while (pos < code.length && code[pos] !== quote) {
          if (code[pos] === "\\") pos++;
          pos++;
        }
      }
      pos++;
    }
    if (closeParenPos < 0) {
      result += code.slice(idx + fnName.length + 1);
      i = code.length;
    } else if (secondCommaPos >= 0) {
      result += code.slice(idx + fnName.length + 1, secondCommaPos);
      result += ")";
      i = closeParenPos + 1;
    } else {
      result += code.slice(idx + fnName.length + 1, closeParenPos + 1);
      i = closeParenPos + 1;
    }
  }
  return result;
}
function transformPrototypeCall(code, pattern) {
  const search = pattern + "(";
  let result = "";
  let i = 0;
  while (i < code.length) {
    const idx = code.indexOf(search, i);
    if (idx === -1) {
      result += code.slice(i);
      break;
    }
    result += code.slice(i, idx);
    let pos = idx + search.length;
    let depth = 1;
    const firstArgStart = pos;
    let commaPos = -1;
    while (pos < code.length && depth > 0) {
      const ch = code[pos];
      if (ch === "(") depth++;
      else if (ch === ")") {
        depth--;
        if (depth === 0) break;
      } else if (ch === "," && depth === 1 && commaPos === -1) {
        commaPos = pos;
        break;
      } else if (ch === "'" || ch === '"') {
        const quote = ch;
        pos++;
        while (pos < code.length && code[pos] !== quote) {
          if (code[pos] === "\\") pos++;
          pos++;
        }
      }
      pos++;
    }
    if (commaPos >= 0) {
      const firstArg = code.slice(firstArgStart, commaPos).trim();
      let afterComma = commaPos + 1;
      while (afterComma < code.length && code[afterComma] === " ") afterComma++;
      pos = afterComma;
      depth = 1;
      while (pos < code.length && depth > 0) {
        const ch = code[pos];
        if (ch === "(") depth++;
        else if (ch === ")") {
          depth--;
          if (depth === 0) break;
        } else if (ch === "'" || ch === '"') {
          const quote = ch;
          pos++;
          while (pos < code.length && code[pos] !== quote) {
            if (code[pos] === "\\") pos++;
            pos++;
          }
        }
        pos++;
      }
      const secondArg = code.slice(afterComma, pos).trim();
      result += `(${firstArg}).hasOwnProperty(${secondArg})`;
      i = pos + 1;
    } else {
      result += search;
      i = idx + search.length;
    }
  }
  return result;
}
function transformAssertThrows(code) {
  const pattern = "assert.throws(";
  let result = "";
  let i = 0;
  while (i < code.length) {
    const idx = code.indexOf(pattern, i);
    if (idx === -1) {
      result += code.slice(i);
      break;
    }
    result += code.slice(i, idx);
    let pos = idx + pattern.length;
    let parenDepth = 1;
    let braceDepth = 0;
    let bracketDepth = 0;
    const args = [];
    let currentArgStart = pos;
    while (pos < code.length && parenDepth > 0) {
      const ch = code[pos];
      if (ch === "(") parenDepth++;
      else if (ch === ")") parenDepth--;
      else if (ch === "{") braceDepth++;
      else if (ch === "}") braceDepth--;
      else if (ch === "[") bracketDepth++;
      else if (ch === "]") bracketDepth--;
      else if (ch === "," && parenDepth === 1 && braceDepth === 0 && bracketDepth === 0) {
        args.push(code.slice(currentArgStart, pos).trim());
        currentArgStart = pos + 1;
      } else if (ch === "'" || ch === '"' || ch === "`") {
        const quote = ch;
        pos++;
        while (pos < code.length && code[pos] !== quote) {
          if (code[pos] === "\\") pos++;
          pos++;
        }
      }
      if (parenDepth === 0) {
        args.push(code.slice(currentArgStart, pos).trim());
      }
      pos++;
    }
    let endPos = pos;
    while (
      endPos < code.length &&
      (code[endPos] === ";" || code[endPos] === " " || code[endPos] === "\n" || code[endPos] === "\r")
    )
      endPos++;
    if (args.length >= 2 && args[1]) {
      result += `assert_throws(${args[1]});`;
    }
    i = endPos;
  }
  return result;
}
function stripUndefinedThrowGuards(code) {
  const pattern = /if\s*\(/g;
  let result = "";
  let lastIdx = 0;
  let match;
  while ((match = pattern.exec(code)) !== null) {
    const ifStart = match.index;
    let pos = ifStart + match[0].length;
    let depth = 1;
    while (pos < code.length && depth > 0) {
      if (code[pos] === "(") depth++;
      else if (code[pos] === ")") depth--;
      pos++;
    }
    const condition = code.slice(ifStart + match[0].length, pos - 1);
    if (!/!==?\s*undefined\b/.test(condition) && !/undefined\s*!==?/.test(condition)) continue;
    let braceStart = pos;
    while (braceStart < code.length && /\s/.test(code[braceStart])) braceStart++;
    if (braceStart >= code.length || code[braceStart] !== "{") continue;
    let bracePos = braceStart + 1;
    let braceDepth = 1;
    while (bracePos < code.length && braceDepth > 0) {
      if (code[bracePos] === "{") braceDepth++;
      else if (code[bracePos] === "}") braceDepth--;
      bracePos++;
    }
    const body = code.slice(braceStart + 1, bracePos - 1);
    if (!/\bthrow\b/.test(body)) continue;
    let endPos = bracePos;
    let elseBody = "";
    const afterBrace = code.slice(bracePos).match(/^\s*else\s*\{/);
    if (afterBrace) {
      const elseStart = bracePos + afterBrace[0].length;
      let elseDepth = 1;
      let elseEnd = elseStart;
      while (elseEnd < code.length && elseDepth > 0) {
        if (code[elseEnd] === "{") elseDepth++;
        else if (code[elseEnd] === "}") elseDepth--;
        elseEnd++;
      }
      elseBody = code.slice(elseStart, elseEnd - 1);
      endPos = elseEnd;
    }
    let sideEffect = "";
    const callMatch = condition.match(/^(.+?)\s*!==?\s*undefined\s*$/) || condition.match(/^undefined\s*!==?\s*(.+)$/);
    if (callMatch && /\(/.test(callMatch[1])) {
      sideEffect = callMatch[1].trim() + ";\n";
    }
    result += code.slice(lastIdx, ifStart) + sideEffect + elseBody;
    lastIdx = endPos;
    pattern.lastIndex = endPos;
  }
  result += code.slice(lastIdx);
  return result;
}
function resolveUnicodeEscapes(source) {
  const parts = [];
  let i = 0;
  while (i < source.length) {
    if (source[i] === '"' || source[i] === "'" || source[i] === "`") {
      const quote = source[i];
      let j = i + 1;
      while (j < source.length) {
        if (source[j] === "\\") {
          j += 2;
          continue;
        }
        if (source[j] === quote) {
          j++;
          break;
        }
        j++;
      }
      parts.push(source.slice(i, j));
      i = j;
    } else {
      let j = i + 1;
      while (j < source.length && source[j] !== '"' && source[j] !== "'" && source[j] !== "`") {
        j++;
      }
      const segment = source
        .slice(i, j)
        .replace(/\\u([0-9a-fA-F]{4})/g, (_match, hex) => String.fromCharCode(parseInt(hex, 16)));
      parts.push(segment);
      i = j;
    }
  }
  return parts.join("");
}
function stripUndefinedAssert(code, fnName) {
  let result = "";
  let i = 0;
  while (i < code.length) {
    const idx = code.indexOf(fnName + "(", i);
    if (idx === -1) {
      result += code.slice(i);
      break;
    }
    if (idx > 0 && /\w/.test(code[idx - 1])) {
      result += code.slice(i, idx + fnName.length);
      i = idx + fnName.length;
      continue;
    }
    let pos = idx + fnName.length + 1;
    let depth = 1;
    let bracketDepth = 0;
    let braceDepth = 0;
    let commaCount = 0;
    let firstCommaPos = -1;
    let closeParenPos = -1;
    while (pos < code.length && depth > 0) {
      const ch = code[pos];
      if (ch === "(") depth++;
      else if (ch === ")") {
        depth--;
        if (depth === 0) {
          closeParenPos = pos;
          break;
        }
      } else if (ch === "[") bracketDepth++;
      else if (ch === "]") bracketDepth--;
      else if (ch === "{") braceDepth++;
      else if (ch === "}") braceDepth--;
      else if (ch === "," && depth === 1 && bracketDepth === 0 && braceDepth === 0) {
        commaCount++;
        if (commaCount === 1) firstCommaPos = pos;
      } else if (ch === "'" || ch === '"') {
        const quote = ch;
        pos++;
        while (pos < code.length && code[pos] !== quote) {
          if (code[pos] === "\\") pos++;
          pos++;
        }
      }
      pos++;
    }
    if (firstCommaPos >= 0 && closeParenPos >= 0) {
      let secondArgEnd = closeParenPos;
      let scanPos = firstCommaPos + 1;
      let scanDepth = 1;
      let scanBracketDepth = 0;
      let scanBraceDepth = 0;
      while (scanPos < closeParenPos) {
        const ch = code[scanPos];
        if (ch === "(") scanDepth++;
        else if (ch === ")") scanDepth--;
        else if (ch === "[") scanBracketDepth++;
        else if (ch === "]") scanBracketDepth--;
        else if (ch === "{") scanBraceDepth++;
        else if (ch === "}") scanBraceDepth--;
        else if (ch === "," && scanDepth === 1 && scanBracketDepth === 0 && scanBraceDepth === 0) {
          secondArgEnd = scanPos;
          break;
        } else if (ch === "'" || ch === '"') {
          const quote = ch;
          scanPos++;
          while (scanPos < code.length && code[scanPos] !== quote) {
            if (code[scanPos] === "\\") scanPos++;
            scanPos++;
          }
        }
        scanPos++;
      }
      const secondArg = code.slice(firstCommaPos + 1, secondArgEnd).trim();
      if (secondArg === "undefined" || /^void\s+0$/.test(secondArg)) {
        result += code.slice(i, idx);
        let endPos = closeParenPos + 1;
        while (endPos < code.length && (code[endPos] === ";" || code[endPos] === " ")) endPos++;
        result += "/* stripped undefined assert */";
        i = endPos;
        continue;
      }
    }
    result += code.slice(i, idx + fnName.length + 1);
    i = idx + fnName.length + 1;
  }
  return result;
}
function renameYieldOutsideGenerators(source) {
  if (!/\byield\b/.test(source)) return source;
  const hasGeneratorFunction = /\bfunction\s*\*/.test(source);
  const hasGeneratorMethod = /(?:^|[,{;)\s])\s*\*\s*(?:[\w$]+|\[[\s\S]*?\])\s*\(/.test(source);
  if (!hasGeneratorFunction && !hasGeneratorMethod) {
    return source.replace(/\byield\b/g, "_yield");
  }
  function skipString(src, i) {
    const quote = src[i];
    i++;
    while (i < src.length && src[i] !== quote) {
      if (src[i] === "\\") i++;
      i++;
    }
    return i + 1;
  }
  function findMatchingBrace(src, openIdx) {
    let depth = 1;
    let j = openIdx + 1;
    while (j < src.length && depth > 0) {
      if (src[j] === "{") depth++;
      else if (src[j] === "}") depth--;
      else if (src[j] === '"' || src[j] === "'" || src[j] === "`") {
        j = skipString(src, j);
        continue;
      }
      j++;
    }
    return j;
  }
  function skipParams(src, i) {
    let depth = 1;
    i++;
    while (i < src.length && depth > 0) {
      if (src[i] === "(") depth++;
      else if (src[i] === ")") depth--;
      else if (src[i] === '"' || src[i] === "'" || src[i] === "`") {
        i = skipString(src, i);
        continue;
      }
      i++;
    }
    return i;
  }
  function findFunctionExtent(src, startIdx) {
    let i = startIdx;
    while (i < src.length && /\s/.test(src[i])) i++;
    if (i < src.length && /[a-zA-Z_$]/.test(src[i])) {
      while (i < src.length && /[\w$]/.test(src[i])) i++;
    }
    while (i < src.length && /\s/.test(src[i])) i++;
    if (i >= src.length || src[i] !== "(") return null;
    const paramStart = i;
    i = skipParams(src, i);
    while (i < src.length && /\s/.test(src[i])) i++;
    if (i < src.length && src[i] === ":") {
      i++;
      while (i < src.length && src[i] !== "{") i++;
    }
    if (i >= src.length || src[i] !== "{") return null;
    const bodyEnd = findMatchingBrace(src, i);
    return { paramStart, bodyEnd };
  }
  const allFuncs = [];
  const funcRegex = /\bfunction\s*(\*?)/g;
  let match;
  while ((match = funcRegex.exec(source)) !== null) {
    const isGen = match[1] === "*";
    const afterKeyword = match.index + match[0].length;
    if (!isGen && afterKeyword < source.length && /[\w$]/.test(source[afterKeyword])) continue;
    const extent = findFunctionExtent(source, afterKeyword);
    if (!extent) continue;
    allFuncs.push({
      start: extent.paramStart,
      end: extent.bodyEnd,
      isGenerator: isGen,
      children: [],
    });
  }
  const methodRegex = /\*\s*(?:[\w$]+|\[[\s\S]*?\])\s*\(/g;
  let methodMatch;
  while ((methodMatch = methodRegex.exec(source)) !== null) {
    const before = source.substring(Math.max(0, methodMatch.index - 20), methodMatch.index).trimEnd();
    if (
      !(
        before.endsWith(",") ||
        before.endsWith("{") ||
        before.endsWith(";") ||
        before.endsWith(")") ||
        before.endsWith("async") ||
        before.length === 0
      )
    ) {
      continue;
    }
    const parenStart = methodMatch.index + methodMatch[0].length - 1;
    let j = skipParams(source, parenStart);
    while (j < source.length && /\s/.test(source[j])) j++;
    if (j >= source.length || source[j] !== "{") continue;
    const bodyEnd = findMatchingBrace(source, j);
    allFuncs.push({
      start: parenStart,
      end: bodyEnd,
      isGenerator: true,
      children: [],
    });
  }
  const arrowRegex = /=>\s*\{/g;
  let arrowMatch;
  while ((arrowMatch = arrowRegex.exec(source)) !== null) {
    const braceIdx = arrowMatch.index + arrowMatch[0].length - 1;
    const bodyEnd = findMatchingBrace(source, braceIdx);
    allFuncs.push({
      start: braceIdx,
      end: bodyEnd,
      isGenerator: false,
      children: [],
    });
  }
  allFuncs.sort((a, b) => a.start - b.start);
  for (const r of allFuncs) {
    let parent = null;
    for (const candidate of allFuncs) {
      if (candidate === r) continue;
      if (candidate.start < r.start && candidate.end > r.end) {
        if (!parent || candidate.start > parent.start) {
          parent = candidate;
        }
      }
    }
    if (parent) {
      parent.children.push(r);
    }
  }
  const roots = allFuncs.filter((r) => !allFuncs.some((c) => c !== r && c.start < r.start && c.end > r.end));
  function findInnermostFunc(pos, ranges) {
    for (const r of ranges) {
      if (pos >= r.start && pos < r.end) {
        const child = findInnermostFunc(pos, r.children);
        return child || r;
      }
    }
    return null;
  }
  const yieldRegex = /\byield\b/g;
  let result = "";
  let lastIndex = 0;
  let yieldMatch;
  while ((yieldMatch = yieldRegex.exec(source)) !== null) {
    const pos = yieldMatch.index;
    const innermost = findInnermostFunc(pos, roots);
    const isKeyword = innermost !== null && innermost.isGenerator;
    result += source.slice(lastIndex, pos);
    result += isKeyword ? "yield" : "_yield";
    lastIndex = pos + "yield".length;
  }
  result += source.slice(lastIndex);
  return result;
}
function stripBalancedCall(source, name) {
  const pattern = new RegExp(`\\b${name}\\s*\\(`, "g");
  let result = source;
  let match;
  const matches = [];
  while ((match = pattern.exec(result)) !== null) {
    const callStart = match.index;
    let depth = 0;
    let i = match.index + match[0].length - 1;
    for (; i < result.length; i++) {
      if (result[i] === "(") depth++;
      else if (result[i] === ")") {
        depth--;
        if (depth === 0) break;
      }
    }
    if (depth !== 0) continue;
    let end = i + 1;
    while (end < result.length && (result[end] === ";" || result[end] === " " || result[end] === "	")) end++;
    if (end < result.length && result[end] === "\n") end++;
    matches.push({ start: callStart, end });
  }
  for (let j = matches.length - 1; j >= 0; j--) {
    const m = matches[j];
    result = result.slice(0, m.start) + result.slice(m.end);
  }
  return result;
}
function transformVerifyPropertyCalls(source) {
  const pattern = /\bverifyProperty\s*\(/g;
  let result = source;
  const replacements = [];
  let match;
  while ((match = pattern.exec(result)) !== null) {
    const callStart = match.index;
    const argsStart = match.index + match[0].length;
    let depth = 1;
    let i = argsStart;
    for (; i < result.length; i++) {
      if (result[i] === "(") depth++;
      else if (result[i] === ")") {
        depth--;
        if (depth === 0) break;
      }
    }
    if (depth !== 0) continue;
    const argsStr = result.slice(argsStart, i);
    let end = i + 1;
    while (end < result.length && (result[end] === ";" || result[end] === " " || result[end] === "	")) end++;
    if (end < result.length && result[end] === "\n") end++;
    const topLevelCommas = findTopLevelCommas(argsStr);
    if (topLevelCommas.length < 2) {
      replacements.push({ start: callStart, end, replacement: "" });
      continue;
    }
    const objExpr = argsStr.slice(0, topLevelCommas[0]).trim();
    const nameExpr = argsStr.slice(topLevelCommas[0] + 1, topLevelCommas[1]).trim();
    const descPart =
      topLevelCommas.length > 2
        ? argsStr.slice(topLevelCommas[1] + 1, topLevelCommas[2]).trim()
        : argsStr.slice(topLevelCommas[1] + 1).trim();
    const valueExpr = extractDescriptorValue(descPart);
    if (valueExpr !== null) {
      let accessExpr;
      if (/^"[^"]*"$/.test(nameExpr) || /^'[^']*'$/.test(nameExpr)) {
        const key = nameExpr.slice(1, -1);
        if (/^\d+$/.test(key) || /[^a-zA-Z0-9_$]/.test(key)) {
          accessExpr = `${objExpr}[${nameExpr}]`;
        } else {
          accessExpr = `${objExpr}.${key}`;
        }
      } else {
        accessExpr = `${objExpr}[${nameExpr}]`;
      }
      const replacement = `assert_sameValue(${accessExpr}, ${valueExpr});
`;
      replacements.push({ start: callStart, end, replacement });
    } else {
      replacements.push({ start: callStart, end, replacement: "" });
    }
  }
  for (let j = replacements.length - 1; j >= 0; j--) {
    const r = replacements[j];
    result = result.slice(0, r.start) + r.replacement + result.slice(r.end);
  }
  return result;
}
function findTopLevelCommas(s) {
  const commas = [];
  let depth = 0;
  let inString = null;
  for (let i = 0; i < s.length; i++) {
    const ch = s[i];
    if (inString) {
      if (ch === "\\" && i + 1 < s.length) {
        i++;
        continue;
      }
      if (ch === inString) inString = null;
      continue;
    }
    if (ch === '"' || ch === "'" || ch === "`") {
      inString = ch;
      continue;
    }
    if (ch === "(" || ch === "{" || ch === "[") {
      depth++;
      continue;
    }
    if (ch === ")" || ch === "}" || ch === "]") {
      depth--;
      continue;
    }
    if (ch === "," && depth === 0) commas.push(i);
  }
  return commas;
}
function extractDescriptorValue(descStr) {
  const valueMatch = descStr.match(/\bvalue\s*:\s*/);
  if (!valueMatch) return null;
  const exprStart = valueMatch.index + valueMatch[0].length;
  let depth = 0;
  let inString = null;
  let i = exprStart;
  for (; i < descStr.length; i++) {
    const ch = descStr[i];
    if (inString) {
      if (ch === "\\" && i + 1 < descStr.length) {
        i++;
        continue;
      }
      if (ch === inString) inString = null;
      continue;
    }
    if (ch === '"' || ch === "'" || ch === "`") {
      inString = ch;
      continue;
    }
    if (ch === "(" || ch === "{" || ch === "[") {
      depth++;
      continue;
    }
    if (ch === ")" || ch === "]") {
      depth--;
      continue;
    }
    if (ch === "}") {
      if (depth === 0) break;
      depth--;
      continue;
    }
    if (ch === "," && depth === 0) break;
  }
  const expr = descStr.slice(exprStart, i).trim();
  return expr.length > 0 ? expr : null;
}
var preambleCache = /* @__PURE__ */ new Map();
function buildPreamble(
  needsAssertThrows,
  needsStrAssert,
  needsBoolAssert,
  needsCompareArray,
  needsAssertCompareArray,
  needsPropertyHelper,
  needsFnGlobalObject,
  needsIsConstructor,
  needsDecimalToHex,
  needsNans,
  needsIsNativeFunction,
  needsAssertNativeFunction,
  needsTcoHelper,
  needsDone,
  needsAsyncTest,
  needsDoneForAsyncTest,
  needsTestTypedArray,
) {
  let p = `let __fail: number = 0;
let __assert_count: number = 1;

class Test262Error {
  message: string;
  constructor(msg: string) {
    this.message = msg;
  }
}

function isSameValue(a: number, b: number): number {
  if (a === b) { return 1; }
  if (a !== a && b !== b) { return 1; }
  return 0;
}

function assert_sameValue(actual: number, expected: number): void {
  __assert_count = __assert_count + 1;
  if (!isSameValue(actual, expected)) {
    if (!__fail) __fail = __assert_count;
  }
}

function assert_notSameValue(actual: number, expected: number): void {
  __assert_count = __assert_count + 1;
  if (isSameValue(actual, expected)) {
    if (!__fail) __fail = __assert_count;
  }
}

function assert_true(value: number): void {
  __assert_count = __assert_count + 1;
  if (!value) {
    if (!__fail) __fail = __assert_count;
  }
}`;
  if (needsAssertThrows) {
    p += `

function assert_throws(fn: () => void): void {
  __assert_count = __assert_count + 1;
  try {
    fn();
  } catch (e) {
    return;
  }
  if (!__fail) __fail = __assert_count;
}`;
  }
  if (needsStrAssert) {
    p += `

function assert_sameValue_str(actual: string, expected: string): void {
  __assert_count = __assert_count + 1;
  if (actual !== expected) {
    if (!__fail) __fail = __assert_count;
  }
}

function assert_notSameValue_str(actual: string, expected: string): void {
  __assert_count = __assert_count + 1;
  if (actual === expected) {
    if (!__fail) __fail = __assert_count;
  }
}`;
  }
  if (needsBoolAssert) {
    p += `

function assert_sameValue_bool(actual: boolean, expected: boolean): void {
  __assert_count = __assert_count + 1;
  if (actual !== expected) {
    if (!__fail) __fail = __assert_count;
  }
}

function assert_notSameValue_bool(actual: boolean, expected: boolean): void {
  __assert_count = __assert_count + 1;
  if (actual === expected) {
    if (!__fail) __fail = __assert_count;
  }
}`;
  }
  if (needsCompareArray) {
    p += `

function compareArray(a: number[], b: number[]): number {
  if (a.length !== b.length) return 0;
  for (let i: number = 0; i < a.length; i++) {
    if (a[i] !== b[i]) return 0;
  }
  return 1;
}`;
  }
  if (needsAssertCompareArray) {
    p += `

function assert_compareArray(actual: number[], expected: number[]): void {
  __assert_count = __assert_count + 1;
  if (actual.length !== expected.length) { if (!__fail) __fail = __assert_count; return; }
  for (let i: number = 0; i < actual.length; i++) {
    if (actual[i] !== expected[i]) { if (!__fail) __fail = __assert_count; return; }
  }
}`;
  }
  if (needsPropertyHelper) {
    p += `
function verifyEnumerable(obj: any, name: any): void {}
function verifyNotEnumerable(obj: any, name: any): void {}
function verifyWritable(obj: any, name: any, val?: any): void {}
function verifyNotWritable(obj: any, name: any, val?: any): void {}
function verifyConfigurable(obj: any, name: any): void {}
function verifyNotConfigurable(obj: any, name: any): void {}
function verifyEqualTo(obj: any, name: any, val: any): void {
  assert_sameValue(obj[name], val);
}
function verifyNotEqualTo(obj: any, name: any, val?: any): void {}
function verifyCallableProperty(a: any, b: any, c?: any, d?: any, e?: any, f?: any): void {}
function verifyPrimordialProperty(a: any, b: any, c?: any, d?: any): void {}
function verifyPrimordialCallableProperty(a: any, b: any, c?: any, d?: any, e?: any, f?: any): void {}`;
  }
  if (needsFnGlobalObject) {
    p += `

function fnGlobalObject(): number { return 0; }`;
  }
  if (needsIsConstructor) {
    p += `

function isConstructor(f: number): number { return 0; }`;
  }
  if (needsDecimalToHex) {
    p += `

function decimalToHexString(n: number): string { return "0"; }`;
  }
  if (needsNans) {
    p += `

let distinctNaNs: number[] = [NaN];`;
  }
  if (needsIsNativeFunction) {
    p += `

function isNativeFunction(f: number): number { return 1; }`;
  }
  if (needsAssertNativeFunction) {
    p += `

function assertNativeFunction(f: number): void {}`;
  }
  if (needsTcoHelper) {
    p += `

let $MAX_ITERATIONS: number = 100000;`;
  }
  if (needsDone) {
    p += `

function $DONE(err?: any): void {
  __assert_count = __assert_count + 1;
  if (err) { if (!__fail) __fail = __assert_count; }
}`;
  }
  if (needsAsyncTest) {
    p += `

function asyncTest(fn: () => void): void {
  try {
    fn();
    $DONE();
  } catch (e) {
    $DONE(e);
  }
}`;
    if (needsDoneForAsyncTest) {
      p += `

function $DONE(err?: any): void {
  __assert_count = __assert_count + 1;
  if (err) { if (!__fail) __fail = __assert_count; }
}`;
    }
  }
  if (needsTestTypedArray) {
    p += `

function testWithTypedArrayConstructors(fn: any): void {
  const constructors = [Int8Array, Uint8Array, Int16Array, Uint16Array, Int32Array, Uint32Array, Float32Array, Float64Array];
  for (let i = 0; i < constructors.length; i++) {
    fn(constructors[i]);
  }
}`;
  }
  return p;
}
function wrapTest(source, meta) {
  let body = source.replace(/\/\*---[\s\S]*?---\*\//, "");
  body = resolveUnicodeEscapes(body);
  body = renameYieldOutsideGenerators(body);
  body = body.replace(/\bswitch\s*\(\s*(-?\d+(?:\.\d+)?)\s*\)/g, "switch ($1 as number)");
  body = body.replace(/\bswitch\s*\(\s*(null)\s*\)/g, "switch ($1 as any)");
  body = transformPrototypeCall(body, "Object.prototype.hasOwnProperty.call");
  body = transformPrototypeCall(body, "Object.prototype.propertyIsEnumerable.call");
  body = body.replace(/\.propertyIsEnumerable\s*\(/g, ".hasOwnProperty(");
  body = transformAssertThrows(body);
  body = body.replace(/\bassert\.throwsAsync\s*\(/g, "assert.throws(");
  body = transformAssertThrows(body);
  body = stripUndefinedAssert(body, "assert.sameValue");
  body = stripUndefinedAssert(body, "assert.notSameValue");
  body = body.replace(/\bvar\s+(\w+)\s*=\s*undefined\s*;/g, "var $1;");
  body = stripUndefinedThrowGuards(body);
  body = body.replace(/\bassert\.sameValue\b/g, "assert_sameValue");
  body = body.replace(/\bassert\.notSameValue\b/g, "assert_notSameValue");
  body = body.replace(/\bassert\.compareArray\b/g, "assert_compareArray");
  body = body.replace(/\bassert\s*\(/g, "assert_true(");
  body = stripThirdArg(body, "assert_sameValue");
  body = stripThirdArg(body, "assert_notSameValue");
  body = stripThirdArg(body, "assert_compareArray");
  body = body.replace(
    /assert_sameValue\s*\(\s*typeof\s+([^,]+?)\s*,\s*"([^"]+)"\s*\)\s*;?/g,
    '{ __assert_count = __assert_count + 1; if (typeof $1 !== "$2") { if (!__fail) __fail = __assert_count; } }',
  );
  body = body.replace(
    /assert_sameValue\s*\(\s*typeof\s+([^,]+?)\s*,\s*'([^']+)'\s*\)\s*;?/g,
    '{ __assert_count = __assert_count + 1; if (typeof $1 !== "$2") { if (!__fail) __fail = __assert_count; } }',
  );
  body = body.replace(
    /assert_notSameValue\s*\(\s*typeof\s+([^,]+?)\s*,\s*"([^"]+)"\s*\)\s*;?/g,
    '{ __assert_count = __assert_count + 1; if (typeof $1 === "$2") { if (!__fail) __fail = __assert_count; } }',
  );
  body = body.replace(
    /assert_notSameValue\s*\(\s*typeof\s+([^,]+?)\s*,\s*'([^']+)'\s*\)\s*;?/g,
    '{ __assert_count = __assert_count + 1; if (typeof $1 === "$2") { if (!__fail) __fail = __assert_count; } }',
  );
  body = body.replace(
    /assert_sameValue\s*\(\s*"([^"]+)"\s*,\s*typeof\s+([^)]+?)\s*\)\s*;?/g,
    '{ __assert_count = __assert_count + 1; if (typeof $2 !== "$1") { if (!__fail) __fail = __assert_count; } }',
  );
  body = body.replace(
    /assert_sameValue\s*\(\s*'([^']+)'\s*,\s*typeof\s+([^)]+?)\s*\)\s*;?/g,
    '{ __assert_count = __assert_count + 1; if (typeof $2 !== "$1") { if (!__fail) __fail = __assert_count; } }',
  );
  const simpleExprPat = "[\\w.]+(?:\\[[^\\]]*\\])*(?:\\.\\w+(?:\\[[^\\]]*\\])*)*(?:\\([^)]*\\))?";
  body = body.replace(
    new RegExp(`assert_sameValue\\s*\\(\\s*(${simpleExprPat})\\s*,\\s*("[^"]*")\\s*\\)`, "g"),
    "assert_sameValue_str($1, $2)",
  );
  body = body.replace(
    new RegExp(`assert_sameValue\\s*\\(\\s*("[^"]*")\\s*,\\s*(${simpleExprPat})\\s*\\)`, "g"),
    "assert_sameValue_str($1, $2)",
  );
  body = body.replace(
    new RegExp(`assert_sameValue\\s*\\(\\s*(${simpleExprPat})\\s*,\\s*('[^']*')\\s*\\)`, "g"),
    "assert_sameValue_str($1, $2)",
  );
  body = body.replace(
    new RegExp(`assert_sameValue\\s*\\(\\s*('[^']*')\\s*,\\s*(${simpleExprPat})\\s*\\)`, "g"),
    "assert_sameValue_str($1, $2)",
  );
  body = body.replace(
    new RegExp(`assert_notSameValue\\s*\\(\\s*(${simpleExprPat})\\s*,\\s*("[^"]*")\\s*\\)`, "g"),
    "assert_notSameValue_str($1, $2)",
  );
  body = body.replace(
    new RegExp(`assert_notSameValue\\s*\\(\\s*("[^"]*")\\s*,\\s*(${simpleExprPat})\\s*\\)`, "g"),
    "assert_notSameValue_str($1, $2)",
  );
  body = body.replace(
    new RegExp(`assert_notSameValue\\s*\\(\\s*(${simpleExprPat})\\s*,\\s*('[^']*')\\s*\\)`, "g"),
    "assert_notSameValue_str($1, $2)",
  );
  body = body.replace(
    new RegExp(`assert_notSameValue\\s*\\(\\s*('[^']*')\\s*,\\s*(${simpleExprPat})\\s*\\)`, "g"),
    "assert_notSameValue_str($1, $2)",
  );
  body = body.replace(
    /\bassert_sameValue\s*\(\s*result\s*,\s*vals\s*\)\s*;?/g,
    "/* stripped object identity assert */",
  );
  body = body.replace(/__expected\.index\s*=\s*(\d+)\s*;/g, "var __expected_index: number = $1;");
  body = body.replace(/__expected\.input\s*=\s*("(?:[^"\\]|\\.)*")\s*;/g, "var __expected_input: string = $1;");
  body = body.replace(/__expected\.index\b(?!\s*=)/g, "__expected_index");
  body = body.replace(/__expected\.input\b(?!\s*=)/g, "__expected_input");
  body = body.replace(
    /assert_sameValue\s*\(\s*(\w+(?:_input|\.input))\s*,\s*(\w+(?:_input|\.input))\s*\)/g,
    "assert_sameValue_str($1, $2)",
  );
  body = body.replace(/assert_sameValue\s*\(\s*(\w+\[\w+\])\s*,\s*(\w+\[\w+\])\s*\)/g, "assert_sameValue_str($1, $2)");
  body = body.replace(/assert_sameValue\s*\(\s*([^,]+?)\s*,\s*(true|false)\s*\)/g, "assert_sameValue_bool($1, $2)");
  body = body.replace(/assert_sameValue\s*\(\s*(true|false)\s*,\s*([^)]+?)\s*\)/g, "assert_sameValue_bool($1, $2)");
  body = body.replace(
    /assert_notSameValue\s*\(\s*([^,]+?)\s*,\s*(true|false)\s*\)/g,
    "assert_notSameValue_bool($1, $2)",
  );
  body = body.replace(
    /assert_notSameValue\s*\(\s*(true|false)\s*,\s*([^)]+?)\s*\)/g,
    "assert_notSameValue_bool($1, $2)",
  );
  body = body.replace(/\bassert_true\s*\(\s*compareArray\b/g, "assert_true(compareArray");
  const needsStrAssert = /\bassert_(sameValue|notSameValue)_str\b/.test(body);
  const needsBoolAssert = /\bassert_(sameValue|notSameValue)_bool\b/.test(body);
  const needsCompareArray = /\bcompareArray\b/.test(body);
  const needsAssertCompareArray = /\bassert_compareArray\b/.test(body);
  const needsAssertThrows = /\bassert_throws\b/.test(body);
  const resolvedMeta = meta ?? parseMeta(source);
  const includes = resolvedMeta.includes ?? [];
  if (includes.includes("propertyHelper.js")) {
    if (/\bverifyProperty\b/.test(body)) {
      body = transformVerifyPropertyCalls(body);
    }
    for (const fn of ["verifyCallableProperty", "verifyPrimordialProperty", "verifyPrimordialCallableProperty"]) {
      if (new RegExp(`\\b${fn}\\b`).test(body)) {
        body = stripBalancedCall(body, fn);
      }
    }
  }
  const needsPropertyHelper = includes.includes("propertyHelper.js");
  const needsFnGlobalObject = includes.includes("fnGlobalObject.js") && /\bfnGlobalObject\b/.test(body);
  const needsIsConstructor = includes.includes("isConstructor.js") && /\bisConstructor\b/.test(body);
  const needsDecimalToHex = includes.includes("decimalToHexString.js") && /\bdecimalToHexString\b/.test(body);
  const needsNans = includes.includes("nans.js") && /\bdistinctNaNs\b/.test(body);
  const needsIsNativeFunction = includes.includes("nativeFunctionMatcher.js") && /\bisNativeFunction\b/.test(body);
  const needsAssertNativeFunction =
    includes.includes("nativeFunctionMatcher.js") && /\bassertNativeFunction\b/.test(body);
  const needsTcoHelper = includes.includes("tcoHelper.js") && /\$MAX_ITERATIONS\b/.test(body);
  const needsDone = /\$DONE\b/.test(body);
  const needsAsyncTest = includes.includes("asyncHelpers.js") && /\basyncTest\b/.test(body);
  const needsDoneForAsyncTest = needsAsyncTest && !needsDone;
  const needsTestTypedArray = includes.includes("testTypedArray.js") && /testWithTypedArrayConstructors/.test(body);
  const cacheKey = [
    needsAssertThrows,
    needsStrAssert,
    needsBoolAssert,
    needsCompareArray,
    needsAssertCompareArray,
    needsPropertyHelper,
    needsFnGlobalObject,
    needsIsConstructor,
    needsDecimalToHex,
    needsNans,
    needsIsNativeFunction,
    needsAssertNativeFunction,
    needsTcoHelper,
    needsDone,
    needsAsyncTest,
    needsDoneForAsyncTest,
    needsTestTypedArray,
  ]
    .map((b) => (b ? "1" : "0"))
    .join("");
  let preamble = preambleCache.get(cacheKey);
  if (preamble === void 0) {
    preamble = buildPreamble(
      needsAssertThrows,
      needsStrAssert,
      needsBoolAssert,
      needsCompareArray,
      needsAssertCompareArray,
      needsPropertyHelper,
      needsFnGlobalObject,
      needsIsConstructor,
      needsDecimalToHex,
      needsNans,
      needsIsNativeFunction,
      needsAssertNativeFunction,
      needsTcoHelper,
      needsDone,
      needsAsyncTest,
      needsDoneForAsyncTest,
      needsTestTypedArray,
    );
    preambleCache.set(cacheKey, preamble);
  }
  const implicitVars = /* @__PURE__ */ new Set();
  const declaredVars = /* @__PURE__ */ new Set();
  for (const m of body.matchAll(/\b(?:var|let|const)\s+([a-zA-Z_$][\w$]*)/g)) {
    declaredVars.add(m[1]);
  }
  for (const m of body.matchAll(/\{\s*(?:[\w\\u]+\s*:\s*(\w+)\s*,?\s*)+\}\s*=/g)) {
    for (const inner of m[0].matchAll(/[\w\\u]+\s*:\s*(\w+)/g)) {
      const v = inner[1];
      if (!declaredVars.has(v) && v !== "__fail") {
        implicitVars.add(v);
      }
    }
  }
  let implicitDecls = "";
  if (implicitVars.size > 0) {
    implicitDecls = [...implicitVars].map((v) => `var ${v}: number;`).join("\n  ");
    implicitDecls = "\n  " + implicitDecls;
  }
  const hoistedVars = /* @__PURE__ */ new Set();
  const varDeclPattern = /\bvar\s+(\w+)\s*=\s*(\d+)\s*;/g;
  const classBodyPattern = /\bclass\s+\w*\s*(?:extends\s+\w+\s*)?\{([\s\S]*?)\n\}/g;
  const classBodies = [];
  for (const cm of body.matchAll(classBodyPattern)) {
    classBodies.push(cm[1]);
  }
  if (classBodies.length > 0) {
    const classBodyText = classBodies.join("\n");
    for (const vm of body.matchAll(varDeclPattern)) {
      const varName = vm[1];
      if (new RegExp(`\\b${varName}\\b`).test(classBodyText)) {
        hoistedVars.add(varName);
      }
    }
  }
  let hoistedDecls = "";
  let bodyForFunc = body;
  if (hoistedVars.size > 0) {
    for (const v of hoistedVars) {
      const initMatch = bodyForFunc.match(new RegExp(`\\bvar\\s+${v}\\s*=\\s*(\\d+)\\s*;`));
      const initVal = initMatch ? initMatch[1] : "0";
      hoistedDecls += `let ${v}: number = ${initVal};
`;
      bodyForFunc = bodyForFunc.replace(new RegExp(`\\bvar\\s+${v}\\s*=\\s*\\d+\\s*;`), ``);
    }
  }
  const strictDirective = resolvedMeta.flags?.includes("onlyStrict") ? '"use strict";\n' : "";
  const preBody = `${strictDirective}
${preamble}
${hoistedDecls}
export function test(): number {
  ${implicitDecls}
  try {
    `;
  const asyncResultExport = needsDone
    ? `
export function __asyncResult(): number {
  if (__fail) { return __fail; }
  return 1;
}
`
    : "";
  const postBody = `
  } catch (e) {
    if (!__fail) __fail = -1;
    throw e;
  }
  if (__fail) { return __fail; }
  return 1;
}
${asyncResultExport}`;
  const bodyLineOffset = preBody.split("\n").length - 1;
  const metaBlock = source.match(/\/\*---[\s\S]*?---\*\//);
  const metaLines = metaBlock ? metaBlock[0].split("\n").length - 1 : 0;
  return {
    source: preBody + bodyForFunc.trim() + postBody,
    bodyLineOffset: bodyLineOffset - metaLines,
  };
}
var TEST_CATEGORIES = [
  // ── language ──
  "language/arguments-object",
  "language/asi",
  "language/block-scope",
  "language/comments",
  "language/computed-property-names",
  "language/destructuring",
  "language/directive-prologue",
  "language/eval-code",
  "language/export",
  "language/expressions",
  "language/function-code",
  "language/future-reserved-words",
  "language/global-code",
  "language/identifier-resolution",
  "language/identifiers",
  "language/import",
  "language/keywords",
  "language/line-terminators",
  "language/literals",
  "language/module-code",
  "language/punctuators",
  "language/reserved-words",
  "language/rest-parameters",
  "language/source-text",
  "language/statementList",
  "language/statements",
  "language/types",
  "language/white-space",
  // ── built-ins (consolidated — each entry covers all subdirectories) ──
  "built-ins/AbstractModuleSource",
  "built-ins/AggregateError",
  "built-ins/Array",
  "built-ins/ArrayBuffer",
  "built-ins/ArrayIteratorPrototype",
  "built-ins/AsyncDisposableStack",
  "built-ins/AsyncFromSyncIteratorPrototype",
  "built-ins/AsyncFunction",
  "built-ins/AsyncGeneratorFunction",
  "built-ins/AsyncGeneratorPrototype",
  "built-ins/AsyncIteratorPrototype",
  "built-ins/Atomics",
  "built-ins/BigInt",
  "built-ins/Boolean",
  "built-ins/DataView",
  "built-ins/Date",
  "built-ins/DisposableStack",
  "built-ins/Error",
  "built-ins/FinalizationRegistry",
  "built-ins/Function",
  "built-ins/GeneratorFunction",
  "built-ins/GeneratorPrototype",
  "built-ins/Infinity",
  "built-ins/Iterator",
  "built-ins/JSON",
  "built-ins/Map",
  "built-ins/MapIteratorPrototype",
  "built-ins/Math",
  "built-ins/NaN",
  "built-ins/NativeErrors",
  "built-ins/Number",
  "built-ins/Object",
  "built-ins/Promise",
  "built-ins/Proxy",
  "built-ins/Reflect",
  "built-ins/RegExp",
  "built-ins/RegExpStringIteratorPrototype",
  "built-ins/Set",
  "built-ins/SetIteratorPrototype",
  "built-ins/ShadowRealm",
  "built-ins/SharedArrayBuffer",
  "built-ins/String",
  "built-ins/StringIteratorPrototype",
  "built-ins/SuppressedError",
  "built-ins/Symbol",
  "built-ins/Temporal",
  "built-ins/ThrowTypeError",
  "built-ins/TypedArray",
  "built-ins/TypedArrayConstructors",
  "built-ins/Uint8Array",
  "built-ins/WeakMap",
  "built-ins/WeakRef",
  "built-ins/WeakSet",
  "built-ins/decodeURI",
  "built-ins/decodeURIComponent",
  "built-ins/encodeURI",
  "built-ins/encodeURIComponent",
  "built-ins/eval",
  "built-ins/global",
  "built-ins/isFinite",
  "built-ins/isNaN",
  "built-ins/parseFloat",
  "built-ins/parseInt",
  "built-ins/undefined",
  // ── annexB (legacy browser behaviors, standard test262 format) ──
  "annexB/built-ins",
  "annexB/language",
];
var TEST262_ROOT = join2(import.meta.dirname ?? ".", "..", "test262");
function findTestFiles(category) {
  const dir = join2(TEST262_ROOT, "test", category);
  if (!existsSync(dir)) return [];
  const files = [];
  function walk(d) {
    for (const entry of readdirSync(d, { withFileTypes: true })) {
      const full = join2(d, entry.name);
      if (entry.isDirectory()) walk(full);
      else if (entry.name.endsWith(".js") && !entry.name.includes("_FIXTURE") && !entry.name.endsWith(".imports.js"))
        files.push(full);
    }
  }
  walk(dir);
  return files.sort();
}
var TEST_TIMEOUT_MS = 8e3;
async function handleNegativeTest(source, meta, relPath, category) {
  if (!meta.negative) return null;
  const { phase, type } = meta.negative;
  const totalStart = performance.now();
  if (phase === "parse" || phase === "early" || phase === "resolution") {
    const strippedSource = source.replace(/\/\*---[\s\S]*?---\*\//, "");
    const strictPrefix = meta.flags?.includes("onlyStrict") ? '"use strict";\n' : "";
    const minimalWrapped = strictPrefix + strippedSource + "\nexport {};\n";
    let compileMs = 0;
    const compileStart = performance.now();
    try {
      const result = await compile(minimalWrapped, {
        fileName: "test.ts",
        emitWat: false,
      });
      compileMs = performance.now() - compileStart;
      const totalMs = performance.now() - totalStart;
      const timing = {
        totalMs: round2(totalMs),
        compileMs: round2(compileMs),
        instantiateMs: 0,
        executeMs: 0,
      };
      if (!result.success || result.errors.some((e) => e.severity === "error")) {
        return { file: relPath, category, status: "pass", timing };
      }
      if (result.errors.some((e) => e.severity === "warning")) {
        return { file: relPath, category, status: "pass", timing };
      }
      try {
        const imports = buildImports(result.imports, void 0, result.stringPool);
        await WebAssembly.instantiate(result.binary, imports);
      } catch {
        const totalMs2 = performance.now() - totalStart;
        return {
          file: relPath,
          category,
          status: "pass",
          timing: {
            totalMs: round2(totalMs2),
            compileMs: round2(compileMs),
            instantiateMs: 0,
            executeMs: 0,
          },
        };
      }
      return {
        file: relPath,
        category,
        status: "fail",
        error: `expected ${phase} ${type} but compilation succeeded`,
        timing,
      };
    } catch {
      compileMs = performance.now() - compileStart;
      const totalMs = performance.now() - totalStart;
      return {
        file: relPath,
        category,
        status: "pass",
        timing: {
          totalMs: round2(totalMs),
          compileMs: round2(compileMs),
          instantiateMs: 0,
          executeMs: 0,
        },
      };
    }
  }
  if (phase === "runtime") {
    return null;
  }
  return {
    file: relPath,
    category,
    status: "skip",
    reason: `unknown negative phase: ${phase}`,
  };
}
function extractWasmFuncName(err) {
  const stack = err?.stack ?? String(err);
  const atMatch = stack.match(/at\s+(\w[\w$]*)\s+\(wasm:\/\//);
  if (atMatch) return atMatch[1];
  const fnMatch = stack.match(/function\s+#\d+:"([^"]+)"/);
  if (fnMatch) return fnMatch[1];
  return void 0;
}
function lookupSourceMapOffset(sourceMapJson, wasmOffset) {
  try {
    const sm = JSON.parse(sourceMapJson);
    const mappings = sm.mappings;
    if (!mappings) return void 0;
    const sources = sm.sources ?? [];
    const segments = mappings.split(",");
    let absWasmOffset = 0;
    let absSourceIdx = 0;
    let absLine = 0;
    let absCol = 0;
    let bestLine = -1;
    let bestCol = -1;
    let bestSource = "";
    let bestOffset = -1;
    for (const seg of segments) {
      if (!seg) continue;
      const values = decodeVLQSegment(seg);
      if (values.length < 4) continue;
      absWasmOffset += values[0];
      absSourceIdx += values[1];
      absLine += values[2];
      absCol += values[3];
      if (absWasmOffset <= wasmOffset) {
        bestLine = absLine;
        bestCol = absCol;
        bestSource = sources[absSourceIdx] ?? "";
        bestOffset = absWasmOffset;
      } else {
        break;
      }
    }
    if (bestLine >= 0) {
      return { line: bestLine, column: bestCol, source: bestSource };
    }
  } catch {}
  return void 0;
}
function decodeVLQSegment(segment) {
  const BASE64 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
  const values = [];
  let i = 0;
  while (i < segment.length) {
    let vlq = 0;
    let shift = 0;
    let continuation = true;
    while (continuation && i < segment.length) {
      const digit = BASE64.indexOf(segment[i]);
      if (digit === -1) break;
      vlq |= (digit & 31) << shift;
      continuation = (digit & 32) !== 0;
      shift += 5;
      i++;
    }
    const isNeg = (vlq & 1) === 1;
    values.push(isNeg ? -(vlq >>> 1) : vlq >>> 1);
  }
  return values;
}
function enrichErrorMessage(errMsg, err, sourceMapJson, bodyLineOffset) {
  const parts = [errMsg];
  const funcName = extractWasmFuncName(err);
  if (funcName) {
    parts.push(`in ${funcName}()`);
  }
  if (sourceMapJson) {
    const stack = err?.stack ?? "";
    const offsetMatch = stack.match(/:0x([0-9a-fA-F]+)/);
    if (offsetMatch) {
      const byteOffset = parseInt(offsetMatch[1], 16);
      const mapped = lookupSourceMapOffset(sourceMapJson, byteOffset);
      if (mapped && mapped.line > 0) {
        const adjustedLine = mapped.line - bodyLineOffset;
        const srcLine = adjustedLine > 0 ? adjustedLine : mapped.line;
        parts.push(`at source L${srcLine}`);
      }
    }
  }
  return parts.join(" ");
}
async function runTest262File(filePath, category, timeoutMs = TEST_TIMEOUT_MS) {
  const totalStart = performance.now();
  const relPath = relative(TEST262_ROOT, filePath);
  const source = readFileSync(filePath, "utf-8");
  const meta = parseMeta(source);
  if (filePath) {
    const relTest = filePath.replace(/.*test262\//, "");
    if (HANGING_TESTS.has(relTest)) {
      return {
        file: relPath,
        category,
        status: "skip",
        reason: "compiler hang (see HANGING_TESTS)",
      };
    }
  }
  if (
    meta.negative &&
    (meta.negative.phase === "parse" || meta.negative.phase === "early" || meta.negative.phase === "resolution")
  ) {
    const negResult = await handleNegativeTest(source, meta, relPath, category);
    if (negResult) return negResult;
  }
  const isRuntimeNegative = meta.negative?.phase === "runtime";
  if (!isRuntimeNegative) {
    const filter = shouldSkip(source, meta, filePath);
    if (filter.skip) {
      return { file: relPath, category, status: "skip", reason: filter.reason };
    }
  }
  const { source: wrappedSource, bodyLineOffset } = wrapTest(source, meta);
  const wrappedLines = wrappedSource.split("\n");
  const originalLines = source.split("\n");
  function adjustLine(line) {
    if (line < 1 || line > wrappedLines.length) return line;
    const errorText = wrappedLines[line - 1].trim();
    if (
      !errorText ||
      errorText === "{" ||
      errorText === "}" ||
      errorText === "try {" ||
      errorText === "} catch (e) {"
    ) {
      const adjusted2 = line - bodyLineOffset;
      return adjusted2 > 0 ? adjusted2 : line;
    }
    for (let i = 0; i < originalLines.length; i++) {
      if (originalLines[i].trim() === errorText) return i + 1;
    }
    for (let i = 0; i < originalLines.length; i++) {
      if (originalLines[i].includes(errorText)) return i + 1;
    }
    const adjusted = line - bodyLineOffset;
    return adjusted > 0 ? adjusted : line;
  }
  let result;
  const compileStart = performance.now();
  let compileMs = 0;
  try {
    result = await compile(wrappedSource, {
      fileName: "test.ts",
      sourceMap: true,
      emitWat: false,
    });
    compileMs = performance.now() - compileStart;
    if (compileMs > 3e4) {
      const totalMs = performance.now() - totalStart;
      const timing = {
        totalMs: round2(totalMs),
        compileMs: round2(compileMs),
        instantiateMs: 0,
        executeMs: 0,
      };
      return {
        file: relPath,
        category,
        status: "compile_error",
        error: `compilation timeout (${round2(compileMs)}ms)`,
        timing,
      };
    }
  } catch (compileErr) {
    compileMs = performance.now() - compileStart;
    const totalMs = performance.now() - totalStart;
    const timing = {
      totalMs: round2(totalMs),
      compileMs: round2(compileMs),
      instantiateMs: 0,
      executeMs: 0,
    };
    if (isRuntimeNegative) {
      const errMsg = compileErr.message ?? String(compileErr);
      if (meta.negative.type === "ReferenceError" && errMsg.includes("before initialization")) {
        return { file: relPath, category, status: "pass", timing };
      }
      return {
        file: relPath,
        category,
        status: "compile_error",
        error: errMsg,
        timing,
      };
    }
    return {
      file: relPath,
      category,
      status: "compile_error",
      error: compileErr.message ?? String(compileErr),
      timing,
    };
  }
  if (!result.success || result.errors.some((e) => e.severity === "error")) {
    const totalMs = performance.now() - totalStart;
    const timing = {
      totalMs: round2(totalMs),
      compileMs: round2(compileMs),
      instantiateMs: 0,
      executeMs: 0,
    };
    if (isRuntimeNegative) {
      const errMsgs = result.errors.filter((e) => e.severity === "error").map((e) => e.message);
      if (meta.negative.type === "ReferenceError" && errMsgs.some((m) => m.includes("before initialization"))) {
        return { file: relPath, category, status: "pass", timing };
      }
      return {
        file: relPath,
        category,
        status: "compile_error",
        error:
          result.errors
            .filter((e) => e.severity === "error")
            .map((e) => `L${adjustLine(e.line)}:${e.column} ${e.message}`)
            .join("; ") || result.errors.map((e) => `L${adjustLine(e.line)}:${e.column} ${e.message}`).join("; "),
        timing,
      };
    }
    return {
      file: relPath,
      category,
      status: "compile_error",
      error:
        result.errors
          .filter((e) => e.severity === "error")
          .map((e) => `L${adjustLine(e.line)}:${e.column} ${e.message}`)
          .join("; ") || result.errors.map((e) => `L${adjustLine(e.line)}:${e.column} ${e.message}`).join("; "),
      timing,
    };
  }
  if (isRuntimeNegative && result.errors.some((e) => e.severity === "warning")) {
    const totalMs = performance.now() - totalStart;
    return {
      file: relPath,
      category,
      status: "pass",
      timing: {
        totalMs: round2(totalMs),
        compileMs: round2(compileMs),
        instantiateMs: 0,
        executeMs: 0,
      },
    };
  }
  let instantiateMs = 0;
  let executeMs = 0;
  try {
    const importResult = buildImports(result.imports, void 0, result.stringPool);
    const imports = importResult;
    const instantiateStart = performance.now();
    const { instance } = await WebAssembly.instantiate(result.binary, imports);
    instantiateMs = performance.now() - instantiateStart;
    importResult.setInstance?.(instance);
    const testFn = instance.exports.test;
    if (typeof testFn !== "function") {
      const totalMs2 = performance.now() - totalStart;
      return {
        file: relPath,
        category,
        status: "compile_error",
        error: "no test export",
        timing: {
          totalMs: round2(totalMs2),
          compileMs: round2(compileMs),
          instantiateMs: round2(instantiateMs),
          executeMs: 0,
        },
      };
    }
    const executeStart = performance.now();
    let ret2 = testFn();
    executeMs = performance.now() - executeStart;
    const asyncResultFn = instance.exports.__asyncResult;
    if (ret2 === 1 && typeof asyncResultFn === "function") {
      await new Promise((resolve) => setTimeout(resolve, 0));
      try {
        ret2 = asyncResultFn();
      } catch {}
    }
    const totalMs = performance.now() - totalStart;
    const timing = {
      totalMs: round2(totalMs),
      compileMs: round2(compileMs),
      instantiateMs: round2(instantiateMs),
      executeMs: round2(executeMs),
    };
    if (isRuntimeNegative) {
      return {
        file: relPath,
        category,
        status: "fail",
        error: `expected runtime ${meta.negative.type} but execution succeeded`,
        timing,
      };
    }
    if (ret2 === 1 || ret2 === 1) {
      return { file: relPath, category, status: "pass", timing };
    }
    let assertCtx = "";
    if (typeof ret2 === "number" && ret2 >= 2) {
      const assertIdx = ret2 - 1;
      const assertRegex = /\bassert\b[.\w]*\s*\(/g;
      let nth = 0;
      let m;
      while ((m = assertRegex.exec(source)) !== null) {
        nth++;
        if (nth === assertIdx) {
          const lineStart = source.lastIndexOf("\n", m.index) + 1;
          const lineEnd = source.indexOf("\n", m.index);
          const line = source.slice(lineStart, lineEnd === -1 ? void 0 : lineEnd).trim();
          assertCtx = ` | assert #${assertIdx}: ${line.slice(0, 160)}`;
          break;
        }
      }
      if (!assertCtx) {
        assertCtx = ` | assert #${assertIdx} of ${nth} total`;
      }
    } else if (ret2 === -1) {
      assertCtx = " | uncaught exception";
    }
    return {
      file: relPath,
      category,
      status: "fail",
      error: `returned ${ret2}${assertCtx}`,
      timing,
    };
  } catch (err) {
    const totalMs = performance.now() - totalStart;
    const timing = {
      totalMs: round2(totalMs),
      compileMs: round2(compileMs),
      instantiateMs: round2(instantiateMs),
      executeMs: round2(executeMs),
    };
    if (isRuntimeNegative) {
      return { file: relPath, category, status: "pass", timing };
    }
    if (err instanceof WebAssembly.CompileError || err?.constructor?.name === "CompileError") {
      return {
        file: relPath,
        category,
        status: "compile_error",
        error: enrichErrorMessage(err.message, err, result.sourceMap, bodyLineOffset),
        timing,
      };
    }
    if (err?.message?.includes("unreachable") || err?.message?.includes("wasm")) {
      return {
        file: relPath,
        category,
        status: "fail",
        error: enrichErrorMessage(err.message, err, result.sourceMap, bodyLineOffset),
        timing,
      };
    }
    return {
      file: relPath,
      category,
      status: "fail",
      error: enrichErrorMessage(String(err), err, result.sourceMap, bodyLineOffset),
      timing,
    };
  }
}
function round2(n) {
  return Math.round(n * 100) / 100;
}
function classifyError(errorMsg) {
  if (!errorMsg) return void 0;
  if (/dereferencing a null/i.test(errorMsg)) return "null_deref";
  if (/illegal cast/i.test(errorMsg)) return "illegal_cast";
  if (/out of bounds/i.test(errorMsg)) return "oob";
  if (/unreachable/i.test(errorMsg)) return "unreachable";
  if (/^TypeError\b|TypeError \(null\/undefined/i.test(errorMsg)) return "type_error";
  if (/^RangeError\b|Maximum call stack/i.test(errorMsg)) return "range_error";
  if (/^SyntaxError\b/i.test(errorMsg)) return "syntax_error";
  if (/^Promise\b|promise/i.test(errorMsg)) return "promise_error";
  if (/^returned -1\b/.test(errorMsg)) return "exception_in_test";
  if (/^returned \d+/.test(errorMsg)) return "assertion_fail";
  if (/Compiling function|No dependency provided|not a function/i.test(errorMsg)) return "wasm_compile";
  if (/expected .+ but compiled/i.test(errorMsg)) return "negative_test_fail";
  if (/expected runtime .+ but succeeded/i.test(errorMsg)) return "negative_test_fail";
  if (/no test export/i.test(errorMsg)) return "wasm_compile";
  if (/Cannot |Invalid /i.test(errorMsg)) return "runtime_error";
  return "other";
}
export {
  TEST_CATEGORIES,
  classifyError,
  classifyTestScope,
  enrichErrorMessage,
  extractWasmFuncName,
  findTestFiles,
  handleNegativeTest,
  lookupSourceMapOffset,
  parseMeta,
  runTest262File,
  shouldSkip,
  wrapTest,
};
